# E-8 lands · E-14 the diagonal sort · E-15 the determinacy question — 2026-08-02

Rail at the post-rename baseline; nothing deposits.

## (1) E-8 LANDS

`composition-barrier` merged to lv main (fast-forward): **lv main = `14720d9`**, pushed, local =
remote. Post-merge full build **green (3412 jobs)**; axiom re-print verbatim — all five barrier
terminals ("does not depend on any axioms") at the landed main. The Gate-4 precedent's conditions
were met on the face: validated by discrimination, axiom-free, checkpoint clean.

**The keystone's single touch → v0.5** (one touch, two folds, as ruled): the Correspondence gains
the barrier row at the landed pin (`CompositionBarrier.composition_barrier`;
`barrier_witnesses` — axiom-free, model-level anti-overclaim on its face, Face-E's sibling); and
the **Forward is rewritten** to carry the reality-vs-bound note's three-level form (RH is a
bound · Q is the two-variable form · positivity-delivers-the-bound · ζ owns the one-variable half
free), **the wall's three certifications** (METHOD — the toolkit barrier; DATA — the composition
barrier; SUPPLY — the five-row pattern), and the ladder + determinacy up-mountain pointers.

## (2) E-14 — THE DIAGONAL SORT

**Registered (verbatim):** *"simplicity sorts as the pair register's diagonal-limit, NOT as arity
three."*

**The classical anchors, at cite.** Montgomery (1973): the pair-correlation method yields
simplicity-class statements — at least a positive proportion of zeros simple unconditionally via
that circle of ideas, and under the pair-correlation conjecture almost all zeros simple: the
simplicity fractions are **outputs of the TWO-point function**. The multiplicity of a zero enters
the explicit formula through ζ′(ρ)-weighted terms — a double zero is the **coincidence of a pair**
(ρ = ρ′), i.e. mass on the diagonal of the two-point structure. The k = 3 slot's honest occupant:
**n-level correlations (Rudnick–Sarnak) — density register at every n**, statistics of located
zeros, no placement content at any level.

**The arc's simplicity cluster, re-read against the arity + boundary structure — the sorted
ladder:**

| rung | register | what lives there |
|:--|:--|:--|
| k = 1 (single-index) | density; edge-reaching | Λ(n) ≥ 0, Φ > 0, the counting laws; compiled edge lemmas |
| k = 2, interior (off-diagonal pair) | **placement — the wall** | Q, Weil positivity, `h2`; the supplier table's target |
| k = 2, **diagonal edge** | **simplicity** | double zero = pair coincidence (ρ = ρ′); Montgomery's simplicity fractions; the geometric clause (uniform transversality Im ξ′(ρ) ≠ 0) = uniformity ALONG the diagonal; the derivative-wall's raw-infinitude escape (the escape-kind invariant) = the diagonal's uniformity escaping any single-object carrier; GUE/codimension arguments = density-register evidence for the diagonal edge |
| k = 3 and above | density at all n | n-level statistics (Rudnick–Sarnak); no known placement content |

**Verdict: the registered expectation CONFIRMED — simplicity is the pair register's
diagonal-limit, not arity three.** The k = 3 suspect from E-8's original scope line is CLEARED:
above k = 2 the ladder carries only density-register statistics so far. The corpus's own
two-wall structure lands cleanly on the sort: the sign wall = the pair interior; the geometric
clause = the pair diagonal — same register, two boundary strata, matching their distinct
escape-kinds (the compiled invariant). No genuine third-variable content in simplicity was found;
the surprise branch did not fire.

## (3) E-15 — THE DETERMINACY QUESTION (filed rr ~1.5; the bounded face run)

**The mapping table (moment-problem object ↔ arc object; each row graded):**

| moment-problem object | arc object | grade |
|:--|:--|:--|
| the moment sequence m_n | the one-variable prime-side data (the Λ-ledger sums; the λ_n diagonal; Φ's moments — BUILD-2's meter literally computes this sequence) | DICTIONARY-with-instance (the meter is the arc's moment sequence, calibrated) |
| the measure μ | the spectral measure of the pair form Q / the zero measure | DICTIONARY |
| Hamburger/Stieltjes **indeterminacy** (moments need not determine the measure) | one-variable data underdetermines the two-variable form — **the landed E-8 barrier** | each a THEOREM in its own setting; the identification a READING (E-8 is the arc's compiled indeterminacy instance at rank 2) |
| **Carleman's condition** (Σ m₂ₙ^{−1/2n} = ∞ ⟹ determinacy) | the restoration shape: a growth condition on prime-side one-variable data under which the pair form IS determined — **the arc's version, typed as the open question** | OPEN (the full E-15 run) |
| moment determinacy ⟷ essential self-adjointness of the Jacobi operator (Nelson/classical) | the reality-vs-bound split: the operator half free, the bound half the wall | READING — the moment world states the operator/determinacy link as a theorem; the arc's split is its shadow |

**Registered expectation for the full run (verbatim):** *"the condition lands EQUIVALENT to the
polarization (the wall re-named in determinacy language; the toolkit import is the yield) rather
than weaker; the weaker branch flagged as the route-class surprise."* The bounded face files the
table and the registration; the full run stands priced at ~1.5 research-reach. The yield either
way: the moment-problem toolkit (Carleman, Krein, Nevanlinna parametrization) imported into the
arc's vocabulary with each tool's grade known in advance.

## The slate, re-printed

E-1 RUN+v2 · E-2 armed · E-3 standing (rr) · E-4 armed · E-5 armed · E-6 RULED-IN · E-7a RUN /
E-7b standing · **E-8 RUN → LANDED (`14720d9`); the k-ladder above it now SORTED by E-14** ·
E-9 RUN · E-10 RUN · E-11 armed · E-12 priced-on-motivation · E-13 COMPLETE · **E-14 RUN —
confirmed: simplicity = the pair diagonal; k = 3 cleared** · **E-15 FILED (rr ~1.5; bounded face
done — the mapping table)** · carryovers standing.

## Pin table

| repo | pin |
|:--|:--|
| SIDE-lv-conservation | main = `14720d9` (E-8 landed; build 3412 green; five terminals axiom-free) |
| PLACE-papers | `4e61b89` (keystone v0.5: barrier row + Forward fold; REGISTRY) |
| SIDE-kernel | `44895f9`; v1.7 = `2957e7d` — unmoved |
| relay | this report's commit |
| rail | post-rename baseline held |

Mirror rebuilt at the papers pin; nothing deposits. **The board, the sorted ladder at its head:**
k = 1 compiled edge · k = 2 interior = the wall (three certifications) · k = 2 diagonal =
simplicity (the geometric clause's true address) · k ≥ 3 = density only. The determinacy
question (E-15 full) and the Type-II supplier theorem (E-3) are the sharpest armed research-reach
items; everything else at your pick.
