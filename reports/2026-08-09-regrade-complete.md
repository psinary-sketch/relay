# THE WAVE-WIDE CORRESPONDENCE RE-GRADE — COMPLETE — 2026-08-09

**109 of 109 Correspondence rows statement-read at their pins.** Slate item 1 — the hard blocker on
pass 2 — is **discharged**. Rail `de621b1` / `2147a03` **unmoved, both clean**. **Nothing deposits.**

---

## §1 — THE DENOMINATOR, CORRECTED BEFORE THE TOTALS

**The read-versus-span rule applies to my own headline number.** The corpus carries **109
Correspondence rows** and **107 distinct terminals**. The "99" used through sittings 1–5 was an
early distinct-terminal count taken **before** the four renamed papers and the branch-pinned
terminals resolved. **The true denominator is 109, and all 109 are read.**

Per-paper counts were also stated before grading each block, and **three of the ferry's figures were
high**: SIMPLICITY 17 ✓, GRH_CASCADE 9 ✓, **FOUNDATIONS 8 (not 9)**, **ADDMULT 7 (not 8)** — 41 rows
this block, **not 26**; INDEX_ARITY **11 (not 18)** last block.

---

## §2 — BLOCK 6: ALL 41 CONFIRMED

**The corpus is at its most careful exactly where it is most exposed.**

| paper | what the rows do |
|:--|:--|
| **GRH_CASCADE** | grades two rows **"SHELL — work-order (W-5 family). PULLED as a citation per author ruling"** — the third grade used correctly, and self-caught |
| **FOUNDATIONS** | grades three silence rows **ENCODES-CONCLUSION**, quoting the Boolean model's definitional assignment (`isSilent I := I.kappa_x…`) |
| three papers | flag `[Fintype Domain]` as **"never used — exhaustiveness decorative, per loom"** |
| **ADDMULT** L391 | *"INTERFACES — a valid logic lemma **whose entire weight is its hypothesis**"* |
| **SIMPLICITY** L433 | *"structural core; the analytic identification of the five contributions is **manuscript-resident**"* — over `curveDim − obstrCodim < 0 := by omega` |

### The check that mattered most

`AddMult.twin_primes_infinite`, `.goldbach`, `.sophie_germain_infinite` are **the actual statements
of those conjectures, with NO hypotheses in their signatures**:

```lean
theorem twin_primes_infinite : ¬(∃ N : ℕ, ∀ p > N, ¬(Nat.Prime p ∧ Nat.Prime (p + 2)))
theorem goldbach : ∀ n : ℕ, n ≥ 4 → (∃ k, n = 2 * k) → ∃ p q, Nat.Prime p ∧ Nat.Prime q ∧ p + q = n
theorem sophie_germain_infinite : ¬(∃ N : ℕ, ∀ p > N, ¬(Nat.Prime p ∧ Nat.Prime (2 * p + 1)))
```

**Read at source: all three are `sorry`, each at a clearly-marked analytic boundary** —
*"MILESTONE: Hardy-Littlewood asymptotic"*, *"circle method or Helfgott-style approach"*, *"sieve
density bounds"* — and the file's own header reads **"3 theorems, 3 sorrys, 0 axioms."** The rows
grade them **"Open — the conjecture's analytic content."**

> **Kernel and row agree. The three most dangerous statements in the corpus are marked open on both
> surfaces, with the missing analytic tool named at each boundary. A reader looking for an overclaim
> would look here first, and would not find one.**

---

## §3 — THE THREE-TIER DISTINCTION FILED

> **CONSILIENCE (paths agree) < CATALOGUE CLOSURE (no other paths — proved, machine-checked) <
> CLAUSE DISCHARGE (the one door's lock — `h2`, OPEN).**

**The proof that tier 2 does not reach tier 3 is the corpus's own witness: the Epstein object.** Disc
−23 has a **full enumerable specification** — functional equation, archimedean factor, theta
transformation, order, entirety — and **off-line zeros** (ρ ≈ 0.9533 + 16.290i, ρ ≈ 0.798 +
29.5518i, doubly-sourced in-corpus). **An object can honour the whole catalogue and violate the
sign.** What it lacks is the Euler product, which is why the remaining door is **multiplicative and
singular**.

**No grade moves on this filing.** Every row already reading *"the reduction, not a proof of RH;
`h2` carried open"* was at tier 2 and saying so.

---

## §4 — THE FINAL DELTA TABLE

| outcome | count | detail |
|:--|--:|:--|
| **CONFIRMED at grade** | **103** | the row's sentence matches its terminal's statement at its pin |
| **RE-CELLED** | **6** | PATHS L271 · L273 · L276 · L281 · SURROUND L170 · L171 — stipulation printed, clauses counted |
| **CORRECTED** | **4** | PATHS L274 · PATHS L316 · REGISTRY 1.5a-5 *(one error, three sites)* · SIMPLICITY L446 |
| **PORTED** | **1** | FANO `## Reaches`, 8 rail-only lines |
| **STRUCK** | **0** | |
| **HELD-AT-RAIL** | **0** | the one authorization sought was **returned unused** |
| **WORK-ORDERS with triggers** | **5** | rowgen `if`-defenc · rowgen dotted-names · prose-outruns-row (2 cohorts) · manuscript-reflection-derivations · per-paper reconciliation ×12 |

**Tooling errors of my own, caught and recorded rather than reported as findings: 75 false "defects"
across three resolver generations** (working-tree walk · HEAD-stamped config · mispaired repo pins),
plus a dotted-name lookup miss and a truncated-identifier miss. **The IMPOSED-PRESENT loom line
exists because of them.**

---

## §5 — THE GROUND STATEMENT PASS 2 INHERITS

**ESTABLISHED.** Every cited terminal **exists at a pin its own paper offers** — 0 missing, 0 stale,
under a resolver that reads the paper's pins rather than HEAD. **103 of 109 rows confirmed exactly
as written**, including the register pentagon's five modus-ponens edges (INTERFACES, bridges named,
one **DISCLAIMED**), the C₇ stand-in (**ENCODES-CONCLUSION**, definitional assignment quoted), two
**SHELL**-and-pulled rows, and the three open conjectures.

**CORRECTED.** Four factual errors, three of them one error at three sites (*"equivalence compiled"*
for a single implication), plus a **DERIVES** on a conditional terminal that README and two other
papers already read as INTERFACES. Six rows re-celled to print a modelling stipulation — **and the
convergent-identification ruling reframes those six: a row modelling a free-layer fact is the
architecture, not a defect; the counted shortfall is distance to the manuscript leg.** One profile
cell repaired that gave an axiom profile for a **namespace**, which cannot have one.

**NOT EVALUATED — and this must not be read as blessed.** **(1) The manuscript leg**: every
"stipulation, printed" names mathematics living in a chapter, and **no chapter was read**;
`W-ORD-MANUSCRIPT-REFLECTION-DERIVATIONS` asks the deciding question and is **priced, not run**.
**(2) The rounded-profile check** across all terminals — only three had `#print axioms` re-run at
pin. **(3) The prose surfaces** — two cohorts still outrun their corrected rows, trigger-gated.
**(4) `h2` itself**, untouched.

> **THE ONE-LINE READING: the compiled record says what it says it says. What it says is a
> reduction, not a proof — and the re-grade did not test the leg where the proof is claimed to
> live.**

---

## §6 — HOUSEKEEPING

**A commit-message defect, recorded rather than force-pushed away:** the block-6 commit message lost
the word `` `sorry` `` to shell command substitution (backticks inside an unquoted `printf`),
leaving *"all three are ⟨gap⟩ at a clearly-marked analytic boundary"*. **The repository files are
correct and the full statement is in `OPEN_TRAILS` §H-COMPLETE and above. A published commit is not
rewritten for a message typo.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `483bd33` |
| relay | → this report's commit |
| **rail `de621b1` / `2147a03`** | **UNMOVED, both clean throughout six blocks** |

**PASS 2 OPENS ONLY ON THE AUTHOR'S WORD. Nothing deposits.**
