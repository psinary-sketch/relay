# D-2a — THE LEAD LAW, compiled — 2026-08-05

The kernel leg opens. Pins at open: PLACE-papers = `e1972e9`; relay = current; lv main =
`14720d9`; kernel `44895f9` — unmoved. Nothing deposits.

## THE REGISTERED EXPECTATION (VERBATIM, recorded BEFORE compiling)

**What the statement WILL say:** for a Duursma zeta coefficient sequence of a SELF-DUAL code
— i.e. one satisfying the self-dual functional equation p(2g−i) = q^(g−i)·p(i) — the leading
coefficient of the Galois-normalized certificate is the SQUARE of the zeta polynomial's
constant coefficient: **lead(H) = p₀²**. The normalization step (lead(h) = p(2g)/q^g) is
derived from the h-construction, not assumed; the functional equation is the hypothesis.

**What the statement will NOT say, and must not:** it will not mention ζ, RH, or `h2`; it
will not assert that any code's zeta polynomial exists or that Duursma-RH holds anywhere; it
will not prove Duursma's P(0) formula (p₀ = A_d/((q−1)·C(n,d))) — that is a cited datum, and
the theorem is stated so that the formula is not needed for the conclusion. **It is a
statement about coefficient sequences with a functional equation, and nothing more.**

**What is stipulated at cite vs derived in-kernel:** STIPULATED — Duursma's construction of
P from the weight enumerator, his P(0) formula, and the self-dual functional equation
(Duursma 2001/2003; the DH stipulated-datum precedent). DERIVED IN-KERNEL — that the
h-family's leading coefficient is the top normalized coefficient, and that the FE forces it
to equal p₀, hence lead(H) = p₀².

**The salt-check to be run before it counts as DERIVED:** does the statement encode its own
conclusion? Is any definition doing the theorem's work?

## THE COMPILATION

### COMPILE-PENDING
