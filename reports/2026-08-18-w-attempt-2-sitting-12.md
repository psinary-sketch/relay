# `W-ATTEMPT-2` · SITTING 12 — DOES WEIL'S FAMILY REACH EXACTNESS
## ### **`W-EXACT` AT EVERY `a`: THE DEFICIT DECAYS THROUGH THE GATED RANGE TO THE DOUBLE-PRECISION FLOOR — THE ARCHIMEDEAN INTERLEAVE IS EXACT IN THE LIMIT OF WEIL'S FAMILY AT THE MODEL, THE INEXACTNESS WAS FINITE-DEGREE — WITH THE `∞`-WEIGHT'S HS CONVENTION FAILING AS ITS OWN REGISTRATION PREDICTED, AND THE `ℚ₃` LEVEL-4 CELL CLOSED AT ZERO MISMATCH ON ALL 41 MILLION ENTRIES**

**Relay report · 2026-08-18 (thirty-second sitting) · author-called · attempt work RELAY-ONLY ·
nothing deposits**

> ### **THE CORRECTED STOP AND THE PROTOCOL CORRECTION, carried. THE FERRY'S CORRECTION ON THE FACE,
> carried into the packet: "0.872 is the archimedean deficit" was an over-read of a model number —
> RETIRED; the window-independent facts are the residual (sitting 11) and, now, its finite-degree
> nature (this sitting). No sign at complete roster; the register untouched.**

```
PLACE-papers  origin/main : b7acc3e (UNTOUCHED this sitting)
relay         origin/main : (this report, the sitting-12 batch)          local +1 HELD
instruments   b27 (degree sweep; 91/91 checks) · b28 (HS convention; 5/5) · b29 (the n = 4 cell;
              19/19 EXACT) — registrations banked BEFORE every run; all six data files 0 non-ASCII
```

> ### **RULE-3 LOG:** *head cites banked at `afb8cfd` ✓. The OBJECT DISTINCTION registered before any
> number: sitting 11 measured multiplication-window compressions `G F G`; this sitting measures the
> compression of `F` to Weil's test-class SUBSPACE (the generalized eigenproblem the ferry named,
> solved by QR + SVD, no coefficient search) — the two `σ_max`'s are different objects and the run
> prints the non-conflation line (including for the numerical near-coincidence `0.871896` at
> `(√2, d = 8)`, ungated, moving with `N` — explicitly NOT the banked 0.87182). Terms coined: none.*

---

## §1 — ITEM 1: ### **THE DEGREE SWEEP — `(W-exact)` AT EVERY `a`**

*The registered density longhand (the family's Gaussian weight is determinate — Hermite completeness —
so the continuum shape is `σ_max(d) → 1`) is what the model confirms, with the rate now measured:*

1. ### **AT `a = 2` AND `a = 3` THE DEFICIT `1 − σ_max(d)` DECAYS WITH ACCELERATING RATIOS THROUGH THE
   WHOLE GATED RANGE AND REACHES THE DOUBLE-PRECISION FLOOR** *(a = 3: `1.8×10⁻⁷` at `d = 18` down to
   the `10⁻¹⁵` floor by `d = 34`; successive-ratio spread 43× — accelerating, not geometric-constant;
   no stall anywhere, so `(W-floor)` never fired and the candidate list was deliberately NOT exercised
   — comparing constants to a non-floor would be the over-read the ferry retired).*
2. ### **THE SECONDARY FULL-DEGREE SWEEP (odd `u`-powers in) SHARPENS, NOT CHANGES, THE READING:** *the
   deficit reaches float-zero by `d = 10–22` at every `a` — the odd powers make the approach visible at
   small `a`, where the even-only family is slow (`√2` even-only: gated band ends at `d = 6` with
   deficit `0.14` — a WEAK `(W-exact)`, read from direction not arrival, flagged prominently in-run).*
3. ### **THE SCOPE, SAID WITH THE VERDICT: the archimedean interleave is exact IN THE LIMIT of Weil's
   family AT THE MODEL (centered-DFT, calibrated windows) — the real place's inexactness was
   FINITE-DEGREE.** *Joined to sittings 6/10/11: the real place refuses hard truncation (mathematics),
   carries a stable deficit at any FIXED calibrated window (model number), and loses the deficit
   entirely along Weil's own family — inexactness at `∞` is a property of stopping, not of the place.*
4. **Instrument notes, disclosed:** *the N-stability gate cut at the BOTTOM (low degrees are the
   grid-sensitive numbers; only `√2` also cut at the top, the registered direction) · QR rank
   truncations recorded as data · no non-monotonicity anywhere ·* ### **and a precision note carried
   back to b25: its unreduced FFT phase argument costs `~5×10⁻¹²` at `N = 16383` — harmless at b25's
   5-digit level, fixed by exact modular reduction in b27, and recorded so no future 6-digit gate
   trips on it.**

## §2 — ITEM 2: ### **THE HS CONVENTION — `(H-fails)`, BY ITS OWN REGISTERED DERIVATION**

*`Q_HS = h·‖GDG‖_F` measured at `N ∈ {1023, 4095, 16383}`: growth `~N^{0.25–0.28}` (the registered
`δ′`-kernel longhand predicted `~N^{1/4}`: `D` is first-order, `GDG` is not Hilbert–Schmidt in the
continuum, and no `h`-weighting of the grid sum can converge); zero stable digits.* ### **The failure
is the CONVENTION'S CATEGORY, not the model's: the finite-place law is built on the GROUP ELEMENT (a
bounded unitary `U_p`), the `∞` channel has been using the GENERATOR. FILED with the second candidate
NAMED: the bounded-dilation convention `U_λ` (mirroring the finite places exactly), whose windowed HS
norm converges by the composition-kernel computation — its grid implementation (off-grid points,
band-limited interpolation) the declared question for the sitting that runs it.** *The ledger keeps
its caveat; the raw-channel exponent `~N^{0.77}` was independently reproduced in passing.*

## §3 — ITEM 3: ### **THE `ℚ₃` LEVEL-4 CELL — `(closed)`: THE DECLARED SKIP IS RETIRED**

### **HOST GRAM `= 9·`LEVEL GRAM ON THE NOSE — ZERO MISMATCH ON ALL `6400² = 40,960,000` ENTRIES,
EXACT, in 153 s against the 1800 s budget set before running.** *The route was cross-validated before
use: entry-by-entry agreement with b26's own path at `n = 1–3`; two NEGATIVE CONTROLS (a wrong mass
constant; a corrupted column) both flagged by both routes at identical address sets — the test is not
vacuous and locates breaks; the Gram's symmetry was verified but NOT used to halve the work — the full
square was computed. And b26's honesty is validated in passing: the original projection (591 s) matches
the measured full-route rate (632 s) — the declared skip at a 420 s budget was correct, not an excuse.*
### **The `p = 3` tower's `T2` is now exact at ALL levels 1–4.**

## CLOSING — RELAY-ONLY, FOR THE AUTHOR'S WORD

1. ### **WEIL'S FAMILY REACHES EXACTNESS AT THE MODEL — the deficit was finite-degree. The `∞` place's
   remaining honest gaps are two, both named: the weight channel's CONVENTION (the bounded-dilation
   candidate, filed with its derivation) and everything `n → ∞`/closure-level, named not constructed.**
2. ### **THE FINITE SIDE IS NOW EXACT EVERYWHERE IT HAS BEEN TOUCHED: both towers, all levels run, all
   laws verbatim, zero mismatches — 19/19, 160/160, 203/203, 215/215, 139/139, 52/52, 55/55.**
3. **For your word:** *(i)* the bounded-dilation `∞`-weight sitting; *(ii)* whether the attempt now
   returns to review (the packet is current through sitting 12) or continues testing; *(iii)* the
   standing HELD/counsel items, unchanged.

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO SIGN AT COMPLETE ROSTER. NOTHING DEPOSITS.**
