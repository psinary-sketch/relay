# THE CRITICAL PATH + THREE WONDERS — ### **THE LAW IS A SAWTOOTH, AND IT IS DERIVED**

**Relay report · 2026-08-17 · author-called · relay-only for bench · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : cfd87871f2d55c09b5edaa26608e2faec161071c   VERIFIED (ls-remote)
SIDE-window   origin/main : 7e7743dfe240448e33672499f56c1c5c794fe65e   VERIFIED · tag v0.3.0
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ cfd8787   ### 22 / 22 COHERENT, three ways
SIDE-effects  afa9ccf — untouched by this pass
```

> ### **THE SITTING'S HEADLINE, AND IT IS A CORRECTION TO THIS MORNING'S HEADLINE:** *the `δ/L` law is not
> a two-branch minimum. It is a* ### **SAWTOOTH**, *the two branches are its first two teeth, and the
> whole thing is an elementary theorem about path graphs.*

---

## §1 — LEMMA 5.2 ON THE WIDE RANGE · ### **VERDICT: REGIME-FREE, AT A SCOPE STATED AND NARROWER THAN THE DEBT**

### **WHAT IS AND IS NOT IN HAND — SAID FIRST, BECAUSE IT BOUNDS EVERYTHING BELOW**

**On disk at content:** the supplied `(85)`, `(100)`, `ζ_n`, `ξ_n^an`, Lemma F.1's truncation sentence.
### **NOT ON DISK: CC's STATEMENT OF LEMMA 5.2, AND CC's PROOF OF IT.** *The corpus holds two banked lines
from the 2026-08-13 supply —* `"Prop 5.5 | hypothesis I ⊂ [−log 2, log 2] of length ≤ log 2"` *and*
`"Lemma 5.2 | derived for ρ ∈ (1,2]"` *— plus sitting 7's identification of the debt:* **"the `(Qε)`
series' validity on `(1,3]` is exactly Lemma 5.2's un-re-derived extension."**

> ### **SO THIS ACT DID NOT AUDIT CC's PROOF. IT CANNOT — THE PROOF IS NOT HERE.** *It re-derived from the
> supplied formulas, a different act at a different scope, and the verdict is stated at that scope and no
> wider.* **Kin, binding: *a retrieval gloss is not the source* — the Apostol rider was reported blocked
> rather than filled in from recall.**

### **THE PAPER STEPS, DONE BEFORE ANY NUMBER**

**(1) THE VANISHING STEP IS EXACT AND REGIME-FREE.** *`supp ζ_n ⊆ [1,∞)` by its own definition;
`θ(ρ⁻¹)ξ_n(u) = ρ^{−1/2}ξ_n(ρu)` is supported where `ρu ∈ [0,1]`, i.e. on `[0, 1/ρ]`.* ### **For every
`ρ ≥ 1` those supports meet in at most one point, so `⟨ζ_n | θ(ρ⁻¹)ξ_n⟩ = 0` for ALL `ρ ≥ 1`.**

**(2) THE FORMULA CARRIES NO `ρ`-RESTRICTION.** *Ingredients declared entire; the integral over `[1/ρ, 1]`,
bounded and inside `(0,1]` for every `ρ ≥ 1`; boundary terms evaluating entire functions at finite points.*
### **AND THE OVERLAP SHRINKS AS `ρ` GROWS — the opposite of an obstruction.**

**(3) ### THE SERIES CARRIES NO EIGENVALUE-DECAY FACTOR AT ALL.** *Write `A_n = λ_n ξ_n^an` and
`D_n = λ_n (ξ_n^an)′`. Every term of `C_n` is a product of exactly TWO of them, so `C_n = Ĉ_n/λ_n²` and*

> ### **`[λ_n²/(1−λ_n²)]·C_n = Ĉ_n/(1−λ_n²)`**

*— the prefactor that looks like it supplies convergence is consumed by the two analytic continuations.*
**Convergence rests entirely on `Ĉ_n(ρ)`, which is built from the prolate functions' own transforms and
carries no `λ` at all.** ### **Verified against the banked `Qε` at `10⁻¹⁴` relative, at ten values of `ρ`
— a blocking row, passed before anything else was read.**

### **THE MEASUREMENT, AND THE ONE PLACE IT NEARLY FOOLED ME**

*`|Ĉ_n|` falls monotonically by eleven orders through `n ≈ 7` at every `ρ ∈ [1.5, 20]`, then meets a floor.
Read naively, the "terms needed for `10⁻¹¹`" grows —* `7` *at* `ρ = 1.5`*,* `14` *at* `ρ = 3`*, "`> 48`" at*
`ρ = 6, 8`. ### **THAT WOULD HAVE BEEN A REGIME-BOUND VERDICT, AND IT WOULD HAVE BEEN WRONG.**

**An addendum was registered and run before the verdict was written:** refine `NQ = 500 → 1400` and
`NG = 300 → 1000`.

| | `ρ = 3` | `ρ = 8` | `ρ = 20` |
|:--|--:|--:|--:|
| plateau at `NQ = 500` | `2.6×10⁻¹²` | `1.1×10⁻⁹` | `5.2×10⁻⁹` |
| plateau at `NQ = 1400` | `2.1×10⁻¹²` | `5.3×10⁻¹⁰` | `1.5×10⁻⁹` |
| terms `n ≤ 6`, across all four settings | ### **stable to 2 figures** | ### **stable** | ### **stable** |

> ### **THE TAIL MOVES WHEN THE INSTRUMENT IS REFINED. IT IS THE INSTRUMENT.** *The genuine terms are
> exhausted by `n ≈ 7` at every `ρ` tested; what grows with `ρ` is the quadrature's accuracy floor, because
> resolving `cos(2πtρ)` costs nodes.*
>
> ### **THE STANDING LAW THIS OBEYED: a false negative in a verification instrument is worse than no
> instrument, since it invites a repair of something that is not broken.** *"The series needs 48 terms at
> `ρ = 8`" would have been exactly that, and the check that caught it cost one extra run.*

**(4) NO KINK.** *Apparent one-sided slope differences of `Qε` at `ρ = 2, 3, 4` are `8.44 / 3.16 / 0.713`;
`Qε″·3h` at those points is `8.44 / 3.16 / 0.713`.* ### **Curvature, to three figures.** *Weak evidence and
labelled weak: smoothness of a truncation cannot certify the series it truncates.*

### **VERDICT — ### REGIME-FREE, and what that does and does not buy**

> ### **REGIME-FREE for the `(Qε)` SERIES REPRESENTATION AS SUPPLIED, at bench grade, on `ρ ≤ 20`.**
> *Neither the formula nor the series inherits a `ρ ≤ 2` bound from anything visible in the supplied
> material.* **That is exactly the question sitting 7 named as the debt.**
>
> ### **WHAT DOES NOT CLEAR, NAMED RATHER THAN LEFT IMPLICIT:** *(a) Lemma 5.2 as CC states it — not held,
> not audited · (b) Prop 5.5's length hypothesis on the INTERVAL, a different object · (c)* ### **Lemma
> F.1's "uniform to `10⁻¹¹`" cannot be VERIFIED past `ρ ≈ 2.5` on this quadrature, its own floor exceeding
> `10⁻¹¹` there** *— a statement about my instrument, filed as one.*
>
> ### **THE DEBT NARROWS. IT DOES NOT CLEAR, AND ITS TRIGGER IS UNCHANGED.**

---

## §2 — WONDER ONE · ### **THE CONJECTURE IS DERIVED, AND IT IS FALSE AS STATED**

**The ferry's statement:** *"for the lag form `(η⋆η*)(ℓ)` on a window of length `Λ ≥ 2ℓ`, the number of
negative directions on `V` is a fixed function of `ℓ/ω`, independent of `Λ`."*

### **THE DERIVATION — EXACT, ELEMENTARY, COMPLETE FOR THE LAG FORM**

*The lag term on the grid is a constant times `(S_k)_{ij} = 1` iff `|i − j| = k`.*

| step | content |
|:--|:--|
| **1** | `i ~ j` only when `i = j ± k`, so `{0,…,M−1}` splits into the `k` residue classes mod `k` and `S_k` acts on each as the adjacency matrix of a ### **PATH GRAPH**. With `M = qk + s`: `s` chains of length `q+1`, `k − s` of length `q`. |
| **2** | `P_m`'s spectrum is `2cos(πj/(m+1))`, `j = 1..m` — ### **symmetric about `0`, nullity `1` iff `m` is ODD.** |
| **3** | `#positive = #negative`, hence ### **`NPOS(S_k) = (M − nullity)/2`**, `nullity = s` if `q` EVEN, `k − s` if `q` ODD. |
| **4** | `q=1 → M−k` · `q=2 → k` · `q=3 → M−2k` · `q=4 → 2k` · generally `q=2m−1 → M−mk`, `q=2m → mk`. |

> ### **THE ROOM BRANCH AND THE LAG BRANCH ARE THE `q = 1` AND `q = 2` CASES OF ONE THEOREM.** *Both were
> fitted to a table this morning; both are derived now.*
>
> ### **IN THE CONTINUUM, `r = log L / log 2`: A SAWTOOTH.** *Peaks at exactly `½` at `r = 2m`, i.e.*
> ### **`L = 4, 16, 64, …`** *· troughs at `m/(2m+1)`, i.e.* ### **`L = 8, 32, 128, …`** *with values*
> `⅓, ⅖, 3/7, …`
>
> ### **`min(k, M−k)` IS EXACTLY RIGHT FOR `k ≤ M < 3k` AND EXACTLY WRONG BEYOND.** *Every `L` in this
> morning's sweep had `r < 3` (`L = 7.0` gives `r = 2.81`).*

### **THE TEST — PREDICTIONS COMPUTED AND BANKED BEFORE ANY CELL WAS RUN** *(`relay` `d81264e`)*

| `L` | `r` | measured | ### **sawtooth** | ~~min-law~~ | meas − sawtooth | meas − min |
|:--|--:|:--|--:|--:|--:|--:|
| **8.0** | 3.00 | `348/1039 · 694/2078` | `347 · 693` | `347 · 693` | `+1 · +1` | *(non-discriminating)* |
| **9.0** | 3.17 | `406/1098 · 812/2196 · 1623/4393` | `405 · 811 · 1622` | `347 · 693 · 1386` | `+1 · +1 · +1` | `+59 · +119 · +237` |
| **12.0** | 3.58 | `549/1241 · 1100/2484` | `548 · 1099` | `347 · 693` | `+1 · +1` | `+202 · +407` |
| **16.0** | 4.00 | `692/1385 · 1386/2772` | `692 · 1386` | `347 · 693` | ### **`+0 · +0`** | `+345 · +693` |
| **20.0** | 4.32 | `694/1497 · 1386/2995` | `694 · 1386` | `347 · 693` | ### **`+0 · +0`** | `+347 · +693` |

### **11 OF 11 CELLS TRACK THE DERIVATION WITHIN ONE OFFENDER, EXACT AT FOUR. NINE CELLS DISCRIMINATE;
NINE OF NINE TRACK THE SAWTOOTH. THE MINIMUM LAW IS WRONG BY UP TO `693` — a factor of two in the fraction.**

**And `L = 16` returns to `½` exactly** — `1386/2772` — ### **a second apex, where the minimum law predicts
`0.25`.** *The peak at `L = 4` was the first of infinitely many, not a unique event.*

### **THE CONJECTURE, ANSWERED: ### NO.**

> *Freezing holds on `2 ≤ r < 3` and fails immediately above, where the count is `M − 2k` and depends on
> `M` again.* ### **THE FREEZING IS A WINDOW PHENOMENON WITH A BEGINNING AND AN END, NOT A LAW.** *Its end
> is `r = 3`, i.e. `L = 8` — one step past where anybody had measured.*

### **WHAT IS NOT DERIVED, STATED AS SHARPLY AS WHAT IS**

*Steps 1–4 are about `S_k`. The measured object is `A = A_main + c(ω/2)S_k` on `V`, and* ### **`A_main` was
measured to have 2–3 POSITIVE EIGENVALUES on `V`** *— so it is not negative semidefinite, no Weyl bound
applies, and the one-offender agreement is* ### **observed, not proved.**

---

## §3 — WONDER TWO · ### **THE DERIVATION DID NOT CLOSE, AND THE GRADE RULE FIXED IN ADVANCE FIRES**

**The ferry's registered expectation LANDED:** varying every prime coefficient by `2.5×` on the lag branch
moves the count by ### **`−1, −2, −2` offenders at `L = 4.6, 5.5, 7.0`** *(dimensions `1525`–`1945`)* —
inside the registered `≤ 2`.

### **HALF THE DERIVATION IS A THEOREM AND THE HALF IS WORTH KEEPING**

> *For `t > 0`, `tS` is CONGRUENT to `S` (`tS = (√t I) S (√t I)^T`).* ### **BY SYLVESTER'S LAW OF INERTIA,
> MULTIPLYING EVERY COEFFICIENT BY A COMMON FACTOR CANNOT MOVE THE COUNT AT ALL — exactly, for every factor
> and every window.** *So the uniform direction is silent by theorem, and only the non-lag part can break it.*

### **THE OTHER HALF NEEDED ONE CHECKABLE FACT, AND THE FACT IS FALSE**

*If `A_main` were negative semidefinite, Weyl would bound the count above by the pure-lag count and give a
finite lock-on threshold above which coefficient-independence is rigorous.* **That was registered as the
blocking row.**

| `L` | `M` | ### **`NPOS(A_main)` on `V`** | `λ_max` | `λ_min` |
|:--|--:|--:|--:|--:|
| 4.6 | 1526 | ### **2** | `+8.25×10⁻⁴` | `−5.15×10⁻⁴` |
| 5.5 | 1705 | ### **3** | `+9.55×10⁻⁴` | `−5.26×10⁻⁴` |
| 7.0 | 1946 | ### **3** | `+1.12×10⁻³` | `−5.37×10⁻⁴` |

### **`A_main` IS NOT NEGATIVE SEMIDEFINITE. THE WEYL ROUTE DOES NOT APPLY.**

*And the exactness the theorem would give also fails, by exactly as much as `A_main` is not negligible:
uniform rescaling by `0.4×` / `2.5×` moved the count by* ### **`1, 2, 2` offenders where the pure lag form
requires `0`.**

> ### **VERDICT: STAYS `MEASURED-AT-BANK`. THE FOURTH SELF-LIMIT IS NOT FILED.** *A grade rule written
> before the run decided this, not a judgement made after seeing the numbers.*
>
> **What IS reportable, measured:** *sweeping a common factor over five decades, the count locks onto the
> pure-lag value for every `t ≥ 1` and departs below `t ≈ 0.1` (`−45, −13, −3, 0, 0, 0` at
> `t = 10⁻³ … 10²`).* ### **The working coefficient scale sits inside the locked region — which is why the
> silence is observed, and is not why it is true.**

---

## §4 — WONDER THREE · ### **`SIDE-window` `v0.3.0` = `7e7743d`, PUBLIC, TAGGED**

**Twelve new terminals, every one axiom-free, library at ### 32.**

*For each prime `p`, `p²` plays two roles: the one-prime-`p` branch turn (`lag = room` when
`log L = 2 log p`) and the second rung of the `p`-power ladder.* ### **They name the same number, and this
file compiles the arithmetic half.**

| terminal | statement |
|:--|:--|
| `turn_is_square` | `p ^ 2 = p * p` — ### **the only statement holding for every `p`** |
| `apex_2 … apex_13` | `pPowersLT p (p²) = [p]` and `pPowersLE p (p²) = [p, p²]`, for `p = 2, 3, 5, 7, 11, 13` |
| `apex_counts` · `apex_is_the_entry_point` | one rung below the turn, two at or below; the count increments exactly as the window passes `p²` |
| ### **`apex_ladder_is_not_window`** | ### **`pPowersLT 2 4 = [2]` but `primePowersLT 4 = [2,3]`, AND THE TWO ARE UNEQUAL** |
| `apex_window_is_larger` | at `9` and `25` the full window holds `6` and `13` prime powers against the ladder's `1` |

**Non-claims, in the docstring and the README so they travel with the source:** *Role 1 is a statement
about real logarithms and this library has no reals — not formalized · nothing analytic is proved and
nothing here is evidence for the measured turn · the general `p` is proved for ONE fact only, everything
else being `decide` on a concrete `p` · the ladder is not the window, stated* ### **as a theorem.**

### **EXECUTOR DISCLOSURE — THE AXIOM PRINT CAUGHT A TACTIC ON ITS FIRST RUN**

> `turn_is_square` was first written `by rw [Nat.pow_succ, Nat.pow_one]`. **That compiles. It is correct.
> And it costs `propext`** — `rw` goes through `Eq.mpr`. ### **ONE TACTIC WOULD HAVE ENDED THIS
> REPOSITORY'S HEADLINE PROPERTY, which is that nothing in it depends on any axiom at all.**
>
> *Caught by `AxiomCheckApex.lean` on its first run and replaced by the term-mode
> `congrArg (fun x => x * p) (Nat.one_mul p)`, which is axiom-free.* **Kin: the `v0.1` tabulation error
> caught by `decide` at build time.** ### **Both times the instrument this repository exists to run is what
> found it.**

**Pre-push identifier sweep** over every tracked file for carrier-spec / TECHNE / patent terms:
### **ZERO HITS → eligible for PUBLIC.**

---

## §5 — WHAT LANDED IN THE CORPUS

**`PLACE-papers` `cfd8787`:**

* ### **CORRECTION NINETEEN** — the sawtooth, with its four-step derivation, its eleven measured cells, and
  the freezing conjecture answered NO. **Correction 18's `min(…)` struck as a complete statement and
  retained as the `q = 1, 2` teeth.** Heading re-counted by script: **13 table rows + 6 sections = NINETEEN.**
* **`§6`** — the Lemma 5.2 re-derivation, its five findings, and the verdict at its stated scope, with what
  does *not* clear named in three parts.
* **Meaning line (i) UPGRADED:** *the `½` ceiling now follows from the sawtooth and is touched at every even
  tooth, not once.* **Meaning line (ii) NOT upgraded** — see `§3`.
* **`THE_EULER_SPECIFICATION` `§14.4`** — the `S5` address/weight derivation attempted and not closed;
  Sylvester keeps the uniform half; `A_main`'s definiteness fails and is reported.
* **`FINDINGS` `F.2026-08-17b`**, with the superseding pointer on `F.2026-08-17`.
* **`REGISTRY` + `SPIRAL_MAP`** — `SIDE-window` `v0.3.0` = `7e7743d`, 32 terminals, all axiom-free, with
  the term-mode note carried.

**The mirror**, rebuilt at `cfd8787`: **1,399,149 bytes · 23 entries · roster 22, unchanged since
2026-08-06 · ### 22/22 coherent three ways** (manifest md5 vs disk vs committed blob), with six content
probes taken as exact strings from the artifacts.

---

## §6 — THE METHOD LINE, RECORDED BECAUSE IT IS UNCOMFORTABLE

> ### **THIS RECORD MADE THE SAME MISTAKE TWICE IN ONE DAY, AT TWO SCALES.** *This morning it read the room
> branch as the whole law; this afternoon it read two teeth as the whole law.* **Both times the range
> measured was inherited from a design rather than chosen to discriminate. Both times the repair came from
> measuring where nobody had looked.**
>
> ### **A LAW READ OFF A RANGE IS A LAW ABOUT THAT RANGE** — and the cheap defence is not more precision
> inside the range, it is one cell outside it.

*Second, smaller, and in the other direction:* ### **the Lemma 5.2 probe would have returned a
REGIME-BOUND verdict from its own noise floor** *if the refinement addendum had not been run.* **A tail
that moves when the instrument is refined is the instrument, and asking that question cost one run.**

---

## §7 — WHAT THIS SITTING DOES NOT SAY

* ### **NOTHING ABOUT THE SIGN OF `W_∞ − W_2`. NOTHING ABOUT `h2`. NO OPERATOR INEQUALITY.**
* **The sawtooth is a theorem about `S_k` and a measurement about `Φ`.** *`A_main` is uncontrolled and has
  positive directions; the one-offender agreement is observed.*
* **The Lemma 5.2 verdict is about the `(Qε)` series as supplied**, not about CC's lemma, which is not held.
* ### **NOTHING ABOVE BENCH GRADE ANYWHERE** — the `(1,3]`→wide re-derivation narrowed but did not clear,
  and its trigger stands.
* **The kernel compiles arithmetic.** *It is not evidence for the analytic turn and says so in three places.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **LEMMA 5.2: REGIME-FREE for the `(Qε)` series as supplied, on `ρ ≤ 20`, at bench grade** — with
   CC's own lemma untouched because it is not on disk, and Lemma F.1's constant unverifiable past `ρ ≈ 2.5`
   on this quadrature. **The debt narrows; it does not clear.**
2. ### **WONDER ONE: DERIVED, AND THE CONJECTURE IS FALSE AS STATED.** The count-freezing holds only on
   `2 ≤ r < 3`. Both branches are the `q = 1, 2` cases of `NPOS = (M − nullity)/2`. **11 of 11 cells, nine
   of nine discriminating.**
3. ### **WONDER TWO: NOT DERIVED. The self-limit is NOT filed.** Sylvester keeps the uniform half as a
   theorem; `A_main` has 2–3 positive eigenvalues and the Weyl route fails. **The ferry's registered
   expectation landed; the explanation did not.**
4. ### **WONDER THREE: `v0.3.0` LIVE AND PUBLIC**, 12 terminals, 32 total, all axiom-free — with a `propext`
   leak caught by the axiom print on its first run.
5. ### **CORRECTION NINETEEN LANDED**, superseding this morning's correction eighteen within the day.

### **WHAT WANTS YOUR RULING**

1. ### **THE SAWTOOTH IS DERIVED FOR THE LAG FORM AND MEASURED FOR THE OPERATOR.** *Closing the gap means
   controlling `A_main` — which is now known to have `2–3` positive directions out of `~1,700`.* **That is
   a bounded, nameable next item, and it is not opened here.**
2. **The `(1,3]` debt's trigger** stands unchanged; the narrowing is recorded, not spent.
3. **`L = 32` and `L = 64`** would test the third apex and the third trough. *The derivation predicts `⅖`
   and `½`; the sitting did not run them.*

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — **committed at `relay` local tip, UNPUSHED, absence
from the public tree re-verified against the remote after this pass's push.** *Release condition unchanged:
counsel's answer, then your word.*

**`h2` UNCHANGED. NO SIGN. `WIDEN` PAUSED. RAIL DID NOT MOVE. NOTHING DEPOSITS.**
