# `W-CONSTRUCTION-1` · ACT 5 — THE TRACE COMPUTED, THE FIVE SORRIES ATTACKED
## ### **THE DISTRIBUTIONAL TRACE OF THE COMPRESSED SCALING: THE FINITE PLACES TRACE-SILENT OFF THE IDENTITY (PROOF GRADE, VANILLA-LEG CELLS), THE GLOBAL TRACE `A`-CHANNEL-ONLY, THE TWO-CHANNEL ANATOMY BANKED — THE SORRY LEDGER 5 → 2, FILE A COMPLETE**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 5 · sub-gate declared · Rules 4/5 · the
vocabulary rulings on the face (the classical profile spelled out; each absence in its own terms) ·
nothing deposits · nothing circulates**

> ### **RULE-3 LOG:** *act 4 at `684dc18` ✓; `THE_GLOBAL_SECTION.md` `v0.4` ✓; the banned grep on
> this ferry: CLEAN. One reading FLAGGED, not smoothed: act 4's `Θ_T` named the characteristic
> function; the act-5 order computes the DISTRIBUTIONAL TRACE under that name — linked (boundary
> values ↔ the a.c. density the trace sees), executed as the trace, said so. Terms coined: none.*

```
build document  THE_GLOBAL_SECTION.md v0.5 (§12: the trace; the ledger's movement)
vanilla leg     tools/lean/TraceSilence.lean + AxiomCheckTS — 6/6 "does not depend on any
                axioms" (v4.29.1 pinned); the relay's zero-axiom count: 68
mathlib leg     PadicStandardAddChar.lean COMPLETE (0 sorries; target PROVED; profile
                {propext, Classical.choice, Quot.sound}, no sorryAx, all three prints);
                LocalLimit + GlobalSection recompiled, 0 errors; THE LEDGER: 5 → 2
```

---

## §1 — ITEM 1: THE TRACE, COMPUTED TO ITS GRADE

1. ### **PER-PLACE, FINITE (proof grade): EXACT SILENCE.** *`Tr(U_{p^k}S) = 0` exactly for every
   `k ≥ 1`, both primes, level-stable — the fixed-point localization (`α(p^k−1) ≡ 0 mod p^n` never
   solvable in range), compiled cell-by-cell on the VANILLA leg this act. The identity coefficient
   is the dimension `(p^n−1)²` (divergent in the limit): the per-place trace is `(dim)·δ₀`.* ###
   **The branch "the trace reproduces the prime term": REFUTED, exactly. The prime terms are
   QUOTIENT-channel terms — the fifth law's Frobenius mass is nonzero while the trace is zero, and
   the orbit-quotient trace carries the Weil `q^{−1/2}` shape (banked).**
2. ### **GLOBAL ASSEMBLY: the trace is `A`-CHANNEL-ONLY.** *Stated with the level-normalized trace
   (a CHOICE, on the face — nothing else converges): every nontrivial finite dilation meets a zero
   factor; the global distributional trace reduces to the archimedean flow. On the constrained
   class: `Θ(g) = Tr_∞(ϑ(g_∞)S_∞)` — CC's `ε`-form AT CONTENT (the banked prolate layer:
   `ε′(1⁺) = Σt(n) = 22.9965`, 15/15); the `ε`-derivation on OUR model is a NAMED OPEN LEMMA.*
3. ### **THE COMPARISON `Θ(g)` vs `Σ_ρĝ(ρ) − ‖image(g)‖²`: STATED, NOT DECIDED — the
   `(L-identity)`, which lands only through the closure protocol's four steps in order.** What the
   act banks instead is structural: ### **THE TWO-CHANNEL ANATOMY — any `(L-identity)` must combine
   trace + quotient; the explicit formula's `A − PR` split is REPRODUCED as the object's
   trace/quotient anatomy.**

## §2 — ITEM 2: THE FIVE SORRIES, IN ORDER OF OWNERSHIP

| debt | before | after | what happened |
|:--|:--|:--|:--|
| file A: the spec | sorry | ### **PROVED** | `toZModPow`'s kernel + `norm_le_pow_iff_mem_span_pow`; the membership extracted as a named lemma so definition and theorems share one proof term |
| file A: additivity | sorry | ### **PROVED** | the defect `d = frac(x+y) − frac(x) − frac(y)` has p-power denominator; `‖d‖_p ≤ 1` (three specs + ultrametric) forces `p^K ∣` numerator (the INTEGRALITY BRIDGE, its own theorem) ⟹ `d ∈ ℤ` ⟹ `Circle.exp_eq_exp` |
| file A: the conductor | sorry | ### **PROVED — THE FILE'S TARGET** | forward from `frac = 0`; backward: `exp` injectivity mod ℤ + `0 ≤ frac < 1` ⟹ `frac = 0` ⟹ the spec gives `‖x‖ ≤ 1` |
| B–C: the L² realization | sorry | **STANDS** | the owed list SHRINKS (the character is now proof-grade); remaining: the transform on `LocallyConstant` + tower-Plancherel — spelled at the site |
| infrastructure: Hilbert `⊗′` | sorry | **STANDS** | the construction path spelled at the site (colimit of finite tensors along `E₁`-unit inclusions + Hilbert completion; Mathlib holds neither step); a SORRY, said so |

**Terminal prints, per the vocabulary ruling:** *file A's three prints each `[propext,
Classical.choice, Quot.sound]` — the classical profile, no `sorryAx`; the two standing debts print
`sorryAx` in their lists, visibly; the vanilla leg's six new theorems "does not depend on any
axioms". The relay's zero-axiom total: 68.*

## §3 — ITEM 3: THE RECORD

*`THE_GLOBAL_SECTION.md` `v0.5` (§12, the ledger table, the profile spelled on the face);
correspondence rows 87–90 with leg and profile per row; `F.2026-08-18o`; this report the act's
bank.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED — THE COMPARISON
STATED AND NOT DECIDED. NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING CIRCULATES.**
