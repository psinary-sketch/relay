# THE CONSTRUCTION ERA OPENS — `v0.3.0`, `Δ4`, AND `W(ℤ)` GRADED — 2026-08-10

**`SIDE-carrier-spec` tag `v0.3.0` = `89e574d`, head `0a5b159`, PRIVATE, peel-verified.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — `Δ4`, THE ONE FIELD ADDED

**The stopping rule's condition is met: a candidate forced the refinement.** `(R4)` asks that the
action be by endomorphisms; the second grading showed both canonical objects satisfy it
**vacuously**. **`Δ4` is the field that tells the two apart.**

**The criterion:** prime-indexed ring endomorphisms, `ψ_p ψ_q = ψ_{pq}`, with
**`ψ_p(x) ≡ x^p (mod p)`** — plus an **`equivariant`** field binding the operators to the
specification's own action, **so a candidate cannot pass by bolting an unrelated ring onto
itself.** *That is the `finPart` hole, closed at the point of writing rather than found later.*

> ### **`Δ4` IS NOT A NEW CRITERION. IT IS BORGER'S Λ-STRUCTURE DEFINITION, VERBATIM:**
>
> *"a **commuting family of endomorphisms `ψ_p : X → X`, indexed by the set of prime numbers `p`,
> such that each `ψ_p` agrees with the `p`-th power Frobenius map on the special fiber**"*
> — `arXiv:0906.3146`, read at cite, stated twice.
>
> **The corpus imported the field's own criterion and made it checkable. The contribution is the
> CHECKING — kernel docstring, README and filings all say so.**

**Compiled:** `powerMap_fails_frobenius` — `ψ_p(x) = p·x` at `x = 1` asks `p ∣ (p − 1)`, false for
every `p ≥ 2`, so **the frame's `(R4)`-passing action is EMPTY at `Δ4`** · `int_frobenius`
(Fermat, non-vacuity) · **`witness_arith_and_still_degenerate`** — `Δ4` demands a **congruence**
and mentions no order, sign or positivity, and the witness satisfies the sharpened specification
while separating nothing. **Three sharpenings now; the same degenerate witness passes every one.**

**Twenty-four terminals, none outside the clean profile.**

---

## §2 — `W(ℤ)` GRADED *(read at cite; classical facts only, none re-derived)*

| field | verdict |
|:--|:--|
| `cancel` / `neg` | **PASS** — a commutative ring |
| **`arith_action` (Δ4)** | **PASS** — the big-Witt Λ-structure. **The only object yet graded to pass it** |
| `arch_present` / `arch_independent` | **FAIL** — *"nothing about the archimedean place of ℚ"*, his own sentence |
| `components_faithful` | **NOT STATEABLE** — no archimedean component to state it against. **Recorded as such, not as a pass** |
| `pair` | **FAIL** |

**GRADE: EXTERNAL-AT-CITE.** Nothing about `W(ℤ)` is compiled here, and the row says so rather
than borrowing the kernel's authority.

---

## §3 — THE JUNCTION *(`F.2026-08-10-l`, compiled-fields grade)*

| object | `Δ1` | `Δ4` | `Δ2` |
|:--|:--:|:--:|:--:|
| **the adelic frame** | **✓ compiled** | **✗ compiled** | ✗ |
| **`W(ℤ)`** | **✗ at cite** | **✓ at cite** | ✗ |

> ### **TWO FOREIGN OBJECTS, COMPLEMENTARY FAILURES AT NAMED FIELDS.**

**THE NESTED-CANDIDATE CAVEAT, DISCHARGED BY RUNNING THE PRESCRIBED CHECK.** The law's signature
is *"one candidate failing a strict superset of the other's fields"*: the frame fails `{Δ4, Δ2}`,
`W(ℤ)` fails `{Δ1, Δ2}` — **neither contains the other, so the signature returns NEGATIVE.**
Unlike the ladder, which *was* the frame's product-formula locus, **`W(ℤ)` is not a sub-object,
quotient or linear complement of the adelic frame.** **Two independent tests; the artifact does
not repeat.**

### THE ERA'S STANDING QUESTION *(question grade; stated once; no route claimed)*

> ### **WHAT OBJECT CARRIES BOTH — FROBENIUS-LIFT CONGRUENCES AND AN INDEPENDENT ARCHIMEDEAN COORDINATE — WITH A PAIRING TARGET?**

**The known negative space, quoted beside it so it is not read as a programme:** Borger's
*"further ideas"*, **which named nothing** · the **enrich-don't-strip** law — each school holds
what the other lacks · the **toric-terminus** question, unanswered.

> **Three independent records say the junction is where the field stops. None says it is
> reachable, and this report does not either.**

---

## §4 — STOP-AND-HOLD, SHARPENED FOR THE ERA

> **Any reading or compilation that begins to CONSTRUCT the junction object stops at the
> observation, states at grade, and holds for the author. The era advances by ruled objects, not
> by momentum.**

**Nothing this pass constructed anything.** `Δ4` grades; `W(ℤ)` is graded; the junction is a table
of two rows. **`Δ2` is absent from both objects and from every object the survey placed, and `h2`
is exactly where it was.**

**Also corrected:** the README's profile list carried **20 of the 24** terminals `AxiomCheck`
prints — the four adelic-frame terminals were never added. **Found by a mechanical name-by-name
reconciliation in both directions**, now empty both ways.

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `83425fb` |
| relay | this report's commit |
| **`SIDE-carrier-spec`** | **tag `v0.3.0` = `89e574d`** — `refs/tags/v0.3.0^{}` peel-verified = local; head `0a5b159`; PRIVATE |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
