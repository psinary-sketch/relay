# THE CARTANB VERIFY — THE STANDING BAR, NOT THE GREP BAR — BANKED, AND THE FOLD
## ### **FIVE TERMINALS AT EXACTLY `{propext, Classical.choice, Quot.sound}`, ZERO SORRY AT THE PIN — TWO PILLARS GRADE DERIVES, THE THREE CLASSIFICATION TERMINALS GRADE ENCODES-CONCLUSION (COUNT BY DECLARATION) — T3 / COUSIN-I IS THE MODULE'S OWN DOCUMENTED SET-ASIDE — ◆ UNREACHABLE AT ENCODES GRADE REGARDLESS OF SORRY-STATUS; THE 1.5c-11 FLAG RESOLVES TO THE FOUND STATE**

**Relay report · 2026-08-20 · advisory-executed then ferry-folded (both part 1 of 1, receipt
confirmed, Rule 1) · Rules 3/4/5 · verify: report-only at the advisory; fold: this ferry ·
no kernel touched · nothing deposits · nothing circulates beyond this report · `h2` UNCHANGED.**
Scope, once: bank the verify verbatim; fold 1.5c-11 to the found state with the flag resolved;
file the Nevanlinna lead and the loom line; place the paper tension on the author's desk with a
recommendation and NO edit to the paper; stop.

> ### **RULE-3 LOG:** *(a) the per-terminal axiom prints and grades — banked VERBATIM below from
> the verify session's report; the fold files from this banked copy. (b) the axiom profile is
> written out in full everywhere — `{propext, Classical.choice, Quot.sound}` — never
> abbreviated; the in-session report's two prose shorthand occurrences are expanded here to the
> full profile, disclosed, nothing else changed (the per-terminal prints were already verbatim
> and full). (c) the four fold components verified pairwise independent — confirmed. Banned-stem
> grep on this report: clean; terms coined: none.*

---

## §1 — THE VERIFY REPORT, BANKED VERBATIM

**Pin:** `SIDE-kernel` main @ `5e668b4` (working tree clean; post-v1.7, tag v1.7 = `2957e7d`
unmoved). Toolchain `leanprover/lean4:v4.29.0-rc8250`; Mathlib pinned @
`e960b84129b3caf423ecf0ea7409a8758a47012c`. **Build:** `lake build Bridge.CartanBBridge` —
green, 2663 jobs, exit 0.

**Per-terminal report** (axiom prints verbatim; no `sorryAx` appears in any print, so the
module is genuinely 0-sorry at the pin — the grep bar and the print bar agree here, but the
print is what is reported):

| terminal | axiom print (verbatim) | grade | statement paraphrase |
|:--|:--|:--|:--|
| `output_stage_card` | `depends on axioms: [propext, Classical.choice, Quot.sound]` | **ENCODES-CONCLUSION-or-SHELL** | The locally *declared* two-constructor inductive `OutputStageClass` has cardinality 2 — the "2" enters by declaration, not by derivation from function theory. |
| `elliptic_regularity_collapse` | `depends on axioms: [propext, Classical.choice, Quot.sound]` | **DERIVES** | An analytic function on an open set is C^∞ there — real Mathlib content (`AnalyticOn.contDiffOn` + `uniqueDiffOn`). The "collapses the local hierarchy to one class" reading lives in the docstring, not the statement. |
| `identity_theorem_unique_bridge` | `depends on axioms: [propext, Classical.choice, Quot.sound]` | **DERIVES** | Two analytic functions on a connected open set agreeing near a point agree on the set — Mathlib's identity theorem, composed through the `analyticOn ↔ analyticOnNhd` equivalence. The "unique local-to-global bridge, hence no third class" reading is interpretive, not stated. |
| `output_stage_exhaustiveness_proved` | `depends on axioms: [propext, Classical.choice, Quot.sound]` | **ENCODES-CONCLUSION-or-SHELL** | A local Prop structure bundling the declared count with the two derived pillar theorems under an exhaustiveness-named type; no Lean implication anywhere ties T1/T2 to "exactly two classes." The module says this itself: T3 is not a field, and the certificate is "unconditional on what it states." |
| `formation_n_3_eq_two` | `depends on axioms: [propext, Classical.choice, Quot.sound]` | **ENCODES-CONCLUSION-or-SHELL** | `n₃ := Fintype.card OutputStageClass` equals 2 — a definitional restatement of the declared count. |

All five prints are exactly `{propext, Classical.choice, Quot.sound}` — the Rule-5 Mathlib-leg
expected profile — with no leaked axioms and no sorries.

**The salt-check's load-bearing find (Section 2b, read at content):** the third pillar —
Cartan B / Cousin-I, "no intermediate cohomological scale" — is **explicitly NOT formalized and
deliberately SET ASIDE** (decision dated 2026-06-16 in the module: Mathlib carries the analysis
of meromorphic functions but not the constructive Mittag-Leffler existence theorem; a
conditional-hypothesis encoding was deliberately rejected as non-precedent; the leg is "an
explicit open external input... NOT relied upon by any kernel theorem"). The module also logs a
research lead: a possible in-stack *exclusion* route via Mathlib's Nevanlinna First Main
Theorem.

**Consequence for the 1.5c-11 re-sweep, per the advisory's bar:** **◆ is NOT reachable** — the
print is clean, but the headline classification terminals grade ENCODES, not DERIVES; the only
DERIVES terminals are the two supporting pillars, and the classification content itself rests
on the declared type plus the set-aside T3. **The composite mark keeps, with the grades
named:** the ◇-boundary leg is confirmed and sharpened to its exact address — T3/Cousin-I, a
classical formalization absence owned outside the corpus, documented by the module itself —
and the flag's open question is answered in substance: the ◇'s Lean referent is
`Bridge/CartanBBridge.lean`, which has **no sorry**, so the original bare-◇ ("Lean exists, has
sorry") was stale as to sorry-status; the true state is *"Lean exists, 0 sorry, exactly
`{propext, Classical.choice, Quot.sound}`, headline statements at ENCODES grade, one pillar set
aside."*

**One tension reported, not adjudicated:** the paper (`CARTAN_B.md` §8) states n₃ = 2 as
"**[PROVED]** — certified here," "fully proved within ZFC," via the three classical theorems —
while its kernel counterpart formalizes two of the three pillars, sets the third aside on its
face, and carries the count at ENCODES grade. The paper-vs-kernel gap is the author's to rule
at the fold.

## §2 — THE FOLD (this ferry's four components)

1. **This bank** — §1 above, verbatim; relay is public and the report carries verdict grade
   only (confirmed: prints, grades, and pointers; no raw construction content).
2. **REGISTRY 1.5c-11** — composite row updated to the found state, **flag RESOLVED** with the
   ferry's wording; one-line lesson appended: *bare ◇ was stale in both directions; the
   composite vocabulary plus the statement-read were jointly necessary.*
3. **OPEN_TRAILS** — the Nevanlinna lead staged as a priced question (author-scheduled
   trigger); the CARTAN_B paper tension added as a DESK ITEM, no action, with the navigator
   recommendation filed beside it (additive grade-clarification note + correspondence-row
   truth; the author rules the wording).
4. **VERIFICATION_LOOM** — one dated line: the salt-check's third declared-count catch
   (E-Difficulty shells · census verdicts · CartanBBridge n₃ = 2); the three-grade rubric
   confirmed as a working instrument, not a formality.

## §3 — PINS

*PLACE-papers (private): ledger commit read back in the pin line below. Relay (public):
pre-act `origin/main = 5a8931a`; this act's pin read back below. Mirror: refreshed at the new
PLACE-papers HEAD under the collision clause (`-c`, the third export of the date).*

> **PIN LINE (post-push read-back):** *appended by the pin commit after ls-remote verification.*

## FOOT — THE AUTHOR'S DESK, RESTATED, NO ACTION TAKEN

· **Ruling 1, currency:** the 103 zero-axiom terminals + the purity draft's 11 — federation
  pair or relay-resident.
· **Ruling 2, the two monograph sentences:** additions, never corrections; the §25.8 row
  downstream of ruling 1.
· ### **NEW — the CARTAN_B wording ruling:** the "[PROVED] — certified here" / "fully proved
  within ZFC" sentences against the kernel's two-pillars-plus-set-aside state; navigator
  recommendation: an ADDITIVE grade-clarification note in the paper + the correspondence row
  carrying the found state — additions, never corrections; the author rules the wording.
· **Standing:** the T7 close; the ◇-display conventions downstream of the composite
  vocabulary.

**NO KERNEL TOUCHED. `h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED.
NOTHING DEPOSITS. NOTHING CIRCULATES.**
