# EXPERIMENT ONE — THE TWO-PRIME ROOM · ### **E1 IS VACUOUS BY CONSTRUCTION, AND THE ONE-PRIME LAW IS THE THING THAT MOVED**

**Relay report · 2026-08-17 · author-called · relay-only for bench · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. `WIDEN` PAUSED.**

```
PLACE-papers  at session start : d41df34bbd9deaf21b9967c60da9e5007a0d2e29   VERIFIED (ls-remote)
PLACE-papers  origin/main NOW  : 729069714f48f66af334b0c0dc89ac17dc8e72ad   VERIFIED (ls-remote)
                                 = correction seventeen, the only PLACE-papers act of this pass
relay         at session start : 23dccd8c110f5e21b4d13f43849f41b659a10961   VERIFIED (ls-remote)
SIDE-window   origin/main : 612d223f0eea74e61a6f1a38133d2ee415ce01a8   VERIFIED · tag v0.2.0
SIDE-effects  origin/main : afa9ccfbddc21e5bbd5ee2a4cbdf23f6a88498dd   VERIFIED
relay         local  main : one commit ahead — the two HELD carrier-build acts, UNPUSHED
```

---

## §0 — THE ORIENTATION BLOCK

**Read at source, not from summary:** `VERIFICATION_LOOM` standing laws *(head of file + the 2026-08-10→14
tail, through correction 12's law)* · `OPEN_TRAILS` tail · `REGISTRY` · `FINDINGS` tail · the five relay
reports named by the ferry *(bank-the-day `aabd0fd` · four-rulings `1fb686e` · repairs-and-unblocked
`c19b946` · resurrection part 2 `e555de1` · the battery `23dccd8`)* · `README_prolate.md` · the four
instrument modules at content.

### **EVERY EXPECTED SHA MATCHES. NOTHING STOPPED.**

| repo | expected | read from the remote | |
|:--|:--|:--|:--|
| `PLACE-papers` | `d41df34` | `d41df34bbd9d…` | ### **MATCH** |
| `relay` | `23dccd8` + local `+1` HELD | `23dccd8c110f…`, local `+1` = `c3f387f` | ### **MATCH** |
| `SIDE-window` | `v0.2.0` | `612d223f0eea…`, tag present | ### **MATCH** |
| `SIDE-effects` | merged `afa9ccf` | `afa9ccfbddc2…` | ### **MATCH** |

### **ONE DISCREPANCY, AND IT IS NOT A SHA — REPORTED RATHER THAN SMOOTHED**

**The ferry asked for `relay/tools/e16` READMEs for `prolate_layer` / `qeps_layer` / `phi_layer` /
`battery_full`.** ### **ONLY `README_prolate.md` EXISTS.** *The other three modules carry their pins,
formulas and boundaries in module docstrings — read at content in place of the missing READMEs, and they are
complete enough that nothing was inferred — but* ### **the 2026-08-15 closing sentence "the instrument is
back and banked — `prolate_layer` · `qeps_layer` · `phi_layer` · `battery_full`" reads as though four
documented modules landed, and one is documented.** *Filed, not repaired: writing three READMEs is content,
and this ferry's content is the experiment.*

---

## §1 — THE PRE-REGISTRATION, PRINTED AND COMMITTED BEFORE ANY MEASURED NUMBER

**Banked at `relay` `06199aa` as its own commit, ahead of the run, with the full text at
`data/exp1_registration_2026-08-17.txt` and the instrument at `tools/e16/exp1_two_prime.py`.**

### **THE RULING APPLIED: `V` IS IMPOSED ON EVERY MEASUREMENT — COUNTS AND `C` ALIKE.**

*Grid dimension on `V` is `M − 1` where `M = round(log L / ω)`. Every number in this report is a
`V`-restricted count. The 2026-08-15 finding that the banked pipeline imposed `V` for `C` and not for the
counts is not repeated anywhere here.*

### **THE LAG SCHEDULE — AND A DISCREPANCY IN THE FERRY'S OWN PRE-REGISTRATION, NAMED NOT RESOLVED**

| `L` | active lags `{log q : q prime power < L}` | count |
|:--|:--|--:|
| 3.0 *(the baseline row)* | `log 2` | 1 |
| 3.2 · 3.6 | `log 2`, `log 3` | 2 |
| 4.2 · 4.6 | `log 2`, `log 3`, `2 log 2` | 3 |
| 5.5 · 7.0 | `log 2`, `log 3`, `2 log 2`, `log 5` | 4 |

> **The ferry defines the lag set as `{log q : q prime power < L}` and then says `E2` is *"first at
> `L = 4.2`"*.** ### **UNDER THE PRIME-POWER DEFINITION TWO LAGS ARE ALREADY LIVE AT `L = 3.2`.** *"First at
> `L = 4.2`" is the count under the SUPERSEDED ferry-literal rule (`log 2` always; `2 log 2` once `L > 4`) —
> the rule the 2026-08-14 sitting recorded as disagreeing with the arithmetic.* ### **THIS RUN USES THE
> PRIME-POWER RULE, which is the definition the ferry gives and the schedule the ferry calls corrected.**

### **`E1-UNION`, IN CLOSED FORM — AND THE CONSEQUENCE THAT HAD TO BE PRINTED FIRST**

**The rooms are nested initial segments of one window** — `room(ℓ) = 1 − ℓ/log L`, decreasing in `ℓ` — **and
the smallest active lag is always `log 2`.** The union of nested sets is the largest of them:

> ### **`E1-UNION(L) = 1 − log 2 / log L`, OVERLAPS COUNTED ONCE — WHICH IS IDENTICAL TO THE ONE-PRIME LAW
> AT EVERY `L` IN THE SWEEP.**

| `L` | 3.2 | 3.6 | 4.2 | 4.6 | 5.5 | 7.0 |
|:--|--:|--:|--:|--:|--:|--:|
| ### **`E1-UNION`** | `0.404078` | `0.458874` | `0.516999` | `0.545792` | `0.593402` | `0.643793` |

### **SO `E1` CANNOT BE CONFIRMED AND CANNOT BE REFUTED AS A GENERALIZATION. IT PREDICTS NOTHING THE
ONE-PRIME LAW DOES NOT ALREADY PREDICT, AT ANY `L`, EVER — AND A UNION OF NESTED ROOMS NEVER COULD.**
*Registered in the block, before the run, precisely so it could not be discovered afterwards and presented as
a result.* **The experiment's whole discriminating content is therefore `E2`.**

### **THE COEFFICIENTS, STATED — AND AN EXPOSURE REGISTERED IN ADVANCE**

*The supplied form carries one prime term,* `Φ(η) = ⟨ξ|N_I ξ⟩ + 2√2 log 2 · (η⋆η*)(log 2)`*, and*
`2√2 log 2 = 2√q·log p` *at* `q = 2`*. The ferry names the pattern* `√p · log p`*, so the registered
coefficient is* ### **`C(q = p^k) = 2√q · log p`**.

| `q` | `2` | `3` | `4` | `5` |
|:--|--:|--:|--:|--:|
| ### **registered** `2√q log p` | `1.960516` | `3.805705` | `2.772589` | `7.197625` |
| *sensitivity* `4 log p/√q` *(the Weil weight, matched at `q=2`)* | `1.960516` | `2.537136` | `1.386294` | `2.879050` |

> ### **THE REGISTERED COEFFICIENT GROWS WITH `q`; THE EXPLICIT FORMULA'S OWN WEIGHT DECAYS.** *Both agree at
> `q = 2`, the only place the corpus supplies a value, so* ### **nothing in the banked record separates
> them.** *Registered as a model-vs-true-functional exposure BEFORE the run, so that a departure could not
> afterwards be attributed to it.* **The sensitivity is reported beside the table, never inside it.**

### **BOTH BRANCHES, LONGHAND** *(the question-fit law)* — printed in the block and reproduced here in
summary: *if `E2` sits at the one-prime residual scale with no trend in the number of live lags, the extra
prime powers put no measurable weight on the negative fraction and the union reading is confirmed vacuous as
a predictor while correct as a prediction; if `E2` departs systematically or steps where a lag switches on,
the negative fraction is not a function of the smallest lag alone and the departure's profile is the
finding.* ### **Both sentences finish.**

---

## §2 — THE BLOCKING KNOWN-ANSWER ROW: ### **`L = 3` ON `V`, 3 / 3 EXACT**

*Put first and made blocking. A failure here would have been the act's result.*

| `ω` | `M` | measured on `V` | target *(2026-08-15, `V`-restricted)* | fraction | |
|:--|--:|:--|:--|--:|:--|
| `2×10⁻³` | 549 | `202 / 548` | `202 / 548` | `0.368613` | ### **PASS** |
| `1×10⁻³` | 1099 | `406 / 1098` | `406 / 1098` | `0.369763` | ### **PASS** |
| `5×10⁻⁴` | 2197 | `811 / 2196` | `811 / 2196` | `0.369308` | ### **PASS** |

**Predicted `1 − log2/log3 = 0.369070`; residuals `−0.000457 / +0.000693 / +0.000238`.**

### **AND THE BASIS CROSS-CHECK, BECAUSE THE FAST ROUTE IS NOT THE CORPUS'S ROUTE UNTIL IT IS SHOWN TO BE.**
*`phi_layer`'s `V` basis comes from an SVD of the projector; this instrument uses a Householder reflection,
`O(M²)` against `O(M³)`. At `ω = 2×10⁻³`:* ### **SVD basis → 202 offenders on 548 dims; Householder basis →
202 offenders on 548 dims. IDENTICAL.** *The inertia on `V` is basis-independent, and it was verified rather
than asserted.*

---

## §3 — THE TABLE THE FERRY ASKED FOR

**All on `V`. Registered coefficient. Three `ω` per row, never averaged.**

| `L` | lags | `E1` predicted | measured ×3 *(`ω = 2e-3 / 1e-3 / 5e-4`)* | residual *(at `5e-4`)* | verdict |
|:--|--:|--:|:--|--:|:--|
| **3.2** | 2 | `0.404078` | `0.404475 · 0.404475 · 0.404301` | `+0.000223` | ### **AT `E1`** |
| **3.6** | 2 | `0.458874` | `0.458529 · 0.459375 · 0.459196` | `+0.000322` | ### **AT `E1`** |
| **4.2** | 3 | `0.516999` | `0.500697 · 0.483264 · 0.500174` | `−0.016825` | ### **UNSTABLE IN `ω` — WITHHELD** |
| **4.6** | 3 | `0.545792` | `0.500000 · 0.468197 · 0.500164` | `−0.045628` | ### **UNSTABLE IN `ω` — WITHHELD** |
| **5.5** | 4 | `0.593402` | `0.499412 · 0.477113 · 0.500000` | `−0.093402` | ### **UNSTABLE IN `ω` — WITHHELD** |
| **7.0** | 4 | `0.643793` | `0.500000 · 0.475578 · 0.499100` | `−0.144692` | ### **UNSTABLE IN `ω` — WITHHELD** |

**Counts, for the record:** `235/581 · 470/1162 · 940/2325` · `293/639 · 588/1280 · 1176/2561` ·
`359/717 · 693/1434 · 1435/2869` · `381/762 · 714/1525 · 1526/3051` · `425/851 · 813/1704 · 1704/3408` ·
`486/972 · 925/1945 · 1942/3891`.

> ### **THE WITHHOLDING IS THE REGISTERED RULE FIRING, NOT A CHOICE MADE AFTER SEEING THE NUMBERS.** *The
> block states: "a row whose three fractions do not agree to the residual scale is reported UNSTABLE and its
> verdict withheld." Four rows do not agree, by up to `0.032` where the residual scale is `0.0005`.*
>
> ### **A DEPARTURE THAT MOVES WITH THE GRID IS A STATEMENT ABOUT THE GRID UNTIL IT IS SHOWN TO BE A
> STATEMENT ABOUT THE MECHANISM.** *So the rest of this report is that demonstration, and it does not end
> where it was pointed.*

### **THE MODEL-VS-TRUE-FUNCTIONAL NOTE, PER ROW — AND IT COLLAPSES TO ONE SENTENCE**

*The sensitivity run repeats the sweep at `ω = 1×10⁻³` with the DECAYING coefficient, which halves `C(4)`
and cuts `C(5)` by a factor `2.5`:*

| `L` | 3.2 | 3.6 | 4.2 | 4.6 | 5.5 | 7.0 |
|:--|--:|--:|--:|--:|--:|--:|
| registered `2√q log p` | `470` | `588` | `693` | `714` | `813` | `925` |
| Weil `4 log p/√q` | `470` | `588` | `692` | `713` | `811` | `923` |
| ### **difference, in offenders** | ### **0** | ### **0** | ### **1** | ### **1** | ### **2** | ### **2** |

> ### **CHANGING EVERY COEFFICIENT BY UP TO 2.5× MOVES THE MEASUREMENT BY AT MOST TWO OFFENDERS IN 1,945.**
> ### **THE MEASUREMENT IS A FUNCTION OF THE LAG ADDRESSES, NOT OF THEIR WEIGHTS.**
>
> *Two consequences, both filed as boundaries rather than results.* **(1) None of the departure is
> attributable to the registered `√p·log p` choice** — *the exposure registered in §1 did not fire.*
> **(2) THIS BENCH CANNOT ADJUDICATE THE COEFFICIENT QUESTION AT ALL.** *A quantity insensitive to a
> parameter cannot measure it, and the `√q·log p`-versus-`log p/√q` disagreement is therefore* ### **not
> resolvable here, by any number of `L` or `ω`.** *Named so the question is not later thought to have been
> settled by this table.*

**`C` on `V` = `0.34451`** *(banked `0.34481`, "stable to 3 digits")* — **re-measured under the same
constraint as the counts, which was the point of the ruling.**

---

## §4 — THE DIAGNOSIS, IN THREE REGISTERED ACTS · ### **AND IT REFUTES ITS OWN HYPOTHESIS AT ACT THREE**

### **ACT ONE — THE `ω`-SPLIT IS NOT INSTABILITY. IT IS A TWO-VALUED FUNCTION OF AN ARITHMETIC PROPERTY OF `ω`.**

**Registered at `relay` `e440827` with its mechanism, before any new number** *(`tools/e16/exp1_e2_profile.py`)*:
each lag sits at the integer address `k_q = round(log q / ω)`, and the continuum relation `log 4 = 2 log 2`
survives the grid only when `k_4 = 2k_2`. **Six NEW `ω`, classified by arithmetic in advance, three each way.**

| `L = 4.2`, all lags | `ω` | class | measured | fraction | |
|:--|:--|:--|:--|--:|:--|
| | `1.7×10⁻³` | incommensurate | `422/843` | `0.500593` | ### **P1 LANDS** |
| | `1.2×10⁻³` | incommensurate | `598/1195` | `0.500418` | ### **P1 LANDS** |
| | `8.0×10⁻⁴` | incommensurate | `897/1793` | `0.500279` | ### **P1 LANDS** |
| | `1.4×10⁻³` | ### **COMMENSURATE** | `495/1024` | `0.483398` | ### **P2 LANDS** |
| | `1.1×10⁻³` | ### **COMMENSURATE** | `630/1304` | `0.483129` | ### **P2 LANDS** |
| | `9.0×10⁻⁴` | ### **COMMENSURATE** | `770/1594` | `0.483061` | ### **P2 LANDS** |

### **P1 3/3 · P2 3/3 · P3 6/6** *(at `L = 3.6`, where no `log 4` lag exists, all six `ω` agree with `E1` to
`6×10⁻⁴` — no split, as predicted).*

> ### **THERE WAS NEVER ANY `ω`-INSTABILITY. THERE ARE TWO STABLE VALUES, `0.5003` AND `0.4831`, AND `ω`
> SELECTS BETWEEN THEM BY WHETHER A ROUNDING PRESERVES AN EXACT ARITHMETIC RELATION.** *Within each class the
> spread is under `3×10⁻⁴` — tighter than the law's own residuals.*

### **ACT TWO — A LIMIT OF THE DESIGN, PROVED RATHER THAN MEASURED**

**Before reading act one's numbers I asked whether its six `ω` could separate "the grid preserves
`log 4 = 2 log 2`" from "the log-4 address is even". They could not — and no `ω` ever can:**

> **For any real `x`, `|round(2x) − 2·round(x)| ≤ 1`, and `2·round(x)` is even. So `k_4 ≠ 2k_2` forces
> `k_4 = 2k_2 ± 1`, hence `k_4` ODD; and `k_4 = 2k_2` forces `k_4` EVEN.**
>
> ### **`k_4 = 2k_2` ⟺ `k_4` IS EVEN. IDENTICALLY, AT EVERY `ω`. THE TWO READINGS ARE ONE CONDITION, NOT TWO
> HYPOTHESES, AND AN `ω` SWEEP OF ANY LENGTH WOULD HAVE "CONFIRMED" BOTH FOREVER.**

**The lesson, and it is the executor's:** ### **REGISTERING A MECHANISM DOES NOT MAKE A DESIGN ABLE TO TEST
IT.** *Act one registered its mechanism verbatim in advance, exactly as the standing law requires, and would
still have reported a mechanism it had no power to distinguish from another.* **The check that caught it —
"can these two readings disagree on any input this design can produce?" — is a separate act, and it belongs
beside the falsifiability audit rather than inside it.** *Kin: the question-fit law's own lesson, that a
falsifiability audit catches the forced verdict and misses the disconnected one.*

**What CAN separate them is varying the LAG SET, not the grid** — `S^{k₄} = (S^{k₂})²` is a statement about a
pair — **and that is act three.**

### **ACT THREE — THE MECHANISM CONTROL, AND IT REFUTES THE ATTRIBUTION**

**`P4`, at `L = 4.2`, `ω = 1×10⁻³`** *(commensurate)*, `E1 = 0.516999`:

| lag set | count | fraction | `E2` against `E1` |
|:--|--:|--:|--:|
| `log 2`, `log 3`, `2 log 2` | `693/1434` | `0.483264` | `−0.033735` |
| `log 2`, `log 3` *(log-4 removed)* | `692/1434` | `0.482566` | `−0.034433` |
| ### **`log 2` ONLY** | ### **`693/1434`** | ### **`0.483264`** | ### **`−0.033735`** |

> ### **THE DEPARTURE IS IDENTICAL — TO THE OFFENDER — WITH THE SINGLE `log 2` LAG. THE EXTRA PRIME POWERS DO
> NOT CAUSE IT.**

**`P5`, the inertness control at `L = 3.6`, `ω = 1×10⁻³`:** `log 2 + log 3` gives `588/1280`; `log 3` removed
gives `587/1280`. ### **The second prime power moves the count by ONE offender in 1,280.**

> ### **AND THAT SETTLES A READING THE `AT-E1` VERDICT COULD NOT.** *"`E1` confirmed" and "the second lag was
> inert" are different statements, and the sweep's own table cannot tell them apart, because a lag that does
> nothing agrees with every prediction.* **Measured: the `log 3` lag at `L = 3.6` is inert to one grid point.**

### **ACT FOUR — THE SEPARATING ACT, REGISTERED BEFORE ACT ONE'S NUMBERS WERE READ** *(`relay` `9ced43c`)*

**`L = 4.2`, six lag sets, at `ω = 1×10⁻³` (`k₄` even) and `ω = 1.2×10⁻³` (`k₄` odd). `E1 = 0.516999` at both.**

| lag set | `ω = 1e-3` *(`k₄` EVEN)* | `ω = 1.2e-3` *(`k₄` odd)* | `\|split\|` | |
|:--|--:|--:|--:|:--|
| `{log2}` | `693/1434 = 0.483264` | `578/1195 = 0.483682` | `0.000418` | no split |
| `{log2, log3}` | `692/1434 = 0.482566` | `577/1195 = 0.482845` | `0.000279` | no split |
| ### **`{log2, 2log2}`** | ### **`692/1434 = 0.482566`** | ### **`597/1195 = 0.499582`** | ### **`0.017015`** | ### **SPLIT** |
| `{log3, 2log2}` | `337/1434 = 0.235007` | `281/1195 = 0.235146` | `0.000139` | no split |
| `{2log2}` alone | `51/1434 = 0.035565` | `43/1195 = 0.035983` | `0.000418` | no split |
| ### **all three** | ### **`693/1434 = 0.483264`** | ### **`598/1195 = 0.500418`** | ### **`0.017155`** | ### **SPLIT** |

### **`R2` LANDS, AND THE TWO EFFECTS SEPARATE CLEANLY:**

> **(A)** ### **A `−0.034` DEPARTURE FROM `E1` THAT IS PRESENT WITH `log 2` ALONE AND IS THE SAME IN BOTH `ω`
> CLASSES.** *It needs no second prime power and no commensurability. It is the ONE-PRIME law departing from
> its own formula at `L = 4.2`.*
>
> **(B)** ### **A `+0.017` `ω`-CLASS SPLIT THAT APPEARS ONLY WHEN `log 2` AND `log 4` ARE BOTH LIVE.** *Not
> with `log 4` alone (`0.0356` at both `ω`), not with `log 3 + log 4` (`0.2350` at both).* ### **It is the
> grid's rendering of the relation `log 4 = 2 log 2`, and nothing else.**
>
> ### **AT `L = 4.2` THE ONLY MEASURABLE EFFECT THE SECOND PRIME POWER HAS ON THE NEGATIVE FRACTION IS AN
> ARTIFACT OF HOW THE GRID ROUNDS ITS LAG ADDRESS** — *zero in the commensurate class, `+0.017` in the other,
> and the difference between the classes is a rounding, not a mechanism.* **That ceases to be true at larger
> `L`, and §6 measures where.**

---

## §5 — ### **THE FINDING. THE ONE-PRIME LAW IS NOT `1 − log2/logL`. IT IS THE SMALLER OF THE ROOM AND THE LAG.**

**Act three forced a definition change, registered before its run** *(`relay` `85a2099`,
`tools/e16/exp1_baseline.py`)*: `E2` had been defined against a PREDICTION where it had to be defined against
a MEASUREMENT, because **the one-prime law had never been measured past `L = 4.0` and was assumed to hold
there.** ### **A BASELINE THAT IS ASSUMED IS NOT A BASELINE.**

    E1-RESIDUAL(L,ω)    := fraction[log 2 only] − E1-UNION(L)        ← about the ONE-PRIME law
    E2-INTERACTION(L,ω) := fraction[all lags]  − fraction[log 2 only] ← about the EXTRA prime powers

**The one-prime law, `log 2` alone, on `V`, six `L` × three `ω`:**

| `L` | `E1-UNION` = `1 − log2/logL` | measured, `log 2` only *(`2e-3 / 1e-3 / 5e-4`)* | `E1-RESIDUAL` | |
|:--|--:|:--|--:|:--|
| **3.2** | `0.404078` | `0.404475 · 0.404475 · 0.404301` | `+0.0002` | ### **HOLDS** |
| **3.6** | `0.458874` | `0.456964 · 0.458594 · 0.458805` | `−0.0001` | ### **HOLDS** |
| **4.2** | `0.516999` | `0.483961 · 0.483264 · 0.483095` | ### **`−0.0339`** | ### **FAILS** |
| **4.6** | `0.545792` | `0.455381 · 0.454426 · 0.454277` | ### **`−0.0915`** | ### **FAILS** |
| **5.5** | `0.593402` | `0.407756 · 0.406690 · 0.406690` | ### **`−0.1867`** | ### **FAILS** |
| **7.0** | `0.643793` | `0.358025 · 0.356812 · 0.356464` | ### **`−0.2873`** | ### **FAILS** |

*`E1-RESIDUAL` quoted at the finest grid. The failure is not a drift: it is `−0.29` at `L = 7`, against a
residual scale of `5×10⁻⁴`, and it is stable across all three `ω` to three figures.*

### **AND THE FAILING COLUMN IS NOT NOISE. IT IS THE OTHER BRANCH OF THE SAME FORMULA.**

| `L` | measured *(`log 2` only, `ω = 5×10⁻⁴`)* | `log 2 / log L` | difference |
|:--|--:|--:|--:|
| **4.2** | `0.483095` | `0.483001` | `+0.000094` |
| **4.6** | `0.454277` | `0.454208` | `+0.000069` |
| **5.5** | `0.406690` | `0.406598` | `+0.000092` |
| **7.0** | `0.356464` | `0.356207` | `+0.000257` |

> ### **THE MEASURED NEGATIVE FRACTION OF `Φ_L` ON `V`, WITH THE SINGLE `log 2` LAG, IS**
> ### **`min( 1 − log2/logL , log2/logL )` — the SMALLER of the room and the lag, as a fraction of the window.**
>
> **`1 − log2/logL` is the smaller exactly when `log L < 2 log 2`, i.e. `L < 4`; the two are equal at `L = 4`.**
> *The corpus measured `L = 2.2, 2.5, 3.0, 3.5, 4.0` — **every one of them at or below the apex** — and read
> the branch it could see as the whole function.*

### **IN COUNTS, WHERE IT IS EXACT RATHER THAN APPROXIMATE**

*Let `k₂ = round(log 2 / ω)`, the lag's own length in grid points, and `M = round(log L / ω)`.*

> ### **OFFENDERS = `min( k₂ , M − k₂ )`, TO WITHIN ONE GRID POINT, AT EVERY `L` AND EVERY `ω` TESTED.**

| `ω` | `k₂` | `L=3.2` | `L=3.6` | `L=4.2` | `L=4.6` | `L=5.5` | `L=7.0` |
|:--|--:|:--|:--|:--|:--|:--|:--|
| `2×10⁻³` | 347 | `235` = `M−k₂` ✓ | `292` *(−1)* | ### **`347` = `k₂`** ✓ | ### **`347`** ✓ | ### **`347`** ✓ | `348` *(+1)* |
| `1×10⁻³` | 693 | `470` ✓ | `587` *(−1)* | ### **`693`** ✓ | ### **`693`** ✓ | ### **`693`** ✓ | `694` *(+1)* |
| `5×10⁻⁴` | 1386 | `940` ✓ | `1175` *(−1)* | ### **`1386`** ✓ | ### **`1386`** ✓ | ### **`1386`** ✓ | `1387` *(+1)* |

### **PAST `L = 4` THE OFFENDER COUNT STOPS DEPENDING ON `L` ALTOGETHER.** *It is `693` at `L = 4.2`, at
`L = 4.6`, and at `L = 5.5`, and `694` at `L = 7.0` — the same number while the dimension nearly doubles.*
**The count is pinned at the lag's length; only the window grows underneath it.**

### **WHAT THIS DOES TO THE `L = 4` EXACTNESS — AND IT CUTS BOTH WAYS, SO BOTH ARE SAID**

**Sitting 12's stated reason was:** *"`log 4 = 2 log 2` makes the lag exactly half the window."*

> ### **THAT REASON IS CORRECT, AND IT IS THE TURNING POINT OF THE LAW.** *The two branches meet exactly where
> the lag equals the room —* `log 2 = log L − log 2`*, i.e.* ### **`L = 4`** *— and the common value there is
> exactly* ### **`1/2`**. *So `0.5` at `L = 4` is not "an exact structural coincidence falling out of the
> mechanism": it is the* ### **maximum** *of a function whose banked formula was one of its two branches.*
>
> ### **AND CORRECTION SEVENTEEN'S STRIKE STILL STANDS, UNCHANGED.** *The claim struck was the arithmetic
> one — `693/1386`, exactly one half — and the instrument counts* ### **692**, *`0.49928`, at a cleanly
> separated boundary.* **The apex is `1/2` in the continuum; the grid misses it by one offender, exactly as it
> misses `min(k₂, M−k₂)` by one at two of the six `L` here.** ### **The intuition was better than the corpus
> knew and the number was still wrong, and neither of those observations weakens the other.**

---

## §6 — `E2-INTERACTION`, CORRECTLY DEFINED · ### **`B3` FAILS, AND WHERE IT FAILS IS THE POINT**

**`fraction[all lags] − fraction[log 2 only]`, same `L`, same `ω`, so the grid, the window, the operator and
the law's own behaviour all cancel.**

| `L` | lags | `ω=2×10⁻³` | `ω=1×10⁻³` *(comm.)* | `ω=5×10⁻⁴` | reading |
|:--|--:|:--|:--|:--|:--|
| **3.2** | 2 | ### **`+0` offenders** | ### **`+0`** | ### **`+0`** | ### **EXACTLY INERT** |
| **3.6** | 2 | `+1` | `+1` | `+1` | ### **INERT to one grid point** |
| **4.2** | 3 | `+12` *(`+0.0167`)* | ### **`+0`** | `+49` *(`+0.0171`)* | grid artifact only |
| **4.6** | 3 | `+34` *(`+0.0446`)* | `+21` *(`+0.0138`)* | `+140` *(`+0.0459`)* | ### **ACTIVE** |
| **5.5** | 4 | `+78` *(`+0.0917`)* | `+120` *(`+0.0704`)* | `+318` *(`+0.0933`)* | ### **ACTIVE** |
| **7.0** | 4 | `+138` *(`+0.1420`)* | `+231` *(`+0.1188`)* | `+555` *(`+0.1426`)* | ### **ACTIVE** |

> ### **`B3` FAILS AND `B4` FIRES: THE EXTRA PRIME POWERS DO MEASURABLE WORK — BUT ONLY WHERE THE ROOM-BASED
> LAW HAS ALREADY TURNED OVER.**
>
> **Below `L = 4` they are inert to zero or one offender in thousands.** *At `L = 3.2` the `log 3` lag changes
> not one direction, at any of three grids.* **Above `L = 4` they add offenders, monotonically in `L`, and
> they push the fraction back toward `1/2`** — *the value the one-prime law itself attains only at its apex.*
>
> ### **THE HONEST SHAPE OF IT: THE ROOM PICTURE PREDICTS THE OPPOSITE ORDERING.** *`log 3`'s room at
> `L = 3.2` is `5.5%` of the window and at `L = 7` it is `43.5%`, so a room-based reading expects a small
> effect early and a large one late — which is what is observed. But the same reading expects the effect at
> `L = 3.2` to be a small POSITIVE number, and it is measured at* ### **exactly zero, three times.**
> *A room that is `5.5%` of the window and contributes nothing at all is not a small effect; it is no effect.*
>
> ### **NO MECHANISM IS PROPOSED FOR THE THRESHOLD. It is measured, it sits at the same `L = 4` where the
> one-prime law turns, and that coincidence is recorded rather than explained.**

---

## §7 — THE RIDERS, LANDED IN `THE_ATTEMPT_RECORD`

### **CORRECTION SEVENTEEN**, three parts, all in `phase2/method/THE_ATTEMPT_RECORD.md`:

**(i) THE `L = 4` EXACT HALF, STRUCK VISIBLY AT ITS CLAIM SITE.** *The claim is quoted exactly as written —
sitting 12's* `"AND L = 4 LANDS EXACTLY: 693/1386 is exactly one half … An exact structural coincidence
falling out of the mechanism rather than fitted to it."` — **struck, with the refuting measurement cited by
file and run** *(`tools/e16/battery_full.py` PART 4, `ω = 10⁻³`, banked 2026-08-15: `692` not `693`,
`0.49928`, boundary separation `1.2×10⁻²` against `5×10⁻⁶` at the two genuinely ambiguous rows).*

> ### **THE META-LINE, WHICH IS WHY IT IS FILED SEPARATELY FROM CORRECTION SIXTEEN:**
> ### **AN EXACTNESS FROM AN UNBANKED INSTRUMENT IS UNFALSIFIABLE UNTIL RESURRECTION.**
>
> *An ordinary number from an unbanked instrument is merely unverified.* ### **An EXACT number is worse,
> because exactness is the one property that reads as self-certifying.** *`0.50000` invites no
> re-measurement; it looks like a fact about the mechanism rather than an output of a grid.* ### **The
> prettier the number, the smaller the audience that will ask for its bank.**

**(ii) THE LOG-2 BATTERY ROW RE-LABELLED.** §1 result 5: ~~`MATCHES CC's PUBLISHED SPECTRUM`~~ →
### **`EXTERNAL-TRANSCRIBED`**, *with its tolerance named as inter-discretization (`~10⁻³`) and the parities
identified as what actually reproduces.* **The corpus's own two records of that triple disagree with each
other by `~10⁻³`, the same order by which the rebuild differs from both, while the log-3 triple from the same
code in the same run reproduces to `2.6×10⁻⁵`.** *Offered as a reading on 2026-08-15; landed here as a
label, because a label is what a later reader inherits.*

**(iii) THE `V`-CONSISTENCY NOTE, CARRIED WITH THE `δ/L` TABLE.** *And it found something sharper than the
2026-08-15 finding stated:*

> ### **SITTING 12'S REPORT SAYS "ALL ON `V`" ON ITS FACE** — *"Same construction, lag fixed at `log 2`, all
> on `V`, `ω = 1×10⁻³`"* — **and the counts it then tabulates carry `dim = M`, the full grid.**
> ### **THE DIMENSION IN THE BANKED FRACTIONS IS ITSELF THE PROOF THAT THE CONSTRAINT WAS NOT IMPOSED: no
> `V`-restricted count can have `dim = M`.** *`97/788`, `224/916`, `407/1099`, `561/1253`, `693/1386` — every
> denominator is `round(log L/ω)` exactly.*
>
> *Kin: **a definitional pass is not a computed pass**. "All on `V`" was a claim about what the construction
> was FOR, and nothing computed whether it was so until the pipeline was rebuilt from the formulas rather
> than from the report.*

### **AND THE HEADING COUNT, REPAIRED A SECOND TIME BY THE LAW THAT MINTED IT**

`§2` read **"THE THIRTEEN CORRECTIONS"** — the table's count — **while corrections 14, 15 and 16 had been
appended below the table as sections without moving it.** *Counted, not estimated, by script against the
file:* ### **13 table rows + 4 sections (14, 15, 16, 17) = SEVENTEEN.**

> ### **CORRECTION 13's LAW — *corrections are written from counts, never from a sweep's account of itself* —
> FIRED ON THE VERY ENTRY THAT MINTED IT.** *Because a count taken once is not a count maintained, and a
> heading is exactly the field no later pass re-reads.*

---

## §8 — WHAT THIS EXPERIMENT DOES NOT SAY

* ### **NOTHING ABOUT THE SIGN OF `W_∞ − W_2`. NOTHING ABOUT `h2`. NO OPERATOR INEQUALITY.**
* **The negative fraction of `Φ` in the `η` coordinate is a bench quantity of a DISCRETIZED form**, measured
  at a grid, and is reported as one. ### **The `min(k₂, M−k₂)` statement is a statement about counts on a
  grid; its continuum reading `min(δ, log 2)/log L` is not proved here.**
* ### **LEMMA 5.2's RE-DERIVATION ON `(1,3]` IS STILL UNPAID, AND EVERY `L` IN THIS SWEEP LIES BEYOND IT.**
  *The whole table is extrapolation past a standing debt — which is now doubly worth saying, since the
  extrapolated region is exactly where the banked law turned out not to hold.*
* **No mechanism is proposed** for the turn at `L = 4`, for the threshold in `E2`, or for why two of the six
  `L` miss `min(k₂, M−k₂)` by one at every `ω`.
* ### **THE COEFFICIENT QUESTION IS NOT SETTLED AND CANNOT BE SETTLED HERE** — the measurement is a function
  of the lag addresses and not of their weights, so a bench that varies `L` and `ω` has no purchase on it.

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE GATE PASSED 3/3 AND THE `V` RULING IS APPLIED THROUGHOUT.** `L = 3` on `V` reproduces
   `202/548 · 406/1098 · 811/2196` exactly, and the fast `V` basis was verified identical to `phi_layer`'s
   own SVD route before it was used.
2. ### **`E1-UNION` IS VACUOUS BY CONSTRUCTION — PRINTED AND COMMITTED BEFORE THE RUN.** The rooms are
   nested, so the union is the largest room, so `E1` is the one-prime law verbatim at every `L`. **A
   generalization that generalizes nothing is a property of the design, and it was registered rather than
   discovered.**
3. ### **THE HEADLINE IS NOT ABOUT TWO PRIMES. THE ONE-PRIME LAW IS `min(1 − log2/logL, log2/logL)`** — the
   smaller of the room and the lag — **and the banked `1 − log2/logL` is its `L ≤ 4` branch.** In counts:
   ### **offenders = `min(k₂, M − k₂)` to within one grid point, at every `L` and `ω` tested.**
4. ### **THE `L = 4` "EXACT HALF" SITS AT THE APEX WHERE THE TWO BRANCHES MEET, AND SITTING 12's STATED
   REASON FOR IT WAS CORRECT.** *The arithmetic strike stands — `692`, not `693` — and the intuition it
   struck turns out to have been pointing at a maximum.*
5. ### **`E2-INTERACTION`, DEFINED AGAINST THE MEASURED BASELINE, IS EXACTLY ZERO AT `L = 3.2` AND ONE
   OFFENDER AT `L = 3.6`** — and becomes large only above `L = 4`, where the room-based law has already
   turned over.
6. ### **THE `ω`-SPLIT IS A GRID ARTIFACT CONFINED TO THE `log2`–`log4` PAIR**, isolated by a six-lag-set
   separating act registered before the profile's numbers were read.
7. ### **TWO METHOD FINDINGS, BOTH EXECUTOR-CAUGHT AND BOTH AGAINST MY OWN DESIGN:** *registering a mechanism
   does not make a design able to test it* — `k₄ = 2k₂` ⟺ `k₄` even, identically, so no `ω` sweep could ever
   have separated the two readings it was built to separate — and ### ***a baseline that is assumed is not a
   baseline***, which is what turned a "two-prime interaction" into a one-prime law's second branch.
8. ### **CORRECTION SEVENTEEN LANDED**, three parts, with the heading count repaired from a re-run count.

### **WHAT WANTS YOUR RULING**

1. ### **THE `δ/L` ENTRY IN `THE_ATTEMPT_RECORD` NOW UNDERSTATES ITS OWN OBJECT.** *This sitting restates the
   law's formula from `1 − log2/logL` to a two-branch minimum, on six new `L` at three `ω` each.* **That is
   research content, not a mechanical rider, so it is REPORTED AND NOT LANDED.** *One word lands it; the
   grade it would carry is yours.*
2. **The `(1,3]` debt** now blocks more than it did: the banked table has two rows beyond it, this sweep has
   six, and the turn at `L = 4` lives entirely inside the unre-derived region.
3. **The three missing `e16` READMEs** — `qeps_layer`, `phi_layer`, `battery_full` carry their content in
   module docstrings; the 2026-08-15 closing reads as though four documented modules landed.
4. **The four standing rulings** are unmoved by this pass: `SIDE-effects` repair route *(returned unresolved
   2026-08-14 — the repair had already happened by a third act)* · day-1 digestion scope *(landed
   annotations-only)* · the two prose rulings *(landed)* · `internal/` + `meta/` *(ratified `CENSUS-ONLY`)*.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — **committed at `relay` local tip, UNPUSHED, absence from
the public tree re-verified against the remote after this pass's push.** *Release condition unchanged:
counsel's answer, then your word.*

**`h2` UNCHANGED. NO SIGN. `WIDEN` PAUSED. RAIL DID NOT MOVE. NOTHING DEPOSITS.**
