# THE TOP-LEVEL SILENCE — b263, 2026-08-31

**Scope:** a derivation at content from the owners' own formulas, with b262's ladder as control.
**CONCURRENCY: SOLO (research seat).** b15/b14/b242 govern. The register sentence is unchanged and
**nothing deposits.** b260's `W-ORD-TQ-IDENTIFY` is the named open premise every line inherits.

**Order of record:** owners read at content → `reg_seal.py` built → index queried → registration
**term-scanned, then SEALED, then banked** → the run → the survey → the shadow → the bank → the
filings → HANDOFF.

**Both limits named at every use (b262's discipline):** **(L-A)** act 9's *level* limit — fix `p`,
fix `k`, let `n → ∞`. **(L-B)** the *cutoff* limit — `a² → ∞`, every `n_p` moving and the index set
growing.

---

## The verdict

### **M-2's address is derived, and it is one line of act 9's own text.**

The closed form is defined for `1 ≤ k ≤ n−1` and supplies **`0 for k ≥ n`**. So the quotient channel
contributes **identically zero** at a prime's top level — and a prime with `n_p(a) = 1`, whose only
level *is* the top, contributes nothing at all.

### **The first-level primes are silent.**

It is not a small value, not a limit, not an approximation: **the formula does not reach `k = n`, and
the owner supplies the zero explicitly.** b260 saw the consequence and named it (`φ = 1`); **this act
names its cause, which is a range and not a size.**

---

## S1 — the silence, derived

Extracted from `b220_aggregation_freedom.txt:77`, not typed:

> `tau_q(p,n,k) * p^(k/2) = (p^n - p^k)/(p^n - 1)` **for `1 <= k <= n-1`, `0 for k >= n`.** QED

**And act 9's range is *empty* for a first-level prime:** `1 ≤ k ≤ n−1` reads `1 ≤ k ≤ 0`. **The
prime has a level. The closed form has no range to put it in.** That pair is the silence, and the
shadow decides both halves of it.

**Exhibited: 17 `(p,n,k)` rows, 11 at a top level, 0 violations** — `tau_q = 0` and `φ = 1` exactly
at every one, and **strictly positive one level below**, which is what makes the zero mean something.

## S2 — the size of the silence

b262's ladder **consumed as control; no number re-derived.** Axes as banked: `ψ` is b261's fixed
array, `NV = 4001`, `NU_HALF = 401`, **place set = ALL PRIMES**.

| `a²` | `J = PR − Θ_q` | `m=1` (SILENT) | `T_fixed` | silent share |
|--:|--:|--:|--:|--:|
| 1e2 | 0.374669 | 0.277103 | 0.090425 | 73.96% |
| 1e3 | 0.549193 | 0.487489 | 0.056772 | 88.76% |
| 1e4 | 1.150252 | 1.114605 | 0.031917 | 96.90% |
| 1e5 | 2.268694 | 2.244544 | 0.019816 | 98.94% |
| 1e6 | 4.645790 | 4.628665 | 0.012413 | 99.63% |
| 1e7 | 9.551566 | 9.539401 | 0.007705 | 99.87% |
| 1e8 | 19.708927 | 19.699799 | 0.004814 | **99.95%** |

**Because `Θ_q` gets nothing from them, the `m=1` column is simultaneously their contribution to `PR`
and to the separation — one column, two roles.** The residue's decay is **(L-A)** at work; the rising
silent share is **(L-B)** at work. *The columns are labelled so they cannot be confused.*

---

## S3 — M-2's specification

### The conditional, stated before the specification and not after it

b262 established that **either the finite-place side supplies the first-level mass or the archimedean
side absorbs it, and nothing in the record decides which. The specification is for the first branch
only and is vacuous on the second.**

**Three necessary properties of any candidate aggregation `AGG(a)`:**

1. **(SPEC-1) It counts first levels** — non-zero weight at `(p, n_p)` for `n_p = 1`, of order
   `w_{p,1}`. *Ground: act 9 assigns zero there, and that is where 99.95% of the separation sits.*
2. **(SPEC-2) It reduces to `Θ_q`'s terms at `k ≤ n−1`.** *Ground: act 9 is proof-grade longhand.*
3. **(SPEC-3) It is defined over all primes, not a fixed set.** *Ground: b262's scope wall.*

**They exclude; they do not determine. No function satisfying them is exhibited, and none is shown to
exist. No aggregation is adopted, stated or realized.**

### The reconciliation with b220, made explicit

b220's *"not one of the four excludes any function"* **stands unmoved — it is about the four
constraints already in the record.** This act adds a **fifth, conditional** one, and **(SPEC-1) does
exclude: it excludes `Θ_q` itself.** So on the first branch the admissible set is no longer
everything; on the second it still is. **b220 is conditionally narrowed, not contradicted.**

### The corpus survey — 27 prior-owner lines read at content

| class | count | what they are |
|:--|--:|:--|
| SUPPLIES | 7 | act 9 / b11 state the quotient **value** at `k ≥ n` — the object being aggregated, not a condition on the aggregation |
| DEFINES | 2 | b16 / b17 fix what `n_p` **is** |
| OTHER | 18 | a level mentioned in passing |
| **CANDIDATE CONSTRAINT** | **0** | — |

**No prior holding states any condition on `Q.value` at the top level.** The classifier is shown able
to *return* `CANDIDATE CONSTRAINT` on a constructed line, so its silence is a **measurement**; and
the search is positive-controlled — two held phrases found, an invented one not.

> **And a defect in this act's own run, disclosed before its result was used.** `b263_run.txt`'s S3
> **counted** sixteen prior-owner files and then printed *"NO HOLDING CONSTRAINS THE TOP LEVEL"* —
> **having read none of them.** A count is not a reading, and **a verdict that runs ahead of its
> evidence is the species this corpus hunts; it does not stop being that species because the verdict
> later proves right.** The run is preserved unchanged; `b263_survey.txt` supplies what its S3 owed,
> with the criterion fixed before any line was quoted.

---

## S4 — the meaning, in b236's separated voices

**[DEPOSIT-VOICE]** the fourth register is *"the balance-to-positivity distance at the multiplicative
place"*. **[b236's MAPPING]** at a diagonal `a²` cell it **is the sign of `A − PR`**. **[b236's
CEILING, carried]** *"it discharges nothing"*.

**And `PR`'s growth is carried, to 99.95%, by primes the quotient channel is silent on.** So the
finite-place shadow of the deposit's own fourth register is, along the cutoff direction, almost
entirely made of terms `Θ_q` does not see. **Absent an aggregation that supplies them, b262's
sentence remains: the archimedean side must absorb a divergent quantity.**

**Five misreadings refused** — it is not evidence against the identity; not about b14's double limit;
it does not move `h2`; it does not say `Θ_q` is the wrong object; and, new this act:
**the specification is not a proposal.** It says what an aggregation would have to do, not that any
does it, and not that one exists.

---

## The shadow — **9 terminals, zero axioms, 0 errors, profile printed**

`Core/TopLevelSilenceShadow.lean`, vanilla Lean 4, `decide` only. Its load-bearing polarity control
is `the_level_below_is_inside_the_range` — **the range excludes the top level and nothing else**;
without it the exclusion would read as "the range is empty", true only at `n = 1`. And
`first_level_prime_has_a_level_the_range_does_not_reach` pairs `1 ≤ 1` with `¬(1 ≤ 0)` — **that pair
*is* the silence, as arithmetic.** Three false statements of the same shape were **refused**, lean
exit 1. **It does not compile M-2's specification.**

---

## `W-ORD-REG-HASH` — discharged by doing it

`tools/reg_seal.py` seals a registration with the `sha256` of **every byte above the seal block**
(a file cannot contain its own hash; hashing the prefix is the fixed point). `--verify` recomputes;
`--reseal` **prints both hashes and writes the superseded one into the new block** — *a tool that
could re-seal without leaving a trace would undo its own point.* **Its reach is not oversold: it
proves the body has not changed since sealing; it proves nothing about *when* the seal was written.**

> **And a sequencing defect of mine, which the tool then caught.** I ran the term scan and the seal
> **in one command**, and **sealed a registration that had just returned `NOT CLEAN`.** The ferry's
> order is *term-scanned then banked with its hash* — scan-then-seal means the scan must **pass**
> first. Then `--verify` returned **`SEAL BROKEN`, exit 1**, on my own correction — **a positive
> control obtained for free, before any fixture was written for it.** Superseded `c89a7c33…`;
> current `94b07821…`, verified intact.
>
> Two further live uses were caught in the bank at the closing scan and corrected before shipping.

---

## Gates — 10/10 CLEAN

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b263
  run at    : 2026-08-31T00:46:14 (local)
  input     : 10 checks routed through the harness
  checks    : 10
  pass      : 10
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 4ac4ab2536b614f69319e5279b1eac0c
=== END AUDIT SIDECAR ===
```

**Term scan CLEAN (0 live uses). Registration seal INTACT.**

### And the b262 investment paid visibly

One gate failed on a needle that read `THEY EXCLUDE` where the bank says `THESE EXCLUDE` — **b229's
species again.** But `verify_all` **named the absent needle outright**, and the fix took one
`needle_extract` call. **b229, b260 and b261 each cost a throwaway probe to locate. This cost none.**

---

## What this act does **not** establish

1. **It adopts, states and realizes no aggregation.** M-2 moves to **SPECIFIED-NOT-STATED**, which
   is not a discharge.
2. **The specification is conditional** and vacuous on the archimedean branch.
3. **It exhibits no function satisfying (SPEC-1)–(SPEC-3)**, and does not show one exists.
4. **It does not contradict b220.**
5. **It does not discharge `W-ORD-TQ-IDENTIFY`.**
6. **It proves no asymptotic** — `F₁`'s size is b262's, on b262's graded imports.
7. **It says nothing about b14's double limit.**
8. **It does not read b236's mapping as discharging anything.**
9. **Nothing about `h2` beyond the register sentence exact. Nothing deposits.**

---

## Filings

- **Two rows keyed: `top-level-silence` and `m2-spec`.**
- **M-2: OPEN → SPECIFIED-NOT-STATED**, and still owed.
- **`W-ORD-REG-HASH` discharged.** In flight: M-3…M-5 open; `W-ORD-B38-HIGHMODE`, `W-ORD-CN-LAW`,
  the QUOTED-N extension, `W-ORD-XI-PERMODE`, `W-ORD-ORDINATE-CACHE`, `W-ORD-STAGING-GUARD`,
  `W-ORD-FILE-E-WORKING-COPY-STALE`, `W-ORD-TE-SPEC`, `W-ORD-TQ-IDENTIFY`, `W-ORD-EPS-DECAY`.
- **J4 filed next**, and b263 sharpens why it is live: **the specification's condition *is* the
  branch question, so J4 is the act that bears on which branch the specification is even about.**
- **The thirty-seventh seam's debt:** this is **the first act in the arc to move item 1 at all**, and
  it moves it only from `OPEN` to `OPEN, and now SPECIFIED`. **The debt is not paid.**
- **Index:** `quotient trace` **HIT** — returning both objects this act needed, the closed form *with
  its range* and *"the aggregation is unstated"*. `weil ledger` **HIT**. Neither used as a premise.
- **HANDOFF** brought current by demotion, prior content kept, read-back identical.
- **The patent lane is independent and untouched** — nothing written or staged, no `git add -A`,
  b259's bank still untracked, **receipts pending on the ferry's word, not verified by this seat.**
- **`PLACE-papers` not touched — no mirror rebuild owed, hook not exercised, reported either way.**

### The fork at this stop

1. **J4** — the archimedean twin, and the act that decides which branch the specification is about.
2. **`W-ORD-TQ-IDENTIFY`** — the premise J1, J3 and b263 all inherit.
3. **M-2's statement itself**, now that it has an address — the author's to adopt or route.
4. **M-3; M-5; `W-ORD-CN-LAW`.**
5. **The patent lane**, independent, on your word.

---

### **M-2 IS NO LONGER AN OPEN NAME. IT HAS AN ADDRESS, AND THE ADDRESS IS A RANGE: WHATEVER THE AGGREGATION IS, IT IS THE THING THAT WOULD HAVE TO SPEAK WHERE act 9's FORMULA IS SILENT. THE FIRST-LEVEL PRIMES CARRY 99.95% OF THE SEPARATION AND THE QUOTIENT CHANNEL ASSIGNS THEM ZERO. THE SPECIFICATION EXCLUDES `Θ_q` ITSELF — CONDITIONALLY, ON A BRANCH NOBODY HAS SETTLED, AND J4 IS THE ACT THAT SETTLES IT. NO AGGREGATION IS ADOPTED. M-2 IS STILL OWED. `h2` STANDS EXACTLY WHERE THE DEPOSIT LEFT IT. NOTHING DEPOSITS. LOCKS LAST.**
