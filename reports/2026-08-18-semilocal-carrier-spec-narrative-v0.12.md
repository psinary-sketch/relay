# THE SEMILOCAL CARRIER SPECIFICATION — `C3`'s BUILD SHEET

**Method register · ### `v0.12` · 2026-08-17/18, extended same day (the phase boundary `§1-bis` + its finite-trace identity; renamed, `§2` seated, `B2`–`B4` renumbered `§3`–`§5`, eighth sitting; `§6` = `B5`, ninth; `§7` = `B6`, tenth; `§8` = `B7`, eleventh; `§8.3-bis` + `§9` = `B8`, twelfth; `§8.3-ter` the `E1` cell, thirteenth; `§8.3-quater` = `B9`, fourteenth; `§8.3-quinquies` = `B10`, fifteenth; `§8.3-sexies` = `B11`, sixteenth; `§8.3-septies` = `B12`, seventeenth; ### `§0` review screen + `§8.3-octies` = `B13`, the tensor check (`Son = V₁⊗W₁`) — eighteenth sitting) · `W-ROAD-B` checkpoints `B1`–`B13` · QUESTION-FIT · NO MECHANISM PROPOSED**

## §0 — ### **WHAT THIS DOCUMENT HOLDS, AT GRADE** *(the one-screen review, 2026-08-18; written for external peer review)*

**The object under specification:** `T2`/`C3` — **the address graph and a LEDGER-SIDE positive trace as
one object**, over `ℚ`. **The plain sentence: nothing here is a construction, none of it moves `h2`, and
all of it specifies the object `h2` would rest on.**

**`B1`'s columns, where they stand after `B1`–`B13`:**

| column | status |
|:--|:--|
| **(i) address side** | ### **FILLED at class resolution** (data native; graph instrument-derived) |
| **(ii) place side — completion** | ### **FILLED**: `γ_∞` class-silent scalar; `Ẑ` self-dual under the antipode-involution; the certified archimedean trace lifts componentwise (`h` copies) |
| **(ii) place side — finite trace** | ### **SPECIFIED (`§9`: (α) sum-of-squares shape · (β) weight · (γ) identification) AND SATISFIED BY NOTHING KNOWN** — the field's own Theorem 2 rules Sonin out; the orbit quotient carries the exact Weil weight (the fifth law) but no positivity is claimed |
| **(iii) identification** | ### **ABSENT** as an operator; the bench identification (count = matching) is EXACT at every bipartite cell asked (nine of nine + two probe cells) |
| **(iv) joint move** | ### **DOWNSTREAM of (ii)-finite** — it cannot be asked until the finite trace exists |

**The checkpoints, one line each:** `B1` the four columns · `B2` two candidates, `(iv)` empty · `B3` ran:
NOT STABLE (the monotonicity half withdrawn by the weight control) · `B5` the archimedean place is
class-silent; `Ẑ` self-dual · `B6` the archimedean trace lifts; identification probe exact · `B7` the
finite-trace specification; Theorem 2 verbatim (Sonin archimedean-rigid); the local candidate registered ·
`B8` the candidate closes at `E3` with two exact laws; the ledger-side specification (`§9`) · `B9` the
trace-vanishing DERIVED (fixed-point localization); the sandwich/weight tension; the cross-lag Weil shape ·
`B10` the absorption identity PROVED; the orbit-quotient trace ALIVE; Lefschetz match-in-shape · `B11` the
FIFTH LAW (the pure Weil weight exact in the limit); the census compiled (`SIDE-window` `v0.5.0`, 26/26
axiom-free) · `B12` the Deninger comparison at content: MATCH arithmetic side / NO CONTACT cohomological ·
`B13` the tensor check: ### **`Son = V₁ ⊗ W₁` — the finite place holds a Fourier-dual self-product in
miniature.**

**The model's laws (bench + kernel grade):** `dim Son = (p^n−1)²` *(now EXPLAINED by `B13`)* ·
`‖SUS‖ = √p(p^{n−1}−1)` · `d_k = (p^{n−k}−1)(p^n−1)` · `dim V_inv = (p−1)p^n` ·
`R(k) = (p−1)p^{n+1}(p^{n−1}−p^{k−1})/(p^n−1)`.

**Held / open, at their addresses:** the `(β)` convention on the TRUE functional (the model says Weil
shape; the functional's own convention is a named bench question) · the `1807.06400` BODY read · the two
`HELD` carrier acts (on counsel) · the `(α)`-positivity of any quotient form (UNKNOWN and said so).

> ### **THE MISSING-CODOMAIN QUESTION (SELF-PRODUCT / FROBENIUS LIFT / LEAVES) ON THE FACE: WHAT SUPPLIES THE LEAVES — with its three-road provenance:
> Road A's missing SELF-PRODUCT · column `(vi)`'s missing LEAFWISE METRIC · Deninger's missing FROBENIUS
> LIFT. Three roads, one absence.**

---

## §0-bis — THE RENAME, RULED AND RECORDED

**Renamed 2026-08-17 from `THE_ONE_OBJECT` by the author's ruling, under the writing law: "ONE" was a
CLAIMED PROPERTY — the very property `§1`'s own test exists to decide — and a title may not presume its
sheet's outcome. The new title names the object (carrier), the regime (semilocal), and the genre
(specification).** *Alias for citation continuity: documents citing `THE_ONE_OBJECT` resolve here; the old
name is retired as a heading and survives only as this line.* ### **Name-identity, armed:** *this sheet
(`C3` / `W-ROAD-B` — the one-object question) and `THE_CARRIER_SPECIFICATION` (`C1` / the phase-2 field
spec) and `THE_CARRIER_BUILD` (`C1` / `W-ROAD-A`'s build sheet) are THREE documents; no merge, and "the
carrier spec" unqualified is not a usable citation.* **Renumbering note, same ruling:** *the seed object
(`§2`) was seated by the author between the specification and the candidate table; the former `§2`–`§4`
(`B2`–`B4`) are now `§3`–`§5`, with `§3-bis`/`§3-ter` now `§4-bis`/`§4-ter`. External citations to this
document's `§1`/`§1-bis` are unaffected; the checkpoints `B1`–`B4` are the stable names.*

> ### **THE GATE, ON THE DOCUMENT'S FACE.** *This sheet is `INSTRUMENT` track: writing a specification and
> grading candidates against it is chartered.* ### **DERIVING THE SIGN IS NOT, AND STAYS BEHIND THE
> `ATTEMPT` GATE, WHICH IS AUTHOR-OPENED ONLY AND IS NOT OPEN.** *The charter's `§5` trigger governs: an
> attempt is justified when `C1` or `C3` lands at content.* **This document is `C3`'s build sheet. It is not
> a construction, and a construction would not be a proof.**

> ### **AND THE STANDING STOP RIDES EVERY SECTION: if anything below begins to look like a result about the
> sign of `W_∞ − W_2` — STOP, state at grade, hold for the author.** *Nothing here is such a result.*

---

## §1 — `B1`: ### **THE SPECIFICATION OF "ONE OBJECT"** *(written before any candidate is named)*

**`T2` demands the `(nonArchimedean, unbounded)` quadrant — the row every located attempt fails. In today's
vocabulary the demand is:** ### **the address graph and the semilocal trace AS ONE OBJECT.** *Four columns,
graded as the carrier specification's are.*

| | column | what a construction must contain |
|:--|:--|:--|
| ### **(i)** | ### **THE ADDRESS SIDE** | the shift structure at lags `log q` over the live prime powers — a graph on the support with an edge per lag address, **whose maximum matching number is the pure remainder count** *(derived 2026-08-17: bipartite ⟹ `NPOS = NNEG = (M − nullity)/2`, and `NPOS = μ`)* |
| ### **(ii)** | ### **THE PLACE SIDE** | the semilocal trace — Sonin-type positivity over a **finite set of completions** `{∞, 2, …, p}` — **the thing that pays for a prime**, i.e. in which a prime enters as a LOCAL FACTOR and not merely as a distance |
| ### **(iii)** | ### **THE IDENTIFICATION** | ### **a SINGLE operator whose spectral count IS the matching number on the address side and whose trace IS the positive floor on the place side.** *Not two quantities that agree; one quantity read two ways.* |
| ### **(iv)** | ### **THE JOINT MOVE** | ### **widening the support and adding a place are THE SAME OPERATION on it.** |

### **THE TEST THAT DISTINGUISHES "ONE OBJECT" FROM "TWO OBJECTS GLUED"**

> **Two objects glued:** *each axis can be moved with the other held fixed, and the joint statement is a
> CONJUNCTION of two separate statements about two separate things.*
> ### **One object:** *there is a single operation whose variation moves both, and the two statements are
> two readings of one quantity.*

**And the corpus already holds the sharpest form of this distinction, which is why the test can be stated
concretely rather than in the abstract.** *The place-content homonym, resolved 2026-08-11:*

| | ### **the ledger's ACTIVE PRIMES** | ### **the framework's GOVERNING PLACES** |
|:--|:--|:--|
| what it is | which prime powers contribute to the window | which completions the formalism is built over |
| who fixes it | ### **SUPPORT-FORCED** — given `[1/a², a²]` the contributing primes are DETERMINED | ### **FREELY CHOSEN** — a finite set `S`, picked and then increased |

> ### **THOSE ARE EXACTLY THE TWO AXES, AND TODAY THEY DIFFER IN KIND: one is a FUNCTION of the support
> parameter, the other is a PARAMETER OF THE FORMALISM.** *So `(iv)` is not a detail of the specification —
> it is the whole of it. **A construction satisfies `(iv)` exactly when the free choice and the forced
> determination cease to be different kinds of thing.***

### **AND THERE IS A NAMED TENSION ANY CANDIDATE MUST RESOLVE, WITH A THEOREM ON EACH SIDE**

| side | statement | source |
|:--|:--|:--|
| ### **the place axis is INERT** | *"We prove **the stability of the semilocal Sonin space under the increase of the finite set of places** which govern the semilocal framework"* | `2310.18423` — ### **the field's own theorem**, read at content |
| ### **the support axis is NOT** | the count is `μ`, a sawtooth in `r = log L/log 2` with peaks `½` at `L = 4, 16, 64, 256` and troughs `⅓, ⅖, 3/7` — **measured at `r = 1 … 8` without a miss** | `FINDINGS` `F.2026-08-17d`, `F.2026-08-17e` — ### **the corpus's own derived law** |

> ### **IF BOTH ARE RIGHT, THEN ON THE OBJECTS THEY NAME, ADDING A PLACE CHANGES NOTHING AND WIDENING
> SUPPORT CHANGES THE COUNT.** **Two operations with measurably different effects are not the same
> operation.** ### **SO `(iv)` IS NOT MERELY UNBUILT: ANY CANDIDATE MUST EITHER PUT THE TWO OPERATIONS
> TOGETHER SOMEWHERE THE STABILITY THEOREM DOES NOT REACH, OR EXHIBIT A QUANTITY FOR WHICH STABILITY IS NOT
> A TRIVIALITY.**
>
> **What the tension does NOT say, stated so it is not over-read:** *the stability theorem is about a
> SPACE, not about a trace value; the sawtooth is about a DISCRETIZED count, not about an operator.*
> ### **The two theorems are about different objects and do not contradict each other. What they do is
> price `(iv)`: it cannot be satisfied by adjoining the two candidates as they stand.**

### **QUESTION-FIT ON `(iv)`, BOTH BRANCHES LONGHAND**

* ### **IF a construction satisfies `(iv)`**, then widening the window and enlarging the place set are one
  operation, the sawtooth and the stability theorem are two readings of one behaviour, and `C3` has landed
  at content — **which is one of the two conditions the charter's trigger names.**
* ### **IF no construction can satisfy `(iv)`**, then `T2` is not a row awaiting a cleverer candidate but a
  statement that the two axes are structurally different, **and the honest form of that finding is a
  negative result about the quadrant, not a failure to have tried hard enough.**

**Both sentences finish. The column can fail and its failure bears on the question.**

---

## §1-bis — ### **THE PHASE BOUNDARY, FILED AS A FACT ABOUT COLUMNS `(i)` AND `(iv)`** *(2026-08-17, from the weight control; every arrow at its grade)*

### **THE CHAIN, STATED AT GRADE RATHER THAN AS AN IFF**

| arrow | grade |
|:--|:--|
| **all live addresses odd ⟹ index parity is a bipartition ⟹ the count is the matching number and is weight-independent** *(away from the measure-zero degeneracy)* | ### **DERIVED** *(the week's law)* |
| **admitting `q = p²` forces an even address** — `k_{p²} = 2·k_p` exactly at the measured `ω` *(`1386 = 2·693`)* — **so the first prime power admitted forces the all-odd premise to fail** | ### **DERIVED** *(rounding-exact at `ω = 10⁻³`; at another `ω` the doubling can miss by one and the claim is re-checked, not assumed)* |
| **every `(L, S)` cell with a prime power admitted is NOT bipartite** *(25 of 25)* | ### **MEASURED** *(the 2-colouring audit, `relay` `tools/e16`)* |
| **in the non-bipartite cells the count is weight-dependent** — unit ≠ registered at `25/25` (`W2`); two GENERIC conventions disagree in step-sign at every window, differences up to `99` offenders (`W1`) | ### **MEASURED** *(the weight control)* |

### **TWO CAVEATS, SO THE BOUNDARY IS NOT OVER-CLAIMED AS AN IFF**

* ### **ONE-LAG GRAPHS ARE PATHS AND ARE BIPARTITE AT ANY PARITY.** *The sawtooth cells never depended on
  oddness; the parity premise matters only from the second lag on.*
* ### **EVENNESS OF AN ADDRESS IS `ω`-RELATIVE FOR PRIMES.** *`log 7` reads even at `ω = 10⁻³` (`k = 1946`)
  and `7` is no prime power — only prime SQUARES force evenness structurally.* **So the prime power is the
  first FORCED crossing of the boundary, not the only possible one, and the boundary itself is the graph
  property (bipartiteness), not the number-theoretic label.**

### **THE TRANSITION, LOCATED — AND IT IS THE APEX CELL**

> ### **AS `L` GROWS AT `ω = 10⁻³` THE FIRST EVEN ADDRESS TO GO LIVE IS `2 log 2`'s, AT `L = 4 = 2²` — THE
> `p = 2` APEX CELL.** *The structural crossing of the phase boundary and the apex-at-`p²` condition
> (`SIDE-window` `v0.3` compiles it for `p = 3`; the `L = 9` doubling is its open question) happen at the
> same cells, because they are the same arithmetic fact read twice: `p²` inside the window puts `2·k_p` on
> the address list.* **The discriminating cells `F.2026-08-17e` priced (`L` just above `4`; `L = 27`) sit
> exactly on this boundary.** *Coincidence-of-cells is noted, not promoted: no mechanism connecting the
> apex DOUBLING to the bipartiteness LOSS is derived here.*

### **WHAT THE BOUNDARY SAYS ABOUT THE SPECIFICATION'S OWN COLUMNS**

> ### **COLUMN `(i)`'s MATCHING-NUMBER CONTENT IS A BIPARTITE-REGIME READING — AND THE FULL OBJECT LIVES ON
> THE OTHER SIDE.** *The true window admits every prime power `≤ L`, so the actual address graph is
> non-bipartite for every `L ≥ 4`. The derived law is exact on the idealized side of a boundary the real
> object crosses at `L = 4`; on the real side the count is an eigenvalue count that the weights can move.*
> **`(i)`'s row is therefore re-read: DERIVED on the bipartite idealization, BENCH beyond it — which is
> what the `μ`-column withdrawal already measured.**
>
> ### **AND COLUMN `(iv)` INHERITS THE BOUNDARY: THE JOINT MOVE WOULD HAVE TO BE BUILT ACROSS IT.** *On one
> side the address object is weight-silent and the place operation has nothing to couple to; on the other,
> both operations move the count and the weights move it too.* **The two-class inventory's sentence, now
> with its boundary measured: structure-only and weight-dependent are separated by a graph property, and
> it is crossed by admitting `2 log 2` — the chemistry register's bonding-graph/reactivity line, with the
> line located.**

### **AND `B3`'s VERDICT AS IT STANDS AFTER THE CONTROL, RESTATED ONCE SO THIS SECTION IS CURRENT**

**`NOT STABLE` SURVIVES, robustly** *(every window, every weighting, some step of hundreds)*.
**"NOT EVEN MONOTONE" IS WITHDRAWN as a structural claim** — *a statement about the `(object, weighting)`
pair; typical (13 of 15 cells) but not invariant.* **The week's silence-on-weights is SCOPED to the
bipartite regime, not refuted.**

### **THE PHASE-BOUNDARY ↔ FINITE-TRACE IDENTITY** *(added 2026-08-17, eleventh sitting, from `B7`; ### question grade, flagged `OURS`, no promotion)*

> ### **ARCHIMEDEAN PAYS STRUCTURE; FINITE PAYS WEIGHT; `h2` IS WEIGHT-DEPENDENT.** *Three banked facts,
> one alignment: the lifted archimedean trace reads the test function and never the prime's coefficient
> (`§7(a)` — coefficient-silent); the phase boundary put the weights on the trace side and showed the
> structural count silent on them (`§1-bis` above, the two-class inventory); and the field's own Theorem 2
> (`§8.2`) makes the Sonin mechanism archimedean-rigid — no finite-place Sonin directions.* ### **SO THE
> ONE COLUMN STILL EMPTY — the finite-place trace — IS EXACTLY THE ONE THAT WOULD HAVE TO CARRY THE
> WEIGHTS, AND EVERYTHING BUILT SO FAR IS ON THE STRUCTURE SIDE OF THE BOUNDARY.** *Filed as an alignment
> of three graded facts, not as a theorem; what it prices is `§8.1(c)`'s hardness.*

---

## §2 — ### **THE SEED OBJECT: THE CLASS-RESOLVED EULER PRODUCT** *(seated 2026-08-17 by the author's ruling; ### question grade, flagged `OURS`; NO CONSTRUCTION)*

### **THE OBJECT, NAMED**

> ### **`Z(s) = Π_𝔭 (1 − [𝔭]·N𝔭^{−s})^{−1} ∈ ℂ[Cl]` — THE CLASS-RESOLVED EULER PRODUCT.**
> *Derivation at cite (`THE_CARRIER_BUILD` `§9.1`, longhand): the class map is a monoid homomorphism, so
> the coefficient function `𝔞 ↦ [𝔞]` is completely multiplicative into the group algebra, and unique
> ideal factorization gives the product; componentwise absolute convergence for `Re s > 1`.*

**Its two known projections, both at cite** *(correction twenty-three's yield, `THE_ATTEMPT_RECORD` +
`THE_CARRIER_BUILD` `§9.1`)*:

| projection | value | what it is |
|:--|:--|:--|
| ### **augmentation** `ε(Z)` | `ζ_K(s)` | the `ψ₀`-eigenvalue — *the class-silent shadow* |
| ### **determinant** of `Z`'s regular action | `ζ_H(s)` | the product over the character basis, which **diagonalizes** `Z(s) = Σ_ψ L(s,ψ)·e_ψ` |

> ### **AND THE RIDER STANDS BESIDE THE OBJECT, NOT AFTER IT: A PAIRING ON `Z` THAT IS
> AUGMENTATION-SILENT — its value constant across the class resolution, reading only `ε(Z) = ζ_K` —
> CARRIES NO MORE THAN A PAIRING ON `ζ_K`.** *The requirement is a pairing that SEES the class resolution.*
> **Kin, compiled: `descent_carries_no_warrant` — a pairing that cannot separate two classes carries no
> warrant about them, whatever the form is.**

*Naming-grade kin only: group-ring-valued zeta data of Stickelberger/equivariant type —
`NAVIGATOR-SUPPLIED`; naming is not reading. Resolution caveat, named without promotion: `Cl` is the
coarsest class resolution; ray-class refinements exist and none is chosen here.*

### §2.1 — ### **`B1`'s FOUR COLUMNS ASKED OF `Z(s)`** *(the grading the seating exists for; both branches longhand; NO construction move run)*

| `B1` column | `Z(s)` | the grade's exact content |
|:--|:--|:--|
| ### **(i) address side** | ### **DATA NATIVE, AT CLASS RESOLUTION — OBJECT DERIVED** | *the addresses `log N𝔭` are the exponents of the Dirichlet structure, and they arrive CLASS-LABELLED — strictly richer data than the bench's (norms only). But the address GRAPH — window, lags, matching — is an instrument built ON the data; `Z` carries no window and no count.* |
| ### **(ii) place side** | ### **HALF — FROM THE OPPOSITE SIDE TO THE BENCH's** | *every finite prime enters as a genuine LOCAL FACTOR `(1 − [𝔭]N𝔭^{−s})^{−1}` — exactly what the bench lacked ("primes only through their addresses"). But the factor is ALGEBRAIC, not semilocal-analytic:* ### **no completion `ℚ_p`, no Sonin space, no trace, and NO ARCHIMEDEAN PLACE AT ALL — `Z` is a finite-places object.** *The analytic half — the thing that PAYS for a prime — is absent.* **[Superseded in part by `§6`/`B5`, 2026-08-17: the archimedean COMPLETION enters natively — `γ_∞` scalar, class-silent — and `Ẑ` is self-dual under the antipode-involution; the TRACE half stays absent.]** |
| ### **(iii) identification** | ### **ABSENT — THE EMPTY COLUMN, NAMED EXACTLY** | *no operator whose spectral count is a matching number and whose trace is a floor. The one operator in sight — multiplication-by-`Z` on `ℂ[Cl]` — has determinant `ζ_H` and no matching-number reading.* |
| ### **(iv) joint move** | ### **VACUOUSLY ONE OPERATION — AN EVASION, NOT A PASS** | *on `Z_S = Π_{𝔭∈S}(…)^{−1}` with `S` free, "adding a place" is multiplying in a local factor; but "widening support" has NO INDEPENDENT MEANING — there is no window for support to be forced by. The two operations `B1` demands be identified do not both exist on the object; it carries ONE operation (extend the product), and "same operation" is satisfied only because the second axis is missing.* |

### **BOTH BRANCHES ON `(iv)`, LONGHAND**

* ### **IF `Z`'s ONE OPERATION IS THE GERM OF `(iv)`:** *then some trace/positivity structure on the
  restricted products `Z_S` would have to make the support axis REAPPEAR — the window's support-forced
  active primes as a norm-truncation of the free product — and reproduce, from the single move, the
  sawtooth on one reading and the place-stability question on the other.* **Nothing in hand does this, and
  stating it prices the gap exactly: the missing piece is `(ii)`'s analytic half — the same trace the whole
  sheet keeps arriving at.**
* ### **IF THE ONE-NESS IS VACUOUS:** *`Z` is a CONTENT VEHICLE — it holds column `(i)`'s data at full
  class resolution and holds nothing else — and the honest grade is a content candidate, not a carrier
  candidate.* **`B1`'s row count is unchanged and the seat's value is bookkeeping precision, which is worth
  having and is not a carrier.**

**Both sentences finish; the seating survives either branch, which is why it is a seating and not a bet.**

### §2.2 — ### **THE INVOLUTION'S ACTION ON `ℂ[Cl]`-VALUED OBJECTS — COEFFICIENT-WISE ONLY UP TO THE ANTIPODE** *(the ferry's question, answered longhand)*

**The functional equation of each component couples `s ↦ 1 − s` with `ψ ↦ ψ̄`** *(`Λ(s,ψ) = W(ψ)·Λ(1−s,ψ̄)`
— `STANDARD-AT-TEXT`, the FE row the addendum already carries at cite)*. **And `ψ ↦ ψ̄` on components IS the
group algebra's canonical involution, longhand:** *the antipode `∗: [C] ↦ [C^{−1}]` maps
`e_ψ ↦ e_ψ̄` (compute: `ψ̄(C^{−1}) = ψ(C)`).* ### **SO THE FE DOES NOT ACT COEFFICIENT-WISE — IT ACTS AS
REFLECTION TWISTED BY THE ANTIPODE, `Ẑ(s) = W · Ẑ(1−s)^∗`. The involution on the class-resolved object is
itself class-resolved.**

> ### **AND THE FERRY'S SECOND QUESTION — "IS THE RESULTING HERMITIAN FORM AUGMENTATION-SILENT?" — HAS NO
> FRAME-FREE ANSWER, WHICH IS THE FINDING: THE INVOLUTION ALONE INDUCES NO FORM. A form needs a LINEAR
> FUNCTIONAL to scalarize `f·g^∗`, and the two canonical functionals on `ℂ[Cl]` land on opposite sides of
> the rider:**
>
> | functional | the form `⟨f,g⟩` | verdict under the rider |
> |:--|:--|:--|
> | ### **the augmentation `ε`** | `ε(f g^∗) = (Σ_C a_C)(Σ_C b̄_C)` — **rank one** | ### **AUGMENTATION-SILENT BY CONSTRUCTION — its value constant across the class resolution, reading only `ε` of each argument: the warned failure, exhibited — it carries exactly a pairing on `ζ_K` and no more.** *Graded OUT by the rider itself.* |
> | ### **the identity-coefficient `τ`** | `τ(f g^∗) = Σ_C a_C b̄_C` — the standard inner product on `ℂ[Cl]` | ### **SEPARATES CLASSES — the canonical class-seeing alternative.** *NAMED, NOT PROMOTED: nothing here asks whether any `τ`-built pairing is definite, and nothing may.* |
>
> ### **SO THE RIDER SHARPENS FROM A WARNING INTO A SELECTION PRINCIPLE: any pairing built on `Z` must
> DECLARE ITS FUNCTIONAL, and `ε` is excluded by the rider's own sentence.** *Which functional, if any,
> meets `Δ2`'s demands is a construction question and is NOT OPENED.*

**NO CONSTRUCTION. NO MECHANISM. NO SIGN — the standing stop rode `§2` and did not fire: every statement
above is finite-group algebra done longhand or a projection already at cite.**

---

## §3 — `B2`: ### **THE CANDIDATE TABLE, GRADED AT WHAT THE CORPUS ACTUALLY HOLDS**

> ### **SOURCING RULE FOR THIS TABLE, APPLIED BEFORE IT WAS FILLED:** *a column is graded only against
> material this corpus records as READ, at the grade the `BIBLIOGRAPHY` records.* **Everything else is a
> named read-debt and grades nothing.** *Kin: a retrieval gloss is not the source.*

| | ### **(i) address side** | ### **(ii) place side** | ### **(iii) identification** | ### **(iv) joint move** |
|:--|:-:|:-:|:-:|:-:|
| ### **CC's SEMILOCAL FRAME** *(`2310.18423` read at content; `2511.22755` held)* | ### **IMPORTED, AND BY CHOICE** | ### **✓ NATIVE** | ### **✗** | ### **✗** |
| ### **THE CORPUS'S TOEPLITZ-ON-`η` APPARATUS** *(this week; `relay` `tools/e16`)* | ### **✓ NATIVE** | ### **✗** | ### **HALF** | ### **✗** |

### **CC's SEMILOCAL FRAME, ROW BY ROW**

* **(i) IMPORTED, AND BY CHOICE.** *The addresses enter —* `2511.22755`*:* ***"the construction only involves
  the Euler products over the primes `p ≤ x = λ²`"*** *— but the corpus's own finding on that sentence is
  that* ### **the coupling is a construction decision, not a structural necessity.** *The ledger's active
  primes are support-forced; theirs are selected.*
* **(ii) NATIVE.** *This is what a semilocal frame IS: a formalism over a finite set of completions, with a
  Sonin space and a stability theorem about enlarging that set.* ### **The only candidate with this column
  natively.**
* **(iii) ABSENT.** *No statement anywhere in what the corpus holds identifies a spectral count of their
  operator with a matching number of anything.* **The matching-number law is 2026-08-17's and is about a
  discretization, not about their operator.**
* **(iv) ABSENT — and its own stability theorem is why.** *Enlarging the place set leaves the Sonin space
  stable; widening support is a different operation with a different effect.*

### **THE CORPUS'S OWN APPARATUS, ROW BY ROW — AND THE ROW THAT COST THE MOST TO GRADE HONESTLY**

* **(i) NATIVE.** *The address graph IS the object: vertices `{0,…,M−1}`, an edge per lag address
  `k_q = round(log q/ω)`, and the inertia is the matching number.* ### **Derived, not fitted.**
* ### **(ii) ABSENT — and the near-miss must be named or the row reads as a pass.** *The form
  `Φ(η) = ⟨ξ | N_I ξ⟩ + Σ_q c_q(η⋆η*)(log q)` DOES carry an archimedean term and prime terms in one
  expression.* **But the primes enter ONLY through their addresses `log q`.** ### **There is no local
  factor at `p`, no completion `ℚ_p`, and no place-set parameter anywhere in the object.** *An archimedean
  term plus prime distances is not a semilocal trace, and calling it one would be the place-content homonym
  committed inside the corpus's own instrument.*
* ### **(iii) HALF, AND FROM THE OPPOSITE SIDE TO CC's GAP.** *The spectral count IS the matching number —
  `DERIVED` for the pure shift-graph form, `BENCH-GRADE` for the full operator at one offender across
  `r = 1…8`.* **The trace-is-the-floor half has no counterpart: there is no place side for a floor to live
  on.**
* **(iv) ABSENT.** ### **The bench has no place to add.**

### **THE EMPTY COLUMN, NAMED**

> ### **`(iv)` IS EMPTY ACROSS BOTH CANDIDATES, AND `(iii)` IS HALF-FILLED FROM OPPOSITE SIDES.** *CC has
> the place column natively and no identification; the corpus's bench has the address column natively and
> half the identification.* ### **NEITHER HAS BOTH, AND THE MISSING PIECE IS THE SAME ONE IN BOTH CASES —
> exactly the shape the carrier build found on its own sheet, one road over.**

### **AND THE MEASUREMENT THAT ARGUES THE BENCH IS ALREADY "TWO OBJECTS GLUED" BY `B1`'s OWN TEST**

*This week measured that the archimedean part `A_main` contributes* ### **at most ONE offender** *to a count
of thousands, at every window from `r = 1.7` to `r = 8`, while the prime part's matching number supplies
essentially all of it.*

> ### **THE TWO PARTS OF `Φ` ARE NEARLY DECOUPLED IN THE QUANTITY OF INTEREST.** *By `B1`'s test that is the
> signature of two objects glued, not one object — and the evidence for it is the corpus's own measurement,
> not an argument.* **Filed as the sheet's least comfortable row, because the apparatus is ours.**

### **THE READ-DEBTS, NAMED RATHER THAN GRADED AROUND**

| item | status | why it grades nothing here |
|:--|:--|:--|
| ### **CC's `[19]`** | ### **NOT A DOCUMENT IN THIS CORPUS** | *it is a reference number inside someone else's list; the name-identity law says a pointer is not a document until an ID or a title says otherwise* |
| **CC 1999, the semilocal trace formula** | `10.1006/jnth.1999.2392`, ### **`TITLE-UNVERIFIED`, not read at content** | named in `Spectral_Inertness` as a reinterpretation in operator-algebraic language; **that is a corpus sentence about it, not a read of it** |
| **the CC archimedean / semi-local positivity line** | ### **UNREAD — API `429` twice, declared both times** | the corpus's own record calls it *"the single most load-bearing unread item on this face"* |
| **`2106.01715` body** | read at ### **ABSTRACT only**; the `ζ`-cycle is `PARTIALLY` delivered | the definition is in the body, which was not fetched |

---

## §4 — `B3`: ### **THE FIRST CONSTRUCTION MOVE, NAMED AND NOT EXECUTED**

### **FIRST, THE MOVE THAT LOOKS OBVIOUS AND IS REFUSED, WITH THE CORPUS'S OWN REASON**

*The tempting move is to identify the bench's window with CC's circle:* `L = 2 log S` *is CC's circle length
and it is the corpus's own window width —* ### **the THIRD independent parameter correspondence**, *after
`√2` at the proven boundary and `p ≤ λ²` at the Euler content.*

> ### **THE CORPUS ALREADY REFUSED THIS MERGE, AND THE REASON DOES NOT WEAKEN WITH REPETITION:** *"Their
> circle and our test-function window are different objects; what recurs is the arithmetic of the parameter.
> **NO PROMOTION — no text identifies the constructions, and three coincidences of coordinate are three
> coincidences of coordinate."*** *And its sharper form:* ### ***"a fourth would be no more probative than
> the third, because what recurs is the ARITHMETIC OF A WINDOW, and any two people parameterising the same
> window will agree about it."***
>
> ### **SO THE FIRST MOVE MUST BE ONE WHOSE SUCCESS WOULD NOT BE A FOURTH COORDINATE COINCIDENCE. IT HAS TO
> BE ABOUT A THEOREM, NOT ABOUT A PARAMETER.**

### **THE MOVE, STATED**

> ### **GIVE THE CORPUS'S OWN OBJECT A FREE PLACE-SET PARAMETER, AND ASK WHETHER IT HAS CC's STABILITY
> PROPERTY.**
>
> *Define* `Φ_{L,S}` *— the same form on the window of length `log L`, with the lag terms restricted to a
> **freely chosen** finite set `S` of primes rather than to all prime powers below `L`.* **Then the two
> operations `B1(iv)` asks about both exist on one object:**
>
> * ### **widening support** = increasing `L` *(the forced content changes)*
> * ### **adding a place** = enlarging `S` *(a free choice, exactly the shape of CC's place set)*
>
> ### **AND THE QUESTION IS A THEOREM, NOT A COORDINATE: IS THE COUNT STABLE UNDER ENLARGING `S`?**

### **WHY THIS IS THE SMALLEST OBJECT THAT COULD CARRY BOTH COLUMNS**

*It adds nothing to the apparatus except a restriction map. It imports no local factor, claims no
completion, and does not identify anything with anything.* ### **It manufactures the ONE structural feature
`(ii)` has that `(i)` lacks — a freely chosen finite set that can be increased — and then asks whether the
theorem that governs that set on CC's side governs it here.**

### **BOTH BRANCHES, LONGHAND, BEFORE ANYTHING RUNS**

* ### **IF THE COUNT IS STABLE UNDER ENLARGING `S`** *(at fixed `L`, past some point)*: the corpus's object
  and CC's semilocal object share a **structural theorem** and not merely a coordinate. **That would be the
  first non-coincidental agreement between the two frames**, and it would make `(iv)` a live question rather
  than a closed one — *the two operations would at least be of the same kind.* ### **It would still not be
  `(iv)`, and this sheet would say so.**
* ### **IF THE COUNT IS NOT STABLE UNDER ENLARGING `S`**: the bench's object and CC's differ **at a
  theorem**, not at a parameter. **`(iv)` is refuted for this pair**, and the honest reading is that the
  address graph is not a semilocal object in disguise — *which is a negative result about the pair and is
  worth exactly as much as the positive one.*

### **AND THE EVIDENCE ALREADY IN HAND POINTS ONE WAY — WHICH IS RECORDED HERE SO THE MOVE CANNOT LATER BE
CLAIMED AS A SURPRISE**

*2026-08-17 measured the effect of enlarging the lag set from `{log 2}` to `{log 2, log 3}`:*

| `L` | one lag | two lags | ### **effect of adding the second prime** |
|:--|--:|--:|--:|
| ### **9** | `0.369763` | `0.499545 / 0.500000` | ### **`+285 / +143` offenders** |
| 12 | `0.442834` | `0.442432` | `−1 / +0` offenders |

> ### **AT `L = 9` ADDING A PRIME NEARLY DOUBLES THE COUNT. THAT IS NOT STABILITY.** *So the registered
> expectation is* ### **NOT STABLE** *— the second branch — and the move is worth running anyway, because
> the interesting question is no longer whether it is stable but* ### **whether the instability is confined
> to the windows where the added prime sits at its own apex** *(`9 = 3²`, the open question the charter's
> `§5` already carries with its two readings and its priced cells).*
>
> ### **THE MOVE IS NAMED AND NOT RUN. Registering it against the evidence that predicts its answer is what
> makes running it worth anything.**

---

## §4-bis — `B3` RAN · ### **VERDICT: NOT STABLE — AND NOT EVEN MONOTONE** *(2026-08-17)*

**The move was run as registered:** `Φ_{L,S}` on `V`, `S` a freely chosen set of primes enlarged at fixed
support, at `L = 9, 12, 16, 27, 32` and `ω = 10⁻³`.

| `L` | steps in `NPOS` as `S` grows `{2} → {2,3} → {2,3,5} → {2,3,5,7} → {…,11}` | monotone? |
|:--|:--|:--|
| **9** | ### **`+474`** `−80` `−1` `+0` | ### **no** |
| **12** | ### **`+616`** `−94` `+22` `−6` | ### **no** |
| **16** | ### **`+751`** `−116` `+44` `+2` | ### **no** |
| **27** | ### **`+534`** `+14` `+16` `−3` | ### **no** |
| **32** | ### **`+455`** `+57` `+2` `+9` | yes |

> ### **THE COUNT IS NOT STABLE UNDER ENLARGING `S` AT ANY WINDOW TESTED.** *Every `L` has a first step of
> `+455` to `+751` offenders. The only near-stable steps are at `L = 9`'s tail, where the admitted primes
> contribute no new fitting lag.*
>
> ~~### **AND AT FOUR OF THE FIVE WINDOWS IT IS NOT EVEN MONOTONE — ENLARGING THE PLACE SET CAN *DECREASE*
> THE COUNT.**~~ ### **WITHDRAWN AS A STRUCTURAL CLAIM, 2026-08-17, BY THE WEIGHT CONTROL THE CAVEAT
> ORDERED. See `§4-ter`.** *The step PATTERN — which steps fall, and by how much — is coefficient-dependent
> even among generic weights. What survives is that the count MOVES, not the shape of the movement.*

### **THEREFORE COLUMN `(iv)` IS PRICED EXACTLY, WHICH IS WHAT THE REGISTRATION SAID THIS BRANCH WOULD BUY**

| operation | effect on the count |
|:--|:--|
| **widening support** *(increase `L`)* | the sawtooth — derived for the pure one-lag form, measured at `r = 1 … 8` |
| ### **adding a place** *(enlarge `S`)* | ### **hundreds of offenders, in BOTH directions, with no derived law** |

> ### **TWO OPERATIONS OF DIFFERENT KINDS ON ONE OBJECT. THE CORPUS'S INSTRUMENT IS CONFIRMED "TWO OBJECTS
> GLUED" BY `B1`'s OWN TEST — and this is an honest MEASURED distance from `T2`, not an argument for one.**

### **AND THE RUN'S OWN BLOCKING ROW FIRED, WHICH IS WHY THE FIRST `μ` COLUMN IS WITHDRAWN IN FULL**

*`P2` registered that `NPOS` tracks `μ` to within three offenders. It came back at* ### **`−406` on the first
line** *and the run was stopped there.* **The cause:** *the matching routine assumed INDEX PARITY is a
bipartition — true when every live address `k_q` is odd, and false as soon as a prime power is admitted:*

| `q` | `log 2` | `log 3` | ### **`2 log 2`** | `log 5` | ### **`log 7`** | `3 log 2` | ### **`log 11`** |
|:--|--:|--:|--:|--:|--:|--:|--:|
| `k_q` at `ω = 10⁻³` | 693 | 1099 | ### **1386 (EVEN)** | 1609 | ### **1946 (EVEN)** | 2079 | ### **2398 (EVEN)** |

**A proper 2-colouring audit** *(`relay` `tools/e16/b3_bipartite_audit.py`)* **finds every one of the 25 cells
NOT BIPARTITE**, so no `μ` is computed for any of them — a general maximum matching needs blossom
contraction, which is not in this instrument and is ### **declared rather than approximated.**
*The `NPOS` column is an eigenvalue count of a well-defined symmetric matrix and is unaffected; it is the
column that survives, and every number above is from it.*

> ### **THE NON-MONOTONICITY AND THE BIPARTITENESS AUDIT REACH THE SAME CONCLUSION BY TWO INDEPENDENT
> ROUTES:** *these are not matching numbers.* **Which is itself the scope limit the week's derived law
> needed and did not have.**

**One caveat, named:** *weight-independence was established for the pure form in the bipartite regime.*
### **It is NOT established here, so part of the movement may be coefficient-driven.** *The check is a
re-run at unit weights; it is named and not run.*

**And a numbering note so two figures are not read as contradicting:** *the earlier `+285` at `L = 9` compared
`{log 2}` against `{log 2, log 3}`.* **`B3`'s `+474` compares `S = {2}` — which admits `log 2`, `2 log 2` and
`3 log 2` — against `S = {2,3}`.** *Different objects, both correct.*

---

## §4-ter — ### **THE WEIGHT CONTROL: `W1` FAILS, AND HALF THE VERDICT IS WITHDRAWN** *(2026-08-17)*

**The caveat named with `B3` was run.** *And the comparison was widened from two weightings to three, because
the corpus's own measurement makes the two-way version confounded:* ### **unit weights are the KNOWN
DEGENERACY** *(`registered = Weil = 1098`, `unit = 889` with nullity `419`, at `L = 9`).* **So the decisive
test is registered vs Weil — both generic, ratios spanning `1.00×` to `5.50×`.**

| `L` | registered | Weil | ### **unit** |
|:--|:--|:--|:--|
| **9** | `+474 −80 −1 +0` | `+516 −144 +3 +0` | ### **`−1 +319 −6 +0`** |
| **12** | `+616 −94 +22 −6` | `+586 −80 +8 +1` | `+287 +139 +10 +6` *(monotone)* |
| **16** | `+751 −116 +44 +2` | `+756 −112 +22 −9` | `+571 −9 +26 +0` |
| **27** | `+534 +14 +16 −3` | `+435 −35 +34 −16` | `+476 +12 +28 −13` |
| **32** | `+455 +57 +2 +9` *(monotone)* | `+357 −32 +31 −5` | `+413 +47 +31 −3` |

> ### **`W1` FAILS. THE SIGNS DISAGREE AT EVERY WINDOW, WITH STEP DIFFERENCES UP TO `99` OFFENDERS** *(`L = 9`:
> `42 64 4 0` · `L = 27`: `99 49 18 13` · `L = 32`: `98 89 29 14`)*. **Registered and Weil are both generic and
> they do not agree on the shape.**

### **WHAT IS WITHDRAWN, AND WHAT SURVIVES**

| | |
|:--|:--|
| ### **WITHDRAWN** | *"not even monotone at four of five windows — enlarging the place set can DECREASE the count"* — ### **a statement about the (object, weighting) pair, not about the place set.** *Non-monotonicity is TYPICAL — `13` of the `15` `(L, weighting)` cells — but it is not invariant, and a typical tendency is not a structural fact.* |
| ### **SURVIVES, AND ROBUSTLY** | ### **NOT STABLE.** *At every window and under every one of the three weightings, some step moves the count by hundreds* — `+319` at worst-case `L = 9`/unit, `+287` at `L = 12`/unit, and `+357` to `+756` everywhere else. **The place-set parameter is not inert under any convention tested.** |

> ### **SO COLUMN `(iv)`'s PRICING STANDS AND ITS SHARPEST FORM DOES NOT.** *Widening support moves the count
> by the sawtooth; enlarging the place set moves it by hundreds under every convention.* **Two operations of
> different kinds — that survives.** ### **"In both directions" was a registered-weight statement and is now
> labelled as one.**

### **AND THE FINDING THE CONTROL PRODUCED THAT NOBODY ASKED FOR**

*`W2`: unit differs from registered at* ### **25 of 25 cells.** *And `W1`'s failure says the two GENERIC
conventions differ too.*

> ### **ON THESE LAG SETS THE COUNT IS WEIGHT-DEPENDENT — WHICH IS EXACTLY THE OPPOSITE OF THE BIPARTITE
> REGIME, WHERE IT WAS DERIVED TO BE WEIGHT-INDEPENDENT.** *The two regimes are separated by the same line
> the `μ` column was withdrawn on:* **bipartite (all addresses odd) ⟹ inertia is a matching number and the
> weights cannot move it; non-bipartite (a prime power admitted) ⟹ the weights move it, by up to `99`
> offenders per step.**
>
> ### **THE WEEK'S SILENCE-ON-WEIGHTS IS THEREFORE SCOPED, NOT REFUTED: it is a theorem about the bipartite
> regime and it does not travel.** *Filed on the sheet and cross-linked to the two-class inventory.*

---

## §5 — `B4`: ### **CHECKPOINT — WHAT RETURNS FOR THE AUTHOR'S WORD**

1. ### **`B1` IS WRITTEN, AND ITS HARDEST COLUMN IS `(iv)`** — with a named tension carrying a theorem on
   each side, and the honest note that the two theorems are about different objects and do not contradict
   each other.
2. ### **`B2` GRADES TWO CANDIDATES AND THE EMPTY COLUMN IS `(iv)`, WITH `(iii)` HALF-FILLED FROM OPPOSITE
   SIDES.** *And the sheet's least comfortable row is ours: the bench's two parts are nearly decoupled in
   the quantity of interest, which by `B1`'s own test is the signature of two objects glued.*
3. ### **`B3` NAMES A MOVE THAT IS ABOUT A THEOREM RATHER THAN A COORDINATE**, refuses the `L = 2 log S`
   merge with the corpus's own standing reason, and records the evidence that predicts its answer.
4. ### **FOUR READ-DEBTS ARE NAMED AND GRADE NOTHING.**

**No attempt is opened. No mechanism is proposed. No candidate is promoted. `h2` UNCHANGED. NO SIGN.
NOTHING DEPOSITS.**

---

## §6 — `B5`: ### **THE ARCHIMEDEAN PLACE FOR `Z` — THE EMPTY HALF OF COLUMN `(ii)`, ASKED DIRECTLY** *(2026-08-17, ninth sitting; author-ruled; corpus-first; both branches longhand; ### NO SIGN SENTENCE ANYWHERE IN THIS SECTION)*

### `§6(a)` — THE COMPLETION: ### **THE `Γ`-FACTOR IS CLASS-SILENT — A SCALAR ON `ℂ[Cl]` — SO THE ARCHIMEDEAN PLACE ENTERS NATIVELY**

**The question:** what is the `ℂ[Cl]`-valued analogue of the completed `ξ_K`? **The answer is at cite from
the completions the corpus already holds** *(the addendum's `S3a`/`S3b` rows: continuation + FE present for
both bases)*: each `L(s,ψ)` for a class-group character of imaginary quadratic `K` is unramified with
**trivial `∞`-type**, so every one of the `h` components carries ### **the SAME archimedean factor**
`γ_∞(s) = (|d_K|/4π²)^{s/2}·Γ(s)` — *the `Γ`-factor depends on the signature only, never on the class*
(`STANDARD-AT-TEXT`; for `ζ_K` itself this is its classical completion).

> ### **A DIAGONAL OPERATOR WITH EQUAL EIGENVALUES IS A SCALAR — LONGHAND AND IMMEDIATE. SO
> `Ẑ(s) = γ_∞(s)·Z(s)` WITH `γ_∞` SCALAR-VALUED: THE ARCHIMEDEAN PLACE IS CLASS-SILENT.** *It enters the
> class-resolved object natively — nothing is imported — and it carries `s`-analytic information while
> carrying ZERO `ℂ[Cl]`-information.*

**Both branches, longhand.** ### **IF CLASS-SILENT** *(the branch at cite, this `K`)*: the completion half
of `(ii)` fills natively, **and a tension is exposed and cross-linked rather than absorbed:**
`THE_CARRIER_BUILD` `R3` demands `arch_independent` — *the archimedean coordinate must carry information
the finite ones do not* — **and on `Z` the archimedean coordinate is exactly the coordinate that carries
none of the class resolution.** *Graded tension, filed, no promotion.* ### **IF `∞` WERE CLASS-RESOLVING**:
it would need `∞`-types varying with `ψ`, which unramified-everywhere characters do not have at cite — **the
branch closes at the citation, with one named door: ray-class refinements (signature data at real places;
vacuous for imaginary `K`) — named, not chosen.** *Scope said plainly: the class-silence of `∞` is a fact
about totally imaginary `K` at trivial `∞`-type, not a universal.*

### `§6(b)` — SELF-DUALITY UNDER `s ↦ 1−s` ∘ ANTIPODE: ### **YES, COEFFICIENT-WISE AT CITE, TWISTED BY THE ROOT-NUMBER ELEMENT**

*The `ψ`-wise functional equations* `Λ(s,ψ) = W(ψ)·Λ(1−s,ψ̄)` *(at text)* *assemble, via the antipode's
`e_ψ ↦ e_ψ̄` (longhand, `§2.2`), into:*

> ### **`Ẑ(s) = W · Ẑ(1−s)^∗`, where `W = Σ_ψ W(ψ)·e_ψ ∈ ℂ[Cl]` IS THE ROOT-NUMBER ELEMENT.**
> *And the involution closes, longhand: applying the relation twice forces `W·W^∗ = 1`, which holds because
> `W(ψ̄) = W̄(ψ)` and `|W(ψ)| = 1` (at text) give `W·W^∗ = Σ_ψ |W(ψ)|²·e_ψ = 1`.*
> ### **THE COMPLETED CLASS-RESOLVED OBJECT IS SELF-DUAL UNDER THE ANTIPODE-INVOLUTION, UP TO A UNITARY
> ELEMENT OF THE GROUP ALGEBRA.** *The involution `L1` demands is present on `Ẑ` — as structure, natively.*

### `§6(c)` — THE `τ`-PAIRING QUESTION: ### **WHAT THE FORM IS, WHETHER IT IS HERMITIAN, AND WHAT ITS POSITIVITY WOULD ASSERT — SAID PLAINLY**

**What it is.** For a test function `g`, the `ℂ[Cl]`-valued Weil-type functional is
`W_Z(g) = Σ_ψ W_ψ(g)·e_ψ` — *each `W_ψ` the classical explicit-formula functional of `L(s,ψ)`, at text.*
**The two scalarizations, computed longhand** *(`ε(e_ψ) = δ_{ψ,ψ₀}`; `τ(e_ψ) = 1/h`)*:

| functional | the form | what ledger it is |
|:--|:--|:--|
| `ε` | `W_{ψ₀}(g)` | ### **the `ζ_K` ledger alone — augmentation-silent scalarization, reads one component** |
| ### **`τ`** | `(1/h)·Σ_ψ W_ψ(g)` on scalar `g`; on **class-resolved** test data `g = Σ g_C[C]` it diagonalizes by Parseval into `(1/h)·Σ_ψ W_ψ` **evaluated component-by-component** (`τ(f g^∗) = (1/h)Σ_ψ f̂(ψ)·ĝ̄(ψ)`, longhand) | ### **the HECKE-SIDE LEDGER, CHARACTER BY CHARACTER — jointly, the `ζ_H` explicit formula** |

**Hermitian under the antipode-involution: YES at the algebra level, longhand** *(`τ(g f^∗) = τ(f g^∗)̄` —
Parseval)*, *with each component `W_ψ` Hermitian classically (at text).*

> ### **WHAT ITS POSITIVITY WOULD ASSERT — SAID PLAINLY, SO NOBODY READS A CONSTRUCTION AS A RESULT:**
> *positivity of the `ψ`-component form is, by Weil's criterion (at text; the corpus's Weil-1952 register),*
> ### **GRH FOR THAT `L(s,ψ)`. All components jointly: GRH for `ζ_H`. The `ψ₀`-component alone: RH for
> `ζ_K`.** ### **CONSTRUCTING THE FORM MOVES NONE OF THESE — the form's existence is classical, and its
> positivity is exactly as open as the hypotheses it restates. NOTHING HERE IS EVIDENCE ABOUT ANY OF THEM,
> AND NOTHING HERE BEARS ON THE SIGN OF `W_∞ − W_2`.**

### `§6(d)` — ### **VERDICT ON COLUMN `(ii)`, RE-FILLED — AND THE TRACE HALF IS STILL ABSENT, SAID WHICH**

| `(ii)`'s halves | before `B5` | ### **after `B5`** |
|:--|:--|:--|
| **the completions** | "NO ARCHIMEDEAN PLACE AT ALL" | ### **NATIVE — `γ_∞` scalar, class-silent; enters without import; `Ẑ` self-dual under the antipode-involution up to the unitary root-number element** |
| ### **the trace** | absent | ### **STILL ABSENT.** *`§6(c)`'s form is a LEDGER — an explicit-formula functional over zeros and primes — NOT the trace of an operator on a space. `(ii)` demands Sonin-type positivity over completions: an operator-trace object. Turning the ledger into a trace is the field's own named open question (the suitable Weil cohomology / Lefschetz reading — `THE_CARRIER_BUILD` `§7`, at cite), and claiming it here would claim that question solved.* |

> ### **SO: THE ARCHIMEDEAN PLACE ENTERS `Z` NATIVELY (CLASS-SILENT SCALAR), THE INVOLUTION IS PRESENT ON
> THE COMPLETED OBJECT AS STRUCTURE — AND THE PLACE COLUMN'S ANALYTIC HALF REMAINS THE EMPTY HALF: `(c)`
> SUPPLIES A LEDGER, NOT A TRACE.** *`§2.1`'s `(ii)` row is superseded in part by this section and carries
> a pointer; `(iii)` remains the empty column entirely.*

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. THE ATTEMPT GATE WAS NOT APPROACHED. `h2`
UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

---

## §7 — `B6`: ### **THE ARCHIMEDEAN TRACE LIFTED TO `ℂ[Cl]`** *(2026-08-17, tenth sitting; author-ruled; Rule 3 applied on its first outing; ### NO SIGN SENTENCE ANYWHERE IN THIS SECTION)*

> ### **RULE-3 LOG, AT THE HEAD:** *identities used — `Ẑ = γ_∞·Z` (`§6(a)`, banked) · the `τ`-form Hermitian
> under the antipode-involution (`§2.2`/`§6(c)`, banked) · **the linearity of CC's compressed scaling
> action `ϑ` and Sonin projection `S` in the test function: `NAVIGATOR-ASSERTED`** — the ferry cites
> Selecta Prop 5.5 / Thm 4.7, and the `BIBLIOGRAPHY`'s own `§4-bis` grade forbids quoting or grading any
> sentence of Selecta until it is read.* **Corpus-first anchor instead: the banked trace object
> `Tr(ϑ(g)PP̂Pϑ(g)*)` (`THE_ATTEMPT_RECORD` `§3` row 7, `§4`) — used linearly throughout the corpus's own
> instrument work — and blockage 7's disposition: ### "the sandwiched form is a NORM."** *Banned-list grep
> on the ferry: clean; no new term coined.*

### `§7(a)` — THE LIFT: ### **THE CERTIFIED ARCHIMEDEAN TRACE EXTENDS COMPONENTWISE, AND `τ` MAKES IT CLASS-SEPARATING**

*`ℂ[Cl]`-valued test functions are a finite direct sum of `h` scalar copies (Fourier on `Cl`, longhand:
`g = Σ_ψ ĝ(ψ)e_ψ`).* ### **IF `g ↦ ϑ(g)` is linear and `S` is a fixed projection — the `NAVIGATOR-ASSERTED`
premise — the construction extends componentwise, canonically; and `γ_∞`'s class-silence (`§6(a)`, banked)
means ONE archimedean structure serves every component, so nothing `ψ`-dependent has to be built at `∞`.**
*Then the archimedean trace on the lifted object is*

> ### **`Tr_τ(g) = (1/h)·Σ_ψ Tr(ϑ(g_ψ)·S·ϑ(g_ψ)*)` — `h` COPIES OF THE CORPUS'S OWN CERTIFIED ARCHIMEDEAN
> FORM, EACH STRUCTURALLY NON-NEGATIVE (a sandwiched `Tr(AA*)` is a norm — blockage 7's banked
> disposition), AND CLASS-SEPARATING THROUGH `τ` (each component carries its own test data).**
>
> *Normalization flagged, not smoothed: the ferry's formula omits the `1/h`; `τ(e_ψ) = 1/h` puts it there.
> A constant; recorded per Rule 3.*
>
> ### **AND THE POSITIVITY GUARD, ON THE RESULT'S FACE: archimedean-only positivity is STRUCTURAL — a norm
> is non-negative by construction — and bears NOT AT ALL on `h2` or the sign of `W_∞ − W_2`, which live at
> the JOINT object.** *The loom's concordance row stands: partial positivities, stalling at the same global
> wall. Nothing here moves it.*

### `§7(b)` — ### **THE GAP, RESTATED IN ONE SENTENCE ON THIS SHEET'S FACE**

> ### **WITH THE ARCHIMEDEAN TRACE LIFTED, COLUMN `(ii)`'s REMAINING ABSENCE IS THE FINITE-PLACE TRACE —
> ONE SONIN-TYPE POSITIVE LOCAL TRACE PER PRIME IDEAL `𝔭` — THE OBJECT CC's SEMILOCAL STRATEGY NAMES
> (`{∞, 2, …, p}`, `2310.18423` at content) AND NO ONE HAS BUILT.**

### `§7(c)` — THE IDENTIFICATION PROBE — ### **REGISTERED BEFORE RUNNING, ONE CELL, BENCH-GRADE**

**Instrument:** `relay` `tools/e16/b6_class_probe.py` · registration banked at
`data/b6_registration_2026-08-17.txt` **before any measured number** · run banked at
`data/b6_2026-08-17.txt`. **The lift, minimal and declared:** lag set = prime-IDEAL powers of
`K = ℚ(√−23)` with norm `< L` at address `log N`; class multipliers `2cos(2πjak/3)` per component
(`ψ` and `ψ̄` give the same real form — two distinct components); coefficients = the bench's registered
convention lifted per ideal (`C_K = 2√(N𝔮)·log N𝔭`, Weil weight as the beside-sensitivity); **`A_main`
unchanged** *(declared: `§6(a)`'s class-silence means one scalar archimedean part serves every component;
a `K`-native `Γ_ℂ` density is a named variant, not run)*; `V` imposed everywhere; the scalar known-answer
gate blocks the run. **Cell:** `L = 3.5`, `ω ∈ {2·10⁻³, 10⁻³, 5·10⁻⁴}` — live norms `2, 3`, both split,
both non-principal (longhand in the registration), addresses both odd, **bipartiteness audited in-run, not
assumed.** **Registered:** `P1` — pure counts equal across components (the bipartite law covers signed
nonzero weights); `P2` — `|full − pure| ≤ 1` per component (the scalar precedent's margin), both branches
longhand in the registration, `ψ₁` the stress case (globally negative lag terms, never measured on the
scalar bench).

**OUTCOME** *(run banked `data/b6_2026-08-17.txt`; repair banked `data/b6_mu_repair_2026-08-17.txt`)*:

> ### **THE GATE PASSED (3/3 + basis cross-check identical), THE CELL IS BIPARTITE AT ALL THREE `ω`
> (audited), AND THE PROBE'S FIRST PURE COLUMN WAS WITHDRAWN BY ITS OWN ARITHMETIC:** *the unfloored counts
> gave `ψ₀ + ψ₁ > dim` on exactly proportional matrices — impossible — so the lag-only kernel was being
> split by floating noise. **The registration lacked the corpus's own floor-sweep discipline
> (`F.2026-08-17e`); the defect is the executor's, caught in-pass, and the repair is banked:** the exact
> matching number `μ` (Hopcroft–Karp on the audited bipartition) and floored pure counts.*
>
> | `ω` | `M` | ### **`μ` (exact)** | pure `ψ₀` / `ψ₁` (floored) | ### **FULL `ψ₀` / `ψ₁`** | \|full − μ\| |
> |:--|--:|--:|:--|:--|:--|
> | `2·10⁻³` | 626 | **279** | 279 / 279 | **279 / 279** | ### **0 / 0** |
> | `10⁻³` | 1253 | **560** | 560 / 560 | **560 / 560** | ### **0 / 0** |
> | `5·10⁻⁴` | 2506 | **1120** | 1120 / 1120 | **1120 / 1120** | ### **0 / 0** |
>
> ### **`P1` HOLDS AT THE FLOORED READING** *(components equal, and equal to `μ` — the derived law confirmed
> on the class-labelled lift)*, ### **AND `P2` LANDS ON BRANCH A SHARPER THAN REGISTERED: THE AGREEMENT IS
> EXACT — ZERO OFFENDERS, NOT ONE — PER COMPONENT, AT EVERY `ω`, UNDER BOTH COEFFICIENT CONVENTIONS,
> INCLUDING THE `ψ₁` STRESS CASE (globally negative lag terms).** *The full operator's count is also
> component-INDEPENDENT at this cell — a measured fact consistent with bipartite weight-silence extending
> to the full operator here, noted as one cell's observation and nothing more.*
>
> ### **BENCH-GRADE, ONE CELL, PROVED NOWHERE. The identification mechanism (full tracks `μ`) survives the
> class lift at the first cell it was asked of. It says nothing beyond that cell, nothing about `h2`, and
> nothing about any sign.**

### `§7(d)` — ### **`B1` RE-GRADED AGAINST THE LIFTED OBJECT** *(`Ẑ` with the `τ`-trace)*

| column | grade on the lifted object |
|:--|:--|
| **(i) address side** | ### **FILLED AT CLASS RESOLUTION** *(banked, `§2.1`)* — the data native, the graph instrument-derived |
| ### **(ii-∞)** | ### **FILLED, AT `§7(a)`'s GRADE** — the certified archimedean trace lifts componentwise; structurally non-negative; class-separating through `τ`; *the linearity premise `NAVIGATOR-ASSERTED`, the read-debt named* |
| **the involution** | ### **FILLED — native on `Ẑ`** *(`§6(b)`: antipode-twisted reflection, unitary root-number element)* |
| ### **(ii-finite)** | ### **ABSENT — THE GAP OF `§7(b)`, now the place column's ONLY absence** |
| **(iii) identification** | ### **ABSENT — named; `§7(c)` is its first bench cell on the lift** |
| ### **(iv) joint move** | ### **RESTATED: on the lifted object, widening support moves CONTENT (which ideals enter the norm-truncation) while adding a place would move STRUCTURE (which completions carry a trace) — and the second operation DOES NOT EXIST until `(ii-finite)` is built.** *So `(iv)` is DOWNSTREAM of `(ii-finite): the joint move cannot even be ASKED of the lifted object until the finite-place trace exists.* **The column's whole weight now rests on one named absence.** |

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. THE ATTEMPT GATE WAS NOT APPROACHED. `h2`
UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

---

## §8 — `B7`: ### **THE FINITE-PLACE TRACE** *(2026-08-17, eleventh sitting; author-ruled, full perspective; run on FABLE per the routing; ### NO SIGN SENTENCE ANYWHERE IN THIS SECTION)*

> ### **RULE-3 LOG, AT THE HEAD:** *identities at cite — spec `§6`–`§7` (banked, this document) · the
> `2310.18423` abstract VERBATIM (banked, `OPEN_TRAILS`, the pointer-length-check read).* ### **ONE CLAUSE
> OF THE FERRY'S OWN RULE-3 LINE FAILS THE CHECK: "CC Selecta introduction strategy sentence, banked" — NO
> SELECTA SENTENCE IS BANKED ANYWHERE IN THIS CORPUS** *(the `§4-bis` grade stands: `NAVIGATOR-SUPPLIED`,
> no sentence quotable until read)*. **The `Tr_F = Tr_∞ + Σ_𝔭 Tr_𝔭` target shape is therefore carried as
> the FERRY's shape, `NAVIGATOR-ASSERTED`, anchored corpus-side only on the banked semilocal frame** *("a
> formalism over a finite set of completions `{∞, 2, …, p}`", `§3`'s `B2` row; the stability sentence
> verbatim).* *Banned-list grep on the ferry: clean; no term coined.*

### `§8.1` — ### **THE SPECIFICATION, WRITTEN BEFORE ANY CANDIDATE** *(the columns a candidate `Tr_𝔭` must fill)*

**For a prime ideal `𝔭` of `K`, norm `q`:**

| | column | what the functional must satisfy |
|:--|:--|:--|
| ### **(a)** | ### **SONIN-TYPE AT `𝔭`** | *built from a subspace of local functions on `K_𝔭` vanishing TOGETHER WITH THEIR LOCAL FOURIER TRANSFORM on a compact set — the archimedean definition transposed* **(the archimedean anchor is the banked parts list: Sonin's space · the compression of the scaling action `ϑ` · the projection `S` · the support cutoff · the Fourier vanishing conditions)** |
| ### **(b)** | ### **CLASS-SEPARATING THROUGH `τ`** | on `ℂ[Cl]`-valued data the functional must not factor through the augmentation — *the `§2` rider, now a column* |
| ### **(c)** | ### **CARRIES WEIGHT** | *its value on the lag-`log q` correlation must scale with the prime's explicit-formula coefficient* — **because the archimedean trace is COEFFICIENT-SILENT: the phase boundary (`§1-bis`) put the weights on the trace side, and `§7(a)`'s lifted `Tr_∞` reads the test function, never the prime's coefficient.** *A finite-place trace that is also weight-silent would leave the weights carried by NOTHING, and `h2` is a weight-dependent property (the two-class inventory's sentence).* |
| ### **(d)** | ### **ENTERS AS A SUMMAND** | `Tr_F = Tr_∞ + Σ_{𝔭∈F} Tr_𝔭` *over the finite place set `F` — the ferry's target shape, `NAVIGATOR-ASSERTED` at its CC provenance (Rule-3 log above), corpus-anchored on the banked semilocal frame* |

> ### **AND THE COLUMN THE SPECIFICATION ITSELF EXPOSES, STATED BEFORE ANY CANDIDATE IS NAMED: (c) IS THE
> HARD ONE.** *(a) transposes a definition; (b) transposes the rider; (d) is a shape. But (c) demands the
> ONE thing this week's phase boundary showed the structural side cannot supply — the weights — and demands
> it of a POSITIVE functional.* **A candidate that fills (a), (b), (d) and is weight-silent has built
> another archimedean-type trace at a finite address, not the thing that pays for a prime.**

### `§8.2` — ### **THE READ-DEBT DISCHARGED: `2310.18423` BODY, TWO FETCH MODES — AND THE FIELD'S OWN THEOREM SAYS THE SONIN SPACE IS ARCHIMEDEAN-RIGID**

**Retrieval record:** *the arXiv abs page re-verified the banked abstract VERBATIM (a live match against the
pointer-length-check read — the banked copy is exact); the HTML full text (`arxiv.org/html/2310.18423v2`)
delivered Theorem 2 verbatim; `§§4.5–4.6`'s defining conditions were TRUNCATED in one mode and returned
GLOSS-INCONSISTENT in the other (the vanishing direction flipped between fetches) —* ### **per the
retrieval-gloss law, only the quoted fragments are inherited and the `§4.5` local definition remains a
named partial-read caveat.**

**What is now held at content, quoted:**

> ### **THEOREM 2, VERBATIM: "Let `S` be a finite set of places with `∞ ∈ S` and `λ > 0`. Then the map
> `θ_S` induces a hilbertian isomorphism of the Sonin spaces `θ_S: 𝐒_λ(ℝ, e_∞) → 𝐒_λ(X_S, α)`."**
>
> ### **AND THE STRATEGY SENTENCE — WHICH LIVES IN THIS PAPER'S INTRODUCTION, NOT IN SELECTA (the
> navigator's Rule-3 line corrected on this point):** *"the Sonin space … should, when suitably equipped
> with a prolate type operator, play the role of the sought for Weil cohomology."*

> ### **THE THEOREM'S CONSEQUENCE, DERIVED LONGHAND FROM THE VERBATIM STATEMENT: FOR EVERY FINITE `S` THE
> SEMILOCAL SONIN SPACE IS ONE HILBERTIAN COPY OF THE ARCHIMEDEAN ONE — THE FINITE PLACES CONTRIBUTE NO
> INDEPENDENT SONIN DIRECTIONS.** *"Stability under increase of `S`" is the corollary (all the spaces are
> isomorphic to the same archimedean space), and the corpus's banked reading of the stability line —
> "the place axis is INERT" (`§1`'s tension table) — is not merely confirmed but EXPLAINED: the place axis
> is inert because the Sonin mechanism is archimedean-rigid.*

**`§8.1`'s columns asked of CC's own structures, PRESENT/ABSENT:**

| column | verdict | ground |
|:--|:--|:--|
| **(a) Sonin-type at `𝔭`** | ### **PRESENT-AS-ABSORBED** | *finite places enter the ambient `X_S`, but Theorem 2 makes the Sonin space one archimedean copy — no independent local Sonin content; the `§4.5` per-place definition partially unread, caveat named* |
| **(b) class-separating** | ### **ABSENT** | *nothing held takes classes or characters as data — consistent with the frame's banked input (the field, not a decomposition)* |
| **(c) weight-carrying** | ### **ABSENT** | *no `log p` or `p^{−k/2}` enters any quoted Sonin structure (fetch one: explicitly absent)* |
| **(d) the summand shape** | ### **NOT ESTABLISHED — AND THEOREM 2 CUTS AGAINST IT** | *if the Sonin space is one archimedean copy, a per-place decomposition `Tr_F = Tr_∞ + Σ_𝔭 Tr_𝔭` of a Sonin-compression trace is not the natural reading of their frame; the ferry's target shape stays `NAVIGATOR-ASSERTED` and now carries this tension on its face* |

### `§8.3` — ### **THE CANDIDATE, NAMED NOT RUN — WITH ITS EXPECTATIONS REGISTERED AND THE READ POINTING AT `E2`**

> **The candidate:** *the local Sonin space at `𝔭` — functions on `K_𝔭` vanishing together with their local
> Fourier transform on the compact set `{|x|_𝔭 ≤ 1}`* **(the INTEGERS' ball `O_𝔭` — the ferry's parenthesis
> called it "the units' ball", a terminology slip named per Rule 3: the units are `|x|_𝔭 = 1`)** *— its
> projection `S_𝔭`, the local compressed scaling action `ϑ_𝔭`, and the candidate trace
> `Tr(ϑ_𝔭(g)·S_𝔭·ϑ_𝔭(g)*)`.*

**Registered before touching it, three shapes:**

* ### **`E1`** — *the space is nonzero, the trace is a nonzero positive form, and its restriction to
  lag-`log q` data carries the weight* → **column `(ii-finite)` fills and `(c)` with it.**
* ### **`E2`** — *the local Sonin space is trivial (or rigid — one vector, no dynamics) or the trace is
  weight-silent* → **the finite trace must come from elsewhere, and the "elsewhere" is NAMED: the weights
  live in the explicit formula's prime-side terms — the LEDGER — and a weight-carrying finite trace would
  have to be built from ledger data, not from Sonin data.** *Which would re-derive, from the field's own
  theorem, `§1`'s tension: the place axis is inert because Sonin is archimedean-rigid; the support axis
  moves because the weights live on the lag side.*
* ### **`E3`** — *a third shape, filed openly if it appears.*

> ### **WHERE THE READ POINTS, RECORDED SO THE RUN CANNOT LATER CLAIM SURPRISE: `E2`.** *Theorem 2 says
> finite places add no Sonin directions to the semilocal space; the candidate asks for exactly such
> directions locally.* **The check is priced and named, not run: for symmetric cutoffs the candidate space
> is a finite-dimensional kernel computable by DFT on `ℤ/p^{2n}` — one sitting of bench work, author's
> word required.**

### `§8.4` — THE DECOMPOSITION PROBE — ### **`Q1` LANDS: ALL THREE COLUMNS EXACT, AND THE SCALAR `±1` HAS NO ANALOG HERE**

**Instrument `relay` `tools/e16/b7_decomp_probe.py`; registration banked before any number
(`data/b7_registration_2026-08-17.txt`); run at `data/b7_2026-08-17.txt`. The ferry's reading ambiguity
was NAMED IN THE REGISTRATION** *(the scalar precedent's `±1` was banked on the rational bench, where no
class group exists to decompose; the registered choice: three columns side by side — scalar-`ℚ`, `ψ₀`,
`ψ₁` — at a NEW bipartite cell `L = 3.9`, floors from the start, `μ` exact, gate inherited).*

> ### **GATE PASS · BIPARTITE AT ALL THREE `ω` · AND EVERY CELL EXACT: `|full − μ| = 0` FOR ALL THREE
> COLUMNS (scalar-`ℚ` · `ψ₀` · `ψ₁`), AT ALL THREE `ω` (`μ = 333 / 668 / 1336`), UNDER BOTH COEFFICIENT
> CONVENTIONS.** *Floored pure = `μ` everywhere; the unfloored excess (3–23 kernel-noise counts) confirms
> `B6`'s repair was needed and suffices.*
>
> ### **`Q1`'s REGISTERED SENTENCE THEREFORE STANDS AS THE VERDICT: the scalar `±1` has NO analog at the
> bipartite cells this instrument can reach — the ferry's expectation ("the `±1` lives in the `ψ`-sum,
> each component exact") is VACUOUSLY consistent here, and the `±1`'s home remains the higher-`r` regime,
> out of reach of exact `μ` (blossom absent, declared).** *Two cells (`L = 3.5`, `3.9`), nine component
> measurements, eighteen counts, zero disagreements: the identification mechanism is exact across the
> bipartite regime as far as it has been asked.* **BENCH-GRADE. PROVED NOWHERE.**

### `§8.5` — `B7`'s CHECKPOINT — WHAT RETURNS FOR THE AUTHOR'S WORD

1. ### **THE SPECIFICATION IS WRITTEN (`§8.1`), AND ITS HARD COLUMN IS `(c)` — WEIGHT** *(the phase
   boundary says the structural side cannot supply it; a weight-silent candidate builds another
   archimedean-type trace at a finite address).*
2. ### **THE READ IS DISCHARGED (`§8.2`): Theorem 2 verbatim — the semilocal Sonin space is ONE
   ARCHIMEDEAN COPY, finite places contribute no independent Sonin directions — which EXPLAINS the banked
   "place axis is inert" and CUTS AGAINST the `Tr_∞ + Σ Tr_𝔭` summand shape.** *The strategy sentence is
   banked from the paper's own introduction; the `§4.5` local definition stays a named partial-read
   caveat.*
3. ### **THE CANDIDATE IS NAMED, NOT RUN (`§8.3`), WITH `E1`–`E3` REGISTERED AND THE READ POINTING AT
   `E2`** — *whose "elsewhere" is named: a weight-carrying finite trace would have to be built from
   LEDGER data, not Sonin data.* **The dimension check is priced at one sitting, awaiting your word.**
4. ### **THE PROBE LANDED `Q1`: exact everywhere, the scalar `±1` absent from the bipartite regime's
   cells.**
5. ### **THE `§1-bis` IDENTITY IS FILED AT QUESTION GRADE: archimedean pays structure, finite pays
   weight, `h2` weight-dependent** — *an alignment of three graded facts pricing `(c)`'s hardness.*

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. THE ATTEMPT GATE WAS NOT APPROACHED. `h2`
UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

### `§8.3-bis` — ### **THE DIMENSION CHECK RAN, AND THE CANDIDATE CLOSES AT `E3` — A THIRD SHAPE, WITH TWO EXACT LAWS** *(2026-08-17, twelfth sitting; instrument `relay` `tools/e16/b8_sonin_dim.py`, registration banked before any number, run + post-run verification at `data/b8_2026-08-17.txt`)*

**The model, declared in the registration:** *functions on `p^{−n}ℤ_p/p^nℤ_p ≅ ℤ/p^{2n}`, DFT pairing,
the integers' ball `B = {m ≡ 0 mod p^n}`; `Son(p,n) = {f : f|_B = 0 ∧ f̂|_B = 0}`; the scaling action as
the truncated unitary `m ↦ pm` with `|p|^{1/2}` normalization, edge effects declared.*

> ### **THE SPACE IS NOT TRIVIAL, AND ITS DIMENSION IS A LAW: `dim Son(p,n) = (p^n − 1)²` — matched at
> EIGHT cells including two out-of-sample (`(2,3) → 49`, `(7,1) → 36`).** *The constraint stack has
> exactly one dependency (`p^{2n} − 2p^n + 1`).*
>
> ### **AND THE COMPRESSED SCALING ACTION CARRIES AN EXACT `√p`: `‖S·U·S‖_F = √p·(p^{n−1} − 1)` — matched
> at SEVEN cells** *(`Q² = 2, 12, 80` at `n = 2` for `p = 2, 3, 5`, exact to ten digits; `18` at `(2,3)`;
> the `n = 1` zeros are machine zero, and the law explains them structurally: the `n = 1` space lives on a
> single shell and scaling maps it into the ball where the space vanishes — the `(p⁰ − 1)` factor).*
> **One instrument caveat recorded in the bank: the run's `n = 1` fitted-power line was a polyfit on
> machine-noise magnitudes and is VOID; the executor's own filter admitted floating zeros.**

### **THE VERDICT, AGAINST THE REGISTERED SHAPES:**

* **NOT `E2`-first-face** *(trivial)*: the space is `(p^n − 1)²`-dimensional.
* **NOT `E2`-second-face as registered** *(weight-silent)*: the compressed action carries `√q` at `q = p`
  EXACTLY, at model grade, for every `n ≥ 2` — *with the volume factor `(p^{n−1} − 1)` grading the
  carriage by cutoff, dead exactly at the smallest.*
* **NOT yet `E1`** *(the registered `E1` demands a trace whose restriction to lag-`log q` data scales with
  the explicit-formula coefficient — `‖SUS‖_F` is not that measurement)*.
* ### **`E3` — THE THIRD SHAPE, FILED OPENLY: the transposed local Sonin space is LARGE with an exact
  dimension law, its compressed scaling action carries the `√q` factor exactly with a cutoff-graded volume
  factor, and the READ's `E2`-POINTING IS REFUTED AT MODEL GRADE — while CC's Theorem 2 is UNTOUCHED,
  exactly as `§8.3` pre-registered: their rigidity is a fact about THEIR semilocal coupling (finite data
  pinned to the self-dual vector), not about the local vanishing-model, and the model now makes that
  distinction concrete.**

> ### **WHAT REMAINS FOR `E1`, NAMED AND PRICED: one more bench cell — the candidate trace
> `Tr(ϑ_𝔭(g)S_𝔭ϑ_𝔭(g)*)` evaluated on lag-`log q` correlation data, against the two banked coefficient
> conventions. One sitting. NOT RUN; the author's word opens it.** *A dimension-law observation for the
> record, not promoted: `(p^n − 1)²` is a perfect square — suggestive of a tensor structure on the
> nonzero-residue data; noted, not pursued.*

**THE CANDIDATE IS CLOSED AT THE MEASURED VERDICT: `E3`, with `E1`'s defining measurement the named next
cell. NO SIGN. NOTHING PROMOTED.**

---

## §9 — ### **THE LEDGER-SIDE SPECIFICATION — WHERE A WEIGHT-CARRYING FINITE TRACE WOULD HAVE TO LIVE** *(2026-08-17, twelfth sitting; author-called; written from constraints, NO CONSTRUCTION; ### NO SIGN SENTENCE ANYWHERE IN THIS SECTION)*

> ### **RULE-3 LOG, AT THE HEAD:** *Face E VERIFIED at its banked scope — the Pass-2 verdict, quoted:
> "any derivation of the positivity must use the Euler product essentially" — **an honest RESTRICTED
> theorem over the named toolkit `T`, two-witness/relativization reading, author-adopted; the definitional
> form was rejected and is not used here** · the phase boundary banked (`F.2026-08-17e/f`, `§1-bis`) ·
> Theorem 2 banked verbatim (`§8.2`) · **the summand shape `Tr_∞ + Σ_𝔭 Tr_𝔭` is WITHDRAWN as
> `NAVIGATOR-ASSERTED`, now read-refuted (`§8.2`'s tension ruled by the navigator's own line)** ·
> ### **and one flag: the ferry's `(β)` states the weight as `√q·log q`, WHICH MATCHES NEITHER BANKED
> CONVENTION** *(registered: `2√q·log p`; Weil: `4·log p/√q`; the banked sentence: "nothing in the banked
> record separates them")* — **`(β)` below is stated at the banked grade, not the ferry's form.**
> *Banned-list grep: clean; terms coined: none.*

### `§9.1` — THE THREE CONSTRAINTS, EACH AT ITS CITE

| | constraint | at cite |
|:--|:--|:--|
| ### **(α)** | ### **TRACE SHAPE — MANIFESTLY POSITIVE AS A SUM OF SQUARES**, the `Tr(ϑ(g)·S·ϑ(g)*)` sandwich pattern | *a ledger is not a trace* (`§6(d)`, `§7(b)`); blockage 7's "the sandwiched form is a norm"; **and the `N7` rider carried beside it: "not be supplied by a trace or realization theorem — realization supplies no positivity" (the joint reading) — the positivity must be STRUCTURAL of the `Tr(AA*)` kind, which is exactly why the shape is demanded rather than assumed** |
| ### **(β)** | ### **WEIGHT — restricted to lag-`log q` data it must scale with the prime's explicit-formula coefficient** | *the phase boundary (`§1-bis`): the weights live on the trace side and the structural count is silent on them.* **The coefficient's banked candidates: `2√q·log p` (registered) and `4·log p/√q` (Weil) — unseparated by the banked record; a candidate trace must scale with the convention the true functional carries, and measuring WHICH is itself a named bench question.** *The ferry's `√q·log q` flagged above.* **[UPDATED 2026-08-17, thirteenth sitting, the `E1` cell (`§8.3-ter`): the transposed Sonin candidate FAILS `(β)` in the registered channel — its lag-cross trace coefficient is machine zero at every measured cell — while its diagonal channel's amplitude scaling `√N·q^{−1/2}` has the WEIL shape (found-beyond-registration, question grade). The convention question stays OPEN with its first measured datum.]** **[AND FOURTEENTH SITTING, `B9` (`§8.3-quater`): the vanishing is DERIVED — fixed-point localization, certified at eight cells — and the cross-lag form closes the convention AT THE MODEL: the Weil shape `q^{−1/2}`, asymptotic power `−1/2` exact from the third law, finite-size correction explicit. The true functional's convention remains the separate named question.]** |
| ### **(γ)** | ### **IDENTIFICATION — its negative count on the address graph must equal the maximum matching** | *nine of nine bench component-measurements exact across `B6`–`B7` (`|full − μ| = 0`, two cells, three columns, both conventions)* — **the design requirement is now TESTABLE in one cell per candidate, which is what the nine cells bought** |

### `§9.2` — ### **THE HONEST FRAME, ON THE SPEC'S FACE: THREE INDEPENDENT LINES CONVERGE HERE, AND NO CANDIDATE SATISFYING `(α)`–`(γ)` IS KNOWN TO ANYONE**

> 1. ### **FACE E** *(banked restricted theorem)*: any derivation of the positivity whose tools lie in the
>    named toolkit must use the Euler product **essentially** — *the sign's derivation must touch the
>    multiplicative content.*
> 2. ### **THE PHASE BOUNDARY** *(measured + derived)*: only weight-carrying data can pay for a prime —
>    *the structural side is silent on the weights, and `h2` is weight-dependent.*
> 3. ### **CC's THEOREM 2** *(verbatim at cite)*: the Sonin mechanism carries none of it — *the semilocal
>    Sonin space is one archimedean copy.*
>
> ### **CONVERGENCE, NOT PROOF: three graded facts point at one specification — a positive trace built
> from the prime side of the explicit formula, weight-carrying by construction — and the specification's
> own `§9.1` is currently satisfied by NOTHING. That absence is the sharpest current form of Road B's
> gap.** *And the `N`-constraints ride any future use: a candidate filling `(α)`–`(γ)` would still face
> `N2` (no positive control), `N4` (finite-range is compiled and does not advance; full-range is
> RH-equivalent), and the `W-2` trap (definiteness stays a predicate, never a field).*

### `§9.3` — WHERE SUCH A FORM WOULD LIVE — ### **THREE ROUTES, GRADED FROM HOLDINGS, NONE RUN**

| route | grade | ground, at holdings' depth |
|:--|:--|:--|
| ### **R1 — the Weil form's own prime-side terms as a candidate quadratic form** *("is the prime side ever a sum of squares on any class of test functions?")* | ### **ARRESTED-AS-STATED · SILENT-ON-THE-CLASS-QUESTION** | *the banked lag analysis exists precisely because the prime terms are OFF-DIAGONAL shift structure, not diagonal squares — the address graph IS the record of their non-square shape. Whether some restricted cone of test data makes the prime side a sum of squares: no banked statement either way.* |
| ### **R2 — the Deninger orbit-at-`log p` seat** | ### **PLAUSIBLE-AT-SEAT** | *the one frame where `log p` is GEOMETRIC — the period of the orbit at the closed point (`MATCH`-grade seat, held at abstract). A dynamical trace formula's orbit terms are where a weight-carrying local contribution would natively arise —* **and the weight structure of the orbit contributions is exactly the unfetched `H¹`/trace-formula material: the standing read-debt, which grades nothing further.** |
| ### **R3 — the CCM cyclic-module quotient's finite-place content** | ### **ARRESTED** | *the frame's pairing has a PROVEN VANISHING SUBSPACE (Prop. 6.4, banked) and takes the field, not a decomposition — class-separation absent (`§9.4` of `THE_CARRIER_BUILD`); nothing held gives its finite-place content a positive trace shape.* |

> ### **AND THE `B8(1)` MEASUREMENT NOW SITS BESIDE THE ROUTES AS A FOURTH DATA POINT, `OURS`, MODEL
> GRADE:** *the transposed local Sonin space is large (`(p^n−1)²`) and its compressed scaling action
> carries `√q` exactly (`√p·(p^{n−1}−1)`)* — **feeding `(β)`'s plausibility at a finite address without
> filling any of `(α)`–`(γ)`, and with `E1`'s defining measurement (the trace on lag data) the named,
> priced next cell.**

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. THE ATTEMPT GATE WAS NOT APPROACHED. `h2`
UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

### `§8.3-ter` — ### **THE `E1` CELL RAN: WEIGHT-PRESENT-IN-NORM-ABSENT-IN-TRACE — THE LEAK LOCATED, AND A THIRD EXACT LAW FOUND BEYOND REGISTRATION** *(2026-08-17, thirteenth sitting; author's word given; instrument `relay` `tools/e16/b8_e1_cell.py`; registration with longhand convention predictions banked before any number; run + adjudications at `data/b8_e1_2026-08-17.txt`)*

**The measurement, derived longhand in the registration:** *at a finite place the scaling group is
DISCRETE — `p^ℤ`, step `log p` — so `ϑ_𝔭(g) = log p·Σ_k g(k log p)·U^k`: the lag-`log q` data enters the
candidate trace NATIVELY, and `T(g) = Tr(ϑ(g)Sϑ(g)*)` is the `(α)` sandwich shape by construction. The
`(β)` test channel: `t_k = |Tr(U^k S)|`, the pure lag-`log q` cross coefficient, read against the two
banked conventions (`√q` growing vs `1/√q` decaying; their ratio at lag `q` is `q/2` — the ferry's
"ratio `q`" is the shape-ratio, constant dropped, flagged per Rule 3).*

> ### **THE VERDICT, PER THE REGISTERED MAP: WEIGHT-PRESENT-IN-NORM-ABSENT-IN-TRACE.**
>
> | channel | measured | law |
> |:--|:--|:--|
> | ### **trace (cross), `t_k`** | ### **MACHINE ZERO — eight dense cells, every lag** | *the registered middle verdict's signature, exactly* |
> | **norm, `n_k`** | nonzero | `n_1 = √p·(p^{n−1}−1)` — `B8`'s law re-confirmed |
> | ### **diagonal, `d_k`** | nonzero, integer-structured | ### **`d_k = (p^{n−k}−1)(p^n−1)` for `k < n`, else `0` — A THIRD EXACT LAW, matched at every measured cell** |
>
> ### **THE LEAK IS LOCATED: THE TRACE KILLS WHAT THE NORM CARRIES.** *The strictly shell-lowering
> compressed action has nonzero mass (`√q` in the norm) and identically vanishing trace against the Sonin
> projection — the pure lag-correlation coefficient of the candidate trace is ZERO at every measured cell.
> A structural derivation of the vanishing is one more sitting and is named, not claimed.*
>
> ### **FOUND BEYOND REGISTRATION, QUESTION GRADE: the diagonal channel's amplitude scaling is
> `√(d_k) ≈ √N·q^{−1/2}` — THE WEIL SHAPE, in the one channel that scales.** *Not the registered quantity;
> filed as the `(β)` convention question's first measured datum, not as its answer.*

**Two instrument adjudications, banked with the run:** *(i) the `(5,3)` low-rank trace path returned
`~3·10⁻³` and was ADJUDICATED AS NOISE — the same path at `(3,3)` gives `~2·10⁻²` where the dense ground
truth gives `~5·10⁻¹⁵`; the low-rank path is WITHDRAWN as a trace instrument and `(5,3)` recorded as
not-measured-at-precision; (ii) the eight dense cells are the record.* ### **THE TENSOR CHECK (rider 2)
DOES NOT RUN, BY ITS OWN REGISTERED RULE — `E1` did not land at either convention; its registration
stands banked for a future cell.** *Positivity guard restated: a positive local trace bears not at all on
`h2`, and nothing here is a sign statement.*

### `§8.3-quater` — `B9`: ### **WHY THE TRACE VANISHES — FIXED-POINT LOCALIZATION, NOT `U`-INVARIANCE — AND THE CROSS-LAG FORM CARRIES THE WEIL SHAPE** *(2026-08-17, fourteenth sitting; author-called; instrument `relay` `tools/e16/b9_cells.py`, registration banked before any number, certificates at `data/b9_2026-08-17.txt`)*

> ### **RULE-3 LOG:** *`E1`'s three laws banked (`§8.3-ter`) ✓ · the `(β)` conventions banked ✓ · the
> ferry's "`U`-invariance ⟹ trace vanishes" derivation was `NAVIGATOR-PROPOSED` and was CHECKED
> FIRST-THING —* ### **AND ITS PREMISE IS REFUTED: the local Sonin space is NOT `U`-invariant.**
> *Banned grep: clean; terms coined: none.*

### **THE DERIVATION, WITH ITS CERTIFICATES** *(each checkable piece certified at eight cells)*

* ### **`C1` — THE PREMISE FAILS, AND THE FAILURE IS LOCALIZED:** *`U` PRESERVES the Fourier-side
  condition (`‖F_B·U·K‖` machine zero at every cell — longhand: `(Uf)^ = p^{−1/2}·f̂(p·)`, support moves
  AWAY from the ball) and VIOLATES the ball-support condition exactly at shell 1 (`‖E_B·U·K‖ = 1, √3, 2,
  4, 4` at the `n ≥ 2` cells — `U` maps shell 1 into the ball).* **At `n = 1` the truncated `U` kills the
  space outright (`U·Son = 0`) — the `(p⁰−1)` factor's mechanism, now visible.**
* ### **`C2` — THE ACTUAL CAUSE, DERIVED AND CERTIFIED: FIXED-POINT LOCALIZATION.** *Longhand, proved:
  `Tr(Uᵏ) = p^{−k/2}` exactly — the only fixed point of `m ↦ pᵏm mod p^{2n}` is `m = 0`
  (`gcd(pᵏ−1, p^{2n}) = 1`), and it contributes everything.* ### **Certified at machine precision, all
  cells, all lags: `Tr(UᵏΠ) = p^{−k/2}` — THE CONSTRAINT SPACE ABSORBS EXACTLY THE FIXED-POINT
  CONTRIBUTION, so `t_k = Tr(Uᵏ) − Tr(UᵏΠ) = 0`.** *The absorption identity's full longhand proof (the
  `δ₀`-eigendirection carries it; the rest nets zero) is one more step, CERTIFIED-NOT-PROVED, named.*
* **`C3`** — `F U F† = Uᵀ` exact on the model (machine) — the conjugation used above is exact, not
  approximate. **`C4`** — the compressed shift is quasi-nilpotent (spectral radius at the pseudospectral
  noise floor) — no recurrent spectral content on the Sonin space, supporting-certificate grade.

> ### **THE SENTENCE, BANKED (the ferry's `YES`-branch sentence adapted to the measured `NO` branch):
> THE TRACE OF SCALING LOCALIZES AT ITS FIXED POINT; THE ONLY FIXED POINT LIES IN THE BALL THE SONIN
> SPACE EXCLUDES — THE SANDWICH TRACE SEES NO RECURRENCE, AND THE WEIGHT LIVES IN MODULUS AND DIAGONAL.**

### **THE TENSION, FILED AT QUESTION GRADE, `OURS`** *(cross-linked to Face E and the phase boundary as their local form)*

> ### **AT A FINITE PLACE, THE `(α)`-SHAPE AND THE `(β)`-SHAPE CONFLICT ON THE SONIN-TYPE SPACE:** *a
> sandwich trace over the scaling action localizes at FIXED POINTS; the prime's weight lives at the closed
> ORBITS of scaling — recurrence that exists only on the orbit quotient (`x ∼ px`), not on the space; the
> Sonin space excludes the one fixed point, and the trace dies.* ### **A WEIGHT-CARRYING FINITE TRACE MUST
> EITHER LEAVE THE SANDWICH SHAPE OR LIVE WHERE SCALING RECURS — THE ORBIT QUOTIENT.** *Which is Face E's
> restricted verdict and the phase boundary, said locally: the sign's derivation must touch the closed-
> orbit (Euler) data, and structure-side spaces cannot supply it.* ### **AND `R2` (the Deninger seat)
> SHARPENS WITHOUT PROMOTION: "periodic orbits related to closed points" is precisely the picture in which
> scaling RECURS at period `log p` — the seat's plausibility now has a structural reason, not only a
> geometric one.** *No construction; no candidate promoted.*

### **THE CROSS-LAG FORM (item 2) — `E1′` LANDS: THE WEIL SHAPE, WITH THE FINITE-SIZE CORRECTION EXPLICIT**

*The Hermitian cross-lag form on the diagonal blocks (`d_k` real integers — certified), lags paired
`k ↔ −k` per the antipode-involution; POSITIVITY UNKNOWN AND SAID SO.* **The `(β)` reading:** *measured
amplitudes `√d_k/√N` fit power `−0.42` over seven points at finite `n`; and the third law itself gives,
longhand,* ### **`√d_k/√N = √((1−p^{−(n−k)})(1−p^{−n}))·q^{−1/2}` — asymptotic power `−1/2` EXACTLY: the
WEIL SHAPE, with the fit's bias explained by the finite-size factor rather than fitted away.**

> ### **THE `(β)` CONVENTION, CLOSED AT THE MODEL: the model's cross-lag amplitude carries the WEIL shape
> `q^{−1/2}`, not the registered `2√q·log p` growth.** *Scope said plainly: the model is OUR candidate
> object, not the Weil functional; the true functional's convention remains the separate, named bench
> question.* **The tensor check stays HELD on its trigger.**

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. THE ATTEMPT GATE WAS NOT APPROACHED. `h2`
UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

### `§8.3-quinquies` — `B10`: ### **THE ABSORPTION IDENTITY PROVED · THE ORBIT-QUOTIENT TRACE IS ALIVE · THE LEFSCHETZ SHAPE MATCHES** *(2026-08-17, fifteenth sitting; author's word given; instrument `relay` `tools/e16/b10_cells.py`, registration banked before any number, certificates and run at `data/b10_2026-08-17.txt`)*

### **1. THE ABSORPTION IDENTITY — PROVED LONGHAND, EVERY LEMMA CERTIFIED**

> ### **STATEMENT (portable):** *on the local model, the constraint-space projection `Π` satisfies
> `Tr(UᵏΠ) = Tr(Uᵏ) = p^{−k/2}` — the constraint space absorbs exactly the fixed-point trace of scaling —
> hence `t_k = 0`.*
>
> ### **PROOF.** *(i)* `R_F = {f : f̂ supported on `B`}` **is the space of `p^n`-PERIODIC functions**
> *(`f̂(j) = 0` off multiples of `p^n` ⟺ invariance under `m ↦ m + p^n`)*. *(ii)* `Π_E` = multiplication
> by `1_B`; `Π_F` = the conditional expectation over `p^n`-translates; **computing both products: each
> equals `f ↦ (p^{−n}Σ_B f)·1_B` — so `Π_EΠ_F = Π_FΠ_E = Π_Z`, the orthogonal projection onto
> `span{1_B}`** *[certified `L-A`, exactly `0`]*, **hence `Π = Π_E + Π_F − Π_Z`.** *(iii)*
> `Tr(UᵏΠ_E) = Σ_{m∈B}(Uᵏ)_{mm} = p^{−k/2}` — *the only diagonal entry is `m = 0`
> (`gcd(pᵏ−1, p^{2n}) = 1`), and `0 ∈ B`* *[`L-B`]*. *(iv)* `Tr(UᵏΠ_F) = p^{−k/2−n}·#{m′ : m′(pᵏ−1) ≡ 0
> mod p^n} = p^{−k/2}` *[`L-C`]*. *(v)* `Tr(UᵏΠ_Z) = p^{−k/2}` — *`B` is closed under multiplication by
> `p`* *[`L-D`]*. *(vi)* **inclusion–exclusion: `p^{−k/2} + p^{−k/2} − p^{−k/2} = p^{−k/2}` ∎.**
> *All four component traces certified to `10⁻¹⁶` at six cells including the out-of-sample `(2,4)`.*
> ### **GRADE: DERIVED AT THE MODEL.**
>
> **The transposition table, per step:** *(iii)/(v) transpose verbatim to `K_𝔭` (unique fixed point
> `0 ∈ O_𝔭`; `O_𝔭` closed under multiplication by `p`); (i)/(ii) are MODEL-SPECIFIC as stated — on
> `K_𝔭` the `O`-translation-invariant functions are not `L²`, so the commuting-projection structure
> needs the compact quotient; (iv) transposes as an orbital integral requiring regularization.* **Two
> named caveats; the `K_𝔭` statement is carried as a transposition, not claimed.**

### **2. THE ORBIT-QUOTIENT CELL — ### `Q1` LANDS: THE TRACE IS ALIVE ON THE QUOTIENT**

**The object, defined before the run:** *`V_inv` = off-ball functions invariant under `m ↦ pm` (both
off-ball) — the model realization of the orbit space `x ∼ px`; `S_quot` its orthoprojection. The
Fourier-side Sonin half DOES NOT DESCEND (the transform does not commute with `x ∼ px`) — registered as
informative: the quotient keeps the sandwich shape available while changing the space, exactly as `B9`'s
tension predicted.*

> ### **MEASURED: `T_quot(k) = |Tr(Uᵏ S_quot)| ≠ 0` — THE FIXED-POINT TRACE, MOVED TO A CLOSED ORBIT, IS
> NOT ABSORBED.** *`T_quot(1) = 1.886 / 4.849 / 7.794 / 28.78 / 37.27 / 10.56` across the six cells, with
> exact rational structure (`T(2) = 16/7` at `(2,3)`, `= 162/13` at `(3,3)`, `= 32/5` at `(2,4)`), and*
> ### **the per-lag-step ratio tends to `√p/(p+1) = q^{−1/2}·(1+p^{−1})^{−1}` — THE WEIL SHAPE, carrying
> AN EULER-FACTOR-LIKE LOCAL CORRECTION `(1+p^{−1})^{−1}`** *(observed at consecutive lags of the `n = 3,
> 4` cells; the correction is a named question — truncation artifact or genuine local factor — not
> resolved here).* **`dim V_inv = (p−1)p^n` at all six cells — a fourth dimension law, noted.**
> *`T_quot(k) = 0` for `k ≥ n` — the truncation edge, registered.* ### **POSITIVITY OF ANY QUOTIENT FORM:
> UNKNOWN AND SAID SO. Column `(β)` has, at last, a TRACE-SHAPED candidate at model grade — nonzero and
> Weil-scaled — and nothing more is claimed for it.**

### **3. THE LEFSCHETZ WONDER — ### MATCH-IN-SHAPE, AT MODEL GRADE, NO PROMOTION**

> *The quotient trace is a weighted count over the closed orbit (period `log p`, the lag unit) whose
> lag-`k` weight decays `q^{−1/2}`-shaped — compared longhand at cite:* **(a)** *the explicit formula's
> prime-side term — same lag support, same decay shape (the convention question's model-side answer,
> `§8.3-quater`);* **(b)** *Deninger's seat — "periodic orbits are related to the closed points" (banked
> at abstract) — whose program conjectures precisely a Lefschetz-type reading of the explicit formula;*
> **(c)** *CC's arrest sentence — "understand the above equality as a Lefschetz formula" (`ATTRIBUTED`).*
> ### **VERDICT: MATCH-IN-SHAPE.** *A model-grade miniature of the seat's conjectured mechanism: the
> finite-place weight arises as a closed-orbit trace on the quotient where scaling recurs.* ### **NO
> PROMOTION: shape is not identity; the `(1+p^{−1})^{−1}` correction is unexplained; the model is ours.**
>
> ### **`R2`'s PRIORITY, RE-STATED FROM THE OUTCOME: the Deninger read-debt (the `H¹`/trace-formula
> material) is now Road B's highest-priority retrieval — the quotient cell realizes in miniature exactly
> the shape his seat conjectures, and only the read can say whether the match survives contact with the
> text.**

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. THE ATTEMPT GATE WAS NOT APPROACHED. `h2`
UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

### `§8.3-sexies` — `B11`: ### **THE CORRECTION FACTOR WAS THE TRUNCATION EDGE OF A FIFTH EXACT LAW — THE PURE WEIL WEIGHT SURVIVES — AND THE CENSUS IS COMPILED** *(2026-08-17, sixteenth sitting; author-called; instruments `relay` `tools/e16/b11_factor.py` + `SIDE-window` `v0.5.0`; registrations banked before any number)*

> ### **RULE-3 LOG:** *`B10`'s proof and the four laws banked ✓ · the ratio `√p/(p+1)` at the measured
> cells ✓ · **the Euler-factor reading of `(1+p^{−1})^{−1}` was `NAVIGATOR-PROPOSED`, and the
> discriminator it ordered REFUTED it** (below).* *Banned grep: clean; terms coined: none.* ### **The
> Deninger retrieval runs navigator-side; its comparison slot in this section is OPEN and stays open
> until the text arrives by ferry.**

### **1. THE DISCRIMINATION — `F2` LANDS THROUGH A FIFTH EXACT LAW** *(exact rational arithmetic, no floats, no matrices; 13 cells, `p ∈ {2,3,5,7}`, `n` to the grid limit; denominators checked against the registered patterns BEFORE the trend was read)*

> ### **THE FIFTH LAW: `R(k) = (p−1)·p^{n+1}·(p^{n−1} − p^{k−1})/(p^n − 1)` for `k ≤ n−1`, else `0` —
> VERIFIED 63/63 VALUES.** *(`T_quot(k) = p^{−k/2}·R(k)`; the systematic denominator is
> `(p^n−1)/(p−1)`; the `p+1` at `(2,2)` was the `p = 2` coincidence `p+1 = p²−1`.)*
>
> **Everything follows longhand from the law:**
>
> | consequence | statement |
> |:--|:--|
> | **the ratio law** | `R(k+1)/R(k) = (p^{n−k} − p)/(p^{n−k} − 1)` — ### **`→ 1` as `n` grows: `F2`, TRUNCATION** |
> | ### **the edge value** | *at `n−k = 2` the ratio is exactly `p/(p+1)` —* **`B10`'s `√p/(p+1)` reading was the truncation EDGE of this law, not a local factor** |
> | ### **the limit** | `R(k)/dim V_inv = p(p^{n−1}−p^{k−1})/(p^n−1) → 1`, hence ### **`T_quot(k)/dim V_inv → p^{−k/2} = q^{−1/2}` EXACTLY — the orbit-normalized quotient trace carries the PURE WEIL WEIGHT in the limit, with NO correction** |
>
> ### **THE VERDICT IS CLEANER THAN EITHER REGISTERED BRANCH: not a local Euler factor (F1 refuted), and
> not merely "the factor dies" (F2's letter) — the whole `k`- and `n`-dependence is one closed form, and
> the Weil weight emerges exactly.** *The navigator-proposed Euler-factor reading is refuted by its own
> ordered discriminator — Rule 3's cycle, closed within one sitting.*

### **2. THE CENSUS COMPILED — `SIDE-window` `v0.5.0`** *(`aacb415` = tag `v0.5.0`, pushed and read back)*

*`SIDEWindow/LocalModel.lean`, vanilla Lean 4, decide-only: the fixed-point count `= 1` (the arithmetic
heart of the absorption proof and the localization), the absorption count `= p^n`, ball closure, the
off-ball/edge counts whose difference is the orbit count `(p−1)p^n`, and the laws' integer identities —
the Sonin split, the diagonal law, and the fifth law in cross-multiplied form at the banked cells.*
### **`AxiomCheckLocalModel`: 26/26 terminals "does not depend on any axioms".** *The non-claims travel
in the docstrings: the linear-algebraic and analytic identifications (that the counts are dimensions and
traces; the forest-components lemma) are bench-grade at `relay`, declared not compiled; nothing bears on
`h2`.*

### **3. THE DENINGER COMPARISON SLOT — ### OPEN.** *The retrieval runs navigator-side; the `B10`
Lefschetz match-in-shape waits on the text. Nothing here pre-judges it.* **The tensor check stays HELD on
its trigger.**

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. THE ATTEMPT GATE WAS NOT APPROACHED. `h2`
UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

### `§8.3-septies` — `B12`: ### **THE DENINGER COMPARISON, AT CONTENT — MATCH ON THE ARITHMETIC SIDE, NO CONTACT ON THE COHOMOLOGICAL SIDE** *(2026-08-17, seventeenth sitting; the retrieval navigator-side, quotes carried by ferry; name-identity of `math/0204110` verified live at the abs page by the executor)*

> ### **RULE-3 LOG:** *`math/0204110` = Deninger,* Number theory and dynamical systems on foliated spaces
> *(2002) — title/author/abstract VERIFIED LIVE; the section-level quotes below are
> `NAVIGATOR-AT-CONTENT`, banked at the author's stated grade with their citations carried
> (Conj 3.1 eq 11 · WH 5.6 eq 26 · Thm 2.1 · Cor 3.5 · §4 table · Rem 5.7(5) · §5.8).* *Banned grep:
> clean; terms coined: none. The corpus-side identity (the `B10`–`B11` quotient trace and the fifth law)
> is banked at model grade.*

### **THE COMPARISON, BANKED AT ITS TWO GRADES**

| side | his, at the carried cites | ours, at model grade | verdict |
|:--|:--|:--|:--|
| ### **ARITHMETIC (the closed-orbit local data)** | *orbit length `l(γ_𝔭) = log N𝔭` · `ε_γ(k) = +1` for all `k ≥ 1` · transversal eigenvalues of modulus `N𝔭^{1/2}` — Rem 5.7(5), quoted: **"Are the eigenvalues Weil numbers (of weight 1)?"*** | *the lag unit `log p` (the orbit period) · the orbit-normalized quotient trace `→ q^{−1/2}` EXACTLY (the fifth law) · the antipode-involution pairing `k ↔ −k`* | ### **MATCH-AT-CONTENT — model grade ours, conjectural his** |
| ### **COHOMOLOGICAL (the object whose trace it is)** | *the LHS — the trace on reduced leafwise cohomology, where placement lives via `Θ = α/2 + S`, `S` skew (Thm 2.1 / Cor 3.5)* — **his own text: the conjecture "is not known … if `φ` has fixed points," and number fields FORCE fixed points (§4: infinite place ↔ fixed point)** | *nothing — the corpus holds no leafwise cohomology and claims none* | ### **NO CONTACT — the object is unbuilt on both roads** |

> ### **THE FACE-E READING, STATED PLAINLY: THE CORPUS AND DENINGER BOTH HOLD THE LEDGER'S PRIME SIDE —
> the closed-orbit data with the Weil weight — AND NEITHER HOLDS THE OBJECT WHOSE TRACE IT IS.** *The
> `B10` match-in-shape is upgraded to MATCH-AT-CONTENT on the arithmetic side and DOWN-GRADED to
> no-contact on the cohomological side — which is the honest split the shape-level comparison could not
> see. Nothing is promoted; the `R2` read-debt is RETIRED at this paper (the `H¹`-adjacent material now
> at content via the carried quotes), with the `1807.06400` body debt still standing separately.*

**NO CONSTRUCTION. NO MECHANISM. NOTHING PROMOTED. `h2` UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

### `§8.3-octies` — `B13`: ### **THE TENSOR CHECK — `T1`, WITH THE DECOMPOSITION EXPLICIT: `Son = V₁ ⊗ W₁`, THE FOURIER-DUAL SQUARE** *(2026-08-18, eighteenth sitting; released by the author; instrument `relay` `tools/e16/b13_tensor.py`, registration with the longhand derivation banked before any number)*

> ### **THE DERIVATION (registered, then certified):** *in the chart `m = a + p^n·b` the ball is exactly
> `{a = 0}`, so the support condition is `f ∈ V₁ ⊗ L²` (`V₁` = vanishing at `a = 0`); and the `a′ = 0`
> Fourier rows see only the `b`-marginals, so the transform condition is `f ∈ L² ⊗ W₁` (`W₁` = mean-zero
> in `b` — frequency-vanishing at `0`); the intersection is `V₁ ⊗ W₁` exactly.* ### **HENCE
> `S = P₁ ⊗ Q₁` — A TENSOR SQUARE WHOSE TWO FACTORS ARE THE SAME ONE-POINT-VANISHING CONDITION ON THE
> TWO SIDES OF THE TRANSFORM — and the first law `dim = (p^n−1)²` is EXPLAINED, not merely matched.**
>
> ### **CERTIFIED at seven cells: operator-Schmidt rank EXACTLY 1; the factors match `P₁`, `Q₁` to
> machine precision; `sv[0] = p^n − 1` exactly (an unregistered consistency, noted).**
>
> ### **FILED AT QUESTION GRADE, `OURS`, NO PROMOTION: the finite place holds a SELF-PRODUCT IN
> MINIATURE — the local shadow of "what supplies the leaves" — cross-linked to column `(vi)` and Road A's
> missing self-product.** *A local tensor square is not a global self-product, said on the face.*

**NO CONSTRUCTION. NO MECHANISM. `h2` UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

---

## §10 — ### **THE INTERRELATION: THE BUILT SECTION READ BACK THROUGH THIS SPECIFICATION** *(2026-08-18, sittings 14–16 of `W-ATTEMPT-2`, relay-banked; author-ruled interrelation; every verdict at grade, no merge anywhere; full tables at `relay/reports/2026-08-18-w-attempt-2-sitting-{14,15,16}.md`)*

*The attempt built the object this sheet specified, and the corpus's light was then read back through
it. What landed structural, one line each, each with its relay table:*

1. ### **THE `S`-TABLE HAS ITS ADDRESSES ON THE OBJECT** — *`S4`-at-resolution is the object's whole
   point (the binding restored exactly where the Epstein witness loses it; the `S1b ≡ S4` non-merge
   VISIBLE as separate measured data); `S5`'s addresses are the towers' rungs with `q^{−1/2}` exact;
   `S6`'s domain-of-expression gap has its model form in the diagonal's window (`p^k ≤ a²`); the arrow
   `S3b ⟹ S2` is visible as label-coherence (`W·W^∗ = 1` needs every place's self-dual label).*
2. ### **`N5`'s TWO HALVES ARE NOW BOTH MEASURED ON ONE OBJECT** — *the poles live only in the
   `ψ₀`-channel (the vanishing conditions vacuous-by-construction) AND the coupling does real work only
   at full `ℂ[Cl]` (`T7`).*
3. ### **THE ARRANGEMENT `Z = P + A − PR` IS ON THE OBJECT WITH `P` A CONSTRAINT AND `Z` ABSENT** —
   *`PR` is the prime sum's DIAGONAL FORM (the live factors are exactly the correlation window's
   reach, `T1`'s own derivation: `p^k` enters at `√(p^k)`; the prime-free cells are CC's `(1/2, 2)`);
   the missing `Z` channel is the missing codomain (self-product / Frobenius lift / leaves) in LEDGER
   form — one absence, third vocabulary.*
4. ### **`T1`–`T10` GAINED THEIR FIRST MEASURED CANDIDATE-ROW, AND THE SUB-GATE HONESTLY STANDS** —
   *`T2` partial-at-one-place (the towers' constructed unbounded direction), `T5` at its strongest
   form (radical zero exact), `T4` a DIFFERENT silhouette (recorded, not scored); grades at the model,
   the gate unpassed and said so.*
5. **The one-axis event table** *(every punctuation of the object and every tooth of the two-branch
   law at a prime-power address `a² = p^k` — match-at-address, mechanisms distinct, no merge)* **and
   the compilation targets** *(six, named and sized, none started)* **are at the relay tables.**

**NO CROSS-REGISTER EQUIVALENCE COMPILED OR CLAIMED — the pentagon's law governed the whole reading.
`h2` UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

> **ARC LINE (2026-08-18, sittings 20-21):** *the one-place limit closed at proof grade (the
> constrained sector positive-definite in the limit; the compression forced; one remainder named: the
> spectrum of SUS); the pentagon re-read filed as a keystone DRAFT (`FACES_OF_H2_AT_FINITE_INSTANCE`).
> Relay: the sittings-20-21 report.*

> **ARC LINE (2026-08-18, sitting 22):** *the archimedean E_1 identity lands at machine (the
> constrained sector = the +1 eigenspace on the even Sonin part; the place-uniform sentence at three
> grades; the double limit = the E_1-gluing = the codomain); the family-reach re-reading refused
> structurally; the Mathlib companion scoped (three files, the tower route replacing LCA Plancherel).*

> **§0 POINTER (2026-08-18, the implications filing):** *this road now reads under the charter's `§8`
> — the sign is not the target (the E_1 theorem at every place, grades per place); the target is the
> codomain (one absence, four names); class resolution is scaffolding; definiteness stays a predicate;
> a closure claim must fill all four names. Charter `v1.2`; workflow at `EXECUTOR_RULES` Rule 4.*

> **§0 POINTER (2026-08-18): the TARGET is now specified at `THE_CODOMAIN_SPECIFICATION.md` (v0.1
> DRAFT)** — *this road's object fills its clauses (a)–(d) at finite instance; clause (e) is the
> double limit and the four-named absence.*

> **STANDING POINTER (2026-08-18, the corrections-placement ruling):** *this document carries current
> statements; provenance and corrections live at `relay/reports/W-ATTEMPT-2-CORRECTIONS-REGISTER.md`.
> The sweep of historical correction narratives embedded in earlier sections is priced as this road's
> next revision pass.*
