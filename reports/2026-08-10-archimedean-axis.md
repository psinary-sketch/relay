# ACT 3 — THE ARCHIMEDEAN AXIS, SURVEYED AT CITE — 2026-08-10

**Registered at `85121f3` BEFORE the reading. Read at source; nothing built.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — E1 CONFIRMED, and the bridge sentence is Hesselholt's own

> *"the **Tate cohomology of the circle group** acting on the topological Hochschild homology"* — and
> *"in accordance with **Deninger's philosophy that `Θ` should be the infinitesimal generator of a
> Frobenius flow `Fr^t`**"*, with the **regularized determinant**.

**The two worlds are joined in one sentence, by him — in the finite-field setting.**

## §2 — E2 CONFIRMED, both computations in continuous-flow-with-trace form

**Deninger:** the `ℤ`-action *"**suspended** … to an **ℝ-action**"*; *"the **closed orbits** of the
ℝ-action … in bijection with the finite `φ`-orbits"*, *"`l(γ) = |o| log p`"*; a *"1-codimensional
**foliation** … transversal to the flow lines"*; and *"the **'explicit formulas' … to a transversal
index theorem**."* **Connes'** scaling trace carried at the corpus's cite (his text not re-fetched).

## §3 — E3 PARTIALLY CONFIRMED — the load-bearing half DECLARED UNSUPPORTED

**Supported:** the `T`-action is present over `ℤ` **definitionally**; the extraction theorem exists
**over `𝔽_q` only**.

> ### **NOT SUPPORTED — and it is the half the registration leaned on: THAT THE `T`-ACTION *IS* THE ARCHIMEDEAN SLOT.**
> Hesselholt's identification of `Θ` with Deninger's flow generator is made **in the finite-field
> setting**; neither source makes it over `ℤ`; and **`T` (compact circle) and `ℝ` are different
> groups** — an analogy drawn, not an equality.
>
> **The re-sizing is WEAKER than registered:** from *"no content at ∞"* to ***"a circle action present
> over `ℤ` by construction, whose ARITHMETIC MEANING at `∞` is unestablished, and whose extraction
> theorem is absent."*** *The registered form claimed the first and quietly assumed the second.*

## §4 — THE TRAP, DISCHARGED; THE DOOR LABELLED

> ### **"Real" in `THR` / "real cyclotomic" is `C₂`-equivariance — Atiyah's *Real*, a `ℤ/2` action — NOT the real place. A future sitting matching on the word matches a homonym.**

**Sourcing declared: no `THR` source fetched.** `Real`, `C₂`, `THR` → **0 occurrences** in all three
held sources. **A naming caution at that grade, not a graded row.**

## §5 — THE REMAINING ∞-CANDIDATES

| candidate | grade | reason |
|:--|:--|:--|
| condensed ring spectra over `ℝ` | **PRESENT as analysis · ABSENT as a zeta/trace channel** | *"**liquid ℝ-vector spaces** … a nice abelian category"*; `Spec ℤ` 0×, `Picard` 0×, `Chow` 0× |
| archimedean Fargues–Fontaine / twistor line | **NOT READ — DECLARED** | absent from held sources; **no ID guessed — the one guessed fetch this arc returned the wrong author's paper** |

## §6 — THE AXIS MAP, AND THE RE-PRICING

| at `∞`, in the derived world | status |
|:--|:--|
| a `T`-action on `THH(X)`, including `X = Spec ℤ` | **PRESENT — definitional** |
| a regularized determinant turning it into a zeta factor | **PRESENT over `𝔽_q`** |
| `Θ` as a Frobenius-flow generator | **PRESENT over `𝔽_q`, as an analogy** |
| any arithmetic reading of the `T`-action at `∞` of `ℚ` | **ABSENT from every source read** |
| a theorem extracting an `∞`-term over `ℤ` | **ABSENT** |

> ### **The era's question does NOT reach the registration's terminal sharpening — that presupposed the identification E3 could not establish. It reaches ONE STEP SHORT, and the difference is the whole discipline:**
>
> ### **NOT *"what is the trace theorem for the `T`-action over `ℤ`"* — but first: *"IS THE `T`-ACTION THE ARCHIMEDEAN SLOT AT ALL, or a circle awaiting an interpretation nobody has given it over `ℤ`?"***
>
> **Deninger's regularized determinant and Connes' scaling trace stand as the boundary conditions any
> eventual theorem must reproduce — cited as boundary conditions and nothing else. NO ROUTE CLAIMED.**

## §7 — FOUND-BEYOND

1. **HESSELHOLT ALREADY JOINS THE DERIVED WORLD TO DENINGER — IN ONE SENTENCE, AND ONLY OVER `𝔽_q`.**
   **The bridge the survey was looking for is written down, on the side where everything already
   worked.** *The arc's shape is now visible: every part exists over `𝔽_q`; the crossing to `ℚ` is
   what nobody has.*
2. **THE REGISTRATION CONTAINED THE TRAP IT ARMED AGAINST.** E3 armed the `Real`-vs-`ℝ` homonym trap
   and then **assumed a different unearned identification** — `T`-action = archimedean slot.
   **The guard was pointed at the word; the error came through the structure.**

## §8 — STOP-AND-HOLD

**Nothing built at `∞` — there was nothing at `∞` in the sources read to build with. `h2` is exactly
where it was.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `85121f3` (registration) → `885f7c1` |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE — **untouched this pass** |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
