# THE GLOBAL SECTION — THE RESTRICTED PRODUCT OF THE SONIN CLOSURES AND ITS `E₁` AT COMPLETE ROSTER

**Build document · ### `v0.15` (act 15: `§28` — THE BALL AT CC'S SCALE: the pair structure
DERIVED (Gram = signed `λ_n`; F SWAPS the pair; the block inverses ARE the `ε`-denominators) and
then VERIFIED by its own gate (`1e−5` at the stable cells); the ball-corrected trace COLLAPSES to
`≈ 0` — the band ladder's content was CONSTRAINT-SPAN content; verdict `(BALL-third)` with the
mechanism resolved: the anchor floor is EXPLAINED (the E-basis/bound gap, not a Sonin-trace
failure), the Sonin-subtraction candidate RESOLVED-AS-DERIVED (the band ladder and the Sonin trace
are different objects; the `ε`-machinery is the bridge, now at derivation grade); step one NOT
triggered; prior versions same day) ·
2026-08-18 · `W-CONSTRUCTION-1`, Route 1 (the `E₁` gluing) · ### THE TWO-LEG RULING ON THE FACE:
every "verified" claim below names its leg — VANILLA (our kernels, no Mathlib, no axioms,
finite/combinatorial) or MATHLIB (analytic statements at the classical profile `{propext,
Classical.choice, Quot.sound}`) — and its profile. ·
### SUB-GATE, DECLARED ON THE FACE per the charter's own trigger: `C1`/`C3` are not at content; the
attempt opens under the `§4` precedent (sub-gate, by the author, explicitly, the shortfall written
before any work). Rule 4 governs. SCOPE, once: this act STATES the global object and asks it
questions; every answer at its grade; the register moves only if clause (e) is filled and survives
formalization and review.**

> ### **THE REGISTER SENTENCE, QUOTED UNCHANGED: `h2` IS THE SINGLE OPEN PREMISE.** *Corrections:
> `relay/reports/W-ATTEMPT-2-CORRECTIONS-REGISTER.md`. The target this act builds toward:
> `THE_CODOMAIN_SPECIFICATION` `v0.2`, clause (e) — built over function fields, not yet over `ℚ`.*

---

## §1 — CHECKPOINT R1-1: THE GLOBAL OBJECT, STATED

**Definition (the object).** `𝒮 := ⊗′_v S̄_v` — the restricted tensor product over all places `v` of
`ℚ` (Tate's construction at the corpus's Conservation-of-Spectra grade), local factor the Sonin
closure `S̄_v` with its transform `F_v` (`F_v² = parity_v`), archimedean factor the EVEN Sonin space
with `F` (where `F² = 1`), the restriction taken with respect to a choice of unit vectors
`e_v° ∈ E₁(S̄_v)` at almost all finite `v`.

> ### **R1-1.0 — THE UNITS EXIST; NO CANONICAL CHOICE IS CLAIMED.** *Existence at proof grade:
> `d₁(2, n) = 2^{2n−2} − 2^{n−1} > 0` for `n ≥ 2` (the tower, nested, so `E₁(S̄₂) ≠ 0`); for odd `p`
> the two-line mechanism (`tr M` rational since `ℚ(ζ_{p^{2n}}) ∩ ℚ(i) = ℚ`; `Π` fixed-point-free at
> odd `q`) gives `d_λ = (pⁿ−1)²/4 > 0` — every place has `E₁`-vectors.* ### **AND THE FERRY'S
> CANDIDATE UNIT DOES NOT EXIST AS STATED, said plainly: the ball indicator is ORTHOGONAL to the whole
> Sonin space (`⟨1_ball, f⟩ = Σ_ball f = 0`), so its "E₁-normalized representative" is the zero
> projection.** *The object therefore depends on the CHOICE CLASS `{e_v°}` (a.a.-equal choices give
> the same space; different classes give isomorphic, non-identified spaces). Named-open: a canonical
> unit (the radial refinement is the candidate — see R1-1(iii)). Every question below has a
> choice-independent form.*

> ### **R1-1.F — THE FINDING BEFORE THE THREE QUESTIONS: THE GLOBAL SONIN OBJECT HAS NO NONZERO
> ADELIC-FUNCTION REALIZATION.** *Proof (three lines): an elementary vector `⊗f_v` (with `f_v = e_v°`
> at a.a. `v`) would realize as the adelic function `x ↦ ∏_v f_v(x_v)`; almost every adelic point has
> `x_v ∈ ℤ_v` for a.a. `v`; every Sonin vector VANISHES on the ball; hence the product vanishes at
> a.e. point.* ∎ ### **The global section is an ABSTRACT Hilbert space, not an adelic function
> space — it lives on the far side of exactly the directions Sonin removes, which are exactly the
> directions that make adelic realization possible (the pole/ball directions). KIN, never merged:
> CCM's radical was "all functions that extend to adeles" (`T5`, at cite) — the built object EXCLUDES
> the adelic-extension directions by construction and keeps radical zero; CCM's construction KEEPS
> them and quotients a large radical. Two routes around one obstruction, recorded at kinship grade.**

**(i) The transform.** `F := ⊗_v F_v` is well-defined on `𝒮` BECAUSE the units lie in `E₁`
(`F_v e_v° = e_v°` at a.a. `v` — this is what the `E₁`-restriction is FOR), unitary (each factor
unitary fixing the units), with `F² = ⊗Π_v` the global parity (also fixing the units: `E₁ ⊂` the
even part). ### **PROOF GRADE — with the identification caveat carried: this is the TENSOR transform
on the abstract object; "it is Tate's adelic Fourier transform" is REFUSED as a statement about
functions by R1-1.F (there are no functions). The abstract `F` is the transform the object has.**

**(ii) The global `E₁`.** ### **THE GLOBAL `+1` EIGENSPACE IS NOT `⊗′E₁,v` — IT IS THE SECTOR SUM,
AND THE FINITE CELLS ALREADY SAID SO:** *`E₁(𝒮) = closure ⊕ {⊗′E_{λ_v},v : λ_v ≠ 1 at finitely many
`v`, ∏λ_v = 1}` — the eigenvalue-product-1 sectors (only finitely many factors may leave the unit's
`+1`, so the products are finite). Proof grade: commuting order-4 unitaries on a restricted product.*
**The banked cell arithmetic (T-fixed sectors `λμν = 1`; the class reviving through `(i, −i)`) is
exactly this decomposition's finite shadow — the global `E₁` includes the conjugate-sector
combinations, and `⊗′E₁,v` is its PRINCIPAL sector.** *Both branches of the ferried (ii): "yes" for
the principal sector; "no, and the refinement is the structure" for the whole — the refinement
recorded as the answer.*

**(iii) The compressed scaling.** For an idele `λ` with unit components at a.a. `v`: `U_λ = ⊗U_{λ_v}`
is well-defined on `𝒮` PROVIDED the units are fixed by unit-scalings — ### **the RADIAL refinement:
choose `e_v°` radial (shell-constant). Existence of radial `E₁`-vectors: NAMED-OPEN at proof grade
(the radial Sonin subspace is `F`-stable and parity-even, so its `E₁`-part exists unless the radial
Sonin space is null — expected nonzero for `n ≥ 2`, not yet proved; the one named gap of R1-1).**
*Granting it:* `S U_λ S` is a bounded compression, and ### **THE COMPRESSION IS FORCED GLOBALLY AT
PROOF GRADE: for the principal dilation at one place (`λ = p` at `v = p`, units elsewhere), the
banked one-place witness (`(Uf)^ ≠ 0` on the ball, exact) tensors with the units to a GLOBAL witness:
`U_λ(𝒮) ⊄ 𝒮`.** ∎ *The global SOT question (does `S_n U S_n → SUS` globally as the per-place result
did) is NAMED OPEN.*

## §2 — CHECKPOINT R1-2: THE PAIRING AND THE DIAGONAL

**The pairing.** `B(f, g) = ⟨f, Fg⟩` on `𝒮`; on `E₁(𝒮)`, `B = ‖·‖²` — ### **POSITIVE BY
CONSTRUCTION, SAID PLAINLY: a norm is not a theorem.**

**The diagonal at complete roster.** The global sections `D(a) := (⊗_{p^k ≤ a²}` cell vectors`) ⊗
(⊗_{p > a²} e_p°) ⊗ (∞`-factor`)` sit in `𝒮` canonically given the units, and: ### **R1-2.1 (proof
grade, definitional): the elementary diagonal family `∪_a D(a)`-vectors is DENSE in `𝒮` — density of
finitely-non-unit elementary tensors is the definition of the restricted product.** The ferried
question is sharper — WEIL'S constrained class (`f = g⋆g̃`, `ĝ(±i/2) = 0`, all supports), mapped in:

> ### **R1-2.2 — THE DENSITY QUESTION LOCALIZES TO ONE PLACE, AND IT IS `∞`:** *at every finite place
> the reachable vectors are dense (Schwartz–Bruhat density per level + the tower's on-the-nose
> stability — sitting 20's machinery, proof grade); at `∞` the constrained class reaches `E₁(∞)`
> through TWO projections with GROWING RANK (b33: 9 → 21 at the measured window) but DENSITY IN
> `E₁(∞)` IS OPEN — the measured rank is 21 against an `E₁` of dimension ~500 at that grid.* ###
> **VERDICT: `(D-dense)` PROVED for the structural diagonal; for the Weil class, (D-dense) vs
> (D-proper) REDUCES, at proof grade, to ONE named statement: "the closure of `Π₊S(`the Weil
> family`)` in `E₁(∞)` is all of `E₁(∞)`" — the archimedean density question. If it fails, the
> complement is a PROPER closed subspace of `E₁(∞)` tensoring into a proper subspace globally — and
> that complement is where clause (e)'s `Z`-shaped structure would live (the ferried (D-proper)
> branch's content, localized).* **The obstacle exactly named; not closed this act.**

## §3 — CHECKPOINT R1-3: THE LEDGER MEETS THE OBJECT

*On the constrained class the pole term dies (the constraints; on the object, vacuous by
construction — banked), so the classical arrangement reads `Σ_ρ ĝ(ρ) = W_∞(f) − Σ_p W_p(f)`.* The
object supplies `‖·‖²` on `E₁(𝒮)`. Therefore:

> ### **`(L-remainder)` LANDS, WITH THE REMAINDER'S NAME AND ADDRESS: define `R(g) := (W_∞(f) −
> Σ_p W_p(f)) − ‖image(f)‖²_{E₁}`. Then `R(g) = Σ_ρ ĝ(ρ) − ‖image(f)‖²` — THE REMAINDER IS THE ZERO
> CHANNEL MINUS THE OBJECT'S NORM, and the statement "`R ≥ −‖·‖²`-trivially, `R` vanishes (or the
> ledger is `≥ 0`) on the class" IS A RESTATEMENT OF `h2`, NOT A RESULT — said in exactly those
> words.** *Where it lives in the object: (1) in R1-2's archimedean complement (if `(D-proper)`, the
> `Z`-shaped structure has a home of measurable size); (2) OFF the adelic-function picture entirely
> (R1-1.F: the zero side classically enters through the theta/pole side of the adelic story — the
> very directions the Sonin conditions removed).* ### **`(L-identity)` DID NOT LAND AND IS NOT
> CLAIMED; no partial identity is promoted; the closure protocol's four steps stand between any
> future `(L-identity)` and the register.**

## §4 — ROUTE 2 IN PARALLEL — R2-1: THE SPECTRUM OF `SUS` AT `p = 2`

> ### **THEOREM (proof grade, this act): `T = SUS` on `S̄₂` is a COMPLETELY NON-UNITARY contraction,
> and it has NO eigenvalues of modulus 1.** *Proof: if `Tf = λf` with `|λ| = 1`, then `‖SUf‖ = ‖f‖ =
> ‖Uf‖` forces `(1−S)Uf = 0`, so `Uf ∈ S̄` and `Uf = λf`; iterating, `U^k f = λ^k f ∈ S̄` for all
> `k`; but `(U^k f)^(ξ) = 2^{k/2}f̂(ξ/2^k)`, and the Sonin condition at every `k` forces `f̂` to
> vanish on `2^{−k}ℤ₂` for all `k`, i.e. on all of `ℚ₂`: `f = 0`. The same argument on `T* = SU⁻¹S`
> (the support side) kills any candidate unitary subspace: the unitary part of the Nagy–Foiaş
> decomposition is `0`.* ∎ **Structure beside it: the defect `1 − T*T` is governed by ONE SHELL of
> `f̂` (the first outer shell — the banked witness's home), so `T` is isometric on the
> codimension-one-shell subspace and completely non-unitary globally.**
>
> *The registered comparison (Deninger Rem 5.7(5), banked — "are the eigenvalues Weil numbers of
> weight 1?"): the modulus-`q^{1/2}` structure in OUR object lives in the ORBIT-QUOTIENT channel (the
> fifth law's exact `q^{−1/2}`, banked MATCH-AT-CONTENT), NOT in `SUS`'s unitary spectrum — there is
> none.* ### **FED TO R1-3: the zero channel cannot live in `SUS`'s unitary part; the object's own
> scaling operator is spectrally incapable of carrying unimodular (zero-like) spectrum — consistent
> with, and sharpening, the remainder's off-object address.**

## §5 — THE ARC

*The Mathlib companion (files A–C) opens BESIDE R1-1 as its formalization layer (the sitting-22
scope; the tower route to unitarity is exactly what R1-1(i) uses per place). The other strands,
Phase-2 clusters, the roads' queued revision passes, the held acts and counsel: unaffected.*

## §6 — CHECKPOINT R1-4 (ACT 2): ### **THE ARCHIMEDEAN CLOSURE — `(C-dense)` PROVED**

> ### **THEOREM (proof grade): `cl(Π₊ S Ψ_Weil) = E₁(∞)` — the twice-projected Weil family is DENSE
> in the archimedean constrained sector.** *Proof in three steps: (1) the span of
> `{P(u)e^{−u²/2}}` is dense in `L²(ℝ)_even` — under `x = ±e^u` the measure transports to a
> Gaussian-decay weight in `u`, whose Hamburger moment problem is DETERMINATE, so polynomials are
> dense (Hermite completeness under the Gaussian weight — the `W-exact` mechanism, standard at text);
> (2) an orthogonal projection maps a dense set to a dense set of its range (`‖Pψ_n − h‖ ≤
> ‖ψ_n − h‖` for `h` in the range); (3) compose `S` then `Π₊`.* ∎
>
> ### **THE CONSEQUENCE: R1-2's LOCALIZED QUESTION CLOSES.** *The Weil class's reach is now dense at
> EVERY place (finite: Schwartz–Bruhat + the towers; `∞`: this theorem). The remaining step to
> global `(D-dense)` in `E₁(𝒮)` is the tensor assembly — per-factor density gives density of the
> elementary-tensor image PROVIDED the class's image realizes per-place factors independently:*
> ### **THE FACTORIZABLE-IMAGE LEMMA — the exact missing lemma, NAMED** *(the map `g ↦ section`
> takes one test function to one section; independent factor control needs the class's algebra — not
> proved this act).* **And the finite-degree slowness the bench measured (b33/b34) is the DENSITY'S
> TAIL, not a wall — the b34 instrument note is on the relay record (its first run was VOID by an
> ordering defect, disclosed; its fixed run is the corroboration line, whatever it shows — the proof
> does not lean on it).**

## §7 — CHECKPOINT R1-5 (ACT 2): THE SECTOR SUM AGAINST THE SIGN

1. ### **`B = ‖·‖²` ON THE WHOLE SECTOR SUM — proof grade, one line: on a `∏λ_v = +1` sector,
   `B(f,f) = (∏λ_v)‖f‖² = +‖f‖²`. The global positive sector is ALL of `E₁(𝒮)`, not just the
   principal sector.** *Likewise `−‖·‖²` on `∏λ_v = −1` sectors and null Hermitian part on `±i`
   products — the `(d/4, d/4, d/2)` shape's roster form.*
2. ### **THE `h = 1` CASE DECIDES THE RE-GRADE, AS FERRIED: over `ℚ` the antipode is trivial, so
   T-fixed = `∏λ = +1` = the FULL sector sum — the finite-instance constrained-class positivity was
   FULL-SUM positivity all along** *(b31's structure + the sector algebra; at `h = 3` the class was
   the `ν = +1` part and the anti-invariant channel carried the T-fixed negatives — class-resolution
   structure, as banked).*
3. *Sitting 9's arithmetic, re-read globally: arrivals are imbalance-free (flat factors multiply
   every sector uniformly at the dead edge) — at complete roster each place's entry rescales the
   sector ledger without tilting it; negativity lives exactly in the `∏λ = −1` grading, reachable
   only through finitely many non-unit factors.*

## §8 — CHECKPOINT R1-6 (ACT 2): THE REMAINDER'S ADDRESS, BOTH BRANCHES

> **(branch T — a trace on `𝒮`):** *could `R(g) = Σ_ρ ĝ(ρ) − ‖image‖²` be a distributional trace of
> the compressed scaling on `𝒮` (Deninger's Conj 3.1 shape, at the banked content)?* ### **THE CNU
> THEOREM CONSTRAINS IT AT PROOF GRADE: the compressed scaling has NO unimodular point spectrum (per
> place, R2-1), so the zero channel CANNOT be point spectrum of the object's own scaling. What
> remains possible on-object is an absolutely-continuous/distributional layer (the c.n.u. theory's
> a.c. characteristic function) — NAMED, not developed.**
>
> **(branch A — the ambient):** *`R(g)` lives OFF `𝒮` — in the ambient the object deliberately
> excludes: the pole/ball directions (where theta/Eisenstein live, and through which the zero side
> classically enters), the very directions whose removal makes the object abstract (R1-1.F) and its
> radical zero.* ### **With `(C-dense)` proved, the on-object hiding room SHRANK: the `Z`-shaped
> structure has no home in `E₁(∞)`'s complement inside the object. BOTH BRANCHES POINT THE SAME WAY:
> THE `Z` CHANNEL'S ADDRESS IS THE CODOMAIN'S — off the built object, in what (e) would add. Said
> so; no promotion.**

## §9 — ACT 3: THE FACTORIZABLE-IMAGE LEMMA, ATTACKED — ### **`(F-yes)` AT PROOF GRADE MODULO TWO NAMED STEPS**

> *The argument in three legs:* **(1) FACTORIZABLE SPANNING** — *Weil's test class on the ideles is
> spanned by factorizable data `∏_v g_v` (finitely many non-trivial factors, unit-indicators beyond):
> the Schwartz–Bruhat space of a restricted product is the restricted tensor of the local ones —
> STANDARD-AT-TEXT (Tate's structure, at the corpus's Conservation-of-Spectra grade).* **(2)
> PER-PLACE DENSITY** — *the local images are dense in every `S̄_v` (finite: Schwartz–Bruhat + the
> towers, proved; `∞`: `(C-dense)`, proved at act 2).* **(3) TAIL TRIVIALITY** — *the unit-indicator
> factors act trivially on the units `e_v°` PROVIDED the units are unit-scaling-invariant.* *Then
> factorizable images are per-factor dense elementary tensors ⟹ dense in `𝒮` ⟹ `Π_{E₁}`-projected
> dense in the full sector sum `E₁(𝒮)`.*
>
> ### **THE TWO NAMED STEPS THAT REMAIN:** **(α) THE RADIAL-UNIT EXISTENCE** *(carried from act 1:
> unit-scaling-invariant `E₁` vectors at almost all places — leg (3)'s hypothesis);* **(β) THE
> CONSTRAINT-INVISIBILITY STEP** *(the Weil conditions `ĝ(±i/2) = 0` remove only pole components,
> and the object ANNIHILATES pole components (the ball directions are orthogonal to Son, banked) —
> so the constrained class's image closure equals the unconstrained's; the technical step: the
> image map's spectral support avoids `±i/2` — proof-sketch grade, the one step to write).* ###
> **VERDICT AT ACT 3: `(F-yes)` modulo (α) and (β).**
>
> ### **ACT 4 — THE LEMMA CLOSES: `(F-yes)` AT PROOF GRADE.** **(β) PROVED:** *the constraint removes
> only pole components; the pole components act only through the ball directions (Tate's
> identification of the `±i/2` content with the ball side, at cite), and the object is ORTHOGONAL to
> the ball directions at every place (banked exact; at `∞` by the Sonin condition) — so the compressed
> action `S·ϑ(g)·S` is unchanged by pole components: the constrained class's image closure equals the
> unconstrained's.* ∎ **(α) DISSOLVES BY THE CONSTRUCTION'S OWN DEFINITION, WITH A FINDING:** *Weil
> test data have compact idele support, hence FINITELY MANY active places; the corpus's diagonal
> construction puts the PLAIN unit vector at every inactive place — no action at the tail, so no
> unit-invariance is needed and leg (3) is vacuous.* ### **THE FINDING, recorded so nobody re-routes
> silently: the `ϑ`-tail route GENUINELY FAILS — unit-averaging can annihilate the whole Sonin
> space: at `(2,1)` the unit `3` acts as `−1` on `Son` (`U₃(e₁−e₃) = −(e₁−e₃)`, one line, VANILLA
> LEG at zero axioms), so the unit-average of every Sonin vector is 0 there. Radial Sonin vectors do
> not exist at `(2,1)`.** ### **CONSEQUENCE: `(D-dense)` GLOBAL AT PROOF GRADE — the Weil class's
> diagonal images are dense in `𝒮`, and `Π_{E₁}`-projected dense in the full sector sum `E₁(𝒮)`.
> Route 1's density chain is COMPLETE (the standard-at-text cites named: Schwartz–Bruhat restricted
> structure; Tate's pole/ball identification). What remains of Route 1 is `(L-identity)` — the
> ledger identity itself, the `h2`-restatement, behind the closure protocol.**

### §9.1 — THE KERNEL CATCHES UP *(the sorry-count debt, printed)*

| file | proved (std3 axiom profile) | labeled sorries |
|:--|:--|:--|
| `LocalLimit.lean` | ### **the CNU ENGINE (the full act-1 argument, formalized) · positivity-is-norm · radical-zero** | **1** — `padicFourierData_exists`, OWED TO FILES B–C |
| `GlobalSection.lean` | `F⁴ = 1` from the fields · fixed-sector norm | **1** — `globalSectionData_exists`, OWED TO INFRASTRUCTURE (the Hilbert `⊗′`; Mathlib holds only the topological `RestrictedProduct`, grep-checked) |
| `PadicStandardAddChar.lean` (file A) | ### **the definition's membership (real valuation arithmetic — the root sorry CLEARED) · nonneg · lt-one · vanishing-on-`ℤ_p` · `map_zero`** | **3** — the spec (via `ker_toZModPow`) · additivity mod 1 · the conductor iff — the author's-hand session to PR |
| *(the four-sector decomposition + SOT lemmas)* | stated | ABSTRACT-LEMMA-PASS (not owed to B–C) |

### **TOTAL LABELED DEBT: 5 sorries, each with its owner. Nothing at complete roster is stated
anywhere in the kernel files, and each file says so.**

## §10 — THE OBSERVATION (ACT 4): ### **THE OBJECT IS THE PART WITH NEITHER POLES NOR ZEROS**

> *Filed at grade, cross-linked to `SIGN_ARRANGEMENT_RECONCILIATION`, no promotion:* ### **the poles
> are excluded BY CONSTRUCTION (the Sonin conditions ARE orthogonality to the pole directions —
> banked exact; VANILLA-LEG instances compiled at zero axioms) and the zeros are excluded BY THEOREM
> (the CNU theorem: no unimodular point spectrum on the object — MATHLIB LEG, the engine at std3).**
> *The arrangement `Z = P + A − PR` realized on the object, each term at its address: `P` — a
> CONSTRAINT (the class's own definition); `A` and `PR` — jointly the NORM on the full sector sum
> (the section's weight ledger: the `∞` channel and the finite channels coupled in `‖·‖²` on
> `E₁(𝒮)`); `Z` — the EXACT COMPLEMENT (`R(g) = Σ_ρ ĝ(ρ) − ‖image‖²`, act 2's remainder — the
> arrangement's global form on the object).* **The built object realizes the explicit formula's
> right-hand side and carries neither of the two spectral sides — which is exactly why its sign is a
> norm and its `Z` question is the codomain's.**

## §11 — THE SPECTRAL QUESTION (ACT 4): REGISTERED AND ATTACKED

**Registered:** *(AC-zeros)* the compressed scaling has absolutely continuous spectrum whose density
is the zero side — the remainder lands ON the object as a distributional trace (Deninger's Lefschetz
shape from below; CNU satisfied since a.c. is not point spectrum); *(AC-none)* purely singular/no
a.c. part — the zeros off-object definitively; *(AC-third)* filed. **Both branches carry the honest
line: a distributional-trace identity, if it landed, would be a RESTATEMENT of the explicit formula
on the object, not a sign result — the sign is `R(g)`'s positivity on the class, behind the closure
protocol.**

> ### **THE ATTACK'S LANDING (proof-search, this act): the STRUCTURAL half of `(AC-zeros)` holds AT
> CITE — the minimal unitary dilation of a completely non-unitary contraction is ABSOLUTELY
> CONTINUOUS (Nagy–Foiaş), and our compressed scaling is c.n.u. (the theorem, act 1): there IS a
> canonical a.c. unitary attached to the object's scaling.** *Whether its density is the zero side
> is OPEN and now SHARPENED to one computable object:* ### **the Nagy–Foiaş characteristic function
> `Θ_T` of the compressed scaling — tractable because the defect is ONE-SHELL (banked): `Θ_T` is a
> one-shell-indexed operator function whose boundary values encode the a.c. density. NAMED as the
> next object; not computed this act.** *(The fifth law is the compression's per-level mass; the
> orbit-quotient trace's Weil shape stands banked beside it.)* **Verdict: `(AC-third)`-shaped — the
> dilation's a.c. nature at cite, the density question open, `Θ_T` the address.**

## §12 — THE TRACE COMPUTED (ACT 5): ### **THE DISTRIBUTIONAL TRACE OF THE COMPRESSED SCALING — THE FINITE PLACES TRACE-SILENT OFF THE IDENTITY (PROOF GRADE), THE GLOBAL TRACE `A`-CHANNEL-ONLY, THE TWO-CHANNEL ANATOMY**

**One vocabulary flag on the face, not smoothed:** *act 4's `Θ_T` named the Nagy–Foiaş
CHARACTERISTIC FUNCTION; the act-5 order computes the DISTRIBUTIONAL TRACE under that name. The two
are linked — the characteristic function's boundary values encode the a.c. density the trace sees —
and this section computes the trace. The characteristic function proper remains the named object of
`§11`, not computed.*

### §12(a) — the per-place factors, finite places: **EXACT SILENCE**

> ### **THEOREM (proof grade; VANILLA leg, no axioms): at every banked level the compressed scaling
> is TRACE-SILENT off the identity — `Tr(U_{p^k} S) = 0` EXACTLY for every `k ≥ 1`, both primes,
> level-stable.** *The mechanism is the fixed-point localization (b21/b23 banked): a diagonal term at
> Sonin cell `α` needs `α(p^k − 1) ≡ 0 (mod p^n)` with `1 ≤ α < p^n` — never, since
> `gcd(p^k − 1, p^n) = 1`. Compiled cell-by-cell this act: `TraceSilence.lean`, six theorems, all
> "does not depend on any axioms" (`(2,1)`, `(2,2)`, `(2,3)`, `(3,1)`, `(3,2)` + the identity
> coefficient). The level-regularized trace (the limit of level traces) is 0 for `k ≠ 0` — each term
> exactly zero, proof grade; the identity coefficient is the DIMENSION `(p^n − 1)²`, divergent as
> `n → ∞`: the per-place distributional trace is `(dim)·δ₀`.*

**Both branches, decided:** *(branch: the trace reproduces the prime term `log p · p^{−k/2}(…)`)* —
### **REFUTED, exactly: the trace is zero where the prime term is not.** *(branch: the prime terms
live elsewhere)* — **LANDS: the Frobenius MASS is nonzero while the trace is zero (the fifth law:
`‖SUS‖_F = √p·(p^{n−1} − 1)`), and the ORBIT-QUOTIENT trace carries the Weil `q^{−1/2}` shape
(banked). The prime terms are NOT trace terms on the object — they are quotient-channel terms.**

### §12(b) — the global assembly: **the trace reduces to the archimedean flow**

*The identity coefficients diverge, so the assembly is stated with the level-NORMALIZED trace
`τ = Tr/dim` — a CHOICE, on the face (the von Neumann normalization; nothing else converges).* Then
`τ(U_{p^k} S) = 0` for `k ≠ 0` and `τ(S) = 1` per finite place, and over the restricted product:
**any idele-class dilation with a nontrivial finite component meets a zero factor; finite-trivial
dilations pass with factor 1.** ### **CONSEQUENCE: the global distributional trace of the compressed
scaling SEES ONLY THE ARCHIMEDEAN FLOW — it is `A`-channel-only.**

### §12(c) — evaluated on the constrained class: **the exact form**

`Θ(g) = τ`-assembled `= Tr_∞(ϑ(g_∞) S_∞)` — *the archimedean compressed flow's trace against `g`'s
finite-trivial part. At `∞` the trace is finite against constrained test data and takes CC's
`ε`-form (Theorem 4.7's `ε` as the archimedean form — AT CONTENT from the corpus's banked prolate
layer: `ε′(1⁺) = Σt(n) = 22.9965`, Lemma 5.4's `t(n)` reproduced 15/15 at bench). The
operator-theoretic derivation of the `ε`-form on OUR model (not CC's) is a NAMED OPEN LEMMA — at
content, not at proof grade, said so.*

### §12(d) — the comparison, stated and NOT decided

`Θ(g)` **vs** `Σ_ρ ĝ(ρ) − ‖image(g)‖²` — *the `(L-identity)`. It lands only through the closure
protocol's four steps in order; nothing here decides it.* What this act CAN bank is structural:

> ### **THE TWO-CHANNEL ANATOMY (banked): the object's compressed-scaling trace is `A`-only; the
> prime terms live in the orbit-quotient channel; therefore any `(L-identity)` must combine TWO
> channels — trace + quotient — and the explicit formula's `A − PR` split is REPRODUCED as the
> object's trace/quotient anatomy.** *The arrangement `Z = P + A − PR` (`§10`) now has each term at a
> CHANNEL, not just an address. The comparison itself: behind the closure protocol, untouched.*

### §12(e) — the sorry ledger's movement (act 5): **5 → 2**

| file | before | after | state |
|:--|:--|:--|:--|
| **A** `PadicStandardAddChar` | 3 | ### **0** | **COMPLETE — the target `stdAddChar_eq_one_iff` PROVED** (the spec via `toZModPow`'s kernel; additivity via the p-power-denominator integrality bridge; conductor from the spec); terminal: `{propext, Classical.choice, Quot.sound}`, no `sorryAx`, all three prints |
| B–C `LocalLimit` realization | 1 | 1 | stands; the owed list SHRINKS — the character is now proof-grade; remaining: the transform on `LocallyConstant` + tower-Plancherel |
| infrastructure `GlobalSection` | 1 | 1 | stands; the construction path spelled at the site (colimit of finite tensors along `E₁`-unit inclusions + Hilbert completion — Mathlib holds neither) |
| **VANILLA leg** | 62 theorems | ### **68** | `TraceSilence.lean` + `AxiomCheckTS`: 6/6 "does not depend on any axioms" |

## §13 — THE ADJOINTNESS QUESTION (ACT 6): ### **REGISTERED BEFORE COMPUTATION; THE LITERAL FORM REFUTED AT PROOF GRADE; THE POLAR FORM A RESTATEMENT OF `(L-identity)`, SAID SO**

**Registered, both branches longhand:** *is the quotient channel the adjoint of the trace channel
under the antipode-involution — `Θ_quotient = (Θ_trace)*` twisted by `s ↦ 1−s`?* **(ADJ-yes):** the
two channels are polar parts of ONE operator `Θ = (trace) + (skew)`, Weil's `A − PR = Θ − Θ*·twist`,
and positivity on the constrained class is the vanishing of the skew part on the sector sum — the
`(L-identity)` restated as an adjointness relation between two computed channels. **(ADJ-no):** the
exact failure named in operator terms — the codomain's size as an operator, at one place.
**(ADJ-third):** filed.

> ### **THE ONE-PLACE STATEMENT DECIDES THE LITERAL FORM: REFUTED, proof grade at the banked
> cells.** *The antipode's twist is a REWEIGHTING of the trace sequence (adjoint reads `−k`, the
> twist multiplies by the Weil weight) — a reweighting cannot create support. The trace channel is
> supported AT THE IDENTITY (`§12(a)`, exact zeros, vanilla leg); the quotient channel speaks at
> every `k` (the Weil `q^{−k/2}` shape, banked). Hence NO twist of the trace channel reaches the
> quotient channel.* **The finite shadow, decide-checked this act (`OrbitDictionary.lean`, VANILLA,
> no axioms): the twist of the zero sequence is zero (`twisted_silence`); the quotient's weight is
> the FIBER of the shell shift — multiplication by `p` is exactly `p`-to-1 onto the next shell
> (`fiber_two`, `fiber_three`): the `q^{1/2}` per step is the unitary normalization of a `p`-to-1
> map — and that weight is NONZERO where the twisted trace is zero (`channels_distinct`).**

> ### **THE EXACT FAILURE, IN OPERATOR TERMS: the quotient trace is the trace AFTER the
> fiber-summation (the transfer over the `p`-to-1 shell shift); the codomain's size as an operator,
> at one place, is the transfer's fiber weight — `√p` per step, fiber exactly `p` (vanilla).** *What
> survives of (ADJ-yes) is its POLAR form — `Θ = trace + skew` with `A − PR = Θ − Θ*·twist` — which
> is a RESTATEMENT of `(L-identity)`, said as a restatement; its proof-grade form is the closure
> protocol's business.* **VERDICT: `(ADJ-third)`-shaped — literal-NO at proof grade, polar-OPEN as
> restatement.**

## §14 — THE `ε`-LEMMA (ACT 6): ### **NOT THE SAME `S` — THE DIFFERENCE NAMED: THE `(−1)`-SECTOR TRACE; THE CLEAN HALF PROVED**

*The question: on our model, is `Θ(g)` exactly CC Theorem 4.7's trace `Tr(ϑ(f)S) = W_∞(f) +
∫f(ρ)ε(ρ)dρ` (at content) with the same `S`?*

> ### **ANSWER: NO — and the difference is NAMED, not vague. Our `S` is `Π₊S_Son` (the `E₁`/F-fixed
> restriction of the Sonin projection — the corpus's constrained sector, the sitting-22 theorem);
> CC's `S` is the full (even) Sonin projection. The discrepancy is EXACTLY the `(−1)`-sector trace:
> on Weil-even test data (`g(λ) = g(1/λ)`), `ϑ(g)` COMMUTES with `F` (the intertwining
> `F U_λ = U_{1/λ} F` plus evenness), so the `±1` sectors of the even Sonin space are
> `ϑ(g)`-invariant and the trace SPLITS:*
>
> `Θ(g) = Tr(ϑ(g)S_Son) − Tr(ϑ(g)Π₋S_Son) = W_∞(g) + ∫g ε − Δ₋(g)`
>
> ### **with `Δ₋(g)` the odd-sector defect — one more trace of the same shape, computable, the
> `ε`-lemma's remaining owner (with the trace-class bookkeeping of the split).** *The clean half is
> now MATHLIB-LEG at the classical profile: `eigenvector_of_commute` (LocalLimit.lean, PROVED —
> eigenvectors persist under commuting operators). The benefit stands where it was derived: sitting
> 3's `ε`-sign applies to the FULL-sector form; the prolate layer's 15/15 remains the `ε`-form's
> bench pin.*

## §15 — THE FULL COMPARISON, WRITTEN AS AN EQUATION (ACT 6): ### **EVERY TERM NAMED AND GRADED; NOTHING DECIDED**

> ### `W_∞(g) − Σ_p W_p(g)   ≟   [Tr_∞(ϑ(g)S_Son) + ∫g ε] − Θ_quotient(g)`
>
> *(with `Tr_∞` read per `§14`: our object's trace is CC's minus `Δ₋(g)`; the left side is the
> explicit formula's geometric side on the constrained class in the ferry's stated form.)*

| term | what it is | grade |
|:--|:--|:--|
| `W_∞(g)` | the archimedean Weil term | CITED (classical) |
| `Σ_p W_p(g)` | the prime terms | CITED (classical) |
| `Tr_∞(ϑ(g)S_Son)` | the full-Sonin archimedean trace | AT CONTENT (CC Thm 4.7); our object's trace = this − `Δ₋(g)`, the defect NAMED (`§14`) |
| `∫g ε` | CC's `ε` integral | CITED at content; the sign on the class DERIVED (sitting 3) |
| `Θ_quotient(g)` | the quotient channel | BANKED shape (Weil `q^{−k/2}`); the fiber-normalization EXACT at the banked cells (vanilla); the global normalization OPEN |
| `Δ₋(g)` | the odd-sector defect | NAMED; owner: the `ε`-lemma |

> ### **THE EQUALITY OF THE TWO SIDES ON THE CONSTRAINED CLASS IS `(L-identity)` — THE EXACT FORM
> OF `h2` ON THE BUILT OBJECT. STATED. NOT DECIDED. It lands only through the closure protocol's
> four steps in order.**

## §16 — THE FIELD'S DICTIONARY AS A THEOREM (ACT 6): ### **DENINGER'S §4 TABLE, `AT-CITE` → `DERIVED-AT-MODEL` FOR THE CORPUS'S OWN OBJECT**

*Deninger's table (math/0204110 `§4`, at content): finite place ↔ closed orbit (length `log p`);
infinite place ↔ fixed point.* **Both rows now hold OF OUR OBJECT at stated grades, VANILLA leg
where finite:** *(finite row)* the scaling at a finite place has **no fixed point off the identity**
(`no_fixed_offzero`, no axioms) and acts on valuation shells as **the shift with fiber exactly `p`**
(`shell_shift_2/3` + `fiber_two/three`, no axioms) — with the trace silent off the identity
(`TraceSilence`, cited): ### **the finite places contribute only through RECURRENCE — closed-orbit
data, not fixed-point data.** *(infinite row)* the archimedean compressed flow is the one with an
honest distributional trace against test data (`§12(c)`, the `ε`-form at content) — ### **the
fixed-point channel is the archimedean one.** *Cross-linked, NO MERGE: Deninger's conjectural site
is not claimed; what is claimed is that the dictionary's two rows are DERIVED at our model — the
analogy's grade raised from AT-CITE to DERIVED-AT-MODEL for the corpus's own object, and no
further.*

## §17 — `Δ₋` COMPUTED (ACT 7): ### **THE ODD-SECTOR DEFECT'S CLOSED FORM IS THE ODD-INDEX PART OF CC'S OWN `t(n)` SERIES**

**Registered longhand before computation: (Δ-small) — bounded, sign stated as data; (Δ-structure) —
a closed form in the prolate spectral data with complementary parity; (Δ-third) filed.**

*The sector naming, VERIFIED BEFORE USE: sitting 23's `(R-prolate)` (the even ladder alternates
`+ − + −` exactly; `E₁` = prolate indices `n ≡ 0 mod 4`) composed with P2's banked
`ξ_n = √2·ψ_{2n}` (CC's series index names the n-th EVEN prolate) gives the split IN CC'S OWN
INDEX: `E₁` = even `n`; the `(−1)`-sector = odd `n`.*

> ### **(Δ-structure) LANDS: `Δ₋`'s series is the ODD-INDEX part of CC's own `t(n)` series — the
> same `λ(n)`, `ξ_n(1)` data, complementary parity; `t(n) = λ(n)²ξ_n(1)²/(1−λ(n)²)` per the corpus's
> banked sentence.** *At the `ε′(1⁺)` evaluation pin (b35, registration banked first, run on the
> Opus lane, C1–C3 PASS): the `E₁` share is `14.1773` (61.6%), the odd-sector share `8.8191`
> (38.3%), tail `3.19e−05` — of the banked `22.9964757`.* **(Δ-small)'s sign clause: each `t(n)` is
> POSITIVE STRUCTURALLY (a square over `1−λ² > 0`) — the shares are reported as DATA at the pin; the
> full `ε₋(ρ)` function inherits CC's series shape restricted to odd `n`; its trace-class
> bookkeeping remains the `ε`-lemma's named owner. Recorded as the object's archimedean correction;
> NO sentence about Weil's ledger.**

## §18 — THE QUOTIENT'S GLOBAL NORMALIZATION (ACT 7): ### **`(N-forced)` — THE MECHANISM NAMED AT THREE JOINTS, MODULO ONE CITED LEMMA; THE VOLUME CONVENTION, AND ACT 5'S CAVEAT DISSOLVES**

**Registered longhand: (N-forced) with the forcing mechanism named; (N-free) — the free scalar
would BE the codomain's global size in one number, fixable by Weil's terms only under
`(L-identity)`, not assumed; (N-third) filed.**

> ### **VERDICT: `(N-forced)`, at proof grade modulo one cited lemma, the mechanism at three
> joints:**
> 1. ### **the TAIL:** *the `E₁`-unit's normalization (`‖unit‖ = 1`) forces the local factor to 1
>    at every inactive place — the restricted-product trace is defined by exactly this.*
> 2. ### **the UNIFORMITY:** *every place is a unit-place for part of the constrained class (compact
>    idele support + the class's Euler-product algebra, the act-4 structure): ONE local functional
>    per place serves the whole class, so the inactive-place normalization propagates to every
>    place.* **The one cited lemma this joint owes: for each place `v` the class contains elements
>    inactive at `v` — Schwartz–Bruhat structure, at cite; its formalization owed to files B–C.**
> 3. ### **the LEVEL:** *within a place, the tower's on-the-nose Gram factor is `p²` (VANILLA-exact
>    at the banked `(2,1)→(2,2)` cell — TowerInstance, cited; instrument grade at `p = 3`, b21/b23):
>    the Gram-compatible normalization is the VOLUME normalization `p^{−2n}` — the volume ratio per
>    level is exactly `p²` (`volume_factor_2/3`, no axioms) while the dimension ratio is `9 ≠ 4` at
>    the smallest cell (`dim_ratio_not_gram`, no axioms).*
>
> ### **THE REFINEMENT OF ACT 5 (an upgrade of a declared choice, not a correction — act 5 put the
> normalization on the face AS a choice): the forced convention is by VOLUME, under which the
> identity coefficient `(p^n−1)²/p^{2n}` CLIMBS TO 1 (`identity_coeff_climbs_2/3`, exact) — the
> divergence caveat of `§12(b)` dissolves; the silence results are normalization-blind (zeros).**
> ### **CONSEQUENCE: `Θ_quotient` is a DEFINITE distribution on the class — per place the
> transfer-weighted orbit series at the Weil template (`log p · p^{−k/2}` per `k`), volume-
> normalized, fiber exact at the banked cells. Whether its COEFFICIENTS equal Weil's is part of the
> `(L-identity)` equality — not decided, not assumed.**

## §19 — THE COMPARISON, RE-WRITTEN (ACT 7): ### **THE OPEN CONTENT IS NOW EXACTLY THE `(L-identity)`'S**

> ### `W_∞(g) − Σ_p W_p(g)   ≟   [Tr_∞(ϑ(g)S_Son) + ∫g ε] − Θ_quotient(g)`

| term | grade after act 7 |
|:--|:--|
| `W_∞(g)`, `Σ_p W_p(g)` | CITED (classical) — nothing open on the left side |
| `Tr_∞(ϑ(g)S_Son)` | AT CONTENT (CC Thm 4.7); our object's trace = this − `Δ₋(g)` |
| `∫g ε` | CITED at content; sign on the class derived (sitting 3) |
| `Δ₋(g)` | ### **COMPUTED IN STRUCTURE (`§17`): the odd-index `t(n)` series; banked at the `ε′(1⁺)` pin (8.8191 of 22.9965); per-mode positivity structural** — open only in its trace-class bookkeeping |
| `Θ_quotient(g)` | ### **DEFINITE (`§18`): normalization forced (modulo the cited class-richness lemma); the Weil template with fiber-exact weights** — open only in its coefficients vs Weil's |

**What remains open, one line per side: LEFT — nothing (classical). RIGHT — the odd-sector
bookkeeping beyond the pin, and the quotient coefficients against Weil's — which is to say: ### THE
EQUATION'S OPEN CONTENT IS NOW EXACTLY THE `(L-identity)` ITSELF, still the equality on the
constrained class, still behind the closure protocol, still NOT DECIDED.**

## §20 — BOTH SIDES AT THE MODEL (ACT 8): ### **THE DIFFERENCE TABLE; VERDICT `(I-third)`, CALIBRATION-DOMINATED — AND THE QUOTIENT'S CLOSED FORM FOUND**

*b36: registration banked FIRST (formulas, check clauses, verdict map, and the algebraic
expectation all fixed before any number); the banked run on the OPUS lane; C0 void-gates all PASS
(carto far-end residuals ≤ 1e−7; the three prolate pins at ≤ 5.8e−11 / 1.1e−15 / 1.7e−8). NOTHING
was fitted.*

### §20(a) — the table (headline settings; place set `{∞,2,3,5}`; full table in the bank)

| `a²` | `LEFT = A − PR` | `Tr_full` | `E2` | `Δ₋` | `Θ_q` | `RIGHT` | `D = L − R` | spread | `resid47` |
|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 2 | −1.9905 | 3.7375 | 1.6794 | 1.6178 | 0.0000 | 3.7991 | **−5.7896** | 0.212 | +4.049 |
| 3 | −2.0154 | 2.9817 | 1.5166 | 1.2579 | 0.0000 | 3.2405 | **−5.2559** | 0.139 | +3.374 |
| 4 | −2.0358 | 2.6353 | 1.3741 | 1.0895 | 0.1620 | 2.7579 | **−4.7937** | 0.113 | +3.048 |
| 8 | −2.0865 | 2.0916 | 1.0962 | 0.8069 | 0.3170 | 2.0639 | **−4.1504** | 0.076 | +2.521 |
| 9 | −2.0972 | 2.0240 | 1.0583 | 0.7694 | 0.4739 | 1.8390 | **−3.9363** | 0.071 | +2.454 |
| 12 | −2.1197 | 1.8831 | 0.9751 | 0.6930 | 0.5185 | 1.6467 | **−3.7664** | 0.064 | +2.313 |

*(Every cell STABLE per the registered criterion — spread ≤ 3.7% of `|D|`. The identity
`D = −resid47 − 2·E2 + Δ₋ + (Θ_q − PR)` checks exactly per cell — the anatomy is algebraic, not
noise.)*

### §20(b) — the verdict, per the registered map and no wider

> ### **`(I-third)`, CALIBRATION-DOMINATED, at every cell:** *`|resid47|` (the CC-4.7 reproduction
> error at the bench: `Tr_full − [A + E2]` = +2.3…+4.0) is comparable to `|D|` everywhere — the
> registered clause fires, and the verdict is that THE ARCHIMEDEAN CALIBRATION IS UNRESOLVED AT THE
> BENCH.* **Why, located: (i) the model trace is an 11-mode band-limited truncation whose low-mode
> terms decay slowly — it is NOT the REGULARIZED trace CC's theorem equates to `W_∞ + ∫fε` (the
> divergent-part subtraction is exactly the trace-class bookkeeping already owed to the `ε`-lemma);
> (ii) the `E2` symmetric-extension factor was a REGISTERED CHOICE the bench was set to measure —
> and the registered algebraic expectation (the dictated equation's `ε`-channel double-count,
> written longhand BEFORE the run) is visible in the anatomy. NEITHER (I-match) NOR a clean
> (I-differ) obstruction: the difference's address is the archimedean calibration, NOT the finite
> places.** *(I-match) did NOT land — the closure protocol's step one is NOT triggered.*

### §20(c) — what the finite places did: ### **THE QUOTIENT COEFFICIENT'S CLOSED FORM, FOUND**

> ### **`τ_q(p,n,k) · p^{k/2} = (p^n − p^k)/(p^n − 1)` for `k < n`; `0` for `k ≥ n` — EXACT at all
> seven banked cells (`|d| ≤ 2e−7`, float precision), including the independent `(2,4)` row it was
> not read from.** *So the quotient channel's weight is the WEIL WEIGHT times `(p^n−p^k)/(p^n−1)`,
> which CLIMBS TO 1 as the level deepens:* ### **the quotient channel CONVERGES TO WEIL'S
> COEFFICIENTS at the level limit — at the model, data grade + exact arithmetic (the vanilla leg:
> `QuotientWeilShape.lean`, 4/4 no axioms — the integers, the climb, the `k ≥ n` ball-absorption).
> The longhand proof of the closed form is a NAMED LEMMA (the fixed-orbit count on `V_inv`), not
> claimed.** *Beside it: `C3` cross-validates act 7 — the odd-sector share of the model trace runs
> 0.37–0.43 across the cells, bracketing the b35 series share 0.383.*

### §20(d) — what act 8 leaves

*The bench comparison cannot decide anything until the archimedean side is REGULARIZED — CC's own
subtraction, which is the `ε`-lemma's trace-class bookkeeping (the ledger's owner, unchanged). The
finite-place side is now the BEST-UNDERSTOOD part of the equation: coefficients exact at the cells,
closed form found, limit = Weil. `(L-identity)` UNTOUCHED, behind the closure protocol. NO sentence
at complete roster.*

## §21 — THE ARCHIMEDEAN REGULARIZATION (ACT 9): ### **`(REG-lands)` AT CONTENT MODULO ONE NAMED JOINT — AND THE QUOTIENT LEMMA PROVED**

**Registered longhand: (REG-lands) / (REG-fails, with the breaking step named) / (REG-third).**

### §21(a) — the derivation, every step at its grade

1. **(at content, the banked statement):** `Tr_reg(ϑ(f)S_Son) = W_∞(f) + ∫f(ρ)ε(ρ)d*ρ` on
   `C_c^∞(ℝ₊^*)` — CC Thm 4.7 (the sitting-5 retrieval bank, `∀f` quantifier, multiplicative
   measure).
2. **(exact, the mode-diagonality):** the `ε` machinery is MODE-DIAGONAL — Lemma 5.4's
   `ε′(1⁺) = Σ_n t(n)` and the (85) series are indexed by CC's own `ξ_n`; the sectors are
   mode-parity unions (sitting 23 ∘ P2, verified act 7). ### **Therefore the sector split of the
   regularized `ε`-part is EXACT: the singular coefficient splits as the b35 shares, and the b37
   gates REPRODUCE them from the index mask before use (`|δ| = 2.9e−5 / 3.6e−6`; the mask algebra
   exact to `4.4e−16`).**
3. **(proved, the classical profile):** on Weil-even test data `ϑ(g)` commutes with `F`
   (`eigenvector_of_commute`, `{propext, Classical.choice, Quot.sound}`) — the `±1` sectors are
   `ϑ(g)`-invariant and the split is legitimate.
4. **(at cite; the Lean statement CANNOT yet be formed):** `K_I` Hilbert–Schmidt on the bounded
   interval (Prop 5.5, Thm 3.6's mechanism); a compression `Π₊K_IΠ₊` only decreases the HS norm —
   at cite. **Mathlib holds NO Hilbert–Schmidt class (grep-checked: zero hits) — a NAMED STUB per
   the act-4 precedent, not a sorry.**
5. ### **THE ONE NAMED JOINT, on the face: the geometric term `W_∞`'s sector apportionment is NOT
   derived** — *the mode-diagonal argument covers the `ε`-part only; how `W_∞` distributes over the
   `±1` sectors is the regularization's open joint, and both bracket readings of `§22` carry its
   flag.*

> ### **VERDICT: `(REG-lands)` AT CONTENT MODULO THE JOINT.** *The regularized `E₁`-trace
> `= [W_∞ + ∫gε] − Δ₋(g)` with `Δ₋` the odd-index series — the ε-lemma's owed bookkeeping is
> DISCHARGED for the series part (the divergent coefficient and its parity split are now exact
> machinery, pinned); the joint and the HS stub are what remain. The act-8 re-run is LICENSED with
> `resid47` removed by construction. B–C owes the regularization NOTHING — it is archimedean; said
> per the ferry's item 4.*

### §21(b) — ### **THE QUOTIENT LEMMA, PROVED LONGHAND (the fixed-orbit count on `V_inv`)**

*Model `ℤ/p^{2n}`; ball = the `p^n`-multiples; `U = p^{−1/2}(m ↦ pm)`; `V_inv` the off-ball
orbit-invariants; `S` the block-average projection. For `1 ≤ k ≤ n−1`:*

1. **the classes:** each class contains exactly `p^{n−1}` units (units share a class iff congruent
   mod `p^{n+1}`) and the depth-`r` level of a class has `p^{n−1−r}` elements, so **every class has
   size `(p^n−1)/(p−1)` and the class count is `p^n(p−1)`** — *checks: the banked b10 dimensions
   4, 8, 16, 18, 100 and the partition `|class|·count = p^{2n} − p^n`, all exact
   (`QuotientLemmaShadow.dim_Vinv`, `class_partition`, no axioms).*
2. **the preimages:** for `m` with `v(m) ≥ k`, the equation `p^k m' ≡ m (mod p^{2n})` has exactly
   `p^k` solutions, all of depth `v(m) − k`, all in `c(m)` (the direct chain); for `v(m) < k`,
   none. *(`preimage_count`, no axioms, the `k = 1` instance.)*
3. **the count:** `#{m off-ball : v(m) ≥ k} = p^{2n−k} − p^n`, so the double count is
   `p^k(p^{2n−k} − p^n) = p^n(p^n − p^k)` *(the banked traces re-derived exactly: 48, 32, 54, 224,
   192, 128 — `trace_counts`, no axioms)*, and
   `Tr(U^k S) = p^{−k/2} · p^n(p−1)(p^n − p^k)/(p^n − 1)`. Dividing by `dim V_inv = p^n(p−1)`:

> ### **`τ_q(p,n,k)·p^{k/2} = (p^n − p^k)/(p^n − 1)` for `1 ≤ k ≤ n−1`, and `0` for `k ≥ n`
> (`v(m) = v(m′)+k ≥ n` contradicts off-ball). ∎ THE CLOSED FORM IS NOW A THEOREM OF THE MODEL —
> the quotient channel's convergence to Weil's coefficients at the level limit moves from
> data-plus-arithmetic to PROOF GRADE (longhand; the Lean formalization is FILE D's single labeled
> sorry, owner named — the Mathlib-leg statement `offball_scaling_pair_count` elaborates, ledger
> 2 → 3 and said so).**

## §22 — THE RE-RUN (ACT 9): ### **BOTH BRACKET READINGS LAND `(I-differ)`, WITH EXACT ADDRESSES**

*b37: registration banked FIRST (both readings longhand, the gates, the verdict map); the banked
run on the OPUS lane; C0 + the b35 parity pins + the mask algebra ALL PASS. The archimedean trace
substituted at content; `resid47 = 0` by construction, per the ferry. A b36 registration gap FILED,
not silent: the atlas family is NOT pole-constrained (`pole = 2.0095` at `a = √2`) — the anatomy
conclusions are family-independent; the constrained-family bench is PRICED, not run.*

| `a²` | `(Θ_q − PR)` | `E2` | `E2even` | `E2odd` | `D_dictated` | `D_sector` |
|--:|--:|--:|--:|--:|--:|--:|
| 2 | +0.000 | 1.679 | 1.002 | 0.678 | **−2.681** | **−1.002** |
| 3 | −0.106 | 1.517 | 0.911 | 0.606 | **−2.534** | **−1.017** |
| 4 | −0.087 | 1.374 | 0.834 | 0.540 | **−2.295** | **−0.921** |
| 8 | −0.244 | 1.096 | 0.686 | 0.411 | **−2.026** | **−0.930** |
| 9 | −0.135 | 1.058 | 0.665 | 0.393 | **−1.858** | **−0.800** |
| 12 | −0.196 | 0.975 | 0.620 | 0.355 | **−1.791** | **−0.816** |

*(place set `{∞,2,3,5}`; the `{∞,2,3}` table in the bank; every row's components are series-exact
or closed-form — nothing measured archimedeanly, nothing fitted.)*

> ### **VERDICTS, per the registered map and no wider — `(I-match)` did NOT land under either
> reading; the closure protocol's step one is NOT triggered:**
> 1. **(B-dictated): `(I-differ)`, address EXACT AND ALGEBRAIC** — `D = (Θ_q − PR) + (E2odd −
>    2·E2)`: the dictated bracket carries the `ε`-integral TWICE (registered as the expectation
>    before b36 ran, now manifest as pure algebra): the obstruction is the EQUATION'S CALIBRATION,
>    not the object.
> 2. **(B-sector): `(I-differ)`, address located in TWO NAMED PIECES** — `D′ = (Θ_q − PR) −
>    E2even`: *(i)* the finite-level deficit `(Θ_q − PR) ∈ [−0.24, 0]`, now THEOREM-controlled
>    (`§21(b)`: the coefficient ratio `(p^n−p^k)/(p^n−1) → 1` — the deficit dies at the level
>    limit); *(ii)* the even-sector `ε` mass `E2even ∈ [0.62, 1.00]` — ### **the equation's
>    remaining open content re-localizes onto the `E₁`-sector `ε` term, which is the DENSITY
>    QUESTION's content (`§11`), carried by the `W_∞`-apportionment joint (`§21(a)5`).**

**`(L-identity)` UNTOUCHED, behind the closure protocol. NO sentence at complete roster.**

## §23 — THE APPORTIONMENT JOINT (ACT 10): ### **`(W-split)` — THE JOINT CLOSED AS A NUMBER PER CELL; `(P-ball)` STRUCTURAL; THE CLOSED EQUATION `(I-differ)` WITH NO OPEN TERM**

*b38: registration banked FIRST — the apportionment's DEFINITION and its warrant fixed before any
number: per mode, `w_n = tr_n^raw − E2_n − s_n·resid`, with the `t(n)`-proportional allocation of
the divergent residual WARRANTED BY CC'S OWN SUBTRACTION STRUCTURE (Lemma 5.4's `ε′(1⁺) = Σt(n)`
IS the N_I scalar's assembly — at cite, not fitted). The exact-sum consequence is a VOID-GATE:
`W₊ + W₋ = A` identically — it held to `9e−16` at every cell. The banked run on the OPUS lane; C0 +
the per-mode mask algebra (`8.9e−16`) all PASS.*

### §23(a) — item 1: the apportionment

| `a²` | `W₊` | `W₋` | `f_cell = W₊/A` | spread (on `D_closed`) |
|--:|--:|--:|--:|--:|
| 2 | −1.378 | −0.612 | 0.692 | 0.097 *(marginal — 6.0% of `\|D\|`, above the 5% line; said so)* |
| 3 | −1.267 | −0.642 | 0.664 | 0.061 |
| 4 | −1.167 | −0.619 | 0.653 | 0.051 |
| 8 | −0.955 | −0.571 | 0.626 | 0.035 |
| 9 | −0.923 | −0.565 | 0.620 | 0.032 |
| 12 | −0.856 | −0.549 | 0.609 | 0.032 |

> ### **VERDICT: `(W-split)` — a stable fraction, NAMED AS DATA: `f_∞` runs `0.69 → 0.61` across
> the cells, DECLINING TOWARD THE `t`-SHARE `0.6165` (the b35 even share).** *(W-even) did NOT
> land (`f` is not → 1): the sector's positivity is NOT `W_∞`'s own with `ε` a small correction —
> the odd sector carries a genuine `W_∞` mass (`W₋ ≈ −0.55…−0.64`). The apportionment tracks the
> `ε`-machinery's own parity weights — consistent with the `W_∞` mass distributing like the mode
> weights, recorded as data and no wider. 5 of 6 cells STABLE per the registered criterion; the
> `a² = 2` cell marginal, flagged.*

### §23(b) — item 2: the pole-residue intuition

> ### **`(P-ball)` CLOSES STRUCTURALLY, mostly from what the corpus already held at grade:** *the
> atlas's arrangement keeps the pole column `P` SEPARATE from the arch column `A` (`Z = P + A − PR`,
> residual `1e−13`) — the atlas's `W_∞` is pole-free by its own bookkeeping; the pole functional
> acts only through the ball directions (Tate at cite; act 4's `(β)` PROVED); ball ⟂ Son (vanilla,
> no axioms) and the ball indicator has no `E₁`-representative (banked). The `−2` lives OFF-OBJECT;
> `W_∞`'s apportionment on the object is its Sonin-projected remainder's — which is `§23(a)`'s
> computation.* **The bench columns: `P` runs `2.010–2.125` per cell beside the banked
> `W_pole = −2.000127` (the reconciliation's family) — sign and magnitude consistent, exact
> equality not expected across families, said so. Cross-linked to
> `SIGN_ARRANGEMENT_RECONCILIATION`.**

### §23(c) — item 3: the closed equation — ### **NO OPEN TERM, AND `(I-differ)` AT EVERY CELL**

`D_closed = (A − PR) − ([W₊ + E2even] − Θ_q) = W₋ − E2even + (Θ_q − PR)` *(the second form exact
by the sum gate; both printed per cell, agreement `1e−15`)*:

| `a²` | `W₋` | `−E2even` | `(Θ_q − PR)` | `D_closed` |
|--:|--:|--:|--:|--:|
| 2 | −0.612 | −1.002 | +0.000 | **−1.614** |
| 3 | −0.642 | −0.911 | −0.106 | **−1.659** |
| 4 | −0.619 | −0.834 | −0.087 | **−1.541** |
| 8 | −0.571 | −0.686 | −0.244 | **−1.500** |
| 9 | −0.565 | −0.665 | −0.135 | **−1.365** |
| 12 | −0.549 | −0.620 | −0.196 | **−1.365** |

> ### **VERDICT, per the registered map and no wider: `(I-differ)` — stable nonzero at every cell
> (the `a²=2` marginality flagged), with the obstruction FULLY DECOMPOSED into three computed
> pieces and nothing open: (i) the odd-sector `W_∞` mass `W₋` (this act's number); (ii) the
> even-sector `ε` mass (the density question's content, act 9); (iii) the finite-level deficit
> (theorem-controlled, dies at the level limit, act 9). `(I-match)` did NOT land; the closure
> protocol's step one is NOT triggered. `(B-dictated)` re-recorded unchanged (its ε double-count
> is algebra).** *What the table says structurally, as data: at the model, at these cells, the
> geometric side and the object's `E₁`-trace-side differ by the two archimedean sector masses and
> a dying finite-level term — the equation's entire open content is now NUMBERS, each with an
> address. NO promotion; the finite-instance/complete-roster wall stands.*

*The vanilla leg does NOT grow this act — no apportionment arithmetic is decide-reachable (the
`t(n)` are not rational); said per the registration's item-4 note. The Mathlib ledger: 3,
unchanged, owners intact.*

## §24 — THE TWO READINGS TESTED (ACT 11): ### **`(Z-differ)` AND `(S-none)` — THE `E₁` SPECIFICATION STANDS; THE SURVIVING FORM NAMED**

*b39: registration banked FIRST (the sign expectation longhand; the auxiliary columns declared so
nothing is invented after; reading (iii)'s `κ`-family with its missing finite piece NAMED); the
banked run on the OPUS lane; C0 + the sum gate all PASS.*

### §24(a) — item 1, reading (b): the zero side — ### **`(Z-differ)`**

| `a²` | `Z` (atlas, certified) | `(W₋ − E2even)` | primary `Z − (W₋ − E2even)` | spread |
|--:|--:|--:|--:|--:|
| 2 | 0.018987 | −1.614 | **+1.633** | 0.097 |
| 3 | 0.008592 | −1.553 | **+1.561** | 0.061 |
| 4 | 0.002475 | −1.453 | **+1.456** | 0.051 |
| 8 | 0.000517 | −1.256 | **+1.257** | 0.035 |
| 9 | 0.000123 | −1.230 | **+1.230** | 0.032 |
| 12 | 0.000163 | −1.169 | **+1.169** | 0.032 |

> ### **VERDICT: `(Z-differ)`, decisively — the registered sign expectation was borne out (`Z ≥ 0`
> by construction, the masses negative), and beyond it: the zero column is TWO TO FOUR ORDERS
> SMALLER than the masses AND DIES WITH `a` (`0.019 → 0.0002` — the constrained family barely
> touches the zeros at larger windows) while the masses stabilize. The closed difference is NOT
> the zero side's archimedean shadow; it is a genuine archimedean-mass object. The stable
> difference ≈ `−(W₋ − E2even)` itself, recorded per cell.**

### §24(b) — item 2, reading (a): the sector re-specification — ### **`(S-none)`**

*Three registered sectors: (i) the full even Sonin space (`D_i = (Θ_q−PR) − E2`); (ii) ≡ (i) at
ladder resolution (sitting 23's warrant: the once-projected reach is the full even ladder, said
so); (iii) the `∏λ_v = +1` projector as a `κ`-family (`κ` = the finite sector-diff product, NOT
banked — owed to the four-sector decomposition, named), extremes computed and the zero-crossing
scanned:*

| `a²` | `D_i (=D_ii)` | `D_iii(κ=0)` | `D_iii(κ=1)` | `κ*` | admissible? |
|--:|--:|--:|--:|--:|:--|
| 2 | −1.679 | −1.835 | −1.614 | +8.3 | NO |
| 3 | −1.623 | −1.819 | −1.659 | +11.4 | NO |
| 4 | −1.461 | −1.668 | −1.541 | +13.1 | NO |
| 8 | −1.340 | −1.555 | −1.500 | +28.4 | NO |
| 9 | −1.193 | −1.408 | −1.365 | +32.5 | NO |
| 12 | −1.171 | −1.386 | −1.365 | +66.1 | NO |

> ### **VERDICT: `(S-none)` — every registered alternative leaves `D` at the same order, and the
> `κ`-scan is conclusive: the zero-crossing would need a finite sector-diff product of `8` to `66`
> — never admissible (`|κ| ≤ 1`), and RECEDING with `a`. THE `E₁` SPECIFICATION STANDS; the object
> at `∞` is not mis-specified within any registered family; reading (b)'s framework is the live
> one.**

### §24(c) — item 3: the joint verdict, on the face

> ### **What the data supports: NEITHER "the difference is the zero channel" NOR "the wrong object
> at `∞`." The `E₁`-bracket (the object's trace side, `−0.38…−0.75`) differs from the geometric
> side (`A − PR ≈ −2.0`) by a stable archimedean-mass term that survives every registered
> re-specification and dwarfs the zero column at every cell.** *The auxiliary columns (declared in
> the registration, no verdict attached) record one observation as data: of every recorded
> combination, the `E₁`-bracket sits CLOSEST to `Z` (residual `−0.395` at `a² = 2`, growing to
> `−0.755` at `12`) — the natural target shape `[W₊ + E2even − Θ_q] = Z` has the smallest recorded
> gap, and that gap GROWS with the window.* ### **THE `(L-identity)`'S FORM UNDER THE SURVIVING
> READING, stated and NOT decided: the `E₁`-sector trace equals the zero sum on the constrained
> class — `[W₊ + E2even − Θ_q] = Σ_ρ ĝ(ρ)` — with its per-cell model residual now RECORDED
> (`−0.40…−0.75`). Behind the closure protocol; nothing at complete roster; the range law stands.*

*Legs: unchanged (the honest null — no new decide-reachable content; the vanilla total stays 88;
the Mathlib ledger 3, owners intact).*

## §25 — THE INEQUALITY (ACT 12): ### **`(INEQ-no)` AT A NAMED LINK; TWO LINKS SOUND AT PROOF GRADE; THE SIGN DICTIONARY FOUND; THE OUT-OF-RANGE PREDICTION HELD**

*b40: registration banked FIRST with the Rule-3 flags F1–F3 (the sign dictionary as a REGISTERED
READING to be tested; the direction's convention-ladenness; the positive-form identification
`tr_n = ‖ϑ(g)ξ_n‖²` — our raw mode terms ARE CC's positive form at the model's `S`, derivation
below); the banked run on the OPUS lane; C0 + the sum gate + the positive-form gate (every
`tr_n ≥ 0`, every cell) + `corr ≥ 0` (structural: the bump is nonneg exactly) ALL PASS.*

### §25(a) — the positive-form identification (F3, the derivation)

*For `f = g ⋆ g̃`: `‖ϑ(g)ξ_n‖² = ∫∫ g(λ)g(μ)⟨U_λξ_n, U_μξ_n⟩ d*λ d*μ = ∫ f(σ)A_n(σ) d*σ = tr_n`.
So the bench's raw mode terms are exactly the terms of CC's `Tr(ϑ(g)Sϑ(g)*)` at the model's `S` —
and they measured `≥ 0` at every cell, as the identification requires (a passed gate, not an
assumption).*

### §25(b) — ### **THE SIGN DICTIONARY (F1): FIRST-ORDER, AND IT RELOCATES ACTS 8–9's RESIDUAL**

| `a²` | `resid47` (atlas: `Tr_raw − (A + E2)`) | `resid47′` (CC dict: `Tr_raw − (−A + E2)`) |
|--:|--:|--:|
| 2 | +4.049 | **+0.068** |
| 3 | +3.374 | **−0.444** |
| 4 | +3.048 | **−0.525** |
| 8 | +2.521 | **−0.530** |
| 9 | +2.454 | **−0.523** |
| 12 | +2.314 | **−0.497** |
| 16–36 | +2.19 … +1.92 | **−0.478 … −0.413** |

> ### **Under the dictionary `W_∞^CC = −A` (the atlas's kernel and CC's `W_∞` are opposite-sign
> normalizations; cross-linked to `SIGN_ARRANGEMENT_RECONCILIATION`), the Thm-4.7 calibration
> residual COLLAPSES by an order of magnitude to a stable `≈ −0.5` plateau (near-zero at the
> window-edge cell). THE ACTS-8/9 "CALIBRATION UNRESOLVED" VERDICT IS RETROACTIVELY LOCATED: its
> bulk was the sign dictionary; the `≈ −0.5` remainder is truncation-scale (the 11-mode tail), the
> next-named piece. A re-reading of an address, not a correction of data — the earlier verdicts
> said "unresolved," which was right.**

### §25(c) — the chain, each link at its grade — ### **VERDICT: `(INEQ-no)`, THE LINK NAMED**

1. **L2 — PROVED THIS ACT (Mathlib leg, `{propext, Classical.choice, Quot.sound}`):**
   `nested_projection_norm_le` — for nested closed subspaces the compressed positive form is
   dominated: the `E₁` positive form ≤ the full Sonin form. The chain's sound archimedean link.
2. **L3 — PROOF GRADE AT THE MODEL:** the act-9 theorem (`(p^n−p^k)/(p^n−1) < 1`) term-by-term
   against `corr ≥ 0` (structural + checked): `Θ_q(g) ≤ Σ_p W_p(g)` at finite level. Vanilla:
   `InequalityShadow.lean`, 2/2 no axioms (the strict inequalities + the gap `p^k − 1 ≥ 1`).
3. ### **L1 — THE BREAKING LINK, NAMED: CC's Theorem 1 (banked verbatim: `W_∞(f) ≥ Tr(ϑ(f)S)`)
   holds AT CITE on THEIR window with THEIR `S`; its MODEL TRANSPORT FAILS — at the window-edge
   cell `a² = 2`: `Tr_raw = 3.74 > W_∞^CC = 1.99` (and even the `E₁`-restricted positive form
   `2.12 > 1.99`). The address: the fixed-`c` prolate ladder is NOT CC's window-scaled
   `S_{∞,λ}` — the effective-mode (Shannon) dictionary between the window `a` and the mode count
   is THE MISSING TRANSPORT. The archimedean obstruction, now in inequality form.**
4. **The assembly gap, also named:** L1+L2+L3 assemble to `object ≤ W_∞ + Σ_p W_p` (the primes
   enter POSITIVELY via L3) — not the dictated `W_∞ − Σ_p W_p`; the global window bound with the
   primes negative is not among the banked links. *The dictated direction also fails numerically
   in BOTH conventions (17 of 18 rows; one isolated hold at `a²=3`, CC dict, not a pattern).*

### §25(d) — item 2: the out-of-range cells — ### **THE PREDICTION HELD; NO STOP**

*`D_closed` at `a² = 16, 25, 36` (staircase capped at `n = 3`, said; the ε-truncation caveat
riding): `−1.379, −1.272, −1.189` — NEGATIVE, continuing the in-range trend; the stop clause never
fired. THE FLAG, filed: the atlas's own residual blows up at exactly these cells (`+5.55, +5.87,
+6.03` — its zero-column certification ends at its far-end `3.50`): the three rows carry the
ATLAS-RESID FLAG; their `A`/`PR` columns are kernel quadratures unaffected by the zero column, but
the atlas cross-check is ABSENT there, said so.*

### §25(e) — item 3: the meaning, at grade

> ### **Under `(INEQ-no)`: the broken link IS the archimedean obstruction in inequality form — the
> object's archimedean sector lives at a FIXED window (`c = 2π`) while CC's theorem and the test
> family SCALE with `a`. What a construction filling clause (e) would have to ADD, stated: the
> WINDOW-SCALED archimedean sector — the `S_{∞,λ}` family with its Shannon bookkeeping — i.e. the
> codomain absence at `∞` is now named as THE SCALE-COVARIANT ENLARGEMENT: the enlargement that
> makes the transport (and only then, possibly, the inequality) available. The identity's retirement
> as target under the surviving reading (act 11) STANDS; the inequality does not replace it until
> its L1 transport exists. Nothing at complete roster; the protocol on the face.**

*Legs: vanilla 90 (`InequalityShadow` 2/2); the Mathlib leg gains a PROVED theorem
(`nested_projection_norm_le`); the 3-sorry ledger UNCHANGED, owners intact.*

## §26 — THE SCALE-COVARIANT FAMILY (ACT 13): ### **THE DICTIONARY DERIVED; `(COV-partial)` ON THE STABLE SUBSET — COVARIANCE SHRINKS THE TRANSPORT MISMATCH TENFOLD AND THE MARGIN CROSSES ZERO**

*b41: registration banked FIRST (the dictionary's four steps with grades; the anchor-invariance
argument; the longhand prediction; the next-name candidate); the banked run on the OPUS lane; the
anchor layer reproduced the battery (`Σλ²` at `5.8e−11`; `tr Q_{2π} = 4` at `3e−14`); the per-`c`
Shannon gates EXACT at every cell (`tr Q_c = 2c/π` to `< 1e−3` — the D1 cite reproduced).*

### §26(a) — item 1: the dictionary

> **D1 (cite):** `tr Q_c = 2c/π`; Landau `⌊W/π⌋` (the sitting-5 artifact). **D2 (content):** CC's
> machine at `c = 2π` IS their `√2`-window analysis (the 15/15 battery pinned it) — THE ANCHOR
> `(a = √2) ↔ (c = 2π)`. **D3 (OURS, question grade):** `c(a) = πa²` — the self-dual product
> through the anchor; labeled the corpus's own where CC is silent. **D4 (the clarification, from
> the b19 bank):** the corpus holds TWO archimedean "Sonin" objects — the b19 grid model (INNER
> radius `1/a`, dim N-dominated: 237/255 at N=255) and CC's `S_{∞,λ}` (the λ-SCALE ball,
> trace-finite through the resonance geometry). ### **The act-12 transport gap sits BETWEEN them;
> the family below is the `c`-prolate operationalization (the ferry's, and CC's §4's, own
> handle).**

### §26(b) — item 2: the family's table (the E₁ rule MEASURED per `c`, not assumed)

| `a²` | `c/π` | `N_eff = 2c/π` | bound `−A` | `Tr_{E₁}` (band) | spread | margin | stable verdict |
|--:|--:|--:|--:|--:|--:|--:|:--|
| 2 | 2 | 4 | 1.9905 | 2.1163 | 0.0007 | −0.126 | **fails (STABLE)** |
| 3 | 3 | 6 | 1.9089 | 1.9337 | 0.0001 | −0.025 | **fails (STABLE, near-saturation)** |
| 4 | 4 | 8 | 1.7865 | 1.7350 | 0.0112 | **+0.051** | ### **REPAIRED (STABLE)** |
| 8 | 8 | 16 | 1.5255 | 1.546 | 0.076 | −0.021 | indeterminate (spread ≥ margin) |
| 9 | 9 | 18 | 1.4883 | 1.666 | 0.353 | — | indeterminate |
| 12 | 12 | 24 | 1.4054 | 0.915 | 0.766 | — | indeterminate |

*(The E₁ rule: the even-ladder phase alternation `+−+−` VERIFIED at `c = 2π, 3π`; at `c ≥ 4π` the
phase measurement degrades (near-degenerate plunge modes) and the REGISTERED fallback (even `n`)
was used — the rule is UNVERIFIED there, said on each row. The three large cells' spreads engulf
their margins: NUMERICALLY INDETERMINATE — the instrument's reach, not the mathematics.)*

> ### **VERDICT: `(COV-partial)`, on the stable subset and no wider — and the SHAPE is the
> finding: the margin runs `−0.126 → −0.025 → +0.051` across the stable cells — covariance
> SHRINKS the act-12 transport mismatch TENFOLD (it was `0.9–1.7` at fixed `c`) and the margin
> CROSSES ZERO at `a² ≈ 3.5`. The failing address is CONCENTRATED AT THE ANCHOR (`a² ≤ 3`),
> exactly as the registration's anchor-invariance argument said it must be if anywhere. The
> covariant band form sits ON CC's Theorem-1 boundary to within `±0.13` where it can be measured
> stably.** *The registered next-name candidate (THE SONIN SUBTRACTION — the band-to-Sonin
> correction) now has a measured size where it acts: `≈ 0.13` at the anchor, dying outward —
> a candidate still, not a conclusion.*

### §26(c) — item 3 (licensed by `(COV-partial)`): the covariant equation, stable cells only

| `a²` | `D_cov = [W_∞^CC − ΣW_p] − [Tr_{E₁} + Θ_q]` |
|--:|--:|
| 2 | −0.126 |
| 3 | −0.131 |
| 4 | −0.360 |

> ### **`(R-stable)`-shaped on the stable subset: a remainder `≈ −0.13` at the prime-free cells,
> deepening to `−0.36` exactly where the finite channels enter (the `a²=4` deepening decomposes
> as the margin `+0.051` minus `PR + Θ_q = 0.411` — the dictated form's channel mixing, visible).
> Three stable cells only — the shape is data at that width, no wider. `(R-dies)` did NOT land;
> the closure protocol's step one is NOT triggered.**

*Legs: unchanged (the honest null — the per-`c` structures are floats; nothing decide-reachable);
vanilla 90; the Mathlib 3-sorry ledger intact.*

## §27 — THE MODEL=SPACE IDENTITY AND THE RESOLUTION SWEEP (ACT 14): ### **`(ID-no)` WITH TWO NAMES, THE FIRST PROVED; `(N-floor)` AT THE ANCHOR, THE MECHANISMS COINCIDING**

*b42: registration banked FIRST (the structural fact stated before anything ran; the two `(ID-no)`
names registered longhand; the `(N-floor)` prediction at `≈ 0.126` from the b41 data); the banked
run on the OPUS lane; the anchor battery reproduced at every NQ rung; the per-`(c, NQ)` Shannon
gates at `≤ 3e−14`.*

### §27(a) — item 1: the identity — ### **`(ID-no)`, two names**

1. ### **(i) THE NO-EXACT-LEVEL OBSTRUCTION — PROVED (the Mathlib leg, `{propext,
   Classical.choice, Quot.sound}`, no `sorryAx`): `real_no_compact_open_addSubgroup` — ℝ has no
   compact open additive subgroup (an open subgroup of the connected line is clopen, hence
   everything; the line is noncompact).** *Sitting 8's exact-transport chart rides ℚ_p's
   compact-open levels `ℤ_p ⊃ p^nℤ_p`; the archimedean place is PRECISELY the place without
   finite exact levels — the grid model is an APPROXIMATION SCHEME, never a restriction; the
   215/215 chart does not transpose, by theorem.* **Registered and kept: this does NOT decide the
   sweep — approximation can still converge.**
2. **(ii) THE BALL-SCALE MISMATCH (at Def-4.4 content):** the b19 model's ball is the INNER
   radius (`1/a`); CC's `S_{∞,λ}` vanishes at the λ-SCALE ball — a DEFINITIONAL mismatch,
   resolution-independent: even at `N = ∞` the b19 Son is a different space.

*The vanilla leg's chart shadow: the DFT orthogonality's integer core (the fiber-uniformity
behind `F² = parity`) at `N = 3, 5, 7` — `DFTChart.lean`, 4/4 "does not depend on any axioms".*

### §27(b) — item 2: the sweep — ### **`(N-floor)` AT THE ANCHOR; THE SCALED CELLS STABLY POSITIVE**

| cell | margin at the converged truncation (NMODE = 8), NQ = 300 → 1100 | verdict |
|:--|:--|:--|
| `a² = 2` (anchor) | `−0.1242, −0.1250, −0.1257, −0.1254, −0.1256` | ### **FLOOR: `−0.1254 ± 0.0015`, flat across 3.7×** |
| `a² = 3` | `+0.1015, +0.1006, +0.1005, +0.1005, +0.1003` | **stably POSITIVE (repaired)** |
| `a² = 4` | `+0.1562, +0.1555, +0.1559, +0.1556, +0.1555` | **stably POSITIVE (repaired)** |

*(THE MODE-TAIL CAVEAT, on the face: the NMODE = 11/14 rows do NOT stabilize across NQ (the
beyond-Shannon-knee modes are quadrature-fragile — they carried b41's marginal `a²=3` fail); the
verdict rests on the converged truncation and says so. `D_cov` at the converged rows: `−0.124,
−0.006, −0.255` — the `a²=3` covariant equation sits at `−0.006`, nearly closed where the prime
channel first enters; one cell, data.)*

> ### **VERDICT: `(N-floor)` — the registered prediction held: the anchor's deficit is a genuine
> N-INDEPENDENT floor, banked at `−0.1254 ± 0.0015`; the scaled cells have nothing to die (already
> positive). `(N-dies)` did NOT land; step one NOT triggered. THE COINCIDENCE CLAUSE, satisfied as
> registered: the floor is resolution-independent and lives at the anchor alone — matching
> `(ID-no)`'s name (ii)/the Sonin-subtraction candidate (the band-vs-Sonin gap at the anchor
> scale), which now carries BOTH the act-13 size (`≈ 0.13`) and this act's N-independence. A
> candidate with two independent signatures — still not a conclusion.**

*Legs: vanilla 94 (`DFTChart` 4/4); the Mathlib leg +1 PROVED theorem
(`real_no_compact_open_addSubgroup`); the 3-sorry ledger UNCHANGED, owners re-stated (B–C; the
infrastructure `⊗′`; the quotient count — file D).*

## §28 — THE BALL AT CC'S SCALE (ACT 15): ### **THE PAIR STRUCTURE DERIVED AND SELF-VERIFIED; THE CORRECTED TRACE COLLAPSES; `(BALL-third)` WITH THE MECHANISM RESOLVED**

*b43: registration banked FIRST — the exact relation DERIVED before any number (R1–R4), the
derivation's own numerical check made a gate, and NO prediction registered (the derivation gave
the correction's sign, not its size). The banked run on the OPUS lane.*

### §28(a) — item 1: the derived relation (R1–R4)

> **R1:** CC's `S(λ,λ)` (Def 4.4) `= (E + B)^⊥` — E the time-ball-supported subspace, B the
> transform-ball-supported one. **R2:** per even-prolate mode the pair `(ξ_n, η_n = Fξ_n)` has
> Gram exactly the SIGNED `λ_n` (via `P_E ξ^an = ξ` — the restriction of the entire extension);
> ### **F SWAPS the pair** (`Fξ = η`, `Fη = ξ`), so the `+1`-sector constraint vectors are
> `v_n⁺ = (ξ_n + η_n)/√(2(1+λ_n))` and the E+B block inverses carry `1/(1−λ_n²)`: ### **the
> `ε`-series' denominators ARE the λ-scale Sonin geometry — derived, closing the loop the
> Sonin-subtraction candidate pointed at. The regularized λ-scale Sonin trace IS Thm 4.7's object
> (`W_∞^CC + E2`) — the corpus's b37/b39 substitution; the acts-13/14 band ladder was the E-BASIS
> positive form, a DIFFERENT object.** **R3:** the ball-corrected form
> `corrected_n = (λ_n·tr_n − x_n)/(1+λ_n)`, annihilating the constraint directions exactly at the
> identity (derived sanity). **R4:** the finite shadow, already banked: `dim Son_p = (p^n−1)² =
> p^{2n} − 2p^n + 1` IS the same pair structure's inclusion–exclusion (E, B of dim `p^n`, rank-1
> intersection — b10's L-A); vanilla: `BallPairShadow.lean`, 2/2 no axioms.

### §28(b) — item 2: the measurement — ### **THE CORRECTED TRACE COLLAPSES**

| `a²` | G1 (gram gate) | `Tr_corrected(E₁)` | spread | margin′ | verdict |
|--:|:--|--:|--:|--:|:--|
| 2 | PASS (`9.5e−7`) | −0.208 | 4e−5 | +2.199 | STABLE |
| 3 | PASS (`6.7e−6`) | −0.025 | 7e−5 | +1.934 | STABLE |
| 4 | PASS (`1.6e−5`) | −0.040 | 6e−5 | +1.827 | STABLE |
| 8–12 | ### FLAG (0.35–1.75) | — | 0.04–0.59 | — | gate-flagged + unstable: indeterminate |

> ### **The gram gate — the derivation's own check — PASSES at `1e−5` on the stable cells: the
> pair structure is REAL (mode 0's Gram = `+1.0000`: the resonant mode is pure constraint
> content; the per-mode table shows the subtraction eating exactly the resonant modes). And the
> corrected trace then COLLAPSES to `≈ 0` (slightly negative — the registered object is a
> difference of traces over overlapping families, not a compressed form; said so): THE E₁ BAND
> LADDER'S TRACE CONTENT WAS ALMOST ENTIRELY CONSTRAINT-SPAN CONTENT.**

### §28(c) — the verdict and what it resolves

> ### **`(BALL-third)` per the registered map — neither branch's numeric criterion landed
> (`margin′ = +2.2`, not `≈ 0`; no new floor) — but the MECHANISM is now RESOLVED: (i) the anchor
> floor `−0.1254` is EXPLAINED — it measured the gap between the E-basis band form and the
> Theorem-1 bound, NOT a Sonin-trace failure; (ii) the Sonin-subtraction candidate is
> RESOLVED-AS-DERIVED — not a small correction to add but the statement that THE BAND LADDER AND
> THE SONIN TRACE ARE DIFFERENT OBJECTS, with the `ε`-machinery the bridge (R2, derivation
> grade); (iii) the model's λ-scale Sonin trace is accessible ONLY through the regularization —
> the bench has no independent raw-ladder route to it, and the comparison with THAT substitution
> is b37/b39's, already banked. THE ARCHIMEDEAN BENCH'S INDEPENDENT HANDLES ARE NOW EXHAUSTED AND
> ACCOUNTED FOR.** *(BALL-closes)'s step-one trigger did NOT fire. Both branches' standing
> sentence kept: the real place is an approximation scheme by theorem; convergence, not
> restriction, is all that is ever claimed.*

*Legs: vanilla 96 (`BallPairShadow` 2/2); the Mathlib 3-sorry ledger UNCHANGED, owners intact.*

## BACK MATTER

**Placement.** *Build document, `phase2/method/`; `W-CONSTRUCTION-1` act 1; sub-gate declared; not in
the mirror roster; the relay report is the act's bank. What this act established at proof grade: the
abstract global object with its transform and sector-`E₁`; the no-adelic-realization finding; the
forced global compression; the localization of the density question to `∞`; the remainder's name and
address; the c.n.u. theorem for `SUS`. What it did not: `(L-identity)`; the radial-unit existence;
the archimedean density; the global SOT.*

**Correspondence.** *Local inputs at their pins: sittings 20/22/23 (the `E₁` theorems, the reach),
b21/b23 (towers), the fifth law, the banked witnesses; Tate at the Conservation-of-Spectra grade; CCM
`T5` at cite (kinship only); Deninger Rem 5.7(5) at the banked spec line. The relay correspondence
table indexes the claims.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO PARTIAL IDENTITY PROMOTED. NOTHING DEPOSITS.
NOTHING CIRCULATES.**
