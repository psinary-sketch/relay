# THE FOLD-FORWARD — b234, 2026-08-28

**Scope:** filings and two small tool amendments. No grade moves. Obstacles quoted from owning acts,
never paraphrased. ### **The provisional ruling is carried and NOT executed** — no File E text, no
adoption, no sign moves. Nothing about h2 beyond the register sentence exact. Nothing deposits.

---

## The provisional ruling: recorded, not executed

> *"The OPERATOR governs, provisionally … The amendment, and any consequent re-signing of the b232
> adoption to Day-1's orientation, **EXECUTE ONLY at the Phase-1.1 reconciliation (b235)** … Until
> then no text is edited and the statement stands ONE-RULING-FROM-COMPLETE with this lean
> recorded."*

### **File E is not edited. b232's adoption is not re-signed. No sign moved.** A lean recorded is
not a ruling executed, and the registration said so before the fold began.

---

## 1. The fold

`FINDINGS.md` **§0-bis**, five anchored entries: `arc-live-items` ·
`residue-six-station-migration` · `arc-species-catalogue` · `two-three-connective` ·
`import-ledger-surfaced`. ### **25 acts, b209 → b233, contiguous, no number missing** — counted
from the banks, not from memory. The document was current to **b225** before the fold.

**The document's own rules were read first and obeyed** — *"Each entry has a **stable anchor** … the
tag updates in place. The anchor does not change."* ### **That gives "no grade moves" a mechanical
test: a grade moves only by an in-place tag edit, and an in-place edit deletes a line. The fold
deleted no line** — gated on `git diff --numstat`.

**The obstacles are quotations.** The aggregation (b220): *"**THE RESULT ROUTE IS BLOCKED IN
PRINCIPLE**"*. The operator onto `S̄_v` (b227): *"**A STATEMENT CARRYING THE QUOTIENT CHANNEL'S
OPERATOR ONTO S-bar_v, OR CARRYING u_v INTO V_inv** … **THE FIRST IS NOT IN THE RECORD**"*, and
*"IT WANTS A RESULT OR A RULING; IT DOES NOT WANT A READ"*. The (3,2) Segre cell (b224): *"THE
OBSTRUCTION, NAMED AND **MEASURED RATHER THAN GUESSED**: **DEGREE (COST)**"*. Plus `Definition
3.3.1` (b222/b197), the `Lemma 5.4` citation (b171: *"**'CC' NAMES AUTHORS, NOT A WORK**"*), the
σ_even lemma (b219, **(OTHER)**), the arrangement (b233), the factor question, IMP-1's bench.
### **Not one is softened by restatement — the one thing a reference layer must not do.**

> ### **The tally was verified at content, not taken on trust, and it comes out at FIVE — the
> ferry's number holds.** (1) the level-limit at two places (b223) · (2) term 3's object (b226) ·
> (3) act 7 §4's sentence (b227) · (4) the ledger's cell-level statement, found (b228) and adopted
> (b229), counted as **one** because the tally counts statements not acts · (5) `wInf` (b232). Plus
> the arrangement **at one line** and the aggregation **open-with-coordinates**.
>
> ### **And the row's point: the arc proved nothing.** All five are *statements* — a definition
> adopted, a sentence supplied, a choice stated. ### **The identity is exactly as unproved at b233
> as it was at b209, and the register sentence is untouched.**

The **species catalogue** gains the week's five instances (the range law's fourth; the double-name's
three new sightings, *named in advance each time and used none of them*; a split read as a loss; an
absence needs a positive control; a criterion is not rewritten after the numbers), and notes two
older members that earned new instances — **prints-are-the-verdict** and **the harness's two
guards**.

---

## 2. The amendments — both caught themselves on the way in

**Amendment 1 — the harness normalizes needle whitespace.** The species ran three consecutive acts,
always the witness guard, always the same shape — **a quotation that is present and a substring that
is not**: b227 (`REFUSED BY`/`NAME`), b232 (`the named`/`crime`), b233 (`fails numerically`/`in BOTH
conventions`). ### **Each time the check was right about the corpus and wrong about the bytes.**

| retrofit | old | amended |
|:--|:--|:--|
| b227 `REFUSED BY NAME` | False | **True** |
| b232 `the named crime` | False | **True** |
| b233 `fails numerically in BOTH conventions` | False | **True** |
| *b232 `the named felony`* (control) | False | **False** |
| *b233 `succeeds numerically…`* (control) | False | **False** |

### **An amendment that made everything match would end the species by ending the check.** It
normalizes layout, not content; b232's case-folding repair is folded in so it cannot be lost again.

**Amendment 2 — the `QUOTED` verdict class.** Three clauses, three obligations: scanned and counted
(the hit still prints), nothing exempted silently (the sidecar carries QUOTED beside LIVE), the scan
stays total (`hits` unchanged). ### **And the design that keeps it from being a loophole: `QUOTED`
means *provably* quoted — the span is extracted and then looked up in the corpus; not found at
source, it stays LIVE.** *A class that trusted the quote marks would be a loophole any act could
open by typing a double quote — exactly the exemption b233 refused to self-grant.*

> ### **Its first draft failed its own retrofit.** It required a closing mark on the same line, and
> b233's quotation opens on one line and closes four later — so both hits stayed LIVE. ### **The
> amendment had reproduced the very species it was written to end, one level up.** Caught by
> *running* the retrofit rather than assuming it. Repaired; an opening mark is still required, so an
> act cannot launder its own voice through its own filing.

**Fixture** (banked at `data/b234_fixture_live_voice.md`): an authored stem **and** a quotation
decorated but absent from the corpus — **both stay LIVE: 2 hits / 2 live / 0 quoted, NOT CLEAN.**
**Retrofit** on b233's two lines: **2 hits / 0 live / 2 quoted, CLEAN.** ### **Both faces reported —
old NOT CLEAN, new CLEAN, hits unchanged at 2 — because a verdict that changed under a tool
amendment must show both or it is a rewrite.**

**`W-ORD-IMP1-BUDGET` sharpened, not run:** discretization dominated; half-order edge loss suspected
(consistent with b233's measured ratios of ~2–3 where a clean interior `O((Δv)²)` gives ~4);
edge-graded mesh the first test. ### **Filed as a hypothesis to be tested, not confirmed.**

---

## 3. The desk, and the filings

Six items in one list (`OPEN_TRAILS.md`): File E's arrangement (**provisionally ruled, execution
deferred to b235 by the ruling itself**); the Day-1 `W_∞` citation-hazard repair (**deferred to b235
by design**); b209's rows 46/47; the fold rulings; posture (unchanged, b213 stands);
`SIGN_ARRANGEMENT`'s proposed repair (untouched). Two work-orders sit beside it, filed and not run.

**A collision filed rather than stepped around:** the query `the fold` **hits `the-two`**, because
b231 declared it as that key's alias. ### **b164's own law applies — retrieval by string is not
retrieval by object.** The new key is `fold-forward` with non-colliding aliases; the alias was not
stolen, the collision is recorded.

---

## 4. Gates and scans

**b234's gates: 12 of 12 PASS, CLEAN — on the third run.** Two intermediate refusals, both real: a
fixture path that did not resolve for Windows Python (the fixture is now banked, not in `/tmp`), and
— sharper — ### **this act's own repair to `deletions()` disarmed the fixture that guarded it**, and
b217's first guard said so in the same run. The repaired fixture tests the negation on the same
measurement. ### **A tool amended and not then used is an amendment on paper: these gates are
written against the amended matcher.**

**Term scans, both faces reported.** Relay, unexcluded: **3 hits — 2 LIVE, 1 QUOTED**. ### **The two
LIVE are the fixture's own lines, live by design — that is the fixture working**, and the one QUOTED
is this act's registration quoting the corpus, the amendment's first live use. With the fixture
**declared-excluded** (`--exclude`, which the tool prints as *"stated, never silent"*): **1 hit, 0
live, 1 quoted — CLEAN.** PLACE-papers: **0 hits, CLEAN**, 246 lines. **SGS untouched.**

---

### **What this act did not do:** ### **no grade moved, no tag edited, no File E text, no adoption
re-signed, no sign moved, no bench run.** No obstacle paraphrased. The provisional ruling is
recorded, not executed. ### **Nothing about h2 beyond the register sentence exact. Nothing deposits.
Locks last.**

**Next: b235, THE PHASE-1.1 PASS** — Day 1 whole; `SIGN_ARRANGEMENT` and its proposed repair; the
two-conventions hazard; File E's provisional ruling **to execute or strike**; the {2,3} readings
including FINDINGS' Berry–Keating indicial `−1/2`; `FACES_OF_H2_AT_FINITE_INSTANCE`.
