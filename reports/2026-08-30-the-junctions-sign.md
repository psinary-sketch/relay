# THE JUNCTION'S SIGN — b260, 2026-08-30

**Scope:** a derivation at content with bench controls. **CONCURRENCY: SOLO (research seat).**
A finite cell decides NOTHING global (b14/b15); b242 governs — *"a measured rate is not a tail
bound."* The register sentence is unchanged and **nothing deposits.** The patent lane is
independent and was not touched.

**Order of record:** the owners read at content → the index queried → **the registration banked
before any step of the route was written** → the run → the structure → the shadow → the bank → the
filings → HANDOFF.

---

## The verdict

### **J1 DERIVES: `Θ_q(a) ≤ PR(a)` at every diagonal `a²` cell** — by termwise domination over an index set proved **identical**, and **strictly** at every term carrying non-zero weight.

**One premise is named and not derived** (below). **And the ferry's registered expectation about
`2 log p` is refuted — by a read, not by a measurement.**

---

## First, and before the result: the registration was not sealed, and says so

The ferry's execution line orders *"owners read at content first; registration before any step is
written"* — **which puts the reads before the registration**, and the reads *are* S1 and S2. So the
registration declares the split on its face and labels every line **ESTABLISHED-BY-READ** or
**OPEN-TO-THE-RUN**.

**S1, S2 and S3's algebra were fixed before the run existed. The run is not credited with them.**
What the run genuinely decided is one thing: **F1, the identification** — registered as OPEN and as
*"the outcome I most want to catch."* It did not fire.

---

## S1 — the index sets: **identical, 16 of 16, no boundary terms**

The ferry's registered suspect was *"the k-range `1 ≤ k ≤ n−1` vs `k ≤ n_p(a)`."`
**The suspect is real and correctly located — but it is not an index-set difference. It is a
vanishing inside a common index set.**

- `Θ_q` loops `k ∈ range(1, 2n)` **guarded** by `k·log p ≤ 2L`; `2L = log(a²)`; the guard reads `p^k ≤ a²`.
- `PR` loops `while p**k ≤ a²` **guarded** by the same condition.
- By b17, `p^k ≤ a²` *is* `k ≤ n_p(a)`. And `2n−1 ≥ n` for every `n ≥ 1`, so **the `range` cap never binds and the guard decides.**

Both sets are `{(p,k) : 1 ≤ k ≤ n_p(a)}`. Built independently by each instrument's own guard and
compared: **16 of 16 cells identical. Falsifier F3 did not fire.** **119 index pairs**, which the
shadow decides as arithmetic.

act 9's `1 ≤ k ≤ n−1` is a property of **its closed form**, not of either index set. At `k = n` the
closed form gives `0`. **A term that is present-and-zero is not a term that is absent** — and the
whole structural finding turns on that. **43 of the 119 sit at `k = n`; they are named and carried.**

*One ulp-level hazard, checked rather than waved at:* the two instruments compute the correlation's
argument by different routes (`k·log p` vs `log(p^k)`). **Measured across all 119 terms: worst
difference `0.000e+00`.** They agree bit for bit.

---

## S2 — the per-term forms, and the act's first finding

| | `Θ_q`'s summand | `PR`'s summand |
|:--|:--|:--|
| | `2 log p · tq(p,n,k) · corr(k log p)` | `2 log p · p^{−k/2} · corr(log p^k)` |
| `tq` | `\|tr(Uᵏ S)\| / d` | — |
| `U` | `scaling_matrix(p,n)` = `m ↦ p·m mod p^{2n}` over `√p` | — |
| `S` | `K Kᵀ`, the orthogonal projector onto `V_inv` | — |
| `d` | `dim V_inv` — act 9's *"fixed-orbit count **on V_inv**"* | — |

### **`2 log p` is on BOTH sides. It cancels. So does `corr`. So does `p^{−k/2}`.**

The ferry expected *"2 log p ≥ 2 log 2 > 1 carries the finite places while corr's size decides
tightness."* **Refuted — and refuted by reading the two instruments, before the run.**

**b228 is *not* contradicted, and that matters more than the refutation.** b228 paired act 9's
**bare `tau_q`** against PR's **weight** and found the difference to be exactly `2 log p`. That is
true and stands at its own QUESTION grade. This act pairs the two **assembled summands**, and the
instrument supplies the `2 log p` on the way. **Two pairings of two objects; conflating them would
be b219's double-name species committed on this corpus's own diagnosis.** b10's factor question is
promoted in neither direction.

---

## S3 — the per-term inequality: **derives, strictly**

The reduction (`2 log p > 0` divides out) is **a tautology and is reported as one** — it held
**20000/20000** on arbitrary tuples and carries no content. The content is two sub-claims:

**(S3-i) `tq ≤ p^{−k/2}`.** From act 9 §2: for `k ≥ 1, p ≥ 2` we have `p^k ≥ p > 1`, so for
`k ≤ n−1`, `0 < p^n − p^k < p^n − 1`, ratio in `(0,1)`; at `k = n` the ratio is `0`. **Strict at
every `(p,k)`.** The hypothesis `1 ≤ k` is load-bearing: **at `k = 0` it becomes an equality**, and
the shadow decides exactly that as its polarity control.

> **The cheap route was registered as insufficient before it was tried, and it is.** `‖U‖ = 1` gives
> only `tq ≤ 1` — **too weak by exactly the factor `p^{k/2}` that is the whole content.** act 9's
> closed form is load-bearing and cannot be replaced by a norm bound. (`m ↦ p·m mod p^{2n}` is
> p-to-1, not a permutation.)

**(S3-ii) `corr ≥ 0`.** From the bump's own formula: `exp(−1/(1−t²)) ≥ 0`, normalized by a positive
integral; convolution of non-negatives is non-negative; `dv[0] > 0`; `interp` preserves it.
**Measured: 0 negative bump samples, 0 negative corr samples, across all sixteen cells. F2 did not
fire.** *An autocorrelation is positive-definite but not positive-valued in general — `corr` is
non-negative here because **this** bump is, and for no other reason.*

**Result: 119 terms, 0 violations, 110 strictly less.** The 9 non-strict terms are all `0 ≤ 0`,
every one with `corr` **exactly zero** and sitting at **`p^k = a²`** — the cutoff's own endpoint.
**The inequality is strict at every term that carries any weight at all.**

---

## The premise that is **not** derived

### The identification: the instrument's `\|tr(Uᵏ S)\|/d` **is** act 9's `tau_q(p,n,k)`.

**No owner states it. b220, b228, b229 and b255 all treat the two as one object — and not one of
them checked it.**

| | |
|:--|:--|
| terms compared | **119** |
| of those at `k ≥ n` (closed form `= 0`) | **43** |
| **worst `\|tq − closed_form\|`** | **4.441e-16** at `(a²,p,n,k) = (16,2,4,1)` |
| bar, fixed in the registration before any value existed | `1e-9` |

**F1 did not fire.** It is still a premise and not a derivation, and **J1's grade is written with it
attached.** `W-ORD-TQ-IDENTIFY` filed.

---

## S4 — the sum, and the sixteen-cell **control**

Axes per `W-ORD-TE-SPEC`, printed before any number: `NV=4001`, `NU=12001`, `UMAX=600.0`,
`NU_HALF=401`, `S4=(2,3,5)` fixed. **`EPS_NRHO` and the `rho`-grid are NOT exercised** — this act
computes no `E2` — and are named as unused rather than silently inherited.

| `a²` | `Θ_q` | `PR` | `PR − Θ_q` | b255 junc | Δ |
|--:|--:|--:|--:|--:|--:|
| 2 | 0.000000000 | 0.000000000 | 0.000000000 | 0.000000000 | 0.000e+00 |
| 3 | 0.000000000 | 0.106483707 | 0.106483707 | 0.106483707 | 0.000e+00 |
| 4 | 0.161978165 | 0.249319596 | 0.087341431 | 0.087341431 | 0.000e+00 |
| 8 | 0.317018237 | 0.561045429 | 0.244027193 | 0.244027193 | 0.000e+00 |
| 9 | 0.473861607 | 0.608882321 | 0.135020714 | 0.135020714 | 0.000e+00 |
| 12 | 0.518491210 | 0.714334214 | 0.195843004 | 0.195843004 | 0.000e+00 |
| 16 | 0.588908905 | 0.799150018 | 0.210241113 | 0.210241113 | 0.000e+00 |
| 20 | 0.601007335 | 0.852776552 | 0.251769218 | 0.251769218 | 0.000e+00 |
| 25 | 0.734530668 | 0.896289251 | 0.161758583 | 0.161758583 | 0.000e+00 |
| 32 | 0.847885967 | 0.933309628 | 0.085423661 | 0.085423661 | 0.000e+00 |
| 36 | 0.857844691 | 0.947279444 | 0.089434753 | 0.089434753 | 0.000e+00 |
| 45 | 0.871548588 | 0.968112518 | 0.096563930 | 0.096563930 | 0.000e+00 |
| 50 | 0.875924373 | 0.975801707 | 0.099877333 | 0.099877333 | 0.000e+00 |
| 64 | 0.898564770 | 0.989783876 | 0.091219105 | 0.091219105 | 0.000e+00 |
| 81 | 0.927942449 | 0.999124793 | 0.071182344 | 0.071182344 | 0.000e+00 |
| 100 | 0.928192390 | 1.004850685 | 0.076658295 | 0.076658295 | 0.000e+00 |

**16 of 16. b255 is a CONTROL here and not a premise — no step of the route cited it, and a gate
checks that in source order.** The one non-strict cell is `a² = 2`, where both sides are zero and
the single index pair sits at `p^k = a²` with `corr = 0`. **The theorem predicts exactly that.**

### The reach, said against my own result

**The derivation does not depend on the ladder, on `S4 = (2,3,5)`, or on sixteen cells** — it runs
at every diagonal `a²` cell for any set of primes, the sum staying finite because `n_p(a) = 0` past
`a²`. **And b15 still governs, which is the other half and the more important one: a uniform
*cellwise* inequality is not a limit fact.**

---

## The per-term structure of the inequality, exhibited

`w − τ = w · (1 − (pⁿ−pᵏ)/(pⁿ−1)) = w · (pᵏ−1)/(pⁿ−1)`

- **`k = n`: the fraction is `1`.** act 9's value vanishes; **the whole of PR's term survives.** 43 terms.
- **`k ≤ n−1`: the fraction is `< 1`.** A deficit separation. 76 terms.

**One formula at its own endpoint, not two phenomena.** And the factor that does the work is
`(pᵏ−1)/(pⁿ−1)` — **not `2 log p`, which cancels.**

| `a²` | 2 | 3 | 4 | 8 | 9 | 12 | 16 | 20 | 25 | 32 | 36 | 45 | 50 | 64 | 81 | 100 |
|--:|--|--|--|--|--|--|--|--|--|--|--|--|--|--|--|--|
| `k=n` share of the separation | — | 100% | 7.3% | 74.8% | 13.8% | 29.5% | 48.4% | 54.0% | 9.0% | 0.0% | 0.0% | 0.2% | 0.5% | 2.7% | 5.9% | 9.2% |

**That swing *is* the sawtooth.** When `n_p(a)` steps, the level `k = n` moves out of the vanishing
branch into the `k ≤ n−1` branch — **the level switches on.** That is act 9's `0 for k ≥ n` read at
the staircase's step. **The derivation predicts b255's bench observation; the observation is not a
premise of it.** *The **rises** between steps are **not** derived and stay bench.*

---

## S5 — the meaning

**Under RULE Q (`Q.value := −Θ_q`), the junction piece `(PR − Θ_q)` is `≥ 0` by theorem.** b255's
*"approaches from below"* is no longer a ladder observation, and b255's refusal to extrapolate was
correct then and is not retroactively wrong: it refused to extrapolate from numbers, and this act
did not extrapolate — it derived.

**But `resid(A) = −(E2even + junction)` has two terms and only one is signed.** `E2even`'s
positivity is still bench-only. **The (SIGN-EVENT) question is not closed by J1 alone.**

### `2 log p` gets its first derived **limit**, not its first derived role

**It is common to both assembled summands, it cancels, and it is therefore incapable of carrying,
widening or tightening the junction at this pairing.** What carries it is `(pᵏ−1)/(pⁿ−1)` — a ratio
of **orbit counts**, not a period weight — and the dominant term is the one where that ratio is `1`:
**the top level, where act 9's trace vanishes because the model at level `n` has no orbit of length
`n` left to count.**

> **Filed at grade and not promoted.** b229's finding stands verbatim: *"the adopted definition
> contains a constant no owner has ever justified."* This act does not justify it. **It shows the
> constant is not where the junction lives, which is a different and smaller thing.**

---

## The shadow — **13 terminals, zero axioms, profile printed and read**

`SIDE-global-section/Core/JunctionSignShadow.lean`. Vanilla Lean 4, `decide` only.

It carries the staircase (**both halves**, so `n_p` is a maximum), the slack range cap, the ratio
inequality **in exact integer arithmetic**, and the counts. Four polarity controls, each excluding
something real — the load-bearing one being **`ratio_is_not_below_one_at_zero`: at `k = 0` the
strict inequality fails, so act 9's `1 ≤ k` excludes something.**

**And the tool itself was polarity-controlled:** two deliberately false statements of the same shape
were submitted to the same `decide`. **Both refused, lean exit 1**, errors quoted verbatim in the
profile.

### **It does not compile J1.** The real-valued inequality stays at content; `Real.rpow` gymnastics were refused in the registration, before the file was written.

---

## Three defects in this act's own harness — all three mine, all three disclosed

First run: **10 PASS / 1 FAIL / 2 REFUSED, NOT CLEAN** (`audit_b260_check_harness.txt`,
`4ec66c73…`).

1. **The FAIL** — a gate asked the *registration* for `F1 bar`, **a phrase only the RUN prints.** I
   meant *"the registration fixes the bar"*; I wrote *"the registration contains the run's label for
   it."* **b229's species, one act after b229 wrote the lesson down.**
2. **A REFUSAL** — the `ast` comment-stripper applied to a **plain-text bank**; `ast.parse` raised,
   and the harness refused rather than letting an errored fixture license the check. **A scope
   control applied to the wrong kind of file is not a scope control.**
3. **The second REFUSAL, and the one worth the most** — the gate defending this act's central claim
   (*b255 is a control, not a premise*) anchored on the bare token `B255`, which first appears at the
   **module path constant on line 24**, not the load on line 240. **The matcher found the *name*
   rather than the *use* — b164's law committed in a gate instead of an index — and it was weak in
   exactly the direction that would have flattered the act.** Re-anchored on the load: **240 > 92.
   The claim survives its own repaired gate.**

**Re-run after repairs: 13/13 CLEAN.**

> **And a recursion this act walked into and is naming.** Several gates read the bank at content, so
> a bank that names its own shipping sidecar's hash **cannot reach a fixed point.** The termination
> adopted: **the bank names only the *failing* run and its hash; the shipping sidecar is embedded
> here, in the report, which no gate reads.** All runs are on disk and all are carried, the failing
> one first; superseded ones will be reported UNUSED by `audit_verify`, which is correct.

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b260
  run at    : 2026-08-30T21:20:07 (local)
  input     : 13 checks routed through the harness
  checks    : 13
  pass      : 13
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 d72ee1f94accb338c85681d7967f4457
=== END AUDIT SIDECAR ===
```

---

## A fourth disclosure — and it is not a harness defect

The banned-term review returned **NOT CLEAN: 34 live uses of Rule 3's two stems in this act's own
voice**, almost all of them one word used as the central noun of the structural finding. **They were
corrected before shipping, which is what the rule requires** — to `separation` (b255's own word for
`PR − Θ_q`) and to `sealed`. **No claim, no number, no branch, no bar and no grade changed.**

### But it had a cost, and the cost is the point.

**The Rule 3 correction rewrote the registration *after* the run had executed.** So
`mtime(registration) < mtime(run)` — the filesystem evidence that the registration came first — **is
now false and cannot be made true again.** The gate that depended on it failed, and it failed
correctly.

**It was not weakened to pass. It was re-anchored, and it now asserts the destruction:** the gate
*requires* `mtime(registration) > mtime(run)` and requires the bank to say why, **so that a later
back-dating of the registration would be a gate failure rather than a repair.** What survives as
assurance is content — the `1e-9` bar, all three falsifiers by name, the per-step branch
expectations, the seven prohibitions, and the run's own printed bar matching the registration's —
**none of which a stem substitution could touch.**

**And the honest limit: a content check is weaker than a timestamp. This act asks to be taken at its
word on one point where it previously did not have to be, and that is a real reduction in its
auditability.** *The lesson, so the cost is paid once: **run the banned-term review against the
registration BEFORE the run**, not in the closing sequence where it has always sat.*

---

## Controls

**Tautology control.** (T1) the reduction: **20000/20000** — *it is meant to; it is a tautology and
is reported as one.* (T2) the trace bound on **arbitrary** `tq`: **5898/20000** — **70.5% of
arbitrary tuples refute it.** Had this read 20000/20000, the "derivation" would have been a tautology
wearing a theorem's clothes.

**Positive controls, five, each shown able to say no.** The per-term comparator rejects a `+1`
perturbation; the corr-sign test counts 3 of 3 on a deliberately negative array; the index matcher
rejects the invented `(7,1)`; the closed form gives 4 distinct values across `k`; and the F1
deviation measure catches a one-level shift by **eight orders of magnitude** — so `4.441e-16` is a
real agreement, not a bar too loose to fail.

**The standing clause, checked rather than recited.** `theta_quotient` and `left_side` were
**imported, not re-typed**; the four instruments are byte-unchanged under `git -C`; no value from
either side enters any definition. **The named crime did not occur.** The choice-dependence tripwire
is not tripped.

---

## What this act does **not** establish

1. **It does not prove the identification** — bench-verified premise, and the grade carries it.
2. **It says nothing about the limit.** b15 and b242 govern.
3. **It is not evidence for or against `T + Q = W_inf − W_primes`.** R-III governs the vocabulary.
4. **It does not close M-2** — RULE Q's aggregation is still UNSTATED.
5. **It does not promote b10's / b228's factor question** in either direction.
6. **It does not sign `E2even`.**
7. **It does not generalize past this test function** — a sign-changing bump can break S3 termwise.
8. **It compiles no part of J1 itself.**
9. **Nothing about `h2` beyond the register sentence exact. Nothing deposits.**

---

## Filings

- **Index:** queried before the route was written — `quotient trace` **HIT**, `weil ledger` **HIT**,
  the free-text query **NO KEY** with the tool's reach line carried. **`junction-sign` / `j1` keyed
  on this filing.**
- **J1's `PARKED-BY-AUTHOR` status (b256, "save"): DISCHARGED. J1 is executed.**
- **J2** — `E2even`'s monotone decrease, now **the only unsigned term in `resid(A)`**. Route note:
  **J1's method does not transfer** — `E2even` has neither a per-term closed form nor an index set to
  share. Its honest first step is a **statement-read on `eps`**, not a ladder; b255's (W1) silent
  grid failure is the standing warning. **No route adopted.**
- **New work-order: `W-ORD-TQ-IDENTIFY`** — filed, not run, with the operator-norm dead end recorded
  beside it so nobody re-walks it.
- **In flight:** M-2…M-5 open, **none closed.** `W-ORD-B38-HIGHMODE`, `W-ORD-CN-LAW` (standing),
  the QUOTED-N extension, `W-ORD-XI-PERMODE`, `W-ORD-ORDINATE-CACHE`, `W-ORD-STAGING-GUARD`,
  `W-ORD-FILE-E-WORKING-COPY-STALE`, `W-ORD-TE-SPEC`, **`W-ORD-TQ-IDENTIFY` (new)**.
- **The thirty-seventh seam's debt restated:** term 2's formalization stands, **unpaid and
  untouched.** J1 touches none of its four items.
- **HANDOFF** brought current by demotion, prior content kept, read-back identical — **and the
  three-act omission is named: b257, b258 and b259 are banked and never entered this lead.**
- **The patent lane is independent and untouched.** Nothing written under `patent-package/` or
  `PLACE-papers/`; **staged by explicit path, no `git add -A`**, and b256's live b148 condition is
  re-reported rather than assumed cleared. Monday's uploads noted as **pending and not verified by
  this seat**.
- **`PLACE-papers` was not touched, so no mirror rebuild is owed and none is claimed, and the hook
  was not exercised — reported either way.**

### The fork at this stop

1. **J2** — the only unsigned term left in `resid(A)`, with its route note already saying J1's
   method will not carry it.
2. **`W-ORD-TQ-IDENTIFY`** — prove the identification J1 carries as a premise.
3. **M-2's finite address and the aggregation; M-3; M-5; `W-ORD-CN-LAW`.**
4. **The patent lane**, independent, on your word.

---

### **THE JUNCTION'S SIGN IS A THEOREM ON THE OWNERS' DEFINITIONS, WITH ONE PREMISE NAMED AND CHECKED FOR THE FIRST TIME. `2 log p` HAS ITS FIRST DERIVED *LIMIT*: IT CANCELS AND CARRIES NOTHING. THE SEPARATION IS act 9's RANGE READ AT b17's STAIRCASE. J2 STANDS NEXT. NO GRADE MOVED BUT J1's OWN. `h2` STANDS EXACTLY WHERE THE DEPOSIT LEFT IT. NOTHING DEPOSITS. LOCKS LAST.**
