# `W-ORD-SPECIES` — CHARTER, WALK-SHEET, ACT 1 — 2026-08-11

**The walk proceeds as RESEARCH-INTEGRITY REVIEW, not publishing. Posture unchanged. Nothing deposits.**
**Rail `de621b1` / `2147a03` unmoved.**

> ### PATH-OF-CONSEQUENCE
> **"This register specifies and hunts the display device — a Hermitian polarization with `i`,
> without conjugation, on the enlarged object. **Its definiteness IS the clause**, so every act halts
> at STRUCTURE and holds anything definiteness-shaped."**

---

## §1 — THE `v0.5` WALK-SHEET

**24 rows filed to `OPEN_TRAILS`, one per headline claim: grade · flag · one-line source.
Per-row ruling requested — `CONFIRM` / `RESIZE` / `RE-FLAG` / `QUERY`. No content changed this act.**

> ### The sheet's own honest summary: **of 24 rows, exactly ONE is `MY-SYNTHESIS` — §31, the terminal
> delta, the row the era is named for.** Everything else is at cite, compiled, computed or
> question-graded. **The single flagged row is the single interesting one — the shape a reviewer
> should expect, and should test hardest.**

## §2 — `E1`/`E2`: THIRD SWEEP FAILS; `STANDARD-AT-TEXT` APPLIED

`ti:"abelian varieties" AND abs:"lecture notes"` → one entry, on moduli, not the definitions.

> ### **Three independent sweeps have now failed on the same content.** The instrument-mismatch loom
> line is no longer an inference from one act — **it is corroborated three times.**

`E1` → **`STANDARD-AT-TEXT`**, Birkenhake–Lange ch. 2 · `E2` → **`STANDARD-AT-TEXT`**, Mumford §6 /
theorem of the cube. **Both UNFETCHED and FLAGGED**, applied under the ferry's ratification —
**revert to `NOT-AT-CITE` on one word if that reading is wrong.**

## §3 — CANDIDATE 1: THE 1952 WEIL FORM

`S1` **PASS** (Hermitian, complex test functions) · `S2` **no located source demands a real form** ·
`S3` **PASS** (infinite-dimensional) · `S4` **PARTIAL** (scaling action present; `Θ` not established)
· `S5` **PASS definitionally** (the explicit formula *is* archimedean term + prime terms) ·
`S6` ### **ARRESTED AT LEVEL — it lives on FUNCTIONS.**

> ### **Four passes, one partial, one arrest — and the arrest is the programme.**

> ### ⚠ **THE FINDING, AND IT RESIZES §31: the "new species" is NOT NEW.** The 1952 Weil form is
> already a Hermitian form on an infinite-dimensional complex space with no source demanding a real
> form. **§31 said the constraints "jointly specify a new species"; they specify the species the Weil
> form already belongs to.** ### **What is missing was never the FORM — it is the OBJECT the form
> should live on. The carrier. Which is what this programme has been about since its first page.**
>
> *It REDUCES a claim rather than extending one, so it lands without the gate. §31's wording is
> queued for `RESIZE` on the walk-sheet; the author's row-ruling governs.*

**THE HALT, TAKEN: the Weil form's definiteness IS RH. Nothing is said about it.**

## §4 — CANDIDATE 2: CC's `HC^ar`

`S4` **PASS-AT-CITE** (`Θ` as λ-operation generator) · `S2` ### ⚠ **AT RISK / LIKELY CONFLICTS** — the
`F_∞` fixed points are a **real-structure mechanism**, and `S2` forbids a real form. **`C5` again, on
a second object.** · `S1`, `S3`, `S5`, `S6` **not adjudicated — graded from holdings, not a fresh read.**

## §5 — FOUND-BEYOND

1. > ### **The register opened to hunt a new object, and its first act found the object already in the
   > corpus, 74 years old.** The spec `S1`–`S6` was assembled from this era's holdings, and the first
   > candidate passes four of six. **The era's "terminal requirement" describes something Weil wrote
   > down in 1952.** *A deflation — which is what a register chartered to hunt a new species is least
   > likely to look for.*
2. > **`C5` has now fired on THREE separate objects** — D–S's manufactured real form, the arithmetic
   > torus's real skeleton, CC's `F_∞` fixed points. ### **Every pairing-bearing object this programme
   > has examined supplies a REAL structure, and the no-go forbids exactly that.** *Question grade,
   > `KIN` by shape, **no promotion**.*

## §6 — THE FRIDGE-LIGHT STATEMENT *(filed to `FINDINGS`)*

Three sentences at their earned grades: **the wired fact is deterministic** (compiled) · **every
partial invocation is dark by theorem** (compiled + at cite ×2 — the `s`-dark theorem, the Epstein
witness, the discreteness result) · **the display device is uninstantiated** (this era's terminal
delta, flagged).

> ### *You cannot check whether the fridge light is on by opening the door, because opening the door
> is what turns it on. Here it is sharper and worse — **every partial look is a door, and the corpus
> has a theorem saying so.***

**It does not say `h2` is false, or unprovable, or that the device cannot exist.** It says the
programme **knows precisely what it lacks, has proved no partial view supplies it, and has not built
the thing that would.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `8f40021` (charter + sheet) → this pass |
| relay | this report's commit |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits. `h2` outside every arrow. Nothing definiteness-shaped was written.**
