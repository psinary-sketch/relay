# `W-CONSTRUCTION-1` · ACT 3 — THE FACTORIZABLE-IMAGE LEMMA + THE KERNEL CATCHES UP
## ### **`(F-yes)` AT PROOF GRADE MODULO TWO NAMED STEPS (THE VAGUE LEMMA IS NOW TWO SHARP ONES) — THE CNU ENGINE IS A FORMALIZED LEAN THEOREM AT std3 — FILE A'S ROOT SORRY CLEARED (7 → 3) — THE KERNEL'S TOTAL LABELED DEBT: 5 SORRIES, EACH WITH ITS OWNER**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 3 · sub-gate declared · Rule 4 ·
Fable/proof-search + Fable/Lean · nothing deposits · nothing circulates**

> ### **SCOPE, once: the global object built and abstract; Route 1 at two named steps; the register
> moves only via the closure protocol.**

> ### **RULE-3 LOG:** *act 2 at `b1f1c6c` ✓; the 126-terminal inventory checks (57 + 43 + 26) ✓; the
> limit/CNU theorems were indeed uncompiled and now the CNU ENGINE is compiled ✓. Mathlib holdings
> grep-checked before writing (the topological `RestrictedProduct` exists; the Hilbert `⊗′` does
> not; `starProjection` is the projection API; `mem_iff_norm_starProjection` carries the
> norm-equality iff). Terms coined: none.*

```
build document  THE_GLOBAL_SECTION.md v0.3 (§9, §9.1)
kernel          tools/lean/mathlib-companion/{LocalLimit, GlobalSection, PadicStandardAddChar}.lean
                — all three ELABORATE against real Mathlib (toolchain v4.30.0-rc1 @ cecd0c4d56)
```

---

## §1 — ITEM 1: THE LEMMA *(proof-search)*

*Three legs: factorizable spanning of the idele test class (STANDARD-AT-TEXT, Tate's structure) ·
per-place density (finite proved; `∞` = act 2's `(C-dense)`) · tail triviality on unit-invariant
units.* ### **`(F-yes)` MODULO: (α) the radial-unit existence (carried from act 1); (β) the
constraint-invisibility step (the Weil conditions remove only pole components, which the object
annihilates — the one technical step: the image map's spectral support avoids `±i/2`).** *`(F-no)`'s
complement branch stays registered against their failure. The diagonal-density claim of Route 1 is
now: TWO sharp steps from proved.*

## §2 — ITEM 2: THE KERNEL *(sorries counted, never hidden; axiom prints run)*

1. ### **`LocalLimit.lean` — THE CNU ENGINE IS NOW A PROVED LEAN THEOREM** *(the full act-1 argument:
   norm-equality ⟹ membership via `mem_iff_norm_starProjection`; the orbit induction; the escape
   hypothesis closes it), with positivity-is-norm and radical-zero beside it — all at the Mathlib
   std3 profile (`propext, Classical.choice, Quot.sound`).* **One labeled sorry: the `L²(ℚ_p)`
   realization, OWED TO FILES B–C.**
2. **`GlobalSection.lean`** — the global data structure with `F⁴ = 1` and the fixed-sector norm
   PROVED from the fields; **one labeled sorry: the Hilbert `⊗′` construction, OWED TO
   INFRASTRUCTURE** (Mathlib holds only the topological `RestrictedProduct`, grep-checked).
3. ### **FILE A: THE ROOT SORRY IS CLEARED** *(the definition's membership — real p-adic valuation
   arithmetic, proved in-act) plus nonneg, lt-one, vanishing-on-`ℤ_p`, and `map_zero`:* **7 sorry
   warnings → 3 sorried declarations** *(the spec via `ker_toZModPow`; additivity mod 1; the
   conductor iff).* ### **PR-readiness, honestly: one author's-hand session from PR-shaped — the
   three remaining proofs are the substantive ones, and the precedent note (one statement per PR,
   the author's own review responses) governs.**

### **THE TOTAL LABELED DEBT: 5 SORRIES — 1 to B–C, 1 to infrastructure, 3 in file A — printed in
the build document's §9.1 with the axiom profiles.**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING AT COMPLETE ROSTER IN ANY KERNEL FILE.
NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING CIRCULATES.**
