# THE SERIALIZING CLOSE — b244, 2026-08-29

**Scope:** filings and ruled amendments only. ### **Nothing was computed, compared, or faced off,
and no number crossed between columns.** A finite-place-set object at a finite cutoff decides
NOTHING global (b14/b15). No grade was promoted beyond what its own act earned. Nothing deposits.

> **b15:** *"**a finite-place-set object at a finite cutoff decides nothing global**."*

---

## The ruling, and whose it is

> *"**RULE Q: O1** — `Q.value := −Θ_q` as documented binding, the five owner texts cited, the
> aggregation still UNSTATED and said so. **RULE Δ₋: D1** — RULE M-1's combination amended to
> `T.value := Tr_full + E2 − Δ₋` per §19's own sentence, originals visible, the M-1 card annotated.
> **RULE MODES: K1** — the definition stays Lemma F.1's eleven modes; the per-cell realization
> reports the seven computable plus a tail term in its bar; `W-ORD-MODE-PRECISION` filed (K3)."*

### **A RULING, NOT A DERIVATION.** The author's word, carried by ferry and ratified by the paste.
**b241 routed the Q-orientation and chose nothing; b241 filed Δ₋'s sign and executed nothing.** The
record must show which seat decided, and it was not this one. **Executed in three correspondence
rows — 91, 92, 93 — one per clause, so that striking one does not disturb the other two.**

### The disclosure the standing clause requires

### **Both ruled orientations shrink the residual.** `Q.value := −Θ_q` **is** b240's banked variant
V2. What makes the execution lawful is not that the shrink is small:

1. **The executor did not choose it.** b241 read the texts, found five that *orient* `Θ_q` and none
   that assembles it into `Q.value`, and routed.
2. **The warrant is quotation.** All five texts are cited *by number inside the binding*, so a later
   reader can check the ruling against them.
3. ### **No orientation closes the separation.** V2 stays **19×–24×** the combined bar, V3
   **8.6×–19×**, and the largest term `resid47` is untouched by every one. **A ruling chosen to make
   the columns meet would have to make them meet.**

### **And the residual movement is NOT computed in this act.** It is corroboration discovered after;
b245 runs the columns under its own banked meanings.

---

## 1. The execution

**File E amended at support-voice, originals quoted in place** — the governing **residence** copy at
`SIDE-global-section/Interfaces/FiniteInstanceIdentity.lean`.

- **RULE Δ₋ (D1):** `value := Tr_full + E2 − Δ₋`, with the original `+ Δ₋` quoted in the amendment
  note and the warrant quoted beside it — §19's row *"our object's trace = this − Δ₋(g)"* and
  `b36_act8.py:175`'s `RIGHT = (Tr_full + E2 - Dneg) - Thq`, the only place in the corpus where the
  combination is written as executable code. ### **Δ₋'s definition and binding are UNTOUCHED** —
  §17's odd-index `t(n)` series via `b37_act9.eps_masked(rr, odd)`, which b241 verified b240 had
  bound correctly. **Only the sign moved.**
- **RULE Q (O1):** `value := −Θ_q`, five owner texts cited by number, ### **and the sentence the
  ruling requires be said, said in the binding:** *"THE AGGREGATION IS STILL UNSTATED"*
  (`quotient-trace`, b197, re-confirmed b215). The five texts **orient** `Θ_q` inside their own
  comparisons; **none assembles it into `Q.value`**, and ### **M-2 is not closed by this binding.**
- **RULE MODES (K1):** the realization note added, with b242's numbers written in and the tail
  declared **NOT BOUNDED**.

### The proof that no code moved — run, not asserted

```
comment-stripped HEAD lines : 19
comment-stripped WORK lines : 19
IDENTICAL: True
```

with the relation unmoved at `T.value + Q.value = W.wInf - W.wPrimes`. The diff is **76 insertions,
2 deletions**, and ### **both deletions are inside docstrings** — the `+ Δ₋` line (replaced, original
quoted) and the `-/` that closed the `QuotientTrace` docstring before it was extended.

### The profile — read, not inferred

`lake env lean` from the declared pin `D:/mathlib4`:
`'FiniteInstanceIdentity.finiteInstanceIdentity' depends on axioms: [propext, Classical.choice,
Quot.sound]` — ### **matching the banked line.** b239's law carried: *a docstring cannot move a
profile, and this act did not assert that — it ran the print.*

### The rows, and the M-1 card

Rows 91–93 written by the committed `corr_row.py`, **six cells each, no blanks, verified by
read-back from disk by the tool itself**; `CORRESPONDENCE.md` is **3 insertions, 0 deletions**.
### **The M-1 card lives in the loom and the loom is append-only, so it is annotated by this act's
dated entry and NOT by editing the 2026-08-28 line** — b240's own lesson: *"a global replace on an
append-only document is an edit to the record wearing the clothes of a fix."*

---

## 2. The filings — nine listed at registration, nine filed

1. ### **IMP-1 → `VERIFIED-AT-BENCH`**, spec at `data/b243_imp1_envelope.txt`. **The grade is
   b243's and this act transcribes it**, with its rider intact: **a bench grade, not a proof of CC's
   equation (1)**, on a bound that is a rigorous worst case and **loose** (slack 2.3× to 1.5e6).
2. ### **`bar_L` → HELD**, and ### **the direction at full prominence: `bar_L` may be 2.4×–2.9× TOO
   SMALL.** Filed against this programme's interest, because **a bar that is too small makes a
   separation look more significant than it is** — and an executor who measures a bar and hides
   which way it moves has not reported the measurement. **The consequence for any face-off branch is
   not drawn here.** `W-ORD-LEFT-MODE-AXIS` **DISCHARGED**, on the branch it got.
3. **`W-ORD-ORDINATE-CACHE`** — b238's zeta ordinates lived in a session temp directory that no
   longer exists. *A run whose input lives in a temp directory cannot be repeated.*
4. **`W-ORD-MODE-PRECISION` (K3)** — the extended-precision prolate eigensolver, priced by b242 at
   ~3.45 decimal digits per further mode, ~130–175 dps for a useful tail.
5. ### **`W-ORD-STAGING-GUARD` — FILED, NOT BUILT.** The b241 `git add -A` breach converted to a
   named guard. **The precedent is why it is a work-order and not a resolution:** b148 breached,
   `place_add.py` was built, and **b178 breached again with the guard present, working and unused**,
   because `git add -A` was typed instead. *"A guard that must be chosen is not enforcement."*
   Building a hook changes the command path and is the author's.
6–9. The five-term ledger restated; b245's preconditions; the in-flight register and the
   thirty-seventh seam's debt; HANDOFF, loom and mirror current for all three acts.

### The five-term ledger under the rulings

| term | status |
|:--|:--|
| `2·E2` | **STANDING** — no ruling touched it |
| `Δ₋` | **RULED (D1)** — definition and binding unchanged; only the sign moved |
| `resid47` | **STANDING**, still the largest term at every cell and **growing** — M-4's unpaid size, **and M-4 is not paid** |
| `Θ_q` | **RULED (O1)** — five texts cited; **the aggregation still unstated**; M-2 not closed |
| `PR` | **STANDING** — orientation settled by b235's eight agreeing cells |

### **Two ruled, three standing, none promoted.** And the sentence the ledger still cannot say:
### **nothing here makes the columns meet** — the two ruled terms together are at most ~1.2 of a
5.85–8.09 separation, and at `a² = 2` they are **both zero** while the separation is its largest.

### b245 — the second face-off. Two green, two amber, one open by design

| # | precondition | state |
|:--|:--|:--|
| 1 | this act's rulings executed | ### **GREEN** |
| 2 | the right side's bars certified | ### **GREEN**, looseness rider carried |
| 3 | `bar_L` in its honest form per K1 | ### **AMBER — the real one** |
| 4 | the same banked-meanings discipline | b245's own first act |
| 5 | M-2 | **OPEN**, and O1 does not close it |

### **(3) is the one that matters: K1 tells b245 what to report — seven computable modes plus a tail
term in the bar — but the tail is NOT BOUNDED.** b242's branch is (SLOW) and the envelope was
refused. **So b245 carries a bar with an unbounded term in it and must say so in its own words
rather than inherit a number that looks certified.** And (5) means **b245 remains a per-cell
statement and not a structural one.** ### **The patent session can slot at this evening's STOP and
needs nothing from b245.**

---

## Gates, hook, mirror, pins

**13 of 13 PASS, CLEAN.** ### **But not on the first run in the way that counts, and the reason is
worth more than the count:** run 1 passed 13/13 with gate 2's fixture reading
`code_identical(...) is not True` — ### **the exact negation of its own check.** It failed whenever
the check passed and therefore demonstrated **nothing** about whether the comparison discriminates.
### **A fixture that is the negation of its check is not a must-fail fixture; it is the check
wearing a minus sign** — and the harness cannot tell the difference, as its own header admits
(*"it cannot tell a good fixture from a bad one"*). Replaced with a comparison of File E's stripped
code against `GlobalSection.lean`'s: two real files that genuinely differ, so the predicate is shown
able to say no. **The gate was passing for the wrong reason and I caught it by reading it, not by
running it.**

**The PLACE-papers seat-boundary hook was exercised** and reported either way, as the ferry
requires: **CLEAN — 1 staged path, 0 foreign hits**, prefixes checked
`phase1.5/method/patent-package/`. The patent seat's untracked material was **not staged, not
committed and not mentioned.** Staging went through `place_add.py` — the guard b178 proved must be
*used*, not merely present.

**The loom append: one hunk, 94 insertions, 0 deletions**, with the pre-image verified as a prefix
byte-for-byte *before the write was accepted*.

**The mirror rebuilt and verified under all three clauses: CLEAN** — 40 files, 40 rows, 0
mismatches, declared HEAD `34b71fa` against `ls-remote` `34b71fa9c5c1`.

**Term scans CLEAN in all three repositories** — relay 842 + 1 lines, PLACE-papers 94, SGS 77, 0
live uses.

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b244
  run at    : 2026-08-29T10:51:34 (local)
  input     : 13 checks routed through the harness
  checks    : 13
  pass      : 13
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 c7fb605cd49564021bb49a4917bb4b8e
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : mirror_verify
  act       : b244
  run at    : 2026-08-29T10:49:55 (local)
  input     : mirror-refresh-2026-08-29.zip
  files     : 40
  rows      : 40
  mismatch  : 0
  declared  : 34b71fa
  ls-remote : 34b71fa9c5c1
  VERDICT   : CLEAN ON ALL THREE CLAUSES
  self-hash : sha256/32 2eff954fae60b0b2c3994ccb1519e50d
=== END AUDIT SIDECAR ===
```

**And one work-order got worse, disclosed rather than quietly widened:**
`W-ORD-FILE-E-WORKING-COPY-STALE` — relay's working copy was stale by **two** amendments when b241
found it and is **now stale by three**, because this act amended the governing residence copy and
did not sync. Moving a kernel-adjacent file between layers is the residence ruling's own *"never by
drift"* and is not a filing act's to do.

---

### **THE LEDGERS ARE WHOLE THROUGH b243. THE RULINGS ARE IN THE RECORD WITH THEIR TEXTS. M-2…M-5
STAND OPEN AND THIS ACT CLOSED NONE. NO CODE MOVED AND IT WAS PROVED. NOTHING WAS COMPUTED,
COMPARED OR FACED OFF. `h2` STANDS EXACTLY WHERE THE DEPOSIT LEFT IT. NOTHING DEPOSITS. LOCKS
LAST.**
