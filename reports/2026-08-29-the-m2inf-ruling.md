# THE M-2∞ RULING EXECUTED — b253, 2026-08-29

**Scope:** filings and **one ruled re-binding at support-voice**. **CONCURRENCY: SOLO.** No face-off
ran; no analytic claim was promoted — b252's divergence remains a bench reading and **Q1 is
definitional only**. A finite cell decides NOTHING global (b14/b15). Nothing deposits.

---

## The ruling executed

> **`RULE M-2∞: Q1`** — the **quadrature** construction (`left_side`'s one-axis integral, in which
> **no mode index appears**) is the archimedean object the identity's left column denotes; the
> per-cell realization of the ruled C2+D1 binding is **re-bound** to it; the mode sum
> (`trace_modes`) is **demoted to a truncation diagnostic** under the standing law *"every quoted
> partial sum carries its N and its precision"*.

### **The definition does not move.** C2, D1, RULE Q O1 and RULE MODES K1 all stand as ruled.
**Only the per-cell realization's binding moved.**

### Derived from the owners' own lines, and shown

From `b36_act8.py:184` `resid47 = Tr_full − (A + E2)`, i.e. **(i)** `Tr_full = A + E2 + resid47`;
with the ruled binding **(ii)** `T.value := Tr_full + E2 − Δ₋`, substituting construction for
construction gives

> ### **(iii) `T.value := A + E2 − Δ₋`**

The combination is unchanged; only which construction realizes the archimedean trace has moved.

---

## The cost, disclosed in the executor's own voice

`T.value^OLD − T.value^NEW = **E2 + resid47**`.

### **The re-binding removes `resid47` *and one `E2` term*** — because the old assembly carried `E2`
**twice**: once in the combination, once inside `Tr_full`'s comparison against `A + E2`.

The ferry disclosed the consequence as *"~61–70% of the measured shortfall"*, which names `resid47`
alone. ### **The registration banked the duty to check this *before* the re-binding was written**,
precisely so that finding it could not look like a concession made after the fact.

In the shortfall's algebra:

- **OLD:** `L − R = resid47 + 2·E2 − Δ₋ + (PR − Θ_q)` — exactly b251's measured decomposition.
- **NEW:** `L − R = E2 − Δ₋ + (PR − Θ_q)`.

### **The size of the remainder is not computed: that is a face-off, and b253 ran none. It is b254's.**

---

## The R-label match: **HALTED as ambiguous, and routed**

Q1's final clause required the match and provided the halt. **The halt is taken.**

- **By wording, Q1 is R-I** — *"THE QUADRATURE IS THE OBJECT"*, almost verbatim.
- **By content it is not.** Q1 demotes the mode sum to a **diagnostic**, not to an **approximation**,
  and R-I's own consequent *is* the approximation reading — **which b252 refuted**: the mode sum does
  not settle, and `Δ_2real := Tr_∞ − A − E2` has no limit to be. **A quantity with no limit is not a
  convergence error.** b251 flagged the doubt in R-I's own sentence.
- **By consequence, Q1 is R-III.** The ferry's own *"removes … **by definition**"* is R-III's
  *"THE SHORTFALL IS AN ARTEFACT OF THE PAIRING RATHER THAN A DEFICIT."*

### **What turns on it: under R-I every number b254 measures is a deficit still owed; under R-III the
same numbers are the residue of a pairing error and the 61–70% was never owed at all.** The two
readings assign **opposite meanings to b254's entire table**. b237 governs — an executor does not
settle a definition.

**The halt is not a blanket refusal, and the record shows it:** **R-II is excluded cleanly and
without doubt** (Q1 names the quadrature; R-II names the mode sum). **And the ambiguity was
registered before the dossier's text was weighed** — registration §(D) banked *"I EXPECT THE MATCH TO
BE AMBIGUOUS AND I SAY SO BEFORE WEIGHING IT"*, with both grounds.

### **The halt halts the match only.** The re-binding is derived from Q1's own words and the owners'
lines and **does not consume the R-label** — so every other component executed in full. That was
fixed in the registration before the halt was taken, so the halt could not be used to shrink the act.

---

## The QUOTED-N law, now standing

Any quoted `Tr_full`, `TrN`, `S_N`, `tr[n]`, `Δ_2real` or `resid47` must carry **its mode count `N`
and its precision**, or it is **UNGRADED**. It bites immediately: b251's `Δ_2real` is quotable only
as `(N = 11, float64 modes, suspect above n = 6)`. ### **b251's branch is NOT re-verdicted (b246) —
the law governs future quotation, not past verdicts.**

---

## Three work-orders filed — none run, none reported discharged

*b148 built a guard and b178 breached again with it present and unused: **building a thing is not
discharging it.***

- **`W-ORD-B38-HIGHMODE`** — sweep the corpus for citations of float64 `tr[n]` at `n ≥ 7`; annotate
  each `SUSPECT-BY-b252` **with its `N`**. **Annotate — not delete, not recompute**, no banked branch
  re-verdicted. Known candidates named (b38's own `TRIPLE`, b245, b251) and **explicitly not claimed
  complete**: a work-order that named three files and implied it had found them all would be the
  defect it exists to repair.
- **The QUOTED-N term-scan extension** — a `QUOTED-N` verdict class, **with fixtures in both
  polarities, because one polarity is not a fixture.**
- **`W-ORD-CN-LAW`** — derive the `C/n` form from `A_n(0) = 1` and the weight's source form, with the
  asset list. **b252's width heuristic was registered as a heuristic, and being right did not
  promote it.**

---

## b254 named, with its preconditions — and not run

Expected composition, **stated as an expectation**: arrangement terms plus the naked junction,
`L − R = E2 − Δ₋ + (PR − Θ_q)` — symbolic, **not evaluated at any cell.**

1. **The halted R-label.** b254 can **measure**, but cannot report what its table **means** until the
   label is ruled.
2. **RULE Q's aggregation is still UNSTATED.** b254 **must not treat measurability as statedness.**
3. The QUOTED-N law applies to every number it quotes.
4. ### **`Δ₋`'s own realization is also a mode sum, and whether Q1 touches it is NOT decided here and
   is flagged open** — Q1 names the archimedean *trace*'s construction, and `Δ₋` is a different
   constituent. **An executor who silently extended Q1 to `Δ₋` would be ruling.**

---

## Gates — 14 of 14 CLEAN, and three first-run failures disclosed

### **All three were defects in the gates, not in the artefacts.**

1. One looked for a phrase in the **wrong file** — the hyphenated form lived in the bank, while the
   registration states it in other words.
2. One **sliced a character off a filename**, because `.strip()` on the whole porcelain blob ate the
   first line's leading status space.
3. ### **One matched `left_side` inside a string literal that quoted an owner's name as prose** — a
   correspondence cell and an index row. **b248's species in a new guise: b248 matched inside a
   *comment*; this matched inside *data*.** The `ast` stripper removes docstrings and `#` comments,
   and should not remove data strings. ### **The fix tested what the rule means rather than weakening
   it: a filing act *may* quote an owner's name — the ferry requires it — but may not import or call
   one.** Matching on mentions would have forbidden the very quotation the ferry demanded.

**The tautology control has both halves:** the re-binding derivation holds on **arbitrary values**
(proving it is algebraic restatement and therefore no evidence), **and** the excess claim **fails when
`E2 = 0`** (proving the excess is a claim about `E2`, not an artefact of the algebra). Every check is
a **pure conjunction**; no `or` appears in any check.

**File E: docstring only** — comment-stripped HEAD against work gives **19 code lines both sides,
IDENTICAL**, and the original C2+D1 binding sentence **stays visible**. **CORRESPONDENCE row 94**
written by the committed tool with its cells as **Python literals in a file, never shell arguments**
(b158) — six cells, no blanks, read back. Term scan **CLEAN, 0 live over 942 lines**.
**PLACE-papers was NOT touched, so the hook was not exercised and the mirror was not rebuilt —
reported either way.**

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b253
  run at    : 2026-08-29T14:30:06 (local)
  input     : 14 checks routed through the harness
  checks    : 14
  pass      : 14
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 862a736b25b37fc857a3e16f6eb5b767
=== END AUDIT SIDECAR ===
```

---

## Filings

- **Index:** `m2inf-ruling` and `quadrature-binding` keyed on filing; both queried **NO KEY** first.
- **The thirty-seventh seam's debt restated:** term 2's formalization stands, **unpaid and
  untouched.**
- **In flight:** M-2…M-5 open, **none closed**. New this act: `W-ORD-B38-HIGHMODE`, `W-ORD-CN-LAW`,
  the QUOTED-N extension. Standing: `W-ORD-XI-PERMODE`, `W-ORD-ORDINATE-CACHE`,
  `W-ORD-STAGING-GUARD`, `W-ORD-FILE-E-WORKING-COPY-STALE`, `W-ORD-TE-SPEC`.
  **And one halt on the table: the M-2∞ R-label match.**
- **HANDOFF** brought current by demotion, prior content kept, read-back identical.

### The fork at this stop

1. **The halted R-label** — one ruling; b254 waits on it for its *meaning*, though not for its
   measurement.
2. **b254, the fourth face-off**, with the imposter gone and the junction piece naked.
3. **M-2's finite-place address, M-3, M-5.**
4. **The patent session**, on your word.

---

### **THE ARCHIMEDEAN OBJECT DENOTES ONE CONSTRUCTION BY RULING-WITH-A-TABLE, AND THE DIAGNOSTIC IS
LAWFULLY LABELLED. THE R-LABEL MATCH IS HALTED AND ROUTED — AN EXECUTOR DOES NOT SETTLE A
DEFINITION. NO FACE-OFF RAN. M-2…M-5 OPEN. `h2` STANDS EXACTLY WHERE THE DEPOSIT LEFT IT. NOTHING
DEPOSITS. LOCKS LAST.**
