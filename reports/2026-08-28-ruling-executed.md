# THE RULING EXECUTED — b239, 2026-08-28

**Scope:** filings, one support-voice amendment, one kernel re-print. ### **NO face-off was run and
NO number crossed between the columns.** The first face-off is its own act (b240), under its own
registration. Nothing about `h2` beyond the register sentence exact. Nothing deposits.

---

## The ruling, executed whole

> ### **"RULE M-1: C2, per-cell instrument realization standing until M-4 closes; Δ₋'s bookkeeping
> (M-4) named as the definition's open debt in the correspondence row."**

Three clauses, three obligations, **all three discharged**: the definition written into File E; the
rider recorded beside it; ### **the debt named IN THE CORRESPONDENCE ROW** — not only in a bank the
executor wrote. *A debt recorded only where the executor writes is a debt the next reader will not
meet.*

---

## 1. The idiom — chosen, and shown

File E's own idiom is structures carrying a `value : ℝ` field with docstrings naming owners; it
declares no computational definitions. ### **`Tr_full`, `E2` and `Δ₋` have no formal definitions in
this repository.** Writing `value := Tr_full + E2 + Δ₋` as Lean code would therefore mean **inventing
three realizations the record does not have** — exactly the "realization invented" that b237's scope
refused one act ago.

So the idiom is the ### **DOCUMENTED BINDING**, and — as the ferry requires — **the choice and the
reason the alternative is refused were registered *before* the choice was made**, ### **so that the
softer-looking option stands on the record as the disciplined one and not the convenient one.**

## 2. The amendment (support voice, working HEAD)

`ArchimedeanE1Trace`'s docstring now carries, in order: the ruling **verbatim**; the definition with
each summand named at its instrument at a **diagonal `a²` cell**; the idiom's justification; the
rider — ### **"PER-CELL AT BENCH, STANDING UNTIL M-4 CLOSES … NOT structural"**; the debt —
### **"M-4 — Δ₋'s trace-class bookkeeping … `value` is defined only as far as that bookkeeping is"**;
and **the original docstring quoted in full.**

> ### **Verified exactly, not by eye.** With every comment and docstring stripped, the file's code is
> **identical to its HEAD blob.** The relation stands unmoved at line 107:
> `T.value + Q.value = W.wInf - W.wPrimes` — ### **STATED, NOT PROVED, NOT CLAIMED.**

**CORRESPONDENCE row 90**, written by the committed tool, six cells, no blanks, verified by
read-back. Its grade cell: ### **"DEFINED-BY-RULING (C2), REALIZED PER-CELL AT BENCH, OPEN DEBT M-4
(Δ₋ trace-class bookkeeping)."** Its terminal cell says what the row does *not* introduce: **no new
terminal.**

## 3. The re-print — a disclosed debt, discharged by running it

b235 amended File E and disclosed that it had **not** re-elaborated; b236 disclosed the same; b237 a
third time. ### **A fourth silence would have become a claim.**

**Two pins, because the bank itself says so:** five files at `D:\mathlib4` — **`v4.30.0-rc1`,
mathlib4 `cecd0c4d56`**, exactly the README's declared pin — and `RestrictedTensorLayer1` at
`D:\MY-DOwnloads\mathlib4`, **`v4.29.0`**, because the banked profile's own header reads *"built with
Lean v4.29.0 against Mathlib"*. ### **Using one pin for both would be comparing a new profile to an
old bank and calling a toolchain difference a finding.**

| | |
|:--|:--|
| terminals printed | **29** |
| matched to bank | **29** |
| differing | **0** |
| not in bank | **0** |
| counts agree | **YES** |

### **`FiniteInstanceIdentity.finiteInstanceIdentity` — MATCH.** File E has now been amended twice
(b235's convention line, b239's definition binding) and **its profile is unchanged.**

> *A docstring cannot move a profile — and this act did not assert that.* ### **IT RAN THE PRINT.**
> The law, honoured: *a compile is not a verification and the profile is read.* b227 shipped a file
> that compiled clean and printed `sorryAx`; b231 shipped one that compiled clean and printed **eight**
> axiom-bearing terminals. ### **No claim here rests on an exit code.**

**What the match does not mean:** that the statements are true — only that their axiom dependencies
are what the record says. ### **Core was not touched and its 404-line profile was not re-run**, said
plainly so no reader assumes the whole layer was re-verified.

---

## 4. The face-off checklist, final form — three green, one amber

| # | precondition | state |
|:--|:--|:--|
| 1 | **`T.value`** | ### **GREEN** — ruled and executed this act |
| 2 | **the assembly junction (M-2)** | **OPEN — and the per-cell bench form does not require it** |
| 3 | **the Interfaces re-print** | ### **GREEN** — discharged this act, 29/29 |
| 4 | **the right side's error bars** | ### **AMBER — MEASURED, NOT CERTIFIED** |

**(2), reasoned rather than assumed.** ### **The rider's whole content is per-cell realization**, and
b237 established that the junction sits at the **restricted-product assembly across places**, not at
the per-place computation — `Θ_q` computes each per-place term on `V_inv` without touching `S̄_v`. A
per-cell bench face-off never performs the assembly, so it never meets M-2. ### **And the price, in
the same breath: a per-cell face-off is a PER-CELL statement and not a structural one, and b240 must
carry that limit in its own words.**

**(4), carried unchanged from b238.** The sides agree to **~2e−8 relative at `a²=3` and 5e−9 at
`a²=4`**, but the bound banked to license the grade was **exceeded at one cell of six by 4%**, with
`W-ORD-IMP1-ENVELOPE` standing as the named remainder. ### **That is not a block on b240 — it is a
limit b240 must state.**

## 5. The M-list, with M-1 struck

- ~~**M-1 [RULING]** — what `T.value` is~~ ### **RULED AND EXECUTED.**
- **M-2 [RESULT or RULING]** — the operator onto `S̄_v`. Open. b227: *"THE FIRST IS NOT IN THE RECORD."*
- **M-3 [RESULT]** — the class-richness lemma, *"formalization owed to files B–C"*. Open.
- **M-4 [RESULT]** — ε trace-class bookkeeping. Open — ### **and now also the definition's named
  debt: the one item the executed ruling made load-bearing for a DEFINITION and not only for a proof.**
- **M-5 [CONSTRUCTION]** — the effective-mode dictionary, *"the missing transport"*. Open.

### **Four open: one ruling-or-result, two results, one construction.**

---

## What was refused

### **It was the largest temptation this arc has carried.** With `T.value` now defined and `A − PR`
already adopted, ### **a single instrument call would have produced both sides at a cell.** It was
named at registration as this act's principal risk **and it was refused**. b240 owns the first
face-off, ### **with both sides' meanings fixed before any number is seen.**

**Gates: 12 of 12 PASS, CLEAN — on the first run**, including the exact code-identity test, the
both-pins test, and the no-face-off gate. **All three term scans CLEAN, 0 hits** (relay 707 lines,
PLACE 94, SIDE-global-section 29).

---

### **What this act did not do:** no face-off; no number crossed between columns; no operator
constructed; no realization invented beyond the ruled per-cell binding; no grade re-graded (b237's
`t-value` index row was **annotated, not rewritten** — *a row is a pointer and never a re-grading*);
Core untouched; no deposited text edited. ### **NOTHING DEPOSITS. LOCKS LAST.**

**Next: b240 — THE FIRST FACE-OFF AT BENCH**, under its own registration, preconditions **three green
and one amber**, the amber named in the act's own words.

---

## Mechanics of the act

**The register** (`VERIFICATION_LOOM.md`, +94 lines) and **the HANDOFF** brought current **by
demotion, not by rewrite**: the demoted title was **derived from the file's own lead line** — asserted
to end in `(b238)` before any write — and the prior lead's whole content survives verbatim behind
`*(prior: b238)*`. *A title typed from memory is how a headline gets dropped quietly.*

**Term scans, all three CLEAN, 0 live.** relay 13 files / 1020 lines, PLACE-papers 94 lines,
SIDE-global-section 29 lines. ### **Six hits are shown, not filtered:** all six are **the scanner's
own rule text** (`stems : gap, blind`) inside the three sidecars it emitted, each printed with its
class. *The verdict reads the live count; the hit table is total.*

**Commits and read-back.** relay `1b536cd`, SIDE-global-section `05ec24b`, PLACE-papers `7071d76`.
### **The pre-push hook was exercised, not assumed:** `git push origin main` **from `main` was
REFUSED** — *"pushes to main only from push-\*/repair-\* branches (Rule 4.10)"* — and the push then
ran from `push-b239`. ### **All three read back by `ls-remote`: local and remote IDENTICAL.** In
PLACE-papers only `VERIFICATION_LOOM.md` was staged; ### **the author's untracked patent-package
figures were left exactly as found**, and the seat-boundary pre-commit printed **0 foreign hits.**

**The mirror**, rebuilt because PLACE-papers moved: `mirror-refresh-2026-08-28.zip`, **40 files**,
### **CLEAN ON ALL THREE CLAUSES** — content 40/40, declared source HEAD `7071d76` equal to
`ls-remote origin/main`, roster and archive agreeing name for name.

> ### **Disclosed rather than dropped: the first `mirror_verify` invocation printed NOT CLEAN, and
> the fault was mine at the call, not the mirror's.** I passed `--emit b239` where the tool takes
> `<zip> <remote> <branch>` positionally, so it looked up `refs/heads/b239` and found nothing.
> **Both sidecars are carried** — `audit_b239_mirror_verify.txt` (NOT CLEAN, mis-called) and
> `audit_b239_mirror_verify_r2.txt` (CLEAN, correctly called). ### **A failed run is not deleted
> because it was my error; the record shows the miss and the repair.**
