# STRAND 2's POWER CLAUSE + THE BACKGROUND LAW'S DERIVATIONAL HALF — 2026-08-10

**Machine-free sitting. No worker run, no census walk, no coefficient run. Arithmetic only, on
quantities re-read at source. Expectations registered verbatim at `92ad926` BEFORE any read.**
**The census stayed held aside and was never consulted. Rail `de621b1` / `2147a03` unmoved.**
**Nothing deposits.**

---

## §1 — THE BACKGROUND LAW RETRODICTS BOTH CONSTANTS

**Inputs re-read at source:** `γ = 16.290`, `δ = 0.4533`, `γ²/δ = 585.405` · synthetic
`n_det = 3379`, `c = 5.9`, background `~10³` · Epstein `n_det = 5938`, `c = 10.14`, background
`S∞(n) ≈ n log n` · law `n_det = (γ²/δ)·log(threshold × background)`.

**The residual is named and now measured.** Inverting `c = log(t·B)`:

| setting | background at depth | **threshold `t`** |
|:--|--:|--:|
| synthetic | `1.000 × 10³` | **0.36504** |
| Epstein | `S∞(5938) = 5.1596 × 10⁴` | **0.49105** |

**Agreement within a factor of 1.345.** With a single common threshold **`t = 0.42338`**:

| | predicted `c` | measured `c` | miss | depth |
|:--|--:|--:|--:|--:|
| synthetic | 6.0483 | 5.90 | **+0.148 (+2.51%)** | 3541 vs 3379 |
| Epstein | 9.9917 | 10.14 | **−0.148 (−1.46%)** | 5849 vs 5938 |

> **NOT REFUTED-AT-CONSTANT.** One threshold, two settings, ±0.148 in `c`, under 5% in depth —
> where the prior check could only say the constant-ratio 1.719 and the log-background ratio 1.571
> pointed the same way. **The residual that check named is now a number.** *Two points, one fitted
> parameter — reported as that, not as a confirmation.*

---

## §2 — THE SECOND OBJECT AT CONTENT

Reduced forms of disc −23: `(1,1,6)` principal · `(2,±1,3)`. **`h(−23) = 3`.**

**The representation numbers coincide, by proof not computation:** `(x,y) ↦ (x,−y)` is a bijection
of `ℤ²` carrying `2x²+xy+3y²` to `2x²−xy+3y²`. **One Epstein zeta serves both non-principal
classes.**

| **HOLDS** (the control) | **MOVES** (the variable) |
|:--|:--|
| discriminant −23, level; **the archimedean factor** (depends only on `\|D\|`); **self-dual FE**; **realness on the line**; order 1; `N(T)`; Euler-product-freeness | the representation numbers, hence all coefficients, hence **the zero set — including the off-line zeros' positions** |

> **Everything archimedean holds; only the arithmetic moves.** *Which is exactly what makes the
> experiment powerless.*

---

## §3 — THE POWER CLAUSE: **TWO INDEPENDENT BLOCKS**

Both hypotheses predict `n₂ = c₂·G₂`, `G₂ = γ₂²/δ₂`. They differ only in `c₂` — `H_transfer`:
`10.14`; `H_law`: `log(t·S∞(n₂))` self-consistently.

### BLOCK 1 — `G₂` IS UNKNOWN; NO WINDOW CAN BE CENTRED

**The corpus's two located off-line zeros, `0.953+16.29i` and `0.798+29.55i`, are both zeros of
the PRINCIPAL form.** None is known for the non-principal form. The only route to `γ₂, δ₂` is a
zero search on the second object — **which is the held-aside walk.** Using it to build the
prediction breaks the hold-aside discipline.

### BLOCK 2 — THE SCISSORS, which survives even if `G₂` is handed over free

Against the ±15% window and the attained rung `n = 7000` (dps floor **3821**):

| `G₂/G₁` | `n_transfer` | `n_law` | **sep %** | dps needed | vs reach |
|--:|--:|--:|--:|--:|:--|
| **1.0** | 5936 | 5838 | **−1.7%** | 3242 | **in reach** |
| 1.5 | 8904 | 9202 | +3.3% | 5019 | past |
| 2.0 | 11872 | 12685 | +6.9% | 6915 | 2× past |
| 3.0 | 17808 | 19900 | +11.8% | 10840 | 3× past |
| 5.0 | 29680 | 34980 | **+17.9%** | 19045 | 5× past |
| 10.0 | 59360 | 74823 | +26.1% | 40722 | 11× past |

> ### **SEPARATION EXCEEDS THE ±15% WINDOW ONLY AT `G₂ ≳ 4·G₁` — needing `n ≈ 27,000` at `≈14,700` digits, ~4× the attained depth AND precision. Where the instrument reaches, separation is ≤ 3.3% — five times inside the window half-width.**

**The shape is structural: separation is LOGARITHMIC in `n₂`; precision cost is LINEAR at 0.5441
digits per unit.** Different orders — **no engineering inside this design changes it.**

**(b) CENSUS PRICED AS FAR AS THE RECORD SUPPORTS.** 451 records, 64 complete rows, contiguous
through `t = 32.5`; same box, same row count. **The wall-clock rate was not recorded in a
re-readable form**, so the price is quotable in records and rows only and is **not convertible to
time** without a measurement this sitting is not authorized to run. **Declared, not estimated.**

**(c) I-7 PRE-CHECK — with a NEW clause.** Stage 1 clean (realness holds). **Stage 2: the second
pipeline MUST NOT compute `Z_nonprinc` from the class-sum** `Z_princ + 2Z_nonprinc = 2ζL`
(standard identity, stated from the literature, **not re-derived here**) — that shortcut imports
`ζ` and `L`. **An I-7 violation the first pipeline had no opportunity to commit; the route is
closed before any code exists.**

**(d) WINDOWS: CANNOT BE DRAWN.** The ±15% rule with phase-extension and pre-declared strip
overlaps carries over intact, **but the centres require `G₂`. No window drawn, none guessed.**

---

## §4 — VERDICT: **BLOCKED-AT-POWER**

**Re-designs named, none recommended:** *(i)* a **bounded off-line-zero pre-run** as a declared
input, separate from the census — removes block 1, leaves block 2; *(ii)* **a different
discriminant**, breaking the shared archimedean factor so backgrounds differ and hypotheses
separate at first order — attacks block 2, **gives up the clean control**; *(iii)* **test the
threshold instead of the depth** — the quantity §1 just measured twice.

---

## §5 — FOUND-BEYOND-REGISTRATION *(two items)*

1. **THE SECOND OBJECT IS A LINEAR COMPLEMENT OF THE FIRST, NOT AN INDEPENDENT INSTANCE.**
   `Z_nonprinc = (2ζL − Z_princ)/2`. A law confirmed on both is confirmed on **one instance plus
   something less than one.** ***This is precisely the check installed in `A_METHODOLOGY` §V.7
   the same day — count evidence bases, not phrasings — firing on the very next experiment.***
2. **THE CONTROL AND THE DISCRIMINATING VARIABLE ARE THE SAME QUANTITY.** The background the law
   takes the logarithm of **is** the archimedean factor the design holds fixed. **The design's
   cleanliness is its powerlessness**, and tuning cannot separate them.

## §6 — METHOD-CANON CANDIDATE *(earned; **not promoted**)*

> ### **A CONTROL THAT FIXES THE BACKGROUND FIXES THE CONSTANT.**

**Recognition signature:** *ask which variables each hypothesis actually reads.* **If every
variable the design holds fixed is read the same way by both hypotheses, the design cannot
separate them at any precision** — the tell being a separation that shrinks toward zero as the
control tightens. **A power clause that checks only precision misses this: the failure is in the
design's algebra, not its error bars.**

## §7 — STOP-AND-HOLD

**No progress on the clause and none claimed. Nothing here touches ξ.** The one positive result
is a two-point retrodiction with a single fitted threshold. **The experiment is blocked, the block
is quantified twice, and the re-designs are named but not chosen.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `92ad926` (registration, pre-read) → this pass's commit |
| relay | `0341284` → this report's commit |
| `SIDE-carrier-spec` | tag `v0.2.0` = `2051c52`, PRIVATE — untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits. The run itself was never started and is not requested.**
