# W-WITNESS-LATTICE — ACT 2: THE BEURLING CORNER

**Relay report · 2026-08-13 · author-called · post-reboot · corpus-first · QUESTION-FIT**
**Stopped at no claimed sign result — none arose. Nothing deposits.**

---

## §0 — ORIENTATION BLOCK *(run fresh this session; not recalled)*

**Track.** This act is **UNDERSTAND** — *"read, grade, map … **GATE: NONE — this track is always open**"*
(`THE_H2_PROGRAMME_CHARTER` v0.1, §1). ### **The ATTEMPT track is NOT OPEN and was not entered.**

**Pins, read live** (`git describe` over `D:\SIDE-*`, 42 repos, this session):

| repo | DEPOSIT-PIN *(what prose cites)* | WORKING-HEAD *(live 2026-08-13)* |
|:--|:--|:--|
| `SIDE-kernel` | `v1.5` = `0e5233f` | ### **`v1.7-2-g5e668b4`** |
| `SIDE-lv-conservation` | `v0.8.0` = `6efa9e5` / `v0.10.0` = `93c27ec` | `v0.10.0-18-g2f71068` |
| `SIDE-carrier-spec` | `v0.4.1` | `v0.4.1-1-gb3916cc` |

> ### **THE DIVERGENCE IS NOT A FINDING — THE CORPUS BANKED IT FIRST.** *`REGISTRY` DEPOSIT-PIN/WORKING-HEAD
> block (2026-08-12): "Conflating them is what produced the census's 'the kernel is current at `v1.5`'
> beside a live `v1.7`." **Divergence is not staleness.*** *Recorded so this act does not re-report it as news.*
> **Two repos read DIRTY** (`SIDE-effects`, `SIDE-spinor`) — noted, not acted on.

**Standing laws binding this act.** Canon XV corpus-first · canon XVI `VERIFY-BY` · the **FREE-WRITING LAW**
(conclusions at full strength; status in ledgers) · the **QUESTION-FIT LAW** (ratified 2026-08-11: *a
quantity must be able to fail, and its failure must bear on the question*) · **AUTHORITY IS NOT ACCURACY**
· **PLAUSIBLE-NEIGHBOUR DRIFT** · the **STRUCTURAL-IDENTIFICATION CLAUSE** (name-identity discipline) ·
the **POINTER-LENGTH CHECK** (venue and page count before any at-content path).

**Open, and untouched by this act.** `W-ORD-T5-EPSTEIN-COUNT` (exact per-stratum constants) ·
`W-ORD-EPSTEIN-OFFLINE-DENSITY` (off-line count constants, the `c(a,b) → 0` rate) — both filed 2026-07-31,
both bearing on lattice row 6. ### **Neither is repeated here.** Also open, author-call triggers:
`W-ORD-DELTA-N4-H2`, `W-ORD-CODIM-INSTRUMENT`, `W-ORD-FQ-PHASE-SCREEN`, `W-ORD-CODIM-ARITY`,
`W-ORD-KAPPA-IDENTITY-FORM`.

**Ruled this session, before act 2 ran:** the `S4` amendment (§1 below) and `VACUOUS` (§2 below).

---

## §1 — THE `S4` AMENDMENT, APPLIED

**Executed into `phase2/method/THE_EULER_SPECIFICATION.md` as §9**, with the sealed wording
**struck-visible and dated**, per the corpus's bracket-don't-delete discipline.

* **`S4` now reads MULTIPLICATIVITY** (`a_{mn} = a_m a_n` for `(m,n) = 1`); ~~*totally multiplicative*~~ struck, 2026-08-12 seal date carried.
* **Δ recorded as the distinguishing witness**, evidence = the corpus's own banked stream: `τ(2)² = 576 ≠ −1472 = τ(4)`.
* **Four on-line objects recorded as failing the struck reading:** Ramanujan Δ · Hecke `L` · automorphic `L` · Epstein `Z_Q` at `h = 1`.
* **The amendment is CONSERVATIVE at the witness cell** — `Which_Structure_Confines` already said *"not multiplicative"*, never *"not totally multiplicative"*. **The contrapositive never rested on the struck wording.**

### The consequent question, filed AND answered from the specs' own statements

> ### **"ARE `S4` AND `S1` NOW ONE ROW?" — NO IN GENERAL; YES ON EXACTLY ONE FAMILY.**

**The route exposed an ambiguity in `S1` that had to be declared.** `S1`-AMBIENT (FTA in `ℤ`, the sealed
wording) is **constant `✓` across every Dirichlet series over `ℕ`** — zero discriminating power, exactly as
the placement-capacity column already grades it (*"NONE"*). `S1`-OBJECT (unique factorization in the
arithmetic generating the coefficients) **varies, and fails exactly when `h > 1`.**

> ### **ON THE EPSTEIN FAMILY ALONE THEY ARE ONE SWITCH: `h = 1` ⟺ `O_K` has unique factorization ⟺ the
> Euler product exists ⟺ `S4` holds. THAT IS THE WITNESS'S LIMITATION, NAMED: the disc `−23` witness moves
> `S1`-OBJECT AND `S4` TOGETHER and cannot separate them — and no further Epstein data ever will.**
>
> **Δ separates `S4` from total multiplicativity. NOTHING IN THE CORPUS YET SEPARATES `S1`-OBJECT FROM `S4`.**

**Executor-owned disclosure:** act 1 graded row 6 `S1 = ✗` under the object reading while grading rows 7/12
`S1 = ✓` under the ambient reading — **one column, two readings.** Declared, not silently corrected;
splitting `S1` is a specification change and is **author-ruled, proposed not applied.**

---

## §2 — `VACUOUS` RATIFIED

**Filed into the S-table as §10.** The fourth emptiness kind: ### **the spec is not STATEABLE of the
object — the cell is empty because the question has no referent, not for want of a fact.**

**Worked instance:** `S6` is a property *of the Euler product*. Where `S4` fails there is no product, hence
no domain of convergence, hence nothing for `S6` to be true or false about — `Z_Q` `h>1`, Davenport–Heilbronn,
Hurwitz, random Dirichlet. **Recording those four as `ABSENCE` would have put four phantom work-orders on
the board.**

---

## §3 — ACT 2: THE BEURLING CORNER

### 3.1 — Name-identity guard, discharged first

> ### **`Beurling generalized primes` ≠ `Beurling–Nyman`. THE MERGE DID NOT HAPPEN AND IS RECORDED AS NOT
> HAVING HAPPENED.** *The corpus holds only the latter — the BN distance, its unconditional ceiling
> (Báez-Duarte–Balazard–Landreau–Saias; improved Burnol), the E-7b Gram work. That is a functional-analytic
> RH equivalent over `ℚ`. The object graded below shares a surname and nothing else.*

### 3.2 — The literature, at cite

**Retrieval blocks recorded** (three): Springer `10.1007/s00208-005-0638-2` and `10.1007/s00208-006-0051-5`
both **303 → auth-wall**; Deep Blue `2027.42/46253` **403**; `arXiv:2207.00665` PDF did not parse, recovered
via `ar5iv`. **The at-content path ran on open full text only.**

| # | fact | grade |
|:--|:--|:--|
| **B1** | ### **The Euler product exists.** *"`ζ𝒫(s) = ∑_{j=1}^∞ 1/n_j^s = ∏_{j=1}^∞ 1/(1−p_j^{−s})`, `Re s > 1`"* | ### **AT-CONTENT** — `arXiv:2409.10051` §1 |
| **B2** | ### **Analytic continuation exists, and it is PARTIAL.** *"the analytic continuation of `ζ𝒫(s) − A/(s−1)` to the half-plane **`Re s > θ`**"*, guaranteed by well-behavedness `N𝒫(x) = Ax + O(x^θ)` | ### **AT-CONTENT** — same |
| **B3** | ### **NO FUNCTIONAL EQUATION.** *"our proof … **does not require any (approximate) functional equation** and actually applies to quite a broad class of general Dirichlet series"*; no FE is claimed anywhere in either source | ### **AT-CONTENT, and by absence across two independent papers** |
| **B4** | ### **`σ = (1+θ)/2` IS NOT A CRITICAL LINE.** It is the optimal abscissa for zero-density estimates; *"Unlike the Riemann zeta function with its `σ = 1/2` critical line, Beurling systems have variable structure depending on `θ`"* | ### **AT-CONTENT** — `arXiv:2409.10051` |
| **B5** | ### **PROVEN ZEROS, AND THEIR LOCATION.** Diamond–Montgomery–Vorhauer construct a system with `N_B(x) = κx + O(x^θ)`, `1/2 < θ < 1`, whose zeta has **infinitely many zeros on the curve `σ = 1 − a/log t`, `t ≥ 2`, with NO zeros to the right of it**, `a > (4/e)(1−θ)` | ### **LIT-CITED** — *Beurling primes with large oscillation*, **Math. Ann. 334 (2006) 1–36** |
| **B6** | ### **RH CAN FAIL, AND THE EULER PRODUCT BUYS EXACTLY THE CLASSICAL EDGE.** *"They basically showed that under Axiom A **the Riemann hypothesis may still fail**; moreover, **nothing better than the most classical zero-free region** and error term of (9)–(10) follows from (2) at least if `θ > 1/2`"* | ### **AT-CONTENT** — `arXiv:2207.00665` (via `ar5iv`) |

### 3.3 — The grading, and the row placed

| | S1 | S2 | S3 | S4 | S5 | S6 | PLACEMENT |
|:--|:-:|:-:|:-:|:-:|:-:|:-:|:--|
| **Beurling system (DMV instance)** | ✓ | ### **✗** | ### **SPLIT** | ✓ | ✓ | ✓ | ### **zeros PROVEN at `σ = 1 − a/log t` — and NO LINE TO BE OFF** |

**`S1` `✓`** — the quoted Euler identity **is** the statement of unique factorization; the product equals the
sum exactly when the generalized integers run over the freely generated semigroup. *Read from `B1`, not
inferred around it.*
**`S2` `✗`** — the generalized primes are arbitrary reals `> 1` tending to infinity. **There is no `ℚ`, no
Ostrowski roster, no `p`-adic place.** Definitional.
**`S3` SPLIT** — ### **continuation `✓` (to `Re s > θ` only, `B2`), functional equation `✗` (`B3`).** *The
row that act 1 never had to split, split by the first object that separates its halves.*
**`S4` `✓`** (`B1`) · **`S5` `✓`** — addresses `log p_j`, continuously variable: **this is exactly the
deformation the corner was chartered for** · **`S6` `✓`** and *worse than `ζ`'s* — the product converges
`Re s > 1` and the continuation reaches only `Re s > θ`, so the expression gap is wider, not narrower.

### 3.4 — The registered expectations: **THIRD OUTCOME**

**(E1) as registered — CONFIRMED on three clauses, REFUTED on two.**
Systems with `S1`/`S4` present: ### **YES** (`B1`). Analytic continuation: ### **YES, partial** (`B2`).
Proven zeros off `σ = 1/2`: ### **YES** (`B5`).
### **But the cost is NOT `S2` alone — `S3`'s functional equation is gone too (`B3`) — and therefore the
object DOES NOT POPULATE THE EMPTY CELL. It is not the positive control.**

> ### **"OFF-LINE" PRESUPPOSES A LINE, AND THE LINE IS `S3`'s. Without a functional equation there is no
> symmetry axis, so there is no proposition "the zeros are off it" for the witness to make true.** *DMV's
> zeros are located against `σ = 1` — a zero-free-region statement — not against a critical line.*

**(E2) as registered — DID NOT FIRE.** These systems **keep** `S4` and they **have** continuation. The
branch's antecedent is false.

> ### **THIRD OUTCOME, FILED FIRST-CLASS: `E2`'s CONCLUSION HOLDS THROUGH `E1`'s ANTECEDENT. The empty cell's
> emptiness STRENGTHENS — and the reason is one neither branch registered.**

### 3.5 — The convergence, and it is the act's finding

**Two rows of the lattice, built for different reasons, reach one conclusion:**

| row | `S3` status | consequence |
|:--|:--|:--|
| **`E_k`** (act 1 §3.4) | absent entirely | ### **zeros: NONE AT ALL** |
| **Beurling (DMV)** | continuation without FE | ### **zeros: MANY — but no line to be off** |

> ### **`S4` BINDS ADDRESSES. `S3` MANUFACTURES THE LINE THEY ARE PLACED AGAINST. TWO INDEPENDENT WITNESSES,
> ONE CONCLUSION — and the second was found by an act chartered to look for something else.**

**AND THE SECOND CONVERGENCE, WHICH THE CORPUS OWNS HALF OF.** `B6` says the Euler product, isolated from
`ℚ`'s places, yields **the classical zero-free region and nothing better** — the `σ = 1` edge. The corpus's
own positivity side already said the same thing from a route sharing no machinery:

> **`PATHS` §ANNEX B, boundary B3, quoted:** *"`Λ(n) ≥ 0` yields the **`σ = 1` edge**, never the
> critical-line centre."*

> ### **TWO INDEPENDENT ROUTES — a positivity boundary proved inside the corpus, and an extremal
> construction proved outside it — LAND ON THE SAME BOUNDARY: THE MULTIPLICATIVE STRUCTURE ALONE BUYS THE
> EDGE, NOT THE CENTRE.**
>
> ### **THIS CONSTRAINS AND DECIDES NOTHING. It is a limitation statement about what `S4` can reach without
> `S2` and `S3`; it supplies no sign, proposes no mechanism, and the stop is not triggered.**

---

## §4 — THE PRICE, STATED WITH THE RESULT

### 4.1 — Why the registered price was too low — **and the corpus could have said so first**

The charter priced the Beurling witness at *"the cost of `S2`."* ### **`S2`'s failure is not a separable
charge. It takes `S3` with it, and the S-table says why in its own `S3` row:**

> **`S3`, as sealed:** *"`ξ` built **adelically over that roster**; the product formula `∏_v |x|_v = 1`
> builds the integration domain"*

> ### **THE FUNCTIONAL EQUATION IS MANUFACTURED FROM THE PLACE ROSTER. DELETE THE ROSTER AND THE MECHANISM
> THAT PRODUCES THE FE IS DELETED WITH IT. `S2 ⟹ S3` IN THE CORPUS'S OWN CONSTRUCTION, SO NO WITNESS CAN
> EVER PAY IN `S2` ALONE.**

**Honest note, executor-owned:** *canon XV would have caught this before the fetch. Act 1 read `S3`'s row
and did not read it as a **dependency**. The literature confirmed what the sealed table already implied.*

### 4.2 — The transfer question: **FILED, NOT ANSWERED**

> ### **WHAT CAN A BEURLING WITNESS TELL US ABOUT `ℚ`?**

| | |
|:--|:--|
| ### **IT CAN** | ### **kill a SUFFICIENCY claim.** *Multiplicativity plus continuation, standing alone, does not confine zeros to any line — `B5`/`B6` make that evidential rather than assumed. The carrier localization must therefore be read **necessary-not-sufficient**, and now has a witness for the "not sufficient" half.* |
| ### **IT CANNOT** | ### **bear on the actual carrier question.** *That question is what `S4` does **in the presence of `S2` and `S3`**. The witness is constructed by removing exactly those, so it is silent on the joint regime — which is the only regime `ℚ` occupies.* |
| ### **THE OPEN CELL** | ### **The transfer is one-directional and its strength is UNMEASURED.** *No route is known from a Beurling statement to a `ℚ` statement, and none is proposed here.* **Filed. Not answered.** |

> ### **AND THE COROLLARY THAT CLOSES ACT 1's §3.1 RATHER THAN OPENING IT: the empty cell has no positive
> control, and the corner chartered as its only candidate has been graded and does not fill it.** *Over `ℚ`
> the cell is empty modulo RH; off `ℚ` the object stops being about a line at all. **Both exits are closed,
> and that is a stronger statement of the untestability than act 1 could make.***

---

## CLOSING

**Banked** at `relay/reports/2026-08-13-witness-lattice-act2-beurling.md`.
**Applied this act:** `THE_EULER_SPECIFICATION.md` §9 (`S4` amendment, struck-visible) and §10 (`VACUOUS`
ratified). **No other corpus file touched. No keystone, no REGISTRY row, no SPIRAL row, no FINDINGS entry.**
**Pins read live, not recalled** (42 `SIDE-*` repos; the divergence reading per REGISTRY's own frozen/live rule).
**Mirror:** this report is the mirror surface.

**Returning for the author's word:**
1. ### **The `S1` split** (`S1`-AMBIENT vs `S1`-OBJECT) — proposed, not applied; act 1's table used both readings in one column and that is declared.
2. ### **`S3` must split into CONTINUATION and FUNCTIONAL EQUATION** — the Beurling row separates the halves and the sealed row cannot hold them apart.
3. ### **The Beurling grading itself**, and the third-outcome ruling on `E1`/`E2`.
4. ### **The transfer question**, filed unanswered, with its strength unmeasured.

**`h2` UNCHANGED. NO MECHANISM PROPOSED. NO SIGN. NOTHING DEPOSITS.**

---

### Sources consulted at content

- [arXiv:2409.10051 — *On zero-density estimates for Beurling zeta functions*](https://arxiv.org/html/2409.10051)
- [arXiv:2207.00665 — *The method of Pintz for the Ingham question … in the Beurling context*](https://ar5iv.labs.arxiv.org/html/2207.00665)
- [Diamond, Montgomery & Vorhauer, *Beurling primes with large oscillation*, Math. Ann. 334 (2006) 1–36](https://link.springer.com/article/10.1007/s00208-005-0638-2) — **auth-walled; cited from the two open sources above**
- [*Beurling primes with RH and Beurling primes with large oscillation*, Math. Ann.](https://link.springer.com/article/10.1007/s00208-006-0051-5) — **auth-walled; NOT read; named only**
