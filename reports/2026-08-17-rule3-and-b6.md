# RULE 3 · CORRECTION TWENTY-FOUR CLOSES · CHECKPOINT `B6` — THE ARCHIMEDEAN TRACE LIFTED
## ### **`h` COPIES OF THE CERTIFIED FORM, ONE NAMED GAP — AND THE IDENTIFICATION PROBE LANDS EXACT**

**Relay report · 2026-08-17 (tenth sitting) · author-ruled · `INSTRUMENT` track · sub-gate declared · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 0f07e308a90ac5e01f3b1c8525a7ff90cadb6376   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 0f07e30   ### 22-file roster + MANIFEST, 3 probes PASS
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> ### **RULE-3 LOG (the rule's first outing, minted this sitting):**
> **identities:** `Ẑ = γ_∞·Z` — banked, spec `§6(a)` · `τ`-form Hermitian under the antipode-involution —
> banked, `§2.2`/`§6(c)` · **CC compression linear in the test function — `NAVIGATOR-ASSERTED`** *(the
> Selecta citation is unreadable at `BIBLIOGRAPHY` `§4-bis`'s own grade; corpus-first anchor: the banked
> `Tr(ϑ(g)PP̂Pϑ(g)*)` and blockage 7's "the sandwiched form is a norm")*.
> **banned-list grep on the ferry: clean; no new term coined.**

---

## §1 — RULE 3, MINTED *(`protocols/EXECUTOR_RULES`, precedent: corrections twenty-three and twenty-four)*

*A ferry stating a mathematical identity is executed AFTER the identity is checked against the banked
record and cite — or marked `NAVIGATOR-ASSERTED` and checked first-thing — never before; a ferry coining a
term greps the banned list; both logged one line at the report head.* **Filed after Rule 2, monotonic
order preserved.**

## §2 — CORRECTION TWENTY-FOUR CLOSES *(the three residual stems, content-rephrase, author-supplied)*

| was | now |
|:--|:--|
| *"a pairing blind to them whatever the form is"* (the compiled gloss + quotes) | ### **"a pairing that carries no warrant about them, whatever the form is"** — identifier untouched |
| *"blind to the one variable the witness shows placement tracking"* (`§6` + charter + two relay echoes) | ### **"the frame has taken the object, not its presentation — invariant in the one variable…"** |
| *"blind to (the) coefficients"* (charter ×2 + filing ×2) | ### **"silent on the coefficients"** |

**A full-corpus grep now returns only the correction's own record. The table is closed.** *And the
placeholder-title sweep closes with it: the calibration title ruled LAWFUL at grade (`F.2026-08-17h`); the
remaining candidates stay on your list.*

---

## §3 — CHECKPOINT `B6` *(the spec `§7`, `v0.4`; both branches longhand)*

### **(a) THE LIFT.** *`ℂ[Cl]`-valued test functions are `h` scalar copies (Fourier, longhand). With the
`NAVIGATOR-ASSERTED` linearity premise and the banked class-silence of `γ_∞` (`B5(a)`), the certified
archimedean structure extends componentwise — nothing `ψ`-dependent has to be built at `∞`:*

> ### **`Tr_τ(g) = (1/h)·Σ_ψ Tr(ϑ(g_ψ)·S·ϑ(g_ψ)*)` — `h` COPIES OF THE CORPUS'S OWN CERTIFIED ARCHIMEDEAN
> FORM, EACH STRUCTURALLY NON-NEGATIVE (A NORM), CLASS-SEPARATING THROUGH `τ`.** *The `1/h` normalization
> is flagged against the ferry's formula (a constant; recorded per Rule 3).* ### **POSITIVITY GUARD, ON THE
> FACE: archimedean-only positivity is structural and bears NOT AT ALL on `h2` or `W_∞ − W_2`.** *The
> loom's concordance row stands unmoved.*

### **(b) THE GAP, ONE SENTENCE, NOW ON THE SPEC'S FACE:** ### **with the archimedean trace lifted, column
`(ii)`'s remaining absence is the FINITE-PLACE trace — one Sonin-type positive local trace per prime ideal
`𝔭` — the object CC's semilocal strategy names (`{∞, 2, …, p}`, `2310.18423` at content) and no one has
built.**

### **(c) THE IDENTIFICATION PROBE — REGISTERED, GATED, AND IT LANDED EXACT**

**Instruments:** `tools/e16/b6_class_probe.py` *(registration banked BEFORE any number:
`data/b6_registration_2026-08-17.txt`)* + `b6_mu_repair.py`; runs at `data/b6_2026-08-17.txt`,
`data/b6_mu_repair_2026-08-17.txt`. **Cell:** `K = ℚ(√−23)`, `L = 3.5`, live norms `2, 3` (both split,
both non-principal, longhand), `ω`-triple, `V` imposed, `A_main` unchanged (declared minimal lift under
`B5(a)`'s class-silence), **gate passed 3/3 + basis cross-check, bipartiteness audited at every `ω`.**

> ### **THE BLOCKING DISCIPLINE FIRED ON THE EXECUTOR'S OWN INSTRUMENT FIRST:** *the unfloored pure counts
> gave `ψ₀ + ψ₁ > dim` on exactly proportional matrices — impossible — so the lag-only kernel was being
> split by floating noise.* **The registration lacked the corpus's own floor-sweep discipline
> (`F.2026-08-17e`); the pure column is WITHDRAWN, the defect is the executor's, and the repair is
> banked.** *At the floored reading:*
>
> | `ω` | `μ` (exact, Hopcroft–Karp) | floored pure `ψ₀`/`ψ₁` | FULL `ψ₀`/`ψ₁` | \|full − μ\| |
> |:--|--:|:--|:--|:--|
> | `2·10⁻³` | **279** | 279 / 279 | **279 / 279** | ### **0 / 0** |
> | `10⁻³` | **560** | 560 / 560 | **560 / 560** | ### **0 / 0** |
> | `5·10⁻⁴` | **1120** | 1120 / 1120 | **1120 / 1120** | ### **0 / 0** |
>
> ### **`P1` HOLDS (components equal, = `μ` — the derived law confirmed on the class-labelled lift), AND
> `P2` LANDS ON BRANCH A SHARPER THAN REGISTERED: |full − μ| = 0, NOT ≤ 1 — per component, at every `ω`,
> under BOTH coefficient conventions, including the `ψ₁` stress case (globally negative lag terms).**
> *The full count's component-independence at this cell is noted as one cell's observation.* **BENCH-GRADE,
> ONE CELL, PROVED NOWHERE.**

### **(d) `B1` AGAINST THE LIFTED OBJECT:** (i) **filled at class resolution** · (ii-∞) **filled at (a)'s
grade** *(the linearity read-debt named)* · the involution **filled natively** (`B5(b)`) · ### **(ii-finite)
ABSENT — the place column's only absence** · (iii) **absent** — (c) is its first bench cell · ### **(iv)
RESTATED: the joint move is DOWNSTREAM of (ii-finite) — it cannot even be ASKED of the lifted object until
the finite-place trace exists. The column's whole weight rests on one named absence.**

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **RULE 3 IS MINTED AND WAS APPLIED THE SAME SITTING** — the Selecta linearity premise carries
   `NAVIGATOR-ASSERTED` on its face, with the corpus-first anchor named.
2. ### **CORRECTION TWENTY-FOUR IS CLOSED** — the residual table repaired with your phrasings; the
   full-corpus grep is clean; the placeholder-title sweep is closed.
3. ### **`B6` IS GRADED AND ITS PROBE RAN: the archimedean trace lifts componentwise (h certified copies,
   class-separating through `τ`); the gap is one sentence (the finite-place trace, per prime ideal, named
   and unbuilt); and the identification mechanism survived the class lift EXACTLY — |full − μ| = 0 at the
   first cell, both components, both conventions** — *with the executor's own unfloored pure column
   withdrawn in-pass by the blocking discipline, which is the discipline working.*
4. **Road B's standing sentence after `B6`:** *the lifted object has the address data, the completion, the
   involution, and now the archimedean trace; it lacks the finite-place trace and the identification —
   and `(iv)` waits on the first of those two.*

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
