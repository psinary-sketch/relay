# `W-ORD-JOIN` — THE NO-GO READ — 2026-08-10

**Registered at `060ee01` BEFORE the fetch. Source: C. Deninger, *There is no "Weil-"cohomology
theory with real coefficients for arithmetic curves*, `arXiv:2204.02714`, read at content.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — E1: THE THEOREM AND THE AXIOMS

> **Theorem 2.13.** *"For a number field `k` there is **no functorial real valued cohomology theory**
> `H^•(spec o_k, ℝ)` with endomorphism `Θ_ℝ` such that … `Θ := Θ_ℝ ⊗ id` satisfies property 2.12."*

**Axioms, verbatim:** `Θ` a derivation on cup-product · **`H⁰ = ℂ`, `Θ = 0`** · **`H¹` INFINITE
DIMENSIONAL, `Θ`-eigenvalues = the non-trivial ZEROES of `ζ_k`** · **`H²` with a `Θ`-equivariant
TRACE isomorphism, `Θ = id`.** Property 2.12 is stated **at `s = 1/2`**.

## §2 — E2: THE MECHANISM, AND THE OBSTRUCTION'S SITE

> *"if `E` is **supersingular**, then `(End E)_ℚ` is a **quaternion algebra** … **nonsplit precisely
> at `p` and `∞`**. Thus `(End E)_ℝ` would be the **Hamilton quaternions `ℍ`** whereas
> `End H¹(E, ℝ) = M₂(ℝ)`, and **there is no anti-homomorphism `ℍ → M₂(ℝ)`**."*

> ### **THE OBSTRUCTION SITS AT THE REAL PLACE** — one of the two ramified places. All three
> registered candidates name one thing: **an involution-type map fails because `ℝ` is ramified.**

**Against the corpus's anatomy: KIN, NOT SAME.** The genus paradox is `End(J)` **trivial**; this is
`End(E)_ℝ` **too big** for its target. **Both `End`-side at `∞`; neither is the other.** `Δ1` and the
two-Weil split: **DISTINCT**.

## §3 — E3: THE ESCAPES ARE IN HIS OWN AXIOMS

**ℂ-coefficients** (the list is ℂ-valued) and **infinite-dimensionality** (**axiom 2.3 requires
it**). **The join's forced shape — infinite-dimensional, ℂ-linear — now has a PUBLISHED support
beside the compiled ones.**

## §4 — THE UNREGISTERED FINDING, AND IT IS THE SHARPEST

> *"Complex leafwise cohomology **always has the real structure** given by the cohomology of the real
> forms. **Since such a real structure cannot exist on the cohomologies `H¹(spec o_k, ℂ)`** as we
> [showed]…"*

> ### **DENINGER'S NO-GO CUTS AGAINST DENINGER'S OWN FOLIATED ROUTE.**
> **The programme's own author has published the obstruction to the programme's own realization as
> stated** — *stronger than Borger's "further ideas", because it is a **theorem** rather than an
> inability to imagine.*

## §5 — THE SPECTRAL ACCOUNT: HALF RESOLVES AT CITE

**`Θ = 0` on `H⁰` · `Θ = id` on `H²` — integer values, a two-step ladder — and the ZEROS on `H¹`.**

> ### **THE ASSIGNMENT IS SOURCED.** **The "CANNOT" is NOT** — that a compact circle's integer ladder
> *cannot* carry `H¹` is **this register's inference, not Deninger's statement.** **The assignment is
> his; the impossibility is mine, and it is labelled.**

## §6 — THE PAGE RANGE, CLOSED

**`245–261`** (publisher + two records + CC's bibliography); **EUDML's `262` is the outlier.**
The image-read item is re-filed **`AUTHOR-SIDE-OR-FUTURE-TOOLING`**, both blocked routes named:
**GDZ image-only, no OCR layer; no arXiv route for a 1991 Inventiones paper.**

## §7 — THE REGISTER AT CLOSE

both legs held · object identified · **`Q1` at target grade** with the **four-column screen**, the
**spectral account**, and **the no-go's constraint (ℂ-coefficients and infinite dimension FORCED, at
cite)** · chain **`Q1` → `Q2` → `Q3` = `h2`, outside every arrow** · **fracture square and
`HH(ℚ/ℚ)` still named-unsourced — carried, not dropped.**

**E4:** the bibliography scan returned **nothing the register lacks that it could use** — the
Deninger-line references are his own earlier articles, already carried as declared-unread.
**Reported rather than padded.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `060ee01` (registration) → `8b1110e` |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE — untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing constructed. Positivity, `C₂` and the sign not approached. Nothing deposits.**
