# `W-ORD-Q0` OPENS — FIRST SOURCE ACT — 2026-08-10

**Charter registered at `401ee98` BEFORE the source act. Rail `de621b1` / `2147a03` unmoved.
Nothing deposits.**

**Path discipline, restated per the charter:** **`Q0` → `Q1` → the clause. A trace theorem realizes
the terms and supplies NO positivity. The keyhole conditions and `C₂`'s definiteness stand OUTSIDE
this register.**

---

## §1 — THE SOURCE, LOCATED BY PROPER SEARCH

**arXiv API on `abs:"twistor" AND abs:"Fargues"`** → Woit, *Notes on the Twistor `P¹`*,
**`arXiv:2202.02657`**. **No identifier guessed — the Dubrovin lesson held.**

**Object confirmed, author's own words:** *"**The twistor `P¹` is the analog of the Fargues–Fontaine
curve at the "infinite prime" (the archimedian place of the number field `ℚ`)**."*

## §2 — THE ONE QUESTION: **VERDICT ABSENT**

`zeta` **0** · `trace` **0** · `Frobenius` **0** · `determinant` **0** · `explicit formula` **0** ·
`Deninger` **0**. **An analogy of SHAPE — no zeta, no trace, no extraction of any archimedean term.**

**And the author says so, twice:** *"the significance of which **remains unclear**"* · *"**It may very
well be that the appearance of the twistor `P¹` in physics and in number theory is of no fundamental
significance**, but it also seems quite possible that it is pointing towards some insight … yet to
be found."*

> ### **THE ARCHIMEDEAN OBJECT EXISTS AND CARRIES NO TRACE CHANNEL.** The axis map's last
> declared-unread edge closes at **ABSENT** — **on the author's own assessment, not on my failure to
> find one.**

**`Q0` IS NOT ADVANCED.** The falsification design asks for **an invariant with an output**; this
source supplies **an object and an open question**. **`Q0`'s state is unchanged; one edge is closed
rather than unread.**

## §3 — FOUND-BEYOND: **TWO FALSE ZEROS IN ONE PASS, BOTH MINE**

1. A term-count reported `archimedean` **0** while the text reads **"archimedian"** — spelling variant.
2. **Worse:** the corrective re-probe used `grep -oiE "archimedi\|archimede"` — and **in ERE `\|` is a
   LITERAL PIPE, not alternation.** The fix searched for a string that cannot occur and **returned 0
   again, appearing to confirm the error.**

> ### **A WRONG PROBE THAT AGREES WITH A PREVIOUS WRONG PROBE READS AS CORROBORATION.** The corpus
> already carries the false-zero-probe class; **this is its sharpest instance, because the second
> probe was written specifically to check the first.** **Both were caught by READING, not by probing.**

**Correct count: 1.** *The substantive verdict was never affected — `zeta`, `trace`, `Frobenius` are
single words with no alternation.*

## §4 — WAVE ROUTING *(routing record only — NO DEPOSIT ACT)*

**`THE_CARRIER_SPECIFICATION` v0.2 → Phase-2 wave; REGISTRY row `DRAFT-WAVE-BOUND`.**
**Sequencing law restated where it lands:** provisionals settle before any wave opens · deposits stay
parked at the posture clause · the methodology/patent day precedes publishing. **The draft's status
remains the author's ruling.**

## §5 — THE BRAID AT ROTATION

**construction arc BANKED**, successor register **`W-ORD-Q0` OPEN** (one edge closed) · **strand 2
closed** · **strand 3 discharged-in-corpus / parked-at-deposit** · registered-not-run:
`W-ORD-SLOPE`, `W-ORD-ENCODING-RESIDUE` · stopping rule holds: `finPart`, the graph field, the level
refinements · **Phase 1.1 list parked** · **patent day April 2027** · **TECHNE reconciliation gated as
sequenced** · **`h2` exactly where it was.**

> **Nothing is decaying, and every open item sits under a register, a rule, or a ruling.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `401ee98` (charter) → `27243c3` |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE — untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
