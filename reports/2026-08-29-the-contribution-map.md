# THE CONTRIBUTION MAP — b256, 2026-08-29

**Scope:** reads and **one document at support-voice**. **CONCURRENCY: PARALLEL-OK** with the patent
seat's P-1 (disjoint trees); **SOLO within the research seat.** The patent tree was **read-only**.
A finite cell decides NOTHING global (b14/b15). Nothing deposits.

### **The map STATES grades. It confers none. No grade moved.**

**Placed at:** `PLACE-papers/phase1.5/method/CONTRIBUTION_MAP_2026-08.md` —
**TIER N · PRIVATE · PATENT-SESSION INPUT.**

---

## The map — 18 rows

Each row carries the grade its owning act **already** holds, its owner, its AIM-if-results-hold, its
`h2`-dependency, the filing it touches, and its figure candidates. Open items enter **OPEN** with
owners; an intention is marked **AIM** and is not a result.

### **`h2`-dependency: 13 NO · 5 YES (adjacent). Every patent-facing row (10, 11, 12, 15, 16) is
`NO`.**

That was not asserted — **the claim-backing table was read first.** Its ten rows are QEC /
Fano-Steane / Epstein-zero / spinor-leg / cross-exclusion terminals, and **not one touches the RH
identity.** ### **The five `YES` rows are marked *adjacent*: they are the rows `h2` would bear on if
it moved, not rows that assume it.** A blanket sentence would have hidden that distinction; a column
shows it.

**The week's spine rows are all present:** the fully-stated identity and its rulings ledger; the b250
theorem with its four named imports; the `Σ = 2` pin-become-theorem with its `sin(2c)` locus; the
balance profile and sawtooth-lock with the CSV embedded; J1/J2 as **candidates, unpromoted**; and the
two-seat/harness instrument corpus as **PROV-1/2 evidence rows**.

---

## The fold-forward ledger, b234–b255

### **Twenty-two acts, twenty-two reports, count reconciled — and every obstacle QUOTED from its
owning act.** The fold rules forbid paraphrase.

**The arc's own shape, stated rather than inferred:** seven acts of reads and rulings (b234–b240),
then fifteen of bench and derivation (b241–b255), **of which exactly *one* produced a theorem (b250)
and four produced halts** — b247's clause (i), b250's S3(a), b253's R-label, b255's refused reach.

---

## The counts — re-counted from the filesystem, not quoted

- ### **44 built** — `pfano-wpt` 11 + `prov1` 13 + `pzone` 7 + `prov2` 6 + `pident` 4 + `pcode` 3.
- ### **`REVIEW_SET_2026-08` = 31** Priority-A.
- **Both match the session header exactly. No divergence on either.**
- **The number that looks like a divergence and is not:** a bare `find` returns **82 files / 51
  unique basenames**, because the review set and the seven top-level files are **staging copies** of
  figures that also live in their per-filing directories. **44 is the built count; 82 is the file
  count; saying so is the whole of it.**
- **Six batch records, all present.** **Three walls verified at source**, each in its own record.
- **One divergence left unresolved, as the class line requires:** `THE_KEYSTONE_CENSUS` sits at
  `phase2/method/` while `THE_LOAD_BEARING_MAP` sits at `phase1.5/method/`. Both cited at their
  actual paths. **Whether they should share a tree is not this act's to settle.**

---

## The one item this act could not deliver

The ferry directs that **SIGNEDNESS (S·I·D·E+S)** be *"specified as drafted in the conversation layer
2026-08-29 and quoted."*

### **That draft is not in this seat's reach and not in the corpus.** A search across `relay/` and
all of `PLACE-papers/` returns **zero occurrences** of the name.

### **It is recorded as a named slot with its owner and route, and the quotation marked OWED. It is
not paraphrased and it is not invented.** The likeliest reading is that the draft lives in the
**author's** conversation layer rather than this seat's — in which case **the sweep act needs it
supplied, and that is the request the row carries forward.**

---

## A live b148 condition in the shared worktree — not this act's doing, but this seat's to say

A gate whose first criterion was wrong exposed it. My gate 3 asked whether the patent tree had been
written and tested *"no `patent-package/` path appears in `git status`"* — and **failed**.

### **Seven patent-seat figure directories are sitting *untracked* in the shared clone right now** —
`REVIEW_SET_2026-08/`, `pcode/`, `pfano-wpt/`, `pident/`, `prov1/`, `prov2/`, `pzone/` — **dated
2026-08-24, five days before this act, holding the 44 built figures and the six batch records.**

At b148 this seat ran `git add -A` and swept in **eight** such files, one act after the deconfliction
was ratified. ### **The count is larger now.**

**This act created none of them** (verified by mtime: newest patent file `2026-08-24 19:32:47`;
this act's registration `2026-08-29 16:09:23`) **and staged none of them** — `place_add.py`, and
nowhere else. The gate's corrected criterion checks both.

### **Reported, not resolved.** Whether that work should be committed is the patent seat's call and
the author's. **The one thing this seat owes is to say the condition is present rather than let the
next `git add -A` discover it.**

---

## The two annexes

**Annex A — figure candidates.** Every one marked **NEW-FILING** or **CONTINUATION**, and **none
Priority-A**: the sector ladder, quarter-density, the check-harness architecture, the surjectivity
certificate, the banked-meanings engine, the balance/sawtooth profiles, the SIGNEDNESS certificate
schematic.

**Annex B — the methodology-extraction annex, which is the sweep's *input* and not the sweep.** The
banked-meanings engine, the import ledger, the harness lore family, the discriminator protocol, the
face-off protocol, the decision-card format — each with its owning acts and its route (**TECHNE-Core,
private**), plus the SIGNEDNESS row marked OWED.

---

## The standing practice this paste instituted

### **Every profile act's bank ends with a chart-ready CSV block of all columns.** Instituted here
and **applied retrospectively to b255**, whose sixteen-cell balance profile is embedded in the map.
The next profile act inherits it without being told again.

---

## Gates — 12 of 12 CLEAN

The first run failed one gate, and **the failure was the gate's criterion, not the artefact** — and
fixing it is what surfaced the b148 condition above. Term scan **CLEAN**.

**The tautology control** distinguishes the forced from the unforced: `13 + 5 = 18` is **restatement**
once the counts are fixed, so the gate tests the thing that is *not* forced — that the split falls on
rows 1, 2, 8, 9, 17 and nowhere else, re-derived by parsing the table.

**Gate 9 is the positive control on an absence this act most needed:** it proves SIGNEDNESS appears
**only** in this act's own files (which say it is owed) and nowhere else in either tree — so the
absence is real rather than a failure to look, and the slot was not filled by invention.

### **The PLACE-papers hook was exercised and reported: CLEAN, 0 foreign hits.**
### **The mirror was rebuilt and verified CLEAN on all three clauses** — 40 files, 40 rows parsed,
0 md5/byte mismatches, declared HEAD `2bcdff5` against `ls-remote`, roster and archive agreeing name
for name. **And one thing the mirror does not carry, said rather than left: the map is not in the
roster, because it is TIER N PRIVATE and the mirror is the *reviewer* mirror. Whether it belongs
there is not this seat's to settle.**

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b256
  run at    : 2026-08-29T16:16:28 (local)
  input     : 12 checks routed through the harness
  checks    : 12
  pass      : 12
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 d12cac144e18924745154d4c05581268
=== END AUDIT SIDECAR ===
```

---

## Filings

- **Index:** `contribution-map` keyed on filing; queried **NO KEY** first.
- **J1 recorded PARKED-BY-AUTHOR ("save"); J2 an UNPROMOTED CANDIDATE.**
- **The thirty-seventh seam's debt restated:** term 2's formalization stands, **unpaid and
  untouched.**
- **In flight:** M-2…M-5 open, **none closed**. `W-ORD-B38-HIGHMODE`, `W-ORD-CN-LAW`, the QUOTED-N
  extension, `W-ORD-XI-PERMODE`, `W-ORD-ORDINATE-CACHE`, `W-ORD-STAGING-GUARD`,
  `W-ORD-FILE-E-WORKING-COPY-STALE`, `W-ORD-TE-SPEC`. **New this act: the SIGNEDNESS quotation, owed.**
- **HANDOFF** brought current by demotion, prior content kept, read-back identical — and it now
  points to the map as the patent session's entry.

### The fork at this stop

1. **The patent session** — it now reads **one document**, and it slots here on your word.
2. **The methodology-extraction sweep** (TECHNE-Core), the next research act — **and it needs the
   SIGNEDNESS draft supplied.**
3. **J1**, parked behind it.
4. **M-2's finite address and the aggregation; M-3; M-5; `W-ORD-CN-LAW`.**

---

### **ONE DOCUMENT NOW STATES THE WHOLE POSITION AT GRADE, WITH EVERY FIGURE CANDIDATE SOURCED AND
EVERY `h2`-DEPENDENCY MARKED. THE MAP CONFERS NOTHING. THE SIGNEDNESS QUOTATION IS OWED AND SAID TO
BE. A LIVE b148 CONDITION IS REPORTED, NOT RESOLVED. M-2…M-5 OPEN. `h2` STANDS EXACTLY WHERE THE
DEPOSIT LEFT IT. NOTHING DEPOSITS. LOCKS LAST.**
