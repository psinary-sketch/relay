# Order of work, three items, one session — consolidated report — 2026-07-31

Sequence executed as ruled: (1) Epstein family probe → (2) constellation-backlog pass → (3)
uniquely-available conclusions register. The probe spec was NOT reconstructed from memory — the session
STOPped and the author re-pasted it; execution began on receipt. Per-item commits below, all pushed,
local = remote verified at each step.

## Item 1 — Epstein family probe (own report: `reports/2026-07-31-epstein-family-probe.md`, relay `1dbf056`)

**The registered prediction survived**: the clause is DISCRETE at the multiplicative place — present
exactly at h = 1 / Euler-product presence, failing identically at every non-pure point of the
Bombieri–Hejhal linear-combination family, no intermediate stratum. The precision worth the probe: the
gradation that exists (off-line count constants, first-zero heights) lives entirely in the density
register — the paper's own density/placement dichotomy realized inside the family. The T5/conductor
coordinate is orthogonal to the switch (leading rate (T/π)log T for every stratum; |D| second-order;
h invisible). h2's content is binary at the multiplicative place; the wall is not approachable along
the family. Work-orders: `W-ORD-EPSTEIN-OFFLINE-DENSITY` (new, trigger at author call),
`W-ORD-T5-EPSTEIN-COUNT` confirmed and fed. h2-arc OPEN_TRAILS addendum: PLACE-papers `274001a`.

## Item 2 — W-ORD-CONSTELLATION-BACKLOG pass (relay tools `9b6987b`; PLACE-papers `8993cf8`)

**Checker refinement first** (rowgen constellation, report-only scope unchanged): flags now classify
ACTIONABLE / PROVENANCE (rename-record "was/formerly/moved-from" markers) / EXTERNAL (off-corpus
references: SIDE-* repos, kernel, relay, D:\ paths, download-layer, sibling repos, generated export
artifacts) / HISTORICAL (dated ledger entries: history-class or dated headings, date-token paragraph
starts, date-first table rows; live REGISTRY id-rows exempt). Dated ledger entries report separately
and never flag actionable, per the ruling.

**Before / after:**

| file | as filed (flat) | post-refinement (pre-fix) | post-fix |
|:--|:--|:--|:--|
| REGISTRY.md | 40 + 2 provenance = 42 | 29 actionable · 2 provenance · 10 historical | **0 actionable** · 3 provenance · 4 external · 8 historical |
| SPIRAL_MAP.md | 6 | 2 actionable · 1 provenance · 3 historical | **0 actionable** · 1 provenance · 3 external · 1 historical |

**Target met: REGISTRY + SPIRAL_MAP at 0 actionable.** Fixes (all verified against current file
headers, not the checker's claim alone): 22 stale version cells (d1-2…d1-7 → v2.3/v3.3; 1.5a-3/1.5a-4
→ v0.6.0; 1.5b-6 → v1.1.2; 1.5c-1 → v1.0.2; 1.5c-2 → v1.5; 1.5c-13 → v0.2.2; 1.5g-4 → v0.2.1 per the
file header; 1.5h-4 → v0.5.4; p2-7, p2-4a → v1.1; p2-9 → v0.2.1; p2-31 → v0.2; p2-18 → v1.1.2; p2-29
→ v0.1.2; 1.5a-6 → v0.4; archival 1.5b-1/1.5b-4 version cells → em-dash, deferring to 1.5b-6).
**One genuine reconciliation surfaced and repaired**: the live p2-27 row still cited
`TYPE_D_EXCLUSION.md` v0.1 — a file deleted at `4924040` when the paper was retitled to *The
Additive–Multiplicative Conspiracy*; the dated Row-update documenting the retitle had never been folded
into the live row. Folded now (v0.2.3, REVIEW, provenance annotated). Also: p2-d8's pair-paper note
corrected ("awaiting download" → landed as `QEC_KAPPA_MAGIC_FINDINGS_v0_1.md` at `4a6b76f`);
MANIFEST.md de-backticked in the mirror-procedure line (generated export artifact); SPIRAL_MAP's
TYPE-letter bullet updated to the completed demotion (no pending-language against READY rows). The two
provenance rows stand with **no content change** — the checker now classifies them via their existing
"was" markers. Known documented phantom (1.5d-1 `GRH.md`, never on disk, documented in the dated note)
rides as historical, not silently dropped.

## Item 3 — UNIQUELY-AVAILABLE CONCLUSIONS register (PLACE-papers `cddaff0`)

New structured OPEN_TRAILS section (not a new document; append-only): one entry per conclusion only
this programme's coordinates can state, four lines each (programme coordinates / SPIRAL_MAP §Translation
mirror / concrete cross-field ramifications / completing research as a named work-order with trigger).
Eight seeds, each citing its relay report:

| entry | conclusion | completing work-order (trigger) |
|:--|:--|:--|
| UAC-1 | Δn₄ = +1: the missing H² as a formation distance | `W-ORD-DELTA-N4-H2` (NEW; author call) |
| UAC-2 | codim-≥2 instrument vs codim-1 wall | `W-ORD-CODIM-INSTRUMENT` (NEW; author call) |
| UAC-3 | SIDE as a theory of derivation structure | FACE-E Tier 2 (existing; trigger attached) |
| UAC-4 | six compiled negatives, one shape | the queued Negatives flagship (author go) |
| UAC-5 | T5 conductor coordinate (fresh from R1; feeds the probe) | T5-EPSTEIN-COUNT + EPSTEIN-OFFLINE-DENSITY (existing) |
| UAC-6 | two-witness/derivability form, assumes nothing of ξ | FACE-E Tier 2 (cross-link) |
| UAC-7 | 𝔽_q phase-brightness, falsifiable | `W-ORD-FQ-PHASE-SCREEN` (NEW; author call; failure = first-class finding) |
| UAC-8 | silence as parameter-on-identity (1^s = 1); vacuum analogy rejected on record | `W-ORD-KAPPA-IDENTITY-FORM` (NEW; author call) |

## Pins

- PLACE-papers: `274001a` (probe addendum) → `8993cf8` (backlog pass) → `cddaff0` (UAC register), all
  pushed, local = remote at each. Mirror rebuilt at `cddaff0` (`mirror-refresh-2026-07-31.zip`, 20
  entries, ready for Project upload).
- relay: `1dbf056` (probe report) + `9b6987b` (rowgen classification) + this report.
- SIDE-kernel unchanged: `44895f9`, v1.7 = `2957e7d` (the paper's citation pin) unmoved. No rail edits
  anywhere in the session; no deposits.
