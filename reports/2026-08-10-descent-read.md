# THE FOURTH ACT — v0.4.0, THE TWO WEILS, THE DESCENT READ — 2026-08-10

**Expectations registered at `e8daf79` BEFORE any read. Grading and reading only; construction
refused. Spec tag `v0.4.0` = `5a7574e`, peel-verified. Rail `de621b1` / `2147a03` unmoved.
Nothing deposits.**

---

## §1 — `v0.4.0`, THE LEVEL DISCIPLINE

> **EVERY GRADED STRUCTURE DECLARES ITS LEVEL, AND CROSS-LEVEL INHERITANCE DOES NOT CARRY.**

`Descent` = a realization plus the equation `pairC a b = formF (realize a) (realize b)`.
**`descent_not_unique`** — two descents of the *same* form, *different* class pairings, so
**"the form descends" is a choice, not a property of the form.** **`descent_inherits_blindness`**
(axiom-free) — a realization that cannot separate two classes yields a pairing blind to them
**whatever the form is**.

**W-2:** the field demands a descent be **declared**, never that it be **good**.
**Four sharpenings; the same degenerate witness passes every one. Twenty-seven terminals**, README
reconciled against `AxiomCheck` both directions.

## §2 — THE TWO WEILS

| | **1948** | **1952** |
|:--|:--|:--|
| paired object | **divisor classes on `C × C`** | **test functions** |
| **level** | **class object** | **space over it** |
| over `𝔽_q` / over `ℚ` | proved / **no object** | proved / **possessed since 1952** |

> ### **THEY COINCIDE OVER FUNCTION FIELDS BECAUSE THE DESCENT EXISTS — the square is its home. Over `ℚ` the 1952 criterion is STRANDED for want of it.**
>
> **`Δ2`'s full content is THE DESCENT. The form is not missing; the floor under it is.**

**The register's sharpest firing** — the coincidence is **real and proved** in one setting, so the
error is inheriting an agreement whose cause is absent. *An identification earned somewhere is far
harder to catch than one never earned at all.*

## §3 — THE DESCENT READ

**E1 diagonal · E2 Frobenius GRAPH · E3 correspondence↔endomorphism↔divisor class — all CONFIRMED
at cite**, with the Mattuck–Tate correction attached (the `H¹` reading is *later*; the descent's
original home is **divisors**). **E4 degree/projection maps — NOT SOURCED THIS PASS**: classical
and standard, **declared, not asserted.**

**All four fail on both junction objects.**

> ### **THE SHARPENING COSTS THE SIDE THAT WAS WINNING: `W(ℤ)` HAS THE FROBENIUS OPERATORS AND NO SQUARE FOR THEIR GRAPHS TO LIVE IN.**
>
> E2 asks not for an endomorphism but for its **GRAPH as a class in a square**. **`Δ4` is
> NECESSARY AND NOT SUFFICIENT: the operators must be GEOMETRIC, not merely algebraic.**

> ### **THE JOB DESCRIPTION: A SQUARE WITH A DIVISOR THEORY, IN WHICH BOTH THE DIAGONAL AND THE FROBENIUS GRAPH ARE CLASSES, WITH DEGREE MAPS TO NUMBERS — i.e. `Spec ℤ ×_{𝔽₁} Spec ℤ`.**

**A convergence from a THIRD direction:** the survey asked *what programmes lack*; the delta, *what
fields fail*; the descent read, **what the 1948 proof consumes**. **Three routes, one object — and
no route to building it.** *Three derivations of a requirement are evidence the requirement is
right, and no evidence it can be met.*

## §4 — THE DENINGER RIDER: PARTIALLY CLOSED

**The first fetch returned the WRONG PAPER** — `math/9807034` is Dubrovin — **corrected by an
arXiv API lookup rather than by guessing again.** Source read: `arXiv:0709.2801`.

**QUOTED:** *"no cohomology theory has yet been developed … **However until now the conjectured
cohomology has not been constructed**"* · the flow side (*"reduced leafwise cohomology"*, *"dense
leaf … `spec ℤ` itself"*, *"infinite dimensional"*) · and the descent-shaped tie: *"comparing the
**'explicit formulas' … to a transversal index theorem**."*

> **THE POLARIZATION HALF IS NOT IN THIS SOURCE** — `polariz` 0, `duality` 0, `pairing` 0,
> `Poincaré` 0, `self-adjoint` 0; the paper carrying it by title was **not fetched**.
> **THIRD ROW RE-GRADED: QUOTED ON THE FLOW SIDE · UNQUOTED ON THE POLARIZATION SIDE.**
> **"Deninger postulates the join" is HALF-VERIFIED — and that is what it is called.**

## §5 — FOUND-BEYOND

1. **Deninger's own sentence supplies the descent's shape** — reading the explicit formula as a
   **transversal index theorem** is reading the function-level form **as a trace**, which *is* a
   descent. Not registered as something to look for.
2. **`Δ4` necessary-not-sufficient is another MISSING KIND of field**, kin to the level field:
   `Δ4` grades operators, **E2 grades their graphs**. **Left unrepaired under the stopping rule.**

## §6 — STOP-AND-HOLD

**Nothing constructed.** A compiled discipline, a two-level filing, a requirement list, a
half-closed omission. **Every requirement fails on both objects, and the object they jointly
describe is the one the field has been unable to build since 1952. `h2` is exactly where it was.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `e8daf79` (registration) → `13bee3b` |
| relay | this report's commit |
| **`SIDE-carrier-spec`** | **tag `v0.4.0` = `5a7574e`**, `refs/tags/v0.4.0^{}` peel-verified = local; PRIVATE |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
