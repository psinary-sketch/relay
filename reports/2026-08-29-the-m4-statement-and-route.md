# THE M-4 STATEMENT AND ROUTE — b247, 2026-08-29

**Scope:** a statement-read and route verification. ### **No derivation was performed and no step
was filled by plausibility.** The E0 gate governs the statement; the import bar governs every
external fact; the double-name species is the named hazard. A finite-place-set object at a finite
cutoff decides NOTHING global (b14/b15). Nothing deposits.

---

## The act in one paragraph

### **The statement is writable from owners except at one place, and that place is the theorem.**
Clause (i) requires `λ(n) → 0` **at a stated rate**, and **no owner in this corpus states one.** The
bench cannot supply it either — `λ(n)²` reaches the float64 floor at `n = 7`, so the measured points
past it are arithmetic noise and cannot constrain a rate. ### **The statement halts at clause (i)'s
rate, and the missing sentence is named.**

**A-1 is (SAME OBJECT); A-2 is (DOUBLE-NAME), settled by a discriminator registered before the
number was seen; IMP-3 is REFUSED.** And one finding the act did not go looking for: ### **clause
(ii)'s first disjunct is FALSE on the certified range.**

---

## 1. The statement, under the E0 gate

Every constituent of `t(n) = λ(n)²·ξ_n(1)²/(1 − λ(n)²)` unfolded to its owner:

- **Which operator's spectrum `λ(n)` is** — `prolate_layer.py` lines 9–17: *"the time-and-band
  limiting operator on [−1,1], with the corpus's fixed c = 2π, `(Qf)(x) = ∫₋₁¹ sin(c(x−y))/(π(x−y))
  f(y) dy`, whose eigenvalues are λ(0) > λ(1) > … in (0,1)"* (Slepian–Pollak 1961).
- **On which space, with which parameter fixed** — `L²[−1,1]`, and ### **`c = 2π` is FIXED**, not
  asymptotic: *"sitting-4 correction: Landau–Widom inapplicable — c = 2π is FIXED"*.
- **Which normalization fixes `ξ_n(1)`** — pins P1–P3: `λ(n) = (−1)ⁿ√(μ₂ₙ)` (even index only);
  `ξ_n = √2·ψ₂ₙ` (half-line norm); and the endpoint ### **not extrapolated** — obtained from the
  eigenfunction equation `ξ_n(1) = (1/λ_n)∫K(1,y)ξ_n(y)dy`.

**Lemma F.1, and exactly where it stops:** it certifies that the **eleven-term truncation is uniform
to 1e−11**. ### **It says nothing about the tail beyond eleven, and nothing about a rate. It is a
truncation certificate, not a convergence theorem.**

### The halt, and the missing sentence

> *"For the time-and-band limiting operator on [−1,1] at **fixed** c = 2π, the concentration
> eigenvalues μ_k satisfy μ_k ≤ F(k) for an explicit F with Σ F(2k)·G(k) < ∞, where G dominates
> ξ_k(1)²."*

### **No owner supplies it. The statement halts here, and this act does not complete it from what
was probably meant.**

### And clause (ii)'s first disjunct is false

`ξ_n(1)` over the certified range: 0.026180, 0.609479, 2.413226, 3.526144, 4.099362, 4.571835,
**4.994344**, then 1.938584, 0.136443, 0.972198, 0.883329 — the collapse sitting exactly where
`λ(n)²` hits the float floor, i.e. noise.

### **It grows monotonically through `n = 6`, so `ξ_n(1)²` runs from 6.854e−04 to 24.94 — a factor
of about 36,000 across seven modes.** So `ξ_n(1)²` is **not bounded** there, and **only the "growth
dominated" form of (ii) is live.** The theorem must be stated the harder way.

Why it still converges on that range: `t(n)` runs 1.197e+01, 8.776e+00, 2.205e+00, 4.340e−02,
1.255e−04, 1.217e−07, 5.168e−11 — the `λ²` decay (≈3 orders/mode) **outruns** the `ξ²` growth
(<1 order/mode). ### **That is an observation over seven points. It is not a rate, not a bound, and
not a theorem.**

---

## 2. The assets — definition against definition

### **A-1 — act 15's derived pair geometry: (SAME OBJECT).**
§28(a) R2: *"the E+B block inverses carry `1/(1−λ_n²)`: the ε-series' denominators ARE the λ-scale
Sonin geometry — derived."* Both sides unfold to the **same index set (even-prolate), the same
eigenvalue (`λ_n² = μ₂ₙ`), the same expression.** ### **But an identification is not an estimate** —
it gives the denominator's origin, not a bound, a rate, or a tail.

### **A-2 — is `ξ_n(1)` the arc's `α`? (DOUBLE-NAME).**

| | `ξ_n(1)` | `α` |
|:--|:--|:--|
| domain | `[−1,1]` | `[1, ∞)` |
| endpoint | **right** | **left** |
| normalization | `∫₋₁¹ψ² = 1`, then `×√2` | `∫₁^∞ψ²` finite, **not normalized** — it *is* the Wronskian identity's right side |

**The discriminator, registered before the number was seen:** b212 measured `|α_odd|/(πΛ) = 1.0` at
every odd eigenvalue — **`|α|` is constant in the index**, so if `ξ_n(1)` were the same object it
would be constant too. ### **Measured: `ξ_n(1)` runs 0.026180 to 4.994344, max/min = 190.77**, against
a forced constant of 0.945442 if the hypothesis held.

### **What is *not* claimed:** that no relation exists. A transform relating the exterior and
interior problems would be a RESULT owed. **This act says only that the two endpoint values are not
the same number and are not defined on the same object.**

**A-3** — b242's per-mode measurements stand as the bench control any imported decay must match, with
the float-floor exclusion enforced. **A-4** — F.1 certifies eleven, K1 realizes seven; **which cut
the envelope is stated at is a RULING, not a calculation**, and it is not made here.

---

## 3. The imports — IMP-3 **REFUSED**

The candidate family the corpus has actually seen is the **`c → ∞`** one, quoted by b130 from a
secondary source. ### **Our operator's `c` is FIXED at 2π. An asymptotic in `c` cannot be applied at
a fixed `c`. The hypotheses do not match and the import is refused.**

**Sourcing, stated plainly as the bar requires: no primary was read at content in this act, and none
exists in this repository.** b130's own grade on the one quotation the corpus holds:
*"SECONDARY-VERIFIED … **the primary is not read**"*, the secondary's title itself not established.
The precedent that governs: ### **"A citation is not a licence; applicability is a separate read."**

### **And the honest qualifier, registered in advance: a fixed-`c` decay theorem is a *different*
theorem from the `c → ∞` one, and refusing the second does not refute the first.** The fixed-`c`
family was **not reached** — no statement of it has been located in this corpus, and this act does
not assert that none exists. b130 filed exactly this as NAVIGATOR-OPEN and **it stays open.**

---

## 4. The route, priced

| step | species | doubt |
|:--|:--|:--|
| **S1** `λ(n) ∈ (0,1)`, strictly decreasing | **IMP owed** (classical; the primary is *not read* here) | LOW |
| **S2** a decay rate at **fixed `c = 2π`** | **IMP owed OR RESULT owed** | ### **HIGH — the binding step** |
| **S3** bound / dominated growth for `ξ_n(1)²` | **RESULT owed** | HIGH |
| **S4** assemble into convergence + envelope | **RESULT owed** | LOW, conditional on S2, S3 |
| **S5** the cut the envelope is stated at | **RULING owed** | — |

### **S2 is the route's binding step and nothing in the corpus supplies it today.**

**On S3, what this act can say without deriving anything:** the endpoint construction is
`ξ_n(1) = (1/λ_n)∫K(1,y)ξ_n(y)dy`, so the obvious estimate is **inverse in `λ_n`** — feeding it into
`t(n) = λ²ξ²/(1−λ²)` gives a bound that **does not decay at all.** ### **The naive route closes
nothing, and S3 needs a sharper input.** *That is an observation about difficulty read off the
endpoint formula — not a derivation and not a step.*

**The derivation act is specified and not run**, filed for the author's confirmation: components,
gates (bench control with float-floor exclusion, tautology control, E0 re-run), grade target
### **DERIVES-ON-IMPORTS at content with a Core shadow where finite-decidable — and not higher**,
and halt conditions fixed in advance, including ### **HALT if `ξ_n(1)` is identified with the arc's
`α` anywhere in it.**

---

## Expectations, judged

**A-1 (SAME OBJECT)** — borne out. **A-2 (DOUBLE-NAME), "and I expect the difference to be the
domain"** — borne out; the discriminator settled it 190.77 to 1. **IMP-3 refusal** — borne out.

### **The statement: partly WRONG, and the correction is this act's own.** I registered that it
would be writable whole *except for (iii)'s envelope*. ### **The halt is not at (iii) but at (i)'s
rate** — (iii) is a consequence; without a rate there is nothing to compute an envelope *from*.
**I named the wrong clause and the reading corrected me.**

**And one thing I did not expect at all:** that clause (ii)'s first disjunct would be **false** on
the measured range. It is, by a factor of 36,000 — **and that sharpens the theorem rather than
weakening it.**

---

## Gates

**14 of 14 PASS, CLEAN** — on the third run. Run 1 failed gate 7 on ### **a conjunct that asserted
nothing**: a leftover malformed pattern tested for falsity, which the whitespace-normalized matcher
resolved to True. **The gate was right to fail — a conjunct that asserts nothing is a conjunct that
can assert anything.** Replaced with the assertion it was meant to make, the correction written into
the gate's own comment.

**Two banned stems were written in this act's own voice and the scan caught both.** Repairing the
registration made it younger than the reads file, which would have broken the
registration-precedes-reads gate. ### **The ordering was restored honestly rather than back-dated:**
the reads tool is deterministic, so it was re-run and the re-run proved to change **zero lines**;
the bank was then written last, where it belongs. **No mtime was touched.**

Gate 4 is a **positive control on an absence** — the forbidden verdict words ("essentially the
same", "closely related", "compatible objects") are shown *findable* in the registration, which
names them in order to forbid them, so their absence from the bank means something. Term scans
**CLEAN**, 0 live over 909 lines. PLACE-papers, the loom and the mirror untouched — hook did not
run, no mirror rebuild required.

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b247
  run at    : 2026-08-29T12:05:02 (local)
  input     : 14 checks routed through the harness
  checks    : 14
  pass      : 14
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 68e88c460ef99e8e34ccd8b2988a3f14
=== END AUDIT SIDECAR ===
```

**Filed:** `m4-statement` and `xi-alpha-question` keyed; ### **`W-ORD-FIXED-C-DECAY`** — locate and
quote, from a real primary, a decay statement for the concentration eigenvalues at **fixed `c`**,
and check its hypotheses against `Q` at `c = 2π`. **Filed, not run.** **b248 named as next: THE
SECOND OBJECT** — the `E2` arrangement read and the junction piece's per-cell split,
**quotation-only**, standing clause at registration. The thirty-seventh seam's debt restated:
four items, the file **open**, the lemma **open**, the toolchain **discharged**, the ledger's
statement **complete at cell level** — unpaid and untouched.

---

### **M-4 NOW HAS A STATEMENT WITH A NAMED HOLE RATHER THAN A NAME ALONE, AND THAT IS THE WHOLE OF
THIS ACT'S ADVANCE. NO DERIVATION WAS PERFORMED. M-2…M-5 STAND OPEN; M-4 IS NOT PAID, NOT PAYABLE
TODAY, AND NOT NEARLY SO. THE DERIVATION ACT AWAITS YOUR WORD, BESIDE THE PATENT SESSION, WHICH
SLOTS AT THIS STOP AND NEEDS NOTHING FROM THIS ACT. `h2` STANDS EXACTLY WHERE THE DEPOSIT LEFT IT.
NOTHING DEPOSITS. LOCKS LAST.**
