# `W-ORD-H1` — CONSTRUCTION ACT 3 — 2026-08-10

**Registered at PLACE-papers `6a425ad` BEFORE any fetch (E1–E4 verbatim).**
**The disclaimed-register authorization is SPENT this act: a module list was computed. Filed at
REALIZATION GRADE, KIN-to-Connes, never placement.**
**NO ordinate comparison in any branch. No positivity, no `C₂`, no sign.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — E1: THE BÖKSTEDT DEBT **CLOSES AT CITE**, third attempt

### The venue premise corrected at source

**The Krause–Nikolaus lectures are NOT on arXiv.** The arXiv API was attempted per the ferry
(author + title terms, no guessed ID) and returned **HTTP 429, twice**. A first probe via `curl`
returned zero results — **and was distrusted, not believed**: a control query on a paper this
register had already fetched *also* returned empty, proving the probe broken rather than the record
absent. *(The filed error class: a wrong probe that agrees with a previous wrong probe reads as
corroboration.)* The document was then taken from **the authors' own page** —
`uni-muenster.de/IVV5WS/WebHop/user/nikolaus/Papers/Lectures.pdf` — **a better source than the one
registered, not a worse one.**

### The result, with the mangling declared

The PDF's text layer encodes glyphs as numeric codes (`050`=`(`, `051`=`)`, `003`=`*`, `000`=`−`,
`012`=`⊗`, `025`=`≥`, `034`=`τ`, `014`=`fi`). **The display DID mangle. The decode is stated rather
than hidden.** Krause–Nikolaus, **Corollary 4.2**:

> ### **`THH_*(ℤ) = ℤ` for `* = 0` · `ℤ/k` for `* = 2k − 1` · `0` else**

> **E1 CONFIRMED — exactly the registered form (`ℤ` in degree 0, `ℤ/i` in degree `2i−1`). The
> dependency declared at act 1 and re-declared at act 2 is CLOSED.**

Also captured at cite: **Theorem 4.1(4)** `THH(ℤ) ≃ Hℤ ⊗ τ_{≥3}ΩS³` · **Theorem 3.13 (Bökstedt)**
`THH_*(𝔽_p) = 𝔽_p[x]` · **Corollary 4.2** for `ℤ/pⁿ` (all except `ℤ/4`) · **Theorem 4.1(1)**
`THH(ℤ/p) ≃ H𝔽_p ⊗ ΩS³`.

## §2 — E2: THE MODULE LIST, COMPUTED

The single cited input — **`THH_*(ℤ)` is `ℤ` in degree 0 and FINITE in every positive degree** —
determines everything below. Each row `DERIVED-AT-CONTENT`; no row `ASSUMED`.

| step | result |
|:--|:--|
| `THH(ℤ)_ℚ` | `ℚ` in degree 0; **`0` in every other degree** — rationalization kills all the torsion |
| `THH(ℤ)^∧_p` | `ℤ_p` in degree 0; `ℤ/p^{v_p(k)}` in degree `2k−1` |
| `∏_p THH(ℤ)^∧_p` | `Ẑ` in degree 0; `∏_p ℤ/p^{v_p(k)} = ℤ/k` in degree `2k−1`, **finite** |
| `(∏_p THH(ℤ)^∧_p)_ℚ` | **`Ẑ ⊗ ℚ = 𝔸_f`** in degree 0; **`0`** elsewhere |
| the map on `π₀` | **`ℚ ↪ 𝔸_f`** — the diagonal, **injective** |

> ### **`H¹-cand = cofib( THH(ℤ)_ℚ → (∏_p THH(ℤ)^∧_p)_ℚ )` has homotopy CONCENTRATED IN DEGREE 0, and `π₀ = 𝔸_f/ℚ`.**
>
> **It is `H(𝔸_f/ℚ)` — Eilenberg–MacLane on the finite adele class group.** Every other homotopy
> group vanishes; `π₁ = ker(ℚ ↪ 𝔸_f) = 0` by the long exact sequence.

**NAME-IDENTITY, ENFORCED.** `𝔸_f/ℚ` is **NOT** Connes's adele class space `𝔸/ℚ`. **It is its finite
part; the archimedean factor is exactly what is absent.** ***`KIN`, explicitly `DISTINCT` as
objects. NO PROMOTION.***

## §3 — THE WEIGHT DATA: **`Θ = 0`**

The `T`-action descends to the cofiber. **On homotopy groups it is trivial** — a homotopy-coherent
action of a connected group acts on `π_*` through `π₀(T) = 0`.

> ### **No nontrivial weight decomposition exists at the level these inputs reach. `Θ` acts as ZERO.**
> **`Θ = 0` is Deninger's axiom for `H⁰`, not `H¹`.** A nontrivial `Θ` would have to come from the
> `S¹`-equivariant refinement (`TC⁻` / `TP`), which these inputs do not reach.
>
> **There is no eigenvalue list, so the forbidden comparison could not arise even in principle.**

## §4 — THE PRE-COMMITTED CLASSES **SPLIT**

| half of the datum | class |
|:--|:--|
| **the module list** (`𝔸_f/ℚ`) | **CLASS (ii) — NON-LADDER STRUCTURE PRESENT.** Realization grade, KIN-to-Connes. |
| **the weight data** (`Θ = 0`) | **CLASS (i) — FAIL-WITH-SHAPE for `H¹`.** The object satisfies the `H⁰` axiom. |

**Dossier row 1 therefore grades FAIL: the nominee is not `H¹`.** Row 3's `ℂ`-linearity question is
**moot at this level** — `𝔸_f/ℚ` is a `ℚ`-algebra with no place at infinity to supply a `ℂ`-structure.

## §5 — E3: THE `∞`-GLUE'S PRIOR ART — **`PARTIAL`**, as registered

**Consani–Marcolli, *Archimedean cohomology revisited*, `arXiv:math/0407480`.** ID **verified at its
own abs page**, not inherited from the ferry. *(The ferry said "Consani's"; the paper is joint with
**Matilde Marcolli**. Attribution corrected, not carried.)*

> *"a **dictionary of analogies** between the geometry of a tubular neighborhood of the **'fiber at
> arithmetic infinity'** of an arithmetic variety and the **complex of nearby cycles** in the
> geometry of a **degeneration over a disk**"*
>
> *"the action of the **Lefschetz**, the **log of monodromy** and the **log of Frobenius** … combine
> to determine a **spectral triple** in the sense of Connes"*
>
> *"The **archimedean part of the Hasse-Weil L-function** appears as a **zeta function of this
> spectral triple**"*

| question | answer |
|:--|:--|
| what plays `Φ` at `∞`? | **the nearby-cycles complex of a degeneration** — but **by the source's own word, an ANALOGY** |
| does her `Θ`/`N` pair match the dossier's division of labour? | ### **PARTIAL.** The **Lefschetz** operator carries the ladder (`sl₂`) — the ladder half matches. **But the assignment differs**: the dossier gives the ladders to **the circle**; here they come from **Lefschetz**. And what the triple delivers is **the archimedean `L`-FACTOR — the leg act 1 already computed as `Γ_ℝ` — NOT the zeros.** |

**THE TWO FORMALISMS.** The recollement's `Φ := i*j*` and geometric nearby cycles `Ψ = i*Rj_*` are
**the same composite**. But CM's archimedean object is, **in the source's own words, a "dictionary of
analogies"**, so:

> ### **`KIN` — and the kinship grade is SUPPLIED BY THE SOURCE ITSELF, not asserted here.**
> `STATED-BY-A-SOURCE`. The merge discipline is satisfied without promotion.

## §6 — E4: TWO FILINGS

- **`VERIFICATION_LOOM.md`** — **A PERMISSION IS NOT AN INPUT**, with the act-2 instance and the
  kinship to `possession-is-not-progress` stated.
- **`internal/TECHNE_ELEMENTS.md`** — a new **INTAKE — UNGRADED, UNNUMBERED** section, logging
  **canonicity-by-own-action (circle-supplies-`ℂ`)**. **No `D.`/`A.`/`P.`/`T.`/`C.`/`Π.` number
  assigned. No TECHNE act performed.** The file sits in `internal/`, outside the 23-file mirror
  roster — **the gating holds by construction, not by intention.**

## §7 — FOUND-BEYOND-REGISTRATION *(two items)*

1. > ### **THREE INDEPENDENT LINES CONVERGED ON THE SAME MISSING FACTOR: THE ARCHIMEDEAN PLACE.**
   > Act 2's design proposed adjoining coefficients at `v = ∞`; act 3's computation returned
   > `𝔸_f/ℚ`, **missing precisely the `ℝ` factor**; the rider's prior art is **entirely about what
   > plays the glue at the infinite place.** *Not registered. The three were commissioned separately
   > and are **not** independent evidence of anything — but the convergence names where this
   > register's next question lives.* **STOP-AND-HOLD OBSERVED: stated at grade, not pursued.
   > `𝔸_f × ℝ = 𝔸` is arithmetic, not a result, and this register does not take the step.**

2. > **THE OUTCOME CLASSES WERE WRITTEN AS EXCLUSIVE AND THE ANSWER SPLIT THEM.** The registration
   > assumed module list and weight data would land together; they did not. **Recorded as a defect
   > in the registration's own design rather than smoothed over in the reporting.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `6a425ad` (registration) → this pass |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE, untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing claimed beyond grade. `Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow.**

> ### **Realization is not placement.**
