# `W-ATTEMPT-2` · SITTING 15 — THE BUILT OBJECT ON THE FOUNDATIONS' MAPS
## ### **THE OBJECT SITS ON THE WEIL-POSITIVITY FAMILY, FIBER-AWARE IN ITS CLASS RESOLUTION, NON-COMPLETING AT THE MODEL BY THE CENSUS'S OWN CRITERION — THE KEYHOLE'S NAMED CONDITIONS GRADED (WITH THE CORPUS'S OWN "NUMBERED LIST NOT STATED" PROBE CARRIED) — `T1`–`T10` RE-GRADED FRESH: THE TABLE GAINS ITS FIRST MEASURED CANDIDATE-ROW, AND THE SUB-GATE STATUS HONESTLY DOES NOT CHANGE**

**Relay report · 2026-08-18 (thirty-fifth sitting) · author-ruled · Fable throughout · nothing deposits**

> ### **RULE-3 LOG:** *PATHS at its census form ✓ (routes 1–3 compiled; route 5 shielded; the Landau
> Shield fiber/landscape criterion) · the keyhole at its banked state — AND THE BANKED PROBE CARRIED,
> NOT SMOOTHED: `OPEN_TRAILS` records that "the keyhole's numbered conditions are not stated in the
> searched corpus" (a prior probe for (iv) returned EMPTY); this sitting grades the three conditions
> the corpus names — (i) Euler-product-essential · (iii) Epstein-separation · (iv)
> supply-at-the-multiplicative-place — and only those · the `T`-table at its sealed wording ✓ · the
> `S`-table at `THE_EULER_SPECIFICATION`'s sealed rows ✓ · the two clauses at their sources
> (`A_Place_to_Stand` step (10): `λ_Z(n) ≥ −λ_A(n)`; the geometric clause the transversality/
> perpendicular family). Terms coined: none.*

---

## (a) — PATHS: WHICH FAMILY, AND COMPLETING OR NOT

> ### **THE OBJECT SITS ON THE WEIL-POSITIVITY ROUTE FAMILY** *(the explicit-formula/positivity line —
> not one of the three compiled canonical routes, not the shielded R-curve family).* ### **BY THE
> CENSUS'S OWN CRITERION (the Landau Shield: landscape-routes cannot complete; fiber-aware routes can),
> THE OBJECT IS FIBER-AWARE IN ONE RESPECT AND NON-COMPLETING AT THE MODEL IN ANOTHER, AND BOTH ARE
> SAID:** *fiber-aware — the τ-ledger is character-by-character (the class resolution carries per-`ψ`
> data, not aggregates; the coupling reweights the `ψ`-components unequally, measured); non-completing
> at the model — the object never reaches the zeros' side at all (no `Z` channel, sitting 16), so the
> landscape/fiber distinction is not even tested at the crossing. The completing-question's address on
> this object is exactly the missing crossing — the same address as the missing codomain.*

## (b) — THE KEYHOLE'S NAMED CONDITIONS, EACH AT THE MODEL WITH ITS SCOPE

| condition (as the corpus names it) | at the model | the scope separating model from clause |
|:--|:--|:--|
| **(i) Euler-product-essential** | ### **MET-AT-MODEL:** the gluing datum consumed IS the class-resolved Euler data (`(4,1,1)` = the label-norm coefficients, exact) | one `K`, finite place sets; class resolution not `ℚ` |
| **(iii) Epstein-separation** | ### **MET-AT-STRUCTURE:** the object is built exactly from the BINDING (`S4`) the Epstein witness lacks — the class-resolved product law does the separating work | the separation is by construction on the model, not a theorem about the families |
| **(iv) supply-at-the-multiplicative-place** | ### **SPLIT, AND THE SPLIT IS NEW:** the object DOES supply a PAIRING at the multiplicative places (what the `E4` survey found Deninger's `ψ_p` do not — they supply an action, not a pairing); what it does NOT supply is the `λ_Z` side — the pairing pairs the section with itself, never the zeros | the `Δ2`-cell demand (supplying the zero channel) stands exactly as before |

## (c) — `T1`–`T10` RE-GRADED FRESH ON THE BUILT OBJECT *(the table's first measured candidate-row; grades AT THE MODEL, said in every cell)*

| row | fresh grade | the cell's content |
|:--|:--|:--|
| `T1` past the prime-silent window | ### **PASS-AT-MODEL** | the diagonal WALKS the boundary the row itself derived ("`p^k` enters at `a = √(p^k)`"): the prime-free cells are exactly CC's window, and every measured punctuation event sits at a `T1` boundary crossing |
| `T2` the (nonArch, unbounded) quadrant | ### **PARTIAL-AT-MODEL — the row's first upgrade from universal FAIL:** the unbounded direction is CONSTRUCTED at one finite place (the tower's inductive Sonin object, pairing stable on the nose, both primes) | the GLOBAL form (all places jointly unbounded — the double limit) remains open; the row every located attempt fails is still not passed |
| `T3` consume the Euler product | **PASS-AT-MODEL** (T7's discriminator; Tier-1 scope kept) | consumption in the sign-relevant sense untested at the clause |
| `T4` sign-type codimension-one silhouette | ### **DIFFERENT-SILHOUETTE, recorded not scored:** the model's shape is (all-positive class) ⊕ (flipped-channel negatives, `d/4`) ⊕ (zero block `d/2`) — NOT `(+1, −1, …, −1)` | whether the Hodge silhouette should appear at the model is itself unknown; the mismatch is data |
| `T5` survive the radical or state it | ### **PASS — the strongest available form: radical ZERO, exactly, at every cell and level** | the model's quotient is trivial; CCM's large radical stands beside it as CONTRAST (`T10` kin unresolved) |
| `T6` Hermitian without a real form at the zeros' level | **PASS-BY-DESIGN** (antipode-Hermitian with the parity twist; the zeros' `H¹` never touched) | the boundary is respected by never reaching it |
| `T7` consume the gluing datum | **PASS** (measured; legs-only pre-refusal did not bite) | at one `K` |
| `T8` thin margin / reach | **SILENT-COMPLIES** — the reach theorem rides; the object makes no claim in `T8`'s regime | — |
| `T9` window-kind | ### **PASS, WITH A CONTRIBUTION: the object's benches re-confirm the row** (hard-vs-soft at `∞`; and the `2T²` identification REFUSED by this row's own law — sitting 16) | — |
| `T10` radical ~ hover kin | the object's radical is ZERO, so on the object the kin-question DISSOLVES (nothing to be kin) | the CCM-vs-hover question itself is untouched |

> ### **DOES THE SUB-GATE STATUS CHANGE? HONESTLY: NO.** *The gate reads "the direct register charters
> when a candidate grades PLAUSIBLE across the table" — at CLAUSE strength. The fresh row is graded AT
> THE MODEL: `T2` is partial (one place, not the quadrant), `T4` is a different silhouette, and every
> PASS carries the model scope. What HAS changed, and is banked as such: the table now contains a
> measured candidate-row where before it had none — the built object enters the candidates table at
> model grade, as a row, not as a pass of the gate. The sub-gate declaration stays true and stays
> declared.*

## (d) — THE `S`-TABLE: EVERY SPEC'S ADDRESS ON THE OBJECT, ARROWS CHECKED

| spec | its address on the built object |
|:--|:--|
| **S1b** (the arithmetic monoid) | the ideal monoid WITH class labels — the labels are the datum `S1b` alone does not carry |
| **S2** (the roster) | the finite place set, with the staircase as its live sub-roster at each `a`; the `K`-level Ostrowski not compiled (the `§12` catch stands) |
| **S3a/S3b** (continuation / FE) | the completion class-silent (`γ_∞` scalar); the FE = the antipode-twisted reflection with unitary `W` — ON the object as structure |
| **S4** (the binding) | ### **THE OBJECT'S WHOLE POINT: the `ℂ[Cl]`-product law restores the binding AT RESOLUTION exactly where the Epstein witness loses it at the component level — and the `S1b ≡ S4` NON-MERGE IS VISIBLE ON THE OBJECT: the monoid (labels) and the binding (the product law) are separate measured data, and the coupling (`T7`) is `S4`-at-resolution doing work `S1b` alone cannot** |
| **S5** (the addresses) | the model's measured content verbatim: orbit length `log p`, weight `q^{−1/2}` exactly (the fifth law); the lag addresses are the tower's own rungs |
| **S6** (domain of validity) | ### **THE DIAGONAL'S WINDOW IS `S6`'s MODEL FORM: the section at bound `a` can speak only of `p^k ≤ a²` (the correlation reach) — the domain-of-expression gap made geometric. The CROSSING (the explicit formula's device) is exactly what the model does NOT have — its absent `Z` channel (sitting 16).** |
| **the arrow `S3b ⟹ S2`** | VISIBLE AT THE MODEL as label-coherence: the involution closes (`W·W^∗ = 1`) only because every place's label is antipode-self-dual — each factor's roster datum is consumed by the FE's closure; measured exact (`c_𝔭^∗ = c_𝔭`) |
| **the arrow structure generally** | the dependency reading holds on the object with no arrow supplying a sign — as the corpus's own filing demands ("NO ARROW SUPPLIES THE SIGN") |

## (e) — THE TWO-CLAUSE VERDICT

> ### **THE OBJECT SPEAKS TO THE ARITHMETIC CLAUSE (`§27.3`-side: the channel structure of
> `λ_Z ≥ −λ_A`) AND NOT TO THE GEOMETRIC CLAUSE (uniform transversality):** *it carries the `A` and
> `P` channels as measured structure (P as a CONSTRAINT — sitting 16) and has no `λ_Z` at all.* ###
> **AND THE ARRIVAL-EDGE STRUCTURE IS NOT A TRANSVERSALITY STATEMENT — measured: negativity enters by
> SECTOR BIRTH (dimension jumps at arrivals; the class's spectrum never crosses zero along `a`;
> off-class dims jump discretely), not by eigenvalue crossings. Birth, not crossing — the geometric
> clause is untouched, and both branches are on the record.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. THE SUB-GATE STANDS DECLARED. NOTHING DEPOSITS.**
