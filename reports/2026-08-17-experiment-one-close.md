# EXPERIMENT ONE — ### **CLOSE.** THE TWO-BRANCH LAW LANDS, THE DEBT IS RE-SCOPED, THE INSTRUMENT IS DOCUMENTED

**Relay report · 2026-08-17 · author-called · author-ruled to land · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 330dc7f7d1b04418e55d89acdab1551892ed48e5   VERIFIED (ls-remote)
                            = correction eighteen + F.2026-08-17 + Euler spec §14
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 330dc7f  ### 22 / 22 COHERENT, three ways
SIDE-window   612d223 (v0.2.0)   SIDE-effects  afa9ccf   — untouched by this pass
```

> ### **EXPERIMENT ONE IS CLOSED.** *It was chartered to measure a two-prime interaction. It measured a
> one-prime law with a second branch, and the route to that finding was its own control refuting its own
> attribution.*

---

## §1 — THE DESIGN FLAW, AND IT WAS NAMED IN THE PRE-REGISTRATION RATHER THAN AFTER

**Printed and committed at `relay` `06199aa`, before any measured number existed:**

> *The rooms are nested initial segments of one window —* `room(ℓ) = 1 − ℓ/log L`*, decreasing in* `ℓ` *— and
> the smallest active lag is always* `log 2`*. The union of nested sets is the largest of them. Therefore*
> ### **`E1-UNION(L) = 1 − log2/logL`, WHICH IS IDENTICAL TO THE ONE-PRIME LAW AT EVERY `L` IN THE SWEEP.**

### **SO `E1` COULD NOT BE CONFIRMED AND COULD NOT BE REFUTED. IT PREDICTED NOTHING THE ONE-PRIME LAW DID NOT
ALREADY PREDICT, AT ANY `L`, EVER — AND A UNION OF NESTED ROOMS NEVER COULD.**

**Why the timing is the whole of it.** *Discovered after the run, "the generalization generalizes nothing" is
a rationalization of a null. Registered before it, it is a* ### **property of the design, stated while it
could still have been changed** *— and it converted the sitting's discriminating content from `E1` to `E2`
before a single eigenvalue was computed.*

> ### **THE 2026-08-14 SITTING HAD ALREADY FOUND THIS AND FILED IT** *(`FINDINGS` `F.2026-08-14b`, reading 1:
> "a 'generalization' that predicts nothing new, ever")* **when it could not run the measurement at all.**
> *The pre-registration did not rediscover it; it carried it forward and made it binding.* ### **The
> superseding pointer is now on that entry.**

---

## §2 — THE ROUTE TO THE FINDING WAS A SELF-REFUTATION, AND IT IS RECORDED AS ONE

**Three registered diagnostic acts followed the sweep's `ω`-unstable rows. The third was a mechanism control,
and it destroyed the hypothesis the first two had been built to support.**

| `L = 4.2`, `ω = 1×10⁻³` | count | fraction | vs `E1 = 0.516999` |
|:--|--:|--:|--:|
| `log 2`, `log 3`, `2 log 2` | `693/1434` | `0.483264` | `−0.033735` |
| `log 2`, `log 3` | `692/1434` | `0.482566` | `−0.034433` |
| ### **`log 2` ONLY** | ### **`693/1434`** | ### **`0.483264`** | ### **`−0.033735`** |

> ### **IDENTICAL TO THE OFFENDER WITH THE SINGLE LAG. THE EXTRA PRIME POWERS DID NOT CAUSE THE DEPARTURE
> THE EXPERIMENT HAD BEEN BUILT TO ATTRIBUTE TO THEM.**

**What the control exposed was a definition, not a number:**

> ### **`E2` HAD BEEN DEFINED AGAINST A PREDICTION WHERE IT HAD TO BE DEFINED AGAINST A MEASUREMENT.** *The
> baseline — "the one-prime law holds at these `L`" — was inherited from a table whose largest `L` was `4.0`,
> and it was inherited* ### **across the exact point where the law turns.**
>
> ### **A BASELINE THAT IS ASSUMED IS NOT A BASELINE.** *Every difference statistic silently certifies its
> own control, and a control that is a formula rather than a measurement certifies nothing.* **Kin, standing:
> *validated at a point is not validated over a range* — here the range was extended by assumption rather
> than by re-running the known-answer row at the far end.**

### **AND THE HONEST ACCOUNTING OF THE PATH: THE HYPOTHESIS THE FIRST TWO ACTS TESTED WAS RIGHT ABOUT ITS OWN
OBJECT AND WRONG ABOUT WHAT IT EXPLAINED.** *The `ω`-split is real, reproducible on six new `ω` classified in
advance (`P1` 3/3, `P2` 3/3, `P3` 6/6), and it explains `0.017` of a `0.034` departure at `L = 4.2` and none
of it at `L = 3.2`.* **A confirmed hypothesis that accounts for half of one row is not the finding, and
saying so required the control that no prediction asked for.**

---

## §3 — THE `ω`-SPLIT, CONFINED — AND THE IDENTITY THAT MAKES IT UNDIAGNOSABLE BY GRID REFINEMENT

**Six lag sets at `L = 4.2`, two `ω`, registered before the profile's numbers were read** *(`relay` `9ced43c`)*:

| lag set | `ω = 1e-3` *(`k₄` even)* | `ω = 1.2e-3` *(`k₄` odd)* | `\|split\|` | |
|:--|--:|--:|--:|:--|
| `{log2}` | `0.483264` | `0.483682` | `0.000418` | no split |
| `{log2, log3}` | `0.482566` | `0.482845` | `0.000279` | no split |
| ### **`{log2, 2log2}`** | ### **`0.482566`** | ### **`0.499582`** | ### **`0.017015`** | ### **SPLIT** |
| `{log3, 2log2}` | `0.235007` | `0.235146` | `0.000139` | no split |
| `{2log2}` alone | `0.035565` | `0.035983` | `0.000418` | no split |
| all three | `0.483264` | `0.500418` | `0.017155` | ### **SPLIT** |

> ### **THE SPLIT REQUIRES THE `log2`–`log4` PAIR AND NOTHING ELSE.** *Not `log 4` alone; not `log 3` with
> `log 4`.* **It is the grid's rendering of the relation `log 4 = 2 log 2`.**

### **AND THE LIMIT OF THE `ω`-SWEEP DESIGN, PROVED RATHER THAN MEASURED**

> **For any real `x`, `|round(2x) − 2·round(x)| ≤ 1`, and `2·round(x)` is even. So `k₄ ≠ 2k₂` forces
> `k₄ = 2k₂ ± 1`, hence `k₄` ODD; and `k₄ = 2k₂` forces `k₄` EVEN.**
>
> ### **`k₄ = 2k₂` ⟺ `k₄` IS EVEN. IDENTICALLY, AT EVERY `ω`.**
>
> **The two readings — "the grid preserves `log 4 = 2 log 2`" and "the log-4 address is even" — are ONE
> CONDITION, not two hypotheses.** ### **NO `ω` SWEEP OF ANY LENGTH COULD HAVE SEPARATED THEM. It would have
> "confirmed" both, forever, at every refinement.** *The separation required varying the LAG SET, not the
> grid.*

### **THE LESSON, EXECUTOR-CAUGHT AND AGAINST MY OWN DESIGN: REGISTERING A MECHANISM DOES NOT MAKE A DESIGN
ABLE TO TEST IT.** *The profile registered its mechanism verbatim in advance, exactly as the standing law
requires, and would still have reported a mechanism it had no power to distinguish from another.* **The check
that caught it — *can these two readings disagree on any input this design can produce?* — is a separate act,
and it belongs beside the falsifiability audit rather than inside it.** *Kin: the question-fit law's own
lesson, that a falsifiability audit catches the forced verdict and misses the disconnected one.*

---

## §4 — BOTH BRANCHES, AND THEIR FATE

> ### **THE LAW: negative fraction of `Φ_L` on `V` = `min( 1 − log2/logL , log2/logL )` — the smaller of the
> room and the lag. In counts, `min( k₂ , M − k₂ )`, to within one grid point, at every `L` and every `ω`
> tested.**

| | fate |
|:--|:--|
| ### **THE ROOM BRANCH** `1 − log2/logL` | ### **NOT WITHDRAWN. RETAINED AS THE LAW'S `L ≤ 4` BRANCH**, exact where the corpus measured it. *The five banked rows (`2.2 … 4.0`) stand at their measured values. Sitting 1's support-geometry derivation of `δ = log L − log 2`, eleven sittings before any measurement, is* ### **right about this branch.** |
| ### **THE LAG BRANCH** `log2/logL` | ### **NEW, MEASURED, BANKED.** *Six `L` × three `ω`, one lag, on `V`; residuals `≤ 2.6×10⁻⁴` against a `1/dim` scale of `3×10⁻⁴`.* **Past `L = 4` the offender count stops depending on `L` at all** — `1386` at `L = 4.2`, `4.6` and `5.5` while the dimension grows from `2870` to `3409`. |
| ### **THE APEX** | ### **`½`, AT EXACTLY `L = 4`**, where lag equals room. *Sitting 12's stated reason for its `L = 4` number — "`log 4 = 2 log 2` makes the lag exactly half the window" —* ### **is correct and names the turning point.** **And correction 17's arithmetic strike is unchanged: the instrument counts `692`, not `693`.** *An exactness may be structurally located and arithmetically false at once; the record keeps both.* |
| **`E2-INTERACTION`, correctly defined** | ### **EXACTLY ZERO offenders at `L = 3.2`** *(three grids, three zeros)*, `+1` at `L = 3.6`, large only above `L = 4`. *A room that is `5.5%` of the window and contributes nothing at all is not a small effect; it is no effect.* |
| **the coefficient convention** | ### **UNADJUDICABLE ON THIS BENCH, AND SAID PLAINLY.** *Changing every weight by up to `2.5×` moves the count by at most two offenders in `1,945`.* **A quantity insensitive to a parameter cannot measure it.** |

---

## §5 — WHAT LANDED THIS PASS

### **(1) THE `δ/L` ENTRY, RESTATED** — `PLACE-papers` `330dc7f`

* **`THE_ATTEMPT_RECORD` §1 result 7** — the two-branch statement; grade ~~`RECONSTRUCTED`~~ →
  ### **`MEASURED-AT-BANK`, and the bank is NAMED** *(`relay` `ac4189f`, the six modules listed)*. **The
  banked table retained and labelled as the `L ≤ 4` branch;** the lag-branch table added; the old reading
  *"`Φ ≤ 0` fails exactly where the prime term has room to act, and nowhere else"* struck visibly and
  restated. **Correction 17's strike stands beside the apex reason restored — both said, neither erasing the
  other.**
* **Three meaning lines at question grade, joins flagged `OURS`:** the failure region is bounded by `½` of
  any window · the bench reads lag ADDRESSES and is silent on coefficients · the coefficient question is
  unadjudicable here.
* **`FINDINGS` `F.2026-08-17`** — the entry, with a superseding pointer added to `F.2026-08-14b`.
* **`THE_EULER_SPECIFICATION` §14** — `S5`'s two halves measured apart. ### **NO CELL RE-GRADED AND NO ROW
  ADDED**, *because the splits this table has taken were each forced by a **population** witness grading
  differently in the two halves, and one instrument's insensitivity is not that.* **Filed as a
  cross-reference precisely because it does not meet the table's own bar for a split.**
* **`CORRECTION EIGHTEEN`** filed with its mechanism and its attribution *(executor-caught by a control run
  against the executor's own design; landing author-ruled)*. **Heading re-counted by script against the file:
  ### 13 table rows + 5 sections = EIGHTEEN.** *Re-run at every correction that lands, which is the only
  form of that law which does not decay.*

### **(2) THE `(1,3]` DEBT, RE-SCOPED AND PROMOTED**

*It was a footnote on the table — "`L = 3.5` and `L = 4.0` lie beyond the range".* **Now a named row in
`THE_ATTEMPT_RECORD` §6 with what it must reach, its re-stated trigger, what it does NOT gate, and its cost.**

> ### **WHAT IT MUST NOW REACH: the region in which the branch turn happens. The turn is at `L = 4`, i.e.
> `ρ = 4 > 3` — outside the range, and outside even the extended range the two largest banked rows already
> sat beyond.** *Every `L` in the 2026-08-17 table lies beyond `(1,3]`; `L = 7.0` by more than a factor of two.*
>
> ### **TRIGGER: any claim about the apex or about the lag branch, ABOVE BENCH GRADE, requires it.** *Bench
> reporting of measured numbers on a named grid from a banked instrument does not.* **What is gated is every
> sentence of the form "the law is `min(δ, log2)/logL`" said about the OPERATOR rather than the discretization.**
>
> **The honest shape of the promotion:** ### **the debt did not grow because more work was done. It grew
> because the work landed entirely inside the unre-derived region, and the most interesting feature of the
> law now sits at its far edge.**

### **(3) THE THREE READMEs — THE UNLOSEABLE-INSTRUMENT STANDARD, NOW MET BY THE WHOLE STACK**

| file | carries |
|:--|:--|
| ### **`README_qeps.md`** | eq. (100) / (85) / `ζ_n` / `ξ^an` verbatim · the scaling convention `θ(a)f(x) = a^{1/2}f(x/a)` **FORCED by the supplied support law, not chosen** · `NTERM = 11` as Lemma F.1 and not a tuned parameter · `ε(1) = Qε(1) = 0` and `ε′(1⁺) = 22.9964757` **by three independent routes** |
| ### **`README_phi.md`** | the `(103)/(105)` form · the full discretization · ### **`V` IMPOSED, dimension `M − 1`** · the log-3 spectrum at `Δ ≈ 2.6×10⁻⁵`, branch B firing, the parities, the `L = 3` `ω`-triple on `V` at `3/3` exact, and the basis cross-check · the log-2 gate as `EXTERNAL-TRANSCRIBED` with all three disagreeing records side by side · the rounded-address warning |
| ### **`README_battery.md`** | every part's rows and verdicts · ### **the `V` ASYMMETRY DOCUMENTED RATHER THAN SILENTLY REPAIRED** — PART 4's counts are on the full grid, `C` is on `V`, and *that is what makes the file reproduce the bank, because the bank was taken the same way* · the proof in the denominators · **where new measurement must go instead** · what the battery has since been superseded on |

> ### **THE STANDARD BEING MET IS THE ONE THE `δ/L` LAW WAS LOST FOR: a reader who has only `relay` can now
> re-run every banked number and can find, in the same directory, what each one does and does not certify.**

### **(4) THE MIRROR** — `D:\MY-DOwnloads\mirror-refresh-2026-08-17.zip`

*Standing check, reported rather than claimed:* **exists · `1,395,743` bytes · built 2026-08-17 13:46:39 ·
23 entries (22 `.md` + `MANIFEST.md`) · source `330dc7f` · roster 22 files, unchanged since 2026-08-06.**

| check | result |
|:--|--:|
| manifest md5 == disk bytes | ### **22 / 22** |
| disk bytes == committed blob | ### **22 / 22** |
| zip entry == disk bytes | ### **22 / 22** |

**Content probes, exact strings copied from the artifacts, never retyped:**
`F.2026-08-17 — the δ/L law is a two-branch minimum` ✓ · `min( 1 − log2/logL , log2/logL )` ✓ ·
the loom's newest standing law ✓ · `REGISTRY.md` `134,168` bytes ✓.

---

## §6 — WHAT THIS CLOSE DOES NOT SAY

* ### **NOTHING ABOUT THE SIGN OF `W_∞ − W_2`. NOTHING ABOUT `h2`. NO OPERATOR INEQUALITY.**
* **The negative fraction is a bench quantity of a DISCRETIZED form.** *The `min(k₂, M−k₂)` statement is
  about counts on a grid; its continuum reading is not proved here.*
* **No mechanism is proposed** for the turn at `L = 4`, for the threshold in the interaction, or for why two
  of the six `L` miss `min(k₂, M−k₂)` by one at every `ω`.
* ### **NOTHING WAS OPENED.** *No charter, no attempt, no new register. Experiment One closes; it does not
  hand off.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **EXPERIMENT ONE IS CLOSED**, with its design flaw named in its own pre-registration and its finding
   reached through its own control's refutation of its own attribution.
2. ### **THE TWO-BRANCH LAW IS LANDED** in `THE_ATTEMPT_RECORD`, `FINDINGS`, and the Euler spec's
   cross-reference — at `MEASURED-AT-BANK`, with the bank named.
3. ### **THE `(1,3]` DEBT IS A NAMED ROW** with a trigger a reader can act on.
4. ### **THE INSTRUMENT IS DOCUMENTED END TO END** — four modules, four READMEs.
5. ### **THE MIRROR IS 22/22 COHERENT THREE WAYS** at `330dc7f`, with its probes reported.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — **committed at `relay` local tip, UNPUSHED, absence from
the public tree re-verified against the remote after this pass's push.** *Release condition unchanged:
counsel's answer, then your word.*

### **THE FOUR STANDING RULINGS, UNMOVED:** `SIDE-effects` repair route *(returned unresolved 2026-08-14)* ·
day-1 digestion *(landed, annotations-only)* · the two prose rulings *(landed)* · `internal/` + `meta/`
*(ratified `CENSUS-ONLY`)*.

**`h2` UNCHANGED. NO SIGN. `WIDEN` PAUSED. RAIL DID NOT MOVE. NOTHING DEPOSITS.**
