# THE TWO — b231, 2026-08-28

**Scope:** reads and one derivation-by-quotation. ### **Nothing touches the left side** (b229's
standing clause). ### **No promotion of the `log p` quarry.** The engine row closes at
statement-read per the author's ruling. Nothing about the h2 identity beyond the register sentence
exact. Nothing deposits. Locks last.

**The author's ruling, carried whole and spent:** *"The derivative-level engine row CLOSES AT
STATEMENT-READ; its ~0.4 closure-condition discharge is filed as an optional quiet act with no
trigger; any future engine act names commit `27a3ae7`, never the branch."*

---

## THE VERDICT — ### **(PARTIAL)**

### **The evenness holds. The fold is free. The owner's pair is (ABSENT).**

The `2` is fully **explained** by the folding reading — the `k < 0` half of a two-ended sum laid
onto the `k > 0` half — and it is ### **not owned by it**, because the object the fold folds,

> `Σ_n Λ(n) n^(−1/2) [ g(log n) + g(−log n) ]`

### **is written nowhere in this corpus, in any notation, in any of its four trees.**

### **The algebra is compiled at the zero-axiom bar. The provenance is an import and is listed as
one.**

---

## 1. The quotes

**act 12 §25(a)**, whole: *"For `f = g ⋆ g̃`: `‖ϑ(g)ξ_n‖² = ∫∫ g(λ)g(μ)⟨U_λξ_n, U_μξ_n⟩ d*λ d*μ =
∫ f(σ)A_n(σ) d*σ = tr_n`. So the bench's raw mode terms are exactly the terms of CC's
`Tr(ϑ(g)Sϑ(g)*)` at the model's `S`…"* ### **Read it for what it is: this is the POSITIVE-FORM
IDENTIFICATION, whose subject is the TRACE side.** It names no `n`, no `Λ`, no `log`. And §25(b) —
the sign dictionary, `atlas: Tr_raw − (A + E2)` against `CC dict: Tr_raw − (−A + E2)` — is about
the **archimedean sign**. ### **The prime-side statement the ferry asks for is (ABSENT) from act
12.**

**The instrument**, character for character. The bump (`carto_atlas.bump`):

```python
L = math.log(a)
v = np.linspace(-L, L, NV)
t = v / L
w[m] = np.exp(-1.0 / (1.0 - t[m] ** 2))
w /= np.trapezoid(w, v)
```

`corr` (`b38_act10.family`): `corr = np.convolve(w, w, mode="full") * float(dv[0])`, with
`vc = np.linspace(-2 * L, 2 * L, corr.size)`. And the **k-range**:

```python
for p in primes:
    k = 1
    while p ** k <= a * a + 1e-12:
        ln = math.log(p ** k)
        if ln <= 2 * L:
            PR += 2.0 * math.log(p) / math.sqrt(p ** k) * float(np.interp(ln, vc, corr))
        k += 1
```

### **`k = 1`, `k += 1`. The sum runs upward from one and never below.** `np.interp` is addressed
at `ln = log p^k > 0` only — ### **the negative half of `vc` is never touched.** That is precisely
the shape a fold produces.

**b229's adopted `wPrimes`**, verbatim: `wPrimes(a) := Σ_p Σ_{k=1}^{n_p(a)} (2 log p / p^{k/2}) ·
corr(log p^k)`, carrying b229's own finding — *"the adopted definition contains a constant no owner
has ever justified."*

**b10**, at its grade: *"weighted count over closed orbits by period, weight ~ `p^{−k/2}`, **unit
log p the period** … compared LONGHAND AT CITE against (a) the explicit formula's prime-side term
… MATCH-IN-SHAPE / NOT; **no promotion either way**."* ### **Note what b10 does with the prime
side: it names it as a thing to be compared against — it does not write it.**

---

## 2. The test

### (i) The evenness — ### **HOLDS**

From the formula alone: the grid is symmetric about zero, and `w` depends on `t` **only through
`t²`**, so `w(−v) = w(v)` identically; the normalization is a scalar. A self-convolution of an even
signal is even.

### **A second, independent witness inside the same module, which nobody had read for this
purpose:** `hhat` computes the transform as `np.cos(np.outer(u, v)) @ (w * dv)` — ### **a pure
cosine transform, which is the whole Fourier transform exactly when `w` is even.** The instrument
has relied on the evenness since act 10 without anyone writing it down.

> ### **A finding the ferry did not ask for, landing on b229's own adoption sentence.**
> `np.convolve(w, w)` is a ### **CONVOLUTION, not a correlation** — different objects in general,
> coinciding ### **only because `w` is even**. b229 wrote *"corr IS THE AUTOCORRELATION OF THE
> BUMP"*. ### **That sentence is true, and true only by a fact b229 never checked and did not know
> it was relying on. b229 is correct and was lucky** — had the bump been one-sided, the adopted
> definition would name an object the instrument does not compute.

**The run, controls first** (`tools/e16/b231_evenness.py`; output banked). Control (non-even):
`convolve` asymmetry **13.0**, `convolve` vs `correlate` **7.0** — the test has content and the two
objects are demonstrably different. The bump at all six banked cells: `w` ≤ **2.0e-15**, `convolve`
≤ **5.5e-12**, ### **`correlate` exactly 0.0 at every cell.**

### **The run refuted this seat's own registered expectation of bit-exactness, and the script's own
verdict line says `NO`. It is carried unedited; no tolerance was added to make it say yes.** Cause:
`linspace(−L, L, NV)` is not exactly antisymmetric in floating point (4.4e-16). ### **The evenness
is exact as mathematics and inexact as floating point** — and part (i) rests on the `t²` in the
exponent, a fact about the formula, not the grid.

### (ii) The owner's pair — ### **(ABSENT)**

> ### **The road to that word had a crash in it, and it is reported before the absence it nearly
> corrupted.** This act's first four sweeps returned **zero across four trees** — ### **and so did
> the control.** `explicit formula`, which stands at 200+ occurrences, came back zero. Diagnosis:
> ### **`grep -iF` aborts in this build (rc=134, SIGABRT).** `-F` alone is fine; adding `-i` kills
> the process, and ### **a killed grep prints nothing and looks exactly like a clean absence.**
> Every sweep was re-run on a working instrument with the control first. ### **Had the control been
> skipped, this act would have filed a four-tree (ABSENT) on a crashed process** — and (ABSENT) is
> the load-bearing half of the verdict. b220's species, caught by b220's own rule.

Scope stated (b230's repair): PLACE-papers **live and archive**, relay/reports, relay/data,
SIDE-global-section, SIDE-kernel. Notations: `g(log …)`, `g(−log …)`, `g(x) + g(…)`, `log n) + g`,
`+ g(−…)`, `f(−log …)`, `±log`. ### **No match. Not one, anywhere.**

What the corpus *does* carry: `Λ(n)` on **45 lines**, ### **every one the positivity ingredient** —
*"`Λ(n) ≥ 0` (von Mangoldt) ⟹ zero-free region near σ=1"*; *"the prime-ledger positivity `Λ(m)≥0`
forces `λ_Z(n) ≥ −λ_A(n)`"*. ### **It has never summed it against a test function in either
direction.** ### **b228's species one level down: the corpus has an explicit-formula INSTRUMENT and
no explicit-formula TEXT.**

### (iii) The fold — ### **the algebra free, the premise imported**

For a summand even in the signed index, `Σ_{k∈S} [T(k) + T(−k)] = 2·Σ_{k∈S} T(k)`, and the right
side is the shape of the adopted `wPrimes`. ### **The `2` is not a free constant: it is bought by
the evenness and by nothing else**, with a compiled witness that the identity **fails** without it.

### **But this shows "IF a fold, THEN the mirror" — not that it IS one.** The imports, listed:

1. ### **the classical Weil explicit formula's two-ended prime side** — standard, and **not in this
   corpus**;
2. `Λ(p^k) = log p` **as a summation coefficient**, which the corpus uses only as a sign fact;
3. the modelling choice that the weight rides inside the summand.

### **Every analytic number theorist knows import (1) — which is exactly why it must not wear the
corpus's grade.** Calling this (DERIVED) would derive a corpus fact from outside the corpus while
claiming an owned derivation: the same shape as the compiled crime, one level out.

---

## 3. The shadow

`Core/FoldedMirrorShadow.lean` — ### **14 terminals, zero axioms.** Core now **404/404 axiom-free**,
with the prior 390 lines verified **byte-identical**, so the append is provably additive.
Correspondence **row 89** written by the committed tool (`corr_row.py`) and verified by read-back.
`AllPrints.lean` updated **before** the append (b221): 76 imports, 404 prints.

> ### **The profile convicted the first draft.** It compiled clean — exit 0, no errors — and printed
> ### **eight axiom-bearing terminals** against Core's bar. b227's lesson, second instance. Two
> reusable causes, both now standing rules:
>
> - ### **An overlapping pattern** (a `[x]` case beside `x :: xs`) drags `propext` through every
>   `rfl` over it. The case was never needed — `addS` already pads.
> - ### **Core's `Int` algebra lemmas are not axiom-free.** `Int.mul_add`, `Int.add_assoc`,
>   `Int.add_left_comm` each print `[propext]`; `omega` prints `[propext, Quot.sound]`; ### **the
>   `Nat` lemmas print clean.** The fold therefore carries values in `Nat` and its index in `Int`,
>   so the mirror `−k` stays visible and the arithmetic stays inside the bar.
>   ### **Core's "zero-axiom bar" is, in practice, a `Nat` bar.**
>
> *The probe itself printed `[sorryAx]` on one line where a hand-computed list was wrong — b227's
> failure mode reproduced live inside this act, caught by the same instrument.*

Half the file is controls: `conv_not_even_witness` (without evenness the self-convolution is not
even), `autocorr_even_even_when_signal_is_not` (a correlation is even unconditionally),
`conv_ne_corr_witness` (the compiled form of the finding against b229's sentence),
`fold_fails_for_fBad`, `fold_needs_evenness`.

---

## 4. The factor question, restated — ### **the count goes up**

- ### **Quarry 1 — `log p`: untouched and unpromoted.** b10's *"no promotion either way"* travels
  with every quotation, exactly as the scope requires.
- ### **Quarry 2 — new, created by this act: the pair's provenance.** ### **Where does the
  two-ended sum enter this corpus, if it never has?** Either an owner states the two-ended form
  somewhere and the reading closes at DERIVES, ### **or the corpus's prime side entered the record
  already folded — as executable code, at act 10, with the `2.0 *` written and never explained**,
  which would mean ### **the `2` was never derived by anyone here; it was typed.**

*That is not an accusation of error — the instrument computes the standard thing. It is a statement
about what this corpus **owns** versus what it **inherited**.* The ferry's (DERIVED) branch offered
"one quarry remaining"; ### **the honest count on (PARTIAL) is two, and the second is sharper,
because it is answerable by a search or a ruling rather than by mathematics.**

---

## 5. Filings

**The engine row CLOSES at statement-read** per the ruling; the `~0.4` becomes an **optional quiet
act with no trigger**; ### **"name commit `27a3ae7`, never the branch" promoted from hazard to
standing rule.**

**The candidate seed — the analytic log-derivative passage** (`−ζ′/ζ`, whose coefficients are
`Λ(n)`), ### **the unclaimed half of the double-named "derivative"**: `ξ′` and `−ζ′/ζ` share a word
and are different objects — b219's species named in advance rather than used. ### **Promotion
criterion, exactly as set: it opens only if the `2` discharges and `log p` resists. The `2` has not
discharged, so the seed does not open**, and nothing about it was investigated.

**The sign of `A`: untouched**, and named as ### **the next act's subject — a derivation attempt
before any ruling.** ### **No sign is chosen here either.**

Also filed: the two standing instrument rules (the `grep -iF` crash; the `Nat` bar); `the-two`
**keyed in the banked index with its verdict**, and the stale `weil-ledger-target` row marked
**superseded in part by b229**; the in-flight register; the thirty-seventh seam's debt; HANDOFF.

---

## 6. Gates and scans

**b231's gates: 12 of 12 PASS, CLEAN — on the second run.** The first returned ### **11 PASS, 1
REFUSED**, and it was b217's **fixture** guard again: the `k`-range fixture pointed at
`carto_atlas.py`, which carries ### **its own one-sided `k`-loop at lines 70/79**, so the fixture
passed and discriminated nothing. ***Third consecutive act in which one of the harness's two guards
has caught one of this seat's gates — b227 the witness, b229 the fixture, b231 the fixture — and the
refusal taught something true: the one-sided staircase occurs twice in the instrument layer, not
once.*** The pair gate runs its sweep **in-process**, with the control as its fixture, so a crashed
or empty search is REFUSED rather than read as an absence. **Both sidecars carried.**

---

### **What this act did not do:** no left-side object touched, no calibration, no tuning; ### **no
promotion of the `log p` quarry**; the seed **not opened**; ### **no sign chosen**; no comparison
against any target and no ledger number computed — the one run asks a single question of a single
function and forms no comparison. ### **Nothing deposits. Locks last.**
