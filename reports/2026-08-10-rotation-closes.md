# THE BACKGROUND-LAW ROTATION CLOSES + THE BOARD AT REST — 2026-08-10

**Filing pass. No compute. Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — `F.2026-08-10-k` — THE ROTATION CLOSES *(FAMILY grade)*

`c = log(t·B)` tested at **10 points** — γ ×7.5 · δ ×9 · **B over 2.08 decades** — first-negative
index **measured** at every point, every measurement agreeing with its own independent
recomputation. **PASS: `t = 0.5456`, max residual 0.188 against a committed ±0.30, no strictly
monotone trend.**

> **A PRECISION FIX, MADE WHERE IT LANDS.** The spread 0.1187 is a **log-space** quantity, so
> *"t = 0.5456 ± 0.119"* would conflate two scales. **Correctly: `t = 0.5456` with a
> MULTIPLICATIVE spread of `×÷1.126`, roughly `[0.485, 0.614]`.**

### THE 10.14-vs-5.9 QUESTION — ANSWERED, WITH ITS SCOPE IN THE SAME BREATH

> **LAW-GENERAL, NOT OBJECT-SPECIFIC.** The constants differ because their **backgrounds** differ,
> under one threshold.

**Scope, because "answered" without scope is the overclaim this corpus has a class for:** answered
**at family grade for SYNTHETIC multisets**; for real objects it rests on **one consistent point**.
**The strand-2 depth comparison remains BLOCKED-AT-POWER — this route answered by the cheap door,
not the blocked one, and the two are not the same test.**

> **A DISAMBIGUATION PLANTED DELIBERATELY, because one word's two readings will otherwise diverge
> in the record: the Epstein point is OUTSIDE THE FIT meaning NOT POOLED; its residual −0.105 lies
> INSIDE the family's spread of ±0.188. Outside the fit, inside the spread — both true, neither
> implying the other.**

**Caveats on the face:** commensurability untested (analytic `S∞` vs a term count) · one real
object · **the slope note — 0.9517 where the law asserts exactly 1.**

### `BURIAL` — EVIDENCE LINE UPDATES, `BURIAL` DOES NOT MOVE

Reads **derived-twice · measured-twice · two-kinds · constant-difference-law-accounted-at-family-grade**.
> **NO PROMOTION: this accounts for a difference *within* the one admitted face without adding a
> face. A better-explained instance is still one instance.**

---

## §2 — `W-ORD-SLOPE` *(registered; NOT run)*

The **0.9517-vs-1.000** exponent question. Wider `B` decades (3.5–4 against this run's 2.08),
**synthetic only**, minutes-scale.

> **THE LOAD-BEARING CONSTRAINT: `B` HELD FIXED AT `B = 2M`.** The threshold run measured the
> background's *definition* at **0.22 in `c`** — comparable to the whole effect being chased — so
> **a slope measured across a definition change would measure the definition, not the law**, and
> the confound is silent because both quantities are called "the background".

**Expectations deliberately NOT committed now.** To be committed **at registration if ever
ordered**. *A tolerance chosen months from a possible run, without knowing the instrument's spread
at wider `B`, is a tolerance chosen conveniently.*

---

## §3 — TWO LOOM LINES

**`A MAGNITUDE IS NOT A DEFINITION`.** *"About 10³"* names a size, not which quantity counted how;
**a law whose constant is a logarithm of it absorbs the difference silently.** Worked example with
its **price measured: 0.22 in `c`** — the same 400-ordinate base is `B = 800` under the forced
definition, moving the prior point's residual from −0.402 (outside) to −0.179 (inside). **Kin to
the label/content axis.** **Recognition signature: a constant fitted against a quantity that has
never been varied** — its definition has never been tested, and the constant carries whatever the
definition left out. *Families find this; single measurements never do.*

**`THE MATCHED-PAIR COLLISION`** *(method-canon candidate, earned, not promoted)*.

> ### **To test a one-variable law, collide two points at the same value of that variable and vary everything else.**

Points 7 and 8: identical `B = 2400`, **γ differing 2.2×, δ differing 3.0×**, `c = 7.1736` vs
`7.1387` — **0.035, under a fifth of the family spread. The law's central claim read directly, with
its own variable held exactly fixed.** **Earned, not composed: the check was noticed in the
residuals afterward and is filed as found.** **Recognition signature: a family that varies
everything at once has no collision in it — and a fit that assumes the claim cannot test it.**

---

## §4 — THE BOARD AT REST

| | state |
|:--|:--|
| **strand 1** | **BANKED** — one research-reach item: **CONSTRUCTION, `Δ2` + `Δ4` ON THE FRAME**; author-ruled when ruled, not to be manufactured |
| **strand 2** | **closed at its power block**; the background question **answered by the cheap route** |
| **strand 3** | **DISCHARGED-IN-CORPUS / PARKED-AT-DEPOSIT** (ten instances behind the posture clause) |
| registered, not run | `W-ORD-SLOPE` · `W-ORD-ENCODING-RESIDUE` (ride-along) |
| declared-unrepaired | **`finPart`** — under the stopping rule, as that rule's first test |
| question grade | four filings holding; **`-d` and `-h` share §24.4, genericity check noted** |
| proposed, not promoted | `BURIAL` (one face) · head 4 · **three earned method-canon candidates** |

> ### **NOTHING IS DECAYING AND NOTHING MOVES WITHOUT A RULING.** No worker running, no pin drifting, rails unmoved and clean.
>
> **And the mathematics is where it was: the corpus knows what it lacks, at named fields, in an identified object — and `h2` is exactly where it was.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `92a65b4` |
| relay | `f44f25f` → this report's commit |
| `SIDE-carrier-spec` | tag `v0.2.0` = `2051c52`, PRIVATE — untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
