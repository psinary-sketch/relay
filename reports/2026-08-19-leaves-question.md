# THE LEAVES QUESTION — THE ROADS' JOINT PROOF-SEARCH, RUN
## ### **VERDICT (b): THE OBSTRUCTION NAMED, TWO-LAYERED — THE PROVED LAYER: THE SECTOR DATA DOES NOT FACTORIZE (THE GLOBAL `E₁` IS THE SECTOR SUM, STRICTLY LARGER THAN THE TENSOR DIAGONAL — ACT 1's BANKED FINDING, NOW READ AS THE LIFT'S OBSTRUCTION) — THE NAMED-STEP LAYER: THE SPACE-LEVEL QUESTION REDUCES TO THE PER-PLACE SCHMIDT PURITY OF THE `E₁`-UNITS — THE THREE ROADS' ONE ABSENCE GAINS ITS STRUCTURAL CONTENT**

**Relay report · 2026-08-19 · sub-gate declared · Rules 4/5 · theorem-first (no bench cell opened;
the one licensed check NAMED) · the kernel-purity ruling governing · nothing deposits · nothing
circulates**

> ### **RULE-3 LOG:** *the B13 tensor square at its seven banked cells ✓; act 1's two findings at
> `THE_GLOBAL_SECTION.md v1.0` ✓; Road A's thrice-measured arrest ✓; Deninger `§5.8` at content ✓;
> the banned grep: CLEAN. ORIENTATION RUN as the entry specifies (both `§0` screens;
> `F.2026-08-18a/b`; the loom's consolidation; the week-close pins). THE OPEN_TRAILS NEXT-SESSION
> ENTRY CONSUMED AND STRUCK per its own discipline, with this report as the pointer. MIRROR-LAG
> FLAG CHECKED, not smoothed: the ferry's `f5c4858` is TWO SITTINGS STALE — the mirror was rebuilt
> at `9875578` last sitting (the 2026-08-19-dated zip; the 2026-08-18-dated zip still on disk
> beside it explains the stale read); rebuilt again at this act's HEAD below. Terms coined: none.*

---

## §1 — THE QUESTION, BOTH FORMS, STATED FIRST

**REGISTERED (the entry, verbatim):** does the local tensor square `V₁ ⊗ W₁` — the scaling action
across it, the transform interleaving the factors — admit any global analogue over ℚ? Is Road A's
SELF-PRODUCT the global lift of Road B's finite-place tensor square?

**SHARPENED (from act 1):** is the global Sonin object `⊗′_v S̄_v` itself a tensor square
`V ⊗ W`, `W` the Fourier dual of `V`, as every local factor certifiably is? The technical
content: the restricted product of tensor squares is a tensor square of restricted products ONLY
IF the restriction data factorizes compatibly.

## §2 — THE ANALYSIS, AT ITS GRADES

1. **The local structure (banked, exact; the vanilla shadow compiled this act):** the splitting
   `m = a + pⁿb` of `ℤ/p^{2n}` realizes `L²` as a tensor square, and the DFT interleaves the
   factors through EXACTLY the middle bilinear term:
   `m·m′ ≡ a·a′ + pⁿ(ab′ + a′b) (mod p^{2n})` — compiled at (2,1), (3,1), (2,2), (5,1), every
   tuple (`TensorSquareShadow.interleaving_index_law`, no axioms). The ball is the SQUARE ROOT of
   the modulus — the local square exists because each place sits at its self-dual level.
2. ### **THE PROVED LAYER — the sector data does not factorize.** *Suppose the best case: every
   local unit pure, so `⊗′(V_v⊗W_v) ≅ (⊗′V_v)⊗(⊗′W_v)` as spaces. The constrained sector still
   does not factor: act 1 PROVED the global `E₁` is the SECTOR SUM `{∏λ_v = +1}`, and it is
   STRICTLY larger than the tensor diagonal `⊗′E₁,v` — the mixed patterns (`(−1)(−1) = +1`, …)
   are nonempty since at least two places carry both sectors (banked: b33 at `∞`; the finite
   four-sector data). The gap's combinatorial core: `2^{k−1}` admissible patterns vs the
   diagonal's ONE (`sector_pattern_gap`, no axioms).* ### **The `∏`-coupling is GLOBAL; the
   `⊗`-structure is PLACEWISE; the constrained sector — the object the whole construction is
   about — is not tensor-square geometry. THE FAILING COMPATIBILITY, exactly: the `E₁`-restriction
   (sector) data does not factorize across the product.**
3. **THE NAMED-STEP LAYER — what remains of the space-level question:** whether `⊗′S̄_v` is a
   tensor square OF SPACES reduces to the per-place SCHMIDT PURITY of the banked `E₁`-units
   across `V₁⊗W₁` (a pure fixed vector of the interleaving F must be graph-shaped `v ⊗ F₁v̄`;
   at (2,1) purity is trivial [1×1]; at (2,2)+ it is a finite exact check). **THE ONE LICENSED
   CHECK, NAMED AND NOT RUN (theorem-first): the Schmidt rank of the banked units at (2,2),
   (3,1) — decide-reachable once the unit vectors are constructed in `ℤ[ζ]`-arithmetic; a bench
   cell only at the author's word.**

## §3 — THE VERDICT, PER THE PRE-COMMITTED BRANCHES

> ### **(b) LANDS — THE OBSTRUCTION NAMED: the sector-sum non-factorization (proved layer,
> derivation grade from banked findings) + the unit-purity reduction (the exact missing step).**
> **Which columns it blocks and why: (iii) IDENTIFICATION and (iv) THE JOINT MOVE — the joint
> object would have to carry the `∏`-coupled sector as ONE structure, which no placewise square
> supplies — and thereby (vi) THE LEAVES' METRIC (no leafwise `e^t`-pairing without the joint
> object). Columns (i), (ii), (v) are NOT blocked by this obstruction.**
>
> **(a) NOT landed as the plain tensor square. The one frame (a) leaves alive, priced: the GRADED
> square — the global object as the `∏ = +1` part of a square-with-sector-grading. Against the
> columns: it would supply (v) the presentation and the interleaving structure, but its grading
> IS the unfactored sector data — so it supplies (iii)/(iv) only by presupposing them: CIRCULAR;
> it does not advance clause (e). The pentagon's law on the face: definiteness checked, never
> specified; nothing promoted.**
>
> **(c) filed openly: the purity check could land a surprise (all units pure AND the mixed
> sectors carrying a second square's worth of structure) — open, owned by the named check.**

## §4 — THE CONSEQUENCE AT GRADE (item 3)

### **The three roads' one absence gains its structural content: THE DOUBLE LIMIT'S STRUCTURAL
CONTENT = THE NON-FACTORIZATION OF THE SECTOR DATA ACROSS PLACES.** *Locally every place holds
its self-product in miniature (B13, certified); globally the `∏λ_v = +1` coupling prevents
assembling them into one self-product — Road A's arrest now has its mechanism at the model;
Deninger's Frobenius-lift absence (`§5.8`: CM curves, "almost never" otherwise) is the same
absence in the flow's coordinates. Cross-linked to the four vocabularies: the `Z` channel · the
bright interface / missing `H²` · THE LEAVES · the `E₁` gluing — the leaves are missing BECAUSE
the sector sum is not a diagonal.* **The road documents' `§0` faces carry one added line each
(the arrest mechanism-named); the charter's `C1`/`C3` lines dated; the trigger untouched; the
sub-gate standing; the codomain `§6` unchanged ((e) stays ABSENT — this act names structure, it
builds nothing).**

## §5 — THE RECORD

*The OPEN_TRAILS entry STRUCK as consumed (this report the pointer); the vanilla leg + 2 (total
103, no axioms; NO kernel carries a sorry); `F.2026-08-19c`; rows 170–174; pins; the mirror
rebuilt at HEAD with the lag explained.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO IDENTITY DECIDED; NO REGISTER MOVED.
NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING CIRCULATES.**
