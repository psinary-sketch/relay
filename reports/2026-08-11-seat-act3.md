# `W-ORD-SEAT` — ACT 3: THE SEAT-OBJECT AT THE GAUGE — 2026-08-11

**EXTERNAL-AT-CITE discipline per the `W(ℤ)` precedent: nothing about Deninger's object is compiled;
every cell cites its passage or reads NOT-ADDRESSED.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

> ### PATH-OF-CONSEQUENCE
> **"If this row passes `Δ1` and `Δ4` together, the junction closes at the object level and the
> programme's entire remaining geometric absence is `Δ2` — the cohomology the `C1`–`C9` set
> specifies; no arrow supplies the sign."**

> ### PROCEDURAL DECLARATION — made before the grade, not after
> **One content probe ran BEFORE this act's expectations were registered** (the
> Frobenius/Verschiebung passage, to decide whether anything needed flagging before executing).
> It returned *"The Frobenius and Verschiebung maps of `W(A)` leave `W_rat(A)` invariant and we have
> `F_ν([a]) = [a^ν]`"* and *"They satisfy the usual relations."* **The registration was therefore not
> blind on `E1`'s congruence cell and says so. Every other cell was graded after registration.**

---

## §1 — THE GRADING at spec `v0.4.1`

Object: the suspension `X₀ = X̃₀[ℂ] ×_{ℚ>0} ℝ_{>0}` with its flow, over `W_rat(Spec ℤ)`.

| field | verdict | reason |
|:--|:--|:--|
| `cancel`, `neg` | **PASSES-AT-CITE** | `W_rat(A)` is a subring of the big Witt ring — a commutative ring |
| **`arith_action` (`Δ4`)** | **PASSES-AT-CITE on the family; congruence cell `PARTIAL`** | *"commuting Frobenius endomorphisms for every prime `p`"*; `ψ_pψ_q = ψ_{pq}` covered by *"They satisfy the usual relations"* — **stated by reference, not written out**. The congruence `ψ_p(x) ≡ x^p (mod p)` is **not stated in this source** |
| **`arch_independent` (`Δ1`)** | **PASSES-AT-CITE** | the `ℝ_{>0}` is a factor of the construction, transverse to the finite data (*"a foliation by the one-codimensional images of `X̃₀[ℂ] × {u}`"*). **Product-formula counterexample checked: does NOT apply.** Coupling noted — the suspension quotients by the diagonal `ℚ_{>0}`-action, so `u` is defined only mod `ℚ_{>0}`; transversality survives, determination does not arise |
| **`act` / `R4`** | **`PARTIAL-BOTH-WAYS`** | §2 |
| **`pair` (`Δ2`)** | **FAILS** | no pairing target exists; `C1`–`C9` is the specification of what this cell awaits |
| `finPart`/`archPart` | **PASSES-AT-CITE, structurally** | graded with the spec's own declared-unrepaired `finPart` codomain hole noted, not against it |
| `components_faithful`, `level` | **NOT ADDRESSED** | the source says nothing bearing on them |

## §2 — THE OBJECT SPLITS THE TRANSLATION CRITERION

Constraint 4 requires an **endomorphism** action, not a **translation** action —
*"translations give no endomorphism ring, so no trace form has a carrier"*.

> ### Deninger's object has BOTH, in different structures.
> Flow is translation-type: *"`t` acts by **right multiplication with `eᵗ`**."*
> The `ψ_p` are genuine ring **endomorphisms**.
>
> **`PARTIAL-BOTH-WAYS`, as registered — and the split is the finding.** The corpus treated
> translation-vs-endomorphism as a **dichotomy**, because the programmes it was reading had only
> one. **This object is on neither side: the archimedean direction translates and the finite
> directions are endomorphisms, on the same space.** *A correction to the criterion's implicit
> assumption, not a pass.*

## §3 — THE ANTI-ENCODING CHECK, AND IT BITES

The spec's own degenerate witness passes every structural field and separates nothing —
*"three sharpenings now, the same degenerate witness passes every one."*

> ### Deninger's object sits in that same cell: structural fields pass, `pair` fails.
> ### **Passing `Δ1` and `Δ4` is therefore NOT evidence of being close** — the spec was built to say
> exactly that, and it says it here about the best object the programme has found.
> **This is the spec working, not the object failing.**

## §4 — THE JUNCTION TABLE

| object | `Δ1` | `Δ4` | `Δ2` | grade |
|:--|:--|:--|:--|:--|
| the frame | PASS | **empty** | — | compiled |
| `W(ℤ)` | — | **PASS** | — | at cite |
| **Deninger `W_rat` system** | **PASS** | **PASS** *(congruence `PARTIAL`)* | **FAILS** | **AT-CITE, external** |

> ### The first row in the programme's history to carry both columns.
> ### **The remaining geometric absence is `Δ2` alone: a cohomology on this object satisfying `C1`–`C9`.** The two dossiers — `H¹` geometric, `C₂` arithmetic — are now the complete **two-face specification of one missing thing.**
>
> ### And "`Δ2` alone" must not read as "nearly done." **`Δ2` is the pairing, and the pairing's definiteness is the clause.** *The distance does not shorten; it can only be named more precisely. What closed is a description, not a gap.*

## §5 — FOUND-BEYOND *(two)*

1. > **The inheritance is STATED, not merely plausible — and the registration under-anticipated it.**
   > The registration expected at best *"`W_rat ⊂ W` makes it plausible."* The source says more:
   > *"the Frobenius and Verschiebung maps of `W(A)` **leave `W_rat(A)` invariant**"* — the maps ARE
   > the big-Witt maps restricted. **The cell is still `PARTIAL`, because the rule was set before the
   > read and the congruence is still unwritten here — but the reason is weaker than anticipated,
   > and recording that is the point of registering first.**
2. > **The `Δ4` holder and the `Δ1` holder turned out to be the same object, and nothing predicted
   > it.** `W(ℤ)` was graded for `Δ4` in the construction era; the archimedean seat was isolated by a
   > separate register two eras later. **They met in `W_rat`.** *The programme's two long-separate
   > absences were tracked as independent, and one object answers both.*

## §6 — ROTATION NOTE: a correction to the ferry's item 3

> **`W-ORD-ARITH` act 1 has already RUN and banked** — the `C₂` dossier (nine rows, two declared
> `NOT-LOCATED-AT-CONTENT`) plus the joint reading at necessary conditions only, at PLACE-papers
> `78c9baa` / relay `2140374`. **What stands next for that register is act 2, whose charter is
> unwritten.** *Recorded rather than silently obeyed or silently skipped.*

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | this pass |
| relay | this report's commit |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**`Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow. Realization is not placement.**
