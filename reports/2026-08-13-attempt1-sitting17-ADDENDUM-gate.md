# W-ATTEMPT-1 — SITTING 17, ADDENDUM: THE GATE LANDED — **PARTIAL PASS, AND THE CHARTERED COMPARISON IS CIRCULAR**

**Relay report · 2026-08-13 · ATTEMPT track · relay-only · diagnostic COMPLETE before any sentence**
**Nothing deposits.**

---

## §1 — WHAT PASSED

### **RESOLUTION AT THE SINGULAR REGION — the `u ≈ 0` lesson applied before any number read:**

| `a` | `D(f)` at `n = 2000 / 4000 / 8000` | spread |
|:--|:--|:--|
| `√2` | `2.190771678 / 2.190771678 / 2.190771679` | ### **`8.5×10⁻¹⁰`** |
| `√3` | `1.990998952 / 1.990998952 / 1.990998953` | ### **`1.1×10⁻⁹`** |

> ### **CONVERGED. The failure mode that produced sitting 16's phantom finding was tested for FIRST this
> time, and it is absent.**

### **`L = D + W_∞` POSITIVE ON THE POSITIVE-DEFINITE FAMILY (their (52)):**

| `a` | `D(f)` | `W_∞(f)` | `L` | positive? |
|:--|:--|:--|:--|:--|
| `1.3` | `+2.2445194` | `+1.8356292` | `+4.0801486` | ### **YES** |
| `√2` | `+2.1907717` | `+1.9905249` | `+4.1812966` | ### **YES** |
| `1.5` | `+2.1395204` | `+1.9974053` | `+4.1369257` | ### **YES** |
| `√3` | `+1.9909990` | `+1.9088789` | `+3.8998778` | ### **YES** |
| `2.0` | `+1.8441188` | `+1.7864992` | `+3.6306180` | ### **YES** |

**And `D` behaves as δ's own structure predicts:** *`∫f d*ρ = 1` (the bump is normalized), so `D` is the
`f`-weighted average of `δ`. At `a = 1.3` the support is narrow and `D = 2.2445` sits just above
`δ(1) = 2.2375` — consistent with `δ′(1⁺) = +1`. `D` then falls monotonically as the window widens into δ's
decay.* ### **An independent behavioural check on the assembled δ, and it holds.**

---

## §2 — ### **THE CHARTERED COMPARISON IS CIRCULAR, AND I WILL NOT REPORT IT AS A PASS**

**Item 1 says: *"`L(f) = D(f) + W_∞(f)` per Corollary 2.3 … then Route A vs `(L − D)` agreement = the
gate."***

> ### **IF `L` IS DEFINED AS `D + W_∞`, THEN `L − D = W_∞` IDENTICALLY. THE COMPARISON IS AN ALGEBRAIC
> TAUTOLOGY AND TESTS NOTHING.** *It would return "PASS" to machine precision on any inputs whatsoever,
> including wrong ones.*
>
> ### **A REAL DUAL-ROUTE GATE REQUIRES `L(f) = Tr(ϑ(f)PP̂P)` COMPUTED INDEPENDENTLY VIA THEIR (82) — the
> build named as outstanding since sitting 14 and still not made.** **Reporting the tautology as a passed
> gate would have manufactured a certification.**

**The `E(f) = Tr − W_∞` loop closure against sitting 13's assembled ε was likewise NOT computed** — it was in
the same script's scope and did not get built.

---

## §3 — STATE

| item | verdict |
|:--|:--|
| resolution at `u ≈ 0` | ### **PASS (`10⁻⁹`)** |
| `L` positive on their (52) | ### **PASS (5 rows)** |
| δ's behavioural check vs `δ(1)`, `δ′(1⁺)` | ### **PASS** |
| ### **the dual-route gate proper** | ### **NOT RUN — the chartered form is vacuous; `L` via (82) is required and unbuilt** |
| `E = Tr − W_∞` loop closure | ### **NOT RUN** |
| ### **the ledger** | ### **NOT RUN — still gated** |

---

## CLOSING

**Returning for the author's word:**
1. ### **THE DIAGNOSTIC COMPLETED BEFORE ANY SENTENCE WAS WRITTEN ABOUT IT — the new law's first full cycle, honoured.**
2. ### **THREE REAL CHECKS PASS: singular-region resolution at `10⁻⁹`, `L > 0` on all five rows, and δ's behaviour against its own two anchors.**
3. ### **THE GATE AS CHARTERED IS A TAUTOLOGY (`L := D + W_∞ ⟹ L − D = W_∞`) AND I DECLINE TO BANK IT AS A PASS.** *The genuine gate needs `Tr(ϑ(f)PP̂P)` via (82) — outstanding since sitting 14.*
4. ### **THE LEDGER REMAINS UNRUN for a fifth consecutive sitting, and the reason is now one named unbuilt object rather than a diffuse blockage.**

> ### **THE ARC'S POSITION IS UNCHANGED BY THIS ADDENDUM: instrument components keep certifying, and the one
> comparison that would close the loop needs a build nobody has made. `h2` is exactly where it was.**

**`h2` UNCHANGED. NO SIGN. NOTHING DEPOSITS.**
