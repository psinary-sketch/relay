# THE BRAID BANKS AND HOLDS — 2026-08-10

**Banking pass; no new work. Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — THE CHAIN, COMPLETED ON THE RECORD

| | stage | state |
|:--|:--|:--|
| **Q0** | the **interpretation** — is the `T`-action on `THH(Spec ℤ)` the archimedean slot? | **OPEN**; test **NOT FORMULABLE**, mechanism named |
| **Q1** | the **extraction theorem** for the `∞`-term over `ℤ` | **OPEN**, conditional on Q0; boundary conditions **held** |
| **Q2** | the **pairing** — `Δ2` realized, **the two-Weil descent performed** | **OPEN**, conditional on Q1 |
| **Q3** | the **definiteness** of that pairing | ### **= `h2` IN GEOMETRIC COSTUME — THE CLAUSE ITSELF, NOT A PRECURSOR** |

**The 1948 anatomy, cited as the worked instance where the chain closes: trace formula →
intersection pairing → Hodge index.** *Over function fields all four stages are inhabited and the
last is a theorem; over `ℚ` the first is not yet interpretable.*

> ### **NO ARROW SUPPLIES THE SIGN.** Q0 gives an interpretation, Q1 a theorem that realizes terms,
> Q2 an object and a descent — **not one supplies positivity**. **Q3 is the residue wearing the
> constructed object's clothes.**
>
> ### **The chain is NOT "four steps to RH". It is three steps to a well-posed statement of the clause, and the clause.** *Recording it in full is a claim about **order**, not about **distance**.*

## §2 — TWO LOOM LINES

**AUTHORITY IS NOT ACCURACY.** A precedence order picks the winner of a **disagreement**; it does
not make the winner **true**. The reconciliation was aimed downstream on a well-founded
observation — **the downstream files were current and the declared source of truth was wrong.**
> **Procedural consequence: verify the TOP of the precedence order against an EXTERNAL authority,
> not only the bottom against the top.** *Checking downstream against REGISTRY can only propagate
> REGISTRY's errors into a clean bill of health.*

**PLAUSIBLE-NEIGHBOUR DRIFT.** An error that **matches its neighbours** survives proofreading — a
value is checked for *plausibility* far more often than for *provenance*. **The kernel's date
drifted to the date of the wave's other two records: it did not look like an error, it looked like
a wave.** **Kin to imposed-present** — a false value made safe by **context** rather than by
**checking**. *Signature: a value that agrees with its neighbours and cites no independent source.
Agreement is the reason to check it, not the reason to accept it.*

**And the ellipsis-orthography catch joins the FALSE-ABSENCE PROBE family**, whose rule gains:
**ENUMERATE EVERY SPELLING, OR REPORT THE SPELLING ENUMERATED.** *Through-line unchanged: **a probe
that finds nothing has not established that nothing is there.***

## §3 — THE BOARD AT HOLD

**`W-ORD-Q0` extraction-complete, chain mapped `Q0`→`Q3`** · **wave gate SATISFIED** (evidence
`relay/data/zenodo_fetch_2026-08-10.jsonl`), **deposits PARKED at posture** · **strand 2 closed** ·
**strand 3 discharged / parked** · registered-not-run: `W-ORD-SLOPE`, `W-ORD-ENCODING-RESIDUE` ·
stopping rules holding: `finPart`, the graph field, the level refinements · **library-visit item
open, author-side** (Deninger 1991, pre-arXiv) · **patent day April 2027** · **TECHNE gated as
sequenced** · **rail UNMOVED ACROSS THE ENTIRE ERA** · **`h2` exactly where it was.**

> **Nothing is decaying. Nothing moves without a ruling. The braid holds at rest.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `5e165e7` |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
