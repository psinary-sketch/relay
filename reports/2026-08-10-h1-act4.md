# `W-ORD-H1` — CONSTRUCTION ACT 4 — 2026-08-10

**Registered at PLACE-papers `857a876` BEFORE the computation (E1–E4 verbatim).**
**Disclaimed register governs all spectrum output. NO ordinate comparison in any branch.**
**No positivity, no `C₂`, no sign. Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — E1: THE RE-CELLING, AND THE TOWER READING GRADED

Act 3's nominee is re-celled **`CANDIDATE-H⁰`** at question grade: `Θ = 0` is Deninger's `H⁰` axiom,
and `π₀ = 𝔸_f/ℚ` awaits its `∞`-factor. The dossier gains a **provisional `H⁰` row**.

| level of the registered tower | grade |
|:--|:--|
| **plain → `H⁰`** (`Θ = 0`) | **COMPUTED MATCH** — act 3 |
| **equivariant → `H¹`** | **UNDETERMINED** — reduces to §2's convergence question |
| **trace → `H²`** (`TP` inverts `u`; Deninger's `H²` has `Θ = id`) | **NOT GRADED** — asserting "`u`-inversion = `Θ = id`" would be `ASSUMED`, and `ASSUMED` **voids the cell** |

> ### **THE RHYME IS NOT ADJUDICATED: one level of three is computed, the other two are an undetermined and a void.**
> *It was registered as a hypothesis carrying no weight. It still carries none.* **Adjudication by
> computation, not by rhyme — as ruled.**

## §2 — E2/E3: THE COMPUTATION

### The rational corner

From the single cited input (KN Cor. 4.2: `π_*THH(ℤ) = ℤ` in degree 0, **finite in every positive
degree**), the homotopy-fixed-point spectral sequence `E₂^{s,t} = H^{-s}(BS¹; π_t THH(ℤ))` has
**rational `E₂ = ℚ[u]`, `|u| = −2`** — only `π₀` survives rationally.

> **`TC⁻_*(ℤ)_ℚ = ℚ[u]`** (degrees `0, −2, −4, …`) · **`TP_*(ℤ)_ℚ = ℚ[u^{±1}]`** (every even degree).
>
> ### **This is act 1's `ℚ[u]` leg, recovered independently — from Bökstedt's numbers rather than from the regularized determinant.** *Two routes to the same ladder; recorded as corroboration of the leg, not as a new result.*

> **DECLARED, NOT HIDDEN:** `TC⁻` is an inverse limit, and **rationalization does not commute with
> infinite limits.** "Rational `E₂` = `ℚ[u]`" gives the rationalized abutment **only modulo a
> `lim¹`/convergence statement these inputs do not supply.**

### The `p`-corners

The same spectral sequence gives the ladder coefficient **`ℤ_p` in each even degree `≤ 0`**, from
`π₀ = ℤ_p`. **Beyond that — differentials, extensions, the syntomic content — the sources thin, and
the data is PER-DEGREE DECLARED, not asserted.**

### The product, then the rationalization — where the act's weight falls

In a fixed total degree the spectral sequence has **infinitely many filtrations contributing
torsion** (`uⁿ` against the `ℤ/k`'s). Whether those assemble, per degree, to a **finite group** or to
an **infinite product** decides the nominee:

| case | consequence |
|:--|:--|
| **(A) finite per degree** | the torsion dies under the final `⊗ℚ` → **cofiber `= (𝔸_f/ℚ)[u]`**: act 3's answer tensored with a ladder, **no new direction** |
| **(B) infinite products** | the torsion **survives** rationalization (`∏_ℕ ℤ/2 ⊗ ℚ ≠ 0`) → **content beyond the ladder — and it is exactly "the torsion `ℚ` forgets"** |

> ### **WHICH CASE HOLDS IS UNDETERMINED AT THESE INPUTS.**
> ### **THE ENTIRE QUESTION OF WHETHER THE EQUIVARIANT NOMINEE BEATS THE PLAIN ONE REDUCES TO THAT ONE CONVERGENCE QUESTION.**
> *Naming it exactly is what this act delivers. It is the same discipline act 2 used: when the
> inputs run out, name the precise thing that would resume the work.*

### The outcome classes, **non-exclusive** — the act-3 defect repaired

| structure | class |
|:--|:--|
| **`π₀`-shape** (`𝔸_f/ℚ` at `u⁰`) | **NON-LADDER** |
| **weight data** (powers of `u`) | **LADDER** — `ℤ_{≥0}` for `TC⁻`, `ℤ` for `TP` |
| **`Θ`-spectrum-shape** | **INSUFFICIENT** — and identifying the `u`-grading generator with Deninger's `Θ` would be `ASSUMED`, **voiding the cell**. Not asserted; the cell stays void. |

## §3 — THE `∞`-LEG IS **ADJOINED, NOT DERIVED**

**The fracture square of `2411.05467` has a rational corner, `p`-complete corners, and the glue.
It has NO archimedean corner.** The `ℝ`-leg is act 1's genuinely computed `Γ_ℝ`-carrier — **real,
not invented, and placed by design.**

> ### **SCREEN COLUMN 4 ASKS WHETHER THE CONSTRUCTION ADDS CONTENT BEYOND ITS INPUTS. A HAND-ADJOINED LEG IS AN INPUT.**
> ### **COLUMN 4: `FAIL-WITH-SHAPE`.**
> *The one factor that would carry `π₀` from `𝔸_f/ℚ` to `𝔸/ℚ` is placed there rather than produced.*
> **It inherits: any future nominee must derive its archimedean leg FROM THE GLUE, or declare it
> adjoined.**

**NAME-IDENTITY, AT ITS SHARPEST.** With the leg adjoined, `π₀` would be `𝔸/ℚ` **as a group** —
`DERIVED-AT-CONTENT`. **But Connes's `X = 𝔸/k` is a space carrying the IDELE CLASS ACTION and an
`L²` structure; this is `π₀` of a spectrum carrying a CIRCLE action. Identifying those actions would
be `ASSUMED`.**

> ### **THE CELL ON WHICH ANY SPECTRAL READING WOULD REST IS VOID. I STOP THERE.**
> **No spectrum computed, no action identified, no comparison made.** `KIN` at `π₀` as a group;
> nothing beyond it.

## §4 — E4: THE GRADED TABLE

| screen column | act 1 | **act 4, graded fresh** |
|:--|:--|:--|
| 1 · reads the diagonal | PASS | **PASS — by construction** |
| 2 · retains the torsion `ℚ` forgets | *PASS, definitionally* | **OVERTURNED.** At the plain level act 3's own computation shows the torsion **dies** (`ℤ/k ⊗ ℚ = 0`). At the equivariant level: **UNDETERMINED** — case (A)/(B) |
| 3 · `s` on scaling vs compact | UNDETERMINED | **UNCHANGED** — the inherited action is still the compact `T` |
| 4 · evades the seal by re-description | AT RISK | **`FAIL-WITH-SHAPE`** — §3 |

| dossier row | verdict |
|:--|:--|
| **1 · Deninger's axioms** | `H⁰` row **MATCH** (`Θ=0`) · `H¹` row **UNDETERMINED** · `H²` row **VOID-IF-ASSERTED** |
| 2 · the no-go | **UNDETERMINED** — no real structure exhibited either way |
| 3 · forced shape (`ℂ`, infinite dim) | infinite-dimensional **YES** (`𝔸_f/ℚ` over `ℚ`) · `ℂ`-linear **NO**, and the `∞`-leg that would supply it is adjoined |
| 4 · reproduces the legs | **`Γ_ℝ` present ONLY BECAUSE ADJOINED** — not reproduced |
| 5 · division of labour | consistent by construction |
| 6 · seal + screen | **`FAIL-WITH-SHAPE`** (column 4) |
| 7 · spectral account | **VOID CELL — stopped at §3** |

**`KIN`-AT-`π₀` ADJUDICATION (fracture ↔ adelization):** the cofiber of the gluing map at `π₀` **is**
the adelization quotient — **`DERIVED-AT-CONTENT`** from `π₀THH(ℤ) = ℤ`, `Ẑ⊗ℚ = 𝔸_f`, and the long
exact sequence. **Computed grade, not asserted.**

## §5 — FOUND-BEYOND-REGISTRATION *(two items)*

1. > ### **A SCREEN COLUMN GRADED "PASS — DEFINITIONALLY" SURVIVED TWO ACTS AND THE COMPUTATION OVERTURNED IT.**
   > Column 2 was passed by definition in act 1 (*"the `p`-complete corner IS that torsion"*).
   > **Act 3's own computation then showed the final rationalization destroys exactly that torsion —
   > and neither act 2 nor act 3 re-graded the column.** *A definitional pass is a claim about the
   > construction's intent, not about its output; it was carried for two acts as though it were the
   > second.* **Candidate loom line — offered for the author's word, not filed unilaterally.**

2. > **THE ACT'S DELIVERABLE IS A QUESTION, NOT A TABLE.** Every graded cell is inherited,
   > undetermined, or a fail; **the one genuinely new thing is the identification of a single
   > convergence question that decides the whole nominee.** *Recorded so the table is not mistaken
   > for the result.*

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `857a876` (registration) → this pass |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE, untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing claimed beyond grade. `Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow.**

> ### **Realization is not placement.**
