# THE `(v)` QUESTION AT DEPTH · CORRECTION TWENTY-THREE · THE PHASE BOUNDARY FILED
## ### **THE FOURIER TRANSFORM ON `Cl` — AND THE BANKED PRODUCT IDENTITY WAS WRONG**

**Relay report · 2026-08-17 (seventh sitting) · author-ruled · `INSTRUMENT` track · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 467fbd2cc00f99590790fe590f15e2d7df422a7e   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 467fbd2   ### 22-file roster + MANIFEST, 3 content probes PASS
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> ### **ROUTING, DECLARED FIRST.** *The author ruled the `§8.3` residue redone at proof-search depth and the
> routing note sends proof-search to Fable at high effort.* ### **This sitting ran on FABLE.** *Both
> branches were argued longhand; every derivation below is coefficient bookkeeping and finite-group
> characters; the standing stop rode every section and did not fire.*

---

## §1 — ### **THE STATEMENT MADE EXACT — AND CORRECTION TWENTY-THREE FELL OUT OF IT**

**The ferry's frame is confirmed:** the passage between the Epstein/additive and Hecke/multiplicative
presentations is ### **exactly the finite Fourier transform on `Cl`** — `L(s,ψ) = Σ_C ψ(C)ζ_C` and
`ζ_C = (1/h)Σ_ψ ψ̄(C)L(s,ψ)`, both directions longhand from orthogonality.

> ### **BUT THE BANKED RECOMBINATION LINE — `Π_ψ L(s,ψ) = ζ_K`, carried by the addendum AND by `§8.3` AND
> by this ferry's own `1(a)` — IS FALSE AT `h > 1`.** *Falsified longhand at the witness's own field:
> `−23 ≡ 1 (mod 8)` so `2` splits into two non-principal primes — `ζ_K`'s coefficient of `2^{−s}` is `2`,
> the product's is `(1 + ω + ω²)·2 = 0`.* ### **THE CORRECT IDENTITY: `Π_ψ L(s,ψ) = ζ_H(s)` — THE HILBERT
> CLASS FIELD's DEDEKIND ZETA. `ζ_K` is the single `ψ₀`-factor.** *(Orthogonality gives the factor
> `(1 − N𝔭^{−fs})^{−(h/f)}`, `f = ord([𝔭])`; the `ζ_H` naming is the splitting law, `NAVIGATOR-GRADE`.)*
>
> ### **CORRECTION TWENTY-THREE, FILED** *(`THE_ATTEMPT_RECORD`; strike-visible at the addendum table and
> `THE_CARRIER_BUILD` `§8.3`/`§6`)*. **The witness's per-piece grades are untouched — they grade pieces, not
> the recombination target — and the operation-like reading is STRENGTHENED: the two operations do not even
> land at the same object.**

**The corrected picture, and it is the section's real yield:** the two presentations are ### **two BASES of
one `ℂ[Cl]`-valued Euler product** `Z(s) = Π_𝔭 (1 − [𝔭]N𝔭^{−s})^{−1}` *(longhand: the class map is a monoid
homomorphism, so the coefficient function is completely multiplicative into the group algebra)* — group
basis: the partial zetas; idempotent basis: the Hecke `L`'s; ### **augmentation = `ζ_K`; determinant =
`ζ_H`.** *So the ferry's question "does the Fourier transform DESTROY multiplicativity?" answers NO,
exactly: it RELOCATES it — diagonal in the idempotent basis, convolutive in the group basis.* **The
placement forfeit sits at the linear combination: a mixture of `h ≥ 2` distinct Euler products inherits no
`Re s > 1` nonvanishing; the realized forfeit is the banked citation row. No new placement fact claimed.**

---

## §2 — ### **THE CLAIM TESTED BOTH WAYS · VERDICT: `(v)` COLLAPSES TO `(i′)` PLUS A RIDER ON `(iv)`**

**FOR** *(three steps, longhand)*: monoid-respecting piece = character piece = eigenbasis of the monoid
action, so "sees the monoid-respecting presentation" and "input natively multiplicative" are one property
read twice; the field's own carrier already lives on the product side (Weil's idele-class positivity, at
the species register's grade; CCM's quoted input); a monoid-input carrier needs no presentation input —
both presentations are coordinates of `Z(s)`.

**AGAINST** *(the steelman, pursued honestly)*: the frame that would hold both presentations as objects is
named — ### **the group algebra `ℂ[Cl]` with its two distinguished bases, i.e. `Z(s)` itself** — *and its
input type is the monoid-with-classes, not a presentation.* **Pursued to the end, the AGAINST branch is
the collapse's constructive form, not a counterexample.** *What it does prove: bare `(i)` is NOT enough — a
carrier can take the monoid and factor through the augmentation (norms only), satisfying `(i)` nominally
while silent on the witness's variable.*

> ### **VERDICT.** **`(i′)`** — *the content functor factors through the monoid at CLASS RESOLUTION
> (`ℂ[Cl]`), not through the augmentation; multiplicative recombination native, additive derived* — **that
> half of `(v)` collapses.** ### **THE NAMED RESIDUE, WHICH DOES NOT COLLAPSE: the rider on `(iv)` — no
> input choice guarantees the resolution SURVIVES INTO THE PAIRING; the manufactured pairing must not be
> augmentation-silent.** *Kin, compiled: `descent_carries_no_warrant`.* **`§6`'s suspicion and `§8.3`'s
> re-pointing both confirmed, both sharpened: `(v)` was `(i)` asked again downstream, and the downstream
> half is `(iv)`'s business.**

### **THE FRAMES RE-GRADED UNDER THE OPERATION QUESTION** *(the `(v)` column re-filled as the `(i′)` column)*

| frame | `(i′)` | the caveats that ride the grade |
|:--|:--|:--|
| **CCM** | ### **YES — native at the input** | *the ideles are the monoid GROUP-COMPLETED (what that forgets: UNGRADED); and the rider is exactly where Prop. 6.4's degeneracy sits — the warned failure mode, met in the wild* |
| **Deninger** | ### **YES — at the seat** | *generators native (orbits ↔ closed points, at abstract); CLASS RESOLUTION UNGRADED — the `H¹` read-debt grades nothing* |
| **THH / derived** | ### **BARRED** | *the level bar: graded UNREACHABLE, not ABSENT* |

> ### **NO RANKING MOVES; THE DIAGNOSIS MOVES.** *And the AGAINST branch's named object — the
> group-algebra-valued Euler product, kin at naming grade to Stickelberger-type group-ring zeta data
> (`NAVIGATOR-SUPPLIED`, naming is not reading) — is filed at question grade, `OURS`, NOT PROMOTED: a
> bookkeeping object with no involution, no pairing, no roster beyond the finite places.*

---

## §3 — ### **THE PHASE BOUNDARY, FILED AT GRADE** *(`THE_ONE_OBJECT` `§1-bis` · `F.2026-08-17f`)*

**The chain, derived and measured kept apart:** all-odd ⟹ bipartite ⟹ matching number, weight-silent
(**DERIVED**) · `p²` forces an even address, `k_{p²} = 2k_p` rounding-exact at `ω = 10⁻³` (**DERIVED**) ·
25/25 prime-power cells non-bipartite (**MEASURED**) · weight-dependent there, `W1`/`W2` (**MEASURED**).

> ### **TWO CAVEATS KEEP IT FROM AN OVER-CLAIMED IFF** *(and the ferry's own chain needed them)*: one-lag
> graphs are paths — bipartite at ANY parity; and a prime's even address is `ω`-ACCIDENTAL — `log 7` reads
> even at `ω = 10⁻³` and `7` is no prime power. ### **The prime power is the first FORCED crossing of the
> boundary, not the only possible one; the boundary itself is the graph property.**
>
> ### **THE TRANSITION IS THE APEX CELL: `L = 4 = 2²` is where `2 log 2` goes live** — *the boundary
> crossing and apex-at-`p²` are the same arithmetic fact read twice (`p²` in the window puts `2k_p` on the
> address list); cross-linked to `SIDE-window`'s compiled apex condition and to `F.2026-08-17e`'s priced
> discriminating cells.* **Coincidence-of-cells noted, not promoted — no mechanism derived.**

**Consequences filed on the sheet:** column `(i)`'s matching-number content is the **bipartite-regime
reading** and the full object lives beyond it for every `L ≥ 4`; column `(iv)`'s joint move would have to
be **built across the boundary**. *The two-class inventory's line is now located and cross-linked; the
silence-on-weights scope sentence is added in place at `F.2026-08-17e`, dated.* ### **`B3` as it stands:
NOT STABLE survives, robustly; "not even monotone" withdrawn as `(object, weighting)`-dependent.**

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **CORRECTION TWENTY-THREE:** the banked `Π_ψ L = ζ_K` is corrected to `ζ_H` — caught by the depth
   redo the residue flag invited, falsified longhand at the witness's own second coefficient. **The
   sum-vs-product observation is strengthened, not weakened.**
2. ### **THE `(v)` VERDICT AT DEPTH:** collapses to `(i′)` (monoid at class resolution, not through the
   augmentation) **plus the rider on `(iv)`** (the pairing must not be augmentation-silent) — the rider is
   the named residue no input choice can supply.
3. ### **THE FRAMES RE-FILLED:** CCM yes-at-input · Deninger yes-at-seat · THH barred. No ranking moves.
4. ### **THE PHASE BOUNDARY IS FILED AT GRADE** with its two caveats and its located transition (`L = 4`,
   the apex cell), and the silence-on-weights scope now stands in its FINDINGS entry plainly.
5. **The record:** `THE_CARRIER_BUILD` → `v0.4` (`§9`) · `THE_ONE_OBJECT` `§1-bis` · `THE_ATTEMPT_RECORD`
   correction twenty-three · `F.2026-08-17f` · `OPEN_TRAILS` ledger line · mirror rebuilt @ `467fbd2`,
   probes reported above.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. `WIDEN` PAUSED. NOTHING DEPOSITS.**
