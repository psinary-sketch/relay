# THE TECHNIQUE SPECIFICATION v0.1 — 2026-08-11

**Dedicated sitting. No computation shared the budget. Workshop-side. Nothing deposits.**
**RAIL DID NOT MOVE. `h2` unchanged.**

`phase2/method/THE_TECHNIQUE_SPECIFICATION.md` — sibling to the carrier spec, same discipline.

## The requirement table

`T1` semi-local extension, **boundary now DERIVED** — `supp(g⋆g̃) = [1/a²,a²]`, `pᵏ` enters at
`a=√(pᵏ)`, so CC's `(1/2,2)` is exactly the largest prime-power-free correlation window ·
`T2` the W-UNION quadrant · `T3` Euler-product consumption at Face E's **Tier-1 scope verbatim** ·
`T4` sign-type codim-1 · `T5` radical-quotient survival · `T6` `C5`-boundary compliance ·
`T7` gluing-datum consumption, **legs-only assemblies pre-refuted** ·
**`T8` MEASURED thin-margin** (collapse `0.169823 → O(10⁻⁴)`, arch share `1.0000 → 0.6581`) ·
**`T9` MEASURED window-kind** (index-vs-support `DISTINCT`; saturation inherited by any Li route) ·
`T10` near-degeneracy **at question grade, KIN-until-adjudicated, NO PROMOTION**.

## The candidates table

Toeplitz/Sonin (`T1` native, **`T2` its named arrest**) · Burnol (abstract grade, upgrade path
named) · spectral truncation (**located, UNREAD entirely**) · the corpus's two-channel route
(`T3`/`T4` native, **`T2` FAIL — no mechanism here either**). **No cell inferred.**

> ### `T2` is the column where the field's best candidate and this corpus's own route **both read
> `FAIL`** — different reasons, no mechanism either side. **That column is the residue, and it has no
> entry.**

**Written into the document:** it is **not a scoring system** (the degenerate-witness caution
transfers) and **not exhaustive** — *the table's emptiness is a statement about what has been read,
not about what exists.*

## Lenses

**16 table rows, 0 blank cells · 0 literal escapes · 0 mojibake · 1,255 words · 7,958 bytes ·
honest closing verbatim.**

## The QUESTION-FIT law, ratified

**A quantity must be able to fail, and its failure must bear on the question.** Three worked
failures in four acts: the **forced verdict** (`M`), the **disconnected verdict** (`W`), the **wrong
element** (three acts charting the bump instead of the autocorrelation).

> ### A calibration certifies that the instrument computes what it claims. **Nothing in the standing
> rules checked that the claim was the question.** A falsifiability audit catches the first and misses
> the other two. **All three were caught by the author, none by the executor's own checks** — a
> register can be scrupulous about grades, provenance and tolerances and still spend four acts
> measuring the wrong thing carefully.

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | this pass |
| relay | this report's commit |
| **rails** | **`a23ba23` / `de621b1` — both UNMOVED** |

**Standing: visibility `HOLD` · Weil 1952 second-hand · the `v0.5`/`v0.6` walk-sheet still open ·
direct register unchartered. Nothing deposits.**
