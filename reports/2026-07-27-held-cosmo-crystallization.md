# HELD DIFF SET — cosmology wave crystallization (W-COSMO-2; author-owned, Phase-2 cluster)

**Status: HELD for author ruling. NOTHING applied. The cosmology cluster is Phase-2 (Batch 004); its papers are staged-wave-adjacent, author-owned. Nothing deposits.**
Drafted 2026-07-27 (cosmology wave). Verb-to-certificate discipline; the RH corpus's rigor applied to the physics.

---

## SITE 1 — README / AGENTS currency (SIDE-cosmo): the axiom claim corrected at source

**BEFORE** (`AGENTS_SIDE-cosmo`, the verification note)
> The headline `Ω_b = 4/81` claim is verifiable … the prediction is `native_decide`-verified for the (2, 3, 2, 0) tuple, no axiom invocation beyond ZFC.

**AFTER**
> The headline `Ω_b = 4/81` claim is verifiable … the prediction is **`decide`-verified** for the (2, 3, 2, 0) tuple: `#print axioms` gives **`{propext, Quot.sound}`** for `xi_total`/`xi_visible`/`xi_dark`/`xi_wall_sq`/`xi_77` (no `native_decide`, no `Classical.choice` — pure decidable ℕ-arithmetic). The "no axiom beyond ZFC" claim holds honestly.

---

## SITE 2 — group-theory prose (FORMATION_DISTANCE_SYNTHESIS, QUATERNIONIC_DARK_SECTOR): generator-count, not GL-orbit

**Intent (per the 2026-06-05 audit).** The (3,3,1) partition of the seven Trivium elements is **generator-count arithmetic** — the weight of an element is the number of generators in its squarefree product over the fixed basis {−1, 2, 3}, a well-defined arithmetic quantity — **not** a parabolic-orbit / S₃-stabilizer group-theory fact. The diagonal is distinguished as **−6** (weight 3) via the kernel's `diagonal_unpaired` / `unique_diagonal`.

**Rewrite rule (both papers):** replace every "parabolic orbit" / "S₃-stabilizer" / "GL(3,𝔽₂) orbit" attribution of the (3,3,1) partition with "**generator-count over {−1,2,3}**, the kernel cardinalities `w1Count=3, w2Count=3, w3Count=1`, `hamming_decomposition`." The conclusion (the diagonal is distinguished; the partition is (3,3,1)) is unchanged and true — only its *justification* is corrected from group-orbit to generator-arithmetic.

---

## SITE 3 — Planck reference unified (corpus-wide)

**BEFORE** (downstream artifacts): `Ω_b = 0.04897 ± 0.0031` (older Planck reference).
**AFTER** (canonical): **`Ω_b = 0.04930 ± 0.00059`** (Planck 2018, arXiv 1807.06209: `Ω_b h² = 0.02237 ± 0.00015`, `H₀ = 67.36` → `Ω_b`), giving the prediction `4/81 = 0.049383` at **0.13σ**. Later data (ACT DR6 + DESI 2024, `Ω_b h² ≈ 0.0226`) keeps the agreement sub-σ. Uncertainty propagated from `Ω_b h²` + `H₀`.

---

## SITE 4 — grade-labels on the cosmological claims (the screen's output on the face)

Each cluster claim ships **with its screened grade** (W-COSMO-3): `4/81` etc. = **PROVED-arithmetic**; the Størmer chain = **FORCED-substrate** (zero-param); `4/81 = Ω_b` and `77/4 =` dark/visible = **PERMITTED-identified** (matched to data, not derived from cosmology); `1/12 = Ω_b/12` = **PERMITTED-fitted**; T7 = **marginal-null**. **Rewrite rule:** ensure each paper states its claim's grade on its face — the arithmetic forced, the cosmological identification honestly permitted (not "derived"). This is the physics-side application of the verb-to-certificate discipline.

---

## APPLICATION NOTE

All four sites are HELD. The cosmology cluster is author-owned (Phase-2 Batch 004); these diffs apply when the author rules the cosmology bundle. Nothing deposits; the RH keystones are untouched.


---
**APPLIED 2026-07-27** where targets exist (SIDE-cosmo AGENTS currency; STORMER dark/visible + Planck + grade; loom self-correction). FORMATION_DISTANCE parabolic-orbit target absent (already clean). Standalone cluster papers edited on disk (staging).
