# `W-ATTEMPT-2` · SITTING 4 — IS THE DIAGONAL SELF-DUAL
## ### **`D-YES` ON THE FINITE SIDE, SCOPED AS REGISTERED — SUPPORT AND PLACE MOVE AS ONE DATUM `a` ON THIS OBJECT — WITH THE SECTION'S WEIGHT CHANNEL PUNCTUATED BY PLACE-ARRIVALS, AND THE TAG RETIRED (CORRECTION TWENTY-FIVE)**

**Relay report · 2026-08-18 (twenty-fourth sitting) · author-called · attempt work RELAY-ONLY; item 1's
correction landed in the registers with the mirror rebuilt · nothing deposits**

> ### **THE CORRECTED STOP IN FORCE (restated): measured properties of constructed objects are DATA at
> bench grade; refused — promotion to `W_∞ − ΣW_𝔭` at complete roster, or register movement. SUB-GATE
> restated. The range law governed: every factor re-measured at its diagonal cell.**

```
PLACE-papers  origin/main : f247d85 (correction twenty-five only; mirror-refresh-2026-08-18.zip @ f247d85, probes PASS)
relay         origin/main : (this report)                              local +1 HELD
instruments   tools/e16/b17_attempt2_s4.py (registration banked BEFORE the run:
              data/b17_registration_2026-08-18.txt) · data/b17_2026-08-18.txt
```

> ### **RULE-3 LOG:** *sitting 3's table and diagonal ✓ · Tate at the Conservation-of-Spectra grade ✓ ·
> the tensor square ✓ — banked. Banned grep: clean; terms coined: none.*

---

## §1 — ITEM 1: ### **CORRECTION TWENTY-FIVE — THE TAG RETIRED, GREP CLEAN**

*"The four-word question" was a coined tag (navigator-introduced, author-caught — the same leak-shape as
correction twenty-four, one level up: term → tag). Replaced at every site with the content sentence —
**the missing codomain for the pairing and the conformal geometry the involution would act on
(self-product / Frobenius lift / leaves)** — across both road documents' `§0`/`§12`, three `FINDINGS`
entries, `OPEN_TRAILS`, and four relay reports; three line-wrapped instances caught on a second pass;*
### **negative finding: the charter carried NO instance.** *Grep clean, both repositories, both case
forms. Filed at `THE_ATTEMPT_RECORD`; the register commit is `f247d85`; the mirror rebuilt with today's
date-tag and probed.*

## §2 — ITEM 2: THE DIAGONAL, DEFINED BEFORE THE RUN

*`D(a)` = the support-forced section: one bound `a`, every place's cutoff `n_p(a) = #{p^k ≤ a²}` (the
staircase). Against the free product: the section VISITS one cell per `a` where the product holds all
cells — dimension and weight become functions of `a`;* ### **the coupling is cutoff-silent, so the
section KEEPS the class structure in full — the class resolution survives support-forcing untouched.**

## §3 — ITEM 3: ### **THE SELF-DUALITY VERDICT — `D-YES` ON THE FINITE SIDE, AS REGISTERED AND SCOPED**

*Registered longhand before the run: every local model is BUILT self-dual (the symmetric ball; the
certified closure), the archimedean window is symmetric at cite (Tate; the Conservation-of-Spectra
grade) — expectation `D-YES` by self-dual construction, with the split DECLARED: finite side measured,
archimedean side at-cite-by-symmetry, not modeled.*

> ### **MEASURED, both place sets, `a ∈ {1.3, √2, 1.5, √3, 2, 3}`: T-INVARIANCE OF THE SECTION AT
> MACHINE PRECISION AT EVERY DIAGONAL CELL** *(dense through dim 108; factor-certified, declared, at
> `a = 3` — dims 9408 and 150528)*; *closures per live factor at machine.* ### **SUPPORT AND PLACE MOVE
> AS ONE DATUM `a` ON THIS OBJECT: the cell, the coupling, and the weight are functions of `a` alone,
> and the transform respects the section exactly — the first construction of this attempt to move both
> axes as one. SCOPE, SAID WITH IT: at the model — finite place sets, finite cutoffs, the archimedean
> factor carried at cite, not modeled.**
>
> ### **AND THE MEASURED TEXTURE, THE SITTING'S HONEST NEW FACT: THE SECTION'S WEIGHT CHANNEL IS
> PUNCTUATED BY PLACE-ARRIVALS.** *Each newly-admitted place enters the diagonal at its DEAD EDGE
> (`n = 1`, the `(p⁰−1)` death), so the weight dies at every staircase step where a place arrives and
> revives only as the diagonal deepens past it: zero through `a = 2`; first alive at `a = 3` on
> `{∞,2,3}` —* ### **`Q(2,3)·Q(3,2) = 6√6 = 14.6969…` EXACT** *— and dead again at `a = 3` on
> `{∞,2,3,5}` (5 arrives at `n = 1`).* **On the support-forced object, the weight is not monotone in the
> support: it is a sawtooth in the PLACE-ARRIVALS — the diagonal's own version of the corpus's sawtooth,
> recorded plainly at bench grade.**
>
> *The coupling along the staircase: identity on the archimedean-only cell → `(2,−1,−1)` when only `2`
> is live → `(4,1,1)` once `2` and `3` are both live → untouched thereafter — the class structure enters
> exactly when the places do.*

## §4 — ITEM 4: ### **THE TRACE OBSERVATION** *(question grade, `OURS`, no promotion)*

> *The coupling spectrum `(4,1,1)` is the class-group-character TRACE of `Ẑ`'s label-norm coefficient —
> the class structure enters the glued object AS A TRACE over `ℂ[Cl]`, exactly the `τ`-route `B5`
> selected (`τ` class-separating, `ε` graded out by the rider).* **Cross-linked to `B5`'s selection
> principle and the `§9` ledger-side specification; promoted nowhere.**

## §5 — THE CYCLOTOMIC HALF's PRIORITY, RESTATED FROM THE OUTCOME

*With `D-YES` on the finite side, the attempt now has a STABLE object — the section with its pairing,
coupling, and transform-invariance — worth certifying:* ### **the cyclotomic half's priority RISES** *(it
would formalize the pairing on the section, closing step one of the closure protocol against the full
construction rather than its integer shadow). Still designed-not-built; ~1–2 sittings, design first.*

---

## CLOSING — RELAY-ONLY (item 1's register commit excepted and pinned), FOR THE AUTHOR'S WORD

1. ### **THE TAG IS RETIRED (correction twenty-five), grep clean, the charter's negative finding
   recorded.**
2. ### **`D-YES` ON THE FINITE SIDE, SCOPED: support and place are one datum `a` on the built object at
   the model — with the archimedean side at cite, not modeled, and the complete roster the double
   limit, open.**
3. ### **THE SITTING'S NEW MEASURED FACT: the support-forced weight is a sawtooth in the
   place-arrivals — each place enters dead and revives as the diagonal deepens.**
4. **For your word:** *(i)* the cyclotomic half (priority risen); *(ii)* the archimedean factor's model
   (the one unmodeled slot in the `D-yes` scope); *(iii)* whether the attempt's next sitting takes the
   punctuated-weight law to more staircase steps (`a² = 5, 7, 8, 16, 25, 27`) or turns to review.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — replayed on top of this report per the standing
procedure, absence from the public tree re-verified after this push. *Release: counsel, then your word.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO PROMOTION TO `W_∞ − ΣW_𝔭` AT COMPLETE ROSTER.
NOTHING DEPOSITS.**
