# TWO REPAIRS · CHECKPOINT `B5` — THE ARCHIMEDEAN PLACE FOR `Z`
## ### **THE `Γ`-FACTOR IS CLASS-SILENT, THE COMPLETED OBJECT IS SELF-DUAL, AND A LEDGER IS NOT A TRACE**

**Relay report · 2026-08-17 (ninth sitting) · author-ruled · `INSTRUMENT` track · sub-gate declared · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 5d8bbee21ea2194905f1d642547edcef2955b852   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 5d8bbee   ### 22-file roster + MANIFEST, probes PASS
              (one probe first ran with a mis-written regex and returned a false negative; re-run with the
              corrected string it passes — recorded per the false-negative law, not smoothed)
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

---

## §1 — ### **THE RENAME, AS RULED — AND THE CALIBRATION TITLE IS LAWFUL**

**`git mv THE_SIGN_RECONCILIATION.md → SIGN_ARRANGEMENT_RECONCILIATION.md`** *(matching its own H1;
history preserved; alias line added; the old filename's compression — "the sign, reconciled" beside an
open `h2` — named in the file itself).* **Citing sites updated by three-mode scan:** `OPEN_TRAILS` (path +
prose), `THE_MUTUAL_LIGHT_MAP` (prose + filename), **and the monograph's two provenance paths — path
currency only, no claim moves, the era-annotation text untouched.** *The other listed titles left as
ruled.*

> ### **`THE_CALIBRATION_IS_A_FROBENIUS_NUMBER` — VERIFY-AT-GRADE: TITLE LAWFUL, THE CLAIM IS AT GRADE.**
> *Which, exactly:* **(1)** the kernel exists and is built — `SIDE-spinor-calibration/
> SIDESpinorCalibration/FrobeniusCalibration.lean`, vanilla Lean 4, decide-only, per its own `§9`
> declaration; **(2)** the loom carries it twice — **L1787** (the calibration-arc audit: *"non-fire … the
> kernel documents the PROVED layer only … axiom-free"*) and **L3008** (*"COMPILED axiom-free, 0 sorry /
> 0 axioms"*); **(3)** the document's own `§6` + Status line separate the proved number
> (`‖v‖²·ζ(−1) = −1 = −g(2,3)`) from the interpretive spinor reading, explicitly not claimed as theorem.
> *One note for completeness: its subtitle carries "as one object" — lawful here because the one-ness IS
> the proved factorization (`§3`, one group order), not a presumption.*

---

## §2 — ### **CORRECTION TWENTY-FOUR: "AUGMENTATION-BLIND" → "AUGMENTATION-SILENT"** *(navigator-introduced, author-caught)*

**The term was a banned-term violation — an agent-metaphor doing a compression's work.** *Repaired at every
site, rephrased to content and not token-swapped:* ### **"augmentation-silent" — the pairing's value is
CONSTANT ACROSS THE CLASS RESOLUTION; it reads only `ε(Z) = ζ_K`.** ### **The vocabulary anchor is the
`S5`-silence precedent:** *the corpus's register for measured or derived non-dependence is SILENCE —
"silence-on-weights," "silent on the coefficients," "zero information about the spectral parameter."*

**Sites repaired:** the spec's `§2` rider + `§2.1`/`§2.2` · `THE_CARRIER_BUILD` `§2` rider + `§9.2`/`§9.3`
· `FINDINGS` `f`/`g` · `OPEN_TRAILS` ×2 · relay `v-at-depth` + `rename-and-seed` *(in place, this
correction the record)* — **plus the executor's own free "blind" prose from the same sittings**
*("class-silent shadow" · "silent on the witness's variable")*.

> ### **THREE DAY's-WRITE `blind`-STEMS REMAIN, LISTED NOT REPAIRED — EACH NEEDS YOU, NOT A SWEEP:**
> the compiled `descent_carries_no_warrant` gloss and its verbatim quotes *(kernel-adjacent register)*;
> the sixth sitting's `(v)` formulation and its echoes *(your-voice text from before the ruling)*; the
> charter filing's *"blind to coefficients"* *(whose content register already exists: "silent on the
> coefficients")*. **Full table: `THE_ATTEMPT_RECORD`, correction twenty-four.**

---

## §3 — ### **CHECKPOINT `B5`: THE ARCHIMEDEAN PLACE FOR `Z`** *(the spec `§6`, `v0.3`; corpus-first; both branches longhand)*

### **(a) THE `Γ`-FACTOR IS CLASS-SILENT — SO THE ARCHIMEDEAN PLACE ENTERS NATIVELY.** *Class-group
characters of imaginary quadratic `K` are unramified with trivial `∞`-type, so all `h` components carry
the SAME archimedean factor (at text; the addendum's `S3a`/`S3b` rows at cite) — and a diagonal operator
with equal eigenvalues is a scalar, longhand.* **`Ẑ(s) = γ_∞(s)·Z(s)`, `γ_∞` scalar: `s`-analytic
information, zero `ℂ[Cl]`-information.** *Scope plain (totally imaginary, trivial `∞`-type; ray-class door
named, not chosen). The `R3` `arch_independent` tension filed and cross-linked
(`THE_CARRIER_BUILD` `§9.5-bis`): a carrier built OVER `Z` would owe `R3` an archimedean coordinate with
content the finite places lack — which the class-silent `γ_∞` does not supply.*

### **(b) SELF-DUAL UNDER `s ↦ 1−s` ∘ ANTIPODE — YES AT CITE, TWISTED BY THE ROOT-NUMBER ELEMENT.**
*The `ψ`-wise FEs assemble into* `Ẑ(s) = W·Ẑ(1−s)^∗`, `W = Σ_ψ W(ψ)e_ψ`, *and the involution closes
longhand: applying it twice forces `W·W^∗ = 1`, which holds by `W(ψ̄) = W̄(ψ)`, `|W(ψ)| = 1` (at text).*
### **The involution `L1` demands is present on the completed class-resolved object AS STRUCTURE,
NATIVELY.**

### **(c) THE `τ`-PAIRING QUESTION.** *The `ℂ[Cl]`-valued Weil functional `W_Z(g) = Σ_ψ W_ψ(g)e_ψ`
scalarizes two ways (longhand: `ε(e_ψ) = δ_{ψψ₀}`, `τ(e_ψ) = 1/h`):* **`ε` reads the `ζ_K` ledger alone —
the augmentation-silent scalarization; `τ` gives the HECKE-SIDE LEDGER** — *on class-resolved test data it
diagonalizes character by character (Parseval: `τ(fg^∗) = (1/h)Σ_ψ f̂(ψ)ĝ̄(ψ)`), Hermitian under the
antipode-involution at the algebra level, each component classically Hermitian at text.*

> ### **WHAT ITS POSITIVITY WOULD ASSERT — SAID PLAINLY SO NOBODY READS A CONSTRUCTION AS A RESULT:
> component-by-component positivity IS GRH for the class-group `L(s,ψ)` (Weil's criterion, at text);
> jointly, GRH for `ζ_H`; the `ψ₀`-component alone, RH for `ζ_K`. THE FORM'S EXISTENCE IS CLASSICAL AND
> ITS POSITIVITY IS EXACTLY AS OPEN AS THE HYPOTHESES IT RESTATES. NOTHING HERE BEARS ON THE SIGN OF
> `W_∞ − W_2`.**

### **(d) VERDICT ON COLUMN `(ii)`, RE-FILLED.** **The completion half fills NATIVELY** *(class-silent
scalar, no import; self-dual completed object)*; ### **THE TRACE HALF IS STILL ABSENT — `(c)` SUPPLIES A
LEDGER, NOT A TRACE.** *An explicit-formula functional over zeros and primes is not the trace of an
operator on a space; turning the ledger into a trace is the field's own named open question (the suitable
Weil cohomology / Lefschetz reading, at cite), and claiming it here would claim that question solved.*
**`§2.1`'s `(ii)` row carries the supersession pointer; `(iii)` remains the empty column entirely.**

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE RENAME IS EXECUTED AND THE CALIBRATION TITLE IS RULED-CHECKABLE: LAWFUL, AT GRADE** — with
   the exact loom rows and kernel named.
2. ### **CORRECTION TWENTY-FOUR IS FILED AND APPLIED** — every site repaired to the silence register;
   three residual `blind`-stems listed for you.
3. ### **`B5` IS GRADED: the archimedean place is native and CLASS-SILENT; the involution is native on
   the completed object; the `τ`-form is the Hecke-side ledger whose positivity would BE GRH — a
   restatement, not a result; and the trace half of `(ii)` is STILL the empty half.** *Road B's live gap
   is now stated in one sentence: `Z` has the completion and the involution; it has no trace and no
   identification.*

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
