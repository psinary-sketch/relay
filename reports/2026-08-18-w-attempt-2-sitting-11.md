# `W-ATTEMPT-2` · SITTING 11 — WHAT IS 0.872 · THE `p = 3` TOWER
## ### **`E-ARTIFACT` IN ITS STRONGEST FORM: 0.872 IS THE LOG-GAUSSIAN'S NUMBER, NOT A CONSTANT OF `ℝ` — WEIL'S OWN POLYNOMIAL×GAUSSIAN IS THE BEST CALIBRATED WINDOW AND STILL LEAVES A DEFICIT OF 0.0141 — AND THE TOWER STRUCTURE IS `P-INDEP`: `ℚ₃` CARRIES IT ALL, 160/160 EXACT, WITH FLATNESS MEASURED AND THE CLASS ALIVE AT ITS ARRIVAL DEPTH**

**Relay report · 2026-08-18 (thirty-first sitting) · author-called · attempt work RELAY-ONLY ·
nothing deposits**

> ### **THE CORRECTED STOP AND THE PROTOCOL CORRECTION, carried. Everything below is DATA — float,
> declared, at `∞`; exact at the finite places. No sign at complete roster; the register untouched.**

```
PLACE-papers  origin/main : b7acc3e (UNTOUCHED this sitting)
relay         origin/main : (this report, the sitting-11 batch)          local +1 HELD
instruments   tools/e16/b25_attempt2_s11.py (float, declared; registration banked BEFORE the run;
              122/122 asserted checks) · tools/e16/b26_attempt2_s11.py (160/160 EXACT, one
              declared-skip) · data/b25_*, b26_* (registrations + runs, 0 non-ASCII)
```

> ### **RULE-3 LOG:** *all head cites banked at `8129e0f` ✓. The registration said IN ADVANCE that the
> unconstrained window-sup is trivially 1, so the sup-question was posed within the CALIBRATED family
> only. Terms coined: none; banned check clean.*

---

## §1 — ITEM 1: ### **THE DEFICIT IS THE MODEL'S — `(E-artifact)`, WITH THE SUP-QUESTION ANSWERED INSIDE THE CALIBRATED FAMILY**

*Seven calibrated shapes × four `a` × three `N` (FFT-based centered DFT verified against the dense
matrix; power iteration with restart agreement; 122/122 checks):*

1. ### **`σ_max` MOVES WITH THE CALIBRATED SHAPE BY ~0.5 ABSOLUTE at every `a`, against N-stable
   tolerances of `10⁻²`–`10⁻³`** — *0.87182 (8 N-stable digits, sitting 10's five confirmed and
   extended) is a property of the log-Gaussian at `σ_w = ln a`, not a number about `ℝ`.*
2. ### **THE BEST CALIBRATED WINDOW AT `a ≥ 2` IS WEIL'S OWN POLYNOMIAL×GAUSSIAN (`W-wp1`,
   `(1 + u²/2)e^{−u²/2}`): `σ_max = 0.985863747` at `a = 3` — AND NOTHING REACHES `1 − 10⁻³`.** *The
   exactness deficit survives the whole calibrated family: `0.0141` at the best shape. The 1952
   lemma's own mechanics comes closest; it does not close the gap at the model.*
3. ### **AN EXACT SCALING IDENTITY FELL OUT UNBIDDEN:** *the log-Gaussian family depends only on
   `(width) × (ln a)` — `σ_max(σ_w = ln a/2, a) = σ_max(σ_w = ln a′, a′)` at `a′ = a²`, equal to all
   12 stable digits — the calibration's own scaling law, observed twice in the table.*
4. **The candidate comparison (fixed list, no fitting): NO CLOSED FORM FOUND** — *best point candidate
   2 digits (`cos(1/2)`, `√3/2`, both `~6×10⁻³` off); best curve candidate `erf(ln a)` fails the
   four-point curve test by `6×10⁻²` (its `a = 3` closeness is single-point, explicitly not a
   finding).* *(A moot comparison anyway once `(E-artifact)` landed — recorded for completeness.)*

## §2 — ITEM 2: ### **THE ∞-WEIGHT'S STABLE HOME IS STILL NOT FOUND — REGISTERED EXPECTATION FALSE, FLAGGED NOT SMOOTHED**

*`Q_∞^soft = ‖G D G‖_F`: the Gaussian SUPPRESSES the raw channel's banked `~N^{1.5}` growth to
`~N^{0.75–0.79}` — but does not arrest it (column sweep verified against dense to `6×10⁻¹⁴`; the
operator's behaviour, not an artifact).* ### **`Q_∞^soft` is a grid quantity, not yet a number.** *The
ledger line is carried WITH that caveat: the `∞` channel alive and nonzero at every `a`; the finite
side's exact zeros still annihilate the product wherever they sit (full weight `0` through `a = 2`;
`762.0 × 6√6` at `a = 3` three-place at `N = 4095`; `0` four-place). The candidate next form (an
`h`-weighted Hilbert–Schmidt convention, i.e. the continuum normalization the raw grid sum lacks) is
NAMED for the author's word, not run.*

## §3 — ITEM 3: ### **THE `p = 3` TOWER — `(P-indep)`, 160/160 EXACT: SITTING 10'S STRUCTURE IS THE FINITE PLACE'S SHAPE AS SUCH**

| claim | `ℚ₃`, levels 1–4 |
|:--|:--|
| `T1` tower compatibility | ### **EXACT at all four levels** *(1.55M exact `ζ_{3^{10}}`-sums at `n = 4`, attempted not skipped)* |
| `T2` pairing level-stable | ### **ON THE NOSE at `n = 1–3`** *(`n = 4` DECLARED-SKIPPED: projected 591 s vs the registered 420 s budget — the budget was NOT raised after seeing the projection)* |
| the fifth law | ### **VERBATIM: `Q² = 0, 12, 192, 2028 = 3(3^{n−1}−1)²` exactly** *(with the shell law derived-then-checked; at `n = 1` every shell is annihilated and `Q² = 0` is the law's own value)* |
| localization traces | **`0` exactly, `k = 1, 2`, both hosts, all levels — stable** |
| `T5` forced compression | ### **EXACT NONZERO WITNESSES, every column (`4/4`, `64/64`) — the compressed scaling is forced on `ℚ₃` too** |
| eigen-dims | ### **FLAT AT EVERY LEVEL, MEASURED: `d_λ = (3ⁿ−1)²/4` each — with the mechanism PROVED in-run** *(`tr M` lands rational — `ℚ(ζ_{3^{2n}}) ∩ ℚ(i) = ℚ` forces `dᵢ = d₋ᵢ`; `Π` is fixed-point-free because `q = 3ⁿ` is odd — the `p`-odd pattern is now a two-line theorem-shape, not an observation)* |
| the class at arrival | ### **ALIVE: `d₁(3,1) = 1 > 0` — the arrival-depth DEATH is `p = 2`-SPECIFIC** *(a statement about which sectors exist, verified as registered)* |

*(Method notes disclosed in-file: brute-force depth shallower at `p = 3` by field-degree necessity —
`M² = Π` brute-forced at `n = 1`, DERIVED with its inputs checked at `n ≥ 2`; the two-term cyclotomic
fold verified against the independent power table at every exponent through `3¹⁰`.)*

## CLOSING — RELAY-ONLY, FOR THE AUTHOR'S WORD

1. ### **0.872 IS RETIRED AS A CANDIDATE CONSTANT — the deficit is real but its VALUE is the window's;
   the window-independent fact is the RESIDUAL: no calibrated shape reaches exactness, and Weil's own
   shape comes closest (`0.0141` short).**
2. ### **THE FINITE-PLACE TOWER STRUCTURE IS `p`-INDEPENDENT; the `p`-DEPENDENT items are exactly the
   sector arithmetic's (`p = 2`'s `i`-trace vs `p`-odd flatness; the arrival-death `p = 2`-specific).**
3. **For your word:** *(i)* the `∞`-weight's next convention (the `h`-weighted Hilbert–Schmidt form,
   named); *(ii)* whether the `W-wp` family's polynomial degree is worth sweeping (does the deficit
   `→ 0` in the lemma's own family, or floor out?); *(iii)* the `T2 n = 4` re-run at an honest larger
   budget, if wanted.

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO SIGN AT COMPLETE ROSTER. NOTHING DEPOSITS.**
