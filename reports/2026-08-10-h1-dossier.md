# `W-ORD-JOIN` BANKS AT THE `H¹` DOSSIER — 2026-08-10

**Banking pass. Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

> ### **THE JOIN'S REMAINING SPAN IS `H¹`, CONSTRUCTED FROM THE GLUING DATUM.**
>
> **The completest constraint set held anywhere, AT CITE — and the claim is about ASSEMBLY, not
> about depth.** *Every row is somebody's published theorem, this programme's compiled terminal, or
> explicitly labelled as ours. Assembling them in one place is the contribution; not one of them is.*

## §1 — THE DOSSIER

| # | row | grade |
|--:|:--|:--|
| 1 | **Deninger's axioms**: `Θ=0` on `H⁰` · **zeros on infinite-dim `H¹`** · trace iso, `Θ=id` on `H²`; **2.12 at `s=½`** | **AT CITE** |
| 2 | **his NO-GO**: no real structure on `H¹`; **quaternion mechanism at the ramified real place**; **his own foliated route cut** | **AT CITE** |
| 3 | **FORCED SHAPE: ℂ-coefficients, infinite dimension** — published support beside the compiled results | **AT CITE + COMPILED** |
| 4 | **the two legs**: `Γ_ℝ` rationally with the `√2` bookkeeping; Euler factors | **COMPILED (∞) · AT CITE (p)** |
| 5 | **division of labour**: circle → ladders **SOURCED**; glue → `H¹`, **compact-cannot-carry LABELLED-OURS** | **MIXED, each half marked** |
| 6 | **`s`-dark seal** + **four-column screen** | **COMPILED + OURS** |
| 7 | **spectral account**, half-resolved | **QUESTION** |

## §2 — THE `ℤ/2`-AT-THE-REAL-PLACE PATTERN *(question grade)*

**(i)** the no-go's **involution failure** at a **ramified** place · **(ii)** CC's **`F_∞` fixed
points** · **(iii)** the **`√2` halving** (`(√2)² = 2`, Serre's `Γ_ℂ` factor) · **(iv)**
**sign-of-the-rational torsion** (`components_faithful` at `−1`).

> **`SAME` / `KIN` / `DISTINCT`, BY TEXTS. The merge discipline governs.**
> **Four things wearing `ℤ/2` at `∞` is four things until a text says otherwise. NO PROMOTION.**

## §3 — THE REGISTER'S STATE

> ### **`EXTRACTION-COMPLETE, ATTEMPT-SPECIFIED`.**
> **Further motion on `Q1` is CONSTRUCTION PROPER — `H¹` from the glue: genuinely new mathematics,
> author-ruled when ruled.** **`Q2`–`Q3` unchanged behind it. `h2` outside every arrow.**

**Carried, not dropped:** fracture square + `HH(ℚ/ℚ)` named-unsourced · Deninger Invent. 104
author-side · the *Motives* (Seattle) cousin · Blumberg–Mandell's sentence.

## §4 — THE BOARD FOR ROTATION *(no ranking)*

**(a)** the `H¹` construction attempt as the next research arc · **(b)** the carrier-spec document
takes the dossier as its **v0.3 section** (wave-bound, **digestion-first**) · **(c)** `W-ORD-SLOPE`
at threshold · **(d)** rest, all banked. **The author rules.**

## §5 — FOUND-BEYOND

> **The dossier's honest shape is that its strongest rows are all NEGATIVE OR CONDITIONAL.**
> Rows 1–3 are **an axiom list nobody has satisfied, a no-go, and a forced shape**; row 4's legs are
> **real but were never the difficulty**; rows 5–7 are **half-ours**.
> ### **The one row that would be positive — `H¹` itself — is the row that does not exist.**
> *Recorded so the dossier is not mistaken for progress toward the object.*

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `0a88aac` |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing constructed. Nothing deposits. `h2` exactly where it was.**
