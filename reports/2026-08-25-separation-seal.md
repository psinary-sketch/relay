# b170 — THE SEPARATION SEAL

**Registration `0bec3be`, banked after the owner reads and before the derivation.** 2026-08-25.
Ferry part 1 of 1, receipt confirmed in full before execution.

> ### **THE VERDICT IS SEALED: the class of member-separating statements expressible in the
> requirements' own vocabulary is EMPTY**, and item 1's choice is definitional ***as a derived
> result rather than as a survey outcome.***
> ### **A SEAL IS NOT A RULING. It says the choice is definitional; it does not make the
> choice.** *Core `292 → 296/296`, row 76.*

---

## THE GATE RAN FIRST, AND IT FIRED

The ferry required the renamed-conclusion gate on the characterization **before it was used**.
### **It partially fired, and the firing is carried.** b158's class (iii) already states the
characterization's first half in its own words — *"the parameter lives entirely in the sector
split; nothing summed over sectors can reach it — which is b154 restated"* — ### **and b158
explicitly refused to sell it as a finding. This act inherits that refusal**, cites the half to
b154/b158 at every use, and presents it nowhere as derived here.

### **What survives the gate as new:** the **coefficient-vector** form, which makes the
vocabulary itself the object of study rather than the criteria; the **closure**, from *four
classes returned nothing* to *the class is empty*; and the identification of ### **which
requirement the seal actually rests on.**

---

## COMPONENT 1 — THE CHARACTERIZATION

**Step 1.** A member is a choice of share vector `u` on the simplex `Σu_n = 1` and nothing else
— `a_n`, `b_n`, `A` and `resid_N` are the data, and R3 says the two mode-diagonal objects
*"carry NO apportionment freedom"*.

**Step 2.** For any coefficient vector `c`,
`Σ_n c_n w_n = [Σ_n c_n a_n − Σ_n c_n b_n] − (Σ_n c_n u_n)·resid_N`, and the bracket is
member-independent. ### **So the entire member-dependence of any owner-expressible quantity is
the single number `Σ_n c_n u_n`, and `c` is the whole of what the vocabulary contributes.**
*(b166: this survives the whole downstream chain, because every operation in it is linear.)*

**Step 3 — the criterion, in one line.**
### **A statement separates two members iff `Σ_n c_n u_n ≠ Σ_n c_n u′_n` for some coefficient
vector the vocabulary can express.** Hence **constant `c` ⇒ cannot see the member** (the gate
makes `Σ_n u_n = 1` for all), and **non-constant `c` ⇒ separates something**.

### THE ENUMERATION — b58's SCHEMA SHAPE, APPLIED

*b58 closed row 29's only-if "by the registered schema — `q ≥ 5` longhand; `q = 4`
exhaustive-exact; `q ≤ 3` vacuous, **stated, not hidden**". Three parts, applied here:*

| part | here |
|:--|:--|
| **general, longhand** | steps 1–3: every quantity affine in one number; constants cannot see it |
| **finite, enumerated exactly** | the requirements list **is** finite — R1–R8, each decided below |
| **degenerate, stated not hidden** | within-sector reshuffles are separated by nothing (row 69); and ### **if the truncation remainder were zero the question is vacuous — and the record cannot say whether it is** (b169: `R_N` is undefined) |

**R1, R8** completeness — and on the apportionment side completeness **is** the void gate,
which forces ### **the normalization alone** (b154, b159). **R2** shape, met by every member.
**R3** carries no freedom. **R4** *names* a decomposition, attributes neither part. **R6** names
*which* sector. **R7** well-formedness — `W₊(μ)` is real for every real `μ`. And ### **R5, the
`t(n)` warrant, is the ONE separating statement in the list — non-constant `c` — which R4
confines to the DIVERGENT part.**

---

## COMPONENT 2 — THE VERDICT, AND ITS FILING

*Extracted from the loom at run time, not retyped:*

### **(v-bis) THE SEPARATION SEAL — filed 2026-08-25 (b170)**

*Part (i) says a member must be fixed by definition **because no owner-quotable requirement
selects one**. That was a survey outcome over four criterion classes (b158).* ### **It is now a
derived result.**

**The characterization.** Every object the requirements can speak about is a linear functional
of b38's per-mode terms `w_n = a_n − b_n − u_n·resid_N`, so it equals a member-independent
constant minus `(Σ_n c_n u_n)·resid_N`. ### **A statement separates two members ONLY through
that one number, and the coefficient vector `c` is the whole of what the vocabulary
contributes.** A **constant** `c` gives `Σ_n c_n u_n = Σ_n u_n = 1` by the void gate —
### **so every sector-summed statement is cannot see any member.**

> ***THE FIRST HALF OF THAT IS INHERITED, NOT NEW, AND IS CITED RATHER THAN RE-SOLD:*** b158's
> class (iii) already says *"the parameter lives entirely in the sector split; nothing summed
> over sectors can reach it — which is b154 restated"*, ### **and b158 refused to sell it as a
> finding. This filing inherits that refusal.**

**The enumeration.** Applying b58's own schema shape — *general case longhand, finite case
enumerated exactly, degenerate case stated not hidden* — to the requirements list, which **is**
finite: **R1** and **R8** are completeness statements, and on the apportionment side
completeness **is** the void gate, which forces ### **the normalization alone**; **R2** is a
shape requirement met by every member; **R3**'s two objects carry no freedom by their own
words; **R6** names *which* sector, not how much; **R7** is well-formedness, and `W₊(μ)` is real
for every real `μ`; and ### **R5 — the `t(n)` warrant — is the ONE separating statement in the
list, which R4 confines to the DIVERGENT part.**

> ### **THE VERDICT: SEALED. The class of member-separating statements expressible in the
> requirements' own vocabulary is EMPTY**, and the choice of member is definitional ***as a
> derived result rather than as a survey outcome.*** *Decided in Core: two configurations
> agreeing on the divergent share and on every sector-summed total, differing only in the
> remainder's share, give different `W₊` — and both satisfy the gate.*

> ### **AND THE SEAL'S SCOPE, AT THE VERDICT AND NOT IN A FOOTNOTE:**
> ### **(i) It seals the NAMED VOCABULARY only.** *A definition made from outside it is
> untouched — `μ = ½` separates members perfectly well;* ### **it is not owner-quotable, and
> writing one down IS the definitional act.**
> ### **(ii) The vocabulary is a DOCUMENTED set, not a mathematical space.** b58's
> exhaustiveness ran over a closed mathematical set; ### **this one runs over what the record
> says. If an owner asserts a new requirement, the vocabulary grows and the seal must be
> re-run.**
> ### **(iii) THE LOAD-BEARING STEP IS R4, AND R4 IS A READING** — of the owner's own phrase,
> *"the divergent + truncation residual"*. ### **Read R5 as applying to the WHOLE residual and
> it would fix `μ` outright and the class would not be empty.** ***A seal resting on a reading
> is weaker than one resting on a theorem, and this one rests on a reading.***
> ### **(iv) A SEAL IS NOT A RULING.** It says the choice is definitional; ### **it does not
> make the choice, does not narrow the family, and does not prefer a member.**

---

## THE KERNEL

`SeparationShadow`, four terminals, zero axioms, **first build**. ### **They do not duplicate
row 69 — checked at content before writing:** row 69 decides the gate and the one-scalar
factoring over a **single** residual; these give the residual **R4's two parts with independent
shares** and decide that fixing the divergent share leaves `W₊` free while both configurations
satisfy the gate. Witnesses polarity-verified before any Lean was written (6 positive, 4
negative).

---

## THE DEVIATION — **the scanner caught a barred stem inside a kernel name I had just minted**

### **The pre-ship review returned NOT CLEAN on this act's own files.** Four live uses of a
barred stem — and one of them was the name of a terminal this act had minted,
`void_gate_blind_to_both_shares`, ### **which had already reached the Lean source, `AllPrints`,
`AXIOM_PRINTS`, the correspondence row and the loom filing.**

> ### **THE EXCEPTION WAS NOT AVAILABLE AND WAS NOT CLAIMED.** Rule 3 excepts **quoted** kernel
> identifiers; ### **minting a new one that carries a barred stem would launder the ban through
> the very exception that exists to preserve history.**

Renamed to `void_gate_cannot_see_either_share`, every prose use replaced with the record's own
repair wording, the module rebuilt, `AXIOM_PRINTS` regenerated, and the sweep re-run clean on
both scopes before anything shipped. ### **The word I reached for was the obvious one for the
mathematics — which is exactly the condition under which a ban has to do its work, and it did.**
***The failing run is retained beside the two passing ones.***

**This is the fourth time the discipline has caught its own author (b115, b117, b119, b170), and
### the first time it caught one inside a compiled kernel name.**

---

## THE SEATS, SCORED

**NAVIGATOR: P1 and P2 land**, and P2 is held to the higher bar the ferry set — the scope fenced
at the verdict, the transfer disclosed in advance, the load-bearing reading named.

**EXECUTOR: all five.** (e1) SEALED via coefficient vectors — **lands**. (e2) the gate partially
fires — ### **lands, and against the act**. (e3) ### **the load-bearing step is R4, not R5** —
**lands**, and it is the most useful thing the act found. (e4) Core enters without duplicating
row 69 — **lands**. (e5) ### **I did not predict the seal would survive its own higher bar**,
and registered UNDECIDED as acceptable. ### **It survived — and the refusal to grade myself into
it in advance is why the survival means anything.**

---

## THE THIRTY-SIXTH SEAM'S DEBT

### **Item 1 is now a definitional choice as a derived result, and the ruling is the whole of it
— there is no further research step inside the vocabulary.** Route B's external read waits on
your authorization (b169). ### **The Q-route `Ψ` instability, still unlocated** — the instrument
lane's standing open. b157's six findings; the three front-door items, drafted at b166; **the
dominance second asking, not spent**; the tense hazard filed at b169; the methodology day; and
the deep items reserved for a session with breadth.

---

## THE AUDITS — EMITTED, NOT TYPED

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b170
  run at    : 2026-08-25T19:26:24 (local)
  input     : whole file b170_registration_2026-08-25.txt (created this act)
  input     : whole file b170_separation_seal.txt (created this act)
  stems     : gap, blind
  files     : 2
  lines     : 309
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 18bd307090c3b61fdf33f303804b01c6
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b170-docs
  run at    : 2026-08-25T19:25:05 (local)
  input     : added lines in D:/MY-DOwnloads/PLACE-papers vs HEAD
  stems     : gap, blind
  files     : 1
  lines     : 48
  hits      : 1
  live uses : 1
  VERDICT   : NOT CLEAN
  self-hash : sha256/32 a6ebf2ef2b83c53f6d9ee0afd1ae92b7
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : mirror_verify
  act       : b170
  run at    : 2026-08-25T19:27:42 (local)
  input     : mirror-refresh-2026-08-25.zip
  files     : 33
  rows      : 33
  mismatch  : 0
  declared  : 671a517
  ls-remote : 671a51789046
  VERDICT   : CLEAN ON BOTH CLAUSES
  self-hash : sha256/32 bd0dc1db3f6924597e6add325c500041
=== END AUDIT SIDECAR ===
```

*The relay self-description check was run at the commit and is not embedded: its subject is the
commit that would contain it, so embedding it cannot reach a fixed point. It appears in the
verifier as **UNUSED**, which is run-but-not-quoted and not a failure.*

---

## PINS AT CLOSE — by `ls-remote`, never from recall

| repository | pin |
|:--|:--|
| `PLACE-papers` `main` | `671a5178904694c73e9562ef092d64f3b6423b88` — the seal filed at the brief |
| `SIDE-global-section` `main` | `a70879d95bffba874afbe096f0592ae82b1eddf5` — ### **Core 296/296, row 76** |
| `relay` `main` | `e7a288c4883be2177edaaaedf8d6267bb2843a6b` — **the act; read back by `ls-remote`** *(this pin line is the commit after it)* |
| mirror | `mirror-refresh-2026-08-25.zip` — **33 files**, rebuilt at `671a517`, **CLEAN ON BOTH CLAUSES** |
| HELD | `held/carrier-acts` = `6eada6a` — **LOCAL-ONLY, unpushed** |

**Load this export:** `mirror-refresh-2026-08-25.zip`.

*STOP — the ferry's end.*
