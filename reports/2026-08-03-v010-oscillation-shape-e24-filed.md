# Keystone v0.10 · E-23 the oscillation's shape · E-24 filed (the selection coordinate) — 2026-08-03

The ferry's three moves. Pins at open: PLACE-papers = `1467b95`; relay = `9ea8f38`; lv
`14720d9`, kernel `44895f9` — unmoved; rail at the post-rename baseline. Nothing deposits.

## Registered expectation (VERBATIM from the ferry, recorded BEFORE analysis)

**E-23:** *"the ζ oscillation carries pair-correlation structure the control lacks (the
shadow observed inside the string — three maps meeting); the null branch (no GUE shape at
reachable depth) files the depth bound as the datum. Truncation honesty throughout — k ≤ 16
is a short series; claim only what the window supports."*

## MOVE 1 — THE KEYSTONE'S LOADED TOUCH (v0.9 → v0.10)

### TOUCH-PENDING

## MOVE 2 — E-23: THE OSCILLATION'S SHAPE

**Instrument** (`tools/e16/oscillation_shape.py`; data = the recorded dps-400 depth-16
diagonal and the control's mean; fresh zero-gap data; Pearson probes at lags −2..0;
truncation honesty: n = 14, |r| and n reported, no significance theater).

**What IS established (strong):** the oscillation is ARITHMETIC — rms 0.0842 against the
control's 0.0015: **56× the null**, and since the control shares the exact density and
differs only in lacking the zeros' fluctuation structure, the oscillation is carried by that
structure BY ELIMINATION. The arithmetic object is real and measured.

**What does NOT resolve at this depth (the registered shape claim):** the fixed-lag spacing
probes give corr(o_k, gap-after) = +0.04, (gap-before) = +0.21, (two-before) = −0.45,
(adjacent-mean) = +0.32 — no strong single-lag structure on 14 points. The Jacobi diagonal
is a NONLOCAL functional of the measure; a local-gap probe was the cheapest lens and it does
not capture the shape. **The registered claim adjudicates SPLIT: the shadow's PRESENCE inside
the string is established (the elimination argument); its pair-correlation SHAPE is not
observed at reachable depth — the null branch files the DEPTH BOUND as the datum.** Named
for the continuation (priced on call): deeper strings (larger k) and the correct nonlocal
transfer kernel (gap structure → Jacobi diagonal) — with the kernel, the shape question
becomes well-posed; without it, k ≤ 16 supports presence, not form.

## MOVE 3 — E-24 FILED (NOT RUN)

**E-24 — THE SELECTION COORDINATE (research-reach, ~1.5; FILED, NOT RUN).** Construct or
refute: a normalized self-datum π₀ for ξ satisfying the template's three clauses — (a)
SQUARE-FREE-BY-SYMMETRY: π₀'s square appears freely via the FE's u = z² evenness; (b)
ATTACK-PRICING: the wall-approach quantities (flip depths, margins, the dBN drift) scale in
π₀'s powers; (c) WINDOW-CURRENCY: the knife-edge widths denominate in π₀'s units. **Candidate
construction path (noted, not run):** the string's own mass/oscillation data — s₁ (the
normalized first moment), the diagonal's density mean, and the measured arithmetic
oscillation are the meter's computed ingredients; the coordinate, if it exists, is plausibly
a functional of them (the toy's p₀ IS its certificate's first datum). **Entry criteria:** a
precise candidate functional + one clause verified at computation grade. **Stopping rule:**
either one clause PROVEN to fail for every functional of the string data (the refutation —
itself first-class: it would say ζ's selection statement cannot take the toy's form in the
meter's coordinates), or the priced budget (~1.5) exhausts with the obstruction named. Per
the arc charter; on the slate armed.

## MOVE 1 EXECUTED — keystone v0.9 → v0.10 (recorded here with the closing)

One pass, three-deep, free-writing law observed: the one-law verdict into the measured
anatomy (kind-indexed, five strata, window-in-bracket); the density/arithmetic decomposition
with the control methodology on its face; **the currency clause into §1's wall description**
(the wanted poster: Q comes priced in a currency; the missing coordinate and the missing
object are one absence at two altitudes) with the Forward's question (iii) upgraded to the
vacancy + E-24. Correspondence re-run at pin level (lv `14720d9`, kernel `44895f9` unmoved);
three rows join; no kernel terminal touched.

## CLOSING — pins, slate, board

**The slate re-printed:** E-23 — **RUN, SPLIT: the shadow's presence in the string
ESTABLISHED (56× the null, by elimination); its shape UNRESOLVED at k ≤ 16 — the depth
bound filed; continuation = deeper strings + the nonlocal transfer kernel** · **E-24 FILED,
armed (~1.5): the selection coordinate — construct or refute; the string's data the
candidate raw material** · E-3 (the additive recipe kind-indexed; c₁ opaque; the n = 56
coefficient sampling priced) · E-7b (two cells) · E-16 (d(t) exponent) · E-20 (the continuum
normalization; the oscillation kernel) · E-1 · E-2 · E-4 · E-5 · E-11 · E-12 · LY-REP-A ·
Face-E Tier 2 · the ξ-sweep. **Consolidation DEFERRED, standing.**

**The board restated, the oscillation verdict at its head:** THE ARITHMETIC IS IN THE STRING
— 56× above the density null, carried by the zeros' fluctuations by elimination — and its
SHAPE is beyond the current depth: presence proved, form open, the transfer kernel the named
key. The keystone carries the arc at v0.10 with the currency clause on the wall's face; E-24
stands armed as the vacancy's construct-or-refute.

| repo | pin |
|:--|:--|
| PLACE-papers | `1467b95` at open → this sitting's commit (keystone v0.10 + OPEN_TRAILS addendum) |
| SIDE-lv-conservation | main = `14720d9` — unmoved |
| SIDE-kernel | `44895f9`; v1.7 — unmoved |
| relay | this report's commit; instrument `tools/e16/oscillation_shape.py` |
| rail | untouched — at the post-rename baseline |

Mirror rebuilt at the papers pin on commit. **The handoff package, refreshed in one line:
a substitute orients from the newest mirror (this sitting's PLACE-papers commit) + the
keystone at v0.10 + the latest relay report — the mast is the six-world wall, the one-law
toy, the audited string, and the vacancy; ready whenever credits call it.** Nothing
deposits.
