# The wave-edit runs — BURIAL re-tested · four checkpoints · the work-orders — 2026-08-05

Pins at open: PLACE-papers `fc95ad3`; relay `400fb7f`; lv `2f71068`; kernel `5e668b4`.
Rail freeze lifted **for this pass only**; closing check is a stated diff per document.
Census worker undisturbed throughout. Nothing deposits.

*Ferry correction received mid-pass and applied: item 1 routes to **BURIAL's** refusal criterion
throughout, not head 4's. That is how it was read.*

---

## §1 — BURIAL RE-TESTED AGAINST R4'S MEASUREMENT: **not a second face**

**BURIAL's criterion:** *a face must state the height it reaches, the depth it requires, and the
measured or derived relation between them.*

**R4's law meets all three components** — height γ, depth n ≈ 5.9·γ²/δ, relation measured across
δ = 0.4533 (n = 3379) and δ = 0.1 (n = 15868) with the multiplier constant at 5.77 / 5.98, and
δ ≤ 0.01 past the 40000 cap. On the criterion alone it is admissible.

**But it is a measurement of the face already admitted, not a second one.** The Li/√n face is
*λₙ at depth n probes zeros to height √n* — the **same statistic** (λₙ) in the **same relation**
(depth ∝ γ²). R4's run measures precisely what that face's two derivations predict; it does not
supply an independent height–depth pair from a different object.

**What it does add, and it is worth recording: the constant.** The face moves from
**derived-twice** (Cayley detector law; Lagarias' `S_fin(n) = λₙ(√n)`) to **derived-twice and
measured-once**, with n = 5.9·γ²/δ on the nose.

**Consequence for the consolidation, which is why this ran first: BURIAL stays
proposed-with-ONE-face, better evidenced. Its promotion test — do the exponents agree? — remains
UNRUNNABLE, because agreement needs two exponents and there is still one.** The consolidation
therefore carries one face, not two. **No promotion, as ruled.**

---

## §2 — THE FOUR SCOPE ITEMS: one checkpoint each, diff stated

| # | document | diff | what landed |
|:--|:--|:--|:--|
| **(a)** | `THE_RESIDUE_OF_RH.md` | **+1 line, 0 removed, 0 figures moved** | §6 gains the five-vocabulary reading (supplier = the pair-index positivity §6's pairing belongs to) and the currency null (a pricing statistic must contain the real parts; the currency is definable only after the crossing), **each at its filed grade — synthesis and definitional-with-open-enumeration respectively. No claim of §6 strengthened.** |
| **(b)** | `SIMPLICITY_OF_RIEMANN_ZEROS.md` | **+3 lines, 0 removed** | the cluster's structural home: E-14's diagonal address (simplicity is single-index; what would force it is pair-index) and the Lehmer tie at **flow grade** (54× past a depth-12 meter's reach at 1.4% contrast). **Annotation only — nothing restated, withdrawn, or regraded.** |
| **(c)** | `THE_SUBSTRATE.md` | **+8 lines, 0 removed** | §(ii-bis): [8,4,4] under three era results at their landed pins — the exact Duursma-RH instance (computational record), the lead law (`LeadLaw.lead_law`, lv `2f71068`), the genus ≤ 5 theorem (`Genus5.classification` jointly with the confinement terminals, lv `2f71068`). **Enrichment only; §(ii) unchanged.** |
| **(d)** | `THE_METHOD_CANON.md` | **+12 lines, 0 removed** | §XI: the registered-expectation record folded in as evidence, **counted rather than characterized** |

### (d) in full, since it is the one that makes a claim

**Counted from this era's relay reports:** 4 confirmed as registered · 1 failed and filed as
failed (the triviality pre-screen) · 1 split (W-LI face 2) · **2 third-outcome, neither registered
branch** (the λₙ decomposition; the mechanism theorem's fourth invocation) · 1 not runnable as
posed (BURIAL's exponent test) · 1 refuted (R4's outcome-shape 1).

**Five reversals named:** the ζ-arm's zero placement power · Johansson's datum refuting *"the
arc's first measured placement channel"* · the detector's 50-digit reach (2×10⁻²³ → 9×10⁻²¹) ·
the occurrence proxy retired · F.2026-08-05-j corrected by -l.

**What the count is evidence FOR, at its size:** six of ten registered expectations resolved as
something other than the confirmation they proposed, and four of those six were visible only
because the expectation was fixed in advance. **It is NOT evidence that the results are correct**
— only that the discipline does the work it was adopted for. Instances listed so the count can be
checked rather than trusted.

---

## §3 — THE PROVENANCE WORK-ORDERS

### Li/Weil (11) — **CLOSED: 6 by numerical check, 4 by derivation-at-source, 1 re-filed**

| identity | check | verdict |
|:--|--:|:--|
| `S_n = wⁿ + w⁻ⁿ`, `w = 1 − 1/ρ` | 2.3×10⁻¹⁶ against an independent route | **CLOSED** |
| `E_quad(n) = 2Re[1−wⁿ] + 2Re[1−w⁻ⁿ] = 4 − 2Re(S_n)` | 8.9×10⁻¹⁶ | **CLOSED** |
| `E_quad(n) = 4 − 2(rⁿ+r⁻ⁿ)cos(nφ)` | 0.0 | **CLOSED** |
| the quadruple's λ contribution = E_quad | 1.8×10⁻¹⁵ | **CLOSED** |
| `\|w\| > 1 ⟺ Re ρ < ½` — the geometry the block rests on | 0 counterexamples in 4000 random ρ | **CLOSED** |
| `log\|w_out\| ≈ δ/γ²` | worst relative error 1.6×10⁻² over 12 (γ,δ) pairs | **CLOSED** |

**By derivation-at-source (4):** *λₙ ≥ 0 IS the Weil functional on the Li test function* (an
identification at cite, closed by the λₙ-decomposition reading) · *E = the zero side Σ ĝ(γ_ρ)* (the
explicit formula, classical) · *Λ ≥ 0 forces the drift D* and *D(n) ≥ 0 of λ_Z* (dBN pencil
statements whose content is Rodgers–Tao at cite).

**RE-FILED WITH ITS REASON (1):** the bare threshold `n ≈ 2γ²/ε` **as a detection depth**. Checked
and found to be the **e-folding** depth; the detection depth carries a log factor. **Corrected at
INSTRUMENTS rather than closed as written** — a formula that is right about one quantity and
mislabelled as another is not closed by verifying the quantity it is right about.

### Shape-discriminant (4) — **CLOSED by cite-adjacent repair**

`INDEX_ARITY: +0 lines, 0 removed, 0 figures changed.` The shape-discriminant paragraph now names
its provenance inline (relay `2026-08-05-e25-derive-the-constant.md`, `2026-08-05-w-control-audit.md`),
so `c_ideal(L)`, `x_j = N(γ_j)` and `Π(L)` are resolvable from that page — **I-8's locality
condition satisfied: the statement is now pointed at from the mention.**

### Cosmology (4 remaining) — **CLOSED by the standing note** landed at the previous checkpoint,
which carries the forced-vs-permitted status into each ledger that restates `4/81`.

### Number-field (5) and spectral-realization (4) — **NOT CLOSED, and said plainly**

Number-field wants numerical checks on small fields (`d(K) = 2^(r₁+r₂+2)`, the Dedekind
formation total) — a real computation not run this pass. Spectral-realization's four are
identifications at cite requiring source reading, not arithmetic.

**COUNT: 24 of 35 closed · 1 re-filed with reason · 10 remaining**, each with its route already
named and none carrying a defect claim.

---

## §4 — I-8, PINS, RAIL, CENSUS

**I-8 applied to every term newly named this pass.** *ii-bis / the code garment* — stated at its
use with a table. *The cluster's structural home* — stated where named. *The registered-expectation
record* — the counts and instances are the statement. *The five-vocabulary reading* and *the
currency null* in the residue paper — both stated inline with their grades rather than named and
left. **No term was introduced this pass whose statement is not co-located with its mention.**

**THE RAIL.** The freeze was lifted for this pass only. **No rail document was edited** — the four
scope items live in `phase1.5/`, which is corpus, not rail. **`D:\PLACE-phase1.5` and
`D:\PLACE-phase2` are therefore unchanged, and the freeze is RESTORED at the same baseline it had:
`de621b1` / `2147a03`.** The new baseline is the old baseline; that is the honest record and no
new pin is minted for a freeze that never moved.

**THE CENSUS.** Undisturbed throughout. **175 records banked, complete through t = 12.0**, running
and resumable by the command of record:

```
cd /d D:\relay && python tools\e16\epstein_census.py
```

---

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `fc95ad3` → this pass's commit |
| relay | `400fb7f` → this report's commit |
| SIDE-lv-conservation | `2f71068` — unmoved |
| SIDE-kernel | `5e668b4` — unmoved |
| rail | `de621b1` / `2147a03` — **freeze restored at the unchanged baseline** |

Nothing deposits.
