# `W-ORD-ARITH` — ACT 2: THE TWO-FACE JUNCTION — 2026-08-11

**Registered at PLACE-papers `190c619` BEFORE the adjudications.**
**ABSOLUTE stop at its sharpest — adjacency to the sign face is not permission.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

> ### PATH-OF-CONSEQUENCE
> **"This act reads the arithmetic dossier and the geometric constraint set in mutual light — its
> outcome is the completest statement of whether the two faces specify ONE missing object; no arrow
> supplies the sign."**

---

## §1 — THE CONSTRUCTION'S HYPOTHESES, AT CITE

> *"An integral domain `A` with quotient field `K` is called **Fatou** if `W_rat(K) ∩ W(A) =
> W_rat(A)` in `W(K)`. **A Noetherian integral domain is Fatou** [Haz83, §3]."*
> *"We call an integral scheme `X` **Fatou** if for every open affine `U` the ring `O_X(U)` is
> Fatou. **Every integral, locally Noetherian scheme is Fatou.**"*

> ### The hypotheses are SCHEME-THEORETIC throughout — Fatou (integral, locally Noetherian),
> ### normal, finite type over `Spec ℤ`. **There is no Euler-product condition anywhere.**

## §2 — THE JUNCTION TABLE

| # | pairing | verdict |
|--:|:--|:--|
| **E1** | keyhole (i) ↔ the suspension's `ℚ_{>0}` | **DISTINCT — REGISTERED EXPECTATION REFUTED.** Existence is conditional on **scheme-hood**, not Euler-product structure. The `ℚ_{>0}` comes from the Frobenius family, which comes from Witt vectors, which need **only a ring** |
| **E2** | keyhole (iii) Epstein-separation ↔ the same quotient | **KIN — confirmed in outcome, REFUTED in mechanism.** The seat does separate `ζ` from the Epstein family by existence, but because a single-class Epstein zeta **is not a scheme's Hasse–Weil zeta** — not because it lacks an Euler product. Within scope the Euler product always holds, so it can never be the discriminator |
| **E3** | `C₂` ↔ the `H¹` row | **CONDITIONAL — the chiasmus does NOT close.** §3 |
| **E4** | keyhole (iv) ↔ the split criterion's two channels | **KIN.** The `ψ_p` sit at the multiplicative place but supply a Frobenius **action, not a pairing**. Keyhole (iv)'s demand is unmet, and what would meet it is exactly `Δ2` |
| **E5** | discreteness ↔ `E1`/`E2` | **KIN, strongly — same structural fact at two altitudes.** Both say the Euler-product-bearing objects and the off-line-zero objects are disjoint unless RH fails. *Not `SAME-REQUIREMENT`: one is about controls, the other about construction scope* |

## §3 — THE CHIASMUS, AND WHY IT DOES NOT CLOSE

For the two faces to be one specification seen twice, one identification is required:

> ### that **the pairing on `H¹` IS the Weil form**.
> ### **That identification is exactly the `Δ2` cell — and `Δ2` is the cell that fails.**

**The chiasmus is conditional on the missing thing.** The two faces cannot be shown to be one
specification without the object whose absence is what both faces are describing. *Asserting it
would be `ASSUMED`, which voids the cell.*

## §4 — THE DELIVERABLE

> ### **VERDICT: `KIN`, CONDITIONAL ON `Δ2`. NOT "one specification seen twice."**
>
> What the table establishes is narrower and firmer than the era's hoped-for statement: **both
> faces' remaining content routes through the same cell.** `N1`–`N7` and `C1`–`C9` are not shown to
> be the same requirements — `E1` refuted the sharpest candidate identity outright, and `E3`'s
> chiasmus needs the missing object in order to be stated. **What they share is a destination, not
> a content.**
>
> **And in every branch, unchanged: what is specified is THE OBJECT. The definiteness on it is THE
> CLAUSE. The clause is `h2`. And `h2` comes to the author, never from an act.**

## §5 — THE PER-CHANNEL `R4` QUESTION, FILED AND HELD

**Candidate refinement (question grade): `R4` grades per channel** — translation-type on the
continuous structure, endomorphism on the discrete. Forced by `SEAT` act 3's split finding.

> ### **HELD UNREPAIRED.** The stopping rule governs: the spec changes when a candidate forces it,
> and **no candidate is pressing** — Deninger's object graded successfully *with* the split recorded
> as a finding, not a defect. *Filed so the next candidate that hits it finds the question written.*

**Five-register two-channel convergence, merge discipline explicit:** `λ_A`/`λ_Z` · `Δ1`/`Δ4` ·
flow/`ψ_p` · keyhole (iv)'s two places · `C7`'s two legs.
> ### **Five appearances is five things until a text says otherwise. NO PROMOTION.**

## §6 — FOUND-BEYOND *(two)*

1. > ### **The registration named the wrong discriminator, and the correction is load-bearing.**
   > `E1` and `E2` both assumed the Euler product was the separating condition. **It is not:
   > the condition is scheme-hood and the Euler product is its consequence.** *The corpus has been
   > treating "Euler-product-essential" as a **primitive** keyhole condition; this read shows that in
   > the geometric setting it is a **derived** one.* **Filed as a question against keyhole (i)'s own
   > status.**
2. > **The act was chartered to confirm a chiasmus and it returned a conditional.** The two-face
   > reading was the era's most attractive available statement, and the table does not support it.
   > **An attractive synthesis failing at one cell is exactly the shape these stopping rules exist to
   > catch — and it was caught by the cell that has failed in every act since `W-ORD-H1`: `Δ2`.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `190c619` (registration) → this pass |
| relay | this report's commit |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**`Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow. No progress on the sign.**
