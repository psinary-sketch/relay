# THE RENAME BY THE WRITING LAW · THE CLASS-RESOLVED EULER PRODUCT SEATED · THE SWEEP CLOSES
## ### **`THE_ONE_OBJECT` → `THE_SEMILOCAL_CARRIER_SPECIFICATION`, AND `Z(s)` GRADED AGAINST `B1`**

**Relay report · 2026-08-17 (eighth sitting) · author-ruled · `INSTRUMENT` track · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 4d095a3737a0ca6cbde66ecad6232d15ec2e122b   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 4d095a3   ### 22-file roster + MANIFEST, 3 content probes PASS
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

---

## §1 — ### **THE RENAME, EXECUTED AS RULED**

**`git mv phase2/method/THE_ONE_OBJECT.md → THE_SEMILOCAL_CARRIER_SPECIFICATION.md`, history preserved
(recorded as `R` in the commit).** *The writing-law reason, now in the document's own `§0`: "ONE" was a
CLAIMED PROPERTY — the very property `§1`'s own two-objects-glued test exists to decide — and a title may
not presume its sheet's outcome. The new title names the object (carrier), the regime (semilocal), the
genre (specification). Alias line for citation continuity; the old name is retired as a heading.*

* **Reference scan, three match modes** *(bare name · prose · path, per the reference-scan rule)*:
  ### **four citing sites in living registers, all updated** — `FINDINGS` ×2, `OPEN_TRAILS` ×2 *(the Road-B
  line carried name + path + prose and got all three)*. **Two hits in banked relay session reports
  (`two-roads`, `v-at-depth`) resolve through the alias and are not rewritten.** *Post-rename re-scan:
  every remaining old-name occurrence is an intentional alias/at-filing note — nothing dangles.*
* **To seat `§2`, `B2`–`B4` renumbered `§3`–`§5`** *(`§3-bis`/`§3-ter` → `§4-bis`/`§4-ter`)*, noted in
  `§0`; **external citations all point at `§1`/`§1-bis`, which did not move; the checkpoints `B1`–`B4` are
  the stable names.**
* ### **NAME-IDENTITY ARMED IN `§0`:** *`THE_SEMILOCAL_CARRIER_SPECIFICATION` (`C3`/Road-B) ·
  `THE_CARRIER_SPECIFICATION` (`C1`, phase-2 field spec) · `THE_CARRIER_BUILD` (`C1`/Road-A) are THREE
  documents; "the carrier spec" unqualified is not a usable citation.* **The adjacency is the rename's one
  cost and it is priced by the arming.**

### **THE PLACEHOLDER-TITLE SWEEP — CANDIDATES FOR YOUR RULING, NONE RENAMED**

*Shape sought: `THE_X` where `X` is a property claimed rather than an object named. Forty `THE_*` documents
scanned; most are lawful (object + genre: record, map, census, protocol, spec).* **The candidates:**

| file | why it is listed | severity |
|:--|:--|:--|
| ### **`THE_DEEP_STRUCTURE_CROSSREFERENCE`** | *"deep structure" presumes a deep structure exists — and the sitting it records ruled `DISTINCT` on three of its four questions, so the title claims more than its own verdicts* | ### **closest to the `ONE_OBJECT` shape** |
| ### **`THE_MUTUAL_LIGHT_MAP`** | *"mutual light" claims the mutuality the map was built to investigate* | same shape, weaker stakes |
| ### **`THE_SIGN_RECONCILIATION`** | *the FILENAME compresses the document's own honest H1 — `THE SIGN-ARRANGEMENT RECONCILIATION` (two bookkeeping arrangements reconciled) — into what reads as "the sign, reconciled" beside an open `h2`* | **filename-vs-title mismatch; the doc itself is lawful** |
| `THE_CALIBRATION_IS_A_FROBENIUS_NUMBER` *(cluster)* | *a sentence-claim title (its subtitle even says "as one object") — lawful ONLY if the claim is at grade; listed for a grade check, not for renaming on shape alone* | verify-grade |
| `THE_UNCONDITIONAL_SURROUND` | *"unconditional" is the paper's graded content (the surround itself carries no hypotheses); lawful if the grade holds — listed for completeness* | low |
| `THE_RIPE_LIST` · `THE_LOAD_BEARING_MAP` · `THE_BRIGHTENING_MOVES` | *the property names the SELECTION CRITERION for membership, not a claimed theorem — lawful under that reading; listed so the sweep is complete* | low |

---

## §2 — ### **THE SEED OBJECT, SEATED AND GRADED** *(`THE_SEMILOCAL_CARRIER_SPECIFICATION` `§2`, question grade, `OURS`)*

**Seated:** ### **the class-resolved Euler product `Z(s) = Π_𝔭 (1 − [𝔭]·N𝔭^{−s})^{−1} ∈ ℂ[Cl]`** — the two
known projections at cite (augmentation `ε(Z) = ζ_K`; determinant of the regular action `= ζ_H`; the
character basis diagonalizes `Z = Σ_ψ L(s,ψ)e_ψ`), **with the rider beside it, not after it: an
augmentation-blind pairing on `Z` carries no more than a pairing on `ζ_K`** *(kin:
`descent_carries_no_warrant`, compiled)*. *Ray-class resolution caveat named without promotion. No
construction.*

### **`B1`'s FOUR COLUMNS ASKED OF `Z(s)` — THE GRADING, BOTH BRANCHES LONGHAND**

| column | grade |
|:--|:--|
| **(i) address side** | ### **DATA NATIVE AT CLASS RESOLUTION** *(addresses `log N𝔭`, class-labelled — richer than the bench's)*; **the address OBJECT (window, lags, matching) derived-by-instrument, as everywhere** |
| **(ii) place side** | ### **HALF — FROM THE OPPOSITE SIDE TO THE BENCH's:** *genuine local factor at every finite prime (what the bench lacked), but ALGEBRAIC — no `ℚ_p`, no Sonin space, no trace, and NO ARCHIMEDEAN PLACE AT ALL* |
| **(iii) identification** | ### **ABSENT — THE EMPTY COLUMN, NAMED EXACTLY.** *The one operator in sight (multiplication-by-`Z`) has determinant `ζ_H` and no matching-number reading.* |
| **(iv) joint move** | ### **VACUOUSLY ONE OPERATION — AN EVASION, NOT A PASS.** *"Adding a place" is native on `Z_S`; "widening support" has no independent meaning — there is no window for support to be forced by. "Same operation" holds only because the second axis is missing.* |

**Branch A** *(the one operation is `(iv)`'s germ)*: a trace/positivity structure on the restricted
products would have to make the support axis REAPPEAR as a norm-truncation shadow and reproduce sawtooth +
stability from one move — **nothing in hand does this; the gap prices to `(ii)`'s analytic half, the same
trace the whole sheet keeps arriving at.** **Branch B** *(the one-ness is vacuous)*: `Z` is a **content
vehicle** — column `(i)` at full resolution and nothing else — a content candidate, not a carrier
candidate. *Both sentences finish; the seating survives either branch.*

### **THE INVOLUTION ANSWER** *(the ferry's parenthetical question, longhand)*

> ### **THE FE DOES NOT ACT COEFFICIENT-WISE — IT ACTS AS REFLECTION TWISTED BY THE ANTIPODE.**
> *`Λ(s,ψ) = W(ψ)Λ(1−s,ψ̄)` couples `s ↦ 1−s` with `ψ ↦ ψ̄`, and `ψ ↦ ψ̄` on components IS `[C] ↦ [C^{−1}]`
> (longhand: the antipode maps `e_ψ ↦ e_ψ̄`). The involution on the class-resolved object is itself
> class-resolved: `Ẑ(s) = W · Ẑ(1−s)^∗`.*
>
> ### **AND "IS THE RESULTING HERMITIAN FORM AUGMENTATION-BLIND?" HAS NO FRAME-FREE ANSWER — THAT IS THE
> FINDING. The involution alone induces NO form; a form needs a scalarizing functional, and the two
> canonical functionals split exactly along the rider:** *`ε(fg^∗)` is rank-one and blind by construction —
> the warned failure, exhibited, graded OUT by the rider's own sentence; `τ(fg^∗)` (identity-coefficient)
> is the standard inner product and separates classes —* ### **NAMED, NOT PROMOTED; nothing asks whether
> any `τ`-built pairing is definite, and nothing may.** **The rider sharpens from a warning into a
> selection principle: a pairing on `Z` must declare its functional.**

---

## §3 — ### **THE FALSE-IDENTITY SWEEP CLOSES**

*Three match modes (bare formula both spacings · prose forms · path) over both repositories.* **Result:
every living-register site was already repaired;** ### **ONE un-repaired site found — `relay`
`2026-08-14-research-turn-close.md`, the witness table — repaired strike-visible with the `ℚ(√−23)`
counterexample, the original text preserved under the strike.** *Correction twenty-three's entry now
carries the full six-row site list with dispositions; the witness's `FINDINGS` entry wording is adjusted
("two bases of one class-resolved Euler product; augmentation vs determinant"), per-piece grades
unchanged.* ### **Sweep verdict: no un-repaired site remains in either repository.**

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE RENAME IS EXECUTED** — history preserved, four living sites updated, nothing dangling, the
   renumbering noted, name-identity armed on the three carrier documents.
2. ### **THE PLACEHOLDER-TITLE LIST IS ABOVE** — one strong candidate (`THE_DEEP_STRUCTURE_CROSSREFERENCE`),
   one filename-mismatch (`THE_SIGN_RECONCILIATION`), the rest graded low or lawful. **Nothing renamed
   without your ruling.**
3. ### **THE SEED IS SEATED AND GRADED:** the empty column is `(iii)`; `(iv)` is vacuous on it; the
   involution acts only up to the antipode; the rider is now a selection principle (`ε` out, `τ` named-not-
   promoted).
4. ### **THE SWEEP IS CLOSED** with correction twenty-three's site list as its record.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
