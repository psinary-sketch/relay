# `W-ATTEMPT-2` — THE CODOMAIN · THE ATTEMPT RECORD
## ### **SUB-GATE DECLARED · THE FIVE CHECKPOINTS RUN IN ORDER · AND THE MINIMAL INSTANCE RETURNS AN `H²` WITH ZERO RADICAL, TWISTED-HERMITIAN TO MACHINE PRECISION — UNGRADED, UNREGISTERED, EVERY PRE-REFUSAL IN FORCE**

**Relay report · 2026-08-18 (twenty-first sitting) · author-ruled attempt opening · ### RELAY-ONLY — THE
REGISTER IS UNTOUCHED BY EVERYTHING IN THIS ACT · nothing deposits**
**### SUB-GATE, ON THE FACE: NO CANDIDATE GRADES `PLAUSIBLE` ACROSS `T1`–`T10` AS OF THIS OPENING.**
**### NO SENTENCE ABOUT THE SIGN OF `W_∞ − ΣW_𝔭` LEAVES THE RELAY, and none is in this report.**

```
PLACE-papers  origin/main : 38b0d5b (UNTOUCHED this act — relay-only banking; no mirror claim made)
relay         origin/main : (this report)                              local +1 HELD
instruments   tools/e16/b14_attempt2.py · data/b14_registration_2026-08-18.txt (checkpoints 1–3 banked
              BEFORE the run) · data/b14_2026-08-18.txt (the run + the parity-twist check)
```

> ### **RULE-3 LOG:** *all six cited identities banked and verified last sitting (the surround table ·
> the pentagon's law at pin · the certified tensor square · the class-resolved Euler product · the
> antipode-involution · the charter's checkpoints and closure protocol). Banned grep: clean; terms
> coined: none. `T0` (the pentagon's law) governed every step below: no step asserted definiteness.*

---

## CHECKPOINT 1 — THE MECHANISM, STATED BEFORE ANY WORK *(banked verbatim in the registration)*

> ### **(M) GLUE THE FINITE-PLACE SQUARES ALONG THE CLASS RESOLUTION.** *At each `𝔭` the certified square
> `V₁⊗W₁` (`S = P₁⊗Q₁`); the class-resolved `Ẑ ∈ ℂ[Cl]` carrying involution (FE ∘ antipode, unitary
> twist) and weight (exact `q^{−1/2}`, model grade); the global object: the `ℂ[Cl]`-linear tensor of the
> local squares over the place set, the archimedean factor entering CLASS-SILENTLY (`B5`) as the
> certified sandwich, each finite place carrying its antipode-SELF-DUAL class label `c_𝔭 = [𝔭]+[𝔭̄]`.*
> ### **THE CLAIM TO TEST: this object has an `H²` — a codomain receiving the `τ`-pairing — Hermitian
> under the involution. NOT CLAIMED: ANY SIGN.**

## CHECKPOINT 2 — GRADED AGAINST `T0`–`T10` *(full grading banked in the registration; the summary)*

*`T1` addresses-by-design (prime content as structure, not support) · ### **`T2` PREDICTED-FAIL-UNLESS
the class resolution couples the axes — the one hypothesis no located frame has tested because none
takes the presentation as data; said before the run so no later sentence can claim it** · `T3`
by-construction at spec level, Face E's Tier-1 scope kept · `T4` checkable at the model, guarded · `T5`
the radical computed below · `T6` complies-by-design, scope said (the model is real-representable; the
zeros' `H¹` is not reached) · ### **`T7` THE ACT'S SHARPEST TEST, with the registered discriminator: the
coupling element's character spectrum — flat = legs-only (pre-refuted); unequal = the gluing datum
consumed. Longhand prediction: `(4,1,1)`, the norm-6 coefficients of `Ẑ`** · `T8` not reached (the reach
theorem rides) · `T9` complies · `T10` kin-watch armed.*

## CHECKPOINT 3 — FALSIFICATION, BOTH BRANCHES *(banked before the run)*

**(F-fail):** four named failing steps — involution non-closure · antipode incoherence · flat coupling
(the `T7` bite) · total radical (Prop 6.4's fate) — *each the corpus's own name for the four-word
question over `ℚ`.* **(F-land):** the codomain exists — *then positivity is a PROPERTY OF A CONSTRUCTED
OBJECT, to be checked never asserted; the closure protocol's step one is priced against the
CONSTRUCTION;* ### **the overclaim PRE-REFUSED IN WRITING: `{∞,2,3}` at `h = 3`, model cutoff `n = 1`,
is a finite-place-set object at a finite cutoff — the complete roster is the double limit and STAYS
OPEN whatever this act shows.**

## CHECKPOINT 4 — THE MINIMAL INSTANCE, WORKED *(`K = ℚ(√−23)`, `h = 3`, `{∞, 2, 3}`; every number below is measured or verified in-run)*

| check | registered expectation | ### **measured** |
|:--|:--|:--|
| the witness data | 2 splits non-principal · 3 non-principal · 6 principal → `[𝔭₂][𝔭₃] = 0` | ### **VERIFIED in-run** *(the class relation among the place labels — the coupling datum)* |
| antipode coherence | `c_𝔭^* = c_𝔭` | ### **EXACT (`0.0`)** — F-fail (b) does not fire |
| involution closure | `F_𝔭` preserves each square | ### **machine-exact** — F-fail (a) does not fire |
| ### **the `T7` discriminator** | character spectrum of `c₂c₃` = `(4,1,1)` — the norm-6 coefficients of `Ẑ` | ### **EXACTLY `(4, 1, 1)` — the gluing datum IS the class-resolved Euler data; the class structure reweights the `ψ`-components unequally. LEGS-ONLY PRE-REFUSAL DOES NOT BITE.** F-fail (c) does not fire |
| ### **the `τ`-Gram (`T5`)** | rank / radical | ### **RANK 12 OF 12 — RADICAL ZERO** *(the sharp contrast with Prop 6.4's proven large radical, noted as `T10`-kin, not promoted)* — F-fail (d) does not fire |
| ### **Hermitian under the involution** | up to the `F² = parity` twist (`B5`/`B9` structure) | *raw residual `12.0`;* ### **TWISTED residual `‖G − (P⊗1)G†‖ = 5.8×10⁻¹⁵ — HERMITIAN IN THE PARITY-TWISTED SENSE, TO MACHINE PRECISION** *(and `F² = P`, `P² = 1` verified exact)* |
| the `H²` candidate | its dimension a number, its involution-invariance a check | ### **`H² = ℂ[Cl]`: dim `3 = h`; antipode-invariant dim `2`** |
| inertia *(GUARDED)* | a property of this constructed finite object; NOT `h2` | `(+3, −3, 0:6)` on the untwisted symmetrization — *recorded, and no sign sentence follows from it* |
| the laws re-measured *(range law: nothing inherited)* | per-factor products | *all three scaling channels measured `0` — the `n = 1` factors are compressed-dead (`Q(p,1) = 0`), so* ### **the minimal instance's DYNAMICS are trivial: the codomain/pairing structure lives, the scaling channels do not, at this cutoff. The dynamic re-measurement needs `n ≥ 2` factors — priced, not run.** |

> ### **CHECKPOINT 4's HONEST SENTENCE: AT THE MINIMAL INSTANCE, THE GLUING CLOSES — an `H²` exists
> (`ℂ[Cl]`, dim `h`), the `τ`-pairing lands in it with ZERO radical, the pairing is Hermitian under the
> involution in the parity-twisted sense to machine precision, and the gluing datum consumed is exactly
> the class-resolved Euler data.** *Partial banked as partial: the dynamics are dead at `n = 1`; the
> archimedean factor is carried, not modeled; the place set is `{∞,2,3}`; the cutoff is `n = 1`.*

## CHECKPOINT 5 — REVIEW *(whatever exists returns UNGRADED and UNREGISTERED)*

1. ### **F-LAND AT THE MODEL INSTANCE — WITH EVERY PRE-REFUSAL IN FORCE.** *A codomain exists for the
   glued object at `{∞,2,3}`, `h = 3`, `n = 1`. The complete roster is the double limit and stays open.
   Nothing here is graded, registered, or promoted; the register sentence is untouched.*
2. ### **THE CLOSURE PROTOCOL's STEP ONE, PRICED AGAINST THE CONSTRUCTION** *(not against any sign)*:
   formalizing the glued module + coupling element + antipode + the twisted-Hermitian identity. **The
   integer half (the group-ring arithmetic: the labels, `c₂c₃ = 2[0]+[1]+[2]`, the character spectrum as
   exact cyclotomic integers, antipode self-duality) is decide-able in vanilla Lean — ~1 sitting. The
   pairing half needs exact cyclotomic arithmetic for the DFT entries (`ℚ(ζ₃₆)`-scale) — Mathlib or a
   hand-rolled tower — ~1–2 further sittings, design first.** *Price: 2–3 sittings total, split named.*
3. ### **WHAT WOULD MOVE THE ATTEMPT NEXT, NAMED NOT RUN:** *(i)* the `n ≥ 2` instance (live dynamics:
   does the weight `q^{−1/2}` flow through the glued pairing?); *(ii)* the second place set (`{∞,2,3,5}`:
   does the coupling stay Euler-exact — `c₂c₃c₅`'s spectrum vs the norm-30 coefficients?); *(iii)* the
   `T2` question proper — whether the class-relations coupling is support↔place coupling — untouched, as
   predicted at checkpoint 2.
4. ### **NO SENTENCE ABOUT THE SIGN OF `W_∞ − ΣW_𝔭` APPEARS ABOVE OR FOLLOWS FROM ANYTHING ABOVE.** *The
   inertia line is a guarded property of a 12-dimensional constructed model object.*

---

## CLOSING — RELAY-ONLY, FOR THE AUTHOR'S WORD

**Checkpoints 1–3 in full (banked before the run) · checkpoint 4 as far as one sitting honestly reached
(further than predicted: four registered checks landed exactly, one landed in its twisted form) ·
checkpoint 5's review above. The register is untouched. `W-ATTEMPT-2` stands OPEN at its first
checkpoint-cycle, sub-gate, awaiting your word on: the formalization pricing (step one), the `n ≥ 2`
instance, and the second place set.**

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — replayed on top of this report per the standing
procedure, absence from the public tree re-verified after this push. *Release: counsel, then your word.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO SIGN. NOTHING PROMOTED. NOTHING DEPOSITS.**
