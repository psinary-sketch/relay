# `W-CONSTRUCTION-1` · ACT 6 — THE ADJOINTNESS QUESTION AND THE ε-LEMMA
## ### **THE LITERAL ADJOINTNESS REFUTED AT PROOF GRADE (THE TWIST CANNOT CREATE SUPPORT; THE VANILLA SHADOW 7/7 NO-AXIOMS); THE POLAR FORM A RESTATEMENT OF `(L-identity)`, SAID SO — THE ε-LEMMA ANSWERED: NOT THE SAME `S`, THE `(−1)`-SECTOR DEFECT NAMED, THE CLEAN HALF PROVED — THE COMPARISON WRITTEN, NOTHING DECIDED — THE DICTIONARY DERIVED-AT-MODEL**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 6 · sub-gate declared · Rules 4/5 · nothing
deposits · nothing circulates**

> ### **RULE-3 LOG:** *act 5 at `732c985` ✓; `v0.5`, `TraceSilence` 6/6, file A at the classical
> profile, the two-channel anatomy ✓; CC Thm 4.7, the antipode-involution (B5), Deninger math/0204110
> `§4` all at cite ✓; the banned grep on this ferry: CLEAN. Both adjointness branches were REGISTERED
> LONGHAND BEFORE any computation, per the order. Terms coined: none.*

```
build document  THE_GLOBAL_SECTION.md v0.6 (§13 adjointness; §14 the ε-lemma; §15 the
                comparison equation; §16 the dictionary)
vanilla leg     tools/lean/OrbitDictionary.lean + AxiomCheckOD — 7/7 "does not depend
                on any axioms" (v4.29.1 pinned); the relay's zero-axiom count: 75
mathlib leg     LocalLimit.eigenvector_of_commute PROVED at {propext, Classical.choice,
                Quot.sound}; 0 errors; THE LEDGER UNCHANGED: 2 (B–C; infrastructure)
```

---

## §1 — ITEM 1: THE ADJOINTNESS QUESTION

**Registered first, both branches longhand (`§13` of the build document): (ADJ-yes) the polar form —
`Θ = trace + skew`, `A − PR = Θ − Θ*·twist`, positivity = the skew part's vanishing on the sector
sum; (ADJ-no) the exact failure named in operator terms; (ADJ-third) filed.** Then the one-place
statement, which DECIDES the literal form:

1. ### **REFUTED, proof grade: the antipode's twist is a REWEIGHTING of the trace sequence — it
   cannot create support. The trace channel is supported at the identity (act 5, exact zeros); the
   quotient channel speaks at every `k` (the Weil shape, banked). No twist of the one reaches the
   other.** *The finite shadow decide-checked: the twist of the zero sequence is zero
   (`twisted_silence`); the quotient's weight is the FIBER of the shell shift — multiplication by
   `p` is exactly `p`-to-1 (`fiber_two`, `fiber_three`: the `q^{1/2}` per step is the unitary
   normalization of that fiber) — and it is nonzero where the twisted trace is zero
   (`channels_distinct`). All at no axioms.*
2. ### **THE EXACT FAILURE, OPERATOR TERMS: the quotient trace is the trace AFTER the
   fiber-summation (the transfer over the `p`-to-1 shell shift); the codomain's size as an operator
   at one place is the transfer's fiber weight — `√p` per step, fiber exactly `p`.**
3. ### **WHAT SURVIVES: the POLAR form only — and it is a RESTATEMENT of `(L-identity)`, said as a
   restatement; its proof-grade form is the closure protocol's business. VERDICT:
   `(ADJ-third)`-shaped — literal-NO at proof grade, polar-OPEN as restatement.**

## §2 — ITEM 2: THE ε-LEMMA

> ### **ANSWER: NO — not the same `S`. Ours is `Π₊S_Son` (the `E₁`/F-fixed restriction, the
> sitting-22 theorem); CC's is the full (even) Sonin projection. The difference is EXACTLY the
> `(−1)`-sector trace: on Weil-even test data `ϑ(g)` commutes with `F` (intertwining + evenness),
> the sectors split, and `Θ(g) = W_∞(g) + ∫g ε − Δ₋(g)` — the odd-sector defect `Δ₋` NAMED, one
> more trace of the same shape, the lemma's remaining owner (with the trace-class bookkeeping).**

*The clean half is MATHLIB-LEG PROVED this act (`eigenvector_of_commute`: eigenvectors persist
under commuting operators; the classical profile per terminal). The benefit lands where derived:
sitting 3's `ε`-sign applies to the FULL-sector form; the prolate 15/15 stays the bench pin.*

## §3 — ITEM 3: THE COMPARISON, WRITTEN

`W_∞(g) − Σ_p W_p(g)  ≟  [Tr_∞(ϑ(g)S_Son) + ∫g ε] − Θ_quotient(g)` — **on the build document's face
(`§15`) with every term graded: cited (the Weil terms), at-content (`Tr_∞`, `∫gε` — with our trace
= CC's minus `Δ₋`), banked-shape with the fiber-normalization exact at the cells (`Θ_quotient`),
named-open (`Δ₋`).** ### **THE EQUALITY ON THE CONSTRAINED CLASS IS `(L-identity)` — THE EXACT FORM
OF `h2` ON THE BUILT OBJECT. STATED. NOT DECIDED.**

## §4 — ITEM 4: THE DICTIONARY

### **Deninger's `§4` table, both rows DERIVED at our model: finite place ↔ recurrence/closed-orbit
data (no fixed point off the identity; the shift with fiber exactly `p`; the trace silent — VANILLA,
no axioms), infinite place ↔ the fixed-point/trace channel (the `ε`-form at content). Grade:
AT-CITE → DERIVED-AT-MODEL for the corpus's own object. Cross-linked, NO MERGE — Deninger's site is
not claimed.**

## §5 — ITEM 5: THE RECORD

*`THE_GLOBAL_SECTION.md` `v0.6`; correspondence rows 91–95 with leg and profile per row;
`F.2026-08-18p`; both legs' prints run; this report the act's bank.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED — RESTATED ONCE
(the polar form) AND SAID SO. NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING CIRCULATES.**
