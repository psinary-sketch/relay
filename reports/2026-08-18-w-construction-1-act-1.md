# `W-CONSTRUCTION-1` · ACT 1 — ROUTE 1, THE `E₁` GLUING: THE GLOBAL OBJECT STATED AND QUESTIONED
## ### **THE GLOBAL SECTION EXISTS AS AN ABSTRACT HILBERT SPACE AND HAS NO ADELIC-FUNCTION REALIZATION (THREE LINES, PROOF GRADE) — THE GLOBAL `E₁` IS THE SECTOR SUM THE FINITE CELLS FORETOLD — THE DENSITY QUESTION LOCALIZES TO ONE PLACE, `∞` — THE LEDGER'S REMAINDER IS THE ZERO CHANNEL WITH ITS ADDRESS OFF THE OBJECT — AND `SUS` IS COMPLETELY NON-UNITARY**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` opened author-ruled, Route 1 of the spec's `§8` ·
### SUB-GATE DECLARED on the build document's face (`C1`/`C3` not at content; the `§4` precedent) ·
Rule 4 governing · Fable/high effort, proof-search · nothing deposits · nothing circulates**

> ### **SCOPE, once: this act STATES the global object and asks it questions; every answer at its
> grade; the register moves only if clause (e) is filled and survives formalization and review.**

> ### **RULE-3 LOG:** *the spec `v0.2` `§8` at `e860b0b` ✓ · sitting 20's theorems ✓ · Tate at the
> Conservation-of-Spectra grade ✓ · the `E₁` theorem per place ✓ · CCM `T5` and Deninger Rem 5.7(5)
> at their banked cites, used at KINSHIP grade only. One ferried construction detail corrected
> in-act, on the record: the "ball indicator's `E₁`-normalized representative" DOES NOT EXIST (the
> indicator is orthogonal to the whole Sonin space — its projection is zero); the units' existence is
> proved otherwise and the canonical-choice question is NAMED-OPEN. Terms coined: none.*

```
build document   PLACE-papers phase2/method/THE_GLOBAL_SECTION.md v0.1 — the act's content at grade
this report      the act's relay bank; correspondence rows 71–74
```

---

## THE CHECKPOINT RESULTS, ONE PARAGRAPH EACH

1. ### **R1-1 — STATED, WITH A FINDING:** *the object `𝒮 = ⊗′_v S̄_v` (units in `E₁`, existence
   proved at every place; no canonical choice claimed) carries `F = ⊗F_v` unitary with `F² =` global
   parity (proof grade — the `E₁`-restriction is exactly what makes `F` well-defined);* ### **and
   the global Sonin object has NO nonzero adelic-function realization** *(a.e. adelic point lies in
   a.a. local balls; Sonin vectors vanish there — three lines). The object is abstract; CCM kinship
   (their radical = the adelic-extension directions we exclude) recorded, never merged.* ### **The
   global `E₁` is the SECTOR SUM (`∏λ_v = 1`, finitely many non-unit factors) — the finite cells'
   T-fixed arithmetic was its shadow all along; `⊗′E₁,v` is the principal sector.** *The compression
   is FORCED globally (the one-place witness tensors out); the radial-unit existence and the global
   SOT are the act's named gaps.*
2. ### **R1-2 — THE DIAGONAL IS DENSE DEFINITIONALLY; THE WEIL CLASS'S DENSITY LOCALIZES TO `∞`:**
   *finite places dense at proof grade (Schwartz–Bruhat + the towers); the whole question reduces to
   ONE statement — the closure of `Π₊S(`Weil family`)` in `E₁(∞)` — with b33's growing-rank reach as
   the evidence and the density itself OPEN. If it fails, the complement is where clause (e)'s
   `Z`-shaped structure would live — `(D-proper)`'s content, localized.*
3. ### **R1-3 — `(L-remainder)` LANDS; `(L-identity)` IS NOT CLAIMED:** *on the class,
   `R(g) = Σ_ρ ĝ(ρ) − ‖image‖²` — the remainder IS the zero channel minus the object's norm, its
   vanishing/positivity on the class A RESTATEMENT of `h2` (said in those words), its address off the
   adelic picture and in R1-2's possible complement. No partial identity promoted; the closure
   protocol stands between any future `(L-identity)` and the register.*
4. ### **R2-1 — A THEOREM: `SUS` AT `p = 2` IS COMPLETELY NON-UNITARY WITH NO MODULUS-1
   EIGENVALUES** *(the Nagy–Foiaş unitary part is zero; the one-shell defect structure beside it).
   The Frobenius-modulus comparison: the `q^{1/2}` structure lives in the orbit-quotient channel
   (banked), not in `SUS`'s unitary spectrum — there is none. Fed to R1-3: the object's own scaling
   cannot carry unimodular (zero-like) spectrum; the remainder's off-object address sharpens.*

**The arc: the Mathlib companion opens beside R1-1 as its formalization layer; everything else
unaffected; the held acts and counsel unchanged.**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO PARTIAL IDENTITY PROMOTED. NOTHING DEPOSITS.
NOTHING CIRCULATES.**
