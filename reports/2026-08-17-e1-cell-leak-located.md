# THE `E1` CELL — WEIGHT-PRESENT-IN-NORM-ABSENT-IN-TRACE
## ### **THE LEAK LOCATED, A THIRD EXACT LAW WITH THE WEIL SHAPE, AND THE TENSOR CHECK HELD BY ITS OWN RULE**

**Relay report · 2026-08-17 (thirteenth sitting) · the author's word given for one sitting · `INSTRUMENT` track · sub-gate declared · bench · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**
**POSITIVITY GUARD ON THE FACE: a positive local trace bears not at all on `h2` — and the measured trace here is ZERO.**

```
PLACE-papers  origin/main : a3f1e69e8243dad2b5d1f680fcc4587ee68e1d81   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ a3f1e69   ### 22-file roster + MANIFEST, 3 probes PASS
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> ### **RULE-3 LOG:** *`B8(1)`'s two laws — banked (`§8.3-bis`) ✓ · both `(β)` conventions banked, neither
> chosen ✓ · Thm 2 at scope ✓ ·* ### **one flag: the ferry's "ratio `q` per cell" — the banked conventions'
> exact ratio at lag `q` is `q/2`; the shape-ratio is `q`, the constant dropped.** *Banned grep: clean;
> terms coined: none.*

---

## §1 — THE MEASUREMENT *(hardened registration, longhand, banked before any number)*

*At a finite place the scaling group is DISCRETE — `p^ℤ`, step `log p` — so the compressed action takes
the explicit formula's own lag data natively:* `ϑ_𝔭(g) = log p·Σ_k g(k log p)·U^k`, *and the candidate
trace `T(g) = Tr(ϑ(g)Sϑ(g)*)` is the `(α)` sandwich by construction.* **Three channels registered:
`t_k = |Tr(U^k S)|` (the pure lag-cross coefficient — the `(β)` test), `d_k = Tr(U^k S U^{k†})` (diagonal
mass), `n_k = ‖SU^kS‖_F` (the norm, `B8`'s `Q`). Both conventions predicted longhand: `√q` growing
(registered) vs `1/√q` decaying (Weil).**

## §2 — ### **THE VERDICT, PER THE REGISTERED MAP: WEIGHT-PRESENT-IN-NORM-ABSENT-IN-TRACE**

| channel | measured (eight dense cells: `p ∈ {2,3,5} × n ∈ {1,2}`, `(2,3)`, `(3,3)`) | law |
|:--|:--|:--|
| ### **trace `t_k`** | ### **MACHINE ZERO — every cell, every lag** | — |
| **norm `n_k`** | nonzero | `n_1 = √p·(p^{n−1}−1)` — `B8` re-confirmed |
| ### **diagonal `d_k`** | nonzero, integer | ### **`d_k = (p^{n−k}−1)(p^n−1)` for `k < n`, else `0` — A THIRD EXACT LAW, matched at every measured cell** |

> ### **THE LEAK IS LOCATED: THE TRACE KILLS WHAT THE NORM CARRIES.** *The compressed scaling action has
> `√q` mass and identically vanishing trace against the Sonin projection — the candidate's pure
> lag-correlation coefficient is zero everywhere measured. A structural derivation of the vanishing is one
> more sitting, named not claimed.*
>
> ### **FOUND BEYOND REGISTRATION, QUESTION GRADE: `√(d_k) ≈ √N·q^{−1/2}` — THE WEIL SHAPE, in the one
> channel that scales.** *Filed as the `(β)` convention question's first measured datum, not its answer.*

**Two instrument adjudications, banked with the run:** *(i) the `(5,3)` low-rank trace path returned
`~3·10⁻³` and was adjudicated NOISE — the same path at `(3,3)` gives `~2·10⁻²` where the dense ground
truth gives `~5·10⁻¹⁵`; the low-rank path is WITHDRAWN as a trace instrument, `(5,3)` recorded
not-measured-at-precision; (ii) the eight dense cells are the record.*

## §3 — THE TENSOR CHECK: ### **HELD, BY ITS OWN REGISTERED RULE**

*Rider 2 was registered to run ONLY if `E1` landed at either convention. It landed at the middle verdict.*
**The registration stands banked for a future cell; nothing about the `(p^n−1)²` square is claimed.**

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE `E1` QUESTION IS ANSWERED AT THE MODEL: the transposed Sonin candidate is a large space with
   `√q` in the norm and ZERO in the trace — it FAILS `(β)` in the registered channel.** *The ledger-side
   specification (`§9`) remains satisfied by nothing, and the candidate's standing is now measured, not
   argued.*
2. ### **THREE EXACT LAWS NOW SIT ON THE MODEL** — `dim = (p^n−1)²` · `n_1 = √p·(p^{n−1}−1)` ·
   `d_k = (p^{n−k}−1)(p^n−1)` — *with the third's amplitude scaling Weil-shaped, question grade.*
3. ### **NAMED, PRICED, NOT RUN:** *(i)* the structural derivation of the trace-vanishing (one sitting);
   *(ii)* the tensor check (banked, waiting on its trigger); *(iii)* the `(β)` convention bench on the
   true functional.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
