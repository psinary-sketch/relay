# THE TWISTED-PROJECTOR EXTENSION — THE DOORWAY ACT
## ### **THE FOUR SECTOR BASES BUILT AT EVERY CELL WITH EVERY GATE EXACT AND EVERY BANKED SECTOR DIMENSION RE-DERIVED; THE BALL-SILENCE LAW TRANSFERS VERBATIM; ONE FINDING AGAINST THE CODIMENSION INSTRUMENT, BANKED WITH ITS WITNESS AND ITS MECHANISM — A PURE VECTOR IN E_i(2,2) THAT IS EXACTLY THE TOWER IMAGE OF THE DEAD CELL'S 1×1-SHAPED GENERATOR; AND THE MIXED-PATTERN FRONTIER REACHED: BRANCH (a) AT THE VERIFIED SCOPES — THE SECTOR PAIRING ON MIXED DIRECTIONS IS DETERMINED BY LOCAL CHARACTER DATA, WITH ONE IN-RUN-CORRECTED DERIVATION SLIP BANKED, NOT SMOOTHED.**

**Relay report · 2026-08-21 · ferry-executed (part 1 of 1, receipt confirmed, Rule 1; a
byte-identical DUPLICATE of the ferry arrived mid-execution and is acknowledged as a
re-paste — the act ran once) · Rules 3/4/5 · exact arithmetic in the declared per-cell rings
· nothing about `h2` claimed; compatibility, not factorization; the range law governs every
sentence · nothing deposits · nothing circulates beyond this report.**

> ### **RULE-3 LOG:** *(a) banks b44–b52 at content; the per-cell rings DECLARED in the
> registration (p = 2: ℤ[ζ_N] with i = ζ^{N/4} folded at the test; odd p: the composite
> ℤ[ζ_N][i] as pairs, componentwise tests) — never improvised. (b) the projector family
> 4qP_λ = (q + λ̄S)(1 + λ̄²Π) derived longhand and VERIFIED AS GATES before any basis was
> trusted: completeness Σ_λ u_λ = 4q·f numerically at every basis vector; exact
> λ-eigenvectors; inter-sector orthogonality (full at small cells, sampled at large,
> declared); S² = q²Π re-verified. (c) the exponent-transport screen stands (no table-type
> transport was claimed this act). (d) the dependency order (2 and 3 consume 1's bases) held.
> ONE IN-RUN CORRECTION, recorded per Rule 4.1: the first M3 implementation conflated the
> WHOLE-SPACE projector trace with the SONIN-SECTOR pairing — a false identity whose FAIL was
> an instrument error, not a measured excess; the corrected M3 (the Sonin-compressed
> character formula, division-free, its reduction by character orthogonality derived
> longhand in the bank) PASSES EXACTLY. Terms coined: none. Banned-stem grep: clean.*

---

## §1 — THE PREDICTIONS, VERBATIM, WITH VERDICTS BESIDE THEM

> **(P1)** *the trace of each twisted projector re-derives the banked sector dimension, and
> the four dimensions sum to the Sonin dimension.* ### **LANDS — exact at every λ at every
> cell** (tr(4qP_λ) = 4q·d_λ against the banked (d₁, d₋₁, dᵢ, d₋ᵢ); sums 1, 4, 9, 16, 49,
> 64 = the Sonin dims — completeness in numbers).

> **(P2)** *generic position predicts no pure vector in any twisted sector; any pure vector
> found is a finding against the instrument, banked with its witness.* ### **THE FINDING
> LANDED, WITH ITS MECHANISM: E_i(2,2) contains a genuinely pure nonzero vector** — the
> mod-ℓ screen passed one basis vector and the EXACT ℤ[ζ₁₆] minor test confirms every 2×2
> minor vanishes. **Identified exactly (cross-proportionality in ℤ[ζ₁₆]): it is the TOWER
> IMAGE ι(u_i^{(2,1)}) of the dead cell's single E_i generator — supported on ONE position
> row, hence rank 1: PURITY IMPORTED THROUGH THE TOWER FROM THE 1×1 BOUNDARY SHAPE.** The
> codimension instrument's miss is thereby mechanized (E_i(2,2) is not in generic position —
> ι imports boundary-shape purity), and the same mechanism EXPLAINS the principal sector's
> cleanliness: E₁(2,1) = 0, nothing to import — MIXED-FORCED at E₁ stands unchallenged.
> Everywhere else: mixedness generic, exact witnesses ((2,1)'s 1×1 pure-by-shape the
> registered exception). **The P2-instrument refinement (correct the codimension call by the
> tower-imported subspaces) is STAGED for the author, not minted.**

> **(P2′)** *the ball-silence law transfers verbatim to the twisted sectors.* ### **LANDS —
> verified, not assumed:** the ball rows vanish in every twisted sector at every cell (the
> Sonin vanishing is sector-independent), so X_E = 0 with c = 0 by the same longhand.

> **(P3)** *the mixed-pattern counts, computed in the registration before construction.*
> ### **EXACT AT ALL FIVE ROSTERS: 1 (R5), 16 (R1), 112 (R2), 2176 (R3), 11776 (R4).**

## §2 — THE FRONTIER (b54): BRANCH (a) AT THE VERIFIED SCOPES, PLAIN

At R5 (full): the single mixed direction w = u_i^{(2,1)} ⊗ u_{−i}^{(3,1)} verified in the
global +1 sector (S_glob w = 6w exact); the Gram factorization (M1), the compression
factorization on all four registered op pairs (M2), and the CORRECTED M3 — the
Sonin-compressed character formula in division-free form — all EXACT. At R2 (the registered
sample): the pattern-block diagonalization (distinct blocks orthogonal via a vanishing local
factor, all six pairs) and the per-block Gram-product identity (all four blocks, direct vs
product) — EXACT. **The sector-restricted pairing on the mixed-pattern directions is
DETERMINED BY LOCAL CHARACTER DATA at the verified scopes — the form-level law extends
across the whole global sector there. Synthesis grade, fence intact: banked cells, levels,
and rosters only; R1/R3/R4 constructions not run (declared; their counts exact); the
level-limit and the archimedean junction undecided.** The pairing act's question (ii),
opened at its recorded address, is now ANSWERED at those scopes — and no excess appeared, so
no branch-(b) object exists to stage.

## §3 — THE RECORD AND PINS

*SIDE-global-section: `main = 70cac2a` — `TwistedShadow` (3 terminals, zero-axiom: the five
mixed counts decided; the tower witness PURE and in E_i, both kernel-checked); Core
138/138; row 33; tag `v0.1.0` unmoved. Relay: the b53/b54 registrations + banks +
instruments; the working-layer copy. No PLACE-papers edit; no mirror rebuild (disclosed).*

> **PIN LINE (post-push read-back, ls-remote):**
> `SIDE-global-section main = 70cac2aed06bab68832ec7a54ef0a8721ede53af` (TwistedShadow +
> row 33; Core 138/138; tag `v0.1.0` = peeled `706a81b` unmoved) ·
> `relay origin/main = 09d1e57978470db2d74d779934c325c874a55e26` (this act, parent
> `ac7303c`; pushed from `push-twisted-ext` per Rule 4.10, hook standing; HELD ancestry
> clean). **MIRROR:** unchanged and current — PLACE-papers HEAD `cfb2fa1` did not move;
> `mirror-refresh-2026-08-21-c` remains the live export. *This pin-line commit's own SHA is
> stated in the closing message, per the regress rule.*

## FOOT

**The author's desk holds the p2-10 citation ruling, plus the staged P2-instrument
refinement (the tower-import correction).** Standing sequence, per the ratified order: the
tower extension carrying the R5 discriminator, with the formation-card conversion and the
s-darkness species classification batched as strikeable components; then the deficit
comparison reading the R5 verdict; the p2-9 merge schedules freely.

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED. NOTHING DEPOSITS.
NOTHING CIRCULATES.**
