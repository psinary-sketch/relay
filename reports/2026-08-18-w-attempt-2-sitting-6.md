# `W-ATTEMPT-2` · SITTING 6 — THE ARCHIMEDEAN MODEL
## ### **THE ONE UNMODELED SLOT IS MODELED — `ext-partial` AS REGISTERED: THE BALL READING CARRIES EVERY INVARIANCE AND PAIRING CLAUSE AT MACHINE, THE HARD TWO-SIDED WINDOW DIES EXACTLY AS THE UNCERTAINTY PRINCIPLE SAYS, AND THE TWIST IDENTITY NOW HOLDS AT EVERY PLACE OF THE SECTION**

**Relay report · 2026-08-18 (twenty-sixth sitting) · author-called · attempt work RELAY-ONLY ·
nothing deposits**

> ### **THE CORRECTED STOP IN FORCE (restated): measured properties of constructed objects are DATA at
> bench grade; refused — promotion to `W_∞ − ΣW_𝔭` at complete roster, or register movement. SUB-GATE
> restated. Everything below is a property of a DECLARED MODEL of the real place, never of the
> Hypothesis.**

```
PLACE-papers  origin/main : f247d85 (UNTOUCHED this sitting)
relay         origin/main : (this report, in the sittings-5/6 batch)     local +1 HELD
instruments   tools/e16/b19_attempt2_s6.py (registration banked BEFORE the run:
              data/b19_registration_2026-08-18.txt) · data/b19_2026-08-18.txt
```

> ### **RULE-3 LOG:** *cites as in sitting 5's head, all banked ✓; terms coined: none; banned check
> clean.* ### **THE FLAGGED DISAGREEMENT, CARRIED ON THE FACE: the ferry's window `[a⁻¹, a]` vs
> sitting 4's banked `[1/a², a²]` — BOTH radii `λ ∈ {1/a, 1/a²}` and BOTH outer edges were measured;
> no convention adjudicated; the two normalizations agree on every verdict below.**

---

## §1 — THE MODEL, AS REGISTERED

*The finite Fourier model of the real place: `N` odd grid points, self-dual spacing `h = √(2π/N)`,
centered DFT with* ### **`F² = parity` measured at machine at every `N`** *(the same exact identity the
finite places carry as `M² = Π`); class-silent (`B5`) — nothing archimedean touches the coupling. Two
Sonin readings registered longhand: (W1) the BALL reading — `f` and `Ff` vanish on `|x| < λ`, the outer
edge the grid's own truncation (the same declared artifact as the local towers' finite `n`); (W2) the
HARD TWO-SIDED WINDOW — both supports confined to `[λ, Λ]`, with the longhand prediction ON THE
REGISTRATION that the continuum kills this space (Paley–Wiener/uncertainty, at cite).*

## §2 — THE MEASURED TABLE *(36 cells: `N ∈ {255, 511, 1023}` × `a ∈ {1.3, √2, 1.5, √3, 2, 3}` × both `λ`)*

| clause | ### **measured, every cell** |
|:--|:--|
| `M1` dimension | **positive everywhere** (`237…1017`), moving in grid-steps with `a` — the model's own staircase |
| `M2` T-invariance `‖(1−S)FK‖` | ### **`10⁻¹⁴`–`10⁻¹³` at every cell — machine zero; the invariance is structural at `∞` by the same two-sided-ball mechanism as at the finite places** |
| `M3` radical | ### **rank = dim at EVERY cell (`985/985`, `1017/1017`, …) — radical ZERO at the real place** |
| `M3` Hermitian, raw | `22 → 31 → 45` (large, growing `~√dim`) — **the twist is doing real work** |
| `M3` Hermitian, parity-twisted `‖G† − P_S G‖` | ### **`2×10⁻¹⁴`–`4.5×10⁻¹⁴` at every cell — THE SAME TWIST IDENTITY THE FINITE PLACES CARRY EXACTLY (sitting 5) HOLDS AT MACHINE AT `∞`: `G† = G·Π` IS NOW A PROPERTY OF EVERY PLACE OF THE SECTION** |
| `W2` the hard window | ### **dim 0 at every `a ≤ 2`; at `a = 3` (the only window wide enough for transients) dim `19 → 14 → 9` as `N` doubles — DYING UNDER REFINEMENT, exactly the registered prediction: the real place refuses hard two-sided truncation, and the decay was WATCHED, not just cited** |
| `M4` the weight channel `Q_∞` | **alive (nonzero) at every `a`** — and TRUNCATION-DOMINATED: `~N^(3/2)` (`2090 → 5982 → 17029`), with equal-dim rows agreeing EXACTLY, so at fixed `N` it is a pure function of the ball's grid occupancy. ### **The registered `N`-stability clause did NOT land — disclosed, not smoothed; the raw generator mass is a property of the truncation bulk, and a refined statistic is NAMED for future work, not run.** |

## §3 — THE VERDICT: ### **`ext-partial`, THE REGISTERED BRANCH — AND WHAT IT SAYS**

1. ### **"AT CITE BY SYMMETRY" IS RETIRED FOR THE INVARIANCE AND PAIRING CLAUSES.** *The archimedean
   factor now EXISTS AS A COMPUTED OBJECT carrying: T-invariance at machine · radical zero at full
   rank · the parity-twisted-Hermitian identity at machine.* ### **`D-YES` EXTENDS TO THE FULL SECTION
   AT THE MODEL in the invariance-and-pairing sense** *— finite factors exact (sitting 5), the
   `∞`-factor measured at machine — with the weight clause below said in the same breath.*
2. ### **THE REAL PLACE'S DISTINCTIVE GEOMETRY, LOCATED AND MEASURED:** *the finite places afford hard
   two-sided truncation EXACTLY (`(pⁿ−1)²`-dim spaces, certified); the real place does not — its
   two-sided window is empty or refinement-dying, so its Sonin factor is necessarily BALL-VANISHING
   WITH A SOFT OUTER EDGE.* **If a codomain absence has an address at `∞`, this is its shape: not a
   failure of interleaving (that held at machine) but the impossibility of hard archimedean support —
   the uncertainty principle standing where a `p`-adic ball would stand.**
3. ### **THE PUNCTUATED-WEIGHT RE-READ, PER CHANNEL:** *the `∞` channel is ALIVE at every `a` while the
   finite channels are punctuated (0 through `a = 2`; `6√6` at `a = 3` three-place; 0 again
   four-place) — so the full-section weight keeps the sawtooth's zeros, and BETWEEN the zeros the
   `∞` channel multiplies in.* **But the `∞` magnitude is truncation-dominated in this model, so the
   ledger banks only its ALIVENESS and its ball-occupancy staircase — the magnitude awaits the refined
   statistic. Nothing more is claimed.**

## CLOSING — RELAY-ONLY, FOR THE AUTHOR'S WORD

1. ### **THE ONE UNMODELED SLOT IS MODELED; the scope line of `D-yes` shortens from "archimedean at
   cite" to "archimedean weight-magnitude model-dominated".**
2. ### **THE TWIST IDENTITY `G† = G·Π` IS NOW UNIFORM ACROSS THE SECTION — exact at every finite place,
   machine at `∞`.**
3. **Named, not run:** *the refined `∞`-weight statistic (a ball-difference or window-compressed form
   that subtracts the truncation bulk) · more staircase steps · the Mathlib cyclotomic companion.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO PROMOTION TO `W_∞ − ΣW_𝔭` AT COMPLETE ROSTER.
NOTHING DEPOSITS.**
