# THE INSTRUMENT REPAIRED + THE LADDER RE-GRADED — v0.2.0 — 2026-08-10

**`SIDE-carrier-spec` v0.2.0 = `2051c52`, PRIVATE, tag peel-verified.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — THE REPAIR

**The defect was correctly left for the author's ruling; the ruling was: repair.**

* **`arch_present` → SCAFFOLDING.** Retained, **with its defect quoted verbatim in its own
  docstring** rather than quietly deleted. `arch_present_of_arch_independent` proves it is now
  **redundant** — the precise sense in which it is scaffolding, and the reason it stays legible
  against its own history instead of being erased.
* **`arch_independent` — the repair. Independence stated as SEPARATION:** *there exist two
  classes agreeing at every finite place and differing at the archimedean one.* Not a magnitude
  condition, not an order condition.
* **`Grading.factored_fails_arch_independent` — the worked counterexample, compiled.**

> **W-2 DISCIPLINE, CHECKED AT THE MOMENT OF WRITING, NOT IN A LATER AUDIT.** The new field
> speaks only about **information** — never magnitude, sign, or positivity — **so it cannot
> smuggle definiteness in.** `witness_arch_independent_and_still_degenerate` compiles it: the
> witness satisfies the new field and **remains completely degenerate.** **The repair
> strengthened the specification without moving it one step toward the conclusion.**

**Sixteen terminals, none outside the clean profile. 774 jobs, 0 sorry, 0 native_decide.**

---

## §2 — THE RE-GRADED ROW

| field | v0.1.0 | **v0.2.0** |
|:--|:--|:--|
| `cancel`, `neg`, `finPart` | PASSES | **PASSES** |
| `components_faithful` | FAILS on `ℚ^×` | **FAILS** |
| **archimedean** | `arch_present` **PASSES, hollowly** | **`arch_independent` FAILS — compiled** |
| `act` | SPLITS | **SPLITS** |
| `pair` | FAILS | **FAILS** |

**Expectation confirmed:** `arch_independent` fails by the product formula, **compiled rather
than argued** — `log|q| = −Σ_p v_p(q)·log p`, so no two classes agree below and differ above.

> **NOTHING ABOUT THE LADDER CHANGED. THE INSTRUMENT DID.**
>
> ### **THE ROW'S HONEST SENTENCE: THE DIVISOR LADDER IS THE CARRIER'S FINITE-PLACES SHADOW — COMPLETE BELOW, AND SILENT AT THE ONE PLACE WHERE FREEDOM IS REQUIRED.**

---

## §3 — `F.2026-08-10-d` — THE ONE-FACT-THREE-COSTUMES QUESTION *(QUESTION grade)*

**(i)** the hollow-pass mechanism (**compiled**) · **(ii)** `2602.15941`'s genus-zero passage
(*"the classical Jacobian … is trivial, suggesting a curve of genus zero"*) · **(iii)** §24.4's
single-object obstruction (*"ξ is a single object, with no ensemble to equidistribute"*).

> **NAME-IDENTITY DISCIPLINE, STATED BEFORE ANY ANSWER.** Three different names, three different
> objects — a pair of maps, an ideal class group, one analytic function. **Resemblance of shape
> is not identity of content**, and this programme has a filed error class for exactly this merge.
> **No verdict is entered.**

**Adjudication route recorded so it can be closed honestly:** verify at content whether
`Pic⁰`-triviality **is** the product-formula dependence; whether §24.4's obstruction **reduces
to** the same absence of archimedean freedom; **verdict from the texts, not the rhyme —
SAME-FACT / KIN / DISTINCT.** **DISTINCT is a real and expected outcome**: *"there is only one of
it"* recurs across mathematics for unrelated reasons.

## §4 — `F.2026-08-10-e` — THE SIGN-AT-TORSION OBSERVATION *(QUESTION grade; null arguably favoured)*

The divisor picture is blind **exactly** to ℤ/2 sign data (compiled: `two_torsion_trivial_of_spec`;
the exclusion lands at `−1`). The clause is sign-type. **Related, or a pun?**

> **THE REASON THE NULL IS LIVE IS ITSELF A NAME-IDENTITY REASON.** *"Sign"* does two different
> jobs: **group-theoretic** (the ℤ/2 torsion of `ℚ^×`, an involution) versus **order-theoretic**
> (position relative to zero in an ordered target). **Nothing so far connects them.** And the
> free-layer scoping — *the seed generates involutions, not pairings* — points the same way:
> **involutions are exactly ℤ/2 data**, so that layer is precisely the one that does not produce
> pairings. *(Carried on the author's authority; not independently verified here.)*

---

## §5 — THE REQUIREMENTS ENTRY, RESTATED IN DELTA FORM

**The six abstract constraints remain as the outer specification.** The inner statement — what a
carrier must **add** to the known object:

| # | must be ADDED | support | grade |
|--:|:--|:--|:--|
| **Δ1** | **archimedean independence — the genus escape** | `factored_fails_arch_independent` | **COMPILED** |
| **Δ2** | **a bilinear pairing target** | the survey's one absence; `pair` FAILS at the field | **CITED + graded** |
| **Δ3** | **sign / torsion carriage** | `two_torsion_trivial_of_spec`; `F.2026-08-10-e` | **COMPILED (exclusion) + QUESTION (significance)** |

> **THE DELTA IS NOT A PLAN.** It names what is missing from a specific known object at three
> named places. **No construction, no route, and nothing in it closer to the clause than the
> survey was.**

---

## §6 — STOP-AND-HOLD

**No progress on the clause and none claimed.** The repair made the instrument stricter and the
candidate's verdict worse; both questions are filed **as questions**, with the reasons a null
answer is live written into each. **The strongest single sentence produced this pass is a
statement about an instrument, not about ζ.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `61829ab` → this pass's commit |
| relay | `09e56ed` → this report's commit |
| **`SIDE-carrier-spec`** | **tag `v0.2.0` = `2051c52`; `refs/tags/v0.2.0^{}` peel-verified = local; PRIVATE** |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
