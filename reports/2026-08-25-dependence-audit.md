# b166 — THE DEPENDENCE AUDIT

**Registration `c6ca357`, banked before the owners were read and before the audit.**
2026-08-25. Ferry part 1 of 1, receipt confirmed in full before execution.

> ### **THE ENTIRE DENSITY LANE'S MEMBER-DEPENDENCE IS ONE AFFINE DIRECTION.**
> *Core `284 → 288/288`, correspondence row 74, no blank cells. No instrument was run and no
> new measurement was made — every number below is banked at a named act, and what this act
> did to them is algebra.*
>
> ### **NO MEMBER IS PREFERRED, RECOMMENDED OR ARGUED FOR, AND THE AUDIT'S OUTPUT MAY NOT BE
> ### CITED AS EVIDENCE FOR ANY MEMBER.** Route A and route B remain the author's.

---

## THE DEVIATION, DECLARED IN THE REGISTRATION BEFORE THE WORK

The ferry's execution line reads *"owners read at content first; run-registration before the
audit."* ### **This act banked its registration FIRST.** *Law 8 — predictions are registered
before the evidence — governs the executor's own scoreable grounds, and grounds written after
reading the owners would be retrodictions wearing a prediction's clothes.* The ferry's clause
is satisfied in its operative sense, since the reading still precedes the audit.
### **A DEVIATION TOWARD THE STRICTER LAW IS STILL A DEVIATION AND IS RECORDED AS ONE.**

---

## COMPONENT 1 — THE AUDIT

### THE STRUCTURAL FACT IT ALL TURNS ON

b154 at content: `W₊(u) = Σ_even a_n − Σ_even b_n − μ·resid_N` with `μ := Σ_{n even} u_n`.
b115's decisive structural fact at content: **`A_n` and `e_n` carry no window parameter.**
### **Hence with `Δ(u) := Σ_n A_n(u) − Σ_n e_n(u)` — window-free —**

> ### **`Ψ_μ = Ψ_σ + (σ_even − μ)·Δ`**

and because every operation the lane applies after b38's definition is **linear in the
density** — integration against `C_a`, the `u = Lσ` rescaling, the `ΦK` pairing, the
coarse/fine convolution split — ***the same shift passes through all of them unchanged***:
`I_μ(L) = I_σ(L) + (σ_even − μ)·J(L)`, and likewise for the coarse and fine pairings
separately. ### **THE LANE DOES NOT HAVE SEVERAL INDEPENDENT OPEN DEPENDENCES. IT HAS ONE,
AND ONE MEASUREMENT DECIDES ALL OF THEM.**

### TWO CHECKS RUN BEFORE ANY VERDICT

*The audit's own arithmetic is checked against the record it reads, not assumed:*

- ### **THE ARCH COLUMN'S SIGN, FORCED RATHER THAN ASSUMED.** `D = N/A` reproduces the
  recorded deviations only with ### **`A` NEGATIVE AT EVERY CELL**, and `|N/D|` then returns
  b155's banked `|A|` column to its printed digits *(1.990520 vs 1.990528; 1.405381 vs
  1.405398)*. ### **Two acts that never cited each other agree on a column neither printed.**
- ### **THE SHIFT LAW AGAINST b155's OWN NUMBER.** From b154's family alone,
  `D_μ = D_σ + (μ−σ_even)·resid_N/|A|` — *which is b155's own law, reached independently.*
  Erasing the `a²=12` deviation needs `dμ = 0.004411` from b110's `−0.007261`; b155 banks
  `0.004435`, computed there from b109's **rounded** `−0.0073`. ### **The difference is the
  rounding, named rather than smoothed; neither number is wrong at its own input.**

### THE VERDICTS, UNDER THE RUBRIC THE FERRY FIXED

| result | verdict | the dependence's form, or the deciding step |
|:--|:--|:--|
| the collapse to one density *(b115)* | ### **INVARIANT** | `Ψ_μ` is window-free for every member |
| the scale-average form *(b115)* | ### **INVARIANT** | derived from the window's pure dilation; no member in it |
| the arch column divides out *(b115)* | ### **INVARIANT** | zero set of `D_μ` = zero set of `N_μ`, every member |
| both derivative forms *(b115)* | ### **INVARIANT** | properties of the kernel, which the window builds |
| b116's orientation **implication** | ### **INVARIANT** | its proof uses only `ΦK` mean-zero, positive-then-negative |
| `σ_even`'s cell-independence *(b110)* | ### **INVARIANT** | the parameter is not in its definition — ***a measured quantity that is invariant outright*** |
| the crossing *(b110)* | ### **PARAMETER-CONDITIONAL** | `D_μ = D_σ + (μ−σ_even)·resid_N/\|A\|`; a sign change exists in the grid exactly for `μ ∈ (0.5792095, 0.6209115)` |
| the small-`L` limit's sign | ### **PARAMETER-CONDITIONAL** | `Ψ_μ(0) = N_even − μ·NMODE`, derived; vanishes at `μ = ½` |
| b109's exact reduction | ### **BOTH, ON THE READING** | form-**invariant** when the target moves with the member; **conditional** with an extra `(σ_even−μ)·resid_N` when the target is held at the t-mass share |
| b117's one-sign verdict | ### **UNDECIDED** | `I_μ = I_σ + (σ_even−μ)·J`; ### **bank `Δ` on the licensed range** |
| b119's dominance | ### **UNDECIDED** | coarse and fine pairings affine in `μ`; ### **the same one step** |

### THE ROOT CAUSE OF THE SPLIT VERDICT

### **`σ_even` PLAYS TWO ROLES IN b38's DEFINITION** — the apportionment **weight** and the
t-mass share that is the share statement's **target** — ### **and b38's member is exactly the
one that ties them together.** *In the family they come apart.* A reader who does not say
which he holds fixed gets two different dependence verdicts for one identity and takes one of
them for an error. ***b163's finding is the fixed-target reading, and it is confirmed here at
content and by exact algebra.***

### THE NUMBERS THE PARAMETER MOVES

**The crossing.** Per-cell zero shifts `dμ = −D_σ/ratio`: `−0.0372908`, `−0.0267681`,
`−0.0215848`, `−0.0057463`, `−0.0023937`, `+0.0044112`. ### **All six deviations are positive
for `μ > 0.6209115` and all six negative for `μ < 0.5792095`, so a sign change exists in the
recorded grid exactly for `μ ∈ (0.5792095, 0.6209115)` — an interval of width `0.0417020`.**
b38's member lies inside it; the family's free end `μ = 0` lies outside it, and there every
recorded deviation is negative.

**The small-`L` limit.** From b115 (2g) — `A_n(0) = 1` every mode, `e_n(0) = 0` exactly —
### **`Ψ_μ(0) = N_even − μ·NMODE = 5 − 10μ`, derived, no measurement.** At b38's member it
returns `−1.1650030`, ### **which is b115's banked `Ψ(0) = −1.165003` to its printed
digits.** At the free end it is `+5`. It vanishes at `μ = ½`.

> ### **THE GUARD, HARD, ON THAT LAST NUMBER.** `½` is `N_even/NMODE`, the even sector's
> **counting** share. ### **That a limit's zero sits at a recognizable number earns that
> number nothing** — the standing anti-numerology guard applies here exactly as the `√2`
> precedent applied to the crossing's location. ### **NOTICED IS NOT EARNED.**

### THE TWO UNDECIDED, AND THE ONE STEP THAT DECIDES BOTH

`I_μ = I_σ + (σ_even−μ)·J` with `J(L) := ∫₀² ΦK(s)Δ(Ls) ds`. `I_σ` is banked; ### **`J` is
not, because `Δ` is banked at exactly one point — `Δ(0) = NMODE` — and nowhere else.**

> ### **THE DECIDING STEP, NAMED: BANK `Δ(u) = Σ_n A_n(u) − Σ_n e_n(u)` ON THE LICENSED
> RANGE.** *One instrument run on b38's own `trace_modes` and `e2_of_grid` — the functions
> b115 and b116 already call.* ### **No new mathematics, no new definition, and nothing the
> executor would have to define.**
>
> ### **WHY IT IS NOT RUN HERE:** this ferry's scope is *"derivation-first from banked
> objects only"*. ### **A measurement is not a banked object, and the audit does not widen
> its own scope to reach a result it would like to have.**

*And what is undecided is the VALUE at other members.* ### **b117's sign and b119's dominance
at b38's member are not disturbed and not re-graded**, and stand with their own three stated
limits — samples, sampling, margin.

### THE SORT, SCORED AGAINST THE FERRY'S OWN WARNING

The ferry registered that *"structural rewritings are candidates for INVARIANT and computed
deviations are candidates for PARAMETER-CONDITIONAL, but the verdict is what the derivation
says, and **an expectation that sorts neatly is a warning rather than a confirmation**."*

### **THE SORT FAILS IN BOTH DIRECTIONS.** A **structural rewriting that is not simply
invariant** — b109's reduction, whose verdict depends on which target is held fixed. And a
**computed quantity that is invariant outright** — `σ_even`'s cell-independence, untouched
because the parameter is not in its definition. ### **THE SHAPE OF A STEP IS A POOR GUIDE TO
ITS DEPENDENCE** — the second instance of the lesson b163 learned.

---

## COMPONENT 2 — THE STATEMENT AND THE FILINGS

### THE STATEMENT, FILED AT THE BRIEF AND AT THE REGISTER'S ROUTE-A ROW

> **A route-A ruling does NOT reach the lane's architecture** — the collapse to one density,
> the scale-average form, the arch column's dividing out, both derivative forms, b116's
> orientation implication and `σ_even`'s cell-independence hold **for every member**, because
> their derivations run on the window's construction and the kernel's shape and ***neither
> carries the parameter***. **It DOES reach the lane's recorded numbers** — each cell's
> deviation moves by `dμ·resid_N/|A|`, a **cell-dependent** amount; whether a sign change
> exists in the recorded grid is true only on an interval of width `0.0417020`; and the
> small-`L` limit's sign changes across the family. **And for two results it is not yet
> enough** — b117's one-sign verdict and b119's dominance are affine in the member along the
> single direction `Δ`, which is banked at one point, ### **so at any member but b38's
> neither is decided until `Δ` is banked on the licensed range.** ### **None of this is
> evidence for any member.**

### THE FOUR UNESCAPED-PIPE ROWS — DISCHARGED

Seven stray characters located **by position**: `tr(S|Son)` and `tr(Π|Son)` *(row 36)*,
`8 | N at 16, 64, 256` *(row 48)*, `|x| ≤ 1` twice *(row 55)*, `|G|² = N` twice *(row 62)*.
Each escaped in place. ### **ESCAPING ONLY — the rendered text is identical and no cell
content was altered.**

### **THE RE-PARSE: all four now read at SIX cells with grade `DERIVES` — exactly what b164
recorded them as carrying at a shifted column.** ### **And the whole table re-censused: 74
rows, 74 parsed, none unparsed.** Grade families `66 DERIVES + 2 INTERFACES + 6
other-or-none-by-design`, which reconciles with b164's tally of the original 73 under a
different bucketing ***with no row moving***.

### A FRONT-DOOR DEFECT FOUND IN THE SPINE'S OWN HEADER

### **`CORRESPONDENCE.md` said 271 while its own rows and `AXIOM_PRINTS.txt` said 284.** The
breakdown ended at *"+ 3 mechanism"* — row 68, b115. ### **Rows 69–73 and their thirteen
terminals were added across five acts without the header moving, and four seam closes read
past it.** Found because the count was **read** rather than carried. Repaired to **288**,
with the per-module counts ### **re-counted from the module files, not taken from any
summary**.

### **THE LESSON: A DOCUMENT'S OWN HEADER IS A FRONT DOOR TOO.** *The front-door watch has
been looking outward at README, AGENTS and Milestones; this one was inside the spine.*

---

## THE THREE FRONT-DOOR CORRECTIONS — DRAFTED, NOT APPLIED

### **Nothing is rewritten in place.** *Two of the three live in `SIDE-effects`, which this
seat does not write; the third is drafted rather than applied because the ferry says drafted.*

**(1) `SIDE-effects/AGENTS.md`, under "Theorems exported"** — currently names twenty
theorems; b157 found **2 present, 18 absent**. Proposed replacement for the "Named theorems
include:" list:

> Named theorems — **checked against the source, and the source is the honest party**:
>
> - **Present in `SIDEEffects/Structural.lean` (2):** `no_type_d`, `formation_seven`
> - **Retired as content-free and no longer exported (18):** `all_gapped`, `mass_gap`,
>   `all_excluded`, `sectors_complete`, `gap_bounds`, `twist_cancels`,
>   `formation_preserved_grh`, `grh_exclusion`, `no_ls_zero`, `no_conspiracy_twins`,
>   `no_conspiracy_goldbach`, `no_conspiracy_sg`, `all_bound`, `sha_bounded`,
>   `all_mismatch_absent`, `bsd_full`, `artin_from_grh`, `side_exclusion` — see the
>   retirement ledger at the head of `SIDEEffects/Structural.lean` (audit Phase S.2–S.4),
>   which records each as a True-stub or opaque-Prop skeleton and names where genuine content
>   lives.
>
> The genuine content the ledger points to is present at `Phase15/Module1.lean`
> (`crt_exhaustiveness`, `no_type_d_conspiracies`) and `Phase15/SIDEFramework.lean`
> (`SIDE_exclusion`, `determination_collapse`, `side_bridge`) — **named as present, not
> graded here**.

**(2) `SIDE-effects/SIDEEffects/Milestones.lean`, line 67** — currently
`3 theorems, 3 sorrys, 0 axioms.` Proposed:

> `3 theorems, 3 sorrys, 0 `axiom` declarations — and all three terminals depend on `sorryAx`,`
> `because a sorry-closed theorem carries an axiom. #print axioms on any of the three says so.`

*b157's ground, unchanged: in a file whose entire purpose is honesty about what is and is not
proved, that line is the one sentence a reader could take the wrong way.* ### **The rest of
the file is exemplary and the draft changes nothing else.**

**(3) `SIDE-global-section/README.md`, lines 22 and 25** — currently **212 terminals**
*"across 50 modules"* and `AXIOM_PRINTS.txt` **(250/250)**. Proposed, with the breakdown
**extracted from `CORRESPONDENCE.md` at run time rather than retyped**:

> **288 terminals** (103 construction-era + 11 purity-and-distance + 7 silence-theorem + 8 Plancherel-shadow + 3 cross-place + 3 pairing + 3 twisted + 3 deficit + 22 character-sum + 3 flatness + 3 silence-only-if + 6 product-minus-sum + 3 metaplectic-root + 2 twisted-root + 3 theta + 5 odd-pairing + 2 product-law + 4 tower-limit + 5 phase-plane + 4 lefschetz + 3 frame-boundary + 3 fourth-visit + 3 theta-continuation + 3 coherence + 4 critical-pass + 6 adelic-plane + 5 radialization + 3 window + 4 rigidity + 3 value + 3 theta-bridge + 3 determination + 4 constructor + 4 bridge-half + 4 extension + 4 tower-monotone + 3 link + 3 orientation + 3 mechanism + 4 apportionment + 4 share-dependence + 2 double-entry + 1 refinement-arity + 2 sector-occupancy + 4 dependence) across **62 modules** plus their AxiomCheck

> re-verified at assembly, `AXIOM_PRINTS.txt` (**288/288**). No terminal failed the bar; none
> is excluded.

*(62 = 77 `Core/*.lean` files minus 15 AxiomCheck files, counted this act.)*

---

## THE AUDITS — EMITTED, NOT TYPED

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b166
  run at    : 2026-08-25T17:32:07 (local)
  input     : whole file b166_registration_2026-08-25.txt (created this act)
  input     : whole file b166_dependence_audit.txt (created this act)
  stems     : gap, blind
  files     : 2
  lines     : 388
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 d7b918d4a8e4c57bfed79a2d4a8486b0
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b166-docs
  run at    : 2026-08-25T17:35:19 (local)
  input     : added lines in D:/MY-DOwnloads/PLACE-papers vs HEAD~1
  stems     : gap, blind
  files     : 1
  lines     : 92
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 953fcd8842bbaa136bfb27b28902368c
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : mirror_verify
  act       : b166
  run at    : 2026-08-25T17:34:52 (local)
  input     : mirror-refresh-2026-08-25.zip
  files     : 33
  rows      : 33
  mismatch  : 0
  declared  : fa2b464
  ls-remote : fa2b464f086a
  VERDICT   : CLEAN ON BOTH CLAUSES
  self-hash : sha256/32 120251f0f2527ec8ae546dbaa8572e16
=== END AUDIT SIDECAR ===
```

*The relay self-description check was **run** at the commit and is not embedded: its subject
is the commit that would contain it, so embedding it cannot reach a fixed point. It appears
in the verifier as **UNUSED**, which is run-but-not-quoted and not a failure.*

### **AND THE MIRROR'S FIRST RUN IS RETAINED BESIDE ITS SECOND, WHICH NEEDS SAYING PLAINLY:
the first returned NOT CLEAN because the invocation omitted the remote, so clause 2 never
ran** — ### **it was an incomplete invocation, NOT a mirror defect.** *b156's retention
repair is why both runs survive to be compared.*

---

## THE SEATS, SCORED

**NAVIGATOR: both land.** P1 — every named result decided under the rubric with its chain
shown, and the honest expectation stated in advance then failing in both directions, which
the ferry itself named as the outcome to watch for. P2 — one paragraph from the verdicts
only, no member named as preferable, filed at both places, with the finite-decidable part
entering Core.

**EXECUTOR: all four grounds land — and one of them landed for a reason its author did not
have.** (e1) at least one result UNDECIDED — **landed**, two do; ### **but the registration
blamed `R_N`'s undefined split, and the actual blocker is a different unbanked object, `Δ`,
which needs no definition at all — only a measurement.** ***A prediction that lands on the
wrong ground is recorded, not quietly enjoyed.*** (e2) the neat sort fails — **landed, in
both directions**. (e3) something enters Core — **landed**, four terminals, and the
calibration trigger was armed. (e4) the pipe repair changes the parsed count and nothing else
— **landed**; had any grade read differently the repair would have stopped.

---

## THE DEFECTS THIS ACT MADE, RECORDED RATHER THAN ABSORBED

- ### **A guard that fired falsely on my own substring.** `'DependenceShadow' not in text`
  returned false — because ### **`ShareDependenceShadow` CONTAINS it.** *That is b164's own
  law biting its author: **retrieval by string is not retrieval by object**.* Nothing was
  written; the guard was rewritten to exact-line membership.
- ### **A Lean parse error that produced `sorryAx`**, caught at the first build. Nothing
  carrying a `sorry` was ever staged. **Rule 5 held because the build was read, not assumed.**
- ### **A terminal that came back depending on `propext`**, restated in pure Bool form and
  re-run to zero axioms. ### **The zero-axiom bar is a bar, not a preference.**
- ### **An anchor quoted from my memory of a line-wrap, wrong by two characters** — re-read
  at content and shortened to a span verified unique. ***A span quoted from memory is not a
  span read.***
- ### **THE VOCABULARY SCANNER PRINTED `CLEAN` OVER AN EMPTY SCOPE.** Bare filenames are
  ignored; it needs `--new`. ### **`files in scope: 0` and `VERDICT: CLEAN` on the same
  screen** — *a checker reporting clean because it looked at nothing.* Re-run correctly over
  388 real lines, clean. ### **FILED AS A FINDING AND NOT REPAIRED — a tool change is gated,
  and this ferry did not authorise one.**

---

## THE THIRTY-FIFTH SEAM'S DEBT, RESTATED

Whatever the next arc lands; the rulings if any come — item 1's, route B's, and the
methodology day's; b157's six findings; ### **the three front-door items, now DRAFTED but
still the author's to apply**; ### **and one new item: `Δ` on the licensed range, the single
measurement that would decide both undecided verdicts together**; and the deep items reserved
for a session with breadth. ### **The four unescaped-pipe rows are DISCHARGED this act and
leave the debt.**

*The patent seat's untracked trees under `phase1.5/method/patent-package/figures/` were seen
in `git status` and* ### **left untouched — read, never written.**

---

## PINS AT CLOSE — by `ls-remote`, never from recall

| repository | pin |
|:--|:--|
| `PLACE-papers` `main` | `fa2b464f086a929d909b519122d06df87c54dd24` — the loom filings |
| `SIDE-global-section` `main` | `2b6bac63031936ec2c9de06b7579ec3aab10c56d` — **Core 288/288, row 74, the pipe repair** |
| `relay` `main` | *the pin line below, read back after the push* |
| mirror | `mirror-refresh-2026-08-25.zip` — **33 files**, rebuilt at `fa2b464`, **CLEAN ON BOTH CLAUSES** |
| HELD | `held/carrier-acts` = `6eada6a` — **LOCAL-ONLY, unpushed** |

**Load this export:** `mirror-refresh-2026-08-25.zip`.

*STOP — the ferry's end.*
