# `W-CONSTRUCTION-1` · ACT 7 — THE TWO UNKNOWNS COMPUTED
## ### **`Δ₋`'S CLOSED FORM: THE ODD-INDEX PART OF CC'S OWN `t(n)` SERIES (THE `ε′(1⁺)` PIN SPLIT 14.1773 / 8.8191, b35 ON THE OPUS LANE, C1–C3 PASS) — THE QUOTIENT'S NORMALIZATION `(N-forced)` AT THREE NAMED JOINTS, THE VOLUME CONVENTION DISSOLVING ACT 5'S CAVEAT — THE COMPARISON'S OPEN CONTENT NOW EXACTLY `(L-identity)`**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 7 · sub-gate declared · Rules 4/5 · nothing
deposits · nothing circulates**

> ### **RULE-3 LOG:** *act 6 at `048de1f` ✓; `v0.6` `§§13–16`, `OrbitDictionary` 7/7,
> `eigenvector_of_commute` at the classical profile, the `§15` grading ✓; the banned grep on this
> ferry: CLEAN. The ferry's "verified before use" RUN: sitting 23's parity naming re-read at content
> (`n ≡ 0 mod 4` on prolate indices) and composed with P2's banked `ξ_n = √2·ψ_{2n}` — the split
> lands in CC's OWN index (`E₁` = even `n`; `(−1)` = odd `n`) BEFORE any number moved. Routing per
> the ferry: the OPUS lane was AVAILABLE — b35 ran on Opus, first attempt, recorded inside the
> banked registration. Terms coined: none.*

```
build document  THE_GLOBAL_SECTION.md v0.7 (§17 Δ₋; §18 the normalization; §19 the
                comparison re-written)
bench           data/b35_registration_2026-08-18.txt (banked FIRST) + b35_2026-08-18.txt
                (the Opus lane; C1–C3 PASS)
vanilla leg     tools/lean/NormalizationForced.lean + AxiomCheckNF — 5/5 "does not
                depend on any axioms" (v4.29.1 pinned); the relay's zero-axiom count: 80
mathlib leg     recompiled, 0 errors; THE LEDGER UNCHANGED: 2 (B–C; infrastructure)
```

---

## §1 — ITEM 1: `Δ₋`

**Registered longhand first: (Δ-small) / (Δ-structure) / (Δ-third).** Then:

1. ### **(Δ-structure) LANDS: `Δ₋`'s series is the ODD-INDEX part of CC's own `t(n)` series** —
   *the same `λ(n)`, `ξ_n(1)` data, complementary parity, per the corpus's banked
   `t(n) = λ(n)²ξ_n(1)²/(1−λ(n)²)`.* At the `ε′(1⁺)` pin: **`E₁` share `14.1773` (61.6%); the
   odd-sector share `8.8191` (38.3%); tail `3.19e−05`** — of the banked `22.9964757`.
2. ### **(Δ-small)'s sign clause: per-mode positivity is STRUCTURAL** (each `t(n)` a square over
   `1−λ² > 0`); the shares are DATA at the pin. *The full `ε₋(ρ)` inherits CC's series shape
   restricted to odd `n`; the trace-class bookkeeping remains the ε-lemma's named owner.* **Recorded
   as the object's archimedean correction. NO sentence about Weil's ledger.**

## §2 — ITEM 2: THE NORMALIZATION

> ### **VERDICT: `(N-forced)`, proof grade modulo ONE cited lemma — the mechanism at three joints:
> the TAIL (the `E₁`-unit's norm-1 forces factor 1 at inactive places); the UNIFORMITY (every place
> is a unit-place for part of the class — the class-richness lemma, Schwartz–Bruhat at cite, owed
> to B–C — so one local functional per place serves the whole class); the LEVEL (the banked
> on-the-nose Gram factor `p²` forces the VOLUME normalization: the volume ratio is exactly `p²`
> while the dimension ratio is `9 ≠ 4` at the smallest cell — vanilla, no axioms).**

*The refinement of act 5 — an UPGRADE of a declared choice, not a correction: under the volume
convention the identity coefficient `(p^n−1)²/p^{2n}` climbs to 1 (exact instances), and `§12(b)`'s
divergence caveat dissolves; the silence results are normalization-blind.* ### **CONSEQUENCE:
`Θ_quotient` is a DEFINITE distribution on the class — the Weil template with fiber-exact,
volume-normalized weights. Its coefficients against Weil's: part of `(L-identity)`, not assumed.**
*(N-free)'s registered content — the free scalar as the codomain's size in one number — is
REFUTED by the mechanism, modulo the cited lemma, and said so.*

## §3 — ITEM 3: THE COMPARISON, RE-WRITTEN

**`§19` on the build document's face, every term re-graded. One line per side: LEFT — nothing open
(classical). RIGHT — the odd-sector bookkeeping beyond the pin, and the quotient coefficients
against Weil's.** ### **THE EQUATION'S OPEN CONTENT IS NOW EXACTLY THE `(L-identity)` ITSELF —
still the equality on the constrained class, still behind the closure protocol, still NOT DECIDED.**

## §4 — ITEM 4: THE RECORD, AND THE TWO-SORRY LEDGER'S PATHS RE-STATED

*`v0.7`; rows 96–99; `F.2026-08-18q`; both legs' prints run; this report the bank.* **The ledger
(2, owners intact) with what act 7 needs from each: B–C (the L² realization) would formalize the
class-richness lemma (joint ii) and the quotient's orbit series; the infrastructure sorry (the
Hilbert `⊗′`) is exactly where the forced normalization becomes a THEOREM — the unit-normalized
restricted-tensor trace is its statement.**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED. NOTHING PROMOTED.
NOTHING DEPOSITS. NOTHING CIRCULATES.**
