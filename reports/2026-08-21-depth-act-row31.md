# THE DEPTH ACT, ROW 31 — THE GENERAL CHARACTER-SUM THEOREM, THE CONJUGATE-PAIR MECHANIZATION, AND THE FLATNESS LAW
## ### **ALL THREE COMPONENTS LAND. THE GENERAL μ₄ CHARACTER-SUM THEOREM IS PROVED AND COMPILED AT ZERO AXIOMS — GENERAL ROSTER, GENERAL DIMENSIONS, HYPOTHESES NAMED — CLOSING ROW 31'S NAMED OPEN AND ROW 32'S NOWHERE-FLAT AND GENERAL-k NAMED OPENS; THE CONJUGATE-PAIR ROUTE CLOSED IT WITHOUT THE FORTY-MONOMIAL EXPANSION AND NOTHING RESISTED. THE FLATNESS ARBITRATION LANDS BRANCH (a): ODD-PLACE FLATNESS AND THE PLACE-2 UNIT TRACE ARE THEOREMS AT EVERY LEVEL — THE FLAT-PLACE COLLAPSE IS A LAW, THE CHARACTER WEIGHT CONCENTRATES AT THE PLACE 2 BY THEOREM, AND THE PLACE-2/ℤ[i] READING IS CONFIRMED-AS-STRUCTURE AT THE PROVED SCOPE. CORE 166/166.**

**Relay report · 2026-08-21 · ferry-executed (part 1 of 1, receipt confirmed, Rule 1) ·
Rules 3/4/5 · registration banked before any code or algebra; P1/P2 verified from the
BANK-READ dims before anything was built · nothing about `h2` claimed; the range law and
the leaves distinction govern · nothing deposits · nothing circulates beyond this report.**

> ### **RULE-3 LOG:** *(a) `form_level_lift` read AT STATEMENT before deriving (its two
> hypotheses — trace-linearity, trace-multiplicativity — are the general theorem's, plus
> the μ₄ spectral decomposition, and no more). (b) the sector dims READ FROM THE BANKS by
> the instrument itself (regex over the b53/b55 bank texts — never hardcoded, never from
> the ferry's summary); the tuple order confirmed against `dead_cell_power_traces`. (c) the
> purity-locus law entered as the registered explanation-candidate only; Component 3's P3
> run tested it (and it held). (d) the component order held: 2 used 1's statement shape; 3
> independent of both. OPERATIONAL DISCLOSURES: core Int algebra lemmas (`Int.add_comm` et
> al.), `simp`, and `ac_rfl` all print TAINTED at this pin (propext; `ac_rfl` also
> Quot.sound) — each print-checked BEFORE use and routed around; the entire mechanization
> is explicit rewrite chains over named hypotheses. One instrument slip (a dead first-draft
> regex block) crashed the run before any measurement; removed and disclosed. Banned-term
> grep on the registration, bank, instrument, and this report: clean. Terms coined: none.*

---

## §1 — THE REGISTERED PREDICTIONS, VERBATIM, WITH VERDICTS BESIDE THEM

> **(P1)** *"The conjugate-pair lemma: tr(M³) = conj(tr M) and tr(Π) real at every banked
> cell — verified from the banks before the mechanization is attempted."* ### **VERIFIED
> at all eight banked cells from the bank-read dims** — t₃ = conj(t₁) everywhere; t₂ real
> by form; the values exactly the longhand targets (odd: 0, 0; place-2: i, −1).

> **(P2)** *"every odd-place cell exactly flat; every place-2 cell carrying twisted trace
> exactly i (the i-sector excess exactly one, the other three sectors equal)."* ###
> **VERIFIED at all banked levels** — odd flat at 1, 4, 16, 169; place-2 tuples
> (d, d, d+1, d) at d = 0, 2, 12, 56.

> **(P3)** *"at each place-2 cell, the i-sector excess direction is spanned by the
> imported boundary seed … decidable exactly at the banked cells; the prediction extends
> one level past wherever it is checked by the tower-import mechanism, stated as the
> extension clause, not assumed."* ### **VERIFIED** — the ι-chain of the (2,1) seed
> (u = δ₁ − δ₃, Su = 2iu longhand) is a nonzero exact i-eigenvector with single-row
> support (rows 2 → 4 → 8) at (2,2), (2,3), (2,4); **the (2,3) instance is NEW this
> act**; the excess count d_i − d₋ᵢ = 1 matches the one imported seed line everywhere,
> the purity-locus law naming it canonical. The extension clause stands as stated.

## §2 — COMPONENT 1: THE ABSTRACT GENERAL THEOREM — **LANDS**

The general character-sum identity is derived as the identity-operator instance of
`form_level_lift`: for commuting order-four local transforms on a tensor product over a
GENERAL roster with GENERAL sector dimensions, **four times the principal-sector
dimension equals the sum over the four powers of the products of the local traces** —
values in the Gaussian integers, with reality of the total following from the
conjugate-pair lemma (t₃ = conj t₁ place-wise, conjugation multiplicative over the
product, the even powers real by form) — never from expansion. Hypotheses named and no
more: trace-linearity, trace-multiplicativity, the μ₄ spectral decomposition with
integer sector dims. **Compiled general: `CharacterSumShadow.character_sum_general`.**
The five banked rosters (and the extended sixth) are decided as instances of the general
shape (N₁ = 1, 16, 144, 2304, 12544, 38025 — the banked global dimensions exactly).

## §3 — COMPONENT 2: THE CONJUGATE-PAIR MECHANIZATION — **LANDS WITHOUT THE FALLBACK**

The registered smarter route closed the identity at the zero-axiom bar: Gaussian
integers as pairs over an abstract coefficient structure with twelve named ring-law
hypotheses; conjugation proved multiplicative over pair products; **the third-power
product proved to be the conjugate of the first-power product** (`prod3_eq_conj_prod1`)
so the imaginary parts cancel as I + (−I) in the final assembly — the forty-monomial
brute expansion never ran. The step identities reduced to a small reusable rewrite kit
(the difference product, the sum product, the shuffle, the interchanges), each an
explicit rewrite chain. **22 terminals, each `does not depend on any axioms`; Core now
166/166.** **Nothing resisted — no sharpened named-open statement was needed:** row 31's
general μ₄ identity and row 32's nowhere-flat and general-k named opens are **CLOSED at
the abstract level** (the general theorem holds for arbitrary dims — nowhere-flat
included — and arbitrary roster length); the correspondence rows updated additively,
row 35 filed. Row 29's general-q only-if is not this act and stands untouched at its
own address.

## §4 — COMPONENT 3: THE FLATNESS LAW — **BRANCH (a): PROVED IN WHOLE AT THE REGISTERED SCOPE**

The longhand derivation (banked in the registration BEFORE the run): the chart Gauss
computation — `tr(S) = Σ ζ^{m²}` over the chart m = a + qb collapses to q at odd q and
q(1 + i) at q = 2ⁿ, elementary, no quadratic reciprocity — plus the ball-pair
inclusion–exclusion (the two ball projectors commute; the rank-one intersection is the
u-line with Su = qu) give **tr(S|Son) = 0 (odd) and i·q (place 2), and tr(Π|Son) = 0
(odd) and −1 (place 2), at EVERY level**; the linear solve (four real equations, full
rank) then forces the dims: odd (d, d, d, d) with 4d = (q−1)² — **flatness forced**;
place-2 (d, d, d+1, d) with 4d = q(q−2) — **the unit trace i with excess exactly one**.
The integer endpoints are EXACT against the banked tuples at all eight cells and
compiled (`FlatnessShadow`, 3 terminals, zero-axiom, row 36).

**The branch-(a) consequences, as registered:** the flat-place collapse is a **law**
rather than a per-instance fact; the character weight of the cross-place identity
concentrates at the place 2 **by theorem**, carried by the boundary seed's unit trace
(the P3 chain: the one imported line, exact at every banked place-2 cell); the
place-2/ℤ[i] reading is **CONFIRMED-AS-STRUCTURE at the proved scope**.

**The staged keystone sentence (§4 of THE_GLOBAL_SECTION v2.0), for the author's
approval — NOT filed:** *"The flat-place collapse is now a law rather than a
per-instance fact — odd places are flat and the place-2 twisted trace is exactly i at
every level (the chart Gauss computation, correspondence rows 35–36) — so the character
weight of the cross-place identity concentrates at the place 2 by theorem, carried by
the imported boundary seed's unit trace."*

**The range law, on the face:** the flatness law and the character-sum theorem are
statements about the finite places' sector arithmetic at exact levels (the law at every
level; the decided endpoints at the banked cells); nothing about the level limit, the
archimedean junction, or `h2` follows from either. Compatibility, not factorization.

## §5 — THE RECORD AND PINS

*Relay: the b57 registration + raw run + assembled bank (`data/b57_row31_depth.txt`) +
instrument (`tools/e16/b57_row31_depth.py`) + working-layer copies
(`tools/lean/CharacterSumShadow.lean`, `tools/lean/FlatnessShadow.lean`) + this report.
SIDE-global-section: the two modules + rows 35/36 + the row-31/32 closure notes +
regenerated `AllPrints`/`AXIOM_PRINTS` (166/166) + README counts. PLACE-papers:
UNTOUCHED (the staged sentence awaits the author) — mirror NOT refreshed, per the ferry.*

> **PIN LINE (post-push read-back, ls-remote):**
> `SIDE-global-section origin/main = a3fbc73af79e49df74a9f028a211f624900fe873` (tag
> `v0.1.0` = peeled `706a81b` unmoved) · `PLACE-papers origin/main = a565095` unmoved ·
> `relay origin/main = [READ BACK AT PUSH]` (pushed from `push-depth-row31` per Rule
> 4.10, hook standing; HELD ancestry clean; the HELD commit's two carrier-build act
> files verified ABSENT from the public tree by name). **MIRROR:** unchanged —
> `mirror-refresh-2026-08-21-f.zip` @ PLACE-papers `a565095`. *This pin-line commit's
> own SHA is stated in the closing message, per the regress rule.*

## FOOT

**Per the ferry's foot, the author's desk holds three items:** (1) the p2-10 citation
supersession (recommendation standing: supersede-by-counter-note); (2) the
formation-card route choice (recommendation standing: in-kernel replication); (3) the
two keystone voice sentences (report §3 of the consolidation act) — and now beside them,
**the staged §4 flatness sentence above awaits the author's yes or no.** After this act:
row 29 and the remaining local-formalization units queue as the depth line's
continuation; the navigator presents the state at the next seam.

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED. NOTHING DEPOSITS.
NOTHING CIRCULATES.**
