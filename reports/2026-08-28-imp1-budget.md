# THE IMP-1 BUDGET — b238, 2026-08-28

**Scope:** a bounded bench act on the import and the instruments only. ### **The corpus's left side
appears nowhere.** Nothing about h2 beyond the register sentence exact. Nothing deposits.

---

## The verdict

### **BRANCH (HELD).** Five cells of six within the banked bound; one over — `a²=3` at `NV=6001`,
**`2.218e−08` against `2.133e−08`, over by a factor of 1.04.** The banked pass rule said, before the
run, *"one cell over the bound is branch (HELD)."* ### **IMP-1 stays `TRUSTED-AT-CITE`. No
projection was widened.** `W-ORD-IMP1-BUDGET` is **discharged**, as the ferry directs, whatever the
branch.

> ### **And the split, reported rather than blurred: clause (b)'s test PASSED; clause (d)'s branch
> did not.** The tolerance was fixed before measuring — *"I state the factor now, before measuring:
> 10"* — and **the budget explains every observed residual to within ~3–4%**. ### **What failed is
> not the budget's account of the physics; it is the envelope estimate for its constant.**

---

## 1. The budget — five sources, four of them zero

With a reference grid at `NV = 32001` and a control on the chunked transform (`3.331e−16`, passes):

| source | measured result |
|:--|:--|
| **S1 uniform grid** | ### **`A`, `P`, `Z` carry NO grid error** — every `\|dX\|` at machine epsilon (`0` to `8.9e−16`). ### **`PR` carries the entire residual**, matching it to ~3% at every cell and grid. |
| **S2 the bump's edge** | ### **REFUTED.** `∫_{−1}^{1} exp(−1/(1−t²)) dt = 0.4439938161680794` (mpmath.quad, dps 40); the instrument's trapezoid returns **that value at every N from 2001 to 32001, `\|error\| = 0.000e+00`**. ### **No convergence order to measure, because there is no error.** |
| **S3 interpolation** | reproduces `\|dPR\|` to ~2% — ### **it is the source.** |
| **S4 zero truncation** | residuals **identical to every printed digit** at `N = 250, 500, 1000`; tails re-derived at `1e−21…1e−27`. |
| **S5 float species** | zeros at dps 25 vs 50 agree to `0.000e+00`; residuals unchanged. ### **The floor is float64 accumulation, ~1.2e−13**, measured at the one cell whose prime column is empty. |

> ### **b234's registered half-order edge hypothesis does not exist**, and the registration
> predicted that refutation *before* measuring — *"so that a refutation cannot later be dressed as a
> discovery … and if the measurement says otherwise, the measurement wins and my reasoning is
> reported wrong."* Euler–Maclaurin holds exactly: the bump is `C^∞` with **every derivative
> vanishing at the endpoints**, so the trapezoid has **no boundary terms at any order**.
> ### **A hypothesis carried through two acts is retired by one control.**

**And a finding about the measurement itself.** The interpolation order is **erratic** — 1.10, 1.22,
1.58 at `a²=4`; 1.27, 1.76, 3.92 at `a²=3` — **and that is not a defect.** ### **The evaluation
points `log p^k` are fixed while the grid refines, so the distance from the nearest node jitters with
`NV`: an order is not well defined for a fixed point on a refining grid.** b233 saw the same ratios;
**b238 can say why there is no clean exponent to find.**

---

## 2. The criterion, and its mechanism

`|resid| ≤ K(a²)·h² + F + tail`, with `K` the **maximum** of `|dPR|/h²` — an envelope, not a fit
line. ### **Fit on `NV ∈ {2001, 8001, 16001}` only; tested at `{4001, 6001}`, both excluded from the
fit, and `6001` never computed before.**

> ### **The criterion file was written at 20:57:43; the final run's at 20:58:48 — 65 seconds apart,
> on disk, and a gate compares them.** The final script **reads its bounds from the banked file
> rather than recomputing them**, so the comparison cannot drift.

---

## 3. The re-verdict, and the refusal

`(a²=3, NV=6001)` needed `K = 0.6616`; the banked envelope was `0.6363`. ### **Short by 4%.**

> ### **The cause is not an unexplained physical effect — it is an under-sampled envelope.** `K` was
> the maximum over **three** samples of a quantity the measurement had just shown to jitter, and the
> peak at `6001` exceeded all three. ### **A maximum over three samples of a jittering quantity is
> not an envelope; it is three samples.**

### **What was refused, and it was one keystroke away.** Raising `K(a²=3)` from 0.6363 to 0.70 — or
adding any safety factor — would have delivered **(PROMOTED) at all six cells.** The registration
said, before any measurement: *"I will not widen a projection to cover a residual."* ### **It was not
widened — and a gate now checks the banked `K` against `K` recomputed from the banked measurements,
so the refusal is arithmetic rather than a promise.**

***b233 failed a criterion that was wrong; b238 failed a criterion that was nearly right — and the
second refusal is the harder one.***

**What (HELD) does not mean:** the two sides agree to **~2e−8 relative at `a²=3`, 5e−9 at `a²=4`, and
1.2e−13 where the prime column is empty.** ### **That is agreement. What is held is a grade.**

---

## Filings and checks

IMP-1 **stays TRUSTED-AT-CITE**, with the account annotated at `THE_IDENTITY_CHAIN` §35.1.
**`W-ORD-IMP1-BUDGET` discharged**; **new `W-ORD-IMP1-ENVELOPE`** — re-derive `K` as a true envelope,
or **bound `|corr''|` directly and use the analytic `h²/8`**, removing the jitter from the estimate
entirely. ### **No right-side error spec is filed** — the ferry files it on (PROMOTED) only, and **a
spec filed off a failed criterion would be the same over-reach the criterion exists to prevent.** The
checklist is annotated: ### **the right side's error bars are measured but not yet certified.**

**Gates: 12 of 12 PASS, CLEAN — on the first run**, including the timestamp gate and the arithmetic
"K not widened" gate. Both term scans CLEAN, 0 hits.

---

### **What this act did not do:** no axis chosen for agreement (test axes named before the final
script existed, timestamps on disk); no projection widened; no criterion rewritten after the numbers;
no instrument modified (the one local helper carries a control); the left side appears nowhere.
### **Nothing about h2 beyond the register sentence exact. Nothing deposits. Locks last.**
