# THE POSTURE CORRECTED · THE PLACEMENT RULED · THE ROUTES NAMED
## ### **CORRECTION TWENTY-SEVEN: THE CODOMAIN IS BUILT OVER FUNCTION FIELDS AND NOT YET BUILT OVER `ℚ` — "DOES NOT EXIST" RETIRED AS THE WRONG VERB — THE CORRECTIONS REGISTER MOVED TO THE WORKING LAYER WITH THE STANDING LAW MINTED — AND THE SPEC'S §8 MAKES THE TARGET CONCRETE: THREE COMPONENTS, DISTANCES NAMED, ROUTES RANKED, NONE RUN**

**Relay report · 2026-08-18 · author-ruled · nothing deposits · nothing circulates**

> ### **SCOPE, once: finite instance speaks nothing at complete roster; the specification is a
> CONSTRUCTION TARGET with a worked precedent.**

> ### **RULE-3 LOG:** *the spec v0.1 at `a2e4680` ✓; the `§6` grading ✓; the incident close at
> `6b98b2f` ✓. Terms coined: none.*

---

## §1 — THE POSTURE (item 1; correction twenty-seven, author-caught)

*The spec's `§5` now reads:* ### **"this is Weil's 1952 object at complete roster; it is BUILT over
function fields (`§6`, the Jacobian row) and NOT YET BUILT over `ℚ`."** *The same tense at the
charter (`v1.3`: the `C1` status line — "a construction with a worked precedent, not an object no
field has ever seen" — and `§8(ii)`) and in the keystone draft's §3. The correction is logged in the
working-layer register.*

## §2 — THE PLACEMENT (item 2; executed, nothing deleted)

*`THE_ATTEMPT_RECORD` `§2` (the corrections, one through twenty-seven, 628 lines) moved VERBATIM to*
### **`reports/W-ATTEMPT-2-CORRECTIONS-REGISTER.md`** *(attribution intact; the record keeps `§1`
results at current grade plus ONE pointer line); the correspondence table's correction rows (39, 45)
collapsed to current-state with the register pointer; the spec and the packet carry the reader-first
framing; the loom carries the STANDING LAW (corrections in the working layer with attribution;
conclusion documents current-true plus one pointer).* ### **FLAGGED, NOT SILENTLY DONE SHALLOWLY: the
full sweep of historical correction narratives EMBEDDED in the two road documents is PRICED as a
separate revision pass (~1 sitting per road) — the roads carry the standing pointer meanwhile.**

## §3 — THE ROUTES (item 3; the spec at `v0.2`, `§8`)

*From the `§6` Jacobian row, the three components of (e) with the corpus's nearest held object and
the exact distance:* **1. the self-product `X × X`** *(held: the place-wise tensor squares; distance:
the GLOBAL product — Road A's thrice-measured arrest; both branches said)* · **2. the Frobenius
graph** *(held: the compiled involution + the proof-grade forced compression; distance: an honest
endomorphism vs a contraction — the LIFT absence at Deninger's `Δ2`)* · **3. the intersection pairing
with the Hodge index** *(held: the exact model `H²` and the `E₁` theorem; distance: clause (e)
itself — plus the measured sign-SHAPE difference, `(+1, −1, …)` vs `(d/4, d/4, d/2)`, a question with
both branches open).* ### **Ranked by holdings: R1 the `E₁`-gluing · R2 the lift question · R3 the
global self-product. NO ROUTE RUN.**

**The record: charter `v1.3` · spec `v0.2` · `F.2026-08-18j` · `OPEN_TRAILS` · the packet re-framed ·
correspondence rows 69–70 · the mirror at final HEAD. `h2` UNCHANGED. NOTHING DEPOSITS. NOTHING
CIRCULATES.**
