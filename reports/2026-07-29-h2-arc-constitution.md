# h2 research arc constituted + claim-carrier sweep — 2026-07-29

Two ledger entries (no new tracking documents) and a report-only claim-carrier sweep. **No text edits to any
claim-carrier this pass — public re-description wording is an author call.**

## (a)(b) The two ledger entries — committed `PLACE 1a599ad`

- **OPEN_TRAILS "THE h2 RESEARCH ARC"** (charter 1–7): **1** object (discharge `h2` = `ConservationHypothesis`
  = the arithmetic clause / positive space on the zeros, RH-equivalent; one register discharges all five);
  **2** the standing verified assets fenced off from re-litigation (the surround `h1_complete_at_Phi`, the
  Conservation seal, the seven-class exclusions, the three reduction terminals, `RegisterPentagon`, the
  finite-range positivity, the compiled negatives, the escape-kind invariant, `RH ⟺ covers_all`); **3** the
  four discharge faces with barriers (A analytic/Li — the rigid 2T² tail; B spectral/Hilbert–Pólya — the
  space is the wall; C totality/coverage — the T3 commutation, unrestricted false; D 𝔽_q→ℚ/Weil positivity
  — the missing family); **4** the negative discipline (no discharge until compiled+checkpoint-surviving;
  two-darknesses; no partial result is a discharge; no unqualified claim-carrier); **5** the checkpoint
  discipline (Voice-pattern statement-read + E0 salt-check + rowgen diff before any row); **6** the honest
  stopping rule (a face closes only by a checkpoint-surviving compiled terminal; the null is
  "no discharge; the residue stands named"); **7** governing lineage (standing laws + F.2026-07-29-c;
  shelved sub-arc work-orders).
- **FINDINGS F.2026-07-29-d** — register resolution: **h2 is research-frontier**, verified by the chain reads
  (`91b7da9` Chapter 13 = seal not discharge; `f583008` Chapter 15 quantifies over classes not zeros;
  `e16b615` discharge = the positive space on the zeros); corpus internally consistent, **zero discharge
  claims**; arc constituted on that basis.

## Claim-carrier sweep — verdicts

Grep of the corpus + fetch of the public artifacts for the four claim-carriers, as *claims* (not dated
mentions). **Result: the public claim-carriers already carry the h2 disclosure.**

| carrier | where (live claim-carriers) | verdict |
|:--|:--|:--|
| **"three verified routes"** | **Not in the corpus.** The SIDE-kernel GitHub description (the item cited as 2026-07-10 wording) has **already been updated**. | **ALIGNED** — phrase retired |
| **"end-to-end"** | `A_Place_to_Stand:107` reads *"the three route terminals … composing under the named premise `h2` (criterion + verified surround, **not end-to-end**)"*; other hits are module-compilation mentions (`TheBridgeComplete compiles end-to-end`) | **ALIGNED** — explicitly disclaimed |
| **"proof of the Riemann Hypothesis"** | Public: `day1/ONE_PAGE_PROOF.md` **title** — body step (8) discloses *"the argument's one named premise … five equivalent registers (§27.3)"*; the monograph Zenodo **title** (see below). Internal: `internal/CRITICAL_RESOLVE`, `CATALOGOS`, `CASCADES`, `SIDE_EFFECTS` (internal tree, not public) | **ALIGNED in body/abstract**; the **title-headlines** don't carry the qualifier — **author call** |
| **"0 sorry, 0 axioms"** | `A_Place_to_Stand` §25.8 kernel-concordance table, **per-module** (`OstrowskiBridge.lean … 0 sorry, 0 axioms`; `ConservationBridge.lean — RH from Conservation + balance — 0 sorry, 0 axioms`) — accurate of each module/implication; Zenodo does **not** claim it | **ALIGNED** — per-module/per-implication, not a whole-proof claim |

### Public artifacts fetched — verbatim

- **SIDE-kernel GitHub description (current):** *"Lean 4 kernel of the PLACE TO STAND **two-leg approach** to
  the Riemann Hypothesis: it machine-checks the logical structure (route terminals such as
  `ConservationBridge.riemann_hypothesis` **on its named premise**, and the E-Difficulty SieveCeiling
  subsystem); the classical mathematics is carried by the companion manuscripts. Current tag v1.4."*
  → **ALIGNED.** The 2026-07-10 wording the ruling cites ("SIDE proof … Three verified routes…") is **stale —
  already replaced** by this two-leg / named-premise form. *(One nit for the author: it says "Current tag
  v1.4" while the working pin is v1.5/derivative-engine — a currency lag, not a claim error.)*
- **Zenodo kernel v1.3 (`10.5281/zenodo.21417776`):** title *"Formal verification of the Riemann Hypothesis
  via exhaustive mechanism exclusion"*; Route 3 = *"the implication from the named interface
  `ConservationHypothesis` to `RiemannHypothesis`"*; *"the kernel verifies the logical architecture; the
  companion manuscript … proves the mathematics"*; `ConservationHypothesis` *"an assumed premise, not a
  proved result within the kernel."* → **ALIGNED** (formal verification, two-leg, named premise; no "three
  routes"/"0 sorry 0 axioms" as unconditional).
- **Zenodo monograph v1.1.1 (`10.5281/zenodo.21436278`):** title *"A Place To Stand: Proof of the Riemann
  Hypothesis via the SIDE method"* + *"MAIN THEOREM. The Riemann Hypothesis…"* — **but** the description
  *"names the proof's single premise in five registers (§27.3) with its full literature ancestry … locates
  it at one compiled goal state"* and is characterized as a *"conditional proof: if a stated open premise
  holds, then RH follows,"* and **does not claim "0 sorry, 0 axioms."** → **ALIGNED in the abstract**; the
  **title** "Proof of the Riemann Hypothesis" is a headline the abstract fully qualifies — whether the title
  itself should carry the qualifier is an **author call**.

## Verdict

**Every public claim-carrier is aligned with the h2 disclosure** — the register discipline has already
propagated to the GitHub description and both Zenodo deposits (named premise / two-leg / conditional proof,
no unconditional "proof"/"three routes"/"0 sorry 0 axioms"). The **only** items where the headline outruns
the qualifier are the two **titles** — the monograph deposit title and `ONE_PAGE_PROOF.md` — each fully
qualified in its own body/abstract. Whether those titles should carry the qualifier at the headline is left
to the author. **No text edited this pass.**

## Pins

- PLACE-papers `main` — **`1a599ad`** (local = remote, clean). RH rail empty vs baseline `0696797`.
- No public artifact edited. Nothing deposited.
