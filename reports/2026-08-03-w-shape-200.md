# W-SHAPE-200 — the deep string and the shadow's form — 2026-08-03

The priced head continuation, STAGED per the filed operational note. Pins at open:
PLACE-papers = `640c2bc`; relay = `a893fb7`; lv `14720d9`, kernel `44895f9` — unmoved; rail
at the post-rename baseline. Nothing deposits.

## REGISTERED EXPECTATIONS (VERBATIM from the ferry, recorded BEFORE Stage 1 runs)

**(i)** *"the window reaches O(10²) resolved pairs as priced (the engineering estimate
holds; if it falls short, the shortfall's cause — quadrature, precision, tail — files as the
datum and re-prices the next attempt)."*

**(ii)** *"the ζ-string's resolved-pair statistics are REPULSION-CLASS and distinguishable
from the control's (the control shows the density-only behavior)."*

**(iii)** *"the sharp cell — the pair-statistics match the GUE form within the window's
stated power: CONSISTENT confirms the shadow's FORM inside the constructed object (three
maps closing into one measured curve); TENSION files first-class with maximal caution (it
would be evidence the string's arithmetic oscillation carries non-GUE structure — a genuine
surprise about either the truncation or the object); BEYOND-DEPTH remains honorable and
files the new bound."*

## STAGE 0 — THE PRECISION LAW SIZED TO K ~ 200 (stated before running, as the ferry requires)

The measured precision law (the dps-40/depth-6 failure · dps-150/depth-12 · dps-400/depth-16
data of the filed instrument notes) fits the cancellation model **dps(K) ≈ 4·Σ_{k≤K}
log₁₀(2γ_k) + margin** — the Cholesky's digit loss is the Hankel pivot decay
Π(zero-scale)². Projections: K = 16 → ≈ 110 + margin (observed: 150–400 ✓ with margin);
**K = 48 → ≈ 385 + margin → dps ≈ 500; K = 200 → ≈ 1,920 + margin → dps ≈ 2,200.** At
dps 2,200 the moment generation needs ~600 zero-evaluations at that precision (the density
tail CANNOT supply 10⁻²⁰⁰⁰-relative accuracy — the tail must be exact summation, so the
object must be a TRUNCATED measure with exactly-computed atoms) — a dedicated multi-hour
computation against this environment's 10-minute task ceiling. **The engineering estimate
therefore does NOT hold at K ~ 200 in this environment; the maximum feasible depth is sized
below by a timing probe, the shortfall files under (i), and the K ~ 200 attempt re-prices as
a dedicated run.**

**The truncated-object design (the tail lesson, applied):** both objects are DEFINED as
truncated measures on the first J = 300 atoms — ζ-300 (the first 300 true zeros, each
computed at full working precision; moments by exact summation, no tail estimate) and
control-300 (the first 300 smooth-density points, same pipeline). The comparison is between
two exactly-defined finite objects; the resolved window at the achieved depth sits far above
the truncation edge; the truncation is part of the objects' definitions, stated, not an
error term.

## STAGE 0.5 — THE TIMING PROBE

**The probe:** zetazero is cheap even at high precision (0.9 s at n = 300, dps 520) — the
zero supply is NOT the constraint. **The binding constraint is the Cholesky/eigen cubic cost
at the K = 200 precision** (dps ≈ 2,200 → a 200×200 factorization ≈ hours, unsplittable
within the 10-minute task ceiling). **The sitting's maximum safe depth: K = 64** (dps
620–700; ~32-node resolved window projected). The K ~ 200 shortfall adjudicates under (i)
below.

**The discriminant, stated in advance (per the ferry):** on the resolved band's UNFOLDED
adjacent gaps (γ-coordinates, local-density normalization), **D := Var(gap/mean) / 0.180**
(0.180 = the GUE nearest-neighbor variance, at cite). Acceptance for CONSISTENT at this
window's power (~30 samples; variance-estimate sd ~ 35%): **D ∈ [0.5, 2]**; the control's
rigid null: D ≈ 0; TENSION: D outside [0.2, 5] with the instrument clean. Second lens: the
small-gap fraction (< 0.5·mean): GUE ≈ 5–6%, rigid ≈ 0. **Honesty clause: the resolved band
is the LOWEST ~30 zeros, below the universality regime — low-height systematics ride the
window; (ii) (repulsion-class vs rigid) is well-powered, (iii) (the GUE form proper) is
claimed only within the stated band.**

## STAGE 1 — THE MOMENT CACHE

**Executed** (`tools/e16/shape_s1.py`, 333 s): 300 true zeros + 300 smooth-density points at
dps 620, cached versioned; **exit check PASS** — cross-source (dps 620 vs independent dps-300
recomputation) worst rel diff 5.0×10⁻³⁰², the recomputation's own floor.

## STAGE 2 — THE STRING AT DEPTH

**Executed** (`tools/e16/shape_s2.py`): truncated-measure moments by exact summation (no
tail estimate — the objects are the 300-atom measures by definition); **checkpoints PASS** —
moment double-source (dps 700 vs 500) worst rel 1.7×10⁻⁴⁹⁹ (ζ) / 1.0×10⁻⁴⁹⁹ (control); all
recurrence β > 0 through K = 64, both strings; Jacobi data cached.

## STAGE 3 — THE READ

**Executed** (`tools/e16/shape_s3.py`). **The window, measured first: 36 resolved nodes → 35
adjacent gaps** (node-vs-atom rel < 1%), both objects — 5× the previous window of 7.

**(i) — the O(10²) registration FALLS SHORT, the shortfall filed as the datum:** 35 pairs,
not ~100. The cause, named precisely: NOT quadrature (the zero supply is cheap — the probe),
NOT the tail (eliminated by the truncated-object design), but **the precision law × the
factorization's cubic cost against the 10-minute task ceiling** (K = 200 needs dps ≈ 2,200;
a 200×200 Cholesky/eigen at that precision is an hours-scale unsplittable step). **The
re-price: K ~ 200 = a dedicated multi-hour run with a blocked, checkpointable factorization
— a different instrument class, priced on call.**

**(ii) — REPULSION-CLASS AND DISTINGUISHABLE: registered CONFIRMED, decisively.**

| object | Var(gap/mean) | D = Var/0.180 | small-gap fraction (< 0.5·mean) |
|:--|:--|:--|:--|
| ζ-string (35 gaps) | 0.1168 | **0.649** | 2.9% |
| control (35 gaps) | 0.00084 | 0.0047 | 0 |

The ζ resolved-gap variance is **140× the control's** — the control shows exactly the
density-only rigidity; the ζ string carries genuine fluctuations with small-gap suppression
(2.9%, far below Poisson's ~39%) — repulsion-class, unambiguous at this n.

**(iii) — THE SHARP CELL: D = 0.649 ∈ [0.5, 2] — CONSISTENT, exactly as pre-committed.**
The pair-statistics match the GUE form within the window's stated power: **the shadow's FORM
is observed inside the constructed object — the three maps (the shadow row's statistics ·
the string · the sum-rule bridge) close into one measured curve at this window's power.**
The honesty clauses, kept: D sits at the band's low end (0.65 < 1) — the lowest 36 zeros are
more rigid than asymptotic GUE, the known low-height systematic (convergence to GUE improves
with height, at cite — the Odlyzko-era observation), and the unfolding at low γ is
imperfect; CONSISTENT is claimed at the stated power and nothing finer; no TENSION; the
K ~ 200 window remains the form-test at full power.

**The E-24 rider (the π₀ limit's first depth datum, riding free):** the relative
resolved-band log-repulsion at K = 64: boundary = 50.18, oscillation = 87.01 (K = 16
comparators: 11.16, 5.03) — **the oscillation share overtakes the boundary at depth** (5 <
11 at K = 16 → 87 > 50 at K = 64): the arithmetic pair-energy grows with the window while
the boundary term saturates slower — and the raw sums grow with pair count, so the π₀
construction needs a per-pair (or per-level) normalization: the normalization question is
E-24's first concrete sub-problem, now with two data points to calibrate against.

## STAGE 4 — FILINGS

**The verdict at grade:** (i) the window fell short of O(10²) with the cause named and the
re-price stated (the dedicated blocked-factorization run) · (ii) repulsion-class,
140×-distinguishable — CONFIRMED · **(iii) CONSISTENT — D = 0.649 in the pre-committed band:
the shadow's form observed inside the constructed object at 35-gap power, low-end position
attributed to the known low-height systematic, nothing finer claimed.**

**E-24's candidate row, updated:** the boundary-separated pair-energy now has TWO depth
points (K = 16, 64); the oscillation share overtakes the boundary at depth; the π₀
construction's first concrete sub-problem = the per-pair/per-level NORMALIZATION of the
relative pair-energy (the limit object needs it; two calibration points in hand). The
construct-or-refute stays armed at research-reach.

**The keystone's next-cargo, four-deep (touch NOT executed — it lands as one pass when
breadth returns or the author calls it):** the sum-rule verdict · π₀'s first candidate row ·
the two-sides verdict · **the shape verdict (repulsion-class 140×; GUE-CONSISTENT at 35-gap
power; the K ~ 200 re-price).**

**The slate re-printed:** W-SHAPE — **RUN at K = 64: window 35 (short of the O(10²)
registration, cause filed, re-priced) · repulsion-class CONFIRMED · GUE-CONSISTENT at stated
power** · the K ~ 200 dedicated run (research-reach, the blocked-factorization instrument
class, priced on call) · E-24 armed (the normalization sub-problem named, two calibration
points) · E-3 (kind-indexed recipe; c₁; n = 56 sampling) · E-7b (two cells) · E-16 (d(t)
exponent) · E-20 (continuum normalization) · the standing rest. **Consolidation DEFERRED,
standing.**

## CLOSING — pins, mirror, the handoff refreshed

| repo | pin |
|:--|:--|
| PLACE-papers | `640c2bc` at open → this sitting's commit (OPEN_TRAILS addendum + handoff refresh) |
| SIDE-lv-conservation | main = `14720d9` — unmoved |
| SIDE-kernel | `44895f9`; v1.7 — unmoved |
| relay | this report's commit; instruments `tools/e16/shape_s{1,2,3}.py` |
| rail | untouched — at the post-rename baseline |

**The board restated, the shape verdict at its head:** THE SHADOW'S FORM IS IN THE STRING AT
THIS WINDOW'S POWER — the deep string's resolved gaps fluctuate at 140× the density null
with GUE-band variance (D = 0.649, pre-committed band [0.5, 2]) and small-gap suppression;
the control is rigid as it must be; the full-power form test is priced (K ~ 200, the blocked
factorization). Three maps closed into one measured curve, honestly bounded. Mirror rebuilt
at the papers pin on commit. Nothing deposits.
