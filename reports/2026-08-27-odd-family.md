# b212 — THE ODD FAMILY

**2026-08-27 · relay `reports/2026-08-27-odd-family.md`**
**Ferry part 1 of 1, receipt confirmed IN FULL — after an earlier paste of the same brief was HELD for truncation (Rule 1).**
**Registration `0b8fc93`, banked before any computation or derivation. Bank: `data/b212_odd_family.txt`.**
**Gates `data/b212_gates.txt` · run `data/b212_raw_run.txt` · ladder `data/b212_ladder.txt`.**

> *** ### **(DERIVES) ON THE DERIVATION, (LADDER) ON THE LADDER.**
> ### **`α_odd·β′_odd = ∫₁^∞ ψ_odd² dx`, `c₀^odd = +1`** — the same constant as the even family,
> **re-derived on the cosine solution and not inherited.**
> ### **`c² = −1` FROM `F² = parity` ON ODDS ⟹ `c = ±i`, BOTH VALUES OCCUR, SO `E_i(S(1,1))` AND
> `E₋ᵢ(S(1,1))` ARE BOTH NONZERO** at derivation grade on named imports.
> ### **UNDER THE AUTHOR'S RULING, ALL FOUR `F`-SECTORS OF TERM 3's ARCHIMEDEAN UNIT ARE NOW
> ACCOUNTED FOR.** *Core 339 → 360, every terminal zero-axiom.* ***

---

## COMPONENT 0 — THE RULING FILED (P0)

Filed at **three sites**, with the provenance line travelling with it at every one:

- **`THE_IDENTITY_CHAIN` §13** — b200's routed question **answered**. ### **Everything b200 wrote is
  left exactly as written** — the census, both readings, the absence of a recommendation — *because
  a routed question that is later answered still has to show what was routed.*
- **`THE_CODOMAIN_SPECIFICATION` §1** — the flagged `E₁(∞)` line **resolved**. b201 flagged that the
  line's *subject* read as the Sonin sector while its *citation* named b33's compression modes.
  ### **The ruling ratifies the subject and retires the citation, so the mismatch is now named as a
  real one** rather than left open.
- **`THE_GLOBAL_SECTION`** — b159's scope sentence **annotated, not rewritten.**

*** ### **AND (P0)'s REGISTERED EXPECTATION LANDED: the executor registered that it expected b159 to
need NO correction at all. It needs none.** b159's own sentence already reads *"the ±i sectors are
unoccupied **on the constrained class**"*, and the paragraph after it already says *"the ±i sectors
need odd classical index … which is what b35 records as **odd control purely ±i**."*
### **The ±i sectors were always the odd ones' address. b159 found them empty on an even class,
which is what an even class does.** ***

**Provenance, everywhere: the conversation layer, 2026-08-27. Not a derivation. Not citable as
evidence about the sector.**

---

## COMPONENT 1 — THE GATES (P1)

**G-PARITY: PASS at all three parameters**, run in this act before any odd sign was read.

| τ | even | odd | merged | alternating | rank 2 |
|:--|:--|:--|:--|:--|:--|
| 2π | 6 | 7 | 13 | **yes** | **EVEN** |
| 4π | 3 | 4 | 7 | **yes** | **EVEN** |
| 6π | 3 | 3 | 6 | **yes** | **EVEN** |

### **The difference from b210, named as (P1) requires:** b210 measured only 2π and 4π, and that
measurement was **b210's one deviation** — an addition to its registration — ### **so it was the
only figure in this arc that never passed a registration gate.** `τ = 6π` **had never been measured
at all.** Both earlier parameters reproduce exactly; the new one behaves the same.

**G-REPRO-ODD.** ODE residual of the cosine solution: **3.3e-30** relative.

### *** AND THE NORMALIZATION SUB-GATE FAILED THE EXECUTOR'S OWN REGISTERED THRESHOLD ***

Registered: *"ratio → 1 to `< 1e-8`."* **Measured 4.5e-3 at x = 50.** ### **The threshold is not met
and the executor does not move it.**

### **It was mis-registered.** `ψ_odd = −cos(τx)/x + O(1/x²)` **is the definition**, so the ratio is
`1 + O(1/x)` and ### **no finite `x` could meet 1e-8** — it would need `x ~ 3e4`, far outside where a
truncated asymptotic series is usable. ### **A threshold no correct object could meet is a defective
threshold, not a failing gate.** And **the ferry did not ask for it** — clause (c) says only
*"normalization … at generic points, not at zeros of the leading term."* The `1e-8` was the
executor's own addition in its own registration.

**The test that actually decides it**, run and labelled **not registered in advance**:

| x | 25 | 40 | 60 | 100 |
|:--|:--|:--|:--|:--|
| `\|ratio−1\|·x` | 0.4498 | 0.2815 | 0.1878 | **0.1127** ← falls |
| `\|ratio−1\|·x²` | 11.2448 | 11.2603 | 11.2658 | **11.2687** ← flat |

### **So the leading coefficient is exactly 1.** `ψ_odd ~ −cos(τx)/x`, no rescaling. **The ferry's
clause passes and the act does not halt.** *The control was also run: the same ratio at
`x = 25.25`, where `|cos| = 1.7e-60`, returns `−1.1e+59` — a number about the denominator and not
about `ψ_odd`, which is why the gate forbids testing there.*

> ### **b207's threshold was never registered, so there was nothing to move. This one WAS**, in this
> act's own registration. ### **A threshold registered before the object's convergence rate is known
> is a guess wearing a gate's clothes.**

**G-STAB: PASS.** Residual falls `1e-9 → 1e-11 → 1e-16`; **the signs are identical at all three
resolutions at all three parameters.**

---

## COMPONENT 2 — THE DERIVATION (P2)

Steps 0–3 carry over **for stated reasons, not by inheritance**: they never mention `ψ`, and `y_I` is
**the same object in both families** — parity belongs to the global eigenfunction, not to the local
solution at the regular singular point. ### **Step 4 is where the cosine solution pays**: the
combination differs, but `V₀^± = 1` is μ-independent in both, so `ψ̇_odd = O(1/x²)` and the boundary
term still vanishes. ### **The vanishing does not depend on which combination `ψ` is, and the proof
says so rather than assuming it.**

### ### **`c₀^odd = +1`** — confirmed at residual **−3.6e-16** (2π), **1.1e-15** (4π), **7.6e-14** (6π).

> **Premise (ii) is re-earned too.** §4.2.2 credits the even entirety to **Proposition 9** — which is
> about the **exponential** solutions `y₁`, not about `ψ`. ### **`ψ_even` and `ψ_odd` are both fixed
> μ-independent combinations of the same two exponentials**, so `β_odd` is entire of order ≤ ½ by
> *the same warrant*, not by analogy.

| # | consequence | grade |
|:--|:--|:--|
| 1 | every zero of `β_odd` is simple — ### **RRT's open conjecture again, now for the odd determinant** | ### **DERIVES** |
| 2 | `sign(α_odd)` alternates | ### **DERIVES** on premise (ii) |
| 3 | `c² = −1` ⟹ `c = ±i` | ### **DERIVES**, no missing step |
| 4 | ### **`E_i(S(1,1))`, `E₋ᵢ(S(1,1))` nonzero** | ### **DERIVES** on named imports |

*** ### **(3) HAS NO MISSING STEP, AND THAT IS A READ THIS ACT HAD TO MAKE.** RRT **Lemma 2 carries
no parity restriction** — *"Let µ ∈ ℝ be a **non classical eigenvalue** … (ii) the dimension of the
eigenspace `E_µ` is one; (iii) … **φ belongs to the Sonin space**"* — while **Proposition 7** opens
*"Let µ be an **even** CM-eigenvalue."* ### **The paper restricts where it means to.** So b203's step
(2) transfers **by quotation**, the one substitution sits at step (iv) and nowhere else, and Lemma
2(iii) puts the odd eigenfunctions in the Sonin space **independently of CM Cor 3.2**. ***

---

## COMPONENT 3 — THE RUN (P3)

**7 eigenvalues at 2π, 4 at 4π, 3 at 6π.** Signs alternating at every parameter, identical at all
three resolutions.

### *** THE MAGNITUDE — AND THE EXECUTOR'S REGISTERED PREDICTION WAS WRONG ***

### **Measured `|α_odd|/(πΛ) = 1.0` at every odd eigenvalue at every parameter**, to twelve figures.

The **ferry** predicted `|α_odd| = πΛ` from the b206 rescaling. ### **The executor registered dissent
in advance**, on the ground that `πΛ` descends from Prop 7's `ψ(Λ) = ±1` — an **even-only** statement
whose odd analogue is **absent** from the source — so *"there is no warrant for the odd magnitude to
be the same constant."*

### ### **THE FERRY WAS RIGHT AND THE EXECUTOR WAS WRONG.**

> ### **And the error is worth more than the result:** the executor treated *"the source does not
> warrant it"* as *"expect it to differ."* ### **AN ABSENT WARRANT IS NOT EVIDENCE OF A DIFFERENT
> VALUE.** The right inference from a missing proposition is that the magnitude is **undecided by
> the source**, not that it is **different**. *Recorded so the next act does not make the same
> substitution.*

---

## COMPONENT 4 — THE LADDER, THE SHADOW, THE FILINGS (P4, P5)

**BRANCH (LADDER).** By merged rank, `sign(α)` runs ### **`− + + −`, period 4, identical at all three
parameters** (13 ranks at 2π, 7 at 4π, 6 at 6π).

*** ### **AND ε AND THE ORIENTATION ARE NOT DETERMINED — EXACTLY AS REGISTERED IN ADVANCE.** Worked
out rather than asserted: `c_even = u_e·sign(α_even)` with `u_e ∈ {±1}`, `c_odd = u_o·sign(α_odd)`
with `u_o ∈ {±i}`, both **k-independent and independent of each other**. The measured ranks 1–4 read
`−u_o, +u_e, +u_o, −u_e`; against `ε·i^k` this forces `u_o = −εi, u_e = −ε` — consistent for **both**
`ε`; against `ε·i^{−k}` it forces `u_o = +εi, u_e = −ε` — **also** consistent for both.
### **All four assignments fit the same measured signs.** ***

> ### **A report claiming a fixed `ε` from this data would be reading a convention as a measurement.**

### THE SHADOW — AND THE MISSING STEP BECOMES A DECIDED FACT

`Core/LadderOrientationShadow.lean`, over a hand-built `U4`. ### **21 terminals, every one
*"does not depend on any axioms"* — and zero-axiom ON THE FIRST COMPILE.** `AXIOM_PRINTS.txt`
regenerated in full: ### **360 lines, 360 zero-axiom, 0 otherwise. Core 339 → 360.** Correspondence
**row 85**, six cells, by the committed tool **from a script file**, verified by read-back.

Stepping by `i` gives `ε·i^k` and implies each family alternates; ### **the converse FAILS, with a
witness exhibited** (`alt2_does_not_imply_stepsI`); and alternation plus a first quarter-turn gives
the ladder ### **up to orientation and no further**, both orientations inhabited.
### **So clause (e)'s missing step is not a caveat in this record. It is a decided fact.**

*** ### **AND THE PRINT DID NOT DECIDE THE FILE'S SHAPE THIS TIME — THE FIRST TIME IN THREE ACTS.**
b207 lost two terminals to `Int.mul_neg`, b211 lost two to `simp`, both `[propext]`. Here every
algebraic law is `cases … <;> rfl` over a four-element type and every decision is `decide` on closed
terms. ### **The lesson from two acts was applied BEFORE the print rather than after it, and that is
the only reason the first compile was clean.** ***

### M22, RESTATED FOR ALL FOUR SECTORS

| sector of `S(1,1)` | status | by |
|:--|:--|:--|
| `E₁` — **THE SONIN SECTOR** | nonzero, derivation grade | b211 |
| `E₋₁` | nonzero | b211 |
| `E_i` | ### **nonzero** | ### **b212** |
| `E₋ᵢ` | ### **nonzero** | ### **b212** |

### **Answered in the form that asks which sectors are occupied: all four are.** Open in the form
that asks **which eigenvalue carries which** — b205's discrepancy, and the same freedom as the
ladder's undetermined `(ε, orientation)`.

### THE OTHER FILINGS

- **An in-flight item for the author — AN EXTERNAL NOTE TO THE SOURCE'S AUTHORS. POSTURE RULING
  REQUIRED. ### NOTHING SENT.** The corpus now holds two things the source does not: a derivation of
  **its own open conjecture** for both parity families, and a measured `|α_odd| = πΛ` whose warrant
  the paper states only for the even case. ### **Whether anything is communicated is a posture
  decision and is the author's alone.** *No note drafted, no contact attempted, deposit locks
  untouched.*
- **The staged amendment is marked CONSUMED in its own file** — not deleted, *because an amendment
  that shaped a brief should remain visible beside the act it shaped.*
- **The register**: odd family **DISCHARGED**; naming **CLOSED BY RULING**; the source-conjecture
  item **EXTENDED** to both families; the external note **ADDED**; and the ladder's `(ε, orientation)`
  filed **with** b205's discrepancy rather than as a second item, ### **so the register does not
  shrink by sleight of hand.**
- **The thirty-seventh seam's debt**: term 2's formalization stands, unpaid and untouched. The six
  rulings and b209's rows 46/47 stand with the author. ### **Locks last.**

---

## THE AUDIT SIDECARS (emitted; copied from the sidecar files, not retyped)

### *** TWO PROCESS FAILURES OF THE EXECUTOR'S OWN, RECORDED RATHER THAN QUIETLY FIXED ***

**(i) THE EXECUTOR SHIPPED A NOT-CLEAN SCOPE.** The review returned **NOT CLEAN at 1 live use** on
`Core/LadderOrientationShadow.lean`, ### **and the commit and push went out anyway** — because the
commit was chained to the check in one shell line instead of the verdict being read first.
Corrected in a following commit. ### **The check fired correctly and the executor overran it. The
tool was not at fault and the habit was: CHAINING A COMMIT TO ITS OWN GATE MAKES THE GATE ADVISORY.**

**(ii) The same species, caught in time** on the PLACE-papers filing — the review was run **before**
the commit, returned 1 live use, and the stem was corrected before anything shipped.
### **The difference between (i) and (ii) is one shell line.**

**Both polarities are carried, per b156's append-only rule.**

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b212-core
  run at    : 2026-08-27T13:03:22 (local)
  input     : whole file LadderOrientationShadow.lean (created this act)
  input     : added lines in D:/SIDE-global-section vs HEAD
  stems     : gap, blind
  files     : 4
  lines     : 269
  hits      : 1
  live uses : 1
  VERDICT   : NOT CLEAN
  self-hash : sha256/32 e5d0fe0df845e249873c1e35271525bd
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b212-core
  run at    : 2026-08-27T13:04:04 (local)
  input     : whole file LadderOrientationShadow.lean (created this act)
  input     : added lines in D:/SIDE-global-section vs HEAD
  stems     : gap, blind
  files     : 2
  lines     : 222
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 4934d05a977ffe283f80b4829af99fbd
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b212-docs
  run at    : 2026-08-27T13:06:15 (local)
  input     : added lines in D:/MY-DOwnloads/PLACE-papers vs HEAD
  stems     : gap, blind
  files     : 4
  lines     : 229
  hits      : 1
  live uses : 1
  VERDICT   : NOT CLEAN
  self-hash : sha256/32 011b4f7fa6bdb2c7d727e6cf699e830b
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b212-docs
  run at    : 2026-08-27T13:06:39 (local)
  input     : added lines in D:/MY-DOwnloads/PLACE-papers vs HEAD
  stems     : gap, blind
  files     : 4
  lines     : 229
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 e70d635cb7c6cb775ea9f8a57a4c541a
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : commit_selfcheck
  act       : b212-docs
  run at    : 2026-08-27T13:18:33 (local)
  input     : D:/MY-DOwnloads/PLACE-papers
  input     : HEAD
  written   : 4
  foreign   : 0
  ro-claim  : none
  compliance : none
  VERDICT   : CLEAN
  self-hash : sha256/32 79c9f5be7afe602b3b2990b241075df1
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : mirror_verify
  act       : b212
  run at    : 2026-08-27T13:18:46 (local)
  input     : mirror-refresh-2026-08-27.zip
  files     : 40
  rows      : 40
  mismatch  : 0
  declared  : e1a2ce5
  ls-remote : e1a2ce53e37f
  VERDICT   : CLEAN ON ALL THREE CLAUSES
  self-hash : sha256/32 b4c3875ebdc6baf7a4c22f4ad52e5f1b
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b212-relay
  run at    : 2026-08-27T13:26:06 (local)
  input     : added lines in D:/relay vs HEAD
  stems     : gap, blind
  files     : 16
  lines     : 1312
  hits      : 16
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 f7de5e1f614b089eed8c17a80811c96c
=== END AUDIT SIDECAR ===
```

> ### **And the relay scope was checked BEFORE its commit, not chained to it** — the correction
> from (i), applied within the same act that made the error.

### THE INDEX QUERIES (clause (f), naming species explicit) — `data/audit_b212_index_query.txt`

**12 of 19 hit — and all four naming-species keys hit**, so the ruling was filed into a vocabulary
that can carry it, which it could not four acts ago. Seven misses, seven named: `alpha`, `beta`,
`psi`, `wronskian`, `alternation`, and now `parity` and `odd family` — **this act's own subject**.
### **The queue has now been named by three consecutive acts without being run. That is no longer a
note, it is a pattern, and it is recorded as one.**

---

## PINS

| repo | pin (`ls-remote`) |
|:--|:--|
| **SIDE-global-section** | `bb99f59` → `a5d6031` → ### **`2f2c184`** — *Core 339 → 360; the second commit is the banned-stem correction* |
| **PLACE-papers** | `9e75ffa` → ### **`e1a2ce5`** — the ruling at three sites + §23 + the register; hook CLEAN, 0 foreign |
| relay | `0b8fc93` (registration) → the b212 pin-line commit |
| SIDE-kernel | `0256e9e` — **UNMOVED** |
| **mirror** | rebuilt at `e1a2ce5`, **CLEAN ON ALL THREE CLAUSES** |
| HELD | `held/carrier-acts` = `6eada6a` — **LOCAL-ONLY, untouched** |

**DEVIATIONS:** none.
**DIVERGENCES:** one, with the ferry's own (P3), registered in advance and ### **resolved against
the executor.**
