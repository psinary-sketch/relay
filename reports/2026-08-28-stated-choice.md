# b226 — THE STATED CHOICE

**2026-08-28 · relay `reports/2026-08-28-stated-choice.md` · CONCURRENCY: SOLO, after b225**
**Ferry part 1 of 1, receipt confirmed IN FULL. Registration banked after the owner reads, before the statement.**
**Bank: `data/b226_stated_choice.txt` · print `data/b226_core_remeasured.txt` (382) · gates `tools/b226_checks.py`.**

> *** ### **VERDICT: (DEFINED).** Term 3's object exists at content on the level-limit premise,
> with the choice stated. ### **The choice-dependence question is filed OPEN**, with von Neumann's
> own equivalence-class machinery cited as where its answer lives. ***
>
> ### **NO TRACE IS DEFINED HERE. TERM 2 IS NOT TOUCHED. NO INCLUSION MAP IS USED, and purity is
> neither required nor tested.** *This is the first act of the plan b225's ruling opened.*

## COMPONENT 1 — THE STATEMENT (P1)

**For each finite place `p`:** `ℓ(p) := 2` if `p = 2`, else `1` — **the lowest level with `d₁ > 0`**
— and `u_p :=` the canonical spanning `E₁`-unit `4q·P₁f₍₁,₁₎` at the cell `(p, ℓ(p))`, normalized to
norm one. **At `∞`:** the rank-2 Sonin-sector eigenfunction normalized in `L²`, `c = +1` under the
pinned convention, **at its grade, BENCH**; its sector named by the b212 ruling, *whose
conversation-layer provenance is not a derivation and travels with it.*

### **`ℓ(2) = 2` and not 1 because `d₁(2,1) = 0` — the arrival depth is why the choice steps up.**

**Every constituent unfolds to the record's own objects:** `Son(p,n)` on `ℤ/p^{2n}` of dimension
`(pⁿ−1)²`; `f₍ᵢ,ⱼ₎ = e_{i+qj} − e_i`; `S` the DFT-like operator; `Π` the involution; `S² = q²Π` so
`M = S/q` has `M⁴ = 1`; `P₁ = (1+M+M²+M³)/4`; `4q·P₁ = (q+S)(1+Π)`.

**`u_p` lies in `E₁(S̄_p)`** by b198 (I2)'s closure sentence — *"each `E_λ(S̄)` is the closure of its
level tower"* — quoted **at the step it is used**. ### **And that sentence is used only to place
`u_p` inside the limit: no inclusion map is used as a map**, as the scope forbids.

*** ### **THE NORMALIZATION'S FIELD, NAMED RATHER THAN GLOSSED.** `‖u‖²` is a **totally-real
algebraic integer and is NOT rational** — at (3,1) exactly `72 − 36ζ⁴ − 36ζ⁵ = 139.657868…`. So
`u_p`'s coefficients are exact in `ℤ[ζ_N]`, but ### **the norm-one vector lives in the real
completion, not in `ℤ[ζ_N]`.** *It costs the plan nothing: Lemma 4.1.2 asks for `‖f_α‖ = 1`, not for
integrality.* ***

### **THE SOURCE — AND THE ONE CLAUSE IT CANNOT SUPPLY**

**Lemma 4.1.2**, quoted at the source document, is what this plan stands on:

> *"Consider an (E) … and a C₀-sequence `(f_α; α ∈ I) ∈ (E)` **with `‖f_α‖ = 1`**. Then
> `Π⊗_{α∈I} H_α` is the **closed, linear set determined by all C₀-sequences differing from it for a
> FINITE NUMBER of α's only**."*

*** ### **A DEFECT IN THE SOURCE MATERIAL, FOUND AT THE READ AND REGISTERED BEFORE IT WAS RELIED
ON:** the OCR extract on disk is **defective exactly at Definition 3.3.1** — the definition of a
C₀-sequence, which clause (a) asks for by name. It reads *"A sequence `f_α`, `α ∈ I`, is a
C₀-sequence, if and"* — then a page-number artifact, then Lemma 3.3.1. ### **The condition is lost.**
So the C₀ condition is carried at **b197's grade (read at content)**, said at the step it is used.
***This act does not present a source quote it does not have.*** ***

## COMPONENT 2 — THE CHECKS (P2)

| gate | verdict |
|:--|:--|
| **G-NORM** | ### **PASS** — `Σ_v ‖u_v‖ − 1 = 0`. ***Not merely convergent: zero.*** |
| **G-SECTOR** | ### **PASS at six stated places; the generic odd place OWED** |
| **G-DEF** | ### **PASS on one named premise**; shell test run, **not** a shell |

**G-SECTOR — `Su = qu` verified exactly in `ℤ[ζ_N]`, entry by entry:**

| place | cell | `d₁` | `(i,j)` | support | `‖u‖²` | `Su = qu` |
|:--|:--|--:|:--|--:|--:|:--|
| 2 | (2,2) | 2 | (1,1) | 12 | 152.491740 | ### **verified** |
| 3 | (3,1) | 1 | (1,1) | 6 | 139.657869 | ### **verified** |
| 5 | (5,1) | 4 | (1,1) | 20 | 196.529025 | ### **verified** |
| 7 | (7,1) | 9 | (1,1) | 42 | 370.139830 | ### **verified** |
| 11 | (11,1) | 25 | (1,1) | 110 | 945.311597 | ### **verified** |
| 13 | (13,1) | 36 | (1,1) | 156 | 1331.330009 | ### **verified** |

*The support is `N − q` at all six cells — recorded as **observed**, not asserted as a theorem.*

*** ### **THE GENERIC STEP IS OWED, EXACTLY AS THE REGISTRATION PREDICTED.** `u = 4q·P₁f` is in `E₁`
**by construction** — a projector image, `P₁` a projector because `M⁴ = 1` follows from `S² = q²Π` at
every place and level. ### **What is not generic is that this particular `(1,1)` generator is
NONZERO.** `d₁ > 0` gives `E₁ ≠ 0`; it does not give `u₍₁,₁₎ ≠ 0`. ### **Six places are not all
places, and five odd places are not all odd places.** The step **wants a result**, and this act did
not perform it. ***

**G-DEF.** The object is `⊗′_v (S̄_v, u_v)` per 4.1.1 and 4.1.2 — the closed linear set determined by
all C₀-sequences agreeing with `(u_v)` at all but finitely many `v`. ### **The one named premise is
the LEVEL-LIMIT premise**, warranted by b198 (I2) *at its grade*, and **a premise here, not a theorem
of this act**. ### **The shell test: it is NOT a shell.** `u_v = 0` everywhere fails G-NORM at once
(`Σ 1` diverges) and fails 4.1.2's own hypothesis. ### **The definition excludes the zero vector at
every place** — precisely what b215's test asks — **and at `p = 2` it excludes level 1 entirely.**

## COMPONENT 3 — THE VERDICT (P3) AND THE SHADOW (P4)

### **THE CHOICE-DEPENDENCE QUESTION — FILED OPEN**

4.1.1 builds the product **relative to an equivalence class**; 3.3.2 defines that class by
`Σ|(f_α,g_α) − 1|` converging. ### **So the object depends only on the class, and whether two
canonical choices land in the same class is OPEN.**
*** ### **Lemma 4.1.1 is the warning that it matters: incomplete products for different classes are
"MUTUALLY ORTHOGONAL." A different choice could give an ORTHOGONAL object, not an isomorphic one.**
### **And this choice's canonicity is a DEFINITION MADE BY RULING, not a theorem.** ***

### **THE SHADOW — 7 of 7 at zero axioms**

`Core/StatedChoiceShadow.lean`, zero-axiom on the first compile. **Core 375 → 382**;
`AXIOM_PRINTS.txt` still carries **zero** axiom-bearing lines; the regenerated 382 are
**byte-identical** to the bank.

**Polarity controls ran first and are not decoration:** `arrival_depth_is_dead` (so
`chosen_level_positive` is not vacuous) and `deviation_can_be_nonzero` (so `c0_deviation_is_zero`
measures norm-one and not the encoding). Then `chosen_level_positive`, `chosen_level_is_lowest`,
`level_one_fails_at_two`, `c0_deviation_is_zero`, `level_is_a_function`.

**Correspondence row 87**, written by the committed tool `corr_row.py` **driven from a file**, so no
cell passed through a hand-quoted shell string — b178's and b193's shared failure mode.

*** ### **AND THE GENERATOR WAS UPDATED *BEFORE* THE APPEND, NOT AFTER.** b221 found that b220 had
appended nine lines to `AXIOM_PRINTS.txt` without adding its module to `AllPrints.lean`, so the
banked file and the tool that produces it had silently diverged. ### **Here the module went into the
generator first, the whole set was regenerated, and the result was diffed against the bank before
anything was called verified.** ***That defect is not repeated.*** ***

## THE AUDIT SIDECARS (emitted; copied from the sidecar files)

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b226
  run at    : 2026-08-28T14:05:32 (local)
  input     : 8 checks routed through the harness
  checks    : 8
  pass      : 8
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 1b3ba15d90be6a807462e168bad204e6
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b226-docs
  run at    : 2026-08-28T14:06:45 (local)
  input     : added lines in D:/MY-DOwnloads/PLACE-papers vs HEAD
  stems     : gap, blind
  files     : 2
  lines     : 150
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 d7651554e27cfdd101716614cb4c8248
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : commit_selfcheck
  act       : b226-docs
  run at    : 2026-08-28T14:09:14 (local)
  input     : D:/MY-DOwnloads/PLACE-papers
  input     : HEAD
  written   : 2
  foreign   : 0
  ro-claim  : none
  compliance : none
  VERDICT   : CLEAN
  self-hash : sha256/32 0c0645ae81356757f3e3c722bcc5b364
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : mirror_verify
  act       : b226
  run at    : 2026-08-28T14:09:33 (local)
  input     : mirror-refresh-2026-08-28.zip
  files     : 40
  rows      : 40
  mismatch  : 0
  declared  : b21075a
  ls-remote : b21075a0dcfc
  VERDICT   : CLEAN ON ALL THREE CLAUSES
  self-hash : sha256/32 271f0d3500acfbe184b58a172b5eab01
=== END AUDIT SIDECAR ===
```

**The seat-boundary pre-commit hook was exercised on PLACE-papers and returned CLEAN** — 2 staged
paths, 0 foreign hits. *Reported either way, as the EXECUTION line requires.*

## WHAT IS **NOT** CLAIMED — at the verdict, as the ferry requires

1. ### **NO TRACE.** None defined, named as definable, or sketched.
2. ### **NO VALUE.** Nothing numerical about term 3.
3. ### **NO TERM-2 STATEMENT.** Term 2 untouched; its four debts stand where b225 left them.
4. ### **NOTHING AT COMPLETE ROSTER.**
5. ### **THE CHOICE'S CANONICITY IS A DEFINITION MADE BY RULING, NOT A THEOREM**, and the
   choice-dependence question is OPEN with 3.3.2 cited.
6. ### **G-SECTOR'S GENERIC ODD PLACE IS OWED**, not discharged.
7. ### **NO INCLUSION MAP IS USED**; purity is not required, mentioned as a requirement, or tested.
8. ### **NOTHING ABOUT `h2` BEYOND THE REGISTER SENTENCE EXACT. NOTHING DEPOSITS.**

## PINS

| repo | pin | note |
|:--|:--|:--|
| **SIDE-global-section** | `1ab4f24` → ### **`b8b3159`** | the shadow + print + generator + row 87; ### **File E byte-identical, checked** |
| **PLACE-papers** | `0e0d8d1` → ### **`b21075a`** | chain §30 + the register; hook CLEAN, 0 foreign |
| relay | → the b226 pin-line commit | registration, bank, print, gates, four sidecars, report |
| SIDE-kernel | `0256e9e` — **UNMOVED** | — |
| mirror | rebuilt at `b21075a`, **CLEAN ON ALL THREE CLAUSES**, 40/40 | — |
| HELD | `6eada6a` — LOCAL-ONLY, untouched | — |

**DEVIATIONS:** none.
**DIVERGENCES:** ### **one, with clause (a)** — it asks for *"exactly what a C₀-sequence is"* at
source, and ### **the extract is defective at Definition 3.3.1.** Named at registration, carried at
b197's grade at the step it is used, and not worked around.

### **THE NEXT ACT IS THE TRACE.** b221 found act 7 §4's sentence to be **a name, not a definition**
— neither what it traces, nor over which space, nor normalized how. ### **With the object defined,
"over which space" has an answer for the first time.** The other two remain the next act's to supply.
