# B3 RUNS · `(v)` ASKED · THE GRAMMAR NOTE
## ### **NOT STABLE, NOT EVEN MONOTONE — AND A BLOCKING ROW THAT FIRED ON ITS FIRST LINE**

**Relay report · 2026-08-17 · author-ruled · `INSTRUMENT` track · sub-gate declared · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 7ea0592769a6e9be2bb20a49a3b1dcc55b6f1f55   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 7ea0592   ### 22 / 22 COHERENT, three ways
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> ### **ROUTING, DECLARED FIRST.** *The ferry recommends Fable at high effort for items 1–2. This sitting ran
> on* ### **Opus 5.** *Item 1's measurement and item 2's reads are work Opus does properly and they are
> reported as done.* **The two conclusions that are proof-search residue rather than measurement are marked
> as such in place, and neither is claimed at more depth than it was reached.**

---

## §1 — `B3`: ### **NOT STABLE — AND AT FOUR OF FIVE WINDOWS, NOT EVEN MONOTONE**

**`Φ_{L,S}` on `V`; `S` a freely chosen set of primes, enlarged at fixed support; `ω = 10⁻³`.**

| `L` | steps in `NPOS` as `S` grows `{2} → {2,3} → {2,3,5} → {2,3,5,7} → {…,11}` | monotone? |
|:--|:--|:--|
| **9** | ### **`+474`** `−80` `−1` `+0` | ### **no** |
| **12** | ### **`+616`** `−94` `+22` `−6` | ### **no** |
| **16** | ### **`+751`** `−116` `+44` `+2` | ### **no** |
| **27** | ### **`+534`** `+14` `+16` `−3` | ### **no** |
| **32** | ### **`+455`** `+57` `+2` `+9` | yes |

> ### **THE COUNT IS NOT STABLE UNDER ENLARGING `S` AT ANY WINDOW TESTED.** *Every `L`'s first step moves it
> by `+455` to `+751` offenders.*
>
> ### **AND AT FOUR OF THE FIVE IT IS NOT EVEN MONOTONE — ENLARGING THE PLACE SET CAN *DECREASE* THE COUNT.**
> *That is stronger than instability: a maximum matching is non-decreasing under adding edges, so a count
> that falls when a lag is admitted is not behaving like a matching number at all.* **`L = 32` is monotone;
> four of five is the figure and it is stated rather than rounded to five.**

### **COLUMN `(iv)` IS PRICED EXACTLY, WHICH IS WHAT THE REGISTRATION SAID THIS BRANCH WOULD BUY**

| operation | effect on the count |
|:--|:--|
| **widening support** *(increase `L`)* | the sawtooth — derived for the pure one-lag form, measured at `r = 1 … 8` |
| ### **adding a place** *(enlarge `S`)* | ### **hundreds of offenders, in BOTH directions, with no derived law** |

> ### **TWO OPERATIONS OF DIFFERENT KINDS ON ONE OBJECT. THE CORPUS'S INSTRUMENT IS CONFIRMED "TWO OBJECTS
> GLUED" BY `B1`'s OWN TEST** — *an honest MEASURED distance from `T2`, not an argument for one.*

---

## §2 — ### **THE BLOCKING ROW FIRED ON THE FIRST LINE, AND THE `μ` COLUMN IS WITHDRAWN IN FULL**

*`P2` registered that `NPOS` tracks `μ` to within three offenders. It returned* ### **`−406`** *on line one,
with* `μ = 1099` *on* `M = 2197` *— where* `⌊M/2⌋ = 1098` *makes `1099` impossible. The run was stopped and
nothing was read from it until the cause was found.*

**The cause:** the matching routine assumed **index parity** is a bipartition — true only when every live
address `k_q` is odd, and false the moment a prime power is admitted:

| `q` | `log 2` | `log 3` | ### **`2 log 2`** | `log 5` | ### **`log 7`** | `3 log 2` | ### **`log 11`** |
|:--|--:|--:|--:|--:|--:|--:|--:|
| `k_q` at `ω = 10⁻³` | 693 | 1099 | ### **1386 EVEN** | 1609 | ### **1946 EVEN** | 2079 | ### **2398 EVEN** |

**A proper 2-colouring audit finds all 25 cells NOT BIPARTITE**, so no `μ` is computed for any of them — a
general maximum matching needs blossom contraction, which is **not in this instrument and is declared rather
than approximated.** ### **The `NPOS` column is an eigenvalue count of a well-defined symmetric matrix and is
unaffected; it is the column that survives, and every number in `§1` is from it.**

> ### **TWO INDEPENDENT ROUTES REACH THE SAME CONCLUSION — the non-monotonicity and the colouring both say
> these are not matching numbers.**

### **AND THE SCOPE NOTE THIS FORCES ON THE WEEK'S DERIVED LAW**

> ### **"EVERY LIVE `k` IS ODD, SO INDEX PARITY IS A BIPARTITION" IS A PROPERTY OF A PARTICULAR `(ω, LAG
> SET)` AND IS NOT AUTOMATIC.** *It held for every cell the law was actually measured on — the one-lag
> sawtooth cells, where the graph is a union of PATHS and is bipartite for any `k` with parity irrelevant,
> and the two-lag cells `{log 2, log 3}`, both odd.* ### **NO BANKED CLAIM IS FALSIFIED. What was missing was
> the warning that the premise can fail, and it fails as soon as a prime power is admitted.**

**One caveat named and not run:** *weight-independence was established in the bipartite regime and is* ### **not
established here**, *so part of the movement may be coefficient-driven. The check is a re-run at unit weights.*

**And a numbering note:** *the earlier `+285` at `L = 9` compared `{log 2}` against `{log 2, log 3}`;* `B3`'s
`+474` *compares `S = {2}` — which admits `log 2`, `2 log 2` and `3 log 2` — against `S = {2,3}`.* **Different
objects, both correct.**

---

## §3 — `(v)` ASKED OF ALL THREE FRAMES · ### **ABSENT ON EACH, WITH ONE REASON THREE TIMES**

*Corpus-first, case-insensitive probes with second-mode confirmation, per the rule extended this morning.*

| frame | `(v)` | what the read found |
|:--|:--|:--|
| **`THH` / derived** | ### **ABSENT** | no crossing of `THH` with Hecke, character, class group, basis or decomposition anywhere. **Plus the hard level bar already on the sheet:** *"`Γ_ℝ` and Euler factors are classical functions;* ### **NO CELL INHERITS ACROSS**" |
| **DENINGER'S SYSTEM** | ### **ABSENT — and partly UNASKABLE from what is held** | no hits, and the sourcing is already declared: ### ***"DENINGER IS NOT QUOTED HERE, BECAUSE HIS TEXT WAS NEVER FETCHED"*** — the postulated `H¹` is the corpus's own characterization. *`1807.06400` and `2204.02714` are held at content; that material is not.* |
| **CCM'S CYCLIC-MODULE CARRIER** | ### **ABSENT** | its own quoted input is ### **"the ideles class group of a global field"** — the FIELD, not a decomposition of it |

> ### **NONE OF THE THREE TAKES A DECOMPOSITION AS INPUT.** *`THH`/derived takes `Spec ℤ`; Deninger's takes a
> dynamical system on the arithmetic scheme; CCM's takes the adele class space of the field.* **All three
> take the OBJECT; the two-decompositions witness varies the PRESENTATION at fixed object.**
>
> ### **SO THE QUESTION CANNOT BE POSED TO ANY OF THEM, AND THE FRAME THAT WOULD LET IT BE POSED IS NONE OF
> THEM.** *Second-mode probe for any frame taking a basis, presentation or decomposition as data: nothing but
> the sheet's own text.*

### **WHICH TURNS `(v)` INTO A STATEMENT ABOUT THE FRAME'S INPUT TYPE**

> ### **A FRAME SATISFYING `(v)` MUST TAKE THE PRESENTATION AS DATA, NOT ONLY THE OBJECT** — *sharper than
> "assigns different structure to two bases", because it says WHERE the difference would have to enter.*
>
> ### **MARKED AS THIS SITTING'S PROOF-SEARCH RESIDUE.** *The three reads are solid and corpus-first; the
> input-type reformulation is the part that would benefit from being redone at depth on Fable.*

---

## §4 — THE GRAMMAR NOTE *(question grade, flagged `OURS`, no claim)*

> **MONOID = NOUN** — *states and addresses; what there is; the matching number counts it.*
> **INVOLUTION = VERB** — *the line-manufacturing morphism, with `σ = ½` its unique fixed point.*
> ### **SIGN = AFFECT** — *definiteness at the complete roster: readable from neither alone, and*
> ### **uncalibratable by example — which is no-positive-control said in the other register.**
>
> ### **AND THE REFINEMENT THAT BLOCKS THE OBVIOUS IMPORT: the sign is exactly definite or not, a two-valued
> thing. The continuum lives in the MARGIN** *(`Λ ≥ 0`; the three-witness knife-edge; the atlas's hover at
> `O(10⁻⁴)`).* **So graded and probabilistic affect models do NOT transfer to `h2`'s truth value — only, if
> at all, to its margin.** ### **And a model of the margin is not a model of the clause.**

*Filed as one paragraph in the interface-darkness / conservation-of-cognition line. Nothing promoted.*

---

## §5 — THE FOUR ROAD-B READ-DEBTS, BY EXACT CITATION *(navigator-side; all four have failed executor-side)*

| # | item | exact citation | executor-side history |
|--:|:--|:--|:--|
| **1** | **CC's `[19]`** | ### **UNRESOLVABLE AS GIVEN** — a reference NUMBER inside a CC paper's own list. *To resolve it the CONTAINING paper must be named; the corpus's phrase is "their `[19]`" in the semilocal context, so the candidates are `2310.18423` or `2511.22755`, both held.* | never a document here; the name-identity law forbids treating a pointer as one |
| **2** | **CC 1999, the semilocal trace formula** | ### **DOI `10.1006/jnth.1999.2392`** *(J. Number Theory, 1999)* — `TITLE-UNVERIFIED` in the `BIBLIOGRAPHY` | named in `Spectral_Inertness` as a reinterpretation in operator-algebraic language; **that is a corpus sentence about it, not a read of it** |
| **3** | **the CC archimedean / semi-local positivity line** | ### **`alainconnes.org` — `positivity.pdf` and `draft4.pdf`** | ### **API `429` twice, host `403` twice.** *The corpus's own words: "the single most load-bearing unread item on this face"* |
| **4** | **`2106.01715` BODY** | **Connes–Consani, *Spectral Triples and Zeta-Cycles*, `arXiv:2106.01715` = Enseign. Math. 69 (2023), 93–148** | ### **abstract only.** *The `ζ`-cycle is NAMED and CHARACTERISED there but not DEFINED; the definition is in the body* |

**And the two Road-A items, for completeness:** `1405.4527` and `1502.05580` — ### **`TITLE-UNVERIFIED`, and
their arrest quote is `ATTRIBUTED`, not body-read** *(PDF `403`)*. **Plus one corpus-internal discrepancy
flagged not resolved:** *`1507.05818` carries the title* `The Scaling Site` *in `OPEN_TRAILS` while the
`BIBLIOGRAPHY` lists it `TITLE-UNVERIFIED`.*

> ### **NOTHING IS RECORDED AS ABSENT. Every row above is a debt with an address, not a negative result.**

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **`B3`: NOT STABLE, AND NOT EVEN MONOTONE AT FOUR OF FIVE WINDOWS.** Column `(iv)` priced exactly;
   the instrument is confirmed two objects glued by its own test.
2. ### **THE BLOCKING ROW CAUGHT A REAL DEFECT** — the `μ` column is withdrawn in full, the audit is banked,
   and the week's law gains the scope note it needed. **No banked claim is falsified.**
3. ### **`(v)` IS ABSENT ON ALL THREE FRAMES, FOR ONE REASON: none takes a decomposition as input** — which
   reformulates `(v)` as a condition on the frame's input type.
4. ### **THE GRAMMAR NOTE IS FILED** at question grade with the margin refinement that blocks the import.
5. ### **THE FOUR READ-DEBTS ARE LISTED BY EXACT CITATION**, none recorded as absent.

### **WHAT WANTS YOUR RULING**

1. **The unit-weight re-run** *(does the non-monotonicity survive removing the coefficient variation?)* —
   one sitting, bench work.
2. **The input-type reformulation of `(v)`** is proof-search residue reached on Opus; *your routing note says
   Fable at high effort for exactly this.*
3. **`[19]` needs its containing paper named** before it can be retrieved at all.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, absence
re-verified after this push. *Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. `WIDEN` PAUSED. NOTHING DEPOSITS.**
