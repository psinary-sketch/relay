# THE ORIENTATION-DEPENDENCE READ — DOES FILE E'S IDENTITY CONSUME AN ABSOLUTE ORIENTATION?
## ### **IT DOES NOT. THE CENSUS PASSES TERM BY TERM AT THE OWNER FILE'S OWN SIGNATURES — THE IDENTITY IS AN EQUATION IN ℝ OVER A RATIONAL INDEX, AND THE ONE CONSTITUENT NEEDING AN ARGUMENT (THE CONSTRAINED CLASS) SITS ON CONJUGATION'S FIXED SIDE BY A REAL-COEFFICIENT PROJECTION. WITH THE ORBIT ARGUMENT — A REAL-VALUED EQUIVARIANT QUANTITY TAKES THE SAME VALUE ON BOTH WINGS — THE VERDICT IS THAT THE ABSOLUTE ORIENTATION DISSOLVES ON THE IDENTITY'S PATH: THE CHOICE IS NOT UNDERDETERMINED FOR THE IDENTITY, IT IS IMMATERIAL TO IT. THE SCOPE IS HELD VERBATIM FROM THE REGISTRATION: CLAIMS ABOUT THE WING KEEP THEIR MISSING FACT, ROW 57's TAG IS NOT RE-GRADED, THE CANON-XIX BAR IS UNTOUCHED, AND THE IDENTITY IS NOT MADE TRUE — THE ACT REMOVES AN OBSTACLE FROM THE PATH, IT DOES NOT WALK THE PATH. CORE 268/268. NOTHING DEPOSITS.**

**Relay report · 2026-08-22 · ferry-executed (part 1 of 1, receipt confirmed
in full, Rule 1) · Rules 3/4/5 · the b104 run-registration banked before the
grading, with the orbit argument, its equivariance premise, its pre-named
failure mode, and its scope limits all written out in advance · a grading
read plus decidable checks — nothing built · the canon-XIX bar rides ·
nothing about `h2` beyond the read's own graded rows · nothing deposits ·
nothing circulates beyond this report.**

> ### **RULE-3 LOG:** *(a) file E read whole at its owner and quoted at its
> declared types before any grading; the b103 exchange theorem (row 66) and
> D3's relation (row 63) at their rows; the junction ledger's arithmetic
> quoted verbatim from row 57. (b) the row-52 precedent quoted as THE SHAPE
> BEING TESTED and never as a conclusion to import — the difference between
> the two instances is stated explicitly at the verdict. (c) the read grades
> under the three-grade rubric; the dissolution was pronounceable only if
> every term decided at statement level, and the one term that could not be
> settled by a glance was argued and cited rather than waved through. (d)
> the three components ordered. Banned-term grep on this act's writing:
> clean.*

---

## §1 — COMPONENT 1: THE INVARIANCE CENSUS (P1)

**The prediction, verbatim:** *"(P1) THE INVARIANCE CENSUS: every term of
file E's identity is conjugation-invariant at statement level — real-valued
traces, weights, and counts on a conjugation-stable class — decided term by
term with the statement's words quoted; branch (b): the
orientation-dependent term named exactly, first-class, the dissolution
refused."*

The census, with the file's own words and declared types:

| term | as declared at the owner | verdict |
|---|---|---|
| the cell | `a_sq : ℚ`, `one_lt : 1 < a_sq` — "the window a² > 1" | **INVARIANT** — rational datum, rational condition |
| the archimedean trace | `value : ℝ` — "the ε-regularized archimedean E₁-trace on the constrained class" | **INVARIANT at statement level** |
| the constrained class | the E₁ sector (the +1-eigenspace of F) | **CONJUGATION-STABLE** — argued, see below |
| the quotient trace | `value : ℝ` | **INVARIANT at statement level** |
| Weil's ledger | `wInf : ℝ`, `wPrimes : ℝ` — "W_∞ (CC convention…) and the prime sum" | **INVARIANT at statement level** |
| the relation | `T.value + Q.value = W.wInf − W.wPrimes` | **INVARIANT** — an equation in ℝ |

**The one item that needed an argument rather than a glance, and its
support:** the constrained class. Conjugation stabilizes it because the E₁
sector's eigenvalue **+1 is real** and hence conjugation-fixed, and because
its projection is `proj4 F 1` = (1/4)(1 + F + F² + F³) — proved at the owner
at b100 — whose coefficients are **real**, so the projection commutes with
conjugation. The contrast is exactly why the item deserved checking: the C₈
frame lives on the **twisted** sectors, which conjugation **exchanges** (rows
39/40/66). The class is on the fixed side of that split, not the exchanged
side.

**One term carries a convention and it is named rather than passed over:**
the docstring attaches "the CC convention" to W_∞. A convention is precisely
what row 52's audit rules gauge — and the identity consumes the convention's
**output**, a real number, not the convention itself.

### **P1 VERDICT: BRANCH (a) — every term conjugation-invariant at statement level. No orientation-dependent term exists to name.**

## §2 — COMPONENT 2: THE LEDGER CHECK (P2), DECIDED IN KERNEL (row 67)

`conj_fixes_real_flips_imaginary` — row 57's own summands under (a,b) ↦
(a,−b): (1,0) fixed, (0,−1) ↦ (0,+1), the suspect's value (1,−1) ↦ (1,+1),
**the real part invariant at 1** on both. Exactly the ferry's P2 sentence,
decided. `twin_conjugate_sum_real` — at every banked q, q(1+i) + q(1−i) =
(2q, 0): the conjugate pair sums to a real number, **and it is the same real
number from either wing**. `real_functionals_agree_on_the_orbit` — the real
part and the norm-square agree on a point and on its conjugate, with norm² =
2 at the suspect, matching row 57's decided value. **All three zero-axiom;
Core 268/268.**

## §3 — COMPONENT 3: THE VERDICT (P3) AND THE FILINGS

The chain, as registered before the grading and now run: **(a)** every term
real over a rational index [§1]; **(b)** conjugation exchanges the two wings
and fixes neither [row 66]; **(c) THE ORBIT ARGUMENT** — a real-valued
quantity produced from the recorded data by a conjugation-**equivariant**
construction takes the same value on both wings, since equivariance sends its
value to that value's conjugate and a real number is its own conjugate;
**(d)** the equivariance premise, supported by row 50's audit and row 52's
one-dictionary audit, **checked rather than assumed — the pre-named failure
mode did not fire.**

### **P3 VERDICT: THE ABSOLUTE ORIENTATION DISSOLVES ON THE IDENTITY'S PATH.** File E's identity consumes the relative correlation (derived, D3) and real, orientation-indifferent terms; it consumes no absolute wing selection. **Whichever wing one "chooses", the identity's terms are literally identical — so the choice is not underdetermined for the identity, it is immaterial to it.** The row-52 precedent is cited as the shape's second instance, with the difference stated precisely: **row 52 dissolved what the record can SAY about the absolute class; this act dissolves what the identity CONSUMES of it.** Related shape, distinct claims, neither imported into the other.

### **THE SCOPE — fixed in the registration, held verbatim here.** This says nothing about claims **about** the wing or the class: every such claim **keeps its named missing fact**, and the orientation fact retires only from the identity's path. It does **not** re-grade row 57's three-tag sentence — whether the [UNDER H-COH-∞] tag is still needed where the value is read as a real quantity is a **consequence candidate for a later act with its own gate**, named here and not acted on. It does not touch the canon-XIX bar's text. **And it does not make the identity true, provable, or its terms computable: it removes an obstacle from the path; it does not walk the path.**

**One absence, named in advance and still standing:** the record states the
ledger's sum as the complex value 1 − i and file E's terms as real, and it
does not state anywhere **which** real functional of the ledger data the
archimedean term is. The orbit argument was built not to need that — it holds
for any real-valued equivariant functional — and the act says so plainly
rather than quietly fixing one.

**The filings.** **THE MAP RE-ORDERS:** the identity's blocker list is now
**G-EXT-3 + the re-rerun + the guarded boundary**, and the orientation fact
moves off the identity's path into **the absolute-claims lane**, where it
keeps full force. L2's obstruction is unchanged as a fact, but its
consequence narrows — the transport was wanted for the absolute reading, so
L2 stays a named obstruction in that lane rather than a blocker on the
identity. **The D3 re-grade remains HELD:** its own list (L2 + G-EXT-3) is a
different list, and this act does not shorten it. Row 67 records the decided
core; rows 57/61/63/66 cited, not moved; file E's row untouched. **The ninth
seam close's debt: b103 + this act (rows 66–67), owed at the cadence.**

## §4 — THE TAG AUDIT (the named section)

H-COH-∞ appears only where blocker lists are restated and in the named
consequence candidate — as a tag, never as support, and the candidate is
explicitly not acted on. No sentence pronounces an absolute class. The
extension's sentences keep their grades; no boundary value is computed,
approached, or cited. Zero decayed sentences at the close-out grep.

## §5 — THE GRADE-LABEL AUDIT (the named section)

The census: decided at the file's declared types, with the class item argued
and cited. The three terminals: decided, zero-axiom. The orbit argument:
derived, general, registered before the read, its premise checked. The
verdict: scoped to the identity's path and stated with its four explicit
non-reaches. The row-52 citation: a shape, with its difference stated. No
unlabeled number entered this report.

## §6 — THE RECORD AND PINS

*Relay: the b104 run-registration + bank + this report. SIDE-global-section:
`Core/OrientationShadow.lean` (3 terminals), CORRESPONDENCE row 67 + the
counts, AllPrints/AXIOM_PRINTS regenerated (**268/268**); no Interfaces work.
PLACE-papers: the act record + the re-ordered map line + the FINDINGS entry.
Mirror: refreshed — **`mirror-refresh-2026-08-22-t.zip` is the one to
load.***

> **PIN LINE (post-push read-back, ls-remote):**
> [PIN-LINE-SPLICE]

## FOOT

**After this act the ninth seam close queues at the cadence (the link arc and
this read into the keystone), and the map's remainder proceeds per P3's
branch — the λ-scale need (G-EXT-3) next among research items. The TECHNE
lane at the author's schedule; the desk's remaining one-liners standing; the
restart kit complete at the newest mirror as the standing safety.**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED.
NOTHING DEPOSITS. NOTHING CIRCULATES.**
