# `W-CONSTRUCTION-1` · ACT 9 — THE ARCHIMEDEAN REGULARIZATION
## ### **`(REG-lands)` AT CONTENT MODULO ONE NAMED JOINT (THE `W_∞` SECTOR APPORTIONMENT) — THE QUOTIENT LEMMA PROVED LONGHAND, THE CLOSED FORM NOW A THEOREM OF THE MODEL — THE RE-RUN: BOTH BRACKET READINGS `(I-differ)` WITH EXACT ADDRESSES, THE OPEN CONTENT RE-LOCALIZED ONTO THE `E₁`-SECTOR ε TERM**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 9 · sub-gate declared · Rules 4/5 · on the
face: the regularization attacked as mathematics; the identity NOT decided by it; nothing deposits;
nothing circulates**

> ### **RULE-3 LOG:** *act 8 at `12e9a5d` ✓; `v0.8` `§20`, the `(I-third)` table,
> `QuotientWeilShape` 4/4, the closed form ✓; CC Selecta `§4–5` at content (Thm 4.7; Lemma 5.4;
> Prop 5.5; K_I HS) ✓; the banned grep on this ferry: CLEAN. Mathlib Hilbert–Schmidt: grep-checked,
> ZERO hits — the HS-compression statement CANNOT be formed; a NAMED STUB per the act-4 precedent.
> A b36 registration gap FILED, not silent: the atlas family is not pole-constrained; the anatomy
> conclusions are family-independent; the constrained bench PRICED. ROUTING: b37 registration and
> instrument by Fable; the banked run ON THE OPUS LANE, first attempt, all gates PASS. Terms
> coined: none.*

```
build document  THE_GLOBAL_SECTION.md v0.9 (§21 the regularization + the proved lemma;
                §22 the re-run)
bench           data/b37_registration_2026-08-18.txt (banked FIRST, both readings
                longhand) + b37_2026-08-18.txt (the Opus lane; C0 + b35 parity pins +
                mask algebra ALL PASS)
vanilla leg     tools/lean/QuotientLemmaShadow.lean + AxiomCheckQL — 4/4 "does not
                depend on any axioms"; the relay's zero-axiom count: 88
mathlib leg     FILE D opened (QuotientCount.lean, elaborates, ONE labeled sorry);
                THE LEDGER 2 → 3 AND SAID SO (B–C; infrastructure; the quotient count)
```

---

## §1 — ITEM 1: THE REGULARIZATION

> ### **VERDICT: `(REG-lands)` AT CONTENT MODULO ONE NAMED JOINT.** The chain: Thm 4.7 at content
> (the banked `d*ρ` statement); ### **the `ε` machinery is MODE-DIAGONAL (Lemma 5.4's per-mode
> `t(n)`), so the sector split of the regularized `ε`-part is EXACT — the b37 gates reproduce the
> b35 shares from the index mask BEFORE use (`2.9e−5` / `3.6e−6`; mask algebra `4.4e−16`)**;
> F-commutation proved (the classical profile); K_I Hilbert–Schmidt at cite, its compression at
> cite, the Lean statement a NAMED STUB (no HS class in Mathlib, grep-checked). ### **THE JOINT,
> on the face: `W_∞`'s sector apportionment is NOT derived — the mode-diagonal argument covers the
> `ε`-part only.** *The ε-lemma's owed bookkeeping is DISCHARGED for the series part; the re-run
> licensed with `resid47` removed by construction. B–C owes the regularization NOTHING — it is
> archimedean (the ferry's item-4 question, answered: nothing).*

## §2 — ITEM 3 (attacked beside): ### **THE QUOTIENT LEMMA, PROVED**

**The fixed-orbit count on `V_inv`, longhand (build doc `§21(b)`): each class holds `p^{n−1}` units
and has size `(p^n−1)/(p−1)`; the class count is `p^n(p−1)`; the preimage count is `p^k`; the
double count `p^n(p^n − p^k)`. Hence:**

> ### **`τ_q(p,n,k)·p^{k/2} = (p^n − p^k)/(p^n − 1)` for `1 ≤ k ≤ n−1`, `0` for `k ≥ n`. ∎**
> *Every banked integer re-derived exactly (the b10 dimensions 4/8/16/18/100; the traces
> 48/32/54/224/192/128) and compiled at no axioms (`QuotientLemmaShadow`, 4/4).* **The quotient
> channel's convergence to Weil's coefficients at the level limit: PROOF GRADE longhand. The Lean
> formalization is file D's single labeled sorry (`offball_scaling_pair_count` — elaborates; the
> ledger 2 → 3, deliberate, owners named).**

## §3 — ITEM 2: THE RE-RUN (licensed; the joint flagged on both readings)

| `a²` | `(Θ_q − PR)` | `E2even` | `E2odd` | `D_dictated` | `D_sector` |
|--:|--:|--:|--:|--:|--:|
| 2 | +0.000 | 1.002 | 0.678 | **−2.681** | **−1.002** |
| 3 | −0.106 | 0.911 | 0.606 | **−2.534** | **−1.017** |
| 4 | −0.087 | 0.834 | 0.540 | **−2.295** | **−0.921** |
| 8 | −0.244 | 0.686 | 0.411 | **−2.026** | **−0.930** |
| 9 | −0.135 | 0.665 | 0.393 | **−1.858** | **−0.800** |
| 12 | −0.196 | 0.620 | 0.355 | **−1.791** | **−0.816** |

> ### **VERDICTS, per the registered map: `(I-match)` did NOT land under either reading — the
> closure protocol's step one is NOT triggered. Both readings `(I-differ)`, addresses EXACT:**
> **(B-dictated)** `D = (Θ_q − PR) + (E2odd − 2·E2)` — *the dictated bracket carries the
> `ε`-integral twice; pure algebra; the obstruction is the EQUATION'S CALIBRATION, not the object.*
> **(B-sector)** `D′ = (Θ_q − PR) − E2even` — *the finite-level deficit is now THEOREM-controlled
> and dies at the level limit (`§21(b)`); what remains is the EVEN-SECTOR `ε` mass:* ### **the
> equation's open content re-localizes onto the `E₁`-sector `ε` term — the density question's
> content, carried by the `W_∞`-apportionment joint.**

## §4 — ITEM 4: THE RECORD

*`v0.9` (`§§21–22`); rows 104–108; `F.2026-08-18s`; both legs' prints run; this report the bank.
The ledger: **3**, each with its owner — B–C (the L² realization; owes the regularization NOTHING);
the infrastructure `⊗′`; the quotient count (file D, the act-9 longhand proof its owner).*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED — NOT DECIDED AT THE
MODEL, SAID SO. NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING CIRCULATES.**
