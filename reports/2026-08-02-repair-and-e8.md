# The repair + E-8 pass — the vocabulary catch and the composition barrier — 2026-08-02

## (1) Vocabulary repair with content sharpening

The banned word leaked into the verdict pass's graph-supplier language — **navigator's leak,
author's catch**. Repaired as REPHRASE-TO-CONTENT, never token-swap, across every landed output
(the keystone fifth row + changelog, FINDINGS F.2026-08-02, two OPEN_TRAILS addenda, both relay
reports): the supplier now reads **the Ramanujan bound — the spectral margin below the trivial
eigenvalue**. The verbatim-quoted registration keeps its record with the repair annotated beside
it. Keystone → v0.4.1. The loom takes the leak+catch line with the scope re-affirmation: **the
drafting-time screen binds instruction-quoted phrases the same as fresh prose** — an analogy
arriving in a ferry is still subject to the ban at the point of landing.

**The three-level form** (the clarification the author's question forced; now §1 of the
reality-vs-bound note, credited): **RH IS A BOUND** (statement) · **Q IS THE TWO-VARIABLE FORM**
(object) · **POSITIVITY-DELIVERS-THE-BOUND** (mechanism — proved in every world where the bound
is proved: intersection form / ferromagnetic coupling / Type-II instances / Deligne's import) ·
**ζ owns the one-variable half free and lacks the proved two-variable positivity** — the exact
address of `h2`.

## (2) E-8 RUN — THE COMPOSITION BARRIER

**Registered (verbatim):** *"the barrier COMPILES — one-variable data underdetermines the
two-variable form (the sort's cleanness made structural); the surprise branch (a composition that
escapes the schema) files bigger and names the escape's shape."*

**THE BARRIER COMPILED — the registered expectation held.** `CompositionBarrier.lean`
(SIDE-lv-conservation, **HELD branch `composition-barrier` = `14720d9`**, pushed, not landed):
- the criterion layer typed (diagonal agreement — coefficient values, single-index moments, edge
  inequalities all factor through it);
- the composition operations typed **by soundness** (the §4b move Face-E validated:
  `DerivesFromCriterion` as transport along one-variable agreement — sums, products, limits, and
  linear maps typed at once by what they can see);
- the target = the wanted-poster carrier positivity (Sylvester, rank 2);
- the witness world minimal and exact: `carrierForm` [[1,0],[0,1]] positive-definite vs
  `impostorForm` [[1,2],[2,1]] — same diagonal, determinant −3, not a carrier (the Epstein/Golay
  agreement-plus-divergence shape at the finite instance);
- **`composition_barrier`: no transport-sound composition of criterion-layer data derives the
  carrier property — even at a point where the property holds.**

Build green (2993 jobs); all five terminals **axiom-free**; 0 sorry, 0 native_decide. Charter §5:
statement-reads at source (the QWantedPoster clauses at the landed pin); E0 verbatim above the
commit. ANTI-OVERCLAIM on the module's face: model-level schema at the minimal witness world; no
ζ-claim beyond the shape; the k-ladder (simplicity as the k = 3 suspect) is the open upper floor.

**Outcome: Face-E's sibling — the second certified explanation of WHY every classical route
stalls at the edge.** Face-E: no Euler-product-free derivation places the zeros (the toolkit
barrier). E-8: no composition of one-variable positivities builds the two-variable form (the
layer barrier). One wall, two certified faces — method-class and data-arity.

## The slate, re-printed

E-1 RUN+v2 · E-2 armed (~1.5) · E-3 standing (rr) · E-4 armed (~1) · E-5 armed (~0.4) · E-6
RULED-IN · E-7a RUN / E-7b standing (rr) · **E-8 RUN — the barrier compiled, HELD at `14720d9`;
its upper floor (the k-ladder) is the successor item** · E-9 RUN (v2 live) · E-10 RUN (Face-E
companion filed) · E-11 armed (~1.0) · E-12 priced-on-motivation · E-13 COMPLETE (40/6,311) ·
carryovers standing.

## Pin table

| repo | pin |
|:--|:--|
| SIDE-lv-conservation | main = `4706a20`; held branch `composition-barrier` = `14720d9` (pushed, held) |
| PLACE-papers | `8ff9dd7` (keystone v0.4.1 + FINDINGS/OPEN_TRAILS repairs + loom) |
| SIDE-kernel | `44895f9`; v1.7 = `2957e7d` — unmoved |
| relay | this report's commit (+ the two repaired reports) |
| rail | post-rename baseline held |

Mirror rebuilt at the papers pin; nothing deposits. **The board, E-8's verdict at its head: the
wall now has two certified explanations — the method-class barrier (Face-E) and the data-arity
barrier (E-8) — and the k-ladder above them is the sharpest open rung.**
