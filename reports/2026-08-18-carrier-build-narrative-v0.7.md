# THE CARRIER BUILD — THE BUILD SHEET

**Method register · ### `v0.7` · 2026-08-13, extended 2026-08-17/18 (the `§0` review screen; ### `§12` — the corpus's own light: the compiled surround at statement vs the six columns) · `W-CARRIER-BUILD` acts 1–2 + `W-ROAD-A` `A1`–`A2` + the `(v)`-at-depth redo + ### the conformal column (`B12`) · QUESTION-FIT · NO MECHANISM PROPOSED**

> ### **`v0.5` ADDS `§11` — COLUMN `(vi)`, CONFORMAL STRUCTURE — FROM THE `B12` DEPTH ANALYSIS OF
> DENINGER'S SIGN MECHANISM, WITH THE `W-2` GUARD RIDING IT.** *The `§10` cross-link is upgraded to its
> content grade. `§§1–9` unchanged.*

> ### **`v0.4` ADDS `§9` — THE `(v)` QUESTION REDONE AT PROOF-SEARCH DEPTH, AUTHOR-ROUTED TO FABLE — AND IT
> CARRIES CORRECTION TWENTY-THREE.** *The banked Hecke-side recombination identity was WRONG at `h > 1`
> (`§9.1`; falsified longhand at the witness's own field), `§8.3`'s sum-vs-product table is corrected
> strike-visible, and the corrected picture is STRONGER for the operation-like reading, not weaker.* ###
> **VERDICT: `(v)` COLLAPSES TO `(i′)` — the monoid input carried AT CLASS RESOLUTION — PLUS A RIDER ON
> `(iv)`, AND THE RIDER IS THE RESIDUE THAT DOES NOT COLLAPSE.** *The three frames' `(v)` column is
> re-filled under the operation question at `§9.4`. `§§1–8` unchanged except the two strike-visible
> corrections.*

> ### **`v0.3` ADDS `§§6–7` AND ONE CLAUSE `(v)` TO `§2`'s QUESTION. `§§1–5` ARE UNCHANGED.** *`§6` seats the two-decompositions constraint as a graded requirement; `§7` records that the self-product read this pass ordered was already banked on 2026-08-09, with its `PRESENT`/`ABSENT` quoted and its sourcing declared.* ### **No frame is re-graded. The arrest does not move; it narrows.**

> ### **`v0.2` — THE FIRST MOVE RAN AND RETURNED A THIRD OUTCOME. NEITHER PRE-COMMITTED BRANCH LANDED.**
> *`1807.06400` upgraded to content, string-level. **0 occurrences** of `involution` · `anti-automorphism` ·
> `time reversal` · `reversal` · `duality` · `functional equation` · `Hermitian` · `polarization` ·
> `conjugation` · `bilinear` · `inner product`. **1 occurrence** of `pairing` — and it is **heterogeneous
> evaluation**, not a form.* ### **The frame's arrest is not a missing form. It is a MISSING SELF-PRODUCT —
> the corpus's own standing absence, reached a third time from a third side.** *Full adjudication:
> `relay/reports/2026-08-13-carrier-build-act2.md`. `§4`'s move is **closed, not retried**.*

> ### **THE GATE, ON THE DOCUMENT'S FACE. This sheet is `INSTRUMENT` track: assembling the constraints and
> naming a construction move is chartered. ### DERIVING THE SIGN IS NOT, AND STAYS BEHIND THE `ATTEMPT`
> GATE, WHICH IS AUTHOR-OPENED ONLY AND IS NOT OPEN.** *A build sheet is a list of what an object would have
> to satisfy. **It is not a construction, and a construction would not be a proof.***

> ### **AND THE CONSTRAINT THAT GOVERNS THE WHOLE SHEET, FROM `W-WITNESS-LATTICE`'s CLOSE: THERE IS NO
> POSITIVE CONTROL. The build cannot be tuned against examples — over `ℚ` the calibrating object would be a
> counterexample to RH; off `ℚ` there is no line.** ### **EVERY ROW MUST THEREFORE BE FORCED BY A
> SPECIFICATION, NEVER FITTED TO A CASE.** *This is the sharpest methodological difference between this
> build and any search.*

---

## §0 — ### **WHAT THIS DOCUMENT HOLDS, AT GRADE** *(the one-screen review, 2026-08-18; written for external peer review)*

**The object under specification:** `C1` — a CARRIER over `ℚ`. **The build question, current form:
what object has (i) the monoid as content source AT CLASS RESOLUTION (`(i′)`, the `(v)`-collapse) ·
(ii) the involution as structure · (iii) the full roster · (iv) a Hermitian pairing born from
`(i)×(ii)`, diagonal quotiented, NOT augmentation-silent (the rider) · (vi) a CONFORMAL pairing with
factor exactly `e^t` on its leaves — with `Definite`/`Nondegenerate` forever predicates, never fields
(the `W-2` trap).** **The plain sentence: nothing here is a construction, none of it moves `h2`, and all
of it specifies the object `h2` would rest on.**

**The frames, at holdings' depth** *(the full grids: `§3`, `§9.4`, `§11.3`)*:

| | (i)/(i′) | (ii) involution | (iii) roster | (iv) pairing | (vi) conformal |
|:--|:--|:--|:--|:--|:--|
| **THH / derived** | partial / barred | ✗ | ✗ | ✗ | ✗ |
| **Deninger** | ✓ seat (resolution ungraded) | ✗ | partial | ✗ (`Δ2`) | ### **design-target; object unbuilt; `§5.8` proved** |
| **CCM** | partial / ✓ input | partial | ✓ | ✓-but-degenerate (Prop 6.4) | ✗ in holdings |
| ### **`Ẑ` (class-resolved)** | ### **✓ native** | ### **✓ antipode-native** | finite places + `∞`-completion | ✗ (no pairing chosen) | ### **✗ — NO LEAVES; both polar shadows carried** |

**What the acts landed and refuted, one line each:** act 1 the sheet + grading (involution column empty
across the field's three frames) · act 2 the `1807.06400` string-read: the arrest is a MISSING
SELF-PRODUCT · `(v)` at depth: collapses to `(i′)` + the rider; correction twenty-three (the Hecke
recombination lands at `ζ_H`) · the seed `Z(s)` seated and graded (a content vehicle, not a carrier) ·
`§10`: Roads A/B touch at the seat (content grade) · `§11`: column `(vi)` — conformality's polar parts
ARE the involution (skew) and the weight (modulus); strictly stronger; guard riding.

**Held / open, at their addresses:** the two `HELD` carrier acts (on counsel) · the `1807.06400` BODY
read · the Hermitian species register's `MY-SYNTHESIS` flag (uncleared) · the mono-collapse escape axis.

> ### **THE MISSING-CODOMAIN QUESTION (SELF-PRODUCT / FROBENIUS LIFT / LEAVES) ON THE FACE: WHAT SUPPLIES THE LEAVES — three roads, one absence: Road
> A's missing SELF-PRODUCT · `(vi)`'s missing LEAFWISE METRIC · the missing FROBENIUS LIFT (Deninger's
> proved case has it; `ℚ` does not). `B13`'s finding sits beside it as the local shadow: at each finite
> place the Sonin space IS a Fourier-dual tensor square, `Son = V₁ ⊗ W₁` — in miniature, no promotion.**

---

## §1 — THE CONSTRAINT SET, EVERY ROW AT ITS CITE

### 1.1 — The carrier specification's fields and its compiled no-gos

| # | requirement | source, at cite |
|:--|:--|:--|
| **R1** | **cancellativity is the door predicate** | `SIDE-carrier-spec`, `arXiv:1502.05580` Lemma 2.3 (*"simplifiable"*) |
| **R2** | **signed classes** (`neg`, `add_neg`) | the symmetrization requirement |
| **R3** | **`ℤ` at finite places, `ℝ` at `∞`, fused in one object — and `arch_independent`: the archimedean coordinate must carry information the finite ones do not** | `arXiv:2205.01391` (Arakelov divisors); independence forced by the first grading |
| **R4** | **the action is by ENDOMORPHISMS, not translations** | `arXiv:2602.15941` (*"a decisive departure"*) |
| **Δ2** | ### **a biadditive `pair` into an ordered target — THE SOLE GEOMETRIC ABSENCE** | *"a bilinear pairing target — the frame yields an integration domain and a measure, **never a form**"*; `pair` FAILS at the named field |
| **Δ4** | **arithmetic content — the Frobenius lift `ψ_p(x) ≡ x^p (mod p)`** | Borger, `arXiv:0906.3146`, verbatim; `multiplication_action_fails_R4` **compiled** |
| **NO-GO** | ### **`no_idempotent_candidate` — COMPILED.** *`R1` forces an idempotent candidate to the zero object, which cannot supply `R3`. **The whole characteristic-one / tropical family is graded out.*** | `SIDE-carrier-spec`, `{propext, Classical.choice, Quot.sound}` |
| **NO-GO** | ### **THE MONO-COLLAPSE LAW.** *`X ×_B X` degenerates to `X` **iff** `X → B` is mono — three graves as instances (Deitmar's trivial descent · initiality · Durov's own proof, quoted).* ### **Constraint `(iv′)`: THE BASE MUST BREAK THE MONO.** *Escape axis recorded: mono-ness is `π₀`-level; higher structure is ungoverned by it.* | `OPEN_TRAILS`, `W-ORD` item 1 |

> ### **THE `W-2` TRAP RIDES EVERY ROW AND IS NOT NEGOTIABLE.** *`Definite`, `Nondegenerate`, and the `tied`
> half stay **predicates on candidates**, never fields.* ### **"Making definiteness a field would encode the
> conclusion. A positive pairing is what the clause needs PROVED; a specification that demands it as an
> input grades every candidate as a success."** *Four sharpenings so far, and the same degenerate witness
> passes every one.*

### 1.2 — The species register, at Weil-1952 content

> ### **THE TERMINAL DELTA, quoted:** *"the classical Riemann-form mechanism is **STRUCTURALLY UNAVAILABLE**
> — a real skeleton where it needs a complex one, and **`C5` independently forbids the real structure** the
> geometric side would want. **What the constraints jointly specify is a NEW SPECIES: a HERMITIAN
> polarization — with `i`, without conjugation — on the enlarged, infinite-dimensional object."***
>
> ### ⚠ **CARRIED WITH ITS OWN FLAG ON: `MY-SYNTHESIS`.** *The corpus states plainly that `E1`/`E2` are not
> at cite and the delta's two halves are joined by an unsourced classical link.* **The flag travels onto this
> sheet unchanged; nothing here upgrades it.**

* **The Hermitian route's order-two datum, at question grade:** *"**conjugation itself as the order-two datum** — the metric route **manufactures** reality, the zeros **forbid** it, so an arithmetic pairing must be **Hermitian-type on an un-conjugatable space**."* — the `ℤ/2` pattern's fifth instance, ### **`KIN`-UNTIL-ADJUDICATED, NO PROMOTION**
* **Level discipline, compiled into the spec:** ### **every graded structure declares its level, and cross-level inheritance does not carry.** *A field stated of the class object is not stateable of a space over it without a **`Descent`** — and `descent_not_unique` compiles the lesson: two descents of the same form give different class pairings, so **"the form descends" is a choice, not a property of the form**.*
* **`descent_carries_no_warrant`** (axiom-free): *a descent whose realization cannot separate two classes yields a pairing that carries no warrant about them, **whatever the form is*** *(gloss re-phrased to content 2026-08-17, correction twenty-four's closing; the identifier is the content phrase and is untouched)*.

### 1.3 — The seat

| | at cite |
|:--|:--|
| **the object** | Deninger, *Dynamical systems for arithmetic schemes*, **`arXiv:1807.06400`** (ID verified at its own abs page): *"we use **sheafified rational Witt vectors** to attach a new ringed space `W_rat(X)` … using `W_rat(X)(ℂ)` we construct **infinite dimensional `ℝ`-dynamical systems whose periodic orbits are related to the closed points of `X`**"* |
| **the flow** | ### **`ℝ`-dynamical — a genuine non-compact real action** |
| **orbits ↔ primes** | ### **"periodic orbits are related to the closed points of `X`"** |
| **grade** | ### **SEAT IS NATIVE, `MATCH`-GRADE** (`W-ORD-SEAT` act 1) — **and declared `AT-CITE-AT-ABSTRACT`, full text NOT read.** *Corroborated AT CONTENT by Morishita §3.* |
| **the absence** | ### **`Δ2`. The seat supplies a flow and orbits; it supplies no form.** |

### 1.4 — `W-WITNESS-LATTICE`'s terminal findings, as build constraints

| # | finding | what it forces on the build |
|:--|:--|:--|
| **L1** | ### **`S3b` MANUFACTURES THE LINE** — `E_k` has `S4` and no zeros; Beurling has `S4` + continuation and zeros with **no line to be off** | ### **THE CARRIER MUST CARRY THE INVOLUTION.** *An object with content and no involution has nothing for placement to be a question about.* |
| **L2** | ### **THE MONOID FINDING** — `Z_Q` at `h>1` has no generating monoid at all; `𝒪_K` has `h>1` in both rows and does not track placement | ### **CONTENT ENTERS THROUGH THE IDEAL / PRIME MONOID, NOT THE AMBIENT RING.** *A build that sources content from the ring is sourcing it from the coordinate that provably does not track placement.* |
| **L3** | ### **NO CALIBRATION** — both exits shut | ### **THE BUILD MUST BE FORCED BY SPECS.** *`h2` is untestable against any object; a "promising" candidate cannot be scored.* |
| **L4** | **`S4` ⟺ an Euler product exists**; `S4`-isolated reaches **exactly the `σ = 1` edge** (B3 + DMV, two routes) | ### **CONTENT ALONE BUYS THE EDGE. The build's novelty must live in the `S3b × S4` interaction, not in richer content.** |

### 1.5 — CCM's quotient move, and the open slot it meets

* ### **THE CARRIER IS WHAT SURVIVES THE DIAGONAL.** *The quotient move is the field's own construction step.*
* ### **AND ITS PAIRING PROVABLY DEGENERATES:** *"the obstruction named is **Proposition 6.4** itself: **the pairing vanishes on `V`** and can be made **arbitrarily small** by adding elements of `V`."*
* ### **`KIN`, AND SHARPLY:** *"the corpus's own carrier specification carries **`Nondegenerate` AS AN OPEN SLOT**, deliberately unfilled — and the field's carrier has a **PROVEN VANISHING SUBSPACE**. `witness_degenerate` and CCM's Prop 6.4 are **the same shape of fact about two different objects: a pairing that exists and degenerates**."* ### **`KIN`-UNTIL-ADJUDICATED. NO PROMOTION. No text connects them; the match is structural and was not sought.**

### 1.6 — The measured boundary conditions

| row | measurement | standing |
|:--|:--|:--|
| ### **THIN MARGIN** | the **three-witness knife-edge**: Rodgers–Tao `Λ ≥ 0` (at cite) · CCM's **large radical** (at cite) · the atlas's **hover at `O(10⁻⁴)`** (measured) | ### **the wanted supplier must be KNIFE-EDGE** — zero margin excludes any everywhere-mechanism |
| ### **RESPONSE STRUCTURE** | *local mechanisms must reproduce the response structure* | ### **carries the reach theorem beside it — "the row constrains TECHNIQUES and is not evidence about the Hypothesis, by a deposited result and not by house style"** |

### 1.7 — The hard wall

> ### **`C5`: NO REAL FORM ON `H¹`. The build may not reach for the classical real polarization — it is
> forbidden, not merely unavailable.** *This is what forces the Hermitian species rather than selecting it
> for elegance.*

---

## §2 — THE BUILD QUESTION, STATED AS CONSTRUCTION

> ### **WHAT OBJECT HAS: (i) THE MONOID AS ITS CONTENT SOURCE · (ii) THE INVOLUTION AS STRUCTURE · (iii) THE
> FULL ROSTER · (iv) A HERMITIAN PAIRING BORN FROM `(i) × (ii)`, WITH THE DIAGONAL QUOTIENTED?**
>
> ### **· (v) — ADDED 2026-08-17 — AND SEES THE MONOID-RESPECTING ~~BASIS~~ PRESENTATION?**
>
> ### **RE-STATED 2026-08-17 BY THE DEPTH REDO (`§9`, correction twenty-three riding):** *`(v)` is not a
> fifth independent demand. It is* ### **`(i′)`: the content enters through the monoid AT CLASS RESOLUTION —
> through the class-group algebra `ℂ[Cl]`, not through its augmentation — multiplicative recombination
> native, additive presentation derived.** *Plus the rider on `(iv)` that is the non-collapsing residue:*
> ### **the manufactured pairing must not be augmentation-silent** — *its value constant across the class
> resolution, reading only `ε(Z) = ζ_K`* — *(kin: `descent_carries_no_warrant`, compiled; term repaired
> from "augmentation-blind" by correction twenty-four, 2026-08-17).*

**Read as construction, not search: `(iv)` says the pairing is not to be IMPORTED and then tested. It is to
be MANUFACTURED by the interaction of the content source and the involution** — which is exactly the
`S3b × S4` interaction the lattice's convergence named, built rather than described.

> ### **AND THE STOP RIDES THE QUESTION: an object satisfying `(i)`–`(iv)` would be a CARRIER, not a proof.
> Definiteness stays a predicate on candidates. Nothing in `§2` asks for a sign.**

---

## §3 — THE THREE FRAMES, GRADED AT HOLDINGS' DEPTH

| | **(i) monoid content** | **(ii) involution** | **(iii) roster** | **(iv) pairing from (i)×(ii)** | ### **NAMED ARREST** |
|:--|:-:|:-:|:-:|:-:|:--|
| ### **THH / derived** | partial | ✗ | ✗ | ✗ | ### **THE CIRCLE'S ARITHMETIC MEANING IS UNASSIGNED.** *"Is the `T`-action on `THH(Spec ℤ)` the archimedean slot — or **a circle awaiting an arithmetic meaning nobody has given it over `ℤ`**?"* **Plus a hard level bar: `THH`, `TP`, homotopy fixed points live one level up; `Γ_ℝ` and Euler factors are classical functions; NO CELL INHERITS ACROSS.** |
| ### **DENINGER'S SYSTEM** | ### **✓ — orbits ↔ closed points** | ✗ | partial | ### **✗** | ### **`Δ2` — NO FORM.** *The frame yields a space, a flow, and orbits; it yields no pairing. His own `2204.02714` no-go sits beside it: **no `H¹` with the zeros' spectrum is claimed.*** |
| ### **CCM'S CYCLIC-MODULE CARRIER** | partial | partial | ✓ | ### **✓ BUT DEGENERATE** | ### **SILENCE ON `C5`, AND A PROVEN VANISHING SUBSPACE** (Prop. 6.4). *The pairing exists and is not nondegenerate.* |

### **THE GRADING'S VERDICT**

> ### **NO FRAME CARRIES THE INVOLUTION AS STRUCTURE. That column is empty across all three, and it is the
> column `L1` says the carrier must have.**
>
> ### **THE TWO NON-EMPTY COLUMNS SIT IN DIFFERENT FRAMES: Deninger has the content source natively
> (`MATCH`-grade seat, orbits at the primes); CCM has a pairing and the quotient move. NEITHER HAS BOTH, AND
> THE MISSING PIECE IS THE SAME ONE IN BOTH CASES.**
>
> **Best-graded on the sheet: DENINGER — because `(i)` is the column that cannot be retrofitted (`L2`: content
> must enter through the monoid), while `(iv)` is the column the question asks to be MANUFACTURED rather than
> possessed.** ### **CCM's pairing, being imported and degenerate, scores its column and does not answer it.**

---

## §4 — THE FIRST NEW CONSTRUCTION MOVE, NAMED AND **NOT EXECUTED**

> ### **THE MOVE: on Deninger's `W_rat` system — whose periodic orbits already sit at the primes — ask
> whether the FLOW'S OWN TIME-REVERSAL `t ↦ −t` is the carrier of the involution, and whether the pairing
> `Δ2` can be BORN from `(orbit monoid) × (time-reversal)` rather than imported.**

**Why this is the smallest modification of the best-graded frame.** *It adds no new object. It asks whether a
structure the frame already has — a real flow has a reversal — is the `S3b` datum the sheet requires, and
whether `(i) × (ii)` then manufactures `(iv)` on the spot.* ### **Nothing is imported; the empty column is
asked to be filled by the frame's own furniture.**

### **BOTH BRANCHES, LONGHAND, BEFORE ANYTHING RUNS**

> ### **BRANCH A — THE MOVE LANDS.** *Time-reversal on the `ℝ`-flow is a genuine carrier of the
> FE-involution; the pairing induced by orbits against their reversals is **Hermitian-type — with `i`,
> without conjugation** — matching the species register's terminal delta; `Δ2` is supplied **natively rather
> than imported**, and the carrier CARRIES the involution as `L1` demands.* **Consequence if it lands: the
> sheet gains its first frame with a non-empty involution column.** ### **IT WOULD STILL SUPPLY NO SIGN —
> `Definite` and `Nondegenerate` remain open predicates, and CCM's Prop. 6.4 stands as the warning that a
> pairing can exist and degenerate.**
>
> ### **BRANCH B — THE MOVE FAILS, AND THE FAILURE IS FIRST-CLASS.** *Time-reversal is **not** the
> FE-involution but a different order-two datum — **the `ℤ/2` pattern's next instance, `KIN`-until-adjudicated,
> and it must be filed as such and not merged** — and the form it induces is **symmetric-real** rather than
> Hermitian.* ### **Then `C5`'s no-real-form wall kills it at exactly the point where the classical
> Riemann-form mechanism was already declared STRUCTURALLY UNAVAILABLE — the same wall, reached by a new
> road.** **That is a real result about the frame and is to be filed first-class, not retried.**

### **THE PRE-COMMITMENTS, WRITTEN NOW**

1. ### **NAME-IDENTITY ARMED ON THE ORDER-TWO DATUM.** *Time-reversal, conjugation, the FE-reflection, and the Rosati involution are **four** order-two data. **The corpus's `E2` is REFUTED and concerns Rosati on `End(J)`;** nothing here may merge with it.*
2. ### **LEVEL DECLARED BEFORE GRADING.** *`W_rat(X)(ℂ)` and its flow are not the class object. Any field claimed there requires a **declared `Descent`**, and `descent_not_unique` says the descent is a choice.*
3. ### **`MY-SYNTHESIS` FLAG INHERITED.** *The Hermitian-species target carries the corpus's own flag; a move aimed at it inherits the flag and does not discharge it.*
4. ### **THE MOVE IS A QUESTION.** *It is not executed this act, and executing it is `INSTRUMENT`-track construction — **not** an `ATTEMPT`, which stays author-opened and closed.*

---

## §5 — THE FLOW ITEMS, SEATED

**Per the author's naming, the two flow items are:**

| | item | standing |
|:--|:--|:--|
| **1** | ### **`W-FLOW-METER`** | **full hunt: ARMED, UNTOUCHED.** *The `BUILD-2` `k(t)` bracket is the `ζ`-pencil's meter; the measured margin curves are pencil coordinates.* |
| **2** | ### **THE dBN FLOW BRACKET** | **template-complete, unconditional ceiling: Rodgers–Tao.** ### **BOTH the Beurling–Nyman distance and the dBN flow are ZERO-MARGIN**, converging on the extremal-selection vocabulary's pinning. |

> ### **THE dBN FLOW IS FILED ON THIS SHEET FOR ONE REASON: IT IS THE ONE SETTING WHERE PLACEMENT PROVABLY
> MOVES.** *`Λ ≥ 0` at cite, with `Λ ≥ 0 ⟹ RH`-iff-boundary and **zero margin**. Everywhere else on this
> sheet placement is static and the question is what holds it; there, it is a flow parameter and the zeros
> demonstrably travel.* ### **ADJACENT INSTRUMENT. IT IS NOT A CONSTRUCTION INPUT.**

> ### **NAME-IDENTITY ARMED — TWO FLOWS, NO MERGE.** *The seat's **scaling flow** (Deninger's `ℝ`-action on
> `W_rat(X)(ℂ)`, orbits at the primes) and the **dBN flow** (Rodgers–Tao's heat-flow deformation of `ξ` in
> `t`) are **different objects with different parameters**.* ### **They must not be merged, and "the flow"
> unqualified is not a usable term on this sheet.**

### The two PROSE rulings, still awaiting the author — **re-listed separately, and NOT flow items**

| | ruling | state |
|:--|:--|:--|
| **P1** | **the six-counts opening** | ### **AWAITING THE AUTHOR.** *Named by the ferry; **did not resolve to a corpus locus in this act's sweep** and is carried on the author's word, not on a citation.* |
| **P2** | **caveat placement** | ### **AWAITING THE AUTHOR.** *Same standing.* |

---

---

## §6 — ### **`(v)`: THE OBJECT MUST SEE THE MONOID-RESPECTING BASIS** *(`A1`, added 2026-08-17)*

### **THE CONSTRAINT, AND IT COMES FROM A WITNESS AND NOT FROM A PREFERENCE**

*The two-decompositions experiment* (`THE_EULER_SPECIFICATION` addendum, 2026-08-14) *holds ONE object fixed
and varies only how it is decomposed:*

| | **BASIS A — Epstein / forms** | **BASIS B — Hecke / characters** |
|:--|:--|:--|
| the object | `ζ_K`, `K = ℚ(√−23)`, `h = 3` — ### **the same field, the same `𝒪_K`, the same discriminant** | the same |
| the pieces | the `h = 3` reduced binary quadratic forms, each an Epstein `Z_Q` | the `h = 3` class-group characters, each a Hecke `L(s,ψ)` |
| `S1b` (monoid) | ### **`VACUOUS`** — a single ideal class is not multiplicatively closed | ### **PRESENT** — the prime ideals generate |
| placement, at cite | ### **OFF-LINE ZEROS KNOWN** | ### **NO KNOWN OFF-LINE ZEROS; GRH OPEN** |

> ### **THE PATHOLOGY LIVES EXACTLY IN THE MONOID-BREAKING BASIS, AND ONLY THE DECOMPOSITION VARIES.**

### **THEREFORE, AS A GRADED REQUIREMENT BESIDE `(i)`–`(iv)`:**

> ### **`(v)` A CARRIER MUST NOT BE INVARIANT UNDER RE-DECOMPOSITION OF ITS OBJECT.** *An object that
> assigns the same structure to basis A and basis B is* ### **one whose frame has taken the object, not its
> presentation — invariant in the one variable the witness shows placement tracking** *(re-phrased to
> content 2026-08-17, correction twenty-four's closing)*, *and therefore cannot carry a statement about
> placement.*

### **`(v)` ASKED OF ALL THREE FRAMES, 2026-08-17 — ### AND THE ANSWER IS UNIFORM, WITH ONE REASON**

*Corpus-first, case-insensitive probes with second-mode confirmation, per the extended reference-scan rule.*

| frame | `(v)` | what the read found |
|:--|:--|:--|
| **`THH` / derived** | ### **ABSENT** | *no crossing of `THH` with Hecke, character, class group, basis or decomposition anywhere in the corpus.* **And a hard structural bar already on the sheet:** *"`THH`, `TP`, homotopy fixed points live one level up; `Γ_ℝ` and Euler factors are classical functions;* ### **NO CELL INHERITS ACROSS.**" *A frame that cannot reach classical Euler factors cannot distinguish two decompositions built from them.* |
| **DENINGER'S SYSTEM** | ### **ABSENT — and partly UNASKABLE from what is held** | *no crossing hits. And the sourcing is already declared in `FINDINGS`:* ### ***"DENINGER IS NOT QUOTED HERE, BECAUSE HIS TEXT WAS NEVER FETCHED"*** *— the postulated `H¹` is the corpus's OWN characterization in the monograph's voice.* **`1807.06400` and `2204.02714` are held at content; the `H¹`/arithmetic-site material is not.** *So `(v)` is `ABSENT` on what is held, with the read-debt named rather than graded.* |
| **CCM'S CYCLIC-MODULE CARRIER** | ### **ABSENT** | *the frame's own quoted input is* ### **"the ideles class group of a global field"** *— the FIELD, not a decomposition of it. Nothing in the construction takes a presentation as data.* |

### **AND THE REASON IS THE SAME ONE THREE TIMES, WHICH IS WORTH MORE THAN THREE SEPARATE ABSENCES**

> ### **NONE OF THE THREE FRAMES TAKES A DECOMPOSITION AS INPUT.** *`THH`/derived takes `Spec ℤ`; Deninger's
> takes a dynamical system attached to the arithmetic scheme; CCM's takes the adele class space of the
> field.* **All three take the OBJECT. The two-decompositions witness varies the PRESENTATION at fixed
> object.**
>
> ### **SO THE QUESTION CANNOT EVEN BE POSED TO ANY OF THEM — AND THE FRAME THAT WOULD LET IT BE POSED IS
> NONE OF THEM.** *A second-mode probe for any frame taking a basis, presentation or decomposition as data
> returns nothing but this sheet's own text.*

### **WHICH TURNS `(v)` INTO A STATEMENT ABOUT THE FRAME'S INPUT TYPE**

> ### **A FRAME SATISFYING `(v)` MUST TAKE THE PRESENTATION AS DATA, NOT ONLY THE OBJECT.** *That is a
> sharper requirement than "assigns different structure to two bases", because it says WHERE the difference
> would have to enter: at the frame's input, before any structure is assigned.*
>
> **Filed at question grade, flagged `OURS`, and marked as the sitting's proof-search residue** — *the reads
> are solid and corpus-first; the input-type reformulation is the part that would benefit from being redone
> at depth.*

### **AND THE QUESTION `(v)` FORCES, STATED AS THE SHEET'S NEW OPEN ITEM**

> ### **WHAT STRUCTURE ON `Spec ℤ × Spec ℤ` — OR ON WHATEVER STANDS IN FOR IT — DISTINGUISHES THE HECKE
> BASIS FROM THE EPSTEIN BASIS OF THE SAME OBJECT?**
>
> *~~Both bases recombine to the same `ζ_K`~~ **[corrected 2026-08-17, correction twenty-three, `§9.1`: the
> form family SUMS to `w·ζ_K`; the character family MULTIPLIES to `ζ_H`, and contains `ζ_K` as its `ψ₀`
> member — the two families are Fourier transforms of each other on `Cl`]**; both carry continuation and
> functional equation; both sit over the same roster.* ### **They differ in `S1b`, and `S1b` is a statement about a MONOID, which is `(i)`'s
> column.** *So `(v)` is not a fifth independent demand so much as the demand that `(i)` be carried far
> enough to be visible in the pairing —* **content that enters through the monoid must still be
> distinguishable after the pairing is formed.**
>
> ### **NO CANDIDATE STRUCTURE IS PROPOSED HERE. The question is filed as the constraint's consequence and
> is the sheet's, not a mechanism.**

---

## §7 — ### **`A2`: THE SELF-PRODUCT READ — AND IT WAS ALREADY BANKED** *(2026-08-17)*

> ### **THE READ THIS FERRY ORDERS WAS RUN ON 2026-08-09 AND ITS RESULT IS IN `OPEN_TRAILS`.** *The
> corpus-first sweep found it before any fetch was attempted, which is the standing law firing again:*
> ### ***possession precedes recognition — before fetching, grep.***

### **WHAT IS PRESENT, QUOTED**

| | |
|:--|:--|
| ### **A SELF-PRODUCT EXISTS** | *"realize the Frobenius correspondences in the **square** of the 'Arithmetic Site'"* · *"This square is a **semi-ringed topos** whose structure sheaf involves Newton polygons"* |
| **and the trace formula is delivered on it** | the complete Riemann zeta is obtained there as a Hasse–Weil zeta |
| ### **THE RR / JACOBIAN LINE DELIVERS MORE THAN WAS REGISTERED** | *"the **cohomologies** (attached to a divisor), their **integer dimension**, and **Serre duality**, which directly extend their classical counterparts for function fields"*, plus a monoidal extension of the **Picard group** of the arithmetic curve |

### **WHAT IS ABSENT, AND THE ABSENCE IS THE FIELD'S OWN SENTENCE**

| | |
|:--|:--|
| ### **THE CODOMAIN** | *"The main open question is the definition of a **suitable Weil cohomology** which would allow one to understand the above equality as a **Lefschetz formula**."* |
| ### **THE RR LINE ON THE SQUARE** | ### **all of it lives on the arithmetic CURVE `Spec ℤ`, NOT on the SQUARE.** *"A cohomology on the curve does not supply the square's `H²`."* |
| **an intersection theory on the square** | ### **not established** — the 2026-08-09 act moved the surface row from `BUILD` to `CHECK-AND-VERIFY` precisely because *a* self-product exists and **what must be checked is whether it carries an intersection theory** |

> ### **THE READ'S OWN VERDICT, VERBATIM: "CONFIRMED IN LETTER, REFUTED IN EFFECT."** *A real structure, on
> the wrong-dimensional object — and the act recorded that this was the* ### **second consecutive sitting**
> *in which an expectation of that shape failed the same way: Arakelov, a surface for a curve over a number
> field; Connes–Consani RR, a cohomology for the curve `Spec ℤ`.* **The absence is specifically AT THE
> SELF-PRODUCT.**

### **THE SOURCING, DECLARED RATHER THAN SMOOTHED**

* ### **THE ARREST STATEMENT IS `ATTRIBUTED`, NOT READ IN THE PAPER BODY.** *The act's own note: "neither
  fetched abstract contained the arrest statement; the full PDF returned `HTTP 403`; the arrest was located
  by search and is recorded as ATTRIBUTED, not as read in the paper body."*
* ### **`1405.4527` AND `1502.05580` ARE `TITLE-UNVERIFIED` IN THE `BIBLIOGRAPHY`.** *Their strings are
  quoted in the corpus; their titles are not established at content here, and the name-identity law forbids
  citing them by title until they are.*
* ### **THE SCALING-SITE LINE IS HELD AT CITE — AND THE FIRST SWEEP FOR IT PRODUCED A FALSE NEGATIVE,
  WHICH IS RECORDED RATHER THAN QUIETLY CORRECTED.** *A case-sensitive probe for* `scaling site` *returned
  nothing and would have put "NOT HELD" into this sheet; the case-insensitive re-run found it immediately.*
  ### **THE LOOM'S OWN LAW: a false negative in a verification instrument is worse than no instrument,
  since it invites a repair of something that is not broken.** *Second instance of the reference-scan rule's
  class, this time on letter-case rather than on file extensions.*

  **What is held:** Connes–Consani, *The Scaling Site*, `arXiv:1507.05818`, at cite —
  > *"The restriction of this structure to the **periodic orbits of the scaling flow** gives, for each prime
  > `p`, **an analogue of an elliptic curve whose "Jacobian is a cyclic group of order `p−1`"**."*
  > — **the scare quotes are the authors' own.**

  **And it is already graded, in the well table:** ### **the one candidate that has a "Jacobian" at cite has
  a FINITE one, and the well's ingredient is a HEIGHT THEORY, which is VACUOUS on a finite group.**
  *So the scaling site supplies per-prime periodic orbits with a finite Jacobian.* ### **It does not supply
  the square, and a height theory on it is vacuous — so it grades ABSENT on the self-product question and
  ABSENT on `(v)`, for two independent reasons.**

  ### **ONE CORPUS-INTERNAL DISCREPANCY, FLAGGED NOT RESOLVED:** *`1507.05818` carries a title here
  (`The Scaling Site`) while the `BIBLIOGRAPHY` lists it among the `TITLE-UNVERIFIED` entries.* **Either the
  title was established and the register is one wave behind, or it entered from a listing rather than from
  content.** *Named for act 2b; nothing here depends on which.*

### **THE ARREST, RE-STATED WITH `(v)` ATTACHED — AND IT NARROWS**

> **Before 2026-08-17 the arrest read:** *a self-product exists as a topos-theoretic object; what must be
> checked is whether it carries an intersection theory.*
>
> ### **IT NOW READS: A SELF-PRODUCT EXISTS; WHAT MUST BE CHECKED IS WHETHER IT CARRIES AN INTERSECTION
> THEORY THAT IS NOT INVARIANT UNDER RE-DECOMPOSITION OF ITS OBJECT.**
>
> *Because by `(v)`, an intersection form that assigns the same value to the Hecke and Epstein
> decompositions of one `ζ_K` cannot carry a statement about placement — and placement is what the pairing
> is wanted for.* ### **THE ARREST DID NOT MOVE. IT GOT NARROWER, WHICH IS THE ONLY KIND OF PROGRESS THIS
> SHEET IS CHARTERED TO PRODUCE.**

**No frame is re-graded by `§§6–7`. No mechanism is proposed. `(v)` is `ABSENT` everywhere and the
self-product's arrest is unmoved.**

---

## §8 — ### **`(v)` AS A CONDITION ON THE INPUT CATEGORY — AND `(v)` MAY BE MISNAMED** *(2026-08-17)*

> ### **ROUTING DECLARED: this section is proof-search and the author's routing sends proof-search to Fable
> at high effort. It was written on Opus.** *The two candidate gradings are corpus-first and solid; the
> conclusion in `§8.3` is the residue and is marked as such.*

### `§8.1` — THE CONDITION, STATED EXACTLY

> ### **THE CARRIER TAKES A PRESENTATION — A DECOMPOSITION OF `ζ_K` INTO CONSTITUENT `L`-SERIES OR EPSTEIN
> FORMS — AS DATA, NOT THE OBJECT ALONE.**

**The question this forces:** *what category has DECOMPOSITIONS as objects and RE-DECOMPOSITIONS as
morphisms, such that the Hecke and Epstein presentations of one `ζ_K` are* ### **distinct objects with a
morphism between them?**

### `§8.2` — THE TWO CANDIDATES, GRADED

| | ### **A — Dirichlet series with the Euler structure as extra data** | ### **B — class-group ELEMENTS (Epstein) vs its CHARACTERS (Hecke)** |
|:--|:--|:--|
| objects | pairs `(D, φ)`: a series together with a specified decomposition | the `h = 3` reduced forms, at cite, against the `h = 3` characters `ψ` of `Cl(K) ≅ ℤ/3`, at cite |
| ### **"sees the basis"** | ### **NATIVE — BY FIAT** | ### **CONDITIONAL** — the two are distinct only if the category does not identify a finite abelian group with its dual |
| what else it carries | ### **NOTHING.** *No pairing, no involution, no roster.* | it touches `(ii)`: a group and its character group are related by a duality, which is order-two-shaped |
| the obstruction | ### **the triviality IS the objection.** *A category built so that presentations are distinct RECORDS the distinction; it does not derive it, and a carrier satisfying `(v)` by fiat satisfies nothing else.* | ### **`G` and `Ĝ` are isomorphic for finite abelian `G` — just NOT CANONICALLY. A functor invariant under isomorphism cannot separate them.** *And the corpus already holds a negative theorem in exactly this neighbourhood:* **element-level assignments are `forbidden-as-canonical` by the functorial-limit negative theorem, basis-relative under `GL(3,𝔽₂)`** *(`SEVEN_DISCRIMINANTS_AND_TRIVIUM` `§4`)* |

### `§8.3` — ### **AND THE THING THE GRADING TURNED UP, WHICH IS THAT `(v)` MAY BE MISNAMED**

**Candidate B forced a re-read of the witness, and the witness does not say what `(v)` says it says.**

| | Epstein side | Hecke side |
|:--|:--|:--|
| indexed by | the class group's ELEMENTS *(reduced forms)* | the class group's CHARACTERS |
| ### **recombination** | ### **`Σ_Q Z_Q(s) = w · ζ_K(s)` — a SUM** | ### ~~**`Π_ψ L(s,ψ) = ζ_K(s)` — a PRODUCT**~~ **CORRECTED 2026-08-17 (correction twenty-three, `§9.1`): `Π_ψ L(s,ψ) = ζ_H(s)`, the HILBERT CLASS FIELD's Dedekind zeta — a PRODUCT, landing at a LARGER object. `ζ_K` itself is the single `ψ₀`-factor.** |

> ### **THE TWO PRESENTATIONS DO NOT DIFFER BY A CHANGE OF BASIS. THEY DIFFER IN THE OPERATION BY WHICH THE
> PIECES RECOMBINE.** *One is additive, the other multiplicative, and no change of coordinates carries a sum
> to a product.*
>
> ### **SO `(v)`'s OWN WORDING — "sees the monoid-respecting BASIS" — IMPORTS A LINEAR-ALGEBRA PICTURE THE
> WITNESS DOES NOT SUPPORT.** *What varies is not how the pieces are indexed but* ### **how they multiply**
> *— which is `S1b`, which is the MONOID, which is column `(i)`.*
>
> ### **`(v)` THEREFORE COLLAPSES TOWARD `(i)`, WHICH IS WHAT `§6` ALREADY SUSPECTED WHEN IT WROTE THAT
> `(v)` "is not a fifth independent demand so much as the demand that `(i)` be carried far enough to be
> visible in the pairing."** *The grading did not add a column. It found that the column it was asked to
> fill is the first column, asked again downstream.*

**BOTH BRANCHES, LONGHAND, AND THE SECOND IS THE ONE THE READING LANDS ON:**

* ### **IF the distinction is BASIS-LIKE** *(a change of coordinates)*: candidate A captures it trivially,
  candidate B conditionally, and `(v)` is a real fifth demand about indexing.
* ### **IF the distinction is OPERATION-LIKE** *(sum vs product)*: `(v)` is not about a basis at all, no
  category of "decompositions with re-decompositions as morphisms" is the right home, and **the requirement
  is that the carrier see the MONOID — which was always `(i)`.**

> ### **NO CONSTRUCTION IS PROPOSED. NO CANDIDATE IS PROMOTED. `(v)` IS NOT WITHDRAWN — it is re-pointed, and
> the re-pointing is filed at question grade and flagged `OURS`, with the note that it was reached on Opus
> and is the part worth redoing at depth.**

## §9 — ### **THE `(v)` QUESTION REDONE AT DEPTH — THE FOURIER TRANSFORM ON `Cl`, THE CORRECTED IDENTITY, AND THE COLLAPSE TO `(i′)`** *(2026-08-17, author-ruled redo of `§8.3`; ### ROUTING: proof-search, run on FABLE at the author's routing)*

> ### **THE STANDING STOP RODE THE WHOLE SECTION AND DID NOT FIRE.** *Nothing below is a result about the
> sign of `W_∞ − W_2`; nothing below bears on `h2`. Every derivation here is bookkeeping on Dirichlet
> coefficients and finite-group characters, done longhand; every placement fact used is one the corpus
> already holds at cite.*

### `§9.1` — THE STATEMENT MADE EXACT — ### **AND THE BANKED IDENTITY IS WRONG, WHICH IS CORRECTION TWENTY-THREE**

**Setting, fixed by the witness:** `K = ℚ(√−23)`, `h = 3`, `w = 2`; `Cl = Cl(K)`, `Ĉl` its character
group; `ζ_C(s) = Σ_{𝔞∈C} N𝔞^{−s}` the partial zeta of the class `C`; `Z_Q = w·ζ_{C(Q)}` the Epstein
zeta of the form `Q` *(the form–class correspondence and `Σ_Q Z_Q = w·ζ_K` are the addendum's
`STANDARD-AT-TEXT` rows and are unchanged)*.

**The Fourier structure, derived longhand from orthogonality and nothing else:**

| | identity | proof |
|:--|:--|:--|
| **analysis** | `L(s,ψ) = Σ_C ψ(C)·ζ_C(s)` | group the ideal sum by class |
| **synthesis** | `ζ_C(s) = (1/h)·Σ_ψ ψ̄(C)·L(s,ψ)` | `Σ_ψ ψ̄(C)ψ(C′) = h·δ_{C,C′}` |

> ### **SO THE PASSAGE BETWEEN THE TWO PRESENTATIONS IS EXACTLY THE FINITE FOURIER TRANSFORM ON `Cl` — an
> invertible LINEAR map between the family `(ζ_C)_C` and the family `(L(s,ψ))_ψ`, term-by-term in the
> Dirichlet coefficients.** *The ferry's framing is confirmed as stated.*

### **BUT THE BANKED RECOMBINATION LINE FAILS AT DEPTH.** *The addendum and `§8.3` both carried
`Π_{ψ∈Ĉl} L(s,ψ) = ζ_K(s)`. **At `h > 1` this is FALSE, and the witness's own field falsifies it at the
second Dirichlet coefficient, longhand:***

* `−23 ≡ 1 (mod 8)`, so `2` splits: `(2) = 𝔭₂𝔭̄₂`, `N𝔭₂ = 2` — **two ideals of norm `2`, so the
  coefficient of `2^{−s}` in `ζ_K` is `2`.**
* the principal form `x² + xy + 6y²` does not represent `2` *(y = 0 forces `x² = 2`; `|y| ≥ 1` forces the
  value `≥ 6 − |x|·|y| + x² ≥ 5`)*, while `2x² + xy + 3y²` does *(x=1, y=0)* — so `[𝔭₂]` is
  **non-principal**, hence of order `3` in `Cl ≅ ℤ/3`.
* the coefficient of `2^{−s}` in `Π_ψ L(s,ψ)` is `Σ_ψ ψ(𝔭₂) + Σ_ψ ψ(𝔭̄₂) = (1 + ω + ω²)·2 = `
  ### **`0 ≠ 2`.**

**What the product actually equals, derived longhand:** `log Π_ψ L(s,ψ) = Σ_𝔭 Σ_k (Σ_ψ ψ([𝔭]^k))/(k·N𝔭^{ks})`,
and `Σ_ψ ψ(C) = h·δ_{C,C₀}`, so only the powers `k` with `[𝔭]^k` principal survive: a prime with
`f = ord([𝔭])` contributes `(h/f)·Σ_m N𝔭^{−fms}/m` — **the Euler factor `(1 − N𝔭^{−fs})^{−(h/f)}`.**
*Identifying that Dirichlet series with a Dedekind zeta uses the splitting law of the Hilbert class field
(`f` = order of Frobenius = order of the class, `g = h/f`), which is class field theory:* `NAVIGATOR-GRADE`,
*named not read.* ### **CORRECTION TWENTY-THREE, THEREFORE: `Π_{ψ∈Ĉl} L(s,ψ) = ζ_H(s)` — THE HILBERT CLASS
FIELD's DEDEKIND ZETA. The multiplicative recombination of the full character family lands at a LARGER
OBJECT than the additive recombination of the form family. `ζ_K` itself is the single `ψ₀`-factor.**
*Filed at `THE_ATTEMPT_RECORD`; the addendum's table and `§8.3`'s corrected strike-visible.*

### **AND THE CORRECTED PICTURE IS SHARPER THAN THE ONE IT REPLACES.** *The two presentations are not two
recombinations of one object by two operations. They are* ### **two BASES of ONE `ℂ[Cl]`-valued object,**
*and the two classical scalar identities are two different canonical maps out of it:*

> **Define** `Z(s) = Σ_𝔞 [𝔞]·N𝔞^{−s}`, **valued in the group algebra** `ℂ[Cl]`. *The class map is a monoid
> homomorphism (`[𝔞𝔟] = [𝔞][𝔟]`), so the coefficient function is completely multiplicative into a
> commutative algebra, and unique ideal factorization gives, longhand:*
> ### **`Z(s) = Π_𝔭 (1 − [𝔭]·N𝔭^{−s})^{−1}` — AN EULER PRODUCT OVER `ℂ[Cl]`** *(geometric series in the
> algebra; componentwise absolute convergence for `Re s > 1`).*
>
> * in the **group basis** `{[C]}`: `Z(s) = Σ_C ζ_C(s)·[C]` — the Epstein/additive presentation;
> * in the **idempotent basis** `e_ψ = (1/h)Σ_C ψ̄(C)[C]`: `Z(s) = Σ_ψ L(s,ψ)·e_ψ` — the Hecke/
>   multiplicative presentation *(the algebra is diagonal there, which is WHY each component carries its
>   own Euler product)*;
> * the **additive recombination** `Σ_C ζ_C = ζ_K` is the `ψ₀`-eigenvalue of `Z(s)` *(the augmentation
>   `ε: [C] ↦ 1`)*;
> * the **multiplicative recombination** `Π_ψ L(s,ψ) = ζ_H` is the **determinant** of multiplication-by-
>   `Z(s)` on `ℂ[Cl]` *(the regular representation's eigenvalues are the `L(s,ψ)`)*.

> ### **SO THE FERRY'S QUESTION — "IS THE PASSAGE FROM THE PRODUCT SIDE TO THE SUM SIDE THE MAP THAT
> DESTROYS MULTIPLICATIVITY?" — HAS AN EXACT ANSWER: NO. THE FOURIER TRANSFORM IS AN ALGEBRA ISOMORPHISM
> AND DESTROYS NOTHING. MULTIPLICATIVITY IS NOT DESTROYED — IT IS RELOCATED: diagonal (per-piece Euler
> products) in the idempotent basis, CONVOLUTIVE (among the pieces, `[C][C′] = [CC′]`) in the group basis.
> A single piece `ζ_C` is not multiplicative alone because a single coordinate of a convolution algebra is
> not a subalgebra.**
>
> ### **AND THE PLACEMENT FAILURE SITS EXACTLY AT THE FORFEIT THIS FORCES:** *each `L(s,ψ)` is an Euler
> product — a convergent product of nonzero factors on `Re s > 1`, hence nonvanishing there, longhand — while
> `ζ_C = (1/h)Σ_ψ ψ̄(C)L(s,ψ)` is a nontrivial linear combination of `h ≥ 2` DISTINCT Euler products, which
> is generically no Euler product and inherits no nonvanishing guarantee anywhere.* **That the forfeit is
> realized — off-line zeros in exactly these combinations — is the addendum's banked citation row
> (Davenport–Heilbronn 1936 · Voronin 1976 · the corpus's own located zero at disc `−23`) and no new
> placement fact is claimed here.**

### `§9.2` — THE CLAIM TESTED: ### **"`(v)` COLLAPSES TO `(i)`" — BOTH BRANCHES LONGHAND**

**FOR** *(the collapse is real)*:

1. ### **"MONOID-RESPECTING PIECE" = "CHARACTER PIECE", EXACTLY.** *A piece whose coefficient function is
   completely multiplicative on the ideal monoid and constant on classes IS a homomorphism `Cl → ℂ^×` —
   i.e., the multiplicative presentation is not one choice among presentations, it is* ### **the eigenbasis
   of the monoid action.** *Every other presentation is a linear mixture of eigencomponents. So "sees the
   monoid-respecting presentation" and "input natively multiplicative" name the same property from two
   sides.*
2. ### **THE FIELD'S OWN CARRIER ALREADY LIVES ON THE PRODUCT SIDE.** *Weil's positivity is a statement on
   the idele class group (species register, Weil-1952 content); CCM's quoted input is "the ideles class
   group of a global field." The dual/product side is where the field put the object, exactly as the
   collapse predicts.*
3. ### **A MONOID-INPUT CARRIER NEEDS NO PRESENTATION INPUT.** *Both presentations are coordinates of
   `Z(s)` in `§9.1`'s two bases — generated from the monoid-with-classes, not supplied to it. `(v)` asks
   for nothing `(i)` does not already contain* — **provided `(i)` is read at class resolution.**

**AGAINST** *(a carrier could take the Fourier structure itself as data — named, and pursued honestly)*:

1. **The named frame:** *a carrier on `Cl × Ĉl` in which both presentations are objects* — made precise,
   this is ### **the group algebra `ℂ[Cl]` with its two distinguished bases, equivalently the
   `ℂ[Cl]`-valued Euler product `Z(s)`.** *It exists (`§9.1` constructs it longhand) and it does hold both
   presentations.*
2. ### **BUT ITS INPUT TYPE IS THE MONOID-WITH-CLASSES, NOT A PRESENTATION.** *The presentations arise in
   it as internal structure (two bases of one algebra). Pursued to the end, the AGAINST branch does not
   produce a counterexample to the collapse — it produces the collapse's* ### **constructive form.**
3. ### **WHAT THE AGAINST BRANCH DOES PROVE IS THAT BARE `(i)` IS NOT ENOUGH.** *A carrier can take the
   monoid and then factor through the AUGMENTATION — remember only `𝔞 ↦ N𝔞` — and such a carrier sees
   only `ζ_K`'s coefficients, cannot form either family, and is silent on the witness's variable while
   satisfying `(i)` nominally.* **So the collapse target is not `(i)` but `(i′): the monoid at class
   resolution — through `ℂ[Cl]`, not through `ε`.**

### `§9.3` — ### **VERDICT: `(v)` COLLAPSES TO `(i′)`, PLUS A RIDER ON `(iv)` — AND THE RIDER IS THE NAMED RESIDUE**

> ### **THE FERRIED CLAIM IS HALF-RIGHT, AND THE HALF IS THE FOR BRANCH's.** *Seeing the monoid-respecting
> presentation as distinguished is equivalent to native multiplicativity of the input — but "natively
> multiplicative" underspecifies by exactly two clauses:*
>
> 1. ### **`(i′)`** — *the content functor must factor through the class-monoid at class resolution
>    (`ℂ[Cl]`), and must NOT factor through the augmentation (the norm monoid).* **Multiplicative
>    recombination native; additive presentation derived.** *This half of `(v)` collapses.*
> 2. ### **THE RIDER ON `(iv)` — THE RESIDUE THAT DOES NOT COLLAPSE:** *no input choice guarantees the
>    resolution SURVIVES INTO THE PAIRING. A pairing manufactured from `(i′) × (ii)` could itself be
>    augmentation-silent — its value constant across the class resolution — and then the carrier satisfies every input clause and still cannot carry a
>    statement about placement.* ### **Kin, compiled: `descent_carries_no_warrant` — a pairing that cannot
>    separate two classes carries no warrant about them, whatever the form is. The residue is that lesson
>    made into a requirement on `(iv)`: the manufactured pairing must separate the `ψ`-components.**
>
> ### **SO `§6`'s SUSPICION AND `§8.3`'s RE-POINTING ARE BOTH CONFIRMED AND BOTH SHARPENED: `(v)` was `(i)`
> asked again downstream — and the downstream half is `(iv)`'s business, stated now as a rider rather than
> as a fifth column.**

### `§9.4` — THE FRAMES RE-GRADED UNDER THE OPERATION QUESTION — ### **THE `(v)` COLUMN RE-FILLED AS THE `(i′)` COLUMN**

*`§8`'s input-type grading (`ABSENT × 3`) asked "does the frame take a presentation as input?" — the
re-pointed question is "is the frame's input natively a product, at class resolution?" — and the answers
move:*

| frame | `(i′)` | at holdings' depth |
|:--|:--|:--|
| ### **CCM's cyclic-module carrier** | ### **YES — NATIVE AT THE INPUT** | *its own quoted input, held: "the ideles class group of a global field" — the multiplicative object, all places at once. Class-group characters are its characters (CFT, `NAVIGATOR-GRADE`), and the `ψ`-isotypic decomposition of functions on an abelian group is classical harmonic analysis, not a CCM-specific claim.* **Two named caveats:** *(a) the ideles are the monoid GROUP-COMPLETED — the integral-ideal monoid is not a distinguished subobject, and what that completion forgets is UNGRADED; (b) the `§9.3` rider is exactly where its Prop. 6.4 degeneracy sits — a pairing with a proven vanishing subspace is the warned failure mode, met in the wild.* |
| ### **Deninger's system** | ### **YES — AT THE SEAT** | *orbits ↔ closed points delivers the monoid's GENERATORS natively (`MATCH`-grade seat, held at abstract).* ### **CLASS RESOLUTION UNGRADED:** *whether the flow tracks `Pic` lives in the unfetched `H¹` material — the standing read-debt, which grades nothing.* |
| ### **THH / derived** | ### **BARRED AT THE LEVEL BAR** | *the input is the ring (spectrum), not the monoid — `L2`'s wrong coordinate — and "no cell inherits across" blocks the grade in either direction.* **Graded UNREACHABLE, not ABSENT.** |

> ### **THE COLUMN'S SHAPE UNDER THE RE-POINTING: the two frames the sheet already ranked highest are the
> two whose input is natively multiplicative — CCM at the input, Deninger at the seat — and the frame the
> bar excludes stays excluded.** *No frame moves in the ranking; what moves is the DIAGNOSIS: the `(v)`
> failure was never about input types, it was about class resolution and about the pairing keeping it.*

### `§9.5` — ### **THE OBJECT THE AGAINST BRANCH NAMED, FILED AND NOT PROMOTED**

> **`Z(s) = Π_𝔭 (1 − [𝔭]·N𝔭^{−s})^{−1} ∈ ℂ[Cl]`** — *the group-algebra-valued Euler product whose two
> canonical bases ARE the two presentations, whose augmentation is `ζ_K`, and whose determinant is `ζ_H`.*
> ### **This is the smallest object on which the two-decompositions witness is a statement about BASES OF
> ONE THING rather than about two things.** *Kin at naming grade only: group-ring-valued zeta data of
> Stickelberger/equivariant type —* `NAVIGATOR-SUPPLIED`, *naming is not reading.*
>
> ### **FILED AT QUESTION GRADE, FLAGGED `OURS`, AND NOT PROMOTED: it is a bookkeeping object, not a
> carrier — it has no involution column, no pairing, no roster beyond the finite places, and nothing in it
> approaches `h2`.** *What it is FOR, if anything: stating `(i′)` precisely — "factors through `ℂ[Cl]`"
> means "factors through this object's coefficient algebra."*

### `§9.5-bis` — ### **CROSS-LINK FROM `B5`** *(2026-08-17, ninth sitting)*

> ### **THE SEMILOCAL SPEC's `§6` (`B5`) FINDS THE ARCHIMEDEAN FACTOR ON THE CLASS-RESOLVED OBJECT TO BE A
> CLASS-SILENT SCALAR — WHICH SITS IN GRADED TENSION WITH `R3`'s `arch_independent`** *(the archimedean
> coordinate must carry information the finite ones do not)*: **on `Z`, the archimedean coordinate carries
> `s`-analytic information and ZERO `ℂ[Cl]`-information — the class resolution is entirely
> finite-places.** *Filed as tension, not as a failure of either row: `R3` constrains a CARRIER, and `Z` is
> graded a content vehicle, not a carrier (`B5`, `§2.1` branch B). The tension prices what a carrier built
> OVER `Z` would owe `R3`: an archimedean coordinate with content the finite places lack — which the
> class-silent `γ_∞` does not supply.* **No promotion; the rider and the gate unchanged.**

### `§10` — ### **CROSS-LINK FROM `B10`: ROADS A AND B TOUCH AT THE SEAT** *(2026-08-17, fifteenth sitting; question grade, `OURS`, no promotion)*

> **[GRADE UPDATED 2026-08-17, seventeenth sitting, `B12`: the shape-level match is now split at
> content — MATCH-AT-CONTENT on the arithmetic side (his closed-orbit data `l(γ_𝔭) = log N𝔭`,
> `ε = +1`, transversal modulus `N𝔭^{1/2}` — carried at `NAVIGATOR-AT-CONTENT` from `math/0204110`,
> name-identity verified live — against the corpus's exact fifth-law weight), and NO CONTACT on the
> cohomological side (his reduced leafwise cohomology, unbuilt for number fields by his own
> fixed-point caveat; the corpus holds no counterpart). The Face-E reading: both roads hold the
> ledger's prime side; neither holds the object whose trace it is. See the spec `§8.3-septies` and
> `§11` below.]**

> ### **THE SEMILOCAL SPEC's `B10` MEASURED, AT MODEL GRADE, A MINIATURE OF THE MECHANISM THIS SHEET'S
> BEST-GRADED FRAME CONJECTURES:** *on the orbit quotient (`x ∼ px`) of the local model, the scaling trace
> is ALIVE at the lags (`T_quot ≠ 0`, per-step ratio `→ √p/(p+1)` — the Weil shape with an
> Euler-factor-like correction), where on the Sonin-type space it vanishes by fixed-point localization
> (`B9`–`B10`, proved).* ### **THE FINITE-PLACE WEIGHT ARISES AS A CLOSED-ORBIT TRACE EXACTLY WHERE
> DENINGER's SEAT PUTS ITS CONTENT — "periodic orbits related to the closed points" — AND THE SHAPE
> MATCHES THE LEFSCHETZ READING CC's ARREST SENTENCE NAMES AS THE OPEN QUESTION.** *Road A's arrest (the
> missing self-product / Weil cohomology) and Road B's gap (the ledger-side finite trace) are, at the
> seat, the SAME absence viewed from two roads — the object that would read the explicit formula as a
> Lefschetz formula.* ### **MATCH-IN-SHAPE ONLY; the `(1+p^{−1})^{−1}` correction unexplained; the model
> is ours; the `H¹` read-debt (now Road B's highest-priority retrieval) grades everything further.**
> *No merge, no mechanism, no promotion.*

### `§9.6` — THE STOP, REPORTED

**No sign result was reached or approached. No mechanism is proposed. No candidate is promoted. The
`ATTEMPT` gate was not approached. The corrections are coefficient bookkeeping; the verdict is a
re-classification of a requirement; the re-grading moves a diagnosis, not a ranking.**

---

## §11 — ### **COLUMN `(vi)`: CONFORMAL STRUCTURE WITH FACTOR `e^t` — THE FUSION VERDICT** *(2026-08-17, seventeenth sitting, `B12`; proof-search on Fable; sources: the `B12` carried quotes from `math/0204110`, `NAVIGATOR-AT-CONTENT`, name-identity verified live)*

### `§11.1` — THE MECHANISM, EXTRACTED EXACTLY *(at the carried cites)*

*Deninger's sign mechanism: the flow on the leafwise tangent bundle is CONFORMAL with factor `e^{αt}`
(eq 9/22); on reduced leafwise cohomology the infinitesimal generator therefore decomposes as*
### **`Θ = α/2 + S` with `S` SKEW** *(Thm 2.1), hence `Re ρ = α/2`; the number-field case FORCES `α = 1`
(§4, from the `e^t` term of the explicit formula), which is* ### **impossible on manifolds (Cor 3.5
Rem 2) and realized on the laminated `§5.8` example — where it PROVES RH for `ζ_E`.**

### `§11.2` — THE FUSION QUESTION, BOTH BRANCHES LONGHAND

**FOR** *(the fusion is real, and it is exact):*

1. ### **THE SKEW HALF IS THE INVOLUTION COLUMN, AT THE OPERATOR LEVEL.** *`Θ = α/2 + S` with `S` skew is
   equivalent to `Θ* = α·Id − Θ` — which is PRECISELY the operator statement of the FE-reflection
   `s ↦ α − s` (at `α = 1`: `s ↦ 1 − s`, the involution column's own map, fixed point `α/2 = ½`).*
2. ### **THE MODULUS HALF IS THE WEIGHT COLUMN.** *The conformal factor `e^{αt}` is the flow's metric
   scaling — at the closed orbits it is the transversal modulus `N𝔭^{α/2} = q^{1/2}` at `α = 1`: the Weil
   weight, which the corpus now holds EXACTLY at model grade (the fifth law).*
3. ### **SO CONFORMALITY IS ONE METRIC CONDITION WHOSE POLAR PARTS ARE EXACTLY THE TWO COLUMNS — modulus
   = weight, skew = involution — AND THE FUSION IS WHAT CONVERTS THE PAIR INTO PLACEMENT
   (`Re ρ = α/2`).**

**AGAINST** *(argued honestly, and it lands a precision, not a refutation):*

* *Stating skewness requires a HILBERT structure — the leafwise metric — which the corpus's involution
  column does not need (the antipode-twisted reflection on `Ẑ` is algebraic).* ### **So `(vi)` is NOT
  merely `(ii)` + weight: it strictly contains a NEW DATUM — the leafwise metric with its scaling law.**
  *And the `W-2` guard must ride: a column demanding "skew with respect to an inner product" walks toward
  demanding the pairing's positivity as input.*

> ### **VERDICT: `(vi)` LANDS, STATED PRECISELY.** **COLUMN `(vi)` — CONFORMAL STRUCTURE: a leafwise
> metric for which the pairing's flow is conformal with factor exactly `e^t`.** *Its polar parts
> REPRODUCE the involution column and the weight column (so `(vi)` implies their coherence), and it is
> STRICTLY STRONGER — the metric is new data.* ### **THE `W-2` GUARD, ON THE COLUMN'S FACE: `(vi)`
> demands a metric with a scaling law, NEVER definiteness of the induced pairing — Deninger's own gap
> (the unbuilt LHS) is exactly the difference, and this column does not close it.**

### `§11.3` — `(vi)` GRADED ON THE FRAMES AND THE CLASS-RESOLVED OBJECT

| frame | `(vi)` | ground |
|:--|:--|:--|
| **`THH` / derived** | ### **ABSENT** | *no leafwise metric, no flow-conformality in holdings; the level bar stands* |
| ### **Deninger's system** | ### **PRESENT-AS-DESIGN-TARGET — the column's SOURCE — and the object UNBUILT for number fields** | *native in the framework (the carried eq 9/22); his own fixed-point caveat blocks the number-field case; `§5.8`: PRESENT AND PROVED on the laminated example* |
| **CCM's carrier** | ### **ABSENT IN HOLDINGS** | *a scaling flow, yes; a leafwise conformal metric, nowhere in what is held* |
| ### **`Ẑ` (class-resolved)** | ### **ABSENT — NO LEAVES** | *`Ẑ` carries the two polar SHADOWS of `(vi)` — the antipode-involution (`B5(b)`) and the exact Weil weight (`B11`) — WITHOUT the geometric substrate that fuses them.* **Which is a precise restatement of the missing identification column: the carrier question is now "what supplies the leaves."** |

### `§11.4` — WHAT `§5.8` HAS THAT `ℚ` LACKS — ### **ONE SENTENCE, AT HIS CITE AND OURS**

> ### **THE PROVED CASE HAS A FROBENIUS LIFT (CM curves have them, "almost never" otherwise — his cite)
> AND `ℚ` DOES NOT — WHICH IS THE CORPUS'S MISSING SELF-PRODUCT (Road A's arrest) AND HIS "MISLEADING"
> VERDICT NAMING THE SAME OBSTRUCTION FROM TWO ROADS: without the lift, neither the dynamical space nor
> the square exists over `Spec ℤ`.**

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. `h2` UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

---

## §12 — ### **THE CORPUS'S OWN LIGHT ON THE CARRIER — THE COMPILED SURROUND READ AT STATEMENT, SET AGAINST THE SIX COLUMNS** *(2026-08-18, twentieth sitting; the author's challenge; every terminal read at its DEPOSIT pin — `SIDE-kernel` `v1.5 = 0e5233f`, `SIDE-lv-conservation` `v0.10.0 = 93c27ec`, both pins verified — salt-check law governing, never from profile)*

> ### **TWO SALT-CHECK CATCHES, FILED BEFORE THE TABLE:** *(1)* ### **the loom's row "Seven voices (each
> pins `σ = ½`)" IS A PROFILE-LEVEL OVERCOUNT: at `v1.5`, `Voice4.lean` DOES NOT EXIST, and `Voice7`'s
> own terminal is `topological_no_sigma_preference` — it RESTS at `½` (`c7_rests_at_half`) and forces
> nothing.** *Five voices force; one rests; one is absent. Dated loom correction filed.* *(2)* *the
> ferry's "Ostrowski for `K`" — the compiled Ostrowski (`place_classification_exhaustive`,
> `ostrowski_exhaustive`) is for `ℚ`; the `K`-level roster is not compiled. Logged.*

### `§12.1` — THE SURROUND, ONE ROW PER TERMINAL, AT STATEMENT

| terminal (at pin) | what it states, read at source | column | what it supplies |
|:--|:--|:--|:--|
| ### **the five forcing Voices** (`balance_theorem`/`balance_at_half` · `symmetries_agree_iff` · `reflect_fixed_iff` · `S_involution`/`S_fixed_point` + `R_order_3` · `self_adjoint_forces_half`) | *`σ = ½` is the unique solution of FIVE separate algebraic conditions — the Euler balance `p^{−σ} = p^{−(1−σ)}`; conjugation-vs-reflection agreement; the reflection's fixed point; the modular `S`-action's fixed point; the self-adjoint constraint* | **(ii) involution** | ### **THE FIXED-POINT HALF, DERIVED — five machineries, our kernels.** *The three-are-one, restated at statement: `S_action` and `reflect` are definitionally ONE map (`1 − σ`); conjugation acts as the identity on `σ`; the three "agree" only AT `½` — the fixed point is where the involutions' disagreement vanishes.* ### **NONE derives a Hermitian structure: no inner product, no skewness anywhere in the five. The involution column's OPERATOR half — column `(vi)`'s skew part — is compiled NOWHERE.** *(`deriv_fe`/`deriv_at_half`: the FE's derivative relation at `½` — the infinitesimal fixed-point algebra, same half.)* |
| ### **`RegisterPentagon`** (five faces, statement-only by design) | *R1 universality · R2 conservation (every ξ-zero forces the Euler balance at some prime) · R3 totality-through-places (`∀∃ ⟹ ∃∀`, NOT compiled — the repo's open `sorry`) · R4 positivity (`λ_n ≥ 0` as a `Prop` on a stream; sharpest form the channel inequality `λ_Z ≥ −λ_A`) · R5 spectral distance (input certified, output DISCLAIMED)* | mapped: ### **R2 ↔ the `(involution × weight)` polar pair — `(vi)`'s PROPOSITIONAL shadow** *(the balance equation IS the involution acting on the weight variable at a prime)* · **R4 ↔ `(iv)` as PREDICATE — the `W-2`-compliant form exactly** · **R5 ↔ `(iii)`** *(the `C₅` distance = `(iii)`'s absence, COMPILED as a boundary marker: `certifiedInput_not_zeroRealizing`)* · **R1/R3 ↔ the roster/joint-move side** *(`R3`'s open `∀∃⟹∃∀` is the compiled shadow of `(iv)`'s joint step)* | ### **WHICH COLUMNS HAVE NO FACE: the LEAVES (`(vi)`'s metric substrate) and `(i′)`'s CLASS RESOLUTION — the pentagon is `ζ`-level, augmentation-side.** ### **And the pentagon's own header is the carrier's law: compiling the cross-register equivalences would encode RH-equivalence — A CARRIER THAT FUSED THE FACES WOULD COMPILE RH-EQUIVALENCE. The carrier must therefore be BUILT, never specified into existence; its definiteness stays a predicate. The specification is lawful exactly because it never fuses.** |
| ### **`h1_complete_at_Phi`** (eight conjuncts, each `#print axioms` std3) | *C₁ realness · C₂ half-plane nonvanishing · C₃ theta transformation · C₄ modularity (Fork B: the holomorphic `ℍ`-lift with the T- and S-laws) · C₅-input (heat trace of `{n²}`) · C₆ holomorphic extension · C₇-entirety · C₇-order — ALL AT THE ONE FIXED WITNESS `Φ`* | **(i)** | ### **`Φ` IS A POINT ON THE LEDGER SIDE — the archimedean realization of the completed-`ζ` integrand — AND ITS COMPLETENESS CERTIFIES THAT COLUMN `(i)`'s DEMANDS ARE JOINTLY SATISFIABLE AT ONE WITNESS.** *What remains open at that witness is exactly placement (`h2`: `mellin Φ(s/2) ≠ 0`).* **Honest note: `Φ` is `ζ`-level — no class resolution; the augmentation shadow of `(i′)`.** |
| ### **`partialPositivity_finiteRange` + `lowFinset`** | *from three NAMED premises (`VerifiedZerosTo T` — external computation; `ExplicitFormulaDecomp` — classical, its finite-set conjunct now PROVED via `lowFinset`; `TailBoundPremise` — classical): `λ_n ≥ 0` for `n ≤ N₀(T) = ⌊2T²⌋`; plus `blTerm_nonneg_of_onLine` PROVED* | **(iv)** | ### **THE PROGRAMME'S OWN CERTIFIED POSITIVITY, AT ITS RANGE — set beside the local model's quotient trace and the atlas's band: THREE POSITIVITY-SHAPED FACTS AT THREE GRADES, NO MERGE** — *finite-range Li (`INTERFACES`, range `n ≤ 2T²`) · the quotient trace's exact Weil weight (`MODEL`, positivity of a form UNKNOWN) · the atlas hover at `O(10⁻⁴)` (`BENCH`). Different objects, different ranges; laid side by side and NOT fused.* |
| ### **Ostrowski + formation counts** (`place_classification_exhaustive` · `ostrowski_exhaustive` · `formation_n2 = 3` · `formation_count`) | *the places of `ℚ` are exhaustively classified; `n₂ = 3`; `2+3+2+0 = 7`* | **(iii) roster** | ### **THE ROSTER COLUMN'S EXHAUSTIVENESS IS ALREADY COMPILED — recorded as such on this sheet.** *Scope: `ℚ`; the `K`-level roster is not compiled (logged above).* |
| ### **`EDifficultyTop`** (`SIDE-effects`, a NAMED PREMISE, consumed never proved) | *top grade ⟺ Type-I discharged ("the domain Ostrowski is constructed — E-Difficulty Level 1"); `RH_typeI_of_top` consumes it* | catalogue side | ### **E-DIFFICULTY SITS ON THE CATALOGUE-CLOSEABILITY ARC, NOT THE LEAVES — the corpus ruled the arcs DISTINCT, and the statement confirms it: Level 1 is roster-construction (Type-I), a `(iii)`-roster fact; nothing in it touches the metric substrate.** |

### `§12.2` — PASS 2's OWN VERDICTS AS THE CARRIER'S TEST *(the corpus's necessary conditions before any external frame's; the joint reading's `N`-list read at source — SEVEN conditions `N1`–`N7`, the ferry's "five" flagged)*

| condition | column it constrains | the six-column carrier, as specified |
|:--|:--|:--|
| **N1** reach `C₂` and only `C₂` | the target | ### **SATISFIES-AS-SPECIFIED** — one pairing, one predicate |
| **N2** no positive control | the method | ### **SATISFIES** — the sheet's own governing constraint (`L3`, spec-forced never fitted) |
| ### **N3** not dark-derivable — needs the BRIGHT INTERFACE located | ### **the leaves** | ### **THE CARRIER LOCATES IT AND DOES NOT HOLD IT — `N3` IS THE CORPUS'S OWN NAME FOR THE LEAVES ABSENCE: `Δn₄ = +1`, the missing `H²` codomain (the codimension diagnosis: "no `H²` receives it … the pairing has no codomain").** |
| **N4** genuinely infinite-range | `(iv)`'s route | *compatible as specified — the positivity must be STRUCTURAL (born from `(i)×(ii)`), which is the sheet's `(iv)`; unbuilt* |
| **N5** localized at `χ₀` | `(i′)` interplay | ### **FIT, READ CAREFULLY: the DIFFICULTY localizes at the principal character (the augmentation) while the STRUCTURE needs the full class resolution — `h2` lives in the `ψ₀`-component; `ℂ[Cl]` is what the carrier needs to exist. No conflict; the two statements are about placement and structure respectively.** |
| **N6** bound `λ_Z` specifically | the weight column | ### **SATISFIES-AS-DIAGNOSIS — the phase boundary re-derives it: archimedean pays structure, finite pays weight; the carrier's trace must reach the ZERO channel** |
| **N7** not supplied by a trace/realization theorem | `(α)`/`(iii)` | ### **SATISFIES-AS-SPECIFIED — the ledger-side `(α)` demands the sandwich SHAPE, never a realization; the rider carries `N7` verbatim** |
| **Face E** — the Euler product essentially | **(i)** | **SATISFIES-AS-SPECIFIED** — the monoid is the content source |
| **the placement-mute derivation** | **(ii)** | **SATISFIES-AS-SPECIFIED** — the interface transmits density, not placement; the finite-place trace (B7's specification) is exactly the compliance |

### `§12.3` — ### **THE VERDICT**

> ### **WHAT THE CORPUS'S COMPILED SURROUND ALREADY SUPPLIES** *(columns filled at DERIVED grade by our
> own kernels, previously graded only from external frames)*: **the involution's FIXED-POINT half (five
> voices, five machineries) · the roster's exhaustiveness (Ostrowski, `n₂ = 3`) · column `(i)`'s joint
> satisfiability at one witness (`h1` at `Φ`, eight couplings) · `(iv)`'s positivity SHAPE as a lawful
> predicate (R4) · `(iii)`'s absence COMPILED as a theorem (the `C₅`-distance marker) · finite-range
> positivity at `INTERFACES`.**
>
> ### **WHAT IT CONSTRAINS:** *the `N`-conditions and Face E as mapped above — and the pentagon's
> non-fusion law, which is the deepest: THE CARRIER IS THE OBJECT ON WHICH THE FIVE FACES WOULD FUSE,
> WHICH IS WHY IT CANNOT BE SPECIFIED INTO EXISTENCE AND MUST BE BUILT, WITH DEFINITENESS FOREVER A
> PREDICATE.*
>
> ### **WHAT REMAINS ABSENT, WITH THE CORPUS'S OWN NAME FOR THE ABSENCE: THE MISSING-CODOMAIN QUESTION (SELF-PRODUCT / FROBENIUS LIFT / LEAVES) IN THE
> PROGRAMME'S OWN VOCABULARY IS — WHAT SUPPLIES THE ONE BRIGHT INTERFACE `Δn₄ = +1`: THE `H²` THE
> PAIRING NEEDS AS CODOMAIN.** *"What supplies the leaves" (the three-road form: self-product · leafwise
> metric · Frobenius lift) and "what supplies the missing `H²`" (`N3`'s bright interface; the codimension
> diagnosis; rider (b)'s cup-product reading `H¹ × H¹ → H²`) are ONE question in two vocabularies — the
> external frames' and the programme's own. The corpus had already named its absence before the roads
> did; the roads found its address.* **And the second absence stands beside it: `(i′)`'s class
> resolution, which no pentagon face and no kernel carries.**
>
> ### **THE `C1` SENTENCE AGAINST THE PENTAGON'S FIVE FACES:** *four of the six columns have
> pentagon-faces (R2 ↔ involution×weight · R4 ↔ `(iv)` · R5 ↔ `(iii)` · R1/R3 ↔ roster/joint); the
> pentagon is SILENT on exactly the two columns the week added — `(i′)`'s resolution and `(vi)`'s
> leaves — and the silence is the same absence. The charter's sentence and the compiled pentagon agree
> everywhere they overlap.*

**NO CONSTRUCTION. NO MECHANISM. NO CANDIDATE PROMOTED. THE ATTEMPT GATE WAS NOT APPROACHED. `h2`
UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

---

## CLOSING

**`W-CARRIER-BUILD` act 1 delivers a SHEET, a GRADING, and a NAMED QUESTION. It builds nothing.**

> ### **THE CONSTRUCTION BEGINS WHERE THE GRADING SAYS: Deninger's frame, at its `Δ2` absence, by asking the
> flow's own reversal to carry the involution. NOT where appetite says — CCM's frame has the pairing and is
> the more finished object, and the grading declines it because an imported degenerate form scores the
> column without answering it.**

**`h2` UNCHANGED. NO MECHANISM PROPOSED. NO SIGN. THE `ATTEMPT` GATE IS CLOSED AND WAS NOT APPROACHED.
NOTHING DEPOSITS.**

---

## §13 — ### **THE INTERRELATION: `§12`'s COMPILED SURROUND READ AGAINST THE BUILT SECTION** *(2026-08-18, sittings 14–16 of `W-ATTEMPT-2`, relay-banked; full tables at `relay/reports/2026-08-18-w-attempt-2-sitting-{14,15,16}.md`; the pentagon's non-fusion law governing throughout)*

*`§12` read the corpus's compiled terminals against the six columns; the attempt then BUILT the
section, and this section records what the two lights show each other — one line per face, verdicts at
grade:*

| `§12` row | against the built object |
|:--|:--|
| **R4 (positivity as predicate)** | ### **INSTANTIATED-AND-CHECKED AT THE MODEL** — `B\|_class ≻ 0` exactly, riding the Euler coefficients `(4,1,1)`; the four separations from complete roster listed in the pentagon's own terms (stream · object · roster · the channel inequality's absent `Z`) |
| **R2 (involution×weight)** | the flip-channel/arrival-edge structure is R2's shape SEEN LOCALLY — kinship grade, `OURS`, no compiled equivalence |
| **R3 (`∀∃⟹∃∀`, the open `sorry`)** | the diagonal is a SHADOW-INSTANCE (support and place one datum at finite roster), not a discharge; the `sorry` stands |
| **R5 (`C₅` boundary marker)** | consistent: the object is certified-input-shaped, zero-side-silent by its own proved localization (`t_k = 0`) |
| **Voice7 (rests, forces nothing)** | role-kinship with the antipode-anti-invariant sector only — named, never merged |
| **`h1_complete_at_Phi` (remainder = placement)** | two DIFFERENT placements kept apart (sign-sectors vs zeros); and `N5`'s fit is now a bench fact on one object |
| **the finite-range Li row** | the `2T²`-vs-diagonal identification REFUSED by `T9`'s own law (Li-index ≠ support); three positivity-shaped facts stay three |
| **the atlas row (`T8`)** | the arch-incumbent/prime-buy-in shape matches the object's ledger AT SHAPE; the `−2.000127` mechanism has NO analogue and should not (the object ate the pole: `P` is a constraint, not a term) |

### **THE SHARPEST LINE:** *the object realizes the reconciliation's right-hand side (`P` as constraint
· `PR` as the punctuated diagonal form · `A` as the model factor) and HAS NO LEFT SIDE — the missing
`Z` channel is `§12.3`'s missing-codomain absence in LEDGER form. The corpus named the absence, the
roads found its address, the ledger now shows its shape.*

**NO CONSTRUCTION CLAIMED BEYOND THE RELAY-BANKED MODEL. `h2` UNCHANGED. NO SIGN. NOTHING DEPOSITS.**

> **ARC LINE (2026-08-18, sittings 20-21):** *the model's one-place limit closed at proof grade; the
> pentagon re-read filed as a keystone DRAFT — the non-fusion law and the missing-codomain diagnosis
> read as one statement in two registers, for the author's review (`FACES_OF_H2_AT_FINITE_INSTANCE`).*

> **ARC LINE (2026-08-18, sitting 22):** *the archimedean E_1 identity at machine; the place-uniform
> constrained-sector sentence licensed at three grades; the double limit named as the E_1-gluing at
> complete roster — the codomain in one word; the keystone draft amended, still DRAFT.*

> **§0 POINTER (2026-08-18, the implications filing):** *this road now reads under the charter's `§8`
> (the five construction implications, each at cite) — in particular (ii): the missing-codomain
> question this document carries is ONE absence with FOUR names, and any closure claim must fill all
> four. Charter `v1.2`; workflow at `EXECUTOR_RULES` Rule 4.*

> **§0 POINTER (2026-08-18): the missing-codomain question this road carries is now a SPECIFICATION —
> `THE_CODOMAIN_SPECIFICATION.md` (v0.1 DRAFT)** — *(a)–(d) filled by the built section; (e) the
> requirement with four names and its falsifier.*

> **STANDING POINTER (2026-08-18, the corrections-placement ruling):** *current statements here;
> provenance and corrections at `relay/reports/W-ATTEMPT-2-CORRECTIONS-REGISTER.md`; the embedded
> historical narratives' sweep priced as this road's next revision pass.*
