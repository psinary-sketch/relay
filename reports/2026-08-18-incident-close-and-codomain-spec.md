# THE INCIDENT CLOSED · THE CODOMAIN SPECIFICATION DRAFTED
## ### **RULE-4 HARDENINGS MINTED AND INSTALLED (THE HOOK REFUSED ITS OWN SELF-TEST) — THE HELD-ON-TOP ARRANGEMENT RETIRED FOR A LOCAL-ONLY `held/*` BRANCH — THE PURGE DRAFT STAGED WITH FULL SHAs, SUBMISSION THE ACCOUNT OWNER'S — AND THE TARGET SPECIFIED: `THE_CODOMAIN_SPECIFICATION` v0.1, (a)–(d) FILLED AT FINITE INSTANCE, (e) THE FOUR-NAMED ABSENCE**

**Relay report · 2026-08-18 · author-ruled · nothing deposits · nothing circulates**

> ### **SCOPE, once: finite instance and one place at a time; clause (e) is the double limit and
> untouched.**

> ### **RULE-3 LOG:** *the arc's records at `3235247`/`f9dfedd` ✓; charter `§8` ✓; the incident
> report ✓. One capability flag executed honestly: a GitHub support ticket can be SUBMITTED only by
> the account owner — the request is drafted ready-to-send (`internal/GITHUB_PURGE_REQUEST_DRAFT.md`,
> full SHAs `ae9e3aca…054a` and `b828418a…2b13`), the counsel inventory line is filed, and the
> submission is flagged as the author's one remaining act. Terms coined: none.*

---

## §1 — THE INCIDENT CLOSED

1. ### **(b) THE HARDENINGS MINTED VERBATIM** (`EXECUTOR_RULES` 4.8–4.11): `git -C` always ·
   `add -A` banned in relay · pushes to main only from `push-*`/`repair-*` branches through the
   pre-push hook (tracked at `tools/git-hooks/pre-push`, installed) that refuses wrong-branch pushes
   and any outgoing `HELD` ancestry · held material on local-only `held/*` branches. ### **The hook's
   self-test PASSED by refusing: a dry-run push from `main` was rejected by the hook itself.**
2. ### **(d) THE HELD-ON-TOP ARRANGEMENT IS RETIRED** — *it made every push one mistake away from
   publication, and the incident was that mistake.* The two carrier acts now live ONLY on
   `held/carrier-acts` (local); local `main` = `origin/main` exactly (`3235247`); ### **verified: the
   remote has exactly one head (`main`), and the acts appear in 0 files on it.** The loom carries the
   incident and the fix; the release condition on the acts is unchanged (counsel, then the author).
3. **(c)** the six `.olean` artifacts removed by a normal commit (this batch). **(a)** the purge
   draft staged; the counsel inventory line filed; **submission awaits the account owner.**

## §2 — THE CODOMAIN SPECIFICATION (v0.1 DRAFT, `phase2/method/`)

*The title names the object and the condition; every clause at cite; the register sentence quoted
unchanged.* **(a)** per place: Sonin closure · `F² = parity` · `E₁` with `B = ‖·‖²` · the forced
compression — grades per place (proof `p = 2` / model `p = 3` / machine + the prolate NAME at `∞`).
**(b)** the gluing datum as a class-character trace, exact and compiled, with the `h = 1` collapse
bounding its role. **(c)** family compatibility: equivariant inclusions, on-the-nose stability, the
diagonal's self-duality. **(d)** archimedean compatibility with the hard-truncation refusal named as
mathematics. ### **(e) COMPLETENESS — the `E₁`-gluing at complete roster: one absence, four names,
and the sentence on its face: "this is Weil's 1952 object at complete roster, and it is exactly what
does not exist."** *§6 grades four objects against (a)–(e) with no blank cells — ours ((a)–(d) at
model, (e) ABSENT), the function-field Jacobian ((e) FILLED and RH follows — the row that shows the
specification is satisfiable somewhere), Deninger's frame (at its `Δ2` absence), CCM (at its radical
arrest). §7 is the falsifier: claiming (e) requires filling all four names and deriving each from
each; (a)–(d) at ANY finite instance shows nothing toward (e); and the draft's own vulnerability is
recorded (a sign route bypassing (e) is not excluded, only made hard to imagine by the `E₁` theorem).*

## §3 — THE RECORD

*`FINDINGS` `F.2026-08-18i` · `OPEN_TRAILS` tail pointer · both roads' `§0` pointers · the packet
framed by the spec · correspondence rows 66–68 · the mirror rebuilt at final HEAD (three roster files
moved) · this batch pushed through the NEW procedure (the hook's first live use).*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING DEPOSITS. NOTHING CIRCULATES.**
