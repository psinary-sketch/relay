# THE COHERENCE ACT — THE RECIPROCITY THEOREM, THE SURVEY GATE, THE CONDITIONAL CLASS BIT, THE MERGED CONSTRUCTION
## ### **ALL FOUR COMPONENTS LAND ON THEIR REGISTERED BRANCHES. P1: THE RATIONAL RECIPROCITY THEOREM — x MINUS THE SUM OF ITS p-ADIC FRACTIONAL PARTS IS AN INTEGER — LONGHAND AT GENERAL ℚ, DECIDED OVER FULL RESIDUE RANGES AT THE BANKED DENOMINATOR FAMILIES. P2: THE CONVENTION SURVEY'S GATE HOLDS — THE PLUS ORIENTATION IS UNIFORM ACROSS EVERY BANKED CELL AND THE CONSTRUCTED LIMIT. P3: THE FORCED ORIENTATION IS UNCONDITIONAL AS AN IMPLICATION — ANY FAMILY EXTENDING THE FINITE PLUS-CONVENTIONS AND TRIVIAL ON ℚ IS FORCED CONJUGATE AT THE REAL PLACE — AND [UNDER H-COH] THE CLASS BIT DERIVES: S1'S VALUE CONTENT REDUCES TO THE SIGN BIT PLUS H-COH-OR-REPLACEMENT, OWNERS NAMED. P4: STEP A OF THE THETA BRIDGE AND N5'S RADIAL-COMPRESSION DICTIONARY ARE THE SAME REQUESTED OBJECT — THE TWO OPENS MERGE INTO ONE NAMED CONSTRUCTION, THE RADIALIZATION, STAGED. CORE 215/215.**

**Relay report · 2026-08-21 · ferry-executed (part 1 of 1, receipt confirmed, Rule 1) ·
Rules 3/4/5 · the b71 run-registration (D1–D4 longhand before any run; the Bézout
data pre-registered; H-COH named at first use and carried inline on every
dependent conclusion) banked first · finite and recorded-ground work only; no
archimedean number computed; the range law governs · the letter disciplines
standing · nothing about `h2` claimed · nothing deposits · nothing circulates
beyond this report.**

> ### **RULE-3 LOG:** *(a) the banked conventions read at content per cell (the
> instruments' shared transform-kernel line grep-verified across b53/b57/b58, the
> b65/b66 imports of the one S_apply; the g-law and limit phases at rows 45); row
> 50's obstruction QUOTED IN THE REGISTRATION as the act's logic — the chooser must
> be global, which is exactly what H-COH is and why it is a named premise, not a
> smoothing. (b) the forced-orientation step is an unconditional implication from
> P1 + P2; every conclusion resting on the coherent-extension premise carries
> [UNDER H-COH] inline — the class bit is CONDITIONAL and says so everywhere it
> appears. (c) the P4 read compared the two requests at content and kept their
> difference alive as the merged construction's two clients — the merge is of the
> REQUESTS, not of what the clients would conclude. (d) the four components
> pairwise independent; component 3 consumed components 1–2 in order per the ferry.
> Banned-term grep on this act's writing: clean. Terms coined: H-COH (the ferry's
> own hypothesis discipline names it); THE RADIALIZATION and
> W-CONSTR-RADIALIZATION / W-LEAN-RECIPROCITY-GENERAL-Q (naming the merged
> construction and the named open — each names an object or what is missing).*

---

## §1 — THE PREDICTIONS, VERBATIM, WITH VERDICTS BESIDE THEM

> **(P1)** *"For rational x, x minus the sum of its p-adic fractional parts is an
> integer — zero-axiom bar at reachable generality, decided instances at banked
> denominator families."* ### **LANDS at the registered generality.** The general-ℚ
> statement carried by the registration's CRT/Bézout longhand; the recombination
> identities DECIDED over the FULL residue ranges at 36 = 4·9, 100 = 4·25,
> 1296 = 16·81, and the triple 900 = 4·9·25 (instrument and kernel agreeing; the
> Bézout inverses pre-registered and checked longhand first). The 1296 range needed
> the compiler's recursion limit raised (`maxRecDepth 8192`) — a depth limit on the
> list equality, no axiom content, disclosed. The general-ℚ COMPILE stays longhand
> as stated in advance: **W-LEAN-RECIPROCITY-GENERAL-Q** (what is missing: rational
> arithmetic in the vanilla kit).

> **(P2)** *"Every banked cell and the constructed limit carry the plus orientation
> uniformly — a GATE read from the banks; any mixed convention is the finding."*
> ### **THE GATE HOLDS — uniform plus.** One shared transform-kernel line
> (`ee = (e + m*mp) % NN`, the +m·m′ exponent) across every banked instrument
> (grep; the later instruments import b58's S_apply — one kernel, not several); the
> g-law phase +a(q−t) and the limit fraction +α(1−τ) at row 45; the decided
> discriminators tr S = q(1+i) with the PLUS imaginary part — (2,1) decided this
> act, (2,2) decided at b70. No mixed convention exists; component 3 ran.

> **(P3)** *"Any all-places character family extending the finite conventions and
> trivial on ℚ takes the conjugate orientation at the real place — derived from P1;
> under named hypothesis H-COH the class bit derives and selection reduces to the
> sign bit … owners named; branch (b) if a step fails."* ### **BRANCH (a), BOTH
> STEPS.** STEP 1, UNCONDITIONAL as an implication: the reciprocity theorem makes
> the finite plus-convention product equal e^{2πix} on ℚ, so triviality FORCES THE
> CONJUGATE at the real place, χ_∞(x) = e^{−2πix} (the arithmetic core decided —
> the conjugate-complement instances). STEP 2 **[UNDER H-COH]**: the archimedean
> center phase is the conjugate faithful value −i, the boundary family is THE
> CONJUGATE CLASS {±ζ₈⁻¹}, and the suspect e^{−iπ/4} is its plus-sign member — THE
> CLASS BIT DERIVES [UNDER H-COH]. **S1's value content restated with owners: (the
> sign bit — owner: the identity limit's value) + (H-COH or its replacement —
> owner: the global coherence construction, the corpus's own adelic-character
> self-duality, unbuilt); the invariance clause unchanged (owner: the index face,
> NA-3).** Row 50 is respected, not contradicted: the local recorded ground is
> orientation-symmetric, so the chooser MUST be global — H-COH is that chooser,
> named as a premise with an unbuilt owner, never asserted as fact.

> **(P4)** *"Step A of the theta bridge and N5's radial-compression dictionary
> possibly the same requested object; coincidence-verdict merges the two opens into
> one named construction, staged."* ### **COINCIDENCE at the requested-construction
> level** (the sharp end of the PARTIAL-or-same prior). Both request THE
> SHELL-SUMMING MAP from the chart/g-family to the shell model. **The two opens
> MERGE into ONE named construction — THE RADIALIZATION (W-CONSTR-RADIALIZATION,
> `data/radialization_construction_STAGED_2026-08-21.txt`) — staged for the
> author.** The prior names retire into its two clients (the theta series toward
> the functional-equation frame; the defect dictionary toward N5's (1,1)) — the
> difference stated so nothing is smoothed. Nothing built.

## §2 — THE KERNEL AND THE RANGE LAW

`CoherenceShadow` — 3 terminals (`reciprocity_instances`,
`orientation_discriminator_2_1`, `conjugate_complement_instances`), each *"does
not depend on any axioms"* — **Core 215/215**; row 51 filed; the fourth-visit
registration and the poster carry the dated additions.

**The range law, on the face:** finite decisions and one unconditional implication
from recorded ground; every archimedean conclusion conditional and tagged [UNDER
H-COH]; no archimedean number computed; S1 stands open at its restated form; the
junction stays a fence; nothing about `h2` follows.

## §3 — THE RECORD AND PINS

*Relay: the b71 run-registration + raw run + bank + instrument
(`tools/e16/b71_coherence.py`) + the working-layer copy
(`tools/lean/CoherenceShadow.lean`) + the staged merged construction + the
fourth-visit registration's dated addition + this report. SIDE-global-section:
`CoherenceShadow` + row 51 + regenerated prints (215/215) + README. PLACE-papers:
the act record + the poster's dated addition. Mirror: refreshed —
**`mirror-refresh-2026-08-21-x.zip` is the one to load.***

> **PIN LINE (post-push read-back, ls-remote):**
> `SIDE-global-section origin/main = f65d884bc211819efc0ef07f3e6b68e2e9ad2366`
> (the act commit; tag `v0.1.0` = peeled `706a81b` unmoved) · `PLACE-papers
> origin/main = 7b75f11368c022c3c975cebd9154e1531a8d4bbc` (parent `0f794af`) ·
> `relay origin/main = 372994a41e607680e0a21e9827ac8320c3b1f1ea` (this act, parent
> `d631d97`; pushed from `push-coherence` per Rule 4.10, hook standing — the hook
> refused the main-branch push and the push-branch push went through; HELD
> ancestry clean; the HELD commit's two carrier-build act files verified ABSENT
> from the public tree by name, count 0). **MIRROR:**
> `mirror-refresh-2026-08-21-x.zip` built at PLACE-papers `7b75f11`. *This
> pin-line commit's own SHA is stated in the closing message, per the regress
> rule.*

## FOOT

**The staged list after this act: the S1 line (now: the sign bit +
H-COH-or-replacement + the invariance clause, owners named); the global coherence
construction (H-COH's owner, unbuilt); THE RADIALIZATION (the merged construction,
two clients); the {V_n} convergence question; the index face's derivation. The
desk is empty; the author's project mirror should be at this act's export
(`-x`).**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED. NOTHING
DEPOSITS. NOTHING CIRCULATES.**
