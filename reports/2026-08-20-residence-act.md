# THE RESIDENCE ACT — RULINGS 1 AND 2, EXECUTED UNDER THE ARCHITECTURE PRINCIPLE
## ### **SIDE-global-section v0.1.0 IS PUBLIC: CORE 114/114 ZERO-AXIOM RE-PRINTED AT ASSEMBLY (THE COUNT RECONCILED EXACTLY: 103 + 11), INTERFACES AT THE CLASSICAL PROFILE DECLARED NEVER LOAD-BEARING, THE CORRESPONDENCE SPINE COMPLETE AT 28 ROWS WITH NO BLANK CELLS — AND THE TWO MONOGRAPH SENTENCES ARE IN THE LIVE MONOGRAPH, PURE ADDITIONS AT WORD LEVEL. THE AUTHOR'S DESK IS CLEAR OF RULINGS.**

**Relay report · 2026-08-20 · ferry-executed (part 1 of 1, receipt confirmed, Rule 1) ·
Rules 3/4/5 · the repository creation is the ruled public act and the only one · the Zenodo
deposits untouched · no new mathematics claimed; every terminal ships at its already-verified
state, re-verified at build · nothing deposits.**

> ### **RULE-3 LOG:** *(a) the architecture ruling — executed as stated: one subject-kernel
> repository (superseding the earlier two-repo pair proposal — the interfaces live INSIDE as
> declared interface files; recorded as the ruling's own shape, not a deviation); the
> load-bearing core carries no axioms at all; the correspondence table is the organizing
> spine. (b) the two monograph sentences — inserted from the `9875578` filing; the filing
> filed locus (a) as a description whose content-words are carried verbatim into the sentence,
> and the §25.8 row's pointer is filled with the repository name and pin — BOTH ADAPTATIONS
> DISCLOSED here as the filing's own clause anticipated. (c) EVERY axiom print re-run at
> build, never carried from memory: Core 114/114 via the generated `AllPrints.lean`
> (`AXIOM_PRINTS.txt`); Interfaces via `lake env lean` against mathlib4 `cecd0c4d56`
> (`AXIOM_PRINTS_INTERFACES.txt`). The Rule-4.12 census over the whole repository FIRED at
> two Interface stubs (`True := trivial` named-debt shells, LocalLimit) — graded SHELL,
> row 28 of the spine, said so rather than smoothed. Terms coined: none. Banned-stem grep:
> clean. One tooling note, disclosed: the assembly used `git add -A` in the NEW repository's
> root commit — the Rule 4.9 ban is scoped to the relay and does not bind a fresh
> single-purpose tree; stated, not hidden.*

---

## §1 — THE REPOSITORY (component 1–2)

**`github.com/psinary-sketch/SIDE-global-section` — public, created under the federation
organization. Tag `v0.1.0`: annotated object `ffc7377`, peeled commit = HEAD = local = remote
= `706a81b9e329e220a6448b4296e5cc42c9433670` (SHA-triple verified by ls-remote).**

| layer | contents | verification at build |
|:--|:--|:--|
| `Core/` | 19 vanilla modules + 15 AxiomCheck audit files; **114 terminals** — the 103 construction-era + the 11 purity-and-distance; **the count reconciled against the corpus audit exactly at assembly** | toolchain `v4.29.1` pinned; every module compiled; `AllPrints.lean` generated over all 114 terminals: **114/114 "does not depend on any axioms"** (`AXIOM_PRINTS.txt`). No terminal failed the bar; none excluded |
| `Interfaces/` | the five Mathlib-facing files, headers declaring the profile and the never-load-bearing role | elaborated against mathlib4 `cecd0c4d56` (`v4.30.0-rc1`); every declaration at exactly `{propext, Classical.choice, Quot.sound}` except the two named-debt stubs (axiom-free because contentless — row 28) |
| `CORRESPONDENCE.md` | **28 rows, no blank cells**: statement · terminal · verbatim print · three-grade rubric grade · status; honest rows where keystone statements have no terminal (rows 25–27: the no-adelic-realization derivation, the Mathlib-leg/prose content, the named-open steps); file E the one boundary row (24) with its address (the closure protocol; `h2` at complete roster) | the Rule-4.12 census run over the whole repository; flags in the grade column; the census note at the foot |
| `README.md` | subject · **the register sentence exactly** (*"RH reduced to a single located clause, reduction machine-verified"* — never wider) · the range law · the corpus-canonical AI-disclosure line | — |

## §2 — RELAY (component 3)

`tools/lean/RESIDENCE.md` added: the working-layer copies point at the repository and tag;
the layer's role restated (new work lands in the working layer first, moves to residence by
tag, never by drift). **The relay's charter strain — verified kernels living indefinitely in
a bench layer — is RESOLVED and said so.**

## §3 — THE MONOGRAPH (component 4; live line only)

Both approved sentences inserted from the `9875578` filing: **(a)** the §27.3
compression-paragraph end — the construction era's new coordinates for registers 4–5 (the
built object's E₁-pairing positive and proved; the Weil-ledger identity STATED as file E's
Prop; the wall unchanged — "the space is the wall" now has a built neighbor that is not it);
**(b)** the §25.8 live-layer concordance row at SIDE-global-section `v0.1.0` (clearly marked
NOT part of the deposited kernel, whose statuses are unmoved). **The diff: 7 insertions, 1
line-level "deletion" that is the §27.3 paragraph line re-emitted with the sentence appended —
word-level removals: ZERO, verified with `--word-diff`; a pure addition.** ERRATA untouched.

## §4 — LEDGERS (component 5)

REGISTRY: the global-section arm block (subject, tag, pin, CORRESPONDENCE-COMPLETE);
**Phase 1.1 RETURNED to COMPLETE-AS-REFINEMENT** with the two additions named. OPEN_TRAILS:
the currency question struck as RULED AND EXECUTED; **the architecture principle recorded as
STANDING LAW for future kernel residence**; **the assembly reading registered as QUEUED at
the head of the h2 line's return, after the breadth block, per the author's sequencing.**

## §5 — PINS

*SIDE-global-section: `v0.1.0` = peeled `706a81b` (ls-remote, SHA-triple). PLACE-papers
(private): `origin/main = 13d8b2f2853da58396209bad63a0437886bc0d96` (ls-remote read-back).
Relay (public): pre-act `origin/main = b629ea0`; this act's pin read back below. Mirror:
refreshed at the new PLACE-papers HEAD under the collision clause (`-f`, sixth export of the
date).*

> **PIN LINE (post-push read-back):** *appended by the pin commit after ls-remote verification.*

## FOOT

**The author's desk is clear of rulings.** Standing sequence: the Trivium pass ferry next,
then the remaining breadth block, then the h2 line's return opening with the assembly reading.

**THE REGISTER SENTENCE UNTOUCHED AND CARRIED EXACTLY. `h2` UNCHANGED. NOTHING PROMOTED.
NOTHING DEPOSITS (the repository is residence, not a deposit). NOTHING CIRCULATES BEYOND THIS
REPORT AND THE RULED PUBLIC ACT.**
