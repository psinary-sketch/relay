# v1.6 retag to the release line (main) — 2026-07-29

Author ruled Option A: base = local main `691295b`. Decisive reason — the rail papers SURROUND and PATHS
cite `Voice7Witness.hadamard_does_not_enforce_online`, which lives in `691295b`; leaving it off origin/main
would leave a dangling citation on the published line. Execute the cherry-picks, retag on main (v1.2
precedent), keep the Phase 2 feature on the branch.

## (1) Cherry-picks onto main (base 691295b), in order — all clean, no conflicts

| # | commit | onto-main SHA | result |
|:--|:--|:--|:--|
| 1 | `75668a0` Voice7 c7_forces_half deprecation | `cb13bbd` | clean, 1 file, +16/−9 |
| 2 | `3b2e8d6` tautology-sweep hygiene | `562ef21` | clean, 4 files, +23/−2 |
| 3 | `01e5633` InvarianceBarrier lemma | `8537763` | clean, 2 files, +55 (new) |

No conflicts at any step. main HEAD = `8537763`; line from v1.5 (`0e5233f`) = C₇-witness `691295b` →
Voice7-depr → hygiene → lemma. Phase 2 feature `27a3ae7` correctly excluded.

## (2) Build + verbatim verification

`lake build` on main: **Build completed successfully (3595 jobs)**, 0 errors.

Tag audit — `#print axioms` / `#check` verbatim:
```
@InvarianceBarrier.invariance_barrier : ∀ {α : Type u_1} {agree : α → α → Prop} {P : α → Prop} {x y : α},
  agree x y → ¬(P x ↔ P y) → ¬InvarianceBarrier.DeterminedBy agree P
'InvarianceBarrier.invariance_barrier' does not depend on any axioms
'Voice7Witness.hadamard_does_not_enforce_online' does not depend on any axioms      ← rail-cited terminal
```
Both **axiom-free**. The audit now confirms every terminal the rail papers cite is present on the release
line and clean. Content checks: `c7_forces_half` carries its deprecation comment (*"DEPRECATED — claiming
name retired (statement-read finding, 2026-07-29) … conclusion is `sigma = sigma`"*, `Kernel/Voice7.lean`);
hygiene renames present (`topological_equivalence_placeholder` `SimplicityRouteD.lean:74`,
`energy_positivity_equivalence_placeholder` `SimplicityRouteE.lean:82`; redirect targets `steane_parameters`
/ `conservation_of_spectra` / `F18_totient24` present). No deviation.

## (3) Tag discipline (v1.2 precedent)

- Pushed main: `origin/main` `0e5233f..8537763` (fast-forward), local = remote = `8537763`.
- Deleted stale **remote** v1.6 (`git push origin :refs/tags/v1.6`, was `6fe58c1`) and **local** v1.6
  (`git tag -d v1.6`) — never deposited, never cited.
- Re-cut **v1.6 annotated** on main HEAD, message naming contents (C₇ compiled witness; Voice7
  `c7_forces_half` deprecated per the σ=σ finding; claiming-name tautology hygiene; InvarianceBarrier general
  two-witness lemma, axiom-free).

**SHA triple (new v1.6):**
```
verified commit = 8537763b1f7c4120f2abe22e83570564e93db754
tag-object      = afaee56722c7be21f6432b28bc5c6773aa6aad9f
peeled          = 8537763b1f7c4120f2abe22e83570564e93db754   (peeled == verified ✓, annotated ✓)
remote refs/tags/v1.6     = afaee567…  (tag-object)
remote refs/tags/v1.6^{}  = 8537763…   (peeled == main HEAD; local = remote)
```
**Old pair gone from refs/tags/v1.6:** old commit `01e5633` — GONE; old tag-object `6fe58c1` — GONE.

## (4) derivative-engine divergence

`git cherry main derivative-engine`: the three corrections (`75668a0`, `3b2e8d6`, `01e5633`) read `−`
(patch-equivalent to the cherry-picks now on main); only **`27a3ae7` (derivative-engine Phase 2)** reads `+`.
**derivative-engine now diverges from main by content in the Phase 2 feature alone**, as ruled — it keeps its
Phase 2 commit and will merge on its own schedule.

## (5) FINDINGS

`F.2026-07-29-f` recorded (PLACE-papers `6c280fd`): the retag and its reason (release tag on a feature
branch bundled unmerged features and stranded a rail-cited terminal off the release line — a dangling
citation on the published branch); the published-vs-local main discrepancy as a **recurrence of the
two-line-canon law** (reviewer's ruling cited `origin/main` while local was one commit ahead; the executor's
STOP caught it); and the **standing check** — before any release tag, verify every paper-cited terminal is
present and clean on the branch being tagged (run `#print axioms` in the tag audit), rail papers first.

## Pins

- SIDE-kernel `main` — `8537763`, tag **v1.6** = `afaee56` (peels to `8537763`). Local = remote. Build green
  (3595 jobs); both audited terminals axiom-free. Old v1.6 (`01e5633`/`6fe58c1`) deleted from origin.
- SIDE-kernel `derivative-engine` — unchanged (`01e5633`), diverges from main by Phase 2 `27a3ae7` only.
- PLACE-papers `main` — `6c280fd` (F.2026-07-29-f). Rail empty vs baseline `11db565`.
- Nothing deposited.
