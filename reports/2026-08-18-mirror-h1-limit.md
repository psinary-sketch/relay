# THE MIRROR REBUILT · THE `h = 1` INSTANCE · THE LIMIT PRICED
## ### **THE MIRROR AT HEAD WITH THE FLAG CLEARED — AND OVER `ℚ` ITSELF: `(H1-same)` FOR THE SIGN, 95/95 EXACT — THE CLASS RESOLUTION WAS STRUCTURE, NOT THE SOURCE OF THE SIGN; WHAT IT SUPPLIED IS THE CHANNEL AND THE LEDGER SEPARATION — WITH THE `n → ∞` LIMIT REGISTERED, NOT RUN**

**Relay report · 2026-08-18 (thirty-ninth sitting) · author-called ("the ripe fruit in order") ·
nothing deposits · nothing circulates**

> ### **THE SCOPE GUARD, RIDING EVERY LINE: the `h = 1` instance is the instance nearest RH proper —
> finite place sets, finite cutoffs, a model; NOTHING about the Hypothesis. The corrected stop in
> force; the register untouched.**

```
mirror        mirror-refresh-2026-08-18.zip @ df6cdf2 (PLACE-papers HEAD) — 23 entries (22 roster +
              MANIFEST), probes PASS (count · source-HEAD line · REGISTRY carries the relay-modules
              row, zip-vs-source md5 match) — THE ROSTER FLAG IS CLEARED
instruments   tools/e16/b31_h1_instance.py (registration banked BEFORE the run) ·
              data/b31_registration_2026-08-18.txt · data/b31_2026-08-18.txt · 95/95 EXACT
registered    data/limit_registration_2026-08-18.txt — the n → ∞ limit PRICED, NOT RUN
```

> ### **MIRROR-REFRESH INSTRUCTION, ON THE REPORT'S FACE: load `mirror-refresh-2026-08-18.zip @
> df6cdf2` into project files before any reviewer reasons about paper content — the MANIFEST's md5 +
> last-commit columns are the reviewer's authority.**

> ### **RULE-3 LOG:** *sittings 17–18 at `ab79acd` ✓; the banked eigen-dims re-derived in-run and
> asserted ✓. ONE REGISTRATION ARITHMETIC CORRECTED IN-RUN, AS THE REGISTRATION'S OWN CHECK CLAUSE
> ANTICIPATED: the `{2:2,3:2}` class dim was written 208 and computes to 144 (longhand printed
> in-run: `32+32+48+32`); the correction fired its NOTE line and 144 is used throughout. Terms
> coined: none.*

---

## §1 — THE `h = 1` INSTANCE: ### **`(H1-same)` FOR THE SIGN, WITH THE `(iv)` READING BANKED BESIDE IT**

*Over `ℚ` (Cl trivial, `ℂ[Cl] = ℂ`, `τ = ε`), places `{∞,2,3}`, cutoffs to `n = 2`:*

| cell | dim | class dim | T-fixed negatives | full-pairing negatives |
|:--|--:|--:|--:|--:|
| `{2:1}` | 1 | ### **0 — the DEATH SURVIVES `h = 1`: it was never a class phenomenon** | 0 | 0 |
| `{2:1,3:1}` | 4 | 1 *(the same conjugate sector `(i,−i)` as at `h = 3`)* | ### **0** | 1 |
| `{2:2,3:1}` | 36 | 9 | ### **0** | 9 |
| `{2:2,3:2}` *(cutoff-capped, declared)* | 576 | 144 | ### **0** | 144 |

1. ### **POSITIVITY HOLDS BY THE SAME MECHANISM: `B|_class = ‖v‖² × (the scalar `a_N = 1`)` —
   positive-definite, certified exactly (rational-SPD factorization, direct at dims 1 and 9; the b20
   fallback's irrational-pivot caveat again in force and again reported).** *The coupling scalar is
   the norm-`N` ideal count of `ℚ` — exactly one ideal per norm — enumerated in-run.* ### **THE CLASS
   RESOLUTION WAS STRUCTURE, NOT THE SOURCE OF THE SIGN.**
2. ### **WHAT THE RESOLUTION SUPPLIED, NOW MEASURED BY ITS ABSENCE: (1) THE CHANNEL — at `h = 1`
   the antipode-anti-invariant part is dim 0, so T-FIXED NEGATIVES DO NOT EXIST AT ANY CELL (0
   everywhere, by enumeration): the arrival-edge negativity-in-the-flipped-channel is a
   CLASS-RESOLUTION phenomenon; the full pairing's `λ`-product `= −1` negatives persist off the
   fixed space. (2) THE LEDGER SEPARATION — `τ = ε`, B5's selection principle vacuous.** *(H1-collapse)
   did not occur for the sign; (H1-third) not needed.*
3. *Per-factor certificates re-asserted at all four factors — including a strengthening beyond the
   b20 precedent, disclosed: `conj(G) = G·Π` verified FULL at `(3,2)` (the agent added exact
   conjugation over `ℚ(ζ₈₁)`; b20 untouched), and `M² = Π` there derived from a new exact `F² = P`
   line (all 81 geometric sums checked).*

## §2 — THE LIMIT, PRICED AND REGISTERED (NOT RUN)

*`data/limit_registration_2026-08-18.txt`: the `L²(ℚ₂)`-closure of the tower's union — three survival
questions with both branches written (the transform survives by closure-of-unitary; the pairing's
radical stays zero by invertibility, with the Hermitian part's persistent half-kernel registered as
STRUCTURE beside nondegeneracy, neither promoted into the other; the compression stays FORCED by
witness-persistence, with the compressed operator's limit spectrum NAMED as the open analytic
question). The Mathlib companion's spec extended: the genuinely missing piece is the Fourier
transform on `ℚ_p` with Plancherel — named as such. Scope in advance: ONE place; the double limit
untouched. Price: ~1 proof-search sitting.*

## §3 — THE RECORD

*Correspondence rows 53–55; the packet kept current (`§2` gains the `h = 1` line; the reviewer
question 3 gains its first measured answer-fragment: the sign's mechanism is not `K`-specific — the
CHANNEL is); REGISTRY's relay-modules row gains the `b31` pointer clause; pins below.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING AT COMPLETE ROSTER. NOTHING DEPOSITS.
NOTHING CIRCULATES.**
