# b176 — THE THIRTY-SIXTH SEAM CLOSE AND THE UNBLOCKED READS

**Registration `c5c83db`, banked after the owner reads and the source read, before the verdicts
were filed.** 2026-08-26. Ferry part 1 of 1, receipt confirmed in full.

> ### **THE EXTERNAL ARC IS ABSORBED AT KEYSTONE v2.36, AND BOTH DOCUMENT-BLOCKED QUESTIONS ARE
> SETTLED.** *Core `302 → 304/304`, row 79. Both reads done **at the PDF**, not at a rendering.*

---

## COMPONENT 1 — THE KEYSTONE INCREMENT, **v2.35 → v2.36**

New **§41, *The external arc***; closing inventory renumbers to **42**. Version call the
executor's per precedent; the navigator recommends the same and **the agreement is recorded**.

The section carries the founding citation's non-resolution and **the single work behind three
lanes**; the PDF read with ### **what it corrected and what it confirmed as still true**; the
term-by-term agreement **with its weight in the same breath**; the two minted laws; the hazard
with its numbers; and the sweep's yield with its honest remainder.

> ### **AND THE SENTENCE THE FERRY REQUIRED, WRITTEN SO IT CANNOT BE MISREAD: THE AGREEMENT IS
> NOT THE PROJECT'S FIRST EXTERNAL CONFIRMATION.** *The record is named beside it:* **b134's
> completeness referee matched `Σ λ(n)² = 2(Si(4π)/(4π)+1)` — an identity quoted from Selecta —
> to better than `1e-6`**; and **b130 read a Landau–Widom asymptotic at content and then ruled
> it INAPPLICABLE**, this layer's `c` being fixed at `2π` while the statement is a `c → ∞`
> asymptotic. ### **A corpus that had never met an external number would be in a different
> position; this one has met several, and has refused one.**

---

## COMPONENTS 2 AND 3 — THE TWO READS

*Extracted from the loom at run time:*

### **(v-novies) THE TWO UNBLOCKED READS, SETTLED — filed 2026-08-26 (b176)**

> ### **THE `R2` DOMAIN: SETTLED-TWO-SIDED.** `Theorem 4.7`, quoted at the PDF: *"Tr(ϑ(f)S) =
> W∞(f) + ∫ f(ρ)ε(ρ) d\*ρ, **∀f ∈ C_c^∞(ℝ\*₊)**, where **ε(ρ⁻¹) = ε(ρ), ρ ∈ ℝ\*₊**, and with
> ε(ρ) given, **for ρ ≥ 1**, by (14)."* ### **The integral runs over all of `ℝ\*₊`; `ε` is
> extended below 1 by its own stated symmetry.** ***That is exactly b36's registered "THE FACTOR
> 2 (symmetric extension to ρ < 1)", taken when "the banked statement's one-sided/two-sided
> domain is not on disk".***
> ### **b36's CHOICE IS CONFIRMED CORRECT — and both halves are said: a registered choice
> vindicated is still a choice that was registered because the record did not know.**
> ### **AND IT DOES NOT RAISE `R2`'s GRADE BY ITSELF.** *It fixes one of two uncertainties; the
> truncation at `ρ_max = a²` is the corpus's own and the source says nothing about it.*
> ### **A SETTLED DOMAIN IS NOT A SETTLED IMPLEMENTATION.**

> ### **THE `Lemma 5.2` NOTE.** Its domain is *"ρ ∈ (1,2]"* at content — ### **confirming the
> corpus's banked line exactly**, so b36's caveat that *"every cell with `a² > 2` rides that
> warrant"* was correctly scoped. The re-derivation's stated reason — *"NOT ON DISK"* —
> ### **no longer holds; the re-derivation stands at its own grade, undisturbed.**
> ### **AND THE TWO FORMS AGREE TERM FOR TERM:** expanding `D_u(f)(x) = x·f′(x)` in the source's
> boundary terms gives `ρ^(−3/2)ξ′(ρ⁻¹)ξ(1)` and `−ρ^(3/2)ξ(1)ξ′(ρ)` — ### **the corpus's
> implemented terms exactly.** *Decided in Core, together with the fact that without that
> expansion they do NOT match — so the agreement is a fact about `D_u`, not about notation.*

> ### **THE ONE DIVERGENCE IS IN THE POINTERS, NOT THE MATHEMATICS:** the corpus cites *(85)*
> and *(100)*; the PDF carries **(84)** and **(98)**. ***A version difference, which the
> corpus's own header anticipated by naming its numbering source.*** ### **AN EQUATION NUMBER
> IS NOT STABLE ACROSS VERSIONS, SO CITING "eq (100)" WITHOUT A VERSION IS THE SAME FAILURE
> CLASS AS CITING "Lemma 5.4" WITHOUT A WORK.** *Filed as the name-identity law's fifth
> instance.*

### **THE STANDING ITEM: THE 117 UNRESOLVED REFERENCES — owner: the author**

*The sweep located 127 external-reference items and resolved ten.* ### **The remaining 117 are
carried forward as a standing item, with the enumerator (`relay/tools/citation_sweep.py`) as the
means of re-locating them** — and with its own limit restated: ### **the narrowed class still
over-reports, so 117 is an upper bound on a locator's output, not a count of defects.**

### THE SOURCE'S OWN WORDS, ON WHICH BOTH TURN

**Theorem 4.7**, quoted from the PDF: *"The functional Tr(ϑ(f)S) is positive and one has
Tr(ϑ(f)S) = W∞(f) + ∫ f(ρ)ε(ρ) d\*ρ, ∀f ∈ C_c^∞(ℝ\*₊), where ε(ρ⁻¹) = ε(ρ), ρ ∈ ℝ\*₊, and with
ε(ρ) given, for ρ ≥ 1, by (14)."*

**Lemma 5.2**, quoted: *"we concentrate on the positive real axis and we only consider the
scaling parameter ρ ∈ [1,2]. **Lemma 5.2** … for ρ ∈ (1,2], one has (Qk)(ρ) = ρ^(1/2)∫(D_uξ)(x)
(D_uζ)(ρx)dx + ρ^(−1/2)(D_uξ)(ρ⁻¹)ζ(1) − ρ^(1/2)ξ(1)(D_uζ)(ρ)."*

**And b36's own symmetry check, which the two-sided conversion needs and which b36 ran:**
R1, quoted — *"A_n(1/λ) = A_n(λ), **checked in-run**"*.

---

## THE KERNEL

`ReDerivationShadow`, two terminals, zero axioms. ### **The module decides that a re-derivation
and its source WRITE THE SAME THING** — not that either is right — and its second terminal shows
that **without the `D_u` expansion they do NOT match**, ### **so the agreement is a fact about
`D_u`'s argument factor and not a coincidence of notation.**

*One build defect recorded: `List.get!` is absent from vanilla Lean and the first build said so;
rewritten with `drop`/`head?`.*

---

## THE SEATS, SCORED

**NAVIGATOR:** the increment at v2.36 (concurred); C2's rubric; C3's note — all delivered.

**EXECUTOR: all five.** (e1) SETTLED-TWO-SIDED with both halves said — **lands**. (e2) ### **the
settlement does not raise `R2`'s grade by itself — lands**, and it is the part most likely to be
over-read, which is why it was registered. (e3) no mathematical divergence, one pointer
divergence — **lands**. (e4) Core enters on the **comparison** — **lands**. (e5) the agreement
must not read as a first — ### **held**, with b134's and b130's instances named in the keystone
section itself, not in a footnote.

---

## THE THIRTY-SEVENTH SEAM'S DEBT

### **The 117 unresolved references — carried forward as a standing item, owner: the author**,
with `relay/tools/citation_sweep.py` as the means of re-locating them and its limit restated:
### **117 is an upper bound on a locator's output, not a count of defects.**
### **The Q-route `Ψ` instability, still unlocated.** b157's six findings; the three front-door
items; the dominance second asking, not spent; the tense hazard; the bra-ket convention;
Prop 5.5's hazard; ### **the b123 trim table, standing available for your project-side
deletions**; the methodology day; the deep items.

### **DISCHARGED THIS ACT:** the external arc absorbed; the `R2` domain settled; the `Lemma 5.2`
question closed at content.

---

## THE AUDITS — EMITTED, NOT TYPED

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b176
  run at    : 2026-08-26T09:18:45 (local)
  input     : whole file b176_registration_2026-08-26.txt (created this act)
  stems     : gap, blind
  files     : 1
  lines     : 94
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 63b3f8e3bea99f28c7b8eae6f4fc01a8
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b176-docs
  run at    : 2026-08-26T09:18:45 (local)
  input     : added lines in D:/MY-DOwnloads/PLACE-papers vs HEAD
  stems     : gap, blind
  files     : 2
  lines     : 158
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 5ed7e31f0f853721809780e66496722a
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : mirror_verify
  act       : b176
  run at    : 2026-08-26T09:21:45 (local)
  input     : mirror-refresh-2026-08-26.zip
  files     : 33
  rows      : 33
  mismatch  : 0
  declared  : bc3b309
  ls-remote : bc3b3093748c
  VERDICT   : CLEAN ON BOTH CLAUSES
  self-hash : sha256/32 097b08e64aaac16ff50149fc8aee0d7d
=== END AUDIT SIDECAR ===
```

---

## PINS AT CLOSE — by `ls-remote`, never from recall

| repository | pin |
|:--|:--|
| `PLACE-papers` `main` | `bc3b309` — keystone v2.36, §41; the loom's settlements |
| `SIDE-global-section` `main` | `7552278` — ### **Core 304/304, rows 1–79** |
| `relay` `main` | *the pin line below, read back after the push* |
| mirror | `mirror-refresh-2026-08-26.zip` — **33 files**, rebuilt at `bc3b309`, **CLEAN ON BOTH CLAUSES** |
| HELD | `held/carrier-acts` = `6eada6a` — **LOCAL-ONLY, unpushed** |

**Load this export:** `mirror-refresh-2026-08-26.zip`.

*STOP — the ferry's end.*
