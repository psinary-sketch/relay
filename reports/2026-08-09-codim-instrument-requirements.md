# `W-ORD-CODIM-INSTRUMENT` — THE POSITIVE-CONTROL READ AND THE REQUIREMENTS TABLE — 2026-08-09

**Expectations E1/E2/E3 were registered and committed at `286a997` BEFORE this reading began.**
Rail `de621b1` / `2147a03` **unmoved**. **Nothing deposits.**

---

## §0 — A FINDING ABOUT THE WORK-ORDER ITSELF, STATED FIRST

**Much of this work-order is already discharged by the existing record, and I did not know that when
I priced it at "~1 sitting".** `OPEN_TRAILS` **UAC-1** and its **rider (b)** already carry the
cohomological translation in full. **My contribution here is assembly and grading, not discovery** —
and the ferry's own framing needed one correction against that record, below.

---

## §1 — COMPONENT 1: THE 𝔽_q CROSSING AS AN INSTRUMENT SPEC (classical, at cite)

**THE FERRY'S FRAMING CARRIES A SUPERSEDED READING, AND THE CORPUS ALREADY CORRECTED IT.** The ferry
names *"the pairing target H¹×H¹→H²"* as what the crossing consumed. Quoted from the record:

> *"**Attribution verified — and corrected.** Weil's 1948 positivity is **intersection-theoretic on
> divisor classes of C × C** (Hodge index ⇒ Castelnuovo–Severi; Frobenius graph and diagonal as
> correspondences), **not "on finite-dimensional H¹"** as the sitting's framing had it — **the H¹
> reading is the later Mattuck–Tate/Grothendieck cohomological formulation.**"*

**Both readings are in the corpus and they are not the same instrument.** The requirements below are
taken from **Weil 1948 as corrected** (divisor classes on a surface), with the cohomological
restatement given alongside because rider (b) uses it as the external-legibility translation.

**WHAT THE AMPLIFICATION MOVE CONSUMED — four requirements, each with its cite:**

| # | requirement | what it is, at cite |
|--:|:--|:--|
| **R1** | **A SURFACE — `C × C`** | the **self-product of one curve**, carrying divisor classes; *"Frobenius graph and diagonal as correspondences"* |
| **R2** | **A CODOMAIN FOR THE PAIRING** | *"H¹(C)×H¹(C) → H²(C) ≅ ℚ_ℓ(−1) is the intersection pairing, Weil-positive"* — rider (b) |
| **R3** | **A POSITIVITY SOURCE ON THAT SURFACE** | **Hodge index theorem ⇒ Castelnuovo–Severi inequality** |
| **R4** | **A TRACE FORMULA** relating the spectrum to the counting | Lefschetz/Frobenius point-counting; the corpus's own bridge: *"the Connes trace formula = the Riemann–Weil explicit formula unifies them"* |

**And the criterion the instrument verifies:** *"Weil 1952's criterion (RH ⟺ positive-definiteness
of a generalized function) is verified, **including that over finite fields the positive-definiteness
provably holds**."*

---

## §2 — THE TWO-COLUMN REQUIREMENT TABLE

| requirement | **what the 𝔽_q crossing needed** | **ℚ-side holding** | status |
|:--|:--|:--|:--|
| **R1 surface** | `C × C`, a **self-product** | *"no `Spec ℤ × Spec ℤ` / no 𝔽₁ / no arithmetic surface; **Arakelov is the wrong surface** — Face D control `85c9e5d`"* | **ABSENT** |
| **R2 codomain** | `H²(C) ≅ ℚ_ℓ(−1)` | *"**no H² receives it** … **the pairing has no codomain**"*; **Δn₄ = +1 IS that missing H² in formation coordinates** | **ABSENT**, and *quantified* — the interface-count difference is exactly one |
| **R3 positivity source** | Hodge index ⇒ Castelnuovo–Severi | nothing; there is no surface on which to have a Hodge index | **ABSENT** — and it is *downstream* of R1, not independent of it |
| **R4 trace formula** | Lefschetz / Frobenius counting | **the Guinand–Weil explicit formula — CLASSICAL OVER ℚ AND ALREADY POSSESSED**; Connes' trace formula is its operator form | **PRESENT** |
| *criterion* | RH ⟺ positive-definiteness of a generalized function | **possessed over ℚ** (Weil 1952 is stated over ℚ); what is absent is its *verification*, not its statement | **PRESENT (statement) / ABSENT (verification)** |
| **partial** | — | `h1_complete_at_Phi` — eight coupling facts at Φ; `partialPositivity_finiteRange` — the clause **proved to `N₀(T)`** under three named premises | **PARTIAL — see E3 below** |
| **partial** | — | Chapter 13's derived conservation: the channels are **sealed** and the interface is **s-dark** | **PARTIAL — supplies sufficiency of the catalogue, not definiteness** |

---

## §3 — COMPONENT 2: THE DIAGNOSIS GRADED — **MIXED**

**The three named absences were: (1) an ensemble to move in · (2) an operator/cohomology realizing
the pairing · (3) a supply of definiteness at the multiplicative place.**

**E1 — CONFIRMED ON TWO OF THREE.** Absence (2) maps exactly onto **R2** (no codomain, Δn₄ = +1).
Absence (3) maps exactly onto **R3** (no Hodge index, because no surface).

> **E1 — CORRECTED ON THE THIRD, AND THIS IS THE READING'S SUBSTANTIVE RESULT. Weil 1948 did not
> consume "an ensemble to move in." It consumed `C × C` — the SELF-PRODUCT OF ONE CURVE.** An
> ensemble is not a self-product, and the two are obtained by different means.
>
> **"The missing family" is the ℚ-obstruction of the GEOMETRIC clause (§24.4: Katz–Sarnak monodromy
> is "generic over a family, not universal"; "ξ is a single object, with no ensemble to
> equidistribute"). It is NOT the obstruction the arithmetic clause faces.** The arithmetic clause's
> first absence is **a surface**, not a family.
>
> **Consequence: the absence list for the arithmetic clause should read — a SURFACE (self-product) ·
> a CODOMAIN for the pairing · a POSITIVITY SOURCE on it. The "ensemble" item was imported from the
> other clause.** Given the two-clause structure only settled in pass 2 sitting 2, this is exactly
> the kind of cross-contamination that structure predicts.

**E2 — FIRED, and in the corpus's favour.** **R4, the trace formula, is a requirement the 𝔽_q
instrument consumed and the ℚ side ALREADY POSSESSES**: the Guinand–Weil explicit formula is
classical over ℚ, and the corpus's own bridge says *"the Connes trace formula = the Riemann–Weil
explicit formula unifies them."* **Weil 1952's criterion is likewise possessed as a statement.**
**So the ℚ side is not missing the machine that turns spectra into counts — it is missing the space
the pairing lives on.** Filed first-class as a correction to the diagnosis: **the absence is
narrower than "the cohomological apparatus"; it is specifically the surface, its H², and the
positivity on it.**

**E3 — PARTIALS, with shortfalls in their own local terms (not one stock word):**

| object | supplies | **shortfall, in its own terms** |
|:--|:--|:--|
| `partialPositivity_finiteRange` | the clause **on `1 ≤ n ≤ N₀(T)`** | **a range boundary** — the tail `n > N₀(T)` is unreached; the shortfall is an *index range*, not a missing hypothesis |
| `h1_complete_at_Phi` | eight of the coupling facts at Φ | **a conjunct** — h2 is the ninth and is not of the same kind as the eight |
| Chapter 13 | the catalogue's **sufficiency** (nothing external speaks) | **a modality** — it establishes *no other force*, never *this force has this sign*. Sufficiency is not definiteness |
| `certifiedInput_not_zeroRealizing` | a **compiled negative** on the certified input `{n²}` | **a witness, not a theorem** — it rules out one spectrum, not the class |
| R5's schema | the *shape* of a Hilbert–Pólya realization | **a strength** — *"a diagonal multiplication operator still witnesses the schema; the schema is not the theorem"* |

**VERDICT: MIXED.** Two absences confirmed and sharpened, one corrected out of the arithmetic
clause's list, one requirement discovered to be already possessed.

---

## §4 — THE REQUIREMENTS DOCUMENT (the deliverable for the construction programme)

**To close the arithmetic clause by the 𝔽_q instrument's own route, the ℚ side would need, in
dependency order:**

1. **A surface** — an arithmetic self-product playing `C × C`'s role. *Known-negative:* `Spec ℤ × Spec ℤ` does not exist in the category; **Arakelov is the wrong surface (Face D control `85c9e5d`)**; 𝔽₁ is not available.
2. **A codomain** — an `H²` for the pairing to land in. **Quantified as Δn₄ = +1**: the interface count differs by exactly one.
3. **A positivity source on it** — a Hodge-index analogue. **Downstream of (1); not separately attackable.**
4. *(already held)* **the trace formula** and **the criterion's statement**.

> **The dependency order is the document's most useful content: (3) cannot be attempted before (2),
> and (2) cannot be attempted before (1). The construction programme has ONE entry point, not three,
> and it is the surface.**

---

## §5 — THE EMPIRICAL-LIMIT CHIASMUS (Tier N, filed, no promotion)

> **The channel's calibration family is Euler-product-free BECAUSE off-line zeros are only KNOWN
> there.**

The instrument is validated on Epstein objects precisely because Davenport–Heilbronn supplies known
off-line zeros — and D–H objects are Euler-product-free **by the same fact that gives them off-line
zeros** (`h > 1` ⟹ no Euler product). **So the instrument's blind spot is not an accident of which
objects were chosen: it is forced.**

**Restated operationally, this is the discreteness result: a positive control on the multiplicative
side would be an Euler-product-bearing object with a known off-line zero — i.e. a counterexample to
RH. A multiplicative-side positive control exists only if RH is false.**

**Consequence for strand 2, stated so it is not discovered later: the empirical programme can
calibrate the detector forever and never test condition (i).** Cross-linked to the discreteness
filing, the channel's validation record, and pass 2 §3(b)'s structural limit. **No promotion.**

---

## §6 — STOP-AND-HOLD

**This work-order graded a diagnosis and drafted a requirements list. It did not approach a
derivation.** The one substantive result is a **correction to an absence list** — an accounting
finding about which clause an obstruction belongs to. **No mathematics was attempted, and nothing is
held back.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `286a997` (registrations) → this pass's commit |
| relay | → this report's commit |
| **rail `de621b1` / `2147a03`** | **UNMOVED, both clean** |

**The requirements table returns to the author for the construction programme's next ruling.
Nothing deposits.**
