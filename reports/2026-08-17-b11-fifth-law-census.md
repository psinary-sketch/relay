# CHECKPOINT `B11` — THE FIFTH EXACT LAW · THE CENSUS COMPILED
## ### **THE CORRECTION FACTOR WAS THE TRUNCATION EDGE; THE PURE WEIL WEIGHT SURVIVES EXACTLY — AND `SIDE-window` IS AT `v0.5.0`**

**Relay report · 2026-08-17 (sixteenth sitting) · author-called · `INSTRUMENT` track · sub-gate declared · item 1 bench (exact rational), item 2 kernel · the Deninger retrieval navigator-side · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 9b8fb80fd17062379b3dcf463aaabd13eb348ba5   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 9b8fb80   ### 22-file roster + MANIFEST, 3 probes PASS
SIDE-window   ### aacb415 = tag v0.5.0, PUSHED, main = tag, READ BACK   SIDE-effects  afa9ccf — untouched
```

> ### **RULE-3 LOG:** *`B10`'s proof, the four laws, and the measured ratio banked ✓ ·* ### **the
> Euler-factor reading of `(1+p^{−1})^{−1}` was `NAVIGATOR-PROPOSED` and is REFUTED by the discriminator
> this ferry itself ordered — the cycle closed within one sitting.** *Banned grep: clean; terms coined:
> none. No executor fetch of Deninger — the retrieval is navigator-side and its slot is OPEN.*

---

## §1 — ### **THE DISCRIMINATION: `F2`, THROUGH A FIFTH EXACT LAW** *(instrument `tools/e16/b11_factor.py`; exact rationals, no floats, no matrices; registration banked before any number)*

*13 cells (`p ∈ {2,3,5,7}`, `n` to the grid limit); the exact rationals banked; the denominator check ran
BEFORE the trend was read (registered order): the systematic denominator is `(p^n−1)/(p−1)`; the `p+1` at
`(2,2)` was the `p = 2` coincidence `p+1 = p²−1`.*

> ### **THE FIFTH LAW: `R(k) = (p−1)·p^{n+1}·(p^{n−1} − p^{k−1})/(p^n − 1)` for `k ≤ n−1`, else `0` —
> VERIFIED 63/63 VALUES.** *(`T_quot(k) = p^{−k/2}·R(k)`.)*
>
> | longhand consequence | statement |
> |:--|:--|
> | ratio law | `R(k+1)/R(k) = (p^{n−k}−p)/(p^{n−k}−1)` — **`→ 1` as `n` grows: TRUNCATION (`F2`)** |
> | ### **the edge** | *at `n−k = 2` the ratio is exactly `p/(p+1)`* — **`B10`'s `√p/(p+1)` was this law's truncation EDGE, not a local factor** |
> | ### **the limit** | ### **`T_quot(k)/dim V_inv → p^{−k/2} = q^{−1/2}` EXACTLY — the orbit-normalized quotient trace carries the PURE WEIL WEIGHT, no correction** |
>
> ### **THE VERDICT IS CLEANER THAN EITHER REGISTERED BRANCH: the whole `k`,`n`-dependence is one closed
> form, and the Weil weight emerges exactly rather than approximately.**

## §2 — ### **THE CENSUS COMPILED — `SIDE-window` `v0.5.0`** *(`aacb415`, pushed, main = tag, read back)*

*`SIDEWindow/LocalModel.lean`, vanilla Lean 4 `v4.29.1`, decide-only, in the repository's own idiom
(honest not-compiled declarations; per-module axiom check):* **(F)** fixed-point count `= 1` — *the
arithmetic heart of the absorption proof and the localization* · **(A)** absorption count `= p^n` ·
**(C)** ball closure · **(O)/(E)** off-ball and edge counts — *orbit count `(p−1)p^n` as `V − E`, the
forest-components lemma DECLARED not compiled* · **(V)** the laws' integer identities — *the Sonin split,
the diagonal law, and the fifth law in cross-multiplied form at the banked cells.*

> ### **`AxiomCheckLocalModel`: 26/26 TERMINALS "does not depend on any axioms".** *The non-claims travel
> in the docstrings: the analytic identifications are bench-grade at `relay`; nothing bears on `h2`.
> `maxRecDepth` raised for the `N = 256` cells — an elaboration limit, no axiom, said in place.*

## §3 — THE DENINGER SLOT: ### **OPEN** *(the retrieval is navigator-side; the `B10` Lefschetz
match-in-shape waits on the text, and nothing here pre-judges it). The tensor check stays HELD.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE CORRECTION-FACTOR QUESTION IS CLOSED BY A LAW, NOT A FIT: the factor was the truncation
   edge, and the pure Weil weight `q^{−1/2}` is EXACT in the limit.** *Rule 3's navigator-proposal /
   executor-discriminator cycle closed within the sitting.*
2. ### **THE MODEL'S CENSUS IS NOW KERNEL-GRADE: five laws' arithmetic compiled, 26/26 axiom-free, tag
   `v0.5.0` pushed.** *Road B's model work is banked at both grades — bench (exact rationals) and kernel
   (decide).*
3. **Standing after `B11`:** *the orbit quotient carries the exact Weil weight; the ledger-side
   specification's `(β)` has a model-grade answer with no correction factor; what remains open on Road B:
   the Deninger text (navigator-side), the tensor check (held), and the `(α)`-positivity of any quotient
   form (unknown and said so).*

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
