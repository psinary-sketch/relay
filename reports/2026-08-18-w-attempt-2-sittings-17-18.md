# `W-ATTEMPT-2` · SITTINGS 17–18 — THE IDENTITIES COMPILED
## ### **FOUR NEW VANILLA MODULES, 30/30 TERMINALS "does not depend on any axioms" — THE ADDRESS LAW, THE ARRIVAL LAW AT NORM 150, THE FLATNESS SOLVER WITH THE TWIST SIGNATURE, AND THE TOWER'S SMALLEST INSTANCE KERNEL-CHECKED IN `ℤ[ζ₁₆]` — THE ATTEMPT'S FORMAL COUNT RISES TO 57, ALL AXIOM-FREE**

**Relay report · 2026-08-18 (thirty-seventh/eighth sittings) · author-ruled ("Lean-verify what landed
as identity") · vanilla Lean 4 `v4.29.1` pinned, theorem not lemma, decide not native_decide, axiom
print per terminal · nothing deposits · nothing circulates**

> ### **RULE-3 LOG:** *sittings 14–16 at `7386b47` ✓; the six targets as sized there ✓; the framing
> line carried (the register line is the exact scope statement; the memo gates `WIDEN` and the held
> acts only — not research). The 0-axiom expectation was STATED IN ADVANCE for every module and MET —
> nothing needed `propext` or Mathlib in the final profiles (one intermediate `propext` leak via
> `List.getD` was caught by the axiom print and rewritten match-free, the `DiagonalSection` precedent).
> Terms coined: none.*

```
modules   tools/lean/StaircaseAddresses.lean (6/6) · CouplingArrival.lean (4/4, imports the sibling
          GroupRingGlue only) · SectorArithmetic.lean (15/15) · TowerInstance.lean (5/5)
          + AxiomCheckIdentities.lean — 30/30 "does not depend on any axioms"
running   57 axiom-free terminals across the attempt's six modules (11 glue + 16 section + 30 new)
records   REGISTRY one-address row · SPIRAL_MAP arm note · correspondence rows 49–52 · packet §2
```

---

## §1 — WHAT EACH MODULE COMPILES *(and what each declares it does not)*

1. ### **`StaircaseAddresses` — THE PRIME-POWER-ADDRESS LAW:** *over `{2,3,5}` and the grid
   `A = a² ∈ [1, 145]`: the diagonal's cell changes at `A` ⟺ `A = p^k` (the `T1` boundary as one
   decidable statement — every punctuation address is a prime-power address); the weight-death law
   (`Q²(p, n_p(A)) > 0 ⟺ A ≥ p²`, every place entering DEAD); the prime-free window (`A = 1` clean,
   first address `A = 2` — CC's window boundary in integer form); the banked apexes `L ∈ {4,16,64}`
   as address instances; the first live full weight at `A = 9`.* **Does not compile: the lag-axis
   identification (a reading, banked at sitting 16), anything at complete roster.**
2. ### **`CouplingArrival` — THE COUPLING-AS-TRACE WITH THE ARRIVAL LAW AT NORM `150 = 2·3·25`:**
   *the norm-150 label enumeration has counts `(2,1,1)`; THE ARRIVAL LAW `(2,1,1) = (c₂·c₃)·c₅` in
   `ℤ[ℤ/3]` (multiplicativity at the integer level — the Euler-exact gluing law's shadow at one more
   norm); the trace identity (spectrum `(4,1,1)`, trace 6) at 150 exactly as at 6; the added place an
   identity for the coupling.* **Does not compile: the identification with the Dirichlet coefficients
   (bench, banked).**
3. ### **`SectorArithmetic` — THE FLATNESS SOLVER AND THE TWIST SIGNATURE:** *(c) the trace-equation
   solver with FLATNESS at `p = 3, n = 1…4` (`solve dim 0 0 0 = (dim/4)⁴`, solving verified) and the
   `p = 2` contrast instances reproducing the banked non-flat dims; the field-intersection argument
   stated in the docstring, the decidable instances compiled. (d) with the banked exact eigen-dims of
   all six certified factors: the signature law `(pos, neg, zero) = (d/4, d/4, d/2)` AT EVERY BANKED
   STAIRCASE CELL (both place sets), the pre-cell `(0, 0, d)`, `pos = neg` everywhere, THE ×16
   FLAT-ARRIVAL LAW as an identity, and the class DEATH/REVIVAL dims (`0 → 2 → 18 → 1568`).*
   **Does not compile: the eigen-dims' derivation (banked bench data, registrations before runs).**
4. ### **`TowerInstance` — THE TOWER'S SMALLEST INSTANCE IN `ℤ[ζ₁₆]` (integer 8-tuples, `ζ⁸ = −1`):**
   *`ι(Son(2,1)) ⊂ Son(2,2)` KERNEL-CHECKED (support side and all four transform ball-rows exactly
   zero); the isometry's integer form; THE PAIRING'S LEVEL STABILITY ON THE NOSE — `hostGram = 16ζ⁴ =
   4·(4ζ⁴)`, b23's `S_h = 4·S_n` at its smallest cell, in the kernel; the fifth law's integers at
   levels 1–4, both primes.* ### **The `(3,·)` instances are PRICED AGAINST THE VANILLA LIMIT and the
   Mathlib companion's spec is EXTENDED to the prime-power cyclotomic towers `ℚ(ζ_{p^k})` — named,
   not faked.**

## §2 — THE RECORD

*Correspondence rows 49–52 (each compiled identity at `DERIVED` with terminal, profile, pin — the
bench rows they upgrade cross-referenced, the bench grades kept for the measured identifications) ·
the arc line extended (testing continues; the identities compiled) · REGISTRY gains the one-address
row for the relay formal modules (recorded as relay-resident, NOT a federation kernel) · SPIRAL_MAP
gains the arm note (live, not folded into any wave; the wave gate stands) · the packet's §2 count
kept current (57).* 

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED — the exact statement of scope. NOTHING AT COMPLETE
ROSTER IS COMPILED ANYWHERE, AND EVERY MODULE SAYS SO ON ITS FACE. NOTHING DEPOSITS. NOTHING
CIRCULATES.**
