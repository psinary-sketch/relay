# THE INSTRUMENT IS RESURRECTED — ### AND TWO ROWS DO NOT REPRODUCE

**Relay report · 2026-08-15 · author-called · relay-only for bench · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE.**

```
PLACE-papers  origin/main : d41df34bbd9deaf21b9967c60da9e5007a0d2e29   VERIFIED
mirror        22 / 22 rows COHERENT at d41df34
relay         origin/main : (this report)                    local +1 HELD
```

> ### **`Q_ε` ARRIVED AND THE INSTRUMENT CAME BACK.** *Three sittings ago the `δ/L` law had no bank at all.
> It now has one, banked under `relay/tools`, and it reproduces the law at all five points — while refuting one
> of the claims made about it.*

---

## §1 — THE BATTERY

### **WHAT PASSES**

| row | result |
|:--|:--|
| the prolate / `ε` substrate *(15 rows)* | ### **PASS** |
| ### **`ε(1) = 0`** | ### **PASS — exactly `0`** *(the support law: the integrand lives only on `[ρ⁻¹,1]`)* |
| ### **`Qε(1) = 0`** | ### **PASS — exactly `0`, identically** |
| `ε′(1⁺)` | `22.996476` — **and by three independent routes** *(the `t(n)` sum; the derivative of (85); finite differences on `ε`)* |
| ### **the log-3 no-prime spectrum** | ### **PASS — `1.089891 / 1.039452 / 0.684735` vs `1.089917 / 1.039477 / 0.684763`, `Δ ≈ 2.6 × 10⁻⁵`** |
| ### **branch B** | ### **FIRES — two eigenvalues exceed 1 at `log 3`, as banked** |
| parities | ### **PASS — even / odd / even at both windows** |
| ### **the `L = 3` `ω`-triple** | ### **PASS EXACTLY — `203/549`, `407/1099`, `812/2197`.** *All three counts, all three fractions, to the digit.* |
| `C` on `V` | ### **PASS — `0.34451` vs `0.34481`, three digits as the report states** |
| endpoint machine-zeros | ### **PASS** |

**Derived, not assumed:** differentiating (85) at `ρ = 1` gives `ε′(1⁺) = Σ[λ²/(1−λ²)]ξ_n(1)²` — ### **exactly
the `t(n)` that was inferred structurally two sittings ago, now derived from the supplied formula.**

---

## §2 — ### FINDING ONE: THE `L = 4` EXACTNESS IS REFUTED

**The instrument counts `692` offenders at `L = 4`, `ω = 10⁻³`. Not `693`. `0.49928`, not exactly one half.**

### **AND IT IS NOT A KNIFE EDGE, WHICH IS THE WHOLE POINT.**

| `L` | offenders | banked | boundary separation *(relative)* | reading |
|:--|--:|--:|--:|:--|
| 2.2 | 96 | 97 | `5 × 10⁻⁶` — degenerate cluster | ### **ambiguous; ±1 is noise** |
| 2.5 | 224 | 224 | — | ### **MATCH** |
| 3.0 | 407 | 407 | — | ### **MATCH** |
| 3.5 | 560 | 561 | `5 × 10⁻⁶` — degenerate cluster | ### **ambiguous; ±1 is noise** |
| ### **4.0** | ### **692** | ### **693** | ### **`1.2 × 10⁻²` — cleanly separated** | ### **NOT ambiguous. A real miss.** |

> ### **THE `L = 4` ROW WAS THE ONE THE REPORT CELEBRATED:** *"`693/1386` is exactly one half … an exact
> structural coincidence falling out of the mechanism rather than fitted to it."* ### **IT DOES NOT REPRODUCE,
> AND UNLIKE THE OTHER TWO OFF-BY-ONES IT CANNOT BE BLAMED ON A NEAR-ZERO TIE.**

### **THE LAW ITSELF IS UNHARMED — say this as loudly as the refutation.** *All five points reproduce
`1 − log2/logL` with residuals of the banked order: `+0.00095, +0.00101, +0.00127, +0.00022, −0.00072`.*
### **What falls is one decorative exactness, not the `δ/L` relation.**

*The attempt record already called this row "the least secure one" because it lies beyond the `(1,3]` debt.*
### **It now has a second, independent reason — and the two are unrelated, which is what makes the coincidence
worth striking rather than explaining.**

---

## §3 — ### FINDING TWO: `V` WAS IMPOSED FOR ONE ROW AND NOT THE OTHER

`Y₊`'s kernel factorises as `e^{t/2}·e^{−t/2}` — ### **and `e^{−t/2}` is exactly the direction `V` removes.**
So the constraint is not decoration; it is what makes the operator bounded in the way the report claims:

| | on `V` | without `V` |
|:--|--:|--:|
| `C = ‖ξ‖/‖η‖` | ### **`0.34451`** ✓ *(banked `0.34481`)* | ### **`0.88648`** ✗ |

> ### **BUT THE BANKED OFFENDER COUNTS MATCH THE *UNCONSTRAINED* GRID EXACTLY** — `203/549`, `407/1099`,
> `812/2197` — *while on `V` they are `202/548`, `406/1098`, `811/2196`.*
>
> ### **THE SAME PIPELINE IMPOSED THE CONSTRAINT WHEN COMPUTING `C` AND DID NOT IMPOSE IT WHEN COUNTING
> OFFENDERS.** *It moves every fraction by about `10⁻³` — **the same order as the residuals the law is being
> judged by**, which is why it is a finding and not a footnote.*

---

## §4 — THE LOG-2 GATE: A DISAGREEMENT, REPORTED NOT RESOLVED

| source | triple |
|:--|:--|
| rebuild *(converged in `ω`)* | `1.052020 / 0.689577 / 0.030814` |
| banked, this ferry | `1.05158 / 0.686494 / 0.0289` |
| banked, sitting 9 | `1.051772 / 0.687924 / 0.029692` |

### **THE CORPUS'S OWN TWO RECORDS OF THIS TRIPLE DISAGREE WITH EACH OTHER BY THE SAME ORDER (`~10⁻³`) AS THE
REBUILD DIFFERS FROM BOTH** — *while the log-3 triple, produced by the same code in the same run, reproduces
to `2.6 × 10⁻⁵`.*

**The natural reading:** *log-3 is the corpus's own numerics, which is why it matches; log-2 is a transcribed
published spectrum, which is why it does not.* ### **Offered as a reading, not a ruling.**

---

## §5 — THE GRADE, AND WHY IT IS NOT A FULL RESTORATION

**`δ/L`: `SUSPENDED-PENDING-INSTRUMENT` → ### `RECONSTRUCTED — THE LAW REPRODUCES, ONE CLAIM REFUTED`.**

*The ferry conditioned restoration to `MEASURED-AT-BANK` on the whole battery passing.* ### **It did not.**
*But leaving the grade at `SUSPENDED` would now be false — the instrument exists, is banked, and reproduces
the law — and `MEASURED-AT-BANK` would paper over three rows that differ.* **Full restoration awaits your
ruling on the `V` inconsistency and the log-2 disagreement.**

---

## §6 — EXPERIMENT ONE: ### HELD AT THE GATE

**The gate was "all pass". Three rows did not. It did not run, and no measured number for it appears here.**

### **BUT THE RECOMMENDATION IS TO RELEASE IT, AND HERE IS THE ARGUMENT:** *Experiment One measures the
negative fraction of `Φ` in the `η` coordinate — and that is* ### **precisely the machinery that reproduced the
`L = 3` `ω`-triple exactly, three counts out of three.** *The two failing rows live elsewhere: one is a
decorative exactness at `L = 4`, the other is a transcribed log-2 spectrum. **Neither touches the measurement
path Experiment One would use.***

**One word releases it.** *With the `V` question ruled at the same time, the sweep `L ∈ {3.2, 3.6, 4.2, 4.6,
5.5, 7.0}` under the corrected lag schedule runs in a single sitting.*

---

## §7 — APOSTOL, AT ITS HONEST GRADE ✓

**The tertiary source is retired.** `§13`'s equivalence now cites **Apostol, *Introduction to Analytic Number
Theory* (Springer 1976), Ch. 11 §11.5, Theorem 11.7** at ### **`SECONDARY-VERIFIED`** — *section and theorem
pinned via published indices;* ### **page-level read still pending a library copy, and the residual debt named
as page-only.** BIBLIOGRAPHY entry added.

> ### **THIS IS THE GRADE I DECLINED TO INVENT LAST SITTING, SUPPLIED PROPERLY.** *It beats the tertiary
> source it replaces and it does not claim a content read that nobody has made.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE INSTRUMENT IS BACK AND BANKED** — `prolate_layer` · `qeps_layer` · `phi_layer` · `battery_full`.
2. ### **`ε(1) = 0`, `Qε(1) = 0`, the log-3 spectrum, the parities, and the `L = 3` `ω`-triple all pass** —
   the last of these **exactly**.
3. ### **THE `L = 4` "EXACT HALF" IS REFUTED**, and not by a tie.
4. ### **`V` WAS IMPOSED INCONSISTENTLY** — `C` needs it, the counts were taken without it, and the difference
   is the size of the residuals.
5. ### **THE LOG-2 GATE DISAGREES WITH ITSELF** across the corpus's own two records.
6. ### **EXPERIMENT ONE HELD, WITH A RECOMMENDATION TO RELEASE.**
7. ### **APOSTOL LANDED AT AN HONEST GRADE.**

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` tip, unpushed, absence re-verified.

**`h2` UNCHANGED. NO SIGN. NOTHING DEPOSITS.**
