# W-ATTEMPT-1 — THE ONE-PRIME EXTENSION

**Relay report · 2026-08-13 · ATTEMPT track, author-opened · the five checkpoints run in order**
### **BANKED TO RELAY ONLY. NO FINDINGS ENTRY. NO REGISTER TOUCH. NO S-TABLE TOUCH. NOTHING DEPOSITS.**

> ### **THE STANDING STOP RIDES ALL FIVE CHECKPOINTS: if any synthesis appears to constitute progress on
> the clause — STOP, state at grade, hold for the author.** *It did not fire. Nothing below is proof-shaped.*

---

## CHECKPOINT 1 — THE CANDIDATE MECHANISMS, STATED BEFORE ANY WORK

**(M1) TOEPLITZ EXTENSION.** *Machinery: compression of the scaling action to a bounded window, the prolate
spheroidal basis, Hermitian-Toeplitz operator control.* The mechanism takes Connes–Consani's own frame —
which proves `W_∞(f) ≥ 0` for smooth positive-definite `f` with `supp f ⊂ (1/2, 2)` and `f̂(±i/2) = 0` — and
asks whether the `Λ = 1` argument admits a **finite-rank correction** carrying the `n = 2` term. The bet is
that a single prime entering the window is a rank-controlled perturbation of a proven positive operator.

**(M2) DIRECT DOMINATION.** *Machinery: the explicit-formula inequality, estimated term by term on the band.*
Prove `W_∞ + W_pole ≥ √2·log 2·F(log 2)` for admissible `F = g ⋆ g̃` with multiplicative support in
`(1/3, 3)`. No structural claim — a concrete analytic estimate, with the measured margin profile stating
exactly how sharp it must be.

**(M3) POSITIVE-KERNEL DECOMPOSITION.** *Machinery: Weil's own lemma mechanics run in reverse — the
Gaussian-polynomial construction used to exhibit rather than to test.* Write the band's ledger kernel as
**(manifest square) + (remainder)** and bound the remainder by the pole term.

---

## CHECKPOINT 2 — GRADED AGAINST `T1`–`T10`, AT HOLDINGS' DEPTH

| | **T1** extend past the window | **T2** the `(nonArch, unbounded)` quadrant | **T3** consume the Euler product | **T4** sign-type codim-1 silhouette | **T8** thin margin |
|:--|:--|:--|:--|:--|:--|
| **M1** | ### **PASS — native** | ### **FAIL — its NAMED arrest, at cite in the spec's candidates table** | PARTIAL | SILENT | UNREAD |
| **M2** | ### **attacks it directly** | ### **FAIL BY CONSTRUCTION — a bounded-support estimate can never reach an unbounded quadrant** | ### **FAIL — one Euler FACTOR is not the Euler PRODUCT** | PARTIAL — the `f̂(±i/2)=0` constraint **is** a codimension-one condition | ### **BINDS HARDEST** |
| **M3** | partial | ### **FAIL** | ### **FAIL** | ### **FAIL — `T4` pre-refuses exactly this: *"A technique must deliver that silhouette, not a bare inequality."*** | SILENT |

### ### **THE TABLE'S VERDICT, AND IT IS NOT THE ONE THE ACT WANTED**

> ### **ALL THREE FAIL `T2`. And the spec says what that means: *"`T2` is not one row among ten — it is the
> row every located attempt fails. That column is the residue, and it has no entry."***
>
> ### **THE SPEC'S OWN CHARTER CONDITION IS THEREFORE UNMET: *"The direct register charters when a candidate
> grades PLAUSIBLE across the table — by grade, not appetite."*** **No candidate grades PLAUSIBLE.**
> *The attempt proceeds on the author's ruling as an **explicitly sub-gate exercise**, and that status is
> recorded here rather than discovered later.*

**SELECTED BY THE TABLE: `M2`, and for a reason that is not enthusiasm.** ### **M1's `T2` failure is an
unexplained arrest — the field's best candidate stops there and no one knows why. M2's `T2` failure is
STRUCTURAL AND HONEST: a bounded-window estimate cannot address an unbounded quadrant, and that is visible
in advance rather than discovered at the wall.** *M3 is excluded by `T4` at the point of statement.*

### **`M2`'s ARREST, NAMED: `T2` AND `T3`, PERMANENTLY AND BY CONSTRUCTION.**
*No outcome of this attempt — success, failure, or partial — can move either row.*

---

## CHECKPOINT 3 — FALSIFICATION DESIGN, BOTH BRANCHES LONGHAND

**IF THE ESTIMATE FAILS:** ### **NOTHING FOLLOWS ABOUT RH. The mechanism was too weak. Said plainly, with no
residue of suggestion.** *A failed inequality on a bounded window is a fact about the inequality. It is not
evidence about zeros, not evidence about the clause, and not a "sign" of difficulty — the difficulty is
already banked at `T8` and needs no confirmation from a failed estimate.*

**IF THE ESTIMATE SUCCEEDS:** ### **IT PROVES WEIL POSITIVITY ON THE STATED CLASS ONLY — positive-definite
`F` with multiplicative support in `(1/λ², λ²)`, `λ² < 3`, `F̂(±i/2) = 0`. THE BOUNDARY MOVES FROM `√2`
TOWARD `√3`. NO MORE.**

### **THE OVERCLAIM, PRE-REFUSED IN WRITING BEFORE ANY WORK**

> ### **CROSSING ONE PRIME IS NOT CROSSING THE CLAUSE. The double limit remains, and it is DOUBLE:**
> **(i)** `λ → ∞` — every prime power must enter, not the first one; **(ii)** the class must open from
> compactly-supported correlations to the full admissible class.
>
> ### **AND THE THIRD THING, WHICH IS THE ONE MOST LIKELY TO BE FORGOTTEN: `T3`. Consuming one Euler FACTOR
> is not consuming the Euler PRODUCT. A technique that handles `p = 2` by an estimate has not begun to use
> multiplicativity — and `W-WITNESS-LATTICE` closed with the finding that multiplicativity is the row the
> placement carrier localizes to.** *An estimate that beats the `p = 2` term is `S5` work. The lattice's
> `S5`-silence result applies to it unchanged.*

---

## CHECKPOINT 4 — THE MINIMAL INSTANCE, WORKED AS FAR AS IT HONESTLY GOES

### 4.1 — The band, exactly *(elementary, checkable, and it is `T1`'s derived boundary instantiated)*

**LEMMA A.** *Let `g` be even and real with `supp g ⊆ [−log λ, log λ]`, and `F = g ⋆ g̃`, so
`supp F ⊆ [−log λ², log λ²]` and `F̂ = |ĝ|² ≥ 0`. If `λ² ∈ (2,3)` then the prime side of the explicit formula
consists of **exactly one term**, at `(p, k) = (2, 1)`.*

**Proof.** A prime power `p^k` contributes iff `k log p ≤ log λ²`, i.e. `p^k ≤ λ² < 3`. The prime powers `≤ 3`
are `2` and `3`; `3 > λ²`. Hence `p^k = 2`. ∎

**This is `T1`'s sentence made one step sharper:** *"`(1/2, 2)` is exactly the largest correlation window
containing no prime power"* — and `(1/λ², λ²)` for `λ² ∈ (2,3)` is exactly the largest containing **one**.

### 4.2 — The reduction

**Keep CC's normalization `F̂(±i/2) = 0`**, which kills the pole term and is the class their proven result
lives on. The target is then:

> ### **(★)  `W_∞(F) ≥ √2 · log 2 · F(log 2)`**  for all admissible `F`.

Both sides are **linear in `F`**, and `F = g ⋆ g̃` makes both **quadratic in `g`**: `F(log 2) = ⟨g, S g⟩`
with `S = ½(T_{log 2} + T_{−log 2})`, and `W_∞(F) = ⟨g, A g⟩` with `A` self-adjoint. So:

> ### **(★★) THE ONE-PRIME EXTENSION IS EXACTLY: `A − √2 log 2 · S ⪰ 0` on `L²[−log λ, log λ]`, restricted
> to the codimension-one subspace `ĝ(±i/2) = 0`.**
>
> ### **AND CC's THEOREM IS EXACTLY `A ⪰ 0` ON THE SAME SUBSPACE OVER THE SMALLER INTERVAL.** *The extension
> is a single explicit operator inequality on a compact interval. **That is the whole content of `M2`, and
> stating it this way is this sitting's first real gain.***

### 4.3 — The mechanism, and the trade-off it turns on

**The overlap.** `F(log 2) = ∫ g(u) g(u − log 2) du` is supported on the overlap of `[−log λ, log λ]` with
its `log 2`-translate, of width

> ### **`δ := log λ² − log 2 = log(λ²/2) ∈ (0, log 1.5)`, and `δ → 0` as `λ² → 2⁺`.**

### **THIS IS WHY `(1/2, 2)` IS PRIME-SILENT — not because `2` exceeds the window, but because the
CORRELATION at `log 2` has no room. At `λ² = 2` the single prime term vanishes identically.** *So the band
is a genuine perturbation of a proven case, and the perturbation parameter is `δ`.*

**The trade-off, stated as the mechanism:**

| regime | prime term | archimedean term |
|:--|:--|:--|
| **`g` spread on scale `≫ δ`** | ### **small** — the correlation integrates over a window of width `δ` | ordinary |
| **`g` concentrated on scale `≲ δ`** | ### **large** — `F(log 2)` approaches `F(0) = ‖g‖²` (Bochner is saturated) | ### **LARGE** — concentration at width `δ` forces frequency spread `∼ 1/δ`, and `A`'s symbol grows like `log(r/2π)`, giving `⟨g, A g⟩ ≳ log(1/δ)·‖g‖²` |

> ### **THE MECHANISM: concentration is what makes the prime term dangerous, and concentration is exactly
> what makes the archimedean term grow — logarithmically and without bound, against a prime term bounded by
> `√2 log 2 ≈ 0.9803 · ‖g‖²`.** *For `δ` small enough that `log(1/δ)` clears `√2 log 2`, the concentrated
> regime is safe.*

### 4.4 — ### **WHERE IT STOPS, AT ITS EXACT LOCATION**

> ### **THE GLUING FAILS IN THE INTERMEDIATE REGIME.** *Neither bound is sharp when `g` is spread on a scale
> **comparable to** `δ`. The spread bound gives no smallness; the uncertainty bound gives no growth.*
> ### **AND THAT IS PRECISELY WHERE THE MEASURED MARGIN LIVES — `T8` records `W = 0.018992` at `a = √2`, the
> band's own edge. The atlas is ground truth that the intermediate regime is where the functional is
> genuinely thin, and it is the regime the argument does not cover.**

**Three further gaps, each named rather than absorbed:**

1. ### **`A ⪰ 0` ON THE ENLARGED WINDOW IS NOT GIVEN.** *CC's theorem is stated on `(1/2, 2)`. `(★★)` needs `A` nonnegative **with a quantitative margin** on `(1/λ², λ²)`. The ferry's scope sentence says the concepts survive semilocally; **a concept surviving is not a constant surviving.***
2. **The uncertainty step is `STANDARD-AT-TEXT` and MINE, not corpus-cited** — the `log(r/2π)` symbol growth is classical, but its use here as a quantitative lower bound on a constrained subspace needs explicit constants that were not computed.
3. ### **`2006.13771` WAS NOT READ AT CONTENT THIS ACT.** *The reduction rests on `T1`'s corpus-relayed statement of CC's theorem — the support window, the vanishing condition, the smoothness class. **If any of those is relayed imprecisely, `(★★)` is misaimed.*** **Named upgrade: read it at content before any further work.**

### 4.5 — What is banked, at its honest size

* ### **LEMMA A** — the band admits exactly one prime power. *Elementary, complete, checkable.*
* ### **THE REDUCTION `(★★)`** — the one-prime extension is a single explicit operator inequality, `A − √2 log 2·S ⪰ 0`, on a compact interval with one linear constraint, whose `S`-free case is CC's theorem. *Complete as a reduction; **it proves nothing**.*
* ### **THE PERTURBATION PARAMETER `δ = log(λ²/2)`** and the observation that the band is a genuine perturbation of a proven case, with `δ → 0` at the lower edge. *Sharpened structure, not a bound.*
* ### **THE TRADE-OFF MECHANISM** — concentration hurts the prime term and helps the archimedean term. *A strategy. **Not a proof, and the gluing is missing.***

---

## CHECKPOINT 5 — REVIEW

> ### **NOTHING PROOF-SHAPED EMERGED. The closure protocol's first step is therefore NOT PRICED, because
> there is no sign step to formalize.** *What exists is one elementary lemma, one reduction, and one strategy
> with a located gap.*

**Returned to the author UNGRADED AND UNREGISTERED, as chartered.** *No `FINDINGS` entry was written. No
register was touched. The S-table was not touched. This report is the only artifact.*

### The honest summary

> ### **THE SITTING'S REAL PRODUCT IS `(★★)` — the recognition that the one-prime extension is a single
> operator inequality differing from a proven theorem by one rank-controlled subtraction. That is a genuine
> sharpening of the question and it is worth banking.**
>
> ### **IT IS ALSO, BY CHECKPOINT 2'S OWN TABLE, WORK THAT CANNOT TOUCH `T2` OR `T3` NO MATTER HOW IT GOES —
> and by the lattice's closing result, work on `S5` addresses, to which `S5`-silence applies unchanged.**
> **Both facts were written down before the work started, and neither changed.**

**`h2` UNCHANGED. NO SIGN. NO MECHANISM CLAIMED. NOTHING DEPOSITS.**
