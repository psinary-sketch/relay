# `W-ORD-Q0` ACT 3 — THE CONVERSION READ — 2026-08-10

**Registered at `129ade8`. Path discipline at head: `Q0` → `Q1` → the clause; NO positivity in any
trace theorem. Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — E1: THE CONVERSION'S EXACT LIST, AT CONTENT

| # | consumed | role |
|--:|:--|:--|
| 1 | `k`, a **finite field of order `q = p^r`** | the base |
| 2 | **`W`** = `p`-typical Witt vectors **of `k`** | coefficient ring `TP` lands in |
| 3 | **`ι : W → ℂ`, A CHOICE of embedding** | **where `ℂ` enters — non-canonical even over `𝔽_q`** |
| 4 | **`− ⊗_{W,ι} ℂ`** | **what is tensored** |
| 5 | periodicity element **`v ∈ TP_{−2}(k)`**, infinite cycle *"in this situation"* | **what is inverted** |
| 6 | **`Fr_q`**, split semisimple ∘ unipotent | **supplies the EIGENVALUES → the Dirichlet series** |

> **NAME-IDENTITY CAUTION planted where it will be needed: the `W` of item 2 is the `p`-typical Witt
> vectors OF A FINITE FIELD — a `p`-adic DVR. It is NOT `W(ℤ)`, the big Witt vectors this survey
> graded two acts ago. Same letter, different object.**

## §2 — E2: THE REQUIREMENT TABLE AGAINST `ℤ`

**ABSENT at 1, 2, 3, 4, 6 · UNKNOWN/ABSENT at 5** (the source asserts the infinite cycle *"in this
situation"* — the finite-field case).

> ### **E3 CONFIRMED, MORE STRONGLY THAN REGISTERED: not ONE item depends essentially on the finite field — FOUR OF SIX DO, and a fifth is asserted only there.**
>
> **Sharpest: `Fr_q` supplies the EIGENVALUES, and the Dirichlet series is built from them. Over `ℤ`
> there is no `q`, so there are no eigenvalues to form a series from.** *Act 2's delta now has its
> mechanism: the conversion's `ℂ` and its spectrum both arrive through the finite field, and neither
> survives its removal.*

## §3 — E4: THE RIDER CLOSES WITHOUT COMPLETING THE BOUNDARY CONDITION

**Reference named in full: C. Deninger, *Motivic L-functions and regularized determinants*, in
*Motives* (Seattle, WA, 1991)** — **AMS Proceedings, pre-arXiv.** Two arXiv API searches returned
nothing matching.

> **The boundary condition DOES NOT stand at full content.** It stands at **shape + hypotheses
> (held)** with the Γ-factor paper **named, venued, and declared unavailable through the search
> protocol used.** *It is now a citable reference that a library visit closes and a search cannot.*

## §4 — FOUND-BEYOND

**`ℂ` enters the working instance BY A NON-CANONICAL CHOICE** — *"let `ι : W → ℂ` be **a choice** of
embedding."* **The conversion that makes the whole `𝔽_q` story work is not canonical even where it
works.** *It sharpens the delta unexpectedly: crossing to `ℤ` would need not merely **a** `ℂ`-structure
but **a reason to prefer one** — and the working instance does not supply that reason even for itself.*

## §5 — QUEUED, NOT RUN: `W-ORD-LEDGER-RECONCILIATION`

**README + SPIRAL_MAP → REGISTRY, then all three re-verified against a live Zenodo fetch** — the
standing gate before any deposit action or external circulation.

> **Why it now has a customer:** the carrier specification v0.2 is **`DRAFT-WAVE-BOUND`** — **the
> first item in some time whose eventual circulation would cross the gate.** The reconciliation stops
> being maintenance and becomes **a precondition with a named dependent.** **Queued and unrun.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `129ade8` (registration) → `c0006e4` |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE — untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**No interpretation asserted, no route claimed, no construction attempted. `h2` exactly where it
was. Nothing deposits.**
