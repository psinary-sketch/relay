# `W-ATTEMPT-2` · SITTING 3 — `T2` PROPER ON THE GLUED OBJECT
## ### **`T2-PARTIAL`, IN THE PREDICTED FORM: THE TWO AXES COMMUTE EXACTLY AND ACT BY LOCAL FACTORS — AND THE CLASS RESOLUTION LIVES ENTIRELY ON THE PLACE AXIS — WITH THE TRUE SUPPORT AXIS IDENTIFIED AS THE DIAGONAL OF THE TOWERS**

**Relay report · 2026-08-18 (twenty-third sitting) · author-ruled · ### RELAY-ONLY — the register
untouched · nothing deposits**

> ### **THE STOP, AS CORRECTED BY THE AUTHOR THIS SITTING (restated on this head as ordered): measured
> properties of the constructed object are DATA, recorded plainly at bench grade; what is refused is any
> promotion to a statement about `W_∞ − ΣW_𝔭` at complete roster, or any movement of the register.**
> **SUB-GATE restated: no candidate grades `PLAUSIBLE` across `T1`–`T10`. The range law governed: every
> factor-law re-measured per cell.**

```
PLACE-papers  origin/main : 38b0d5b (UNTOUCHED — relay-only; no mirror claim)
relay         origin/main : (this report)                              local +1 HELD
instruments   tools/e16/b16_attempt2_s3.py (registration banked BEFORE the run:
              data/b16_registration_2026-08-18.txt) · data/b16_2026-08-18.txt
              (one unpacking bug hit mid-run, patched, full clean re-run banked — recorded)
```

> ### **RULE-3 LOG:** *sitting 2's gluing law ✓ · the inertia pattern ✓ · the `n = 2` laws ✓ — all
> banked. Banned grep: clean; terms coined: none.*

---

## §1 — `T2` STATED IN THE OBJECT'S TERMS *(checkpoint-1 registration, banked before the run)*

*(P) = the place set; (S) = the per-place cutoff. Registered: the operations act on disjoint tensor
slots, so commutation and the compose-identity should be exact — measured, never assumed; the SHAPE
question is the real one, with the per-channel prediction table and the* ### **SCOPE GUARD registered
with `T2-yes`: the model's (S) is the CUTOFF, a proxy — a yes here is `T2`-ON-THE-MODEL, never `T2`
discharged.**

## §2 — THE MEASUREMENTS

**Per-factor tower laws (eight cells, `p ∈ {2,3,5}`, `n` to 3):** *every dim and every `Q` exactly the
law (`dim = (pⁿ−1)²`; `Q = √p(p^{n−1}−1)` — including the new `n = 3` cells: `Q(2,3) = 3√2`,
`Q(3,3) = 8√3` exact); closures and unitarity at machine.*

**The per-channel table (six dense cells: mixed cutoffs `{(1,1),(2,1),(1,2),(2,2)}` at `{2,3}`; four
places at `(1,1,1)` and `(2,1,1)`):**

> ### **AT EVERY CELL: RADICAL 0 · twisted-Hermitian at machine · `H² = ℂ[Cl]` (dim `h`, invariant
> dim 2) · coupling spectrum `(4,1,1)` — INDEPENDENT OF EVERY CUTOFF (S-SILENCE, MEASURED) · inertia
> exactly `(d/4, d/4, 0:d/2)` — the twist's signature at all six cells, recorded plainly.**

**Compose / commute (measured):** *dim path-factors exactly the registered laws (`9`, `16`); both
compose orders equal the direct cell exactly —* ### **COMMUTATOR 0, EXACT** *; the weight channel
MULTIPLICATIVE exactly at the all-live cell (`2√6 = q₂·q₃`), with the `n = 1` dead-factor edge stated
(the `(p⁰−1)` death, measured before); the coupling channel S-silent at every cell.*

## §3 — ### **THE `T2` VERDICT, PER THE REGISTERED MAP: `T2-PARTIAL`, IN THE PREDICTED FORM**

> ### **WHAT THE YES-CLAUSES DELIVER: the two operations COMMUTE EXACTLY, the compose-identity holds,
> and both axes act by LOCAL MULTIPLICATIVE FACTORS — same shape at that level, on this object.**
>
> ### **AND THE SPLIT IS THE FINDING: THE `ℂ[Cl]`-STRUCTURE — coupling, `H²`, radical — IS
> CUTOFF-SILENT. THE CLASS RESOLUTION LIVES ENTIRELY ON THE PLACE AXIS; the weight and dimension
> channels are active on both axes.** *The scope guard holds in full: this is `T2`-ON-THE-MODEL, with
> the cutoff as the support proxy.*

## §4 — ITEM 3: ### **THE ARCHIMEDEAN AXIS IS THE DIAGONAL OF THE TOWERS — AND THE COMPILED RUNGS ARE ITS STAIRCASE**

*Registered longhand and then enumerated: the window bound `a` sets EVERY place's effective cutoff at
once (`n_p^eff = ⌊log a²/log p⌋`). The staircase, measured:* **`a² < 2` — prime-free (the `(1/2,2)`
window) · `2 ≤ a² < 3` — exactly one prime power (Lemma A / the `(1/3,3)` window) · `a² = 4, 9` — the
apex cells, where a tower's cutoff first reaches 2 (the apex-at-`p²`, on the diagonal).**
### **SIDE-window's compiled rungs ARE the diagonal's first steps — confirmed cell by cell.**

> ### **THE RESIDUAL `T2` QUESTION, NOW EXACT ON THE GLUED OBJECT: the FREE product (places added,
> cutoffs raised independently — where all of this attempt's objects live) versus the DIAGONAL SECTION
> (the support-forced simultaneous cutoffs — where the original window lives). The free cells are
> strictly richer than the staircase; THAT GAP is the deeper form of the four-word question — the
> support axis is the diagonal, the place axis is free, and `T2` asks whether an object exists on which
> the diagonal is not a restriction but an identity.** *Named; not attempted this sitting.*

---

## CLOSING — RELAY-ONLY, FOR THE AUTHOR'S WORD

1. ### **`T2-PARTIAL` IN THE PREDICTED FORM, with the yes-clauses' letter met on this object and the
   split as the finding: the class resolution is a PLACE-AXIS structure; weight and dimension live on
   both axes; everything commutes exactly.**
2. ### **THE ARCHIMEDEAN AXIS IS IDENTIFIED: the diagonal of the towers, with SIDE-window's rungs as
   its compiled staircase — and `T2`'s residue is now the free-vs-diagonal gap, stated exactly.**
3. **Named for your word:** *(i)* whether sitting 4 attempts the diagonal itself (the support-forced
   section of the glued object — the object `T2` actually asks about); *(ii)* the cyclotomic half
   (still designed-not-built); *(iii)* the archimedean factor's model (still carried, not modeled).

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — replayed on top of this report per the standing
procedure, absence from the public tree re-verified after this push. *Release: counsel, then your word.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO PROMOTION TO `W_∞ − ΣW_𝔭` AT COMPLETE ROSTER.
NOTHING DEPOSITS.**
