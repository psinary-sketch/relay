# b227 — THE TRACE

**2026-08-28 · relay `reports/2026-08-28-the-trace.md` · CONCURRENCY: SOLO**
**Ferry part 1 of 1, receipt confirmed IN FULL. Registration banked after the owner reads, before the statement.**
**Bank: `data/b227_the_trace.txt` · print `data/b227_core_remeasured.txt` (390) · gates `tools/b227_checks.py`.**

> *** ### **(SUPPLIED) for the reading. DERIVES for the lift. (STEP) for the computation.**
> ### **Act 7 §4's sentence now has all three of its answers.** ### **And no numbers were computed:
> the cell operator does not act on the object's space.** ***
>
> ### **The trace is the CANDIDATE aggregation's ENGINE — NOT ADOPTED — routed to the author.
> Term 2 untouched. No comparison to any target is made or implied.**

## COMPONENT 1 — THE READING (P1): **(SUPPLIED)**

*** ### **THE DEFINITION WAS NEVER IN §4.** b221 read §4 alone and correctly found **a name, not a
definition**. The defining clause is act 7's **TAIL joint**, one section earlier in a different
document:

> *"the `E₁`-unit's normalization (`‖unit‖ = 1`) forces the local factor to 1 at every inactive
> place — **the restricted-product trace is DEFINED BY EXACTLY THIS**."*

### **b221 was right and was looking in the right place for the wrong thing.** No act had put §4 and
the TAIL joint beside each other. ***

**The statement:** `ω_u(T) := ⟨Tu,u⟩ = ∏_v ⟨T_v u_v, u_v⟩`, the vector state of b226's unit.

| §4's unanswered question | the answer |
|:--|:--|
| what it traces | `T = ⊗_v T_v` with `T_v = id` off a finite set |
| over which space | ### **b226's object**, `⊗′_v (S̄_v, u_v)` |
| normalized how | ### **by the reference unit** — `‖u_v‖ = 1`, so inactive factors are `1` |

**Tested, not assumed:** at an inactive place `T_v = id`, so the factor is `‖u_v‖² = 1` — precisely
the property act 7 says the trace *is defined by*. **Shell test: not a shell** — `ω_u(id) = 1`, and a
shell cannot be normalized. *Naming observation recorded rather than corrected: **a vector state is
not an operator trace** — on an infinite tensor product `Tr` does not exist while the state does.*

## COMPONENT 2 — THE LIFT (P2): **DERIVES**

b211's **derived** simplicity — RRT's own open conjecture — makes the rank-2 eigenspace
one-dimensional, so *"THE rank-2 eigenfunction"* is a well-defined **ray at derivation-on-imports**.
Norm-one reduces the ray to a sign.

*** ### **And the vector state is phase-invariant: `⟨T(λu),λu⟩ = |λ|²⟨Tu,u⟩ = ⟨Tu,u⟩`.**
### **So b214's BENCH-grade sign never enters the trace**, and the archimedean leg lifts from bench
to derivation-on-imports **for everything the trace uses**. b214's numerics are re-graded as
*locating* the eigenfunction, not as fixing the vector the state sees. ***

**What resists:** the lift is of the archimedean leg **only**. The level-limit premise, the
ruling-made choice, and the owed generic odd step are untouched.

## COMPONENT 2 — THE COMPUTATION (P3): **(STEP)**, and no numbers

Act 9's count is *"the fixed-orbit count **on `V_inv`**"*. b10 defines `V_inv` and then says:

> *"THE SONIN-TYPE CONDITION TRANSPOSED: support off the ball (the ball-avoidance half).
> **THE FOURIER HALF DOES NOT DESCEND — the transform does not commute with `x ~ px`** … leaving
> the Fourier-side Sonin condition behind, exactly as B9's tension predicted **a weight-carrying
> trace must change the space**."*

*** ### **`u_v` is an `E₁` vector; `E₁` is a Fourier eigenspace of `M = S/q`; on `V_inv` the
transform does not descend, so `E₁` does not exist there.** ### **`ω_u` cannot be evaluated on act
9's cell operator — not for want of arithmetic, but because the operator acts on a different space
and the sector that defines the unit is absent from it.** ***

**The missing piece:** a statement carrying the quotient channel's operator onto `S̄_v`, **or**
carrying `u_v` into `V_inv` — b10 records the second as blocked. ### **It wants a result or a
ruling; it does not want a read.**

> *** ### **THE TEMPTATION, NAMED AND REFUSED.** Numbers *could* have been produced on operators
> that do act on `S̄_v` — on `Π`, or on `M` itself, whose state is `1` because `u ∈ E₁`.
> ### **Those would be numbers about a different operator than the one named, in a table headed by
> act 9's closed form — b219's double-name species.** It was not done. *Two independent refusals
> stood here; the scope's own — no target exists (b221, UNDEFINED) — was the other.* ***

**Cell-species note, carried (b221's divergence):** the **active set** is defined at a **diagonal
`a²`** cell by b17's staircase; b226's **units** live at **local `(p,ℓ(p))`** cells. ### **Different
names, and the statement says at each use which it means.** **G-STAB not run** — no non-exact value
was produced, so there is nothing to stabilize.

## COMPONENT 3 — THE SHADOW (P4)

`Core/TraceFactorizationShadow.lean` — ### **8 terminals, 8 of 8 at zero axioms.** Core
**382 → 390**; zero axiom-bearing lines; regenerated 390 **byte-identical** to the bank. Polarity
controls first: `naive_product_differs`, `zero_factor_kills_positivity`,
`state_sees_non_unit_scalars`. **Correspondence row 88** by the committed tool, driven from a file.

*** ### **AND THE PRINT CAUGHT A SORRIED TERMINAL BEFORE IT SHIPPED.** The first
`state_is_sign_blind` used `decide` on a goal with a free variable; Lean rejected it and the
declaration fell through to **`depends on axioms: [sorryAx]`**. ### **The file COMPILED. The PROFILE
convicted it.** Repaired to a term-mode `congrArg` over a closed `decide`.
***A compile is not a verification; the profile is*** — which is exactly why the EXECUTION line says
a claimed compile is reported only from its own printed profile. ***

**The stand-in is declared on the file's face:** factors are `Int` where the real state's are
complex; the shadowed facts use only a commutative monoid with unit and `λλ̄ = 1`, both true in `ℂ`.
### **No ordering of `ℂ` is used or implied** — the positivity terminals are `Int`-model only.

## THE GATES — 9 of 9 PASS, CLEAN **on the second run**

*** ### **The first returned 8 PASS, 1 REFUSED.** The harness declined to run
`no-target-comparison-refused-by-name` because its **witness could not pass**: the registration
line-wraps the phrase as *"REFUSED BY / NAME"*, so it is not contiguous there.
### **That is b217's SECOND guard doing exactly what it was added for** — a must-fail fixture alone
would have let the gate through. ***A string check across a line-wrapped document is a check about
typography, not content.*** Both sidecars carried. ***

## THE AUDIT SIDECARS (emitted; copied from the sidecar files)

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b227
  run at    : 2026-08-28T14:44:19 (local)
  input     : 9 checks routed through the harness
  checks    : 9
  pass      : 8
  fail      : 0
  error     : 0
  refused   : 1
  VERDICT   : NOT CLEAN
  self-hash : sha256/32 8baca49e805718bf1c42e41799e0068b
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b227
  run at    : 2026-08-28T14:44:51 (local)
  input     : 9 checks routed through the harness
  checks    : 9
  pass      : 9
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 0d2ee1fa31482722ecef21724ce4ccee
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b227-docs
  run at    : 2026-08-28T14:52 (local)
  input     : added lines in D:/MY-DOwnloads/PLACE-papers vs HEAD
  stems     : gap, blind
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 78914286f14090afb6db4659e0f25873
=== END AUDIT SIDECAR ===
```

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : mirror_verify
  act       : b227
  run at    : 2026-08-28 (local)
  input     : mirror-refresh-2026-08-28.zip
  files     : 40
  rows      : 40
  mismatch  : 0
  declared  : 93df977
  VERDICT   : CLEAN ON ALL THREE CLAUSES
  self-hash : sha256/32 4b23908f245a65bc8096e334cd433f8a
=== END AUDIT SIDECAR ===
```

*The `banned_terms` and `mirror_verify` blocks above are trimmed to their decisive fields; the
full emitted blocks are at `data/audit_b227-docs_banned_terms.txt` and
`data/audit_b227_mirror_verify.txt`, and `audit_verify.py` reads those, not these.*

**The seat-boundary pre-commit hook was exercised on PLACE-papers and returned CLEAN** — 2 staged
paths, 0 foreign hits. *Reported either way.*

## WHAT THIS ACT DOES **NOT** ESTABLISH

1. ### **No number at any cell.** The table is empty by decision, not omission.
2. ### **No aggregation is adopted.** The trace is the candidate **engine**, routed to the author.
3. ### **No comparison to any target**, and none implied.
4. ### **The lift is of one leg only.**
5. ### **The shadow models no operator and no space.**
6. ### **Nothing at complete roster. Nothing about `h2` beyond the register sentence exact.**

## PINS

| repo | pin |
|:--|:--|
| **SIDE-global-section** | `b8b3159` → ### **`1935ebf`** — shadow, print, generator, row 88 |
| **PLACE-papers** | `b21075a` → ### **`93df977`** — chain §31 + the register |
| relay | → the b227 pin-line commit |
| mirror | rebuilt at `93df977`, **CLEAN ON ALL THREE CLAUSES** |
| HELD | `6eada6a` — LOCAL-ONLY, untouched |

**DEVIATIONS:** none.
**DIVERGENCES:** ### **one, with clause (e)** — it directs computing `ω_u` on the cell operator, and
### **the cell operator does not act on the object's space.** Named at registration, not worked
around, and ### **no substitute operator was silently put in its place.**

### **THE NEXT ACT IS THE LEDGER'S CELL-LEVEL STATEMENT** — the target b221 found absent and this
act refused to compute against.
