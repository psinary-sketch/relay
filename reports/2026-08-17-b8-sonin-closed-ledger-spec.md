# CHECKPOINT `B8` — SONIN CLOSED AT FINITE PLACES · THE LEDGER-SIDE SPECIFICATION
## ### **TWO EXACT LAWS FROM THE DIMENSION CHECK, VERDICT `E3` — AND THE SPEC NO CANDIDATE ON EARTH SATISFIES**

**Relay report · 2026-08-17 (twelfth sitting) · author-called · `INSTRUMENT` track · sub-gate declared · item 1 bench, item 2 proof-search on Fable · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 2e734af08aa47e6c614c8f2379bd3ba74b662e3e   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 2e734af   ### 22-file roster + MANIFEST, 3 probes PASS
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> ### **RULE-3 LOG:** *CC Thm 2 — banked verbatim (`§8.2`) ✓ · Face E — VERIFIED at its banked scope:
> the Pass-2 verdict "any derivation of the positivity must use the Euler product essentially" is an
> honest RESTRICTED theorem over the named toolkit, author-adopted, definitional form rejected — used
> only at that scope · phase boundary — banked ✓ · **the summand shape `Tr_∞ + Σ_𝔭 Tr_𝔭`: WITHDRAWN by
> the navigator's own line, read-refuted, recorded** · ### **one flag: the ferry's `(β)` weight
> `√q·log q` matches NEITHER banked convention** *(registered `2√q·log p`; Weil `4·log p/√q`;
> "nothing in the banked record separates them")* — `(β)` filed at the banked grade. *Banned grep:
> clean; terms coined: none.*

---

## §1 — THE DIMENSION CHECK *(priced one sitting; run this sitting; registration banked before any number)*

**The model:** functions on `p^{−n}ℤ_p/p^nℤ_p ≅ ℤ/p^{2n}`, DFT pairing, integers' ball
`B = {m ≡ 0 mod p^n}`; `Son(p,n) = {f: f|_B = 0 ∧ f̂|_B = 0}`; scaling as the truncated unitary with
`|p|^{1/2}` normalization, edge effects declared.

> ### **TWO EXACT LAWS, BOTH VERIFIED OUT-OF-SAMPLE:**
>
> | law | cells |
> |:--|:--|
> | ### **`dim Son(p,n) = (p^n − 1)²`** | eight, incl. out-of-sample `(2,3) → 49` and `(7,1) → 36` |
> | ### **`‖S·U·S‖_F = √p·(p^{n−1} − 1)`** | seven — `Q² = 2 / 12 / 80` at `n = 2` exact to ten digits; `18` at `(2,3)`; the `n = 1` machine-zeros ARE the law's `(p⁰ − 1)` factor *(structural: the one-shell space maps into the ball)* |
>
> ### **VERDICT: `E3` — A THIRD SHAPE, FILED OPENLY.** *Not `E2`-trivial (the space is `(p^n−1)²`-
> dimensional); not `E2`-weight-silent as registered (the compressed action carries `√q` EXACTLY at model
> grade, cutoff-graded by the volume factor); not yet `E1` (the defining measurement — the candidate trace
> on lag-`log q` data against the two banked conventions — is the named next cell, priced ONE SITTING,
> not run).* ### **THE READ's `E2`-POINTING IS REFUTED AT MODEL GRADE, AND CC's THEOREM 2 IS UNTOUCHED —
> exactly as `§8.3` pre-registered: their rigidity concerns their semilocal coupling (finite data pinned
> to the self-dual vector), not the local vanishing-model. The model makes that distinction concrete.**
>
> **Two instrument notes, banked with the run:** *the `n = 1` fitted-power line was a polyfit on
> machine-noise magnitudes and is VOID (the executor's own filter admitted floating zeros — recorded per
> the void-test discipline); and the dimension being a perfect square is noted as a suggestive tensor
> structure, not pursued.* **`§8.3` IS CLOSED AT THE MEASURED VERDICT.**

## §2 — THE LEDGER-SIDE SPECIFICATION *(the spec's `§9`; written from constraints; NO construction)*

| | constraint | cite |
|:--|:--|:--|
| **(α)** | ### **manifestly positive as a sum of squares** — the `Tr(ϑ(g)Sϑ(g)*)` sandwich; a ledger is not a trace | `§6(d)`/`§7(b)`; blockage 7; **`N7` rider beside it: realization supplies no positivity** |
| **(β)** | ### **weight-carrying on lag-`log q` data** — scales with the prime's explicit-formula coefficient, **at the banked grade: two conventions, unseparated; measuring WHICH is itself a named bench question** | the phase boundary; exp1's registration |
| **(γ)** | ### **identification** — negative count on the address graph = maximum matching | **nine of nine bench cells exact (`B6`–`B7`); the requirement is now testable in one cell per candidate** |

> ### **THE HONEST FRAME, ON THE SPEC'S FACE: THREE INDEPENDENT LINES CONVERGE — Face E (the sign's
> derivation must use the Euler product essentially, at its restricted scope) · the phase boundary (only
> weight-carrying data can pay for a prime) · Theorem 2 (Sonin carries none of it) — AND NO CANDIDATE
> SATISFYING `(α)`–`(γ)` IS KNOWN TO ANYONE.** *Convergence, not proof; and the `N`-constraints (`N2`,
> `N4`, the `W-2` trap) ride any future use.*

**Where such a form would live — three routes, graded from holdings, NONE run:** ### **R1** the Weil
form's own prime side as a quadratic form — **ARRESTED-AS-STATED** *(the lag terms are off-diagonal shift
structure; the address graph is the record of their non-square shape)*, **SILENT** on the
restricted-class question · ### **R2** the Deninger orbit-at-`log p` seat — **PLAUSIBLE-AT-SEAT** *(the
one frame where `log p` is geometric — the orbit's period; the `H¹` read-debt grades nothing further)* ·
### **R3** CCM's finite-place content — **ARRESTED** *(Prop. 6.4's proven vanishing subspace;
class-separation absent).* **And `B8(1)`'s laws sit beside the routes as a fourth data point, `OURS`,
model grade — feeding `(β)`'s plausibility at a finite address without filling any column.**

## §3 — THE RECORD

*Spec → `v0.6` (`§8.3-bis` + `§9`) · the charter's `C3` sharpened strike-visible:* ### **"the address
graph and a LEDGER-SIDE positive trace as one object"** *(the field's own theorem retired "semilocal
trace" as the shape)* · `F.2026-08-17k` · `OPEN_TRAILS` ledger line · mirror @ `2e734af`, probes pass.

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **SONIN IS CLOSED AT FINITE PLACES BY READ + CELL:** Theorem 2 (their coupling) + the dimension
   check (our transposition) — the candidate closes at `E3` with two exact laws and one named next cell
   (the `E1` trace measurement, one sitting).
2. ### **THE LEDGER-SIDE SPECIFICATION STANDS, AND NOTHING KNOWN SATISFIES IT** — three lines converge on
   it; three routes graded (one plausible-at-seat, two arrested); the `(β)` convention question is itself
   a named bench item.
3. ### **THE CHARTER's `C3` NOW STATES ROAD B's GAP AT ITS SHARPEST: the address graph and a ledger-side
   positive trace as one object.**

### **WHAT WANTS YOUR RULING:** *(i)* the `E1` trace measurement (one sitting, bench); *(ii)* whether `R2`
(the Deninger orbit seat) warrants its read-debt being promoted to a retrieval attempt; *(iii)* the
`(β)`-convention bench question (which coefficient the true functional carries).

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
