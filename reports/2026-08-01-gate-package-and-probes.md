# The gate-read package + two bounded probes — 2026-08-01

Item 1 is the paste-back block (§ below, delivered verbatim in the session reply — the
linkage-read-packet precedent); item 2's verdicts at grade. Rail frozen at `11db565`, empty-diff
at close; nothing deposits; the board restated READ-BOUND with the package as its key.

## Probe (a) — W-CHAIN-CURVE narrowed: the length-16 Type II sweep

**Registered (verbatim):** *"the genus-1 match is small-dimension-forced — expect length-16 to
fail integrality like Golay; a PASS at 16 reopens the mechanism question honestly."*

**Computed** (both length-16 Type II codes — E₈⊕E₈ and d₁₆⁺ — share one weight enumerator, the
classical isospectral pair, so one zeta covers both): degree 10 (genus 5), all 17 equations
satisfied, P(1) = 1, FE verified; **Duursma-RH HOLDS exactly** (ten roots on |T| = 1/√2 to
7.5e−37). **The integrality test PASSES**: P/P(0) = [1, 2, 2, 0, −4, −8, −8, 0, 16, 32, 32] ∈ ℤ[T].
**The registered expectation FAILED — the mechanism question REOPENS, with structure:**
- **Exact factorization:** P₁₆/P(0) = **(1 + 2T + 2T²) · (1 − 4T⁴ + 16T⁸)** — the [8,4,4]
  curve-partner numerator (the supersingular E/𝔽₂) sits INSIDE the length-16 zeta as an exact
  factor; the cofactor is integral and RH-exact in its own right (|T|⁴ = 1/4).
- **Point-count necessary conditions PASS**: would-be N₁…N₅ = 5, 5, 5, 9, 25, all ≥ 0 and
  divisibility-monotone. The length-16 zeta is NOT excluded as an abelian-variety L-polynomial;
  whether a genus-5 CURVE (a Jacobian) realizes it is the honest open distinction.
- **The three-instance state:** [8,4,4] = exact curve identity · length-16 = integral, counts
  pass, curve-partner factor embedded · Golay = non-integral (excluded). The candidate pattern
  shifts from "curve-identity" to "curve-FACTOR / abelian-variety structure," with the Golay's
  d = 8 (vs d = 4 for both passers) the visible discriminating variable — noted, not asserted.
  Seed W-CHAIN-CURVE updated: verdict **UNRESOLVED-REOPENED with structure**.

## Probe (b) — W-FLOW-METER-DEPTH at its cheapest face: the k(t) bracket

**Registered deliverable:** the bracket "threshold above k = __" as data. **Computed** (k = 5, 6
i.e. κ₁₀, κ₁₂ added; t = −0.1 and −0.3; routes agree ≤ 4.3e−21): all margins hold with the
LP-necessary alternating signs at every order through κ₁₂, both flow times (t = −0.1:
M₁₀ = 2.15e−6, M₁₂ = 1.15e−6; t = −0.3: M₁₀ = 1.83e−6, M₁₂ = 9.46e−7). **The bracket: the
detection threshold is above k = 12.** The meter remains insensitive to the provable t < 0
non-reality through twelfth order; the full threshold hunt is research-reach, priced separately
only if wanted.

## The gate-read package (the paste-back block)

Delivered verbatim in the session reply; archived here identically. Four one-screen briefs, each
with its decision isolated, accumulated riders attached, and one-word answer slots. The author's
single pass clears the board.

*(See the session reply block; the archived copy in this report is the same text.)*

## Closing

Consult verdicts filed at grade; seeds updated (W-CHAIN-CURVE: registered expectation + the
reopened verdict + the factor structure; W-FLOW-METER-DEPTH: the k > 12 bracket, cheapest face
done); the four-gate board restated **READ-BOUND** — every open item on the board now waits on
the author's read-decisions, and the package is the key. Rail empty-diff at `11db565`; nothing
deposits.
