# The Landing Presentation — the substrate held batch, prepared to one word

*W-SUBSTRATE-UNIFICATION + W-REVIEWER-LENS, 2026-07-28. The complete held batch on one screen for the author's landing ruling. One word lands it all; nothing lands without it. Nothing deposits.*

## What lands

**1 · The keystone, complete.** `THE_SUBSTRATE.md` — the {2,3} object, its clothing derived once, its specialness at theorem grade, its two forced consequences, its honest boundary. With the batch applied it gains: **§(ii).4 the spinor leg closed** (open research → compiled), the **Related Work** section (the classical components named), and two precision fixes (Steane uniqueness per-stage; λ=12 = the bosonic-string anchor).

**2 · The kernel branch merges.** `SIDE-spinor` held branch **`substrate-spinor-leg` (`4f5848d`)** → `main`. `SpinorLeg.lean`: the metaplectic quarter-twist route to −1, `lake build`-clean, the standard three axioms {propext, Classical.choice, Quot.sound}, −1 derived (salt-checked). This makes the −1 convergence **two compiled, disjoint routes**.

**3 · The ten pointer-diffs.** Each of the ten sites that re-derive the (2,3,2,0) core (CATALOGOS §4.1/Ch16, UNIVERSALITY, AT_REST, CASE_FOR_TWO, CARTAN_B, ENUMERA, SIDE_EXCLUSION, POISSON_EXHAUSTION, A_METHODOLOGY, Seven_Mechanism_Classes) gains **one line — "Full derivation: `THE_SUBSTRATE.md` §(ii)"** — keeping its local paragraph. No local content deleted; documents stay readable alone.

**4 · The CATALOGOS/AT_REST panel re-grades.** The heterogeneous universal-tuple panels (Shannon/Maxwell/DNA assigned tuples) re-graded on **classification × certification**; the tuple column scoped to the arithmetic tier; the non-arithmetic systems → classify-only. Control arm dated.

**5 · The three seam corrections.** The phantom `SIDEOmegaB.omega_b_equals_4_over_81` citation → the real `xi_*` cluster; the Γ₀(4)-torsion erratum (torsion is in ambient PSL₂(ℤ)); the spinor-leg honesty note (now moot — the leg is compiled).

## What changes nowhere else

- **The RH rail is untouched** — the only RH-adjacent edit is the one-line pointer in `day1/Seven_Mechanism_Classes.md`; no mathematical claim, no monograph content, no h2 status changes. (`git diff 67da789..HEAD` on the rail is empty across the whole session.)
- **No deposit.** Everything is internal/staging; the disclaimer stands.
- **The canon** (v1.2) is untouched by this batch — its v1.3 items (the admission-precondition, the parallel-improvement law) remain separately queued.
- **No other keystone's conclusions change** — the pointer-diffs are additive; only the CATALOGOS/AT_REST panels change grade (from a false universality to the true tiered one).

## The verification plan (post-land)

1. **Merge** `substrate-spinor-leg` → SIDE-spinor `main`; **`lake build` + `#print axioms`** re-run at the merged pin (expect the standard three axioms, unchanged).
2. **R-1 on `THE_SUBSTRATE.md`** at the landed state: stamps unanimous, the §(ii).4 note reads *closed*, every Correspondence pin re-printed, free-writing conformance.
3. **Pin table**: PLACE-papers `main`, SIDE-spinor `main` (post-merge), the ten pointer-sites, all local = remote.
4. **Confirm** the RH rail still empty-diff.

## The one word

**"land"** applies all five (or any named subset). **"defer"** holds the batch as-is. The batch is one reviewable set; the author rules the whole or the parts.
