# THE SIGN OF `A` — b232, 2026-08-28

**Scope:** reads and a derivation-by-citation. ### **No comparison run; no left-side value exists to
agree with.** The two-ended sum is a named import at cite. Nothing about the h2 identity beyond the
register sentence exact. Nothing deposits. Locks last.

**The ruling carried (Core policy), the author's:** *"Two bars, explicit: the zero-axiom vanilla
Core is the AUDIT layer … the Mathlib standard profile is the STATEMENT layer … A statement is never
contorted to reach the zero-axiom bar … Both bars at the highest standard; grade cells name which
bar and why."*

---

## THE VERDICT — ### **(FORCED): `wInf = −A`**

### **And the sign came from CC's own equation (1), not from any comparison coming out.**

`wInf(a) := −A(a)` is **ADOPTED**, provenance `tools/e16/b38_act10.py` `left_side` at its pin — the
same instrument and pin from which b229 adopted `wPrimes`. ### **The finite-instance identity's
statement is now COMPLETE AT CELL LEVEL: every column defined, nothing proved, nothing closed.**
File E's own words stand unchanged; the register sentence is untouched.

---

## 1. The identifier, corrected before the read

The ferry named *"the CC paper (arXiv 2112.05500)"*. ### **That is Connes–*Moscovici*, "Prolate
spheroidal operator and Zeta"** — title and authors read at content at b130 (R1) and b171 (1b),
which also checked it for exactly this symbol class and found `eps'(1+)`, `N_I`, "divergent
subtraction" **ABSENT**.

### **The genuine CC work is arXiv 2006.13771, Connes & Consani, "Weil positivity and Trace formula:
the archimedean place"** — and it is the work act 12's dictionary cites *by its objects* (`W_∞`,
`Tr(ϑ(g)Sϑ(g)*)`), neither of which is in 2112.05500. A wrong identifier is followed to its object,
not off a cliff. Registered as a divergence; strikeable.

**The local-copy hunt, reported either way.** b179 recorded the paper *"extracted locally with
pypdf"*, so a copy existed on 2026-08-26. Searched this act: **123 PDFs on D: to depth 5**, by
filename and — for the two plausible externals — **by first-page content through pypdf**.
### **No local copy; that extraction went to a path since removed.** *Positive control: the same
sweep returned 123 files and opened two at content, so the instrument could demonstrably find and
read.* The fetch then ran per established practice (ar5iv), with b130's transmission discipline
holding — **only the public arXiv identifier went out.**

---

## 2. CC's own sentences fix all three sides

**Before equation (1):** *"It was shown by A. Weil that the Riemann Hypothesis (RH) is equivalent to
the negativity of the right-hand side of the Riemann-Weil explicit formula"*

**Equation (1), verbatim as displayed:**

> **f~(0) − Σ_{ρ∈Z} f~(ρ) + f~(1) = Σ_v 𝒲_v(f)**,  **f~(s) := ∫_0^∞ f(x) x^{s−1} dx**

**After it:** *"where Z is the multi-set of non-trivial zeros of the Riemann zeta function"*

**And the sentence that fixes where the primes live:** *"**The key point being that the right-hand
side of the explicit formula, when evaluated on a test function f with compact support, involves
only finitely many primes.**"*

So: ### **zero side on the left; places side (carrying the primes) on the right; `𝒲_∞` one member of
that right-hand sum** (eq. 53: `𝒲_∞(f) = −∫ f(ρ^{−1}) τ(ρ) d*ρ`, with (39)
`τ(ρ) = ρ^{1/2}/2 · (1/(1+ρ) + 1/|1−ρ|)`). ### **No inference of mine placed any of the three.**

---

## 3. The derivation

1. Rearrange (1): `Σ_ρ f~(ρ) = [f~(0) + f~(1)] − Σ_v 𝒲_v(f)`.
2. Set beside the instrument's own docstring arrangement — quoted in
   `SIGN_ARRANGEMENT_RECONCILIATION` §1 as ### **"committed before any answer"**:
   `Σ_γ ĥ(γ) = ĥ(i/2) + ĥ(−i/2) − PRIME + ARCH`.
3. Match zeros to zeros, poles to poles: `Σ_v 𝒲_v = PR − A`.
4. Split by CC's own sentence: `Σ_p 𝒲_p = +PR` and ### **`𝒲_∞ = −A`**.

Since File E binds `wInf` to `𝒲_∞` *in the CC convention* by name — twice, in its header and in the
`WeilLedger` docstring — ### **`wInf = −A`. The sign is forced.**

**The named sub-assumption, disclosed:** CC's finite-place formula is **not in the retrieved text**
(the work's title is "the archimedean place"), so step 4 is a term-class matching licensed by CC's
sentence, not a quoted display.

**Corroboration, independent of step 4.** CC's Theorem 1 — `𝒲_∞(g∗ḡ^♯) ≥ Tr(θ(g)Sθ(g)*)` — beside
`SIGN_ARRANGEMENT` §3's atlas-internal measurement that *"the instrument measures `W_∞ < 0` for
every `a ≳ 1.13`, on test functions of exactly the admissible form `f ⋆ f̃`"*: so `𝒲_∞ ≥ 0 > A` on
the same class. ### **`+A` excluded; `−A` consistent.** *Its left-side contact (act 12 §25(a)'s
positive-form identification) is disclosed; the verdict rests on the derivation, not on this.*

---

## 4. The standing-clause check — the centre of the act

**Where the sign came from:** a source display and a committed arrangement.

**Where it did not come from:** act 12 §25(b)'s residual, which under this very reading collapses
*"by an order of magnitude to a stable ≈ −0.5 plateau"* — `+4.049, +3.374, +3.048 …` becoming
`+0.068, −0.444, −0.525 …`.

> ### **The numbers pointed at `−A` before the source was opened. They point hard. This act did not
> use them.** A residual collapsing **is** a comparison coming out, and b229's standing clause names
> that the crime — a crime with a compiled witness, `cweil_is_the_assumption`.
>
> ### **Had CC's text underdetermined the sign, the verdict would have been (RESIDUAL) with no sign
> chosen — even though the numbers want one answer, and even though the answer they want is the one
> that was independently forced. The route was audited, not the answer.**

And the second half: **no comparison was run because none can be.** File E's `T` and `Q` are data
parameters with **no realization** — there is no left-side value in existence for the adopted `wInf`
to agree with. The scope's restraint is not one this act had to exercise; it is a fact about the
object.

---

## 5. The cross-link, read: the corpus carries two `W_∞`s

act 12 supports its dictionary by cross-linking `SIGN_ARRANGEMENT_RECONCILIATION`, whose §1 verdict
reads ### ***"THEY ARE IDENTICAL. THERE IS NO SIGN-CONVENTION DIFFERENCE."***

Read at content, that is **not** a contradiction — the two documents compare **different pairs**:

| document | compares | result |
|:--|:--|:--|
| `SIGN_ARRANGEMENT_RECONCILIATION` §1 | the instrument ↔ **Day-1** | identical → `A = +W_∞^{Day-1}` |
| act 12 §25(b) | the instrument ↔ **CC** | opposite → `W_∞^CC = −A` |

### **Both are correct, and together they entail a third thing neither states: Day-1's `W_∞` and
CC's `W_∞` are opposite-sign conventions, and File E binds to CC's.** *The mechanism is in the
displays: CC's (1) puts `−Σ_ρ` on the left with the places-sum alone on the right; Day-1 and the
instrument put the zero-sum on the left with the terms on the right. The zero-sum changes sides;
every local term changes sign.*

**Filed as a citation hazard, not an error:** the cross-link points at a document whose headline
verdict appears to refute the sentence it is cited for, and only a content read separates them.

**An observation routed to the author, not ruled:** `SIGN_ARRANGEMENT` §3 found Day-1's row *"`W_∞`
… positive (provable)"* false on the bench and proposed a repair. ### **Under CC's convention
`W_∞^CC = −A > 0` exactly where the bench measured `A < 0`** — so the Day-1 claim reads true in CC's
convention and false in Day-1's own. **The proposed repair is not touched; no deposited artifact is
edited.**

---

## 6. A second arrangement question — named, not decided

File E's relation is `T + Q = wInf − wPrimes`; with both columns now defined its right side is
**`−A − PR`**, while CC's places-sum is **`Σ_v 𝒲_v = −A + PR`**. ### **They differ in the prime
term's sign, so File E's `wInf − wPrimes` is not CC's `Σ_v 𝒲_v`.** That may be intended — File E
states its own relation — **but it is a second convention question, visible only once both columns
were defined, and this act's scope was the sign of `A` alone.**

---

## 7. The import ledger, opened

`THE_IDENTITY_CHAIN.md` §35, head sentence: ### **"imports are enumerated like axioms and never wear
the corpus's grade."**

- **IMP-1 — the two-ended classical prime sum.** Weil (1952), CC's own [33]. Carries b231's fold.
  ### **Status: the reason b231's verdict was (PARTIAL)** — the algebra is owned, the provenance is
  not.
- **IMP-2 — CC's explicit-formula arrangement.** 2006.13771 eq. (1). Carries this act's derivation.
  ### **Status: the citation is the load; it does not become the corpus's result by being used.**
  Sub-assumption named: the finite-place matching.

**The Core two-bar policy** is filed to the method canon as `CORE_TWO_BAR_POLICY.md` and to the
register, ### **with no existing row, grade or profile altered.** It settles b231's `Nat`-bar
finding as a property of the **audit layer** — and fixes that **a statement is never contorted to
reach the zero-axiom bar.**

---

## 8. Gates

**b232's gates: 12 of 12 PASS, CLEAN — on the second run.** The first returned ### **10 PASS, 2
REFUSED**, both by b217's **witness** guard, and both refusals found real defects:

1. ### **A latent bug in the inherited `contains()` helper.** It read the haystack as **bytes** and
   lowercased it with `bytes.lower()` (ASCII-only), while lowercasing the needle as a **string**
   (Unicode-aware). For any needle carrying a non-ASCII capital — and this act's quotations carry
   `Σ`, `Ζ`, `𝒲` — the needle became `σ` while the file kept `Σ`, so ### **the check could never
   match a quotation that was present.** b229–b231 were unaffected only because their needles were
   ASCII. ### **The bug was latent across three acts; this is the first whose quotations exposed
   it.** Repaired: both sides decoded and lowercased as text.
2. **A line-wrapped needle** — the bank carries "the named / crime" across a line break, so the
   exact substring was absent. b227's species, caught again.

***Fourth consecutive act in which one of the harness's two guards has caught one of this seat's
gates — b227 witness, b229 fixture, b231 fixture, b232 witness (twice).*** Every absence in this act
carries a positive control, per the EXECUTION line; the local-copy gate runs its sweep in-process
and **refuses** rather than passes if the sweep returns nothing at all.

---

### **What this act did not do:** no comparison run — and none was possible; no ledger number
computed; the residual collapse **refused by name** and quoted as what was refused; File E quoted,
not edited; `SIGN_ARRANGEMENT`'s proposed repair not touched; no deposited artifact edited; no
existing correspondence row, grade or profile altered. ### **Nothing about h2 beyond the register
sentence exact. Nothing deposits. Locks last.**
