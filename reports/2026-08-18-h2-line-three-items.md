# THE h2 LINE'S THREE OVERLOOKED ITEMS
## ### **THE LI CERTIFICATE: `KIN` — THE SAME ZERO-SIDE SQUARES MECHANISM COMPILED ON BOTH LEGS, THE FAMILIES DISTINCT AND THE DIFFERENCE NAMED (NOT A DIAGONAL CELL; THE SUPPORT AXIS) — THE GEOMETRIC CLAUSE: PURELY ARITHMETIC (BIRTH), SITTING 15 CONFIRMED; THE CLAUSES REMAIN INDEPENDENT — THE h = 1 MECHANISM COMPILED (2/2 NO AXIOMS; RELAY 101)**

**Relay report · 2026-08-18 · Rules 4/5 · Fable/proof · nothing deposits · nothing circulates**

> ### **RULE-3 LOG:** *the era-open sitting at `d73aaa6` ✓; the deposited terminal READ AT ITS PIN
> (salt-check: `93c27ec` verified in `SIDE-lv-conservation`'s history; the local HEAD `2f71068` is
> AHEAD with held branches — noted; the read was at-pin, `PartialPositivity.lean` at its
> statement); the two-clause bank and `§24.4` read at their FINDINGS/monograph rows ✓; b31 read at
> its bank ✓; the banned grep: CLEAN. THE RULING carried to item 5: the classical-profile leg is
> built LOCALLY at full standard; upstream submission a deferred loose end; the program RENAMED.
> Terms coined: none.*

---

## §1 — ITEM 1: THE LI CERTIFICATE AGAINST THE BUILT OBJECT — ### **`KIN`**

**The deposited terminal, at its statement (at-pin):** finite-range Li positivity — `λ_n ≥ 0` for
`n ≤ N₀(T) ≈ 2T²` from zeros verified to height `T`; the Bombieri–Lagarias summand
`blTerm ρ n = Re[1 − (1−1/ρ)^n]`; INTERFACES grade, three named premises; and ### **the on-line
term nonnegativity PROVED in the kernel** (`blTerm_nonneg_of_onLine`: on the line
`|1 − 1/ρ| = 1`, so the term is `1 − cos ≥ 0`).

**Registered both longhand, then the verdict:**

1. ### **Is the certificate's range a diagonal cell? NO — the ranges live on DIFFERENT AXES.**
   *The certificate's range is the `n`-ladder (the Li family's index, each member of UNBOUNDED
   multiplicative support); a diagonal cell is a compact window `[1/a², a²]`. The Li family is
   not in the constrained class (compact support fails), and no member of the class is a Li
   test function.*
2. ### **Are the two positivity-shaped facts one fact? `KIN` — the corpus's own middle verdict
   (per the adjudication-route vocabulary), with the common cause NAMED and compiled on both
   legs:** *on-line, `Re[1−(1−1/ρ)^n] = 2sin²(nθ/2)` — A SQUARE PER ZERO — so the certificate's
   mechanism is the zero-side quadratic shape; the built object's class positivity
   (`Z = Σ|ĝ(γ)|²`; `B = ‖·‖²` on `E₁`, proved) is THE SAME SHAPE at its family. The common
   cause: the Weil-pairing's zero side against a test family (Bombieri–Lagarias at cite — the
   terminal's own citation). THE COMPILED CONTACT POINT: `blTerm_nonneg_of_onLine` (deposited,
   at-pin) and `inner_map_self_of_fixed` (the companion, classical profile) are the squares
   mechanism at the two families.* **NOT SAME-FACT: the families are disjoint in kind (unbounded
   vs compact support) and the ranges on different axes; the difference is exactly the SUPPORT
   AXIS — and both certificates together still leave the `(nonarchimedean, unbounded)` quadrant
   untouched (the W-UNION residue, the corpus's own coordinates).**

## §2 — ITEM 2: THE GEOMETRIC CLAUSE ASKED — ### **PURELY ARITHMETIC (BIRTH)**

**The question:** does the built object's arrival-edge structure (negatives only where a place
enters, in the flipped channel; imbalance-free) state a transversality condition — the pairing's
nondegeneracy transverse to the arrival directions — or is it purely arithmetic?

**Both branches, longhand:** *(transversal)*: the shape matches — nondegenerate except at a
discrete locus, with the radical-zero theorem holding between arrivals — and the window `a` is a
continuous parameter, so "transverse to the arrival directions" is grammatical. *(arithmetic)*:
the arrival events are DIMENSION JUMPS in a staircase (locally constant between arrivals) — birth
events, not zero-crossings of a smooth function; there is no derivative condition in the window
variable to be transverse WITH.

> ### **VERDICT: PURELY ARITHMETIC — sitting 15's "birth" reading CONFIRMED at the built object.**
> *The geometric clause's transversality (`§24.4`; the canonical statement: simplicity via
> `ξ′(ρ) ≠ 0`, the perpendicular crossing) is a DERIVATIVE condition in the `s`-plane; the
> object's arrival structure is a RANK condition in the window parameter; no map between the two
> parameters is compiled or claimed (the non-fusion law).* ### **THE TWO-CLAUSE VERDICT UPDATED:
> the built object is an ARITHMETIC-CLAUSE object end to end; it supplies NO transversality
> content to the geometric clause; the clauses remain INDEPENDENT — the geometric clause keeps
> its own strategy untouched, which is itself the finding.**

## §3 — ITEM 3: THE h = 1 MECHANISM COMPILED

**`H1Mechanism.lean` (VANILLA, 2/2 "does not depend on any axioms"; the relay's zero-axiom
count: 101):** the one-ideal-per-norm integer core (mutual divisibility pins the generator at
the banked norms 6, 36, 150 — the coupling scalar `a_N = 1`) and the collapsed form as a sum of
squares at the banked sample vectors. **The instance nearest RH proper in the federation, at no
axioms; docstrings carry "nothing at complete roster."**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING
CIRCULATES.**
