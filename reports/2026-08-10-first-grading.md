# THE FIRST GRADING — CHAPTER 13's DIVISOR LADDER — 2026-08-10

**Expectations E1–E4 registered VERBATIM and committed at `42f5f90` BEFORE any read.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — THE REMOTE (author-ruled: private)

`psinary-sketch/SIDE-carrier-spec` created **PRIVATE**. **Remote SHA verified by
`git ls-remote`, never by push output:**

```
20f1860312f63df7b6ef1a211c1eda166af479f4   HEAD          (at creation)
20f1860312f63df7b6ef1a211c1eda166af479f4   refs/heads/main
09b9cdd4c60488390b38243181b81af895609bd2   refs/heads/main  (after the grading)
```

both matching local exactly. **No tags exist yet, so no peeling applies** — recorded because the
rule specifies peeled verification where tags exist.

**Description field, derived from what the repo IS at this commit** (per the description rule):
*"A Lean structure encoding the carrier requirements as fields, with the idempotent no-go
compiled. It grades candidates; it does not construct one."*

**REGISTRY carries the pin with its visibility stated and the reason recorded:** a requirements
instrument adjacent to the patent surface; **visibility is the author's to widen and never the
executor's.**

---

## §2 — THE GRADED ROW, at the named fields

Candidate **read at source** — SIDE-kernel `Kernel/ProductFormula_Clean.lean`,
`Kernel/ProductFormula_Rat.lean` — not from my own inventory summary.

| field | verdict | at what |
|:--|:--|:--|
| `cancel` (R1) | **PASSES, as coordinates not as a present object** | `ℚ^×` is a group; the ladder itself contains no class object, it proves an identity `= 1` |
| `neg`/`add_neg` (R2) | **PASSES**, same reading | |
| `finPart` (R3a) | **PASSES** | three levels; `padicValRat p q : ℤ` genuinely signed |
| `components_faithful` (R3c) | **FAILS on `ℚ^×`** | compiled `two_torsion_trivial_of_spec`; `(−1)² = 1` invisible to both components |
| `arch_present` (R3d) | **PASSES — AND THE PASS IS HOLLOW** | compiled `arch_present_is_not_independence`; `log|q| = −Σ_p v_p(q)·log p` |
| `act` (R4) | **SPLITS — both halves compiled** | multiplications are translations and fail; power maps are endomorphisms and pass |
| `pair` (R5) | **FAILS** | a scalar identity, not a bilinear form |

### THE EXPECTATIONS ADJUDICATED

**E1 — CONFIRMED, with an unregistered refinement.** Three levels honest; **the signed ℤ-exponent
exists at the RATIONAL rung only.** The prime-power and natural rungs use `Nat.factorization : ℕ`
— unsigned.

> **E2 — PARTIALLY REFUTED; the substantive correction of this grading.** I registered *"it FAILS
> at `arch_present` — the ladder is a finite-places object."* **It is not a finite-places object**
> — its archimedean member is present and named in a theorem (`archimedean_times_padic_product`,
> the `|q|` factor) — **and it does not fail the field. It passes it.** Chapter 13's two-channel
> result is exactly *why* the pass is hollow; but **"carries nothing" is not what `arch_present`
> tests.** Right about the substance, wrong at the field.

**E3 — CONFIRMED** at the named field. **E4 — CONFIRMED as framed and now DECIDED, both ways at
once.** *Recorded without inflation: passing `(R4)` means the action is by endomorphisms and
nothing more — no trace form, no positivity, no pairing for one to be positive on.*

---

## §3 — FOUND-BEYOND-REGISTRATION *(NOT EMPTY — two items, the first against my own instrument)*

1. **THE INSTRUMENT HAS A DEFECT AND THE CORPUS'S OWN OBJECT FOUND IT.** `arch_present` was
   written to stop a candidate zeroing out its archimedean component. **It does not stop a
   candidate whose archimedean component is a FUNCTION of its finite one** — and the product
   formula is precisely that. **The field demands non-vanishing where it should demand
   independence.** Compiled (`archPart_separates_nothing_new`,
   `arch_present_is_not_independence`), stated in the module header and the README, and **LEFT
   UNREPAIRED**: correcting a field of the specification is an author-ruled change, not an
   executor's patch.
2. **THE TORSION FINDING.** `components_faithful` forces 2-torsion-freeness, so `ℚ^×` is excluded
   — **and the exclusion lands on the sign of the RATIONAL, not anywhere the survey had been
   looking.** Not registered, not sought.

---

## §4 — THE TWO FILINGS

**`F.2026-08-10-b` — the empty-sentence datum. GRADE: DATUM.** The field's only cancellative
programme names **no requirement** at its arrest, so the six-constraint entry is **at cite the
sharpest existing statement of the object** — *not because it is deep* (every constraint in it is
classical or quoted) but because the programmes expected to hold a sharper one do not state one.
**Convergence caveat intact and restated: the sharpest existing statement of a requirement is not
evidence the requirement can be met, and may be sharp about an object that does not exist.**

**`F.2026-08-10-c` — the TORIC-TERMINUS QUESTION. GRADE: QUESTION; null answer respectable and
expected.** Both escape styles land (or hope to land) toric; toric is monoid-driven; **is a
carrier's discriminating property exactly what is invisible monoidally?** Set beside `(R4)`
(translations are trivial), the idempotent no-go (monoid addition has no signed completion), and
the free-layer scoping — **the last carried as the author's framing, which this executor has not
independently verified and says so.** One terminus is **a hope, not a theorem**. **No promotion,
no route claimed, nothing to be built on it before it is answered.**

---

## §5 — STOP-AND-HOLD

**No progress on the clause and none claimed.** The ladder fails at `pair` as the inventory
predicted; the `(R4)` theorems decide about *actions*, not positivity. **The one genuinely new
thing this grading produced is a defect in my own instrument, and it is filed as a defect.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `42f5f90` (registration, pre-read) → `61829ab` |
| relay | → this report's commit |
| **`SIDE-carrier-spec`** | **`09b9cdd`, PRIVATE, remote verified by `ls-remote`** |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
