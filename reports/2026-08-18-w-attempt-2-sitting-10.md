# `W-ATTEMPT-2` · SITTING 10 — THE LIMIT AT ONE PLACE · THE SOFT ARCHIMEDEAN FACTOR
## ### **`L-STABLE`, 203/203 EXACT AT LEVELS `n = 1…6`: THE TOWER ON `ℚ₂` IS COMPATIBLE ON THE NOSE, THE PAIRING LEVEL-STABLE, THE COMPRESSED SCALING FORCED (CC'S STRUCTURE RECOVERED, NOT CHOSEN) — AND AT `∞`, SOFT TRUNCATION SPLITS THE REFUSAL: THE INSTABILITY WAS THE ARTIFACT, THE EXACTNESS DEFICIT IS THE MATHEMATICS**

**Relay report · 2026-08-18 (thirtieth sitting) · author-called · attempt work RELAY-ONLY ·
nothing deposits**

> ### **THE CORRECTED STOP AND THE PROTOCOL CORRECTION, carried. Everything below is DATA about
> finite-level constructed objects and a declared float model at `∞`. No sign at complete roster; the
> register untouched.**

```
PLACE-papers  origin/main : b7acc3e (the §0 pattern ledger, this batch's item 4; mirror UNTOUCHED —
              THE_ATTEMPT_RECORD is not in the 22-file roster, reflected through pointers)
relay         origin/main : (this report, in the sittings-9/10 batch)    local +1 HELD
instruments   tools/e16/b23_attempt2_s10.py (registration banked BEFORE the run:
              data/b23_registration_2026-08-18.txt) · data/b23_2026-08-18.txt · 203/203 EXACT
              tools/e16/b24_attempt2_s10_soft.py (folded item; registration banked BEFORE the run:
              data/b24_registration_2026-08-18.txt) · data/b24_2026-08-18.txt · float, declared
```

> ### **RULE-3 LOG:** *sitting-8 cites (the transform identification; the intertwining; the fifth law;
> `t_k = 0`) all banked ✓; the b24 files were re-emitted once with print-time ASCII folding (the b18
> precedent), numbers verified identical — noted. Terms coined: none; banned check clean.*

---

## §1 — THE TOWER ON `ℚ₂` *(exact at every level; nothing declared float, nothing skipped — the registered fallbacks went UNUSED)*

| claim | verdict |
|:--|:--|
| `T1` `ι(Son(2,n)) ⊂ Son(2,n+1)` | ### **EXACT at `n = 1…6`** *(the integers' ball is level-independent; transform side by the banked intertwining — 508,032 exact `ζ₁₆₃₈₄`-sums at `n = 6` in 0.6 s)* |
| `T2` the pairing level-stable | ### **ON THE NOSE, entry-exact at `n = 1…6`** *(host Gram `= 4·`level Gram under the stated mass normalization — no scalar slack; 15.7M exact entries at `n = 6`)* |
| `T3` the six laws' `n`-dependence | ### **`Q² = 2(2^{n−1}−1)²` VERBATIM at all six levels: `0, 2, 18, 98, 450, 1922` · `Tr(U^k S) = 0` EXACTLY and level-stable, `k = 1, 2`, every host** |
| `T4` class positivity level-stable | ### **CONFIRMED** *(`G_loc R = (KᵀK)R` entry-exact; `KᵀK = I ⊗ T` SPD with integer leading minors `2, 3, …, q` at every level)* |
| `T5` the scaling escapes the Sonin condition | ### **EXACT NONZERO WITNESS — every column witnesses (9/9 at `n = 2`, 49/49 at `n = 3`): `(Uf)^ = √2·f̂(y/2)` lands on `2^{−1}ℤ₂∖ℤ₂`, which Sonin does not control** |

**The eigen-dim tower, exact (`tr M = i`, `tr Π = −1` at every level, measured not patterned):**
`d₁ = d₋₁ = d₋ᵢ = 2^{2n−2} − 2^{n−1}`, `dᵢ = d₁ + 1` — closed forms the measured table exhibits
(`n = 1…6`: `d₁ = 0, 2, 12, 56, 240, 992`). ### **The punctuation continuation CONFIRMED: the
one-place class is dead ONLY at its arrival depth (`d₁(2,1) = 0`) and alive at every deeper level.**

> ### **THE LIMIT'S STRUCTURE, NAMED (the `L-stable` branch): the inductive system `(Son(2,n), ι)` with
> the genuine transform level-compatible and the pairing stable ON THE NOSE has a nonzero union inside
> `L²(ℚ₂)` — the local field's own Sonin object — and the scaling acts on it ONLY COMPRESSED:
> `S U S` is FORCED by the exact witness, not chosen.** ### **CC's compressed-scaling structure is
> RECOVERED on `ℚ₂` as a necessity of the construction — and its mass is exactly the fifth law.**
> *What is constructed vs named is kept sharp: the union is constructed; its `L²`-closure and the
> compression's limit are NAMED, not constructed. No `(L-fails)` break exists — there is no `p`-adic
> form of the archimedean refusal at this place.*

*(Run notes, all in the safe direction, disclosed in-file: the registered float fallbacks for `n = 6`
went unused — a block-diagonal route made every cell exact; the `ℚ(√2)` arithmetic degenerated to pure
`ℚ` with `√2` carried symbolically; one bug in an auxiliary cross-check found and fixed in-run, primary
computation unaffected.)*

## §2 — THE SOFT ARCHIMEDEAN FACTOR *(the folded item; float, declared; log-Gaussian window `g(x) = e^{−(ln|x|)²/2(ln a)²}` — Weil's lemma mechanics at cite — vs the hard indicator, IDENTICAL statistics)*

| statistic | hard window | ### **soft window** |
|:--|:--|:--|
| `N`-stability | wobbles with `N` (counts flip `1→0→1`; `σ_max` drifts) — the banked refinement-death | ### **STABLE TO ~5 DECIMALS ACROSS `N = 255/511/1023` at every `a` — the operator has CONVERGED: a continuum object** |
| transform structure (eigen-cluster to 4th roots) | `0.2–0.5` — no real structure | ### **`2×10⁻²` — clean** |
| invariance residual | `0.83–0.99` | **`0.21–0.25`, `N`-stable** |
| fixed-sector positivity (min Rayleigh) | `0.966–0.975` | ### **`0.9948`** |
| near-exact modes (`σ > 1−10⁻⁶`) | **0 everywhere** | **0 everywhere** |

> ### **THE VERDICT SPLITS THE REFUSAL, AND THE SPLIT IS THE FINDING: `(soft-dissolves)` lands on its
> registered clauses — the refinement-instability WAS the hard cutoff's artifact; the soft-windowed
> factor is a stable object with clean transform structure and positive fixed sector. AND what
> PERSISTS, under either window, is the EXACTNESS DEFICIT: no near-exact mode exists at any finite
> window (`σ_max = 0.872 < 1` at `a = 3`, `N`-independent). The real place's distinctive geometry is
> now a STABLE, QUANTIFIED approximation gap — not a model pathology, and not exactness either: the
> finite places are exact (203/203), the real place is convergently inexact.**

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE TOWER IS `L-STABLE` AND THE COMPRESSED SCALING IS FORCED — the finite place's square
   becomes the local field's own Sonin object, with CC's structure as a consequence.**
2. ### **THE ARCHIMEDEAN REFUSAL IS SPLIT: instability = artifact (dissolved by Weil's own soft
   mechanics); the exactness gap = the mathematics, now stable and measured.**
3. **The record (item 4):** *the pattern ledger filed as `THE_ATTEMPT_RECORD` `§0` (created by this
   ferry's order, flagged; the five patterns the executor's enumeration, flagged; every cite a relay
   pin) — PLACE-papers `b7acc3e`, pushed, ls-remote verified; the mirror untouched with the reason
   stated; the correspondence table gains rows 29–34; the review packet current.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO SIGN AT COMPLETE ROSTER. NOTHING DEPOSITS.**
