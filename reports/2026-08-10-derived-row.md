# ACT 2 — THE MONO-COLLAPSE LAW + THE DERIVED CANDIDATE — 2026-08-10

**Registered at `4347551` BEFORE any fetch. Sources read: Nikolaus–Scholze `arXiv:1707.01799`;
Hesselholt `arXiv:1602.01980`. Grading a literature, not building in it.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — THE MONO-COLLAPSE LAW *(`F.2026-08-10-r`)*

> **`X ×_B X` degenerates to `X` ⇺ `X → B` is a monomorphism.** Elementary; filed because it
> **unifies three separate deaths into one mechanism**: Deitmar's trivial descent · initiality ·
> **Durov's own proved monomorphicity**.

**Constraint (iv) sharpens to (iv′): THE BASE MUST BREAK THE MONO.** And the escape axis is a
statement about what the law does *not* govern: **mono-ness is a π₀-level property.**

## §2 — THE DERIVED ROW *(level declared: ONE LEVEL UP; no cell inherits across the boundary)*

**E1 CONFIRMED IN MECHANISM — stated by Hesselholt himself:**
> *"[THH] is an implementation of **Waldhausen's philosophy to replace the initial ring of algebra
> `ℤ` by the initial ring of higher algebra `𝕊`**."*

**The mono-collapse's hypothesis removed at its root. `(iv′)` PASSES — the first candidate anywhere
in this survey to.**

> **A precision the registration did not keep: `THH_{2i−1}(ℤ) = ℤ/i` was NOT read at a source this
> pass.** Bökstedt's, standard, cited by name — **but not read, and not asserted. The mechanism is
> at cite; the numbers are declared unread.**

**E2 CONFIRMED, DEFINITIONALLY** — *"**Definition II.1.1.** A cyclotomic spectrum is a spectrum `X`
with `T`-action together with `T`-equivariant maps **`φ_p : X → X^{tC_p}` for every prime `p`**."*
A prime-indexed Frobenius family **in the definition**.

**E3 CONFIRMED, reaching Deninger by name** — for a scheme *"smooth and proper over a **finite
field**"*, TP gives *"the cohomological interpretation of the Hasse–Weil zeta function"*; and
*"this theory **precisely is the infinite dimensional theory produced by Deninger**."*

**E4 CONFIRMED — the arrest is the archimedean place.** `archimedean`: **0** in Nikolaus–Scholze,
**1 bare token** in Hesselholt in no argumentative context; the theorem scoped explicitly to finite
fields.

> ### **THE DERIVED CANDIDATE HOLDS `Δ4` NATIVELY AND ARRESTS AT `Δ1` — `W(ℤ)`'s arrest, one level up and with far more machinery. THE JUNCTION DID NOT DISSOLVE; IT WAS REPRODUCED.**

**E5 PARTIAL, and the partiality is the finding:** the trace shape is **present over `𝔽_q` — where
the classical proof already worked — and absent over `ℤ` and at `∞`.** *Present exactly where it is
not needed; absent exactly where it is.*

## §3 — THE JUNCTION TABLE, WITH ITS DERIVED ROW

| object | level | `Δ1` | `Δ4` | `Δ2` | square `(iv′)` |
|:--|:--|:--:|:--:|:--:|:--:|
| the adelic frame | 1-cat | **✓** compiled | ✗ compiled | ✗ | ✗ |
| `W(ℤ)` | 1-cat | ✗ at cite | **✓** at cite | ✗ | ✗ |
| Durov | 1-cat | **✓** at cite | ✗ | wrong object | **✗ proved by the author** |
| **cyclotomic spectra / THH** | **one level up** | **✗** (0 archimedean content) | **✓ definitionally** | **PARTIAL** (`𝔽_q` only) | **✓ — first pass anywhere** |

> ### **RE-PRICED: THE SQUARE IS NO LONGER THE WALL.** `(iv′)` is passed, at cite, by an object that has existed since Bökstedt. **THE WALL MOVED TO `Δ1` — where `W(ℤ)` already stood.**

## §4 — FOUND-BEYOND

1. **THE ESCAPE WAS NAMED IN THE LITERATURE BEFORE THE SURVEY FOUND THE WALL.** Waldhausen's
   philosophy **is** the mono-collapse's cure, as a programme decades old. **So the mono-collapse
   law is a RE-DERIVATION, not a discovery** — filed as such.
2. **`Δ1`-OVER-`ℚ` IS NOW THE ONLY CONSTRAINT NO CANDIDATE HAS PASSED WHERE IT IS NEEDED.** Durov
   passes `Δ1` and has no square; the derived candidate passes the square and has **zero**
   archimedean content. **Every other constraint has been met by someone, somewhere.**

## §5 — STOP-AND-HOLD

**A literature graded; nothing built.** No reading approached a construction at `∞` — **because in
the two sources read there is nothing at `∞` to construct with.** **`h2` is exactly where it was.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `4347551` (registration) → `733d09a` |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE — **untouched this pass** |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits.**
