# `W-ORD-SEAT` — ACT 2 — 2026-08-11

**Registered at PLACE-papers `86757e4` BEFORE the full-text read. Three-Witt trap armed in advance.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

> ### PATH-OF-CONSEQUENCE
> **"The seat is the structure on which `Q1`'s floor, and thence `R4`'s computability, would stand —
> no arrow supplies the sign."**

**The text layer is LETTER-SPACED (40,830 hits). It mangled; a spacing-tolerant probe was built and
the decode is stated, not hidden.**

---

## §1 — E1 UPGRADES TO **AT-CONTENT**, all three clauses

**(a) What the space IS** — *"denote by `W_rat(O_X)` the sheafification of the separated presheaf
`U ↦ W_rat(O_X(U))`… Consider the ringed space **`W_rat(X) = (X_top, W_rat(O_X))`**. It is equipped
with **commuting Frobenius endomorphisms for every prime `p`** which are the identity on `X_top`."*

**(b) HOW the `ℝ`-action is defined** — a **suspension**: *"`X₀ = X̃₀[ℂ] ×_{ℚ>0} ℝ_{>0}` … **`t` acts
on `X₀` by right multiplication with `eᵗ`**. Such a suspension has a **foliation `F` by the
one-codimensional images of `X̃₀[ℂ] × {u}`** … **`ℚ_{>0}` is the group of Poincaré return maps**."*

**(c) The length assignment** — *"the length of `r` should be the logarithm of the norm `N(x₀)`…
**for `X₀ = spec ℤ` the periodic orbits should correspond to the prime numbers `p`, the length being
`ℓ(r) = log p`**."* The Hasse–Weil zeta becomes the **Ruelle zeta**.

> ### The correspondence is **NOT one-to-one** — corrected at source: closed points correspond to
> **compact packets** of periodic orbits, *"not one-to-one but a bit more involved… reminiscent of
> invariant tori. **There are no fixed points of the flow.**"*

**Dimension, with his own dissatisfaction:** *"infinite dimensional whereas **ideally we would want
it to be of dimension `2 dim X₀`**"* · *"There is a minimal condition `E` for which our theorems hold
**but it does not look natural. So this aspect is unsatisfactory.**"*

## §2 — E2: THE COHOMOLOGY AND THE ARREST, IN HIS OWN WORDS

> *"the zeroes and poles of `ζ_{X₀}(s)` **should be the eigenvalues of the infinitesimal generator of
> the induced `ℝ`-action on foliation cohomology groups** of `(X₀,F)` with compact supports."*

**That is `Θ` and the zeros in his own notation — and the verb is "should be". E2 CONFIRMED:
ABSENT-AS-CONSTRUCTION.** What is proved instead: *"**the zeroth foliation cohomology of `(X₀,F)` is
one dimensional**."*

> ### THE ARREST, VERBATIM
> *"the **cohomological problem** pointed out in [KS18] … **persists in our more general context**.
> The **first foliation cohomology of `X₀` could be more or less the right one though**."*

## §3 — E3: SEAT-GRADING RE-RUN

| clause | act 1 | **act 2, at content** |
|:--|:--|:--|
| non-compact | MATCH | **MATCH** — `t` acts by `eᵗ` |
| native | MATCH | **MATCH, STRENGTHENED** — built from `X`'s own structure sheaf |
| carries `s` | PARTIAL | **PARTIAL — now sourced as HIS OWN explicit desideratum**, no longer our inference |
| zeros-capable | NOT ESTABLISHED | **NOT ESTABLISHED — arrest now at cite** |

## §4 — THE CONSTRAINT SET *(the deliverable)*

**C1** carry the `ℝ`-action with `Θ` its generator, zeros as eigenvalues · **C2** be foliation/leafwise ·
**C3** `H⁰` one-dimensional · **C4** Deninger's axioms · **C5** **no real structure on `H¹`** ·
**C6** `ℂ`-coefficient, infinite-dimensional · **C7** reproduce `Γ_ℝ` and the Euler factors ·
**C8** compatible with `HC^ar` · **C9** not our `H⁰`.

> ### `C1` and `C5` are the specification's teeth — and both are Deninger's. He states what the
> cohomology must do and proved a theorem about what it cannot have. **Bounded above and below by
> the same author.**

## §5 — THE WITT TRIANGLE: CHECKED, AND IT RESOLVES

> *"the ring `W_rat(A)` of rational Witt vectors is a **SUBRING of the big Witt ring
> `W(A) = 1 + tA[[t]]`**."*

**`p`-typical `W(k)`: NOT asserted related here, NOT assumed — the trap held.**

> ### THE JUNCTION'S SHARPEST COORDINATE, FILED FIRST-CLASS: **the seat and the `Δ4` Frobenius
> holder share a construction.** `W_rat ⊂ W`, stated by the source — and the ringed space carries
> **commuting Frobenius endomorphisms for every prime `p`**, which is Borger's `ψ_p` family, the
> exact shape of `Δ4`. *Checked, not hoped; the check returned yes.*

## §6 — FOUND-BEYOND *(two)*

1. > ### **TWO `H⁰`s, DISTINCT.** His zeroth foliation cohomology is **one-dimensional**; ours is
   > **`𝔸_f/ℚ`, `Θ=0`**, infinite-dimensional over `ℚ`. *The registration armed the name-identity
   > trap on `W`; the collision came on `H`.*
2. > **The author of the seat is the author of the no-go.** `C1` and `C5` are both Deninger's, six
   > years apart. **The constraint set cannot be loosened by finding a friendlier source — there is
   > no second opinion to seek.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `86757e4` (registration) → this pass |
| relay | this report's commit |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**`Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow. Realization is not placement.**
