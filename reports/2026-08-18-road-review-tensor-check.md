# THE ROAD REVIEW · THE TENSOR CHECK RELEASED
## ### **`Son = V₁ ⊗ W₁` — THE FOURIER-DUAL SQUARE, THE FIRST LAW EXPLAINED — AND BOTH ROAD DOCUMENTS AT THE STRANGER-TEST STANDARD**

**Relay report · 2026-08-18 (eighteenth sitting; the machine clock reads late 2026-08-17 — the zip keeps that date-tag, noted not smoothed) · author-called · `INSTRUMENT` track · sub-gate declared · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 944f5788469b21992ff39545028f3f53817d3546   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 944f578   ### 22-file roster + MANIFEST, 3 probes PASS
SIDE-window   aacb415 = tag v0.5.0                      SIDE-effects  afa9ccf — untouched
```

> ### **RULE-3 LOG:** *`B12`'s fusion, the fifth law, and the Deninger content grade — all banked ✓.
> Banned grep: clean; terms coined: none.*

---

## §1 — ### **THE TENSOR CHECK — `T1`, CERTIFIED, WITH THE DECOMPOSITION EXPLICIT** *(held since `B7`'s rider; released by the author; registration with the longhand derivation banked BEFORE any number)*

> ### **THE DERIVATION:** *in the chart `m = a + p^n·b`, the ball is exactly `{a = 0}` — so the support
> condition is `f ∈ V₁ ⊗ L²` — and the `a′ = 0` Fourier rows see only the `b`-MARGINALS — so the
> transform condition is `f ∈ L² ⊗ W₁` (mean-zero). The intersection is exact:*
>
> ### **`Son(p,n) = V₁ ⊗ W₁`, `S = P₁ ⊗ Q₁` — A TENSOR SQUARE WHOSE TWO FACTORS ARE THE SAME
> ONE-POINT-VANISHING CONDITION ON THE TWO SIDES OF THE TRANSFORM — AND `dim = (p^n−1)²` IS EXPLAINED,
> NOT MERELY MATCHED.**
>
> **Certified at seven cells:** *operator-Schmidt rank EXACTLY 1; factors match `P₁`, `Q₁` to machine
> precision; `sv[0] = p^n − 1` exactly (an unregistered consistency, noted).*
>
> ### **FILED AT QUESTION GRADE, `OURS`, NO PROMOTION: the finite place holds a SELF-PRODUCT IN
> MINIATURE — the local shadow of "what supplies the leaves" — cross-linked to column `(vi)` and Road A's
> missing self-product. A local tensor square is not a global self-product, said on the face.**

## §2 — THE ROAD REVIEW *(both documents opened to a stranger)*

*`THE_SEMILOCAL_CARRIER_SPECIFICATION` `v0.12` and `THE_CARRIER_BUILD` `v0.6` each now open with* ### **a
one-screen "WHAT THIS DOCUMENT HOLDS, AT GRADE"** *— the columns `(i)`–`(vi)` with grades per frame and
per candidate; the checkpoints (`B1`–`B13`; the acts) one line each, what each landed and what each
refuted; the held items at their addresses (the `(β)` convention on the TRUE functional · the
`1807.06400` body read · the two `HELD` carrier acts on counsel · quotient-form positivity UNKNOWN and
said so); the four-word question with its three-road provenance —* ### **WHAT SUPPLIES THE LEAVES:
Road A's missing SELF-PRODUCT · `(vi)`'s missing LEAFWISE METRIC · the missing FROBENIUS LIFT** *— and
the plain sentence:* ### **nothing here is a construction, none of it moves `h2`, all of it specifies the
object `h2` would rest on.** *No internal to-do lists on the face.*

**And the loom carries the week** *(dated 2026-08-18 at the foot)*: **corrections 18–24** with dates and
registers · **the laws minted** (the range law; the matching law with its scope; the phase boundary;
silence-on-weights scoped; the six model laws — the first now EXPLAINED; fixed-point localization + the
absorption identity, PROVED; Rule 3, both halves) · ### **the Rule-3 log: FOUR FIRES, EACH CAUGHT BEFORE
DEPENDENCY** *(the Selecta misattribution → located and banked from the right source; the summand shape →
read-refuted; the `U`-invariance premise → refuted and replaced by a proof; the Euler-factor reading →
refuted by its own ordered discriminator), with the two minor flags beside them.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE TENSOR VERDICT IS `T1` WITH THE STRUCTURE EXPLICIT — the week's last held bench item is
   closed, and it closes by EXPLAINING the week's first law.**
2. ### **BOTH ROAD DOCUMENTS NOW MEET A STRANGER AT THE DOOR** — one screen each, at grade, with the
   four-word question and the h2 sentence on the face.
3. ### **THE WEEK IS CONSOLIDATED IN THE LOOM** — corrections, laws, and the Rule-3 record in one dated
   place.
4. **Standing open, at their addresses:** the `(β)` convention on the true functional · the `1807.06400`
   body · quotient-form positivity · the two `HELD` acts (counsel, then your word).

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
