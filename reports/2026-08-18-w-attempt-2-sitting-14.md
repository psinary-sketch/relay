# `W-ATTEMPT-2` · SITTING 14 — THE BUILT OBJECT AGAINST THE PENTAGON AND THE VOICES
## ### **ROW BY ROW: R4's PREDICATE IS INSTANTIATED-AND-CHECKED AT THE MODEL (never asserted) — R2's POLAR PAIR IS THE FLIP-CHANNEL STRUCTURE SEEN LOCALLY, AT KINSHIP GRADE — R3's OPEN `∀∃⟹∃∀` HAS A SHADOW-INSTANCE IN THE DIAGONAL, NOT A DISCHARGE — AND `N5`'s TWO HALVES ARE NOW BOTH MEASURED FACTS ON ONE OBJECT**

**Relay report · 2026-08-18 (thirty-fourth sitting) · author-ruled ("follow through the ripe fruit;
interrelate; verify") · Fable throughout · relay + the road documents · nothing deposits**

> ### **THE FRAMING CORRECTION ON THE FERRY'S FACE, CARRIED: the review packet is a RECORD kept
> current, not a next act — research continues until the corpus's own light is fully interrelated
> with what was built. The pentagon's non-fusion law governs every row below: NO cross-register
> equivalence is compiled or claimed; verdicts are at kinship/model grade with both branches longhand.
> CORRECTION TWENTY-SIX applied first (item 0): "norm-blind"/"resolution-blind" repaired to content
> ("insensitive to the norm / to resolution") in all living prose; the banked b30 files carry the stems
> as bench record, censused in the correction entry. `h2` untouched.**

> ### **RULE-3 LOG:** *the compiled surround read at `THE_CARRIER_BUILD` `§12` (deposit pins
> `SIDE-kernel v1.5 = 0e5233f`, `SIDE-lv-conservation v0.10.0 = 93c27ec`, as banked there) ✓ · the
> pentagon rows and Voice7's terminal quoted from `§12.1`'s own table ✓ · sittings' certificates at
> `8446941` ✓. Terms coined: none.*

---

## THE TABLE — ONE ROW PER FACE, BOTH BRANCHES LONGHAND, VERDICT AT GRADE

| face | the compiled content (`§12.1`, at pin) | the built object's measured fact | ### **verdict at grade** |
|:--|:--|:--|:--|
| ### **R4 — positivity** | `λ_n ≥ 0` as a `Prop` ON A STREAM — positivity as PREDICATE (the `W-2`-compliant form); sharpest form the channel inequality `λ_Z ≥ −λ_A` | `B\|_class ≻ 0` at every living cell, EXACTLY, with the mechanism `B = L²-Gram ⊗ C\|_fix` riding the Euler coefficients `(4,1,1) > 0` | ### **R4-AT-THE-MODEL: the predicate INSTANTIATED AND CHECKED on one built instance — definiteness stayed a predicate throughout (the pentagon's law kept).** *What separates it from R4 at complete roster, in the pentagon's own terms: (1) the STREAM — R4's `λ_n` runs over all `n`; the model's class is finite-dimensional at finite cells (`N4`'s genuinely-infinite-range unmet by construction); (2) the OBJECT — `λ_n` is ledger-side, `ζ`-level; the model's form is the section's τ-pairing: equating them is the cross-register fusion the law forbids; (3) the ROSTER — finite place sets vs R1's universality; (4) the CHANNEL INEQUALITY — `λ_Z ≥ −λ_A` needs a zero channel the model does not carry (see sitting 16). Branch (b) refused-over-read: this might have been mere Gram positivity of an arbitrary form — refuted by the mechanism: the positivity rides EXACTLY the Euler-side arithmetic, R4's own subject matter, at class resolution.* |
| ### **R2 — conservation (the involution×weight polar pair)** | every `ξ`-zero forces the Euler balance at some prime — "the balance equation IS the involution acting on the weight variable at a prime" | the negative directions live ONLY in the channel the involution flips (antipode-anti-invariant, structural) and first appear AT a prime's arrival edge, with place-support named | ### **THE FLIP CHANNEL IS R2's SHAPE SEEN LOCALLY — KINSHIP AT MODEL GRADE, NOT IDENTITY.** *The compiled voices force `σ = ½` as the fixed point where the involutions' disagreement vanishes; on the model, the involution-FIXED sectors are exactly where the pairing is positive and honest, and the disagreement channel is exactly where negativity lives, localized at primes. Branch (b): no compiled equivalence exists between R2's balance and the sector arithmetic — the kinship is structural (role-for-role), filed `OURS`, question grade, promoted nowhere.* |
| ### **Voice7 (`topological_no_sigma_preference`)** | RESTS at `½` (`c7_rests_at_half`) and FORCES NOTHING — one voice rests where five force | the antipode-anti-invariant sector: present at every cell where its sectors exist, carries the negatives, and FORCES NOTHING ON THE CLASS (the class stays positive regardless of its size) | ### **ROLE-KINSHIP ONLY, SAID PLAINLY: both are the present-but-non-forcing component of their structure. Voice7 is topological and `σ`-level; the sector is class-algebraic. The resemblance is of ROLE, not content — named at `OURS`, never merged.** |
| ### **R1/R3 — roster / totality-through-places** | R3 = `∀∃ ⟹ ∃∀`, NOT compiled — the repo's open `sorry`; R1 universality | the diagonal `D(a)`: per-place data (`∀` place `∃` cutoff) realized JOINTLY by ONE bound (`∃ a ∀` places) — support and place one datum, T-invariance exact | ### **A SHADOW-INSTANCE OF R3's SWAP AT FINITE ROSTER, NOT A DISCHARGE.** *What the diagonal DOES supply: a constructed witness that the per-place/joint swap is REALIZABLE on the model at every finite staircase depth, exactly. What it does NOT supply: the swap as a theorem over the full roster — the diagonal's `∀` ranges over live places only; the double limit stays open; the open `sorry` stays open. Both said.* |
| ### **R5 — spectral distance** | input certified, output DISCLAIMED; the `C₅`-distance compiled as a boundary marker (`certifiedInput_not_zeroRealizing`) | the object's traces are exactly ZERO (`t_k = 0`, localization proved) and its identifications are all ARITHMETIC-side (coupling = Euler coefficients); nothing zero-realizing anywhere | ### **CONSISTENT WITH THE COMPILED BOUNDARY MARKER, AND R5 IS NOT MOVED: the built object is certified-input-shaped, not output-shaped — its trace channel is zero-side-silent by its own proved mechanism.** |
| ### **`h1_complete_at_Phi` — the remainder ("placement")** | eight conjuncts jointly satisfiable at one witness `Φ`; the remainder at that witness is exactly PLACEMENT (`h2`: `mellin Φ(s/2) ≠ 0`); `Φ` is `ζ`-level, no class resolution | the constrained class PLACES SIGN-SECTORS (which directions carry which sign, by place-arrival arithmetic) — it places nothing about zeros (no zero enters the model) | ### **TWO DIFFERENT PLACEMENTS, KEPT APART: the object refines where DEFINITENESS lives without touching where the ZEROS live. And one genuine landing:** ### **`N5`'s SENTENCE ("the difficulty localizes at `ψ₀` while the structure needs the full class resolution") NOW HAS BOTH HALVES MEASURED ON ONE OBJECT — the poles live only in the `ψ₀`-channel (sitting 7's derivation, the vanishing conditions) AND the coupling does real work only at full `ℂ[Cl]` (T7, measured). The fit `§12.2` recorded as a reading is now a bench fact.** |

## COMPILABLE CONSEQUENCES NAMED *(fed to sitting 16's item 4 list)*

*`constrained_class_positive` (the sector-forcing + `C|fix ≻ 0`) · `off_class_negative_antiinvariant`
(the flip-channel theorem) — both decide-reachable at the small cells; the R3-shadow as a finite
instance statement (integer). None started.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO CROSS-REGISTER EQUIVALENCE COMPILED OR CLAIMED.
NOTHING DEPOSITS. NOTHING CIRCULATES.**
