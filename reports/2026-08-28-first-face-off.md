# THE FIRST FACE-OFF AT BENCH — b240, 2026-08-28

**Scope:** a bench computation and its registration. ### **A finite-place-set object at a finite cell
decides NOTHING global (b14/b15), and no branch of this act discharges, weakens, or moves `h2` or the
register sentence.** No grade of any other object moves; no axis, mesh, ε or constant was chosen or
changed after a residual was seen. Nothing deposits.

> ### **THE CEILING, QUOTED FROM ITS OWNERS AND PRINTED IN THE RUN'S OWN TABLE HEADER:**
> **b14:** *"a **finite-place-set object at a finite model cutoff** — the complete roster is the
> double limit and **STAYS OPEN whatever this act shows**."*
> **b15:** *"**a finite-place-set object at a finite cutoff decides nothing global**."*

---

## The verdict, in the banked words

### **BRANCH (DISSONANT)** — at all six diagonal `a²` cells, by the registered factor `D = 10`.

> **The banked meaning, quoted whole:** *"MEANS: A FINDING, REPORTED AT FULL PROMINENCE. And the
> indictment order is registered here, in advance, so that no suspect is convicted by placement."*
> ### **And its second half, which is the half a reader skips:** *"A dissonance is evidence about
> **the assembly this act performed** before it is evidence about the identity."*

| `a²` | `L` | `R` | `\|L − R\|` | combined bar | separation / bar |
|:--|--:|--:|--:|--:|--:|
| 2 | 6.094519 | −1.990528 | 8.085e+00 | 7.453e−01 | **10.85** |
| 3 | 5.104067 | −2.015384 | 7.119e+00 | 5.042e−01 | 14.12 |
| 4 | 4.711349 | −2.035817 | 6.747e+00 | 3.992e−01 | 16.90 |
| 8 | 3.915534 | −2.086520 | 6.002e+00 | 2.672e−01 | 22.46 |
| 9 | 3.949318 | −2.097219 | 6.047e+00 | 2.493e−01 | 24.26 |
| 12 | 3.731639 | −2.119732 | 5.851e+00 | 2.034e−01 | 28.76 |

`L := T.value + Q.value := (Tr_full + E2 + Δ₋) + Θ_q` — the executed **RULE M-1** — against
`R := A − PR`, the adopted ledger at the atlas's convention. ### **`Θ_q` entered as the instrument
returns it: no sign was inserted, because no ruling authorizes one and inserting one to help the
columns meet is b229's named crime.**

### **The closest cell, reported and not smoothed:** `a² = 2` clears `D = 10` **by 8.5%**. ### **Had
the factor been 12, that cell would read (INDETERMINATE).** The factor was fixed before any number
existed and is not revised now; the act's branch is unchanged either way, because the other five
cells clear it by 14× to 29×.

---

## 1. What made "before" checkable

Every meaning, bar, factor and branch was banked in `data/b240_meanings.txt` and **hashed** —
`ffef2e97…ca50d4c1` — **before the first instrument call**; the run prints that hash into its own
output. ### **The gate tests both limbs: the hash appears in the run, *and* the meanings file is
older on disk. Either limb alone is forgeable.** The meanings script **imports no instrument at all**,
and the gate proves that by running the same test against the run script, where it fails.
### **The diagnostics tool refuses to execute unless the branch is already on disk** — the ordering
is enforced by the tool, not by an executor's word.

## 2. The registered expectation — exact as arithmetic, wrong as a diagnosis

Written before the run, from the instrument's source and no number: the instrument defines
`resid := Tr_full − A − E2`, so `Tr_full = A + E2 + resid47` identically, whence

### **`L − R = 2·E2 + Δ₋ + resid47 + Θ_q + PR`**

> ### **The run reproduces it to `8.9e−16` at every cell.**

| `a²` | `2·E2` | `Δ₋` | `resid47` | `Θ_q` | `PR` | `L − R` |
|:--|--:|--:|--:|--:|--:|--:|
| 2 | 3.358857 | 0.677615 | **4.048575** | 0.000000 | 0.000000 | 8.085046 |
| 3 | 3.033289 | 0.605701 | **3.373977** | 0.000000 | 0.106484 | 7.119451 |
| 4 | 2.748101 | 0.540018 | **3.047750** | 0.161978 | 0.249320 | 6.747167 |
| 8 | 2.192479 | 0.410725 | **2.520787** | 0.317018 | 0.561045 | 6.002054 |
| 9 | 2.116618 | 0.393176 | **2.453999** | 0.473862 | 0.608882 | 6.046537 |
| 12 | 1.950128 | 0.354973 | **2.313445** | 0.518491 | 0.714334 | 5.851371 |

### **And the half that was wrong.** The registration named the ε double-count as the expected fault.
The mechanism is confirmed exactly — but **removing the second `E2` (variant V1) leaves 4.88…6.41 of
a 5.85…8.09 separation**. ### **The ε double-count is at most a fifth of it. The dominant term is
`resid47`.** b37 recorded *"resid47: 0 by construction (substitution at content)"* — that is the
**substituted** reading, where CC's Thm 4.7 replaces the raw mode sum. ### **The ruling's C2 binding
names the RAW instrument channels, not the substituted object.**

## 3. The indictment, in the order registered before the numbers

- **SUSPECT 1 — the uncertified envelope and the axes. ### LIVE, AND LARGER THAN EXPECTED.** The
  right side behaved: `|dR|` from `4.4e−16` to `4.1e−08`, and **b238's banked projection held at
  every cell** — including the three whose `K` was a declared **surrogate** (`a² = 8, 9, 12`, never
  measured by b238). ### **But `bar_L` is six orders above `bar_R`** (0.203…0.745 against `~1e−07`),
  and it is **four times ONE STEP** of the registered mode refinement. ### **That is one step of a
  series whose tail nothing in this record bounds** — b238's own species, one level up. The step also
  moves `NQ` and `NMODE` together, so it mixes quadrature with truncation. **Filed:
  `W-ORD-LEFT-MODE-AXIS`.**
- **SUSPECT 2 — the three-normalizations species. ### LIVE; mechanism confirmed, size refuted** (§2).
- **SUSPECT 3 — the assembly conventions. ### LIVE AND UNRESOLVED, AND NOT BY THIS ACT.** Flipping
  `Θ_q` moves the separation by ≤ 1.04 and closes nothing; ### **the restricted-product assembly (M-2) was
  never performed, so this act cannot indict it.**
- **SUSPECT 4, AND ONLY LAST — the form `T + Q = wInf − wPrimes`. ### NOT INDICTED**, and the ground
  is arithmetic rather than loyalty: suspects 1 and 2 account for the whole separation **term by term to
  machine precision**, and its largest term rides an unbounded truncation. ### **THIS ACT PRODUCES NO
  EVIDENCE AGAINST THE IDENTITY, AND CITING ITS BRANCH AS ANY WOULD BE A MISREADING.**

## 4. The finding this act did not go looking for

### **The left side is the unconverged side, by six orders of magnitude.** The right column has been
budgeted (b238) down to `np.interp` and a `1.2e−13` float floor; the left column's uncertainty is
estimated from **a single refinement step of a mode sum**, and its size — 0.05 to 0.19 per step —
is larger than every term of the ledger except `A` itself. ### **Before the identity can be tested
sharply at bench, the left side needs the budget the right side already has.**

## 5. G-INDEP, G-STAB, G-PRIOR

**G-INDEP — run from source, not asserted.** No left-side function calls or receives the right side's
output, and conversely; the run prints each instrument's parameter list. ### **What *is* shared is
`a`, `corr = w ⋆ w`, the place set and the atlas constants — and each sharing is the identity's own
content: an identity at a cell for a test function is not two claims at two test functions.**

**G-STAB.** Both sides at the base axes and one registered refinement each, spreads quoted in full;
no further refinement after the residuals were seen.

**G-PRIOR.** §20(a)'s columns, act 12's residual collapse and §25(c)'s numerical half are listed as
historical in the meanings file. ### **None of their numbers entered the criterion** — the criterion
is b238's banked budget plus this act's own G-STAB spread, and nothing else. The corpus's own
deviation object `D_dict` was computed **last, in the diagnostics block, after the branch**, and is
printed only to show it is a **different object**.

## 6. Gates, filings and mechanics

**Instrument gates:** C0 void gates PASS; ε mask algebra PASS; the `ε′(1⁺)` pins PASS **including the
odd split 8.819138 of 22.996476 — verified before `Δ₋` was used, not after**; kernel-cache gate PASS
(`NU`/`UMAX` never moved, and the hazard of a cache keyed on nothing was named in advance).
**b240's own gates: 15 of 15 PASS, CLEAN, on the first run.**

**Filings.** The bank `data/b240_first_face_off.txt`; the meanings, run and diagnostics files;
`THE_IDENTITY_CHAIN` **§38**; the in-flight register; **`W-ORD-LEFT-MODE-AXIS`** new;
**`W-ORD-IMP1-ENVELOPE` standing** — its registered role was to gate a verdict on (INDETERMINATE),
so it does not gate this one, ### **and it is not discharged.** The index was queried before the
filing (`face-off` → **NO KEY**) and the result is now keyed as **`first-face-off`**.

**The checklist now reads five items.** (1) `T.value` **GREEN**. (2) M-2 **OPEN**, not required and
not performed — ### **so this face-off is a per-cell statement and not a structural one, exactly as
b239 said it would have to be.** (3) the re-print **GREEN**. (4) the right side's bars **AMBER —
MEASURED, NOT CERTIFIED**. ### **(5) the LEFT side's bars — AMBER, and worse than (4).**
**M-list: four open (M-2…M-5).**

## 7. Two misses of my own, both caught by the corpus's own instruments

**MISS 1 — I wrote a banned term into this act's own voice, 44 times.** The stem `gap` is banned;
the first term scan came back ### **NOT CLEAN — 33 live uses in relay, 11 in PLACE-papers**: the
bank, the report, the register entry, §38, the HANDOFF lead, the index row and two tools.
### **The scan is the only reason it is not in the record — I did not notice writing it, and
noticed nothing re-reading it.** Repaired: every occurrence in this act's own text now reads
**separation**, and all three scans re-run **CLEAN, 0 live**.

**MISS 2 — and the repair over-reached, which is the worse of the two.** The repair was a whole-word
global replace per file. `VERIFICATION_LOOM.md` is **append-only history**, and the replace rewrote
### **seven lines belonging to earlier acts** — among them a Yang-Mills row whose subject *is* the
mass gap, an object whose **name contains the stem**. ### **A repair to my own voice edited another
act's recorded sentence.** Caught by the same scan: a hit at **line 548**, four thousand lines above
anything this act appended — ### **a hit outside my own block was the tell.** Repaired properly:
both PLACE-papers files restored to HEAD and the blocks re-appended with the stem fixed **in the
block only**; `git diff` now shows **one hunk per file, pure insertion, 168 lines, no pre-existing
line touched.**

> ### **THE LESSON, NAMED: a global replace on an append-only document is an edit to the record
> wearing the clothes of a fix.** The blast radius of a repair is checked the same way its target
> was found — **by diff, not by intent.**

---

### **What this act did not do:** no meaning invented after the numbers; no variant promoted to
primary; no bar widened to cover a residual; no axis introduced after a residual was seen; no grade
moved (b237's four channels stay **PARTIAL**); no asset graded by agreement with the ledger; no
kernel file touched; nothing about `h2` beyond the register sentence exact.
### **NOTHING DEPOSITS. LOCKS LAST.**

**Next: the author's fork at the STOP** — the patent session, the reconciliation wave, or the engine
(M-2…M-5) — with **`W-ORD-LEFT-MODE-AXIS`** and **`W-ORD-IMP1-ENVELOPE`** as the two bounded bench
items either way.
