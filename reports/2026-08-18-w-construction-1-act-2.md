# `W-CONSTRUCTION-1` · ACT 2 — THE ARCHIMEDEAN CLOSURE, THE SECTOR SUM, THE REMAINDER'S ADDRESS
## ### **`(C-dense)` PROVED IN THREE STEPS — ROUTE 1's LOAD-BEARING STATEMENT CLOSES, AND THE GLOBAL DENSITY REDUCES TO ONE NAMED LEMMA — THE SIGN IS FULL-SUM (THE `h = 1` CASE DECIDES) — BOTH BRANCHES OF THE REMAINDER'S ADDRESS POINT OFF THE OBJECT — FILE A STARTED AGAINST THE LOCAL MATHLIB**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 2 · sub-gate declared · Rule 4 · Fable/high
effort, proof-search · nothing deposits · nothing circulates**

> ### **SCOPE, once: the global object is built and abstract; clause (e) is one closure at `∞`
> (NOW PROVED) plus one identity (NOT claimed); the register moves only via the closure protocol.**

> ### **RULE-3 LOG:** *act 1 at `19d8fc1` ✓; the three findings ✓; R2-1's CNU theorem ✓; `R(g)` ✓;
> CC's Selecta §4 completeness used only through the corpus's banked prolate pins. Terms coined:
> none. TWO instrument notes, disclosed not smoothed: (1) b34's FIRST run was VOID by an ordering
> defect (SVD-orthonormalization scrambled the registered prolate order — the slice tested was not
> the registered slice); the fixed run is the record. (2) The b34 registered `(S-covered)` gate
> (≥ 0.8) was NOT met — the honest verdict is `(S-third)`: coverage RISES monotonically
> `0.33 → 0.53` through `d = 40` — directional corroboration of the density's slow fill; the PROOF
> does not lean on the bench.*

```
build document  THE_GLOBAL_SECTION.md v0.2 (§§6–8 added)
instruments     tools/e16/b34_e1_fair_slice.py (registration banked; first run VOID by defect,
                disclosed; fixed run banked) · data/b34_registration_2026-08-18.txt ·
                data/b34_2026-08-18.txt
mathlib layer   tools/lean/mathlib-companion/PadicStandardAddChar.lean — file A STARTED
```

---

## THE CHECKPOINTS

1. ### **R1-4 — `(C-dense)` PROVED:** *`cl(Π₊SΨ_Weil) = E₁(∞)` — (1) Hermite completeness under the
   Gaussian weight (the moment problem determinate ⟹ polynomial density; the `W-exact` mechanism at
   its true depth); (2) projections map dense sets to dense sets of their ranges; (3) compose.* ∎
   ### **The load-bearing statement of Route 1 closes. Global `(D-dense)` reduces to THE
   FACTORIZABLE-IMAGE LEMMA (named exactly: one test function yields one section; independent
   per-place factor control needs the class's algebra — not proved this act).** *Bench beside it:
   the fair-slice coverage rises `0.33 → 0.53` (the tail, not a wall).*
2. ### **R1-5 — THE SIGN IS FULL-SUM:** *`B = +‖·‖²` on the ENTIRE sector sum (one line: the
   eigenvalue product is `+1` there), `−‖·‖²` on the `−1` grading, null on `±i`;* ### **and the
   `h = 1` case decides the re-grade as ferried: over `ℚ` T-fixed = the full sector sum — the
   finite-instance positivity was FULL-SUM all along; the principal-vs-full distinction was
   class-resolution structure.** *Sitting 9 re-read: arrivals rescale the global sector ledger
   without tilting it.*
3. ### **R1-6 — BOTH BRANCHES POINT THE SAME WAY:** *(T) the CNU theorem forbids the zero channel as
   point spectrum of the object's own scaling — on-object only an a.c./distributional layer remains
   possible, NAMED not developed; (A) the ambient the object excludes (the pole/ball directions,
   theta's home) is the classical entry of the zero side — and with `(C-dense)` proved the on-object
   hiding room SHRANK.* ### **The `Z` channel's address is the codomain's — off the built object, in
   what clause (e) would add. Said so; no promotion.**
4. ### **FILE A STARTED, AGAINST THE LOCAL MATHLIB:** *`PadicStandardAddChar.lean` — the real
   construction (the `p`-power fractional part via `toZModPow`), the AddChar structure, and the
   target statement `stdAddChar_eq_one_iff` (conductor `ℤ_p`); hard proofs `sorry` AND SAID SO. The
   local Mathlib build was PARTIAL; the needed modules were built in-act (`PadicIntegers`,
   `RingHoms`, incremental, ~35 s) and* ### **the file ELABORATES CLEANLY: 0 errors, 7 declared
   `sorry`s — the construction is type-correct against real Mathlib (toolchain `v4.30.0-rc1`,
   checkout `cecd0c4d56`).** *The precedent note governs the campaign: one statement per PR, the
   author's own hand in review responses.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED. NOTHING PROMOTED.
NOTHING DEPOSITS. NOTHING CIRCULATES.**
