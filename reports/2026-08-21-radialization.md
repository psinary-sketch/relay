# THE RADIALIZATION ACT — CLOSURE-SEQUENCE STEP TWO: THE SHELL-SUMMING MAP BUILT, ITS TWO CLIENTS AND THE PLANE QUESTION DECIDED
## ### **ALL FOUR PREDICTIONS LAND ON THEIR REGISTERED BRANCHES; NOTHING RESISTED. THE MAP EXISTS AT EXACT GRADE — ITS DECIDABLE SPINE IS THE GCD LAW. THE DEFECT DICTIONARY CLOSES AT THE OPERATOR/INDEX GRADE: THE RAW TRANSFER INTERTWINES WITH THE SHIFT EXACTLY, THE COMPRESSED TRANSFER DIFFERS AT EXACTLY THE BALL-BOUNDARY SHELL — THE DEFICIENT WEIGHT'S ADDRESS IS THE SEED'S ADDRESS, DECIDED FROM BOTH SIDES — AND ROW 50'S RESIDUE CLOSES WITH N5'S VECTOR STEP NAMED. THE THETA CLIENT'S FINITE HALF LANDS: THE CHART THETA-OBJECT ASSEMBLES BY SHELLS WITH THE PRIMITIVE SHELL SILENT, NO CLASSICAL IDENTIFICATION, THE BRIDGE CONTINUATION STAGED. THE PLANE QUESTION: YES. CORE 230/230.**

**Relay report · 2026-08-21 · ferry-executed (part 1 of 1, receipt confirmed, Rule 1) ·
Rules 3/4/5 · the b76 run-registration (the map's laws and every theta shell sum
longhand before any run; the predictions verbatim) banked first · everything
finite-side and exact · THE S1 FIREWALL ABSOLUTE — the odd ladder's archimedean
use is step three's territory; nothing archimedean computed · THE CLASSICAL
THETA UNCLAIMED (the act-6 antipode refutation cited at the theta client's
head) · nothing about `h2` claimed · nothing deposits · nothing circulates
beyond this report.**

> ### **RULE-3 LOG:** *(a) the staged registration read at content and run
> exactly as filed — divergences: none. (b) the shell model's bank read at
> content this act (b61_theta_unit.txt: the truncated shift, all weights 1, ONE
> deficient weight at the ball boundary; tr Tᵏ = 0 at sizes 3, 5, 7, 9; defect
> exactly (1,1); the seed address v = n−1; N5's vector clause awaiting the
> operator's banked form — which b66 banked); rows 29, 41, 45, 48, 50 at their
> pins; the banked traces (rows 35–36, banked grade); the plane's radial
> coordinate (row 53, decided grade). (c) NAVIGATOR-OPEN: nothing new — N5's
> vector step is a NAMED remaining step with all pieces banked, not an invented
> object. (d) the four predictions pairwise independent given P1. ONE CARRIER
> SUBSTITUTION, disclosed: the eight-coordinate product type failed Decidable
> synthesis at the pinned toolchain; the banked List-Int carrier (b69's pattern)
> was used instead — identical content, a representation choice only.
> Banned-term grep on this act's writing: clean. Terms coined: none (R and its
> clients carry the staged registration's names).*

---

## §1 — THE PREDICTIONS, VERBATIM, WITH VERDICTS BESIDE THEM

> **(P1)** *"THE MAP EXISTS at exact grade on the banked cells: the
> shell-summing map R from the chart's g-family to the shell model, by
> valuation, reproducing the boundary line's shell address (the row 2^{n−1}
> seed to the shell v = n−1) and the banked boundary-index data at every banked
> shell size."* ### **LANDS.** R sums over the valuation shells; the decidable
> spine is the gcd law (gcd(m, N) = p^{v(m)}), so every shell statement is a
> gcd statement. The partitions at sizes 3, 5, 7, 9 (grade: decided, both
> parities at the banked cells); the seed addresses gcd(2ⁿ⁻¹, 2ⁿ) = 2ⁿ⁻¹
> decided at n = 1..4 — b61's seed_boundary_address derived from the map's
> side; the (1,1) boundary index re-derived (the doubling image misses the
> primitive shell; the zero cell terminal — decided). On the g-family:
> R(g_{a,t}) = q·e_{v(a)} when q | t and 0 otherwise — **the twisted rows lie
> in the kernel of R** (the root-of-unity column sums).

> **(P2)** *"THE DEFECT DICTIONARY (N5's client): R intertwines the chart's
> p-term transfer with the shell model's boundary-weight shift up to the banked
> normalization — decided at the banked levels; the radial-compression residue
> of row 50 closes or sharpens to its exact remaining step."* ### **LANDS — THE
> DICTIONARY CLOSES at the operator/index grade.** The raw intertwining is
> EXACT: gcd(2m mod N, N) = min(2·gcd(m, N), N) over the FULL ranges at N = 4,
> 16, 64, 256 (decided). The "up to the banked normalization" is located
> exactly: the compressed transfer differs from the all-weights-1 shift at ONE
> transition — the shell that doubles into the ball is v = n−1 alone (the
> filter identity, decided at the banked levels): **the deficient weight's
> address = the ball-boundary transition = the seed's shell — b61's coincidence
> now decided from both sides.** Row 50's residue CLOSES: R is the dictionary;
> the exit-space's twisted members are R-silent, so its radial image is exactly
> the boundary line's shell image (q·e_{n−1} ↦ q·e_n). **N5's exact remaining
> step, named and not run (not registered): the vector-level identity** — the
> compressed transfer's defect vector (V's banked finite form, b66) against the
> R-preimage of the model's boundary cell; every piece is now banked.

> **(P3)** *"THE THETA CLIENT'S FINITE HALF (step A's finite content): the
> radialized g-family assembles the chart theta-object at finite level with its
> quarter-turn symmetry — the chart-side identity only; no classical
> identification claimed; a landing stages the bridge's continuation, so
> marked."* ### **LANDS — chart-side only, the act-6 caution at the head.** The
> chart theta-object Σ_m ζ^{m²} is the quarter-turn's diagonal — the banked
> traces q(1+i) and q (rows 35–36, banked grade) — and it ASSEMBLES BY SHELLS,
> decided in exact cyclotomic coordinates: (2,1) shells (2i, 1, 1) totaling
> 2 + 2i; (3,1) shells (0, 2, 1) totaling 3 — **the primitive shell silent**;
> (2,2) shells (0, 4i, 2, 1, 1) totaling 4 + 4i — silent again (4ζ + 4ζ⁹ = 0
> in ℤ[ζ₁₆]). Step A's finite content lands; the identification of the chart
> theta-object with any classical theta is NOT claimed and is the staged
> continuation's own question (steps B–C ride the federation's proved
> completed-zeta symmetry, at cite). **The bridge continuation is STAGED, so
> marked.**

> **(P4)** *"THE PLANE QUESTION: R factors through the plane's radial
> coordinate, decided at index grade — the radialization and the plane meeting
> as the staged files asked."* ### **LANDS — YES.** The gcd multiplies across
> the place split (decided at D = 36 and 100), and per place the gcd IS the
> shell — R factors through the plane's radial coordinate. **N-OPEN-5 closes;
> the plane's other four marks stand.**

## §2 — THE KERNEL AND THE RANGE LAW

`RadializationShadow` — 5 terminals (`shell_partition_instances`,
`transfer_shift_gcd_law`, `ball_boundary_address`, `theta_by_shells`,
`plane_factorization`), each *"does not depend on any axioms"* — **Core
230/230**; row 54 filed, no blank cells.

**The range law, on the face:** every decided statement is a finite banked-cell
or full-range statement; the shell model's limit sense (the {V_n} question) and
everything archimedean stay exactly where the inventory left them — step
three's territory, behind the firewall; the classical theta unclaimed; the
junction stays a fence; nothing about `h2` follows.

## §3 — THE RECORD AND PINS

*Relay: the b76 run-registration + raw run + bank + instrument
(`tools/e16/b76_radialization.py`) + the working-layer copy
(`tools/lean/RadializationShadow.lean`) + the staged files' run-record and
N-OPEN-5 lines + this report. SIDE-global-section: `RadializationShadow` + row
54 + regenerated prints (230/230) + README. PLACE-papers: the act record with
the closure-sequence state. Mirror: refreshed —
**`mirror-refresh-2026-08-21-ac.zip` is the one to load.***

> **PIN LINE (post-push read-back, ls-remote):**
> `SIDE-global-section origin/main = 6b40c125b8b24506cb66a1277e86884a32d2c094`
> (the act commit; tag `v0.1.0` = peeled `706a81b` unmoved) · `PLACE-papers
> origin/main = 1bb72e42041b12c43db762f147874d3a97ffb8cd` (parent `34d1889`) ·
> `relay origin/main = 0ac66346afa247d20cbdfb19b85139d39f8f3ea7` (this act,
> parent `a16f7b1`; pushed from `push-radialization` per Rule 4.10, hook
> standing; HELD ancestry clean; the HELD commit's two carrier-build act files
> verified ABSENT from the public tree by name, count 0). **MIRROR:**
> `mirror-refresh-2026-08-21-ac.zip` built at PLACE-papers `1bb72e4`. *This
> pin-line commit's own SHA is stated in the closing message, per the regress
> rule.*

## FOOT

**The closure sequence after step two: steps one and two LANDED whole. Step
three next — the archimedean boundary line's opening (registration to be
produced corpus-first: the odd ladder radialized in the {V_n} limit sense, the
firewall's first sanctioned construction BEHIND the marker, at the recorded
standard) — then steps four through seven unchanged under the map's law. The
desk is empty; the restart kit stands complete; the author's mirror should be
at this act's export (`-ac`).**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED. NOTHING
DEPOSITS. NOTHING CIRCULATES.**
