# CHECKPOINT `B10` — THE ABSORPTION PROOF · THE ORBIT-QUOTIENT CELL
## ### **THE IDENTITY PROVED, THE QUOTIENT TRACE ALIVE WITH THE WEIL SHAPE — AND THE LEFSCHETZ COMPARISON MATCHES**

**Relay report · 2026-08-17 (fifteenth sitting) · author's word given · `INSTRUMENT` track · sub-gate declared · item 1 proof on Fable, item 2 bench · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**
**POSITIVITY GUARD: whatever any quantity's sign on the model, it bears not at all on `h2`.**

```
PLACE-papers  origin/main : 566ecfdca3db743c3210e1f2c89c18b0c1fb824f   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 566ecfd   ### 22-file roster + MANIFEST, 3 probes PASS
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> ### **RULE-3 LOG:** *`B9`'s localization banked ✓ · the absorption identity was certified-not-proved and
> is NOW PROVED (below) ✓ · the Weil-at-model closure banked ✓ · the Deninger seat at its banked grade ✓.*
> *Banned grep: clean; terms coined: none. The day's law governed item 2: the quotient was built as a NEW
> object with its own registration — nothing inherited.*

---

## §1 — ### **THE ABSORPTION IDENTITY, PROVED** *(the spec `§8.3-quinquies`; certificates at `data/b10_2026-08-17.txt`)*

**The structural fact the proof turns on:** `R_F` — functions whose transform is supported on the ball —
**is the space of `p^n`-PERIODIC functions.** *So `Π_E` (multiplication by `1_B`) and `Π_F` (the
conditional expectation over `p^n`-translates) COMMUTE, with product exactly `Π_Z` — certified `0` — and
`Π = Π_E + Π_F − Π_Z`.* **Each component trace equals `p^{−k/2}`:** *(E) the only diagonal entry of `Uᵏ`
sits at `m = 0 ∈ B` (coprimality); (F) the count has exactly `p^n` solutions; (Z) the ball is closed
under multiplication by `p`.* ### **Inclusion–exclusion: `Tr(UᵏΠ) = p^{−k/2} = Tr(Uᵏ)`, hence `t_k = 0`
∎.** *Every lemma certified to `10⁻¹⁶` at six cells including out-of-sample `(2,4)`.* ### **GRADE:
DERIVED AT THE MODEL**, *with the per-step `K_𝔭` transposition table and its two named caveats (the `L²`
issue for `O`-invariant functions; the orbital-integral regularization).*

## §2 — ### **THE ORBIT-QUOTIENT CELL — `Q1` LANDS: THE TRACE IS ALIVE**

**The object, defined before the run:** *`V_inv` = off-ball functions invariant under `m ↦ pm` — the
model's orbit space `x ∼ px`; `dim V_inv = (p−1)p^n` at all six cells (a fourth dimension law); the
Fourier-side Sonin half does not descend — registered as informative, the space changes exactly as `B9`'s
tension predicted.*

> ### **MEASURED: `T_quot(k) ≠ 0` — the fixed-point trace, moved to a closed orbit, is NOT absorbed** —
> *with exact rational structure (`16/7`, `162/13`, `32/5` at named cells) and*
> ### **per-lag-step ratio `→ √p/(p+1) = q^{−1/2}·(1+p^{−1})^{−1}`: THE WEIL SHAPE, CARRYING AN
> EULER-FACTOR-LIKE LOCAL CORRECTION** *(named question: truncation artifact or genuine local factor).*
> *Zeros at `k ≥ n`: the truncation edge, registered. Positivity of any quotient form: UNKNOWN AND SAID
> SO.* ### **COLUMN `(β)` HAS, AT LAST, A TRACE-SHAPED CANDIDATE AT MODEL GRADE.**

## §3 — ### **THE LEFSCHETZ WONDER — MATCH-IN-SHAPE, NO PROMOTION**

*The quotient trace is a weighted count over the closed orbit — period `log p` the lag unit, weight
`q^{−1/2}`-shaped — compared longhand at cite to:* **(a)** the explicit formula's prime-side term ·
**(b)** Deninger's seat (*"periodic orbits are related to the closed points"*, at abstract) · **(c)** CC's
arrest sentence (*"understand the above equality as a Lefschetz formula"*, `ATTRIBUTED`). ### **VERDICT:
MATCH-IN-SHAPE — a model-grade miniature of the seat's conjectured mechanism.** *Shape is not identity;
the correction factor is unexplained; the model is ours.*

> ### **ROADS A AND B TOUCH AT THE SEAT (`THE_CARRIER_BUILD` `§10`): the missing self-product/Weil
> cohomology (Road A's arrest) and the ledger-side finite trace (Road B's gap) are the same absence viewed
> from two roads — the object that would read the explicit formula as a Lefschetz formula.**
> ### **`R2`'s PRIORITY, RE-STATED FROM THE OUTCOME: the Deninger `H¹`/trace-formula read-debt is now
> Road B's highest-priority retrieval.** *Only the read can say whether the match survives contact with
> the text.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE VANISHING IS NOW A THEOREM AT MODEL GRADE** — proof banked beside its certificates, nothing
   left at certified-not-proved.
2. ### **THE QUOTIENT CELL DELIVERED WHAT THE TENSION PREDICTED: move from fixed points to closed orbits
   and the trace comes alive, Weil-shaped.** *Road B's gap now has a working miniature.*
3. ### **THE LEFSCHETZ SHAPE MATCHES, AND THE TWO ROADS MEET AT THE SEAT** — at model grade, no
   promotion, with the correction factor and the `H¹` read-debt as the named next questions.
4. **What wants your ruling:** *(i)* the Deninger retrieval (now Road B's top priority); *(ii)* the
   `(1+p^{−1})^{−1}` correction question (one bench sitting: the `n`-trend at fixed `k` discriminates
   truncation from genuine); *(iii)* the tensor check, still held on its trigger.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
