# `W-ORD-METRIC` — ACT 5: THE RIEMANN-FORM READING — 2026-08-11

**Registered at PLACE-papers `c18e89a` BEFORE any fetch.**
**REVIEW GATE: no Riemann form is constructed anywhere below. Nothing reached the gate.**
**Nothing deposits. Rail `de621b1` / `2147a03` unmoved.**

---

## §1 — E1 & E2: NOT AT CITE, DECLARED

Two API sweeps. `"Riemann form" AND "complex torus"` → **real zero** (well-formed feed). `"Neron-Tate"
AND "quadratic"` → four papers, **none stating the definition or the mechanism.**

> ### `E1` and `E2` are **NOT AT CITE** this act, and **not asserted from background knowledge.**
> **The reason is structural and is itself the finding: these are TEXTBOOK facts, and arXiv indexes
> RESEARCH PAPERS.**

The second fetch again appended its own conclusion. **Not inherited** — per the loom line filed one
act ago.

## §2 — E3: CONFIRMED AT CITE (from the source already held)

> *"**The similarity between the class group `Cl(O_F)` … and the JACOBIAN of an irreducible smooth
> curve** … the class group classifies line bundles on the **AFFINE** scheme `Spec(O_F)`, and the
> Jacobian of a **complete** curve is much better behaved. Following Arakelov one arrives at an
> improved analogy via **a sort of COMPACTIFICATION of `Spec(O_F)` by taking the archimedean primes
> into account.** One thus obtains … **a COMPACT GROUP; an EXTENSION OF THE USUAL (FINITE) IDEAL
> CLASS GROUP BY A REAL TORUS.**"*

> *"`Pic⁰(F)` is compact. **This statement is EQUIVALENT to Dirichlet's Unit Theorem and the
> finiteness of the ideal class group.**"* — with `0 → V/λ(O_F^*) → Pic⁰(F) → Cl(O_F) → 0`,
> **`dim V = r₁+r₂−1`.**

### TWO EARLIER GRADINGS CORRECTED FORWARD

- **Act 4** filed compactness-as-product-formula as *"OURS, not stated by any source read."*
  ### **Wrong — over-cautious. The source states the compactification reading. Corrected to AT CITE.**
- **Act 3** graded the `Jacobian ↔ class group` dictionary **used-not-stated**.
  ### **Now fully repaired — the statement site existed and act 3 had not reached it.** *The
  loom line stands as a general lesson; its worked example is retired.*

## §3 — E4: THE COMPLEX STRUCTURE IS ABSENT

*"complex structure"* — **0 occurrences.** The near-miss, quoted because it is genuinely close:
*"When `ν` is complex, `x_ν` determines **a hermitian product on `ℂ`**."*

> ### Hermitian structure exists — **on the fibers at the complex places.** It is **not** a complex
> structure on `Pic⁰(F)`, whose body `V` is **real** of dimension `r₁+r₂−1`.

**CM theory supplies complex structures to other tori — the adjacent-but-different door, NOT OPENED.**

> ### And for `F = ℚ`: `r₁=1, r₂=0`, so `dim V = 0`. **The real torus is a POINT.**
> ### **Fourth arrival of the same absence** — after survey, specification, and ingredient list, now
> in van der Geer–Schoof's own coordinates. *Carried no further.*

## §4 — THE FINAL DELTA **[MY-SYNTHESIS — FLAG CARRIED]**

> ### The arithmetic candidate is a real torus lacking **(i) a complex structure** and **(ii) a
> Riemann form.**
>
> `E3` is at cite; `E4`'s absence is at cite. **But `E1`/`E2` — the classical link making "no complex
> structure" imply "no Riemann form" — are NOT at cite this act. The delta's two halves are joined by
> a fact this register has not cited, so the MY-SYNTHESIS flag stays on.**

**`C5`/conjugation cross-reference, question grade:** the `ℤ/2` family's candidate unification —
**the missing order-two datum as the missing `i`** (`J² = −1`). ### **`SAME`/`KIN`/`DISTINCT` left to
texts. NO PROMOTION.** *The most attractive synthesis the era has produced, and exactly the kind
these rules exist to hold.*

## §5 — THE REGISTER BANKS

> ### **"This register specified `Δ2`'s raw material and located its final lack. The sign was never
> in scope, and was never approached."**

**`v0.5` consolidation QUEUED, not run. The `v0.4 → v0.5` digestion walk is re-offered as the wave's gate.**

## §6 — FOUND-BEYOND

1. > ### **The register corrected itself in the CONSERVATIVE direction, twice, in one act.** Act 4
   > labelled a source's reading as "ours"; act 3 graded a stated dictionary as "used-not-stated".
   > **Both errors were over-caution — claiming LESS support than the literature gives. Every
   > previously filed error in this corpus ran the other way.** *A discipline built to prevent
   > over-claiming has been observed producing systematic under-claiming.*
2. > **The instrument and the content are mismatched for classical facts.** `E1`/`E2` are in every
   > graduate text and no arXiv abstract. **A register that cites only what it can fetch will
   > systematically fail to anchor the oldest parts of its own argument, and will grade them
   > NOT-AT-CITE beside genuinely open questions — flattening a real distinction.** *A limit of the
   > method, found by the method.*

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `c18e89a` (registration) → this pass |
| relay | this report's commit |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**`Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow. Nothing reached the review gate.**
