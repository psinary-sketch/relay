# `W-ATTEMPT-2` · SITTING 9 — WHERE THE NEGATIVE DIRECTIONS LIVE
## ### **`N-EDGE` ON ALL THREE PARTS, 139/139 EXACT: EVERY FIRST APPEARANCE IS A PLACE-ARRIVAL EDGE, THE T-FIXED NEGATIVES ARE ANTI-INVARIANT AT ALL 14 CELLS, THE CONSTRAINED CLASS STAYS POSITIVE ALONG THE WHOLE STAIRCASE — AND THE ARRIVAL OF 5 IS EXACTLY MULTIPLICATIVE: A FLAT FACTOR THAT PROVABLY CANNOT CREATE IMBALANCE**

**Relay report · 2026-08-18 (twenty-ninth sitting) · author-called · attempt work RELAY-ONLY ·
nothing deposits**

> ### **THE GUARD AND THE PROTOCOL CORRECTION, carried (spec `§6(c)`; the closure protocol gates the
> register, not investigation). Everything below is DATA about the finite constructed model section.
> No sign at complete roster; the register untouched.**

```
PLACE-papers  origin/main : b7acc3e (the §0 pattern ledger — this sitting's item 4; see the record note)
relay         origin/main : (this report, in the sittings-9/10 batch)    local +1 HELD
instruments   tools/e16/b22_attempt2_s9.py (registration banked BEFORE the run:
              data/b22_registration_2026-08-18.txt) · data/b22_2026-08-18.txt · 139/139 EXACT
```

> ### **RULE-3 LOG:** *all head cites banked at `36345da` ✓; the sector arithmetic registered LONGHAND
> before the run — including the derivation that the ferried `(N-edge)` channel-half holds structurally
> for the T-FIXED reading and fails structurally for the FULL reading (whose negatives split
> invariant : anti = 2 : 1 by the class factor's positivity on both channels) — so the full reading's
> invariant-channel negatives could not fire `(N-interior)`. Terms coined: none; banned check clean.*

---

## §1 — THE NEW FACTOR: `(5,1)`, EXACT OVER `ℚ(ζ₂₅)`

*Eigen-dims `(d₁, d₋₁, dᵢ, d₋ᵢ) = (4, 4, 4, 4)` — FLAT — with `tr M = 0`, verified by direct exact
nullity for all four `λ` (the `±i` nullities over the quadratic extension `ℚ(ζ₂₅)(i)`, built for the
purpose).* ### **And `i ∉ ℚ(ζ₂₅)` is PROVED in-run, not assumed** *(cyclic Galois group; the Gauss sum
`s² = 5` exactly identifies the unique quadratic subfield as the REAL `ℚ(√5)`)* — *so `dᵢ = d₋ᵢ` was
forced in advance: the `p`-odd pattern has its mechanism.* *(Verification stronger than registered:
`FK = KM` was checked on ALL columns, not spot-checked — noted, not silent.)*

## §2 — THE STAIRCASE TABLES *(14 cells, two place sets; columns per cell: class dim · T-fixed negatives (anti / inv) · full negatives (inv / anti) · zero block)*

| `a²` | `{∞,2,3}` | `{∞,2,3,5}` |
|--:|:--|:--|
| 2 | `{2:1}`: **0 · 0/0 · 0/0 · 3 — the whole cell is the twist's ZERO block, signature `(0,0,d)`** | same |
| 3 | `{2:1,3:1}`: 2 · 1/0 · 2/1 · 6 | same |
| 4 | `{2:2,3:1}`: 18 · 9/0 · 18/9 · 54 | same |
| 5 | *same cell — every delta ZERO* | ### **`{2:2,3:1,5:1}`: 288 · 144/0 · 288/144 · 864 — EVERY column ×16 exactly** |
| 8 | `{2:3,3:1}`: 98 · 49/0 · 98/49 · 294 | ×16 of it |
| 9 | `{2:3,3:2}`: 1568 · 784/0 · 1568/784 · 4704 | ×16 |
| 12 | *same cell — delta zero* | *same cell — delta zero* |

### **Every staircase cell carries signature exactly `(d/4, d/4, d/2)` — the banked twist signature,
now an enumerated consequence of the sector arithmetic — and `T-fixed invariant-channel negatives = 0
at all 14 cells`, by enumeration AND structurally.**

## §3 — THE VERDICTS

1. ### **FIRST APPEARANCE: `a² = 3`, BOTH readings, BOTH place sets — the cell change admits the NEW
   PLACE 3: EDGE in all four cases. Nothing first-appears at a cell interior.** `(N-edge)` landed on
   all three parts; `(N-interior)` did not fire; `(N-third)` not needed.
2. ### **THE ARRIVAL OF 5 IS EXACTLY MULTIPLICATIVE AND DOUBLY DEAD:** *every column multiplies by
   exactly `16 = dim Son(5,1)`, and the mechanism is proved at the eigen-dims: `(5,1)` is FLAT with
   `tr M = 0`, so gluing it in sends every sector dim to a uniform multiple —* ### **5 can NEVER create
   an imbalance between the positive and negative real sectors at any cell it enters** *(dead in the
   weight — banked — and dead in the sector arithmetic — new).*
3. ### **THE CONSTRAINED CLASS STAYS POSITIVE ALONG THE WHOLE STAIRCASE** *(`P+` extended): verified
   DIRECT-EXACT at the new cell `{2:2,3:1}` (the full 108-dim Gram: `B|_class = E†(KᵀK)E ⊗ C|_fix`
   entry-exact, rational-SPD certificate, dim 18; the off-class block `= −(…) ⊗ C|_anti`, dim 9,
   anti-invariant only); remaining cells sector-assembled, declared honestly.*
4. **One registration line corrected in-run, recorded as a NOTE not smoothed:** *"growth at every
   staircase step" conflated the `a²` grid with the CELL grid — growth is strict at exactly the
   cell-changing steps and exactly ZERO at the three non-changing steps (`4→5` and `9→12` three-place;
   `9→12` four-place). The banked `1 → 9` sub-claim lands.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO SIGN AT COMPLETE ROSTER. NOTHING DEPOSITS.**
