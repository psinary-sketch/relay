# THE `h2` CHARTER → `v1.1` — ### **WHAT WOULD JUSTIFY A RUN, IN TODAY'S VOCABULARY**

**Relay note · 2026-08-17 · author-asked · ### A FILING, NOT A SITTING · nothing deposits**
**NO ATTEMPT OPENED. NO RESEARCH ACT. NO MECHANISM PROPOSED. NOTHING PROMOTED.**
**`h2` UNCHANGED. NO SIGN SENTENCE. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 70cd15418d4fc609cc2d262f27bc436d35eac02b   VERIFIED (ls-remote)
relay         origin/main : (this note)                                local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 70cd154   ### 22 / 22 COHERENT, three ways
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> *The day closed on its own report. This is the frame around it: `THE_H2_PROGRAMME_CHARTER` gains `§§4–7`,
> and* ### **`§§1–3` are unchanged.** *The tracks, the attempt protocol and the closure protocol stand
> exactly as written on 2026-08-12.*

---

## §1 — THE FOUR CONDITIONS, SEATED · ### **EACH WITH A STATUS AND A CITE**

| | condition | ### **STATUS** | cite |
|:--|:--|:--|:--|
| ### **`C1`** | **A CARRIER.** *The build question's object:* `(i)` the monoid as content source · `(ii)` the involution as structure · `(iii)` the full roster · `(iv)` a Hermitian pairing **born from** `(i) × (ii)`, diagonal quotiented — over `ℚ` | ### **UNBUILT** | `THE_CARRIER_BUILD` `v0.2` — `§3` grades three frames and ### **the involution column is empty across all three**; `§4`'s move ran and returned a third outcome, ### **the arrest being a MISSING SELF-PRODUCT reached a third time from a third side**; `§1.1` `no_idempotent_candidate` compiled; name-identity armed on ### **four order-two data** with no merge to the refuted `E2`. *Adjudication in the two HELD relay acts — **unpushed**, and named as such.* |
| ### **`C2`** | **A SPEC-FORCED MECHANISM REACHING THE CENTRE** | ### **NONE — and the absence is PROVED, not outstanding** | `FINDINGS` claim **5**: ### **no positive control exists** *(over `ℚ` the `S4`-present ∧ off-line cell is empty modulo RH; off `ℚ`, `S3b ⟹ S2` removes the axis)* — **the third proven self-limit, and it denies CALIBRATION.** Claim **4**: `S4` isolated reaches ### **exactly the `σ = 1` edge**, at three cites *(`ANNEX B` `B3` · DMV · anticipated in-corpus at `bd2ae1a`)* |
| ### **`C3`** | **`T2` — BOTH AXES AT ONCE.** *In today's vocabulary:* ### **the shift graph of the addresses AND the semilocal trace as ONE object** | ### **NAMED, UNBUILT** | `THE_TECHNIQUE_SPECIFICATION` `T2` *("cover the `(nonArchimedean, unbounded)` quadrant … **the row every located attempt fails**")*; `THE_ATTEMPT_RECORD` `§6` *(`T2`/`T3` unmoved permanently)*; `FINDINGS` `F.2026-08-17d`, `F.2026-08-17e` |
| ### **`C4`** | **THE CLOSURE PROTOCOL** | ### **COMPLETE** | `§3` of the charter, written 2026-08-12 **while nothing was at stake**; priced once at `THE_ATTEMPT_RECORD` `§6` — *"there is no sign step. **PRICE: NOT APPLICABLE**"*, ### **which is the correct output, not a shortfall** |

### **THE TRIGGER SENTENCE, ON THE CHARTER'S FACE**

> ### **AN ATTEMPT IS JUSTIFIED WHEN `C1` OR `C3` LANDS AT CONTENT.**
> ### **UNTIL THEN THE ATTEMPT TRACK RUNS SUB-GATE, DECLARED, AS `W-ATTEMPT-1` DID.**

**And the asymmetry is explained rather than left to be noticed:** ### **`C4` gates nothing** *(it is
complete, and a complete protocol is a precondition for BANKING a result, not for opening an attempt)*; and
### **`C2` cannot be a trigger at all** — *it is a proven self-limit no attempt can remove, so requiring it
would close the track permanently, which is a decision about the programme and not about an attempt.*
**It is listed because an attempt must know it works without calibration, not because it can be satisfied.**

### **AND `§4` NAMES THE PRECEDENT `W-ATTEMPT-1` SET**

*Its own record, quoted verbatim into the charter:* ### ***"THE ATTEMPT RAN SUB-GATE, AND THAT WAS RECORDED
AT ITS OPENING, NOT DISCOVERED LATER … all three candidate mechanisms fail `T2` … The author opened it
anyway, explicitly."***

> ### **AN ATTEMPT MAY BE OPENED SUB-GATE, BY THE AUTHOR, EXPLICITLY, WITH THE SHORTFALL ON THE RECORD'S
> FACE. WHAT IT MAY NOT BE IS OPENED SUB-GATE SILENTLY.**
>
> *And the price list, measured once:* **twenty sittings, a certified instrument, a derived law about the
> discretized lag form, twenty-two corrections —** ### **and `h2` nowhere**, *which its register sentence
> says verbatim.* **That is not a failure of the attempt. It is what a sub-gate attempt costs and returns.**

---

## §2 — THE WEEK'S RESULTS, SEATED AS FEEDERS · ### **AT GRADE, AND NONE PROMOTED**

| result | grade | feeds | ### **why it does NOT promote it** |
|:--|:--|:--|:--|
| **the matching-number law** — `NPOS = μ`; sawtooth its one-lag evaluation | ### **`DERIVED` (pure) / `BENCH` (full)** | `C3` | ### **ONE axis.** *A complete theory of the support axis is not a step toward the place axis — that conflation is what `T2` exists to forbid.* |
| **silence-on-weights** | ### **`DERIVED` for the pure form** | `C3` | *It is the reason* ### **only the trace pays for a prime** *— the graph is silent on the coefficient. It sharpens what a joining object must carry; it does not carry it.* |
| **the two-decompositions witness** | `AT CITE`, one field, structural | `C1` via the monoid | ### **a constraint ON a carrier, not a carrier** |
| **the anti-alignment** | ### **`MECHANISM-NAMED-NOT-CARRIED`** | `C3`, negatively | *right out-of-sample once, wrong once —* ### **the position where a rule is written down and not used** |
| **the scaling-limit refutation, corrections 18–22, the method line** | method, not content | ### **the attempt protocol's checkpoint 3** | *it changes how an attempt would be run, not whether one is justified* |

> ### **THE TABLE'S HONEST SHAPE, PUT IN THE CHARTER RATHER THAN IN A REPORT: THE WEEK PRODUCED ITS BEST
> MATHEMATICS ON THE AXIS `T2` DOES NOT ASK ABOUT.** *A good result on the wrong axis is exactly what a
> justification section exists to price.*

---

## §3 — THE TWO ROADS, RE-PRICED · **BOTH MONTHS · BOTH NON-GATING · BOTH THE ONLY CLAUSE-BEARING CONSTRUCTIONS**

### **ROAD A — THE CARRIER BUILD'S NEXT MOVE** *(feeds `C1`)*

*The self-product question, restated with this week's constraint attached:*

> ### **AN OBJECT SATISFYING `(i)`–`(iv)` MUST ALSO SEE THE MONOID-RESPECTING BASIS.** *The two-decompositions
> witness holds one field fixed and varies only the decomposition, and the off-line zeros appear in exactly
> the basis that breaks the monoid.* **So a carrier that is basis-agnostic between the two decompositions
> cannot be the right object** — its frame would have taken the object, not its presentation: invariant in the one variable the witness shows placement tracking *(re-phrased to content 2026-08-17, correction twenty-four's closing)*.

*`§4`'s named move is* ### **closed, not retried**; *a next move would have to be named afresh, both branches
longhand, before anything runs.*

### **ROAD B — THE SEMILOCAL OBJECT** *(feeds `C3`; it is `T2`'s home)*

*`{∞, 2}`; their `[19]`; the corpus's `2310.18423` stability line — all at cite.*

> ### **WHAT "ONE OBJECT" WOULD HAVE TO CONTAIN: THE ADDRESS GRAPH AND THE TRACE.** *The address graph is
> what the week derived — vertices, an edge per lag address, inertia a matching number, silent on
> the coefficients. The trace is where `log p·p^{−k/2}` and the archimedean place live.* ### **No candidate on
> the corpus's table has both columns.**

### **AND THE INSTRUMENT EITHER ROAD WOULD NEED: THE REGULARIZATION REBUILD**

*Three independent routes, three distinct failures:* ### ***"the object is finite and every naive
discretization of it fails differently … computing it requires REBUILDING THAT STACK, not a better
quadrature."*** ### **Filed as INSTRUMENT for either road, not as a road.** *It decides nothing about the
clause and produces no sign.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE CHARTER IS `v1.1`.** `§§1–3` unchanged; `§4` the attempt track's status and the sub-gate
   precedent; `§5` the four conditions and the trigger; `§6` the feeders at grade; `§7` the two roads.
2. ### **THE TRIGGER IS `C1` OR `C3` AT CONTENT** — and the reasons `C2` and `C4` are not triggers are
   written down, so the asymmetry cannot later read as an omission.
3. ### **NOTHING WAS PROMOTED.** *Every feeder carries a column saying why it does not move its condition.*
4. **`OPEN_TRAILS` carries the pointer.** *The charter itself is `phase2/method/` and is not in the 22-file
   mirror roster; the mirror reflects it through that pointer, which is what the probes check.*

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — **committed at `relay` local tip, UNPUSHED, absence from
the public tree re-verified after this push.** *They are `C1`'s adjudication, which is why `§5` names them
and names them as held.* **Release condition unchanged: counsel's answer, then your word.**

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. `WIDEN` PAUSED. NOTHING DEPOSITS.**
