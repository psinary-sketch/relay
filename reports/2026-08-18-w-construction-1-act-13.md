# `W-CONSTRUCTION-1` · ACT 13 — THE SCALE-COVARIANT ARCHIMEDEAN FACTOR
## ### **THE DICTIONARY DERIVED (`c(a) = πa²`, ANCHORED AT CC'S OWN MACHINE; EACH STEP'S GRADE ON THE FACE) — `(COV-partial)` ON THE STABLE SUBSET: COVARIANCE SHRINKS THE L1 TRANSPORT MISMATCH TENFOLD AND THE MARGIN CROSSES ZERO AT `a² ≈ 3.5` — THE COVARIANT EQUATION'S REMAINDER `(R-stable)`-SHAPED (`≈ −0.13`); STEP ONE NOT TRIGGERED**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 13 · sub-gate declared · Rules 4/5 · on the
face: the factor rebuilt as a family; no identity decided; nothing deposits; nothing circulates**

> ### **RULE-3 LOG:** *act 12 at `9d43f2d` ✓; the Shannon facts read at their bank (tr `Q_c =
> 2c/π`; `⌊W/π⌋`) ✓; CC's λ-family at content ✓; the banned grep: CLEAN. Registered BEFORE any
> number: the dictionary's four steps WITH GRADES (cite / content / OURS-question-grade / the
> b19 two-Sonins clarification); THE ANCHOR-INVARIANCE ARGUMENT (`c(√2²) = 2π` reproduces the
> act-12 layer, so `(COV-repairs)` was EXCLUDED BY THE ANCHOR before the run); the longhand
> prediction and the next-name candidate (THE SONIN SUBTRACTION). The E₁ rule was MEASURED per
> `c`, not assumed — verified at `2π, 3π`; degraded at `c ≥ 4π` with the registered fallback used
> and said. STABILITY RULED THE VERDICT: the three large cells' spreads engulf their margins —
> numerically indeterminate, the instrument's reach, said so. ROUTING: the banked run ON THE OPUS
> LANE. Terms coined: none (the Sonin subtraction was named in the registration as a candidate).*

```
registration    data/b41_registration_2026-08-18.txt (banked FIRST)
bank            data/b41_2026-08-18.txt (the Opus lane; anchor battery + per-c Shannon
                gates ALL PASS)
build document  THE_GLOBAL_SECTION.md v0.13 (§26)
legs            UNCHANGED (honest null): vanilla 90; the Mathlib 3-sorry ledger intact
```

---

## §1 — THE DICTIONARY

**D1 (cite):** `tr Q_c = 2c/π` — reproduced EXACTLY per `c` in-run. **D2 (content):** the anchor
`(a = √2) ↔ (c = 2π)` — CC's machine IS their `√2`-window analysis (the 15/15 battery). **D3
(OURS, question grade):** `c(a) = πa²`. **D4 (from the b19 bank, filed):** the corpus holds TWO
archimedean "Sonin" objects — the b19 grid model (inner radius, dim N-dominated) and CC's
`S_{∞,λ}` (λ-scale ball, trace-finite through the resonance geometry): ### **the act-12
transport gap sits BETWEEN them.**

## §2 — THE FAMILY: `(COV-partial)`, AND THE SHAPE IS THE FINDING

> ### **The stable subset's margins (bound − band form): `−0.126 → −0.025 → +0.051` at
> `a² = 2, 3, 4` — covariance shrinks the act-12 mismatch TENFOLD (it was `0.9–1.7` at fixed
> `c`), the margin CROSSES ZERO at `a² ≈ 3.5`, and the failing address is CONCENTRATED AT THE
> ANCHOR — exactly where the registration's invariance argument said it must be if anywhere. One
> STABLE repaired cell (`a² = 4`, margin `+0.051 >` spread `0.011`). The covariant band form sits
> ON CC's Theorem-1 boundary to within `±0.13` where stably measurable.** *The three large cells:
> numerically indeterminate (spreads `0.08–0.77` engulf margins) — the instrument's reach, not
> the mathematics. The SONIN-SUBTRACTION candidate now has a measured size where it acts:
> `≈ 0.13` at the anchor, dying outward — a candidate, not a conclusion.*

## §3 — THE COVARIANT EQUATION (licensed by `(COV-partial)`)

**`D_cov = −0.126, −0.131, −0.360` on the stable cells — `(R-stable)`-shaped: a remainder
`≈ −0.13` at the prime-free cells, deepening exactly where the finite channels enter (decomposed
on the face). Three stable cells: the shape is data at that width, no wider. `(R-dies)` did NOT
land; the closure protocol's step one is NOT triggered.**

## §4 — THE RECORD

*`v0.13` (`§26`); rows 122–126 — including THE RE-READ MARKER (row 122): rows 100–116's
archimedean columns re-read under the act-12 sign dictionary, RE-READ NOT CORRECTED, the data
stood; `F.2026-08-18w`; this report the bank.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED; `(R-dies)` NOT
LANDED, SAID SO. NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING CIRCULATES.**
