# `W-CONSTRUCTION-1` · ACT 4 — THE TWO STEPS AND THE SPECTRAL QUESTION
## ### **THE FACTORIZABLE-IMAGE LEMMA CLOSES: `(F-yes)` AT PROOF GRADE — (β) PROVED, (α) DISSOLVED BY THE CONSTRUCTION'S OWN DEFINITION WITH THE ϑ-TAIL FAILURE COMPILED AS A FINDING — `(D-dense)` GLOBAL; THE NEITHER-POLES-NOR-ZEROS OBSERVATION FILED; THE SPECTRAL QUESTION `(AC-third)`-SHAPED WITH THE DILATION A.C. AT CITE AND `Θ_T` NAMED — BOTH LEGS PRINTED**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 4 · sub-gate declared · Rule 4 · ### THE
TWO-LEG RULING FILED AS RULE 5 AND ON EVERY ROW · nothing deposits · nothing circulates**

> ### **RULE-3 LOG:** *act 3 at `6fe62bc` ✓; the arrangement identity banked ✓; the banned grep run
> on this ferry: CLEAN. Mathlib dilation theory grep-checked: ABSENT (zero hits) — the a.c.
> statement is a NAMED STUB, not a sorry, because it cannot yet be formed. Terms coined: none.*

```
build document  THE_GLOBAL_SECTION.md v0.4 (§9 closed; §10; §11)
vanilla leg     tools/lean/NeitherPolesNorZeros.lean + AxiomCheckNPZ — 5/5 "does not depend on any
                axioms" (v4.29.1 pinned); the relay's zero-axiom count: 62
mathlib leg     LocalLimit.lean + ac_dilation_stub — compiles, 0 errors; the 5-sorry ledger
                UNCHANGED (the new statement is a named stub owed to dilation theory)
```

---

## §1 — ITEM 1: THE LEMMA CLOSES

1. ### **(β) PROVED (proof grade):** *the constraint `ĝ(±i/2) = 0` removes only pole components;
   pole components act only through the ball directions (Tate's identification at cite); the object
   is ORTHOGONAL to the ball directions at every place (banked exact — and the smallest instances
   now VANILLA-LEG at zero axioms). The compressed action is unchanged: the constrained class's
   image closure equals the unconstrained's.* ∎
2. ### **(α) DISSOLVES, WITH A FINDING:** *Weil test data have compact idele support ⟹ finitely
   many active places; the diagonal construction puts the PLAIN unit at every inactive place — no
   tail action, no invariance needed.* ### **THE FINDING, compiled so nobody re-routes silently: the
   `ϑ`-tail route GENUINELY FAILS — at `(2,1)` the unit 3 acts as `−1` on Son (`unit_action_neg`,
   zero axioms), the unit-average annihilates it, and radial Sonin vectors do not exist there.**
3. ### **CONSEQUENCE: `(D-dense)` GLOBAL AT PROOF GRADE — Route 1's density chain is COMPLETE**
   *(cites named: the Schwartz–Bruhat restricted structure; Tate's pole/ball identification).* **What
   remains of Route 1 is `(L-identity)` — the `h2`-restatement, behind the closure protocol.**

## §2 — ITEM 2: THE OBSERVATION (build doc §10)

### **THE OBJECT IS THE PART WITH NEITHER POLES NOR ZEROS** — *poles excluded BY CONSTRUCTION
(vanilla-leg instances at zero axioms), zeros excluded BY THEOREM (the CNU engine, Mathlib leg,
std3); `Z = P + A − PR` realized with `P` a constraint, `A`+`PR` jointly the norm on the full sector
sum, `Z` the exact complement (`R(g)`, the arrangement's global form). Cross-linked to
`SIGN_ARRANGEMENT_RECONCILIATION`; no promotion.*

## §3 — ITEM 3: THE SPECTRAL QUESTION

*Registered `(AC-zeros)`/`(AC-none)`/`(AC-third)`, both branches carrying the honest line (a
distributional-trace identity would be a RESTATEMENT of the explicit formula, not a sign result).*
### **LANDING: `(AC-third)`-shaped — the STRUCTURAL half of `(AC-zeros)` holds AT CITE (Nagy–Foiaş:
the minimal unitary dilation of a c.n.u. contraction is absolutely continuous; ours is c.n.u. by the
act-1 theorem): a canonical a.c. unitary is attached to the object's scaling. The density question is
OPEN and sharpened to ONE computable object: the characteristic function `Θ_T` of the compressed
scaling — tractable because the defect is ONE-SHELL (banked). Named; not computed.**

## §4 — ITEM 4: BOTH LEGS, PRINTED

| leg | this act's state |
|:--|:--|
| **VANILLA** (zero axioms, v4.29.1) | `NeitherPolesNorZeros.lean`: ball ⟂ Son at `(2,1)`/`(3,1)` · the unit-average annihilation (the finding) · the escape witness `8ζ⁴ ≠ 0` in `ℤ[ζ₁₆]` — **5/5 "does not depend on any axioms"; relay total 62** |
| **MATHLIB** (std3) | the CNU engine + positivity + radical-zero (act 3, proved) · the a.c. statement a NAMED STUB (dilation theory absent from Mathlib, grep-checked) · **the 5-sorry ledger UNCHANGED, owners intact** |

**Rule 5 (the two-leg ruling) filed in `EXECUTOR_RULES`; the correspondence rows carry leg/profile.**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED. NOTHING PROMOTED.
NOTHING DEPOSITS. NOTHING CIRCULATES.**
