# `W-CONSTRUCTION-1` · ACT 10 — THE APPORTIONMENT JOINT
## ### **`(W-split)`: THE JOINT CLOSED AS A NUMBER PER CELL — `f_∞ = 0.69 → 0.61`, DECLINING TOWARD THE `t`-SHARE `0.6165`, THE SUM-GATE EXACT AT `9e−16` — `(P-ball)` STRUCTURAL (THE `−2` LIVES OFF-OBJECT, FROM HELD PROOF-GRADE PIECES) — THE CLOSED EQUATION: `(I-differ)` AT EVERY CELL, THE OBSTRUCTION FULLY DECOMPOSED, NO OPEN TERM**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 10 · sub-gate declared · Rules 4/5 · on the
face: the act computes the joint; the identity NOT decided beyond the registered map; nothing
deposits; nothing circulates**

> ### **RULE-3 LOG:** *act 9 at `4062a03` ✓; `v0.9` `§§21–22`, the proved lemma, file D at 1 sorry,
> the ledger 3 ✓; CC's `W_∞` at (151)/(3) — the Route-A certification against the atlas arch
> column, banked ✓; the reconciliation's `−2.000127` banked ✓; the banned grep on this ferry:
> CLEAN. THE ONE NON-TRIVIAL CHOICE registered WITH ITS WARRANT before any number: the divergent
> residual apportions `t(n)`-proportionally BECAUSE that is CC's own subtraction assembly (Lemma
> 5.4 → Prop 5.5's scalar), at cite, not fitted — and its exact-sum consequence `W₊+W₋=A` was made
> a VOID-GATE. ROUTING: registration + instrument by Fable; the banked run ON THE OPUS LANE (the
> one grep hit on "VOID" is the section header, checked). Terms coined: none.*

```
registration    data/b38_registration_2026-08-18.txt (banked FIRST: the definition, the
                warrant, the gates, the verdict maps)
bank            data/b38_2026-08-18.txt (the Opus lane; C0 + per-mode mask algebra
                (8.9e−16) + the sum gate (9e−16 per cell) ALL PASS)
build document  THE_GLOBAL_SECTION.md v0.10 (§23)
vanilla leg     UNCHANGED at 88 — no decide-reachable apportionment arithmetic (the
                t(n) are not rational); the honest null, per the registration
mathlib leg     the 3-sorry ledger UNCHANGED, owners intact; the HS stub stands
```

---

## §1 — ITEM 1: THE APPORTIONMENT — `(W-split)`

> ### **`f_∞ = W₊/A` per cell: `0.692, 0.664, 0.653, 0.626, 0.620, 0.609` — a stable fraction,
> NAMED AS DATA, declining toward the `t`-share `0.6165` (the b35 even share).** *(W-even) did NOT
> land: the odd sector carries a genuine `W_∞` mass (`W₋ = −0.55…−0.64`) — the `E₁`-sector's
> positivity is NOT `W_∞`'s own with `ε` a small correction.* **5 of 6 cells STABLE per the
> registered criterion; the `a² = 2` cell marginal at 6.0%, flagged, not smoothed.**

## §2 — ITEM 2: `(P-ball)` — STRUCTURAL

**Closed mostly from what the corpus already held at grade:** the atlas's arrangement keeps `P`
separate from `A` (its `W_∞` is pole-free by its own certified bookkeeping); the pole functional
acts only through ball directions (Tate; act 4's `(β)` PROVED); ball ⟂ Son (vanilla, no axioms);
the ball indicator has no `E₁`-representative (banked). ### **The `−2` lives OFF-OBJECT; `W_∞`'s
apportionment on the object is its Sonin-projected remainder's — which is item 1's computation.**
*Bench columns: `P = 2.010–2.125` beside the banked `W_pole = −2.000127` — sign/magnitude
consistent; exact equality not expected across families, said so. Cross-linked.*

## §3 — ITEM 3: THE CLOSED EQUATION — `(I-differ)`, NO OPEN TERM

`D_closed = (A − PR) − ([W₊ + E2even] − Θ_q) = W₋ − E2even + (Θ_q − PR)` (exact by the sum gate;
both forms printed, agreement `1e−15`):

| `a²` | 2 | 3 | 4 | 8 | 9 | 12 |
|:--|--:|--:|--:|--:|--:|--:|
| `D_closed` | −1.614 | −1.659 | −1.541 | −1.500 | −1.365 | −1.365 |

> ### **VERDICT: `(I-differ)` at every cell, per the registered map and no wider — with the
> obstruction FULLY DECOMPOSED into three computed pieces and nothing open: (i) the odd-sector
> `W_∞` mass (this act's number); (ii) the even-sector `ε` mass (the density question's content);
> (iii) the finite-level deficit (theorem-controlled, dies at the level limit). `(I-match)` did
> NOT land; the closure protocol's step one is NOT triggered. `(B-dictated)` re-recorded
> unchanged.** *Structurally, as data: at the model, at these cells, the geometric side and the
> object's `E₁`-trace side differ by the two archimedean sector masses and a dying finite-level
> term — the equation's entire open content is now NUMBERS, each with an address.*

## §4 — ITEM 4: THE RECORD

*`v0.10` (`§23`); rows 109–112; `F.2026-08-18t`; the prints unchanged and said so (the honest
null on the vanilla extension); this report the bank.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED — `(I-match)` DID
NOT LAND AND THE ACT SAYS SO. NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING CIRCULATES.**
