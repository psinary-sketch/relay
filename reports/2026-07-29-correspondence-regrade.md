# Correspondence re-grade, wave-wide — 2026-07-29

Re-grade of every landed Correspondence table in PLACE-papers under the three-grade rubric
(DERIVES / INTERFACES / ENCODES-CONCLUSION-or-SHELL). Reference implementation:
`phase2/quantum/SILENCE_STAGES_DEALIGNMENT.md` §9. Read-first; graded from the statements at source, not
the profile alone; pins and profiles re-verified.

## The load-bearing finding

The tables carrying **pre-rubric "Compiled" rows** — the re-grade's actual target — are exactly the
**`phase1.5/proofs/` RH-proof cluster** (RESIDUE, PATHS, SURROUND, SIMPLICITY). That cluster is the
**protected RH rail**. It is **HELD, not edited**: (a) the governing constant (the RH rail is untouched,
verified `67da789..HEAD` = `[]` this pass), and (b) the re-grade of those tables is precisely the
**held keystone diff** already prepared in `THE_LOAD_BEARING_MAP.md` (Compiled content rows → DERIVES;
balance/positivity/Route-3 rows → INTERFACES-on-h2; **no rail row → ENCODES/SHELL**). Their per-row grade
is reported below, not written.

The **non-rail tables were already rubric-graded** in the Gate-1 wave (`THE_LOAD_BEARING_MAP`: "fully
rubric-graded and clean: GRH · FOUND · IFACE · DOM · … LIC · RCURVE"). The wave re-verifies them: pins
resolve, profiles are accurate (spot-checked at source), no profile-rounding found. One paper (FOUNDATIONS)
carries two non-rubric labels that pose a genuine compiled→ENCODES/SHELL scope question — **held for author
ruling**. One (THE_SUBSTRATE, enshrined) uses "PROVED" where the rubric wants "DERIVES" — **flagged for a
normalization nod**, not edited unbidden.

## Table inventory

| # | paper | REGISTRY | data rows | class | disposition |
|:--|:--|:--|:--:|:--|:--|
| 1 | `phase2/quantum/SILENCE_STAGES_DEALIGNMENT.md` §9 | p2-16 | 10 | reference | already rubric (this pass built it) |
| 2 | `phase1.5/method/EXHAUSTIVENESS_LICENSE.md` | LIC (1.5h) | 6 | non-rail | rubric-clean; re-verified |
| 3 | `phase1.5/method/THE_SUBSTRATE.md` | m5-3 (enshrined) | 7 | non-rail | "PROVED" → DERIVES normalization **flagged** |
| 4 | `phase1.5/spectral/GRH_CASCADE.md` | 1.5d-4 (GRH) | 13 | non-rail | rubric-clean; re-verified (2 SHELL rows already adjudicated) |
| 5 | `phase1.5/structural/FOUNDATIONS_OF_THE_SIDE_PROGRAMME.md` | 1.5f-4 (FOUND) | 15 | non-rail | **HELD for author ruling** (Model-level ×3, SCAFFOLDING ×2) |
| 6 | `phase2/method/ADDITIVE_MULTIPLICATIVE_CONSPIRACY.md` | p2 (ENGINE-adjacent) | 8 | non-rail | rubric-clean; re-verified (Placeholder row already retired) |
| 7 | `phase1.5/rcurve/R_CURVE_CRITERION.md` | RCURVE (1.5g) | (table present) | non-rail | Gate-1 rubric-clean; re-verified |
| 8 | `phase1.5/proofs/PATHS_TO_THE_CRITICAL_LINE.md` | 1.5a-5 | ~28 | **RH RAIL** | **HELD** (held keystone diff; rail untouched) |
| 9 | `phase1.5/proofs/THE_RESIDUE_OF_RH.md` | 1.5a (rail) | 12 | **RH RAIL** | **HELD** (no Status column; would need one added — rail untouched) |
| 10 | `phase1.5/proofs/THE_UNCONDITIONAL_SURROUND.md` | 1.5a (rail) | 15 | **RH RAIL** | **HELD** |
| 11 | `phase1.5/simplicity/SIMPLICITY_OF_RIEMANN_ZEROS.md` | 1.5b (rail) | 21 | **RH RAIL** | **HELD** |

*Excluded (not graded Claim/Kernel/Theorem/Profile/Status tables): `TRIVIUM_IDENTITY_SUBSPACE.md` §7.3
"Structural Correspondence" (prose), `FANO_PLANE.md` "Valence-Fano Correspondence" (prose),
`ENUMERA.md`/`TECHNE_TOOLKIT.md`/`THE_LOAD_BEARING_MAP.md` (technique/meta tables, not paper keystones).*

## Per-row grade — old → new

**Non-rail, already correct (old = new; re-verified at source):**
- **LIC** (6): grades-form-decidable-type, preorder, top/bottom/strict, corpus-instance grades (by `grade`+`rfl`), instances-ordered → **DERIVES**; the E-Difficulty bridge (via named `EDifficultyTop`) → **INTERFACES**. Unchanged.
- **GRH** (13): structural-exhaustiveness, character-insensitive balance, paired-voice chorus, Mechanism Theorem, Ostrowski/per-prime bridges, conspiracy-exclusion + ABC → **DERIVES**; Universal Silence (named `I.is_universal`) → **INTERFACES**; BSD-formation-placement (`decide` over hand-assigned constants) and YM-mass-gap-placement (`Bool := true` proved `= true`) → **SHELL** (already pulled as citations, author ruling 2026-07-13; audit records). Landau-Siegel/Artin → classical (no kernel). Unchanged.
- **ADDITIVE** (8): CRT-exhaustiveness + ABC → **DERIVES** (positive-modulus scope); the general exclusion lemma (hypothesis-is-the-weight) → **INTERFACES**; per-conjecture instantiations over `fun _ => True` → **SHELL/Placeholder** (already retired); M3/M4/M5 carry `sorry` → **Open**; Mechanism Theorem/Universal Silence → DERIVES/INTERFACES. Unchanged.
- **SILENCE (reference)** (10): six DeAlignment terminals **DERIVES** (`fano_two_design` `[propext]`, rest axiom-free), two Steane **DERIVES**, two manuscript-resident. Unchanged.

**Profile re-verification (spot-checked at source):** SIDE-silence-principle `product_formula_silent`,
`distributive_law_silent` → **"does not depend on any axioms"** (matches "axiom-free"); `silence_principle`
→ **`[propext]`** (matches the FOUNDATIONS row's "`[propext]`; axiom-free"). **No rounding found.** No
`[propext]` written as axiom-free anywhere in the non-rail set.

**Pin re-verification:** SIDE-effects, SIDE-silence-principle, SIDE-grh-transfer, SIDE-compression,
SIDE-kernel all present at HEADs; SILENCE pins re-confirmed (SESC `6a4f482`, SIDE-cosmo `c5cba30`). **No pin
corrections.**

## Papers held for author ruling, with the triggering rows

**A · The RH rail (4 tables) — HELD ABSOLUTELY.** PATHS, RESIDUE, SURROUND, SIMPLICITY carry the pre-rubric
"Compiled" rows the wave targets. Editing them changes the rail (forbidden by the governing constant) and
is the held keystone diff (`THE_LOAD_BEARING_MAP`). Analytic re-grade (reported, not written): the
"Compiled" content rows → **DERIVES**; the balance/positivity and Route-3/`covers_all` rows → **INTERFACES**
(named premises: `EDifficultyTop`, Li's criterion, Hilbert–Pólya, `VerifiedZerosTo`/`ExplicitFormulaDecomp`/
`TailBoundPremise`, `NontrivialZeroExistsInStrip`); the `covers_all` structure field → **open premise (h2)**;
`residue_irreducible`, `C7_finite_type_false`, the T3″ countermodel → **DERIVES (incl. compiled negatives)**.
**No rail row grades to ENCODES/SHELL** — the rail is honest. Author ruling releases the write.

**B · FOUNDATIONS_OF_THE_SIDE_PROGRAMME — HELD (the one genuine scope question).** Two non-rubric labels
pose a compiled→ENCODES/SHELL question the ruling reserves for the author:
- **Model-level ×3** — `product_formula_silent`, `distributive_law_silent`, `silence_principle`: `decide`
  over an **in-kernel Boolean model** of the interface (`isSilent I := I.kappa_x100 == 0` on hand-defined
  instances), with the row's own note that *"the arithmetic s-darkness content is bound by SIDE-kernel's
  product-formula chain, not by this model."* Read against the paper's claim (arithmetic silence), the model
  **stipulates** the silence flag rather than deriving the arithmetic content → this is the **ENCODES-CONCLUSION**
  shape (a model-stipulation, the real content elsewhere). Profiles verified (2 axiom-free, 1 `[propext]`) —
  the profile is clean; the **statement-vs-claim match is the question**.
- **SCAFFOLDING ×2** — `sieve_ceiling` (base), `bright_access_required` (base): W-6-EXT relabelled these
  abstract-`Proof` **bookkeeping**, with content in the axiom-free `sieve_ceiling_semantic`. "SCAFFOLDING"
  is the paper's honest word for **SHELL-adjacent** (a work-order, cite the semantic terminal instead).
- Ruling needed: do the three **Model-level** rows re-grade to **ENCODES-CONCLUSION** (model stipulates,
  content bound in SIDE-kernel — cite `conservation_of_spectra`, not the model), and the two **SCAFFOLDING**
  rows to **SHELL** (cite `sieve_ceiling_semantic`)? That is a scope call (the Exclusion Engine precedent),
  not an editorial one — held.

**C · THE_SUBSTRATE (enshrined, m5-3) — normalization flagged.** Uses a combined "grade / axioms" column
with **"PROVED"** for the compiled analytic rows ([[7,1,3]], calibration, Ω_b). "PROVED" is not a rubric
grade; the rubric reads a compiled theorem as **DERIVES** regardless of axiom profile. A pure label
normalization (PROVED → DERIVES, profiles unchanged, no claim change) — but the paper is **enshrined**, so
the normalization awaits an author nod rather than an unbidden edit to a landed keystone.

## Commits this pass

**None.** Every non-rail table is either already rubric-correct (LIC, GRH, ADDITIVE, RCURVE, SILENCE —
re-verified, nothing to fix) or held (FOUNDATIONS, THE_SUBSTRATE); the RH rail is held absolutely. No label
changed, no profile rounded, no pin stale — so there is nothing to commit and nothing to bump. The wave's
product is this report: the corpus's Correspondence layer is rubric-clean outside the held rail, with one
genuine scope question (FOUNDATIONS) and one cosmetic normalization (THE_SUBSTRATE) awaiting the author.

## Pins

PLACE-papers `main` unchanged (working tree clean; RH rail `67da789..HEAD` = `[]`). Kernel repos re-verified
present at HEADs. No PLACE-papers commit this pass — the deliverable is the inventory + grades + holds above.
