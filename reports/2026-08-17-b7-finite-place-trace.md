# CHECKPOINT `B7` — THE FINITE-PLACE TRACE
## ### **THE SPECIFICATION, THE READ (THEOREM 2: SONIN IS ARCHIMEDEAN-RIGID), THE CANDIDATE AT `E2`'s DOOR, AND AN EXACT PROBE**

**Relay report · 2026-08-17 (eleventh sitting) · author-ruled, full perspective · run on FABLE per the routing · `INSTRUMENT` track · sub-gate declared · nothing deposits**
**`h2` UNCHANGED. NO SIGN SENTENCE. NO CONSTRUCTION. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 309298cf6f2fe553a6deb8a128971feb09f854c8   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 309298c   ### 22-file roster + MANIFEST, 3 probes PASS
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> ### **RULE-3 LOG (both halves now filed — and the redundancy fired on day one):**
> **identities:** spec `§6`–`§7` — banked · `2310.18423` abstract — banked verbatim, RE-VERIFIED LIVE this
> sitting · ### **"CC Selecta introduction strategy sentence, banked" — FAILED THE CHECK: no Selecta
> sentence is banked anywhere.** *The sentence was then LOCATED AND BANKED from `2310.18423`'s own
> introduction — the ferry-side and executor-side halves disagreeing and the disagreement producing both
> the error and its repair, which is what the redundancy is for.* **banned-list grep: clean; terms coined:
> none** *(one terminology slip named: the ferry's "units' ball" for `{|x|_𝔭 ≤ 1}` is the INTEGERS' ball).*

---

## §1 — THE SPECIFICATION *(`§8.1`, written before any candidate)* — ### **AND THE HARD COLUMN IS WEIGHT**

**(a)** Sonin-type at `𝔭` *(the archimedean definition transposed; anchor: the banked parts list)* ·
**(b)** class-separating through `τ` *(the rider, now a column)* · ### **(c) CARRIES WEIGHT — the value on
the lag-`log q` correlation must scale with the prime's explicit-formula coefficient, because the
archimedean trace is COEFFICIENT-SILENT and the phase boundary put the weights on the trace side** ·
**(d)** enters as the summand `Tr_F = Tr_∞ + Σ_𝔭 Tr_𝔭` *(`NAVIGATOR-ASSERTED` at its CC provenance)*.

> ### **A CANDIDATE THAT FILLS (a), (b), (d) AND IS WEIGHT-SILENT HAS BUILT ANOTHER ARCHIMEDEAN-TYPE TRACE
> AT A FINITE ADDRESS — NOT THE THING THAT PAYS FOR A PRIME.**

## §2 — THE READ *(`§8.2`; two fetch modes; the retrieval-gloss law applied)*

> ### **THEOREM 2, NOW HELD VERBATIM: "Let `S` be a finite set of places with `∞ ∈ S` and `λ > 0`. Then
> the map `θ_S` induces a hilbertian isomorphism of the Sonin spaces `θ_S: 𝐒_λ(ℝ, e_∞) → 𝐒_λ(X_S, α)`."**
>
> ### **CONSEQUENCE, LONGHAND FROM THE VERBATIM: THE SEMILOCAL SONIN SPACE IS ONE ARCHIMEDEAN COPY FOR
> EVERY FINITE `S` — FINITE PLACES CONTRIBUTE NO INDEPENDENT SONIN DIRECTIONS.** *"Stability" is the
> corollary; the banked "place axis is inert" is not merely confirmed but EXPLAINED — inert because the
> Sonin mechanism is archimedean-rigid.* ### **AND IT CUTS AGAINST THE `Tr_∞ + Σ_𝔭 Tr_𝔭` SUMMAND SHAPE,
> which now carries that tension on its face.**
>
> **Also banked, from the paper's own introduction:** *"the Sonin space … should, when suitably equipped
> with a prolate type operator, play the role of the sought for Weil cohomology"* — **the field's own
> statement tying the Sonin space to the missing codomain; kin to the self-product arrest, noted not
> merged.** *The `§4.5` local definition remains a named partial-read caveat: one fetch mode truncated,
> the other returned gloss-inconsistent vanishing directions — fragments only were inherited.*

**CC's own structures against the columns: (a) PRESENT-AS-ABSORBED · (b) ABSENT · (c) ABSENT · (d) NOT
ESTABLISHED.**

## §3 — THE CANDIDATE *(`§8.3`, named NOT run)* — ### **`E1`–`E3` REGISTERED, AND THE READ POINTS AT `E2`**

*The local Sonin space at `𝔭` (vanishing with local transform on the integers' ball), `S_𝔭`, `ϑ_𝔭`, the
candidate trace `Tr(ϑ_𝔭(g)S_𝔭ϑ_𝔭(g)*)`.* **Registered:** `E1` — nonzero, weight-carrying → the column
fills · `E2` — trivial/rigid or weight-silent → ### **the finite trace must come from ELSEWHERE, and the
elsewhere is named: the LEDGER — a weight-carrying finite trace would have to be built from
explicit-formula prime-side data, not Sonin data** · `E3` — filed openly. ### **Theorem 2 points at `E2`,
recorded so the run cannot later claim surprise. The dimension check is priced at ONE SITTING (a finite
DFT kernel on `ℤ/p^{2n}`) and awaits your word.**

## §4 — THE DECOMPOSITION PROBE *(`§8.4`; registration banked before any number; ambiguity named)*

*The ferry's "scalar full-vs-`μ` cell with the `ψ`-decomposition post hoc" is ambiguous — the scalar `±1`
precedent lives on the RATIONAL bench, which has no class group. The registered choice (per the exp1
precedent of naming ferry discrepancies): three columns side by side — scalar-`ℚ` · `ψ₀` · `ψ₁` — at the
new bipartite cell `L = 3.9`, floors from the start (B6's repair inherited), `μ` exact, gate inherited.*

> ### **GATE PASS · BIPARTITE AT ALL `ω` · EVERY CELL EXACT: `|full − μ| = 0` FOR ALL THREE COLUMNS AT ALL
> THREE `ω` (`μ = 333 / 668 / 1336`), BOTH CONVENTIONS.** *`Q1`'s registered sentence is the verdict: the
> scalar `±1` has NO analog at the bipartite cells this instrument reaches; its home remains the
> higher-`r` regime, out of reach of exact `μ` (blossom absent, declared).* **Across `B6` + `B7`: two
> cells, nine component measurements, zero disagreements.** *Bench-grade, proved nowhere.*

## §5 — THE `§1-bis` IDENTITY *(question grade, `OURS`)* AND THE SWEAR-JAR RULE

> ### **ARCHIMEDEAN PAYS STRUCTURE; FINITE PAYS WEIGHT; `h2` IS WEIGHT-DEPENDENT.** *Three graded facts
> aligned — the coefficient-silent lifted trace (`§7(a)`), the phase boundary (`§1-bis`), Theorem 2's
> rigidity (`§8.2`) — pricing `(c)`'s hardness. An alignment, not a theorem.*

**And Rule 3's ferry-side half (the navigator's swear-jar rule) is filed in `protocols/`:** *every ferry
ships with its Rule-3 line; the executor's check runs regardless; disagreements are reported, never
silently resolved — with this sitting's Selecta/2310-intro catch recorded as the first instance.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **THE SPECIFICATION STANDS, AND ITS HARD COLUMN IS `(c)` — WEIGHT.**
2. ### **THE READ IS DISCHARGED AT ITS DECISIVE POINT: Theorem 2 verbatim — the Sonin space is
   archimedean-rigid — explaining the inert place axis and cutting against the summand shape.**
3. ### **THE CANDIDATE IS REGISTERED WITH THE READ POINTING AT `E2`; the dimension check is priced (one
   sitting) and NOT run.** *Your word opens it.*
4. ### **THE PROBE LANDED EXACT AGAIN — the identification mechanism is now exact across every bipartite
   cell it has been asked of.**
5. **Road B's standing sentence after `B7`:** *the lifted object has address data, completion, involution,
   and the archimedean trace; the finite-place trace is specified but unbuilt, the field's own theorem
   says Sonin cannot supply it, and the weight column names where it would have to come from — the
   ledger.*

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — committed at `relay` local tip, **UNPUSHED**, replayed
on top of this report per the standing procedure, absence from the public tree re-verified after this push.
*Release condition unchanged: counsel's answer, then your word.*

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. NO CONSTRUCTION. `WIDEN` PAUSED. NOTHING DEPOSITS.**
