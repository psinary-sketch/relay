# `W-ORD-H1` BANKS — THE COMPUTED ISOLATION — 2026-08-10

**Banking pass, author-called. Digestion before publication, per the standing ruling.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits. `h2` outside every arrow.**

---

## §1 — THE REGISTER'S CLOSING FINDING *(computed grade)*

> ### **THE COFIBER-NOMINEE FAMILY IS CLOSED.**
> **AMMN Theorem G** — evenness and finiteness — means **every fracture-square nominee, plain or
> equivariant, yields `𝔸_f/ℚ` times a ladder. No non-ladder structure is reachable from the square.**
> *This is not one candidate failing. It is the family being shut by a cited theorem.*

**The arc's positive content, at its earned size — three items, no more:**

| | content | grade |
|:--|:--|:--|
| **1** | **`H⁰`-candidate COMPUTED**: `π₀ = 𝔸_f/ℚ`, `Θ = 0`, matching Deninger's `H⁰` axiom | **COMPUTED** |
| **2** | **the `Γ`-legs computed** (act 1): `det_∞((s−Θ)/2π) = √2·Γ_ℝ(s)⁻¹`, doubly derived, `<1e−40` | **COMPUTED** |
| **3** | **the missing structure ISOLATED BY COMPUTATION: the archimedean scaling seat — external to the square, adjoinable only by declaration** | **COMPUTED (as an isolation, not as an object)** |

> **The third is the arc's real product. The register did not find `H¹`. It found — by computation
> rather than by intuition — exactly what is missing and exactly why the square cannot supply it.**

## §2 — THE SEAL-RETRODICTION *(question grade; `KIN`-until-adjudicated)*

| side | statement | status |
|:--|:--|:--|
| **Chapter 13** | the compiled **`s`-dark theorem** | **THEOREM (compiled)** |
| **the derived computation** | completions + glue yield only **`s`-dark-adjacent data** | **THEOREM (computed, this arc)** |

> ### **TWO WORLDS, TWO METHODS, ONE VERDICT.**
> **`SAME` / `KIN` / `DISTINCT` by future text.** The rhyme is promoted to a **filed question**
> precisely **because both sides are now theorems** — *a rhyme between two guesses is noise; a rhyme
> between two theorems is a question worth a register.* **It is promoted no further. `KIN` until a
> text says otherwise.**

## §3 — THREE LOOM LINES FILED

| line | content |
|:--|:--|
| **a registered refutation is the discipline's payout** | act 5 as the worked example — **wrong answer AND wrong mechanism**, both legible only via verbatim pre-registration. *Register the mechanism, not just the answer.* |
| **name-identity is domain-general** | the robotics catch (`Lyle Antieau` / `Nikolaus Correll`). *A search listing is an identity claim made by a string matcher.* |
| **`open(path,'w')` truncates before it can fail** | **filed in-pass, below** |

## §4 — THE CONSOLIDATION: `THE_CARRIER_SPECIFICATION` v0.3

**`D:\\MY-DOwnloads\\PLACE-papers\\phase2\\method\\THE_CARRIER_SPECIFICATION.md`**

**New §§15–21**, the join / `H¹` era at final grades: the two computed legs with the `√2`
bookkeeping · the seven-row dossier · the recollement / `Φ := i*j*` formalism · **the nominee
family's computed closure (AMMN Theorem G)** · the tower reading with its **one MATCH** · **the
isolation finding** · the `ℤ/2`-at-`∞` pattern at question grade · the declared-unread remainder.

**§11's DECLARED-UNREAD is DISCHARGED in place**: the Bökstedt numbers are now at cite
(Krause–Nikolaus Cor. 4.2), noted as an explicit `v0.3` update rather than a silent edit.

**Lenses re-run:** `DRAFT-WAVE-BOUND` unchanged · **99 table rows, 0 blank cells** · **0 literal
escapes** · **0 mojibake** · 5,276 words · 33,692 bytes · **the honest closing carried VERBATIM**
(three-derivations / not-evidence-it-can-be-met / `h2`-untouched — all three verified present
before *and* after the edit).

### The repair this pass performed, and the damage it did on the way

**v0.2 carried literal `\\uXXXX` escape damage in §§9–14 — `Spec \\u2124` where `Spec ℤ` belongs.**
**True count: 106 occurrences across 50 lines**, repaired in v0.3. *An earlier note in
`OPEN_TRAILS` said "43 lines"; that came from a `\\u2`-only grep which missed the `\\U0001…` forms,
and has been **corrected in place at the true figure rather than left standing**.*

> ### **AND THE BUILDER DESTROYED THE TARGET.** The first `v0.3` script ended in
> `io.open(P,'w').write(s)`; `s` held lone surrogates, `.write()` raised — **and `open(...,'w')` had
> already truncated `THE_CARRIER_SPECIFICATION.md` to ZERO BYTES.** **Restored from git
> (`git checkout --`), verified at 27,001 bytes with the closing intact, and rebuilt with a
> build → encode-test → temp-file → `os.replace()` pipeline.** *Reported here rather than quietly
> repaired: the file was recoverable only because it was committed.*

## §5 — THE BOARD FOR ROTATION *(no ranking; the author rules)*

| | option |
|:--|:--|
| **(a)** | **the scaling-seat question** as a next research register — three named routes (`T`-Tate completion · CC's `𝔸_∞` · Deninger's boundary), **none claimed** |
| **(b)** | **`W-ORD-SLOPE`** at threshold |
| **(c)** | **rest** — all banked |

## §6 — FOUND-BEYOND *(one item; a banking pass carries no registration, declared anyway)*

> ### **THE CONSOLIDATION TARGET WAS ITSELF DAMAGED, AND ONLY OPENING IT TO WRITE `v0.3` REVEALED IT.**
> **Third distinct instance of the class "a script wrote its own escaping into a document"** — after
> the backtick substitution and the ERE `\\|`. **The v0.2 pass reported success and the damage sat
> for the whole arc.** *A document is not verified by having been written.*

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | this pass |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE, untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing constructed. `Q2`–`Q3` untouched. Nothing deposits. `h2` exactly where it was.**

> ### **Realization is not placement.**
