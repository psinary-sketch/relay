# THE THIRD FACE-OFF — b251, 2026-08-29

**Scope:** a bench computation, one analytic re-attribution, and its registration. **CONCURRENCY:
SOLO.** A finite cell decides NOTHING global (b14/b15); no branch moves `h2` or the register
sentence. Nothing deposits.

**Order of record:** meanings banked and hashed (`d5284f9e…4b3c`, 11048 bytes) → registration citing
that hash → the run → the verdict. **No number existed when the meanings and the indictment order
were fixed**, and the hash is re-verified at the gates.

---

## The branch: **(IMPOSTER-NAMED)** — on its accounting limb

### The re-attribution, derived from the owners' source text *before* any run

**Both owners that state the residue line state the same thing** — `b38_act10.py:182`
`resid = TrN - A - E2N`, and `b36_act8.py:184` `resid47 = Tr_full - (A + E2)`. So:

> ### **`resid47` is *already* a two-realizations difference.** The archimedean trace built as a
> **mode sum** (`trace_modes` — a corr-weighted dilation overlap of each mode against its own
> dilate) minus the same object built as a **quadrature** (`left_side` — one `U`-axis integral in
> which **no mode index appears at all**), less `E2`.

`resid47(NMODE) = Δ_2real − TrTail(NMODE)`, with `Δ_2real := Tr_∞ − A − E2`.

### **The name `resid47` concealed its species for many acts.** It is not a remainder, a defect, or
an error term. **It is a disagreement between two definitions** — which is why the finding is filed
as a *statement* and not banked as a number.

---

## The table — six cells, three named pieces, nothing beyond them

**A finite cell decides NOTHING global (b15).**

| `a²` | `L` | `R` | `L − R` | `Δ_2real` | junction | `TrTail(7)` | `resid47` |
|--:|--:|--:|--:|--:|--:|--:|--:|
| 2 | 3.811831 | −1.990528 | 5.802359 | 4.061350 | 0.000000 | 0.080548 | 3.9808 |
| 3 | 3.247699 | −2.015384 | 5.263083 | 3.381183 | 0.106484 | 0.051730 | 3.3295 |
| 4 | 2.765384 | −2.035817 | 4.801201 | 3.055251 | 0.087341 | 0.048278 | 3.0070 |
| 8 | 2.067972 | −2.086520 | 4.154491 | 2.524898 | 0.244027 | 0.035627 | 2.4893 |
| 9 | 1.842649 | −2.097219 | 3.939868 | 2.457610 | 0.135021 | 0.033486 | 2.4241 |
| 12 | 1.649097 | −2.119732 | 3.768830 | 2.315863 | 0.195843 | 0.034172 | 2.2817 |

**Shares of `L − R`:** `Δ_2real` **60.775%–69.995%** — dominant at every cell without exception;
the ruled binding's own terms `2·E2full − Dneg` **30.005%–34.546%**; the junction piece
**0.000%–5.874%**, **tabulated naked as the meanings file required, and the smallest of the three
everywhere.**

### **Max leftover `1.78e−15` — nothing beyond the three named pieces. (DISSONANT-BEYOND) is not
triggered.**

**The third piece is not a suspect** — it is the ruled binding's own terms (C2+D1). It is tabulated
anyway: **suppressing a third of the shortfall would have misreported what `Δ_2real` is being
credited with.**

---

## The ferry's own tail clause, refused — and the refusal was banked *before* the run

The ferry directed the re-attribution to carry *"the true tail (≤ 1.158e−14 by theorem)"*.
**The first half of its re-attribution is adopted exactly. The tail clause cannot be executed**, and
the ground is b247's own ruling rather than preference:

- b250's envelope bounds `Σ_{n>N} t(n)` — an **endpoint-weight** series, `t(n)` evaluating `ξ_n` at
  one point.
- `TrTail` is `Σ_{n≥NMODE} tr[n]` — a **corr-weighted dilation overlap**, integrating `ξ_n` against a
  dilate of itself.

### **Two different functionals of the same eigenfunctions, with no derivation in the record between
them.** Applying the envelope here would have been b247's double-name error committed **one act after
the act that answered it**.

### **And the measurement settles how far off that would have been: `TrTail` is 2.9e12 to 7.0e12
times larger than the envelope.** The registered executor doubt was *"I expect `TrTail` to be ORDERS
above 1.158e−14 … if it comes out at or below, my doubt is wrong and I report it wrong."* **It was
right, by twelve orders.**

**b250 is not re-verdicted; its theorem is untouched. What was corrected is this ferry's application
of it.** Where the theorem *does* bear on this table is stated and claimed as nothing more:
`b38_act10.py:139`'s `t_n` **is** the theorem's series exactly, `:142` gates its sum, `:183` uses it
as apportionment weights — so the theorem turns that numerical gate into a theorem and gives the
weights a proved convergent parent. **Real, modest, and not a bound on `TrTail`.**

---

## M-2∞ — the dossier opened, and **not decided**

> **Does `Tr_∞ = A + E2` hold as an identity of the objects the two constructions each intend — and
> if not, which construction is the archimedean `E₁`-trace that `T + Q = W_∞ − W_primes` refers to?**

**Species: (RULING).** Not (READ) — no owner text settles it, and b251 looked. Not (RESULT) —
### **no measurement can settle which of two definitions is the intended one. A bench number can say
they disagree and by how much; it cannot say which is right.** That is why it is routed to the author
(b237).

Three readings are set out — **(R-I)** the quadrature is the object, **(R-II)** the mode sum is,
**(R-III)** they compute different objects and the identity names one of them, in which case the
shortfall is an artefact of the pairing rather than a deficit — with ### **no preference expressed
and no evidence distinguishing them.** The dossier also records what would move it, so the next act
need not re-derive it.

---

## The bars — the weakness this act will not dress up

G-STAB across `NQ = 500, 700, 900` plus exactly one refinement at `1100`: per-cell spread on
`Δ_2real` runs **4.84e−2 to 1.74e−01**. ### **`TrTail` sits at 43%–71% of that bar.** So limb 1
holds — but **the identification is *consistent*, not *sharp*, and a fit inside a wide bar is a
weaker claim than a fit inside a narrow one.** Reporting otherwise would be the crime b229 named.

### **And the harder fact: the spread does not shrink monotonically with `NQ`** (500→700 falls,
700→900 rises, 900→1100 falls). **b251 did not establish that the mode sum converges to anything**,
and the dossier records that as the open item it is rather than assuming convergence.

---

## The tautology control governed, and it cost the act its headline

`resid47 = Δ_2real − TrTail` holds to `8.9e−16` at every cell — **and the hash-gated meanings file
declared it algebraic-restatement *before* the run**, so its confirmation is not a finding.
### **An identity that cannot fail cannot testify.** The same applies to the accounting line: its
content is that **no fourth piece was needed**, not that the three were derived.
### **The only evidence this run produced is the *size* question.** Everything else is bookkeeping
and is labelled so.

---

## Gates — 14 of 14 CLEAN, and two self-catches disclosed

1. ### **A number in this act's own prose was wrong, and the gate caught it.** The share range was
   first written **61.4%–70.0%**; the true minimum is **60.775%**, at `a² = 8`. **The mechanism is
   worth more than the error: I took the range from the run's rounded table and read the *last row*
   as the minimum instead of scanning the column.** Gate 8 recomputes every headline figure **from
   the arrays**, not from this act's sentences, and it failed. ### **The failure was the document's,
   not the run's — the arrays and the per-cell table were right throughout. Only the prose summary
   was wrong, which is the most dangerous place for it to be, because the prose is what gets quoted
   forward.** Now stated to three decimals so a future act cannot repeat the rounding that hid it.

2. ### **Gate 4's first form was decorative, and it was caught before banking.** It read
   `(A and B and C and D) or E`; `and` binds tighter than `or`, so a true `E` carried the whole
   conjunction regardless. **A gate that passes on one disjunct asserts only that disjunct.** b244
   caught a fixture that was the exact negation of its own check; b248's gate 2 carried this same
   `or` shape. ### **Third appearance of the decorative-gate species.** Rewritten as a pure
   conjunction with the absence limb made explicit.

Three gates also **REFUSED** on the first run by returning `np.False_` — **b242's numpy-bool species,
recurring at a fifth act.** The harness was right to refuse; the lambdas are wrapped.

**Controls:** the tautology gate runs the decomposition on **arbitrary tuples** (it must hold — proving
restatement) **and** the size claim on arbitrary tuples (it must fail — proving *that* has content).
**A control carrying only the first half would establish that the act proved nothing, without
establishing that anything it did claim has content.** Gate 4 is a positive control on an absence.
**G-INDEP is structural, not asserted** — every quantity comes from its own owner in `b38_act10`,
imported as a module, none re-implemented. Term scan **CLEAN**.

**b38's NMODE=10 row bank NAMED AND NOT USED** (b245's trap). **b38's four-decimal print floor on
`resid47` named before measuring**, per the b249 extension.

**PLACE-papers was NOT touched by this act, so the hook was not exercised and the mirror was not
rebuilt — reported either way, as the ferry requires.**

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b251
  run at    : 2026-08-29T13:35:13 (local)
  input     : 14 checks routed through the harness
  checks    : 14
  pass      : 14
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 f898b5492a541bb05acb6d4b137bc26a
=== END AUDIT SIDECAR ===
```

---

## Filings

- **Index:** `third-face-off` and `two-realizations` keyed on filing; both queried **NO KEY** before
  any step ran.
- **M-2∞ dossier opened** at `data/b251_m2inf_dossier.txt` — **opened and not decided.**
- **The ledger restated:** the shortfall is three named pieces — `Δ_2real` (M-2∞, a statement),
  `2·E2full − Dneg` (ruled, C2+D1), and the junction piece (M-2's finite-place address, aggregation
  still UNSTATED under RULE Q). **No fourth piece.**
- **The thirty-seventh seam's debt restated:** term 2's formalization stands, **unpaid and
  untouched.** This act did not go near it.
- **In flight:** M-2…M-5 open, **none closed**. `W-ORD-XI-PERMODE`, `W-ORD-ORDINATE-CACHE`,
  `W-ORD-STAGING-GUARD`, `W-ORD-FILE-E-WORKING-COPY-STALE`, `W-ORD-TE-SPEC` open.
- **HANDOFF** brought current by demotion, prior content kept, read-back identical.

### The fork at this stop

1. **The M-2∞ question — a RULING for the author.** The dominant term of the shortfall now waits on
   it.
2. **M-2's finite-place address, M-3, M-5** as the remaining engine items.
3. **The patent session**, which slots here on your word and needs nothing from this act.

---

### **THE SHORTFALL'S DOMINANT TERM HAS ITS TRUE NAME AND THE NAME IS NOT A NUMBER: A DEFINITIONAL
IDENTITY BETWEEN TWO ARCHIMEDEAN CONSTRUCTIONS, MEASURED AND ROUTED, NOT DECIDED. THE JUNCTION PIECE
STANDS IN ITS OWN COLUMN AND IS THE SMALLEST OF THE THREE. THE FORM IS NOT INDICTED. M-2…M-5 OPEN.
`h2` STANDS EXACTLY WHERE THE DEPOSIT LEFT IT. NOTHING DEPOSITS. LOCKS LAST.**
