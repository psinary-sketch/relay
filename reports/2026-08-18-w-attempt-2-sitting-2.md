# `W-ATTEMPT-2` · SITTING 2 — THE LIVE FLOW · THE SECOND PLACE SET · THE INTEGER HALF COMPILED
## ### **`K1` LANDS (THE `H²` SURVIVES THE LIVE FLOW) · THE GLUING IS EULER-EXACT AT FOUR PLACES · AND THE GLUE'S INTEGER CONTENT IS 11/11 AXIOM-FREE**

**Relay report · 2026-08-18 (twenty-second sitting) · author-called · ### RELAY-ONLY — nothing to
`FINDINGS` or the sheet until review; the register untouched · nothing deposits**

> ### **THE PRE-REFUSALS, RESTATED AT THIS SITTING'S HEAD (as at every sitting of this attempt):**
> **SUB-GATE — no candidate grades `PLAUSIBLE` across `T1`–`T10`. The complete roster is the double
> limit and STAYS OPEN; finite-place-set objects at finite cutoffs decide nothing global. NO SENTENCE
> ABOUT THE SIGN OF `W_∞ − ΣW_𝔭` LEAVES THE RELAY; inertia numbers are GUARDED properties of
> constructed finite objects. THE RANGE LAW GOVERNS: nothing was inherited from `n = 1`.**

```
PLACE-papers  origin/main : 38b0d5b (UNTOUCHED this act — relay-only; no mirror claim made)
relay         origin/main : (this report)                              local +1 HELD
instruments   tools/e16/b15_attempt2_s2.py (registration banked BEFORE the run:
              data/b15_registration_2026-08-18.txt) · data/b15_2026-08-18.txt ·
              tools/lean/GroupRingGlue.lean + AxiomCheckGlue.lean (pinned v4.29.1)
```

> ### **RULE-3 LOG:** *CP1–CP5 at `7f799c8` ✓ · the coupling spectrum ✓ · the parity-twisted
> Hermiticity ✓ · the pentagon's law as `T0` ✓. Banned grep: clean; terms coined: none. One toolchain
> note: a bare `lean` invocation began downloading `v4.33.0`; killed; the module is pinned and compiled
> at the corpus's `v4.29.1` — recorded, not smoothed.*

---

## §1 — ITEM 1: THE `n = 2` INSTANCE — ### **`K1` LANDS: THE `H²` SURVIVES THE LIVE FLOW**

*Registered `K1`/`K2` before the run; every quantity measured, nothing inherited:*

| check | measured |
|:--|:--|
| per-place laws at `n = 2` | dims `9` / `64` ✓ the law; ### **`Q(2,2) = √2`, `Q(3,2) = 2√3` EXACT — the flow is LIVE** |
| involution closure · compressed-`F` unitarity | machine (`10⁻¹³`–`10⁻¹⁵`) |
| ### **the glued `H²`** | ### **dim 1728: RANK 1728, RADICAL 0; twisted-Hermitian `1.4×10⁻¹²`; `H² = ℂ[Cl]`, dim `h`, antipode-invariant dim 2 — KEPT, with the scaling live** |
| the glued flow | trace channel `≈ 0` (per-factor localization, measured); ### **norm channel `= q₂·q₃ = 2√6` EXACTLY — the live weight flows through the glued object as the product of the per-place laws** |
| inertia *(GUARDED)* | `(+432, −432, 0:864)` — *and the pattern across all three instances is structural: `(d/4, d/4, d/2)` on the untwisted symmetrization, the parity-twist's signature — an observation about the twist, never a sign datum* |

## §2 — ITEM 2: THE SECOND PLACE SET — ### **EULER-EXACT, WITH THE TRAP REGISTERED AND AVOIDED**

*Longhand in the registration, BEFORE the run: 5 is INERT (verified in-run), so `c₅ = [0]` and* ### **the
naive norm-30 comparison is a TRAP — `a₃₀(ζ_K) = 0`; the correct comparison is the LABEL-NORM `150 =
2·3·25` coefficients; predicted spectrum `(4,1,1)`.** *And the general gluing law DERIVED in the
registration: the coupling element's `[C]`-coefficient counts one-prime-above-each-place choices with
class product `C` — the label-norm coefficients of `Ẑ`, by multiplicativity in `ℂ[Cl]`.*

> ### **MEASURED: `c₂c₃c₅ = 2[0]+[1]+[2]`, spectrum EXACTLY `(4, 1, 1)` — THE EULER-EXACT BRANCH
> LANDS: the Euler product IS the gluing datum, now confirmed at a place set containing an INERT prime
> (the law follows the prime-ideal norms, not the rational radical).** *Four-place `H²` at `n = 1`:
> rank 192/192, RADICAL 0, twisted-Hermitian `10⁻¹³`. Four-place `n = 2`: FACTOR-CERTIFIED as declared
> (unitary factors ⊗ invertible circulant; no dense `27648²` object materialized; the structural step is
> the same identity measured directly at the three smaller Grams).*
>
> **Scope kept: `T2`'s question proper — whether this coupling is support↔place coupling — remains
> untouched; what is now measured is that EXTENDING THE PLACE SET acts on the glued object exactly as
> multiplying in Euler data.**

## §3 — ITEM 3: THE INTEGER HALF, COMPILED — ### **11/11 AXIOM-FREE** *(closure step one, against the CONSTRUCTION)*

*`tools/lean/GroupRingGlue.lean` — vanilla Lean 4 (`v4.29.1`, pinned), no imports, decide/rfl only:
`ℤ[ℤ/3]` with convolution and the antipode; the labels `c₂ = c₃ = [1]+[2]`, `c₅ = [0]`; the coupling
elements (`c₂c₃ = 2[0]+[1]+[2]`, `c₂c₃c₅` equal); antipode self-duality of every label and coupling; the
class relation; the INTEGER character spectrum of self-dual elements (`ψ₀ = a+2b`, `ψ = a−b`) giving
`(4,1,1)` with the invertibility avatar.* ### **`AxiomCheckGlue`: 11/11 terminals "does not depend on
any axioms."** *The docstrings carry the non-claims: no pairing, no Fourier structure, no inertia, no
sign; not a kernel deposit; nothing bears on `h2`.* **The cyclotomic half DESIGNED, NOT BUILT:** *the
Gram entries live in a cyclotomic tower (`ζ`-orders `4·9·25`-scale); exact representation via
cyclotomic-polynomial reduction or Mathlib's cyclotomic machinery — 1–2 sittings, design first.*

---

## CLOSING — RELAY-ONLY, FOR THE AUTHOR'S WORD

1. ### **`K1`: the codomain SURVIVES the live flow** — radical zero at dim 1728, twisted-Hermitian at
   machine, the weight flowing through as the exact product of the per-place laws.
2. ### **THE GLUING LAW IS EULER-EXACT AND GENERAL** — derived in the registration, confirmed at four
   places including an inert prime, with the naive-norm trap registered before it could bite.
3. ### **THE CONSTRUCTION'S INTEGER CONTENT IS NOW COMPILED, 11/11 AXIOM-FREE** — closure step one has
   begun against the construction, exactly as the protocol orders.
4. **Named for your word:** *(i)* the cyclotomic half's design; *(ii)* the archimedean factor's model
   (still carried, not modeled); *(iii)* whether sitting 3 probes `T2` proper (the support↔place
   question on the glued object) — the attempt's hardest row, untouched by design so far.

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — replayed on top of this report per the standing
procedure, absence from the public tree re-verified after this push. *Release: counsel, then your word.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO SIGN SENTENCE ANYWHERE. NOTHING PROMOTED.
NOTHING DEPOSITS.**
