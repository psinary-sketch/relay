# THE TWO ROADS OPEN AS CONSTRUCTION — ### **B1–B3, A1–A2, AND TWO INSTRUMENT INCIDENTS**

**Relay report · 2026-08-17 · author-ruled · `INSTRUMENT` track · sub-gate declared · nothing deposits**
**NO ATTEMPT OPENED. NO MECHANISM PROPOSED. NO CANDIDATE PROMOTED.**
**`h2` UNCHANGED. NO SIGN SENTENCE. `WIDEN` PAUSED. RAIL DID NOT MOVE.**

```
PLACE-papers  origin/main : 6cdae3ae64cf23c173420c4a9bc2dcf81d931ff9   VERIFIED (ls-remote)
relay         origin/main : (this report)                              local +1 HELD
mirror        mirror-refresh-2026-08-17.zip @ 6cdae3a   ### 22 / 22 COHERENT, three ways
SIDE-window   ab6f269 (tag v0.4.0 = 1bd2865)   SIDE-effects  afa9ccf   — untouched
```

> **Two build documents:** `phase2/method/THE_ONE_OBJECT.md` *(new, `v0.1` — `C3`'s sheet)* and
> `THE_CARRIER_BUILD.md` *(`v0.2 → v0.3` — `C1`'s sheet)*.

---

## §1 — ROAD B · `B1`: ### **THE SPECIFICATION, WRITTEN BEFORE ANY CANDIDATE**

| | column | what a construction must contain |
|:--|:--|:--|
| **(i)** | **THE ADDRESS SIDE** | the shift structure at lags `log q`, **whose maximum matching number is the pure remainder count** |
| **(ii)** | **THE PLACE SIDE** | a semilocal trace over a finite set of completions — **the thing in which a prime enters as a LOCAL FACTOR and not merely as a distance** |
| **(iii)** | **THE IDENTIFICATION** | ### **ONE operator whose spectral count IS the matching number and whose trace IS the positive floor.** *Not two quantities that agree; one quantity read two ways.* |
| **(iv)** | **THE JOINT MOVE** | ### **widening support and adding a place are THE SAME OPERATION on it** |

### **THE GLUE TEST, STATED FROM THE CORPUS'S OWN VOCABULARY**

*The place-content homonym, resolved 2026-08-11:* the ledger's **active primes** are ### **SUPPORT-FORCED**
*(given the window, the contributing primes are determined)*; the framework's **governing places** are
### **FREELY CHOSEN** *(a finite set `S`, picked and then increased)*.

> ### **THOSE ARE THE TWO AXES, AND TODAY THEY DIFFER IN KIND — one is a FUNCTION of a parameter, the other
> is a PARAMETER OF THE FORMALISM.** *So `(iv)` is the whole of the specification: a construction satisfies
> it exactly when the free choice and the forced determination cease to be different kinds of thing.*

### **AND A NAMED TENSION, WITH A THEOREM ON EACH SIDE**

| side | statement | source |
|:--|:--|:--|
| **the place axis is INERT** | *"the stability of the semilocal Sonin space under the increase of the finite set of places"* | `2310.18423`, read at content |
| **the support axis is NOT** | the count is a sawtooth in `r`, measured at `r = 1 … 8` without a miss | this week's derived law |

> ### **ADDING A PLACE CHANGES NOTHING; WIDENING SUPPORT CHANGES THE COUNT. TWO OPERATIONS WITH MEASURABLY
> DIFFERENT EFFECTS ARE NOT THE SAME OPERATION.** *What the tension does NOT say, stated so it is not
> over-read:* **the stability theorem is about a SPACE and the sawtooth about a DISCRETIZED COUNT — they are
> about different objects and do not contradict each other.** *What they do is price `(iv)`: it cannot be
> met by adjoining the two candidates as they stand.*

---

## §2 — ROAD B · `B2`: ### **THE CANDIDATE TABLE, AND THE EMPTY COLUMN**

| | (i) address | (ii) place | (iii) identification | (iv) joint move |
|:--|:-:|:-:|:-:|:-:|
| **CC's semilocal frame** | **IMPORTED, BY CHOICE** | ### **✓ NATIVE** | ### **✗** | ### **✗** |
| **the corpus's Toeplitz-on-`η` apparatus** | ### **✓ NATIVE** | ### **✗** | **HALF** | ### **✗** |

> ### **`(iv)` IS EMPTY ACROSS BOTH, AND `(iii)` IS HALF-FILLED FROM OPPOSITE SIDES.** *CC has the place
> column and no identification; the bench has the address column and half the identification.* **Neither has
> both, and the missing piece is the same one in both cases — the shape the carrier build found one road
> over.**

### **THE ROW THAT COST THE MOST TO GRADE HONESTLY, AND IT IS OURS**

*The bench's `Φ` does carry an archimedean term and prime terms in one expression.* ### **But the primes
enter ONLY through their addresses `log q` — there is no local factor at `p`, no completion, no place-set
parameter.** **An archimedean term plus prime distances is not a semilocal trace, and calling it one would
be the place-content homonym committed inside our own instrument.**

> **And the measurement argues the bench is already two objects glued by `B1`'s own test:** *this week
> measured `A_main` contributing* ### **at most ONE offender** *to a count of thousands at every window from
> `r = 1.7` to `r = 8`, while the prime part's matching number supplies essentially all of it.* ### **The
> two parts are nearly decoupled in the quantity of interest.**

**Four read-debts named and grading nothing:** CC's `[19]` *(a reference number, not a document)* · CC 1999's
trace formula *(`TITLE-UNVERIFIED`, not read)* · the CC archimedean/semi-local positivity line *(unread, API
`429` twice)* · `2106.01715`'s body *(abstract only)*.

---

## §3 — ROAD B · `B3`: ### **THE MOVE, NAMED AND NOT RUN — AND THE OBVIOUS MOVE REFUSED**

**The tempting move is `L = 2 log S`** — CC's circle length is the corpus's own window width, the **third**
independent parameter correspondence after `√2` and `p ≤ λ²`.

> ### **THE CORPUS ALREADY REFUSED THIS MERGE AND THE REASON DOES NOT WEAKEN WITH REPETITION:** *"three
> coincidences of coordinate are three coincidences of coordinate … a fourth would be no more probative than
> the third, because what recurs is the ARITHMETIC OF A WINDOW, and any two people parameterising the same
> window will agree about it."*
>
> ### **SO THE FIRST MOVE MUST BE ABOUT A THEOREM, NOT A PARAMETER.**

### **THE MOVE**

> ### **GIVE THE CORPUS'S OWN OBJECT A FREELY CHOSEN PLACE-SET PARAMETER, AND ASK WHETHER IT HAS CC's
> STABILITY PROPERTY.** *Define `Φ_{L,S}` — the same form, lag terms restricted to a freely chosen finite
> set `S` of primes. Then widening support (`L`) and adding a place (`S`) both exist on ONE object, and the
> question is a theorem:* **is the count stable under enlarging `S`?**

*It adds nothing but a restriction map — no local factor, no completion, no identification.* ### **It
manufactures the one structural feature `(ii)` has that `(i)` lacks, then asks whether the governing theorem
transfers.**

**Both branches longhand**, and the evidence already in hand is recorded so the answer cannot later be
claimed as a surprise: adding the second prime moved the count by ### **`+285 / +143` offenders at `L = 9`**
and by `−1 / +0` at `L = 12`. ### **The registered expectation is NOT STABLE**, *and the move is worth
running anyway because the live question becomes whether the instability is confined to windows where the
added prime sits at its own apex (`9 = 3²`).* **Named and NOT run.**

---

## §4 — ROAD A · `A1`: ### **`(v)` — THE OBJECT MUST SEE THE MONOID-RESPECTING BASIS**

*`§2`'s build question gains a fifth clause, from a witness and not a preference: the two-decompositions
experiment holds one `ζ_K` fixed and varies only the decomposition, and the off-line zeros appear in exactly
the monoid-breaking basis.*

> ### **`(v)` A CARRIER MUST NOT BE INVARIANT UNDER RE-DECOMPOSITION OF ITS OBJECT.** *An object assigning
> the same structure to both bases has taken the object, not its presentation — invariant in the one variable the witness shows placement tracking *(re-phrased to content 2026-08-17, correction twenty-four's closing)*.*

### **`(v)` GRADES ABSENT ON ALL THREE FRAMES — AND THE QUESTION HAS NOT BEEN ASKED OF ANY OF THEM.**

**The question it forces:** ### **what structure on `Spec ℤ × Spec ℤ` distinguishes the Hecke basis from the
Epstein basis of the same object?** *Both recombine to the same `ζ_K`; they differ in `S1b`, which is a
statement about a monoid — so `(v)` is the demand that `(i)` be carried far enough to remain visible after
the pairing is formed.* **No candidate structure is proposed.**

---

## §5 — ROAD A · `A2`: ### **THE READ WAS ALREADY BANKED**

> ### ***POSSESSION PRECEDES RECOGNITION — BEFORE FETCHING, GREP.*** *The read this ferry ordered was run on
> 2026-08-09 and its result is in `OPEN_TRAILS`. The corpus-first sweep found it before any fetch was
> attempted.*

| | |
|:--|:--|
| ### **PRESENT** | a topos-theoretic **square** *("realize the Frobenius correspondences in the square of the 'Arithmetic Site'")*; the complete Riemann zeta on it as a Hasse–Weil zeta; and an RR line delivering *"the cohomologies …, their integer dimension, and Serre duality"* plus a monoidal Picard extension |
| ### **ABSENT** | the codomain — *"the main open question is the definition of a suitable Weil cohomology … as a Lefschetz formula"*; and ### **all of the RR line lives on the arithmetic CURVE, not on the SQUARE** — *"a cohomology on the curve does not supply the square's `H²`"* |

**Sourcing declared:** the arrest statement is ### **`ATTRIBUTED`, not read in the paper body** *(PDF `403`)*;
`1405.4527` and `1502.05580` are ### **`TITLE-UNVERIFIED`** in the `BIBLIOGRAPHY`.

**And the scaling site is HELD** — `1507.05818`, at cite, already graded: its per-prime *"Jacobian"* is
**cyclic of order `p−1`**, so a height theory on it is **vacuous**. ### **It grades ABSENT on the
self-product question and ABSENT on `(v)`, for two independent reasons.**

### **THE ARREST, RE-STATED — AND IT NARROWS**

> **Before:** *a self-product exists; what must be checked is whether it carries an intersection theory.*
> ### **NOW: … AN INTERSECTION THEORY THAT IS NOT INVARIANT UNDER RE-DECOMPOSITION OF ITS OBJECT.**
> ### **THE ARREST DID NOT MOVE. IT GOT NARROWER, WHICH IS THE ONLY KIND OF PROGRESS THIS SHEET IS
> CHARTERED TO PRODUCE.**

---

## §6 — ### **TWO INSTRUMENT INCIDENTS, BOTH RECORDED AS SECOND INSTANCES**

### **(1) `open(path, 'w')` TRUNCATED `THE_CARRIER_BUILD.md` TO ZERO BYTES**

*A landing script raised `UnicodeEncodeError` on `.write()` — and the target was already empty.*
### **Restored from `git` in one command, bit-identical, `git status` clean.**

**The cause is new and is named separately:** *the 2026-08-10 instance INHERITED its surrogates from data;*
### **this one MANUFACTURED them — an astral character (`𝒪`) written into a Python source as the UTF-16
surrogate-pair escape `𝒪`, which is two lone surrogates and cannot encode to UTF-8.** *The file
was well-formed; the script was not.*

> ### **THE UNCOMFORTABLE PART: THE LOOM ALREADY CARRIED THIS LAW WITH THREE REMEDIES, AND THE SCRIPT USED
> NONE OF THEM.** *Build-then-encode, temp-plus-`os.replace`, and `assert len(existing) > threshold` were
> all written down after the same failure destroyed `THE_CARRIER_SPECIFICATION.md`.* **Knowing a remedy and
> applying it are different acts, and only the second one saves a file.** *All three were used for every
> subsequent write this sitting.*

### **(2) A CASE-SENSITIVE PROBE PRODUCED A FALSE NEGATIVE THAT WOULD HAVE ENTERED THE RECORD**

*A probe for `scaling site` returned zero hits, and a sheet row reading* ### ***"THE SCALING-SITE LINE IS NOT
HELD AT ALL"*** *was drafted from it.* **The case-insensitive re-run found it immediately** — held at cite,
quoted, and already graded.

> ### **THE FALSE CLAIM WOULD HAVE INVENTED A READ-DEBT WHERE A GRADED READ ALREADY EXISTED** — the exact
> inversion of *possession precedes recognition*, written into a build sheet as a reason to fetch something
> the corpus had held for six days.
>
> **Rule extended in the loom:** ### **corpus probes run case-insensitive by default, and a nil result is
> re-run by a different match mode before it is written down as an absence** — *because "not held"
> authorizes a fetch, a read-debt and a priced sitting: three costs on a false negative.*

---

## CLOSING — FOR THE AUTHOR'S WORD

1. ### **`B1` IS WRITTEN AND ITS HARDEST COLUMN IS `(iv)`**, with a theorem on each side of the tension and
   the honest note that they concern different objects.
2. ### **`B2`'s EMPTY COLUMN IS `(iv)`; `(iii)` IS HALF-FILLED FROM OPPOSITE SIDES** — and the least
   comfortable row is ours: by `B1`'s own test the bench is two objects glued, and this week's measurement
   is the evidence.
3. ### **`B3` IS NAMED AND NOT RUN**, is about a theorem rather than a coordinate, refuses the
   `L = 2 log S` merge with the corpus's own reason, and records the evidence that predicts its answer.
4. ### **`(v)` IS SEATED AND GRADES ABSENT EVERYWHERE**, with the `Spec ℤ × Spec ℤ` question it forces.
5. ### **`A2` WAS ALREADY BANKED**; the arrest did not move, it narrowed.
6. ### **TWO INSTRUMENT INCIDENTS RECORDED**, one of which destroyed a tracked file and was recovered.

### **WHAT WANTS YOUR RULING**

1. **`B3` is priced at one sitting** and its expectation is registered as NOT STABLE. *Run it, or leave it
   named?*
2. **The `(v)` question is unasked of all three frames.** *Asking it is a read per frame, not a construction.*
3. **Four read-debts on Road B and two `TITLE-UNVERIFIED` IDs on Road A** — *all four of the Road B items
   have failed to load before, twice for one of them.*

### **STILL HELD:** the two `W-CARRIER-BUILD` acts — **committed at `relay` local tip, UNPUSHED, absence from
the public tree re-verified after this push.** *They are `C1`'s adjudication.* **Release condition unchanged:
counsel's answer, then your word.**

**NO ATTEMPT OPENED. `h2` UNCHANGED. NO SIGN. `WIDEN` PAUSED. NOTHING DEPOSITS.**
