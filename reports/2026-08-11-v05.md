# THE `v0.5` CONSOLIDATION — 2026-08-11

**Digestion before publication. Workshop-side. Nothing deposits.**
**Rail `de621b1` / `2147a03` unmoved.**

---

## §1 — `THE_CARRIER_SPECIFICATION` v0.5

**New §§28–32:**

- **§28 the raw-material anatomy** — the composition at cite (finite primes *"supplemented by a
  pairing at infinity"*, Green function, admissible metric) and the definiteness's source at cite
  (Hodge Index = a **signature** `(+1,−1,…,−1)`, not definiteness; the proof consumes **a Jacobian**
  and **height theory on it**). ### **Metric-born pairing; Jacobian-borne definiteness**, provenance
  kept.
- **§29 the well table, FINAL** — idele-class row: **theta LOCATED-AT-CITE** · **RR by Poisson**,
  with the FE-as-duality question filed · **polarization ABSENT-CONFIRMED** (0 occurrences) ·
  **heights PARTIAL** (`deg` linear, not quadratic). Seat's tori **GESTURED**. CC's **finite**
  "Jacobian" at its graded arrest — heights **vacuous**. Plus the authors' own confessed limit
  (*"no definition of `h¹` without recourse to duality"*).
- **§30 the `ℚ`-point finding** — `dim V = r₁+r₂−1`; for `ℚ` that is **0**, ### **the torus is a
  point**. Genus paradox, **fourth arrival**, in the source's own coordinates. **Two gradings
  corrected, both conservative:** act-4's *"ours"* → **AT CITE**; act-3's *used-not-stated* →
  **STATED**.
- **§31 THE TERMINAL DELTA** — see below.
- **§32 a proposed third evidence grade** — **not applied.**

## §2 — THE TERMINAL DELTA

> ### The classical Riemann-form mechanism is **STRUCTURALLY UNAVAILABLE**: a **real** skeleton where
> the mechanism needs a **complex** one — and `C5` independently **forbids** the real structure the
> geometric side would want.
>
> ### What the constraints jointly specify is a **NEW SPECIES: a HERMITIAN polarization — with `i`,
> without conjugation — on the enlarged, infinite-dimensional object.**
>
> ### ⚠ **`MY-SYNTHESIS`. Flag on and staying on.** `E1`/`E2` are not at cite, so the delta's two
> halves are joined by an unsourced classical link. **The era's terminal requirement as assembled —
> a synthesis, not a result.**

**The `ℤ/2` unification stays at question grade. NO PROMOTION.**

## §3 — THE PROPOSED THIRD GRADE *(not applied)*

> ### **`STANDARD-AT-TEXT`** — a named-textbook citation, unfetched, distinct from `AT-CITE` and
> `MY-SYNTHESIS`. **If ratified**, `E1`/`E2` re-grade under it (Birkenhake–Lange; `[n]*θ ∼ n²θ`).
> **The cells stand at `NOT-AT-CITE` until the author rules.**

## §4 — THREE LOOM LINES

- **Over-caution is also an error direction** — the first conservative corrections in this corpus.
  *An under-claim breaks nothing downstream, so no check fires; the register simply carries a weaker
  corpus than it has earned.* **Calibration is now two-sided.**
- **The `ℚ`-point joins the degeneration family** — with the trivial Jacobian, the mono-collapse, and
  `R5`'s vacuous realization. ### **`ℚ` is not the hard case; it is the degenerate one — and a method
  that works better for larger fields is pointed away from the question.** *Evaluate at `F = ℚ` first.*
- **A fetch-only citation rule flattens textbook against open** — the cost is **a collapsed
  distinction**, not a missing citation. *Filed whether or not the remedy is ratified.*

## §5 — LENSES

**129 table rows, 0 blank cells** · **0 literal escapes** · **0 mojibake** · 7,604 words ·
48,698 bytes · **honest closings carried VERBATIM, verified before *and* after** ·
**`DRAFT-WAVE-BOUND` unchanged, workshop-side.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | this pass |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE, untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

> ### **The digestion walk is re-offered as the wave's gate — `v0.5` now, one document, contents and
> stakes as previously presented. Only the author can run it.**

**Nothing deposits. `h2` outside every arrow.**
