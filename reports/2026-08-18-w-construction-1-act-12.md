# `W-CONSTRUCTION-1` · ACT 12 — THE INEQUALITY
## ### **`(INEQ-no)` AT THE NAMED LINK (L1's MODEL TRANSPORT: THE FIXED-`c` LADDER IS NOT THE WINDOW-SCALED `S_{∞,λ}`; THE SHANNON DICTIONARY MISSING) — TWO LINKS SOUND AT PROOF GRADE (L2 PROVED THIS ACT; L3 THEOREM + NONNEG AUTOCORRELATION) — ### THE SIGN DICTIONARY FOUND, FIRST ORDER: `W_∞^CC = −A` COLLAPSES THE ACTS-8/9 RESIDUAL TO `≈ −0.5` — THE OUT-OF-RANGE PREDICTION HELD, NO STOP**

**Relay report · 2026-08-18 · `W-CONSTRUCTION-1` act 12 · sub-gate declared · Rules 4/5 · on the
face: an inequality stated and attacked at proof grade; nothing at complete roster; nothing
deposits; nothing circulates**

> ### **RULE-3 LOG:** *act 11 at `6629c0d` ✓; the banked Theorem-1 verbatim READ from the bank
> (sitting 3: `W_∞(f) ≥ Tr(ϑ(f)S)`; sitting 2's own reading: "the window's job is to control the
> ε-integral") ✓; `Π₊S ≤ S` (sitting 22) ✓; the act-9 lemma ✓; the banned grep: CLEAN. THREE
> Rule-3 flags FILED IN THE REGISTRATION before any number: F1 the sign dictionary (atlas `A` vs
> CC's `W_∞` — registered as a reading, TESTED); F2 the direction's convention-ladenness (the
> ferry's "uniformly negative D predicts INEQ-yes" reads differently per convention — surfaced,
> not smoothed); F3 the positive-form identification (derived: the raw mode terms ARE
> `‖ϑ(g)ξ_n‖²`; gate-checked `≥ 0` everywhere). ROUTING: the banked run ON THE OPUS LANE (14
> PASS; 3 ATLAS-RESID FLAGs = exactly the out-of-range cells). Terms coined: "the scale-covariant
> enlargement" (item 3's naming of the codomain absence at `∞`, per the ferry's order).*

```
registration    data/b40_registration_2026-08-18.txt (banked FIRST: F1-F3, the chain
                grades, the stop)
bank            data/b40_2026-08-18.txt (the Opus lane)
build document  THE_GLOBAL_SECTION.md v0.12 (§25)
vanilla leg     InequalityShadow.lean + AxiomCheckIS — 2/2 "does not depend on any
                axioms"; the relay's zero-axiom count: 90
mathlib leg     nested_projection_norm_le PROVED at {propext, Classical.choice,
                Quot.sound}; the 3-sorry ledger UNCHANGED, owners intact
```

---

## §1 — THE SIGN DICTIONARY (first order, registered-then-tested)

> ### **`W_∞^CC = −A`. Under it, the Thm-4.7 calibration residual collapses from `+4.05…+1.92` to
> a stable `≈ −0.5` plateau — `+0.068` at the window-edge cell. Acts 8–9's "calibration
> unresolved" verdict is RETROACTIVELY LOCATED: its bulk was the sign dictionary; the `≈ −0.5`
> remainder is truncation-scale (the 11-mode tail), the next-named piece. A re-reading of an
> address, not a correction of data — "unresolved" was right when said.**

## §2 — THE INEQUALITY: `(INEQ-no)`, THE LINK NAMED

**The chain at its grades:** ### **L2 PROVED this act** (`nested_projection_norm_le` — nested
projections dominate: the `E₁` positive form ≤ the full Sonin form; the classical profile per
terminal); **L3 proof grade at the model** (the act-9 theorem's ratio `< 1`, term-by-term against
`corr ≥ 0` — structural, the bump nonneg exactly; vanilla 2/2); ### **L1 BREAKS IN TRANSPORT: at
the window-edge cell `a² = 2`, `Tr_raw = 3.74 > W_∞^CC = 1.99` — and even the `E₁`-restricted
positive form (`2.12`) exceeds the bound. The address: the fixed-`c` prolate ladder is NOT CC's
window-scaled `S_{∞,λ}`; the effective-mode (Shannon) dictionary between window and mode count is
THE MISSING TRANSPORT — the archimedean obstruction, now in inequality form.** *The assembly gap
also named: the sound links give `object ≤ W_∞ + Σ_p W_p` (primes positive), not the dictated
minus-form; and the dictated direction fails numerically in both conventions (17 of 18 rows).*

## §3 — THE OUT-OF-RANGE CELLS: THE PREDICTION HELD

**`D_closed` at `a² = 16, 25, 36`: `−1.379, −1.272, −1.189` — NEGATIVE, the registered prediction
held; THE STOP NEVER FIRED.** *The atlas's own residual blows up at exactly these cells
(`+5.5…+6.0` — its zero-column certification ends at its far-end): the rows carry the ATLAS-RESID
FLAG; their `A`/`PR` are kernel quadratures unaffected, but the atlas cross-check is absent there,
said so. Staircase capped at `n = 3` per the ferry; the ε-truncation caveat rides the new rows.*

## §4 — THE MEANING, AT GRADE

> ### **The codomain absence at `∞` is now NAMED: the SCALE-COVARIANT ENLARGEMENT — the
> `S_{∞,λ}` family with its Shannon bookkeeping — the enlargement that makes the L1 transport,
> and only then possibly the inequality, available. The identity's retirement as target (act 11)
> STANDS; the inequality does not replace it until its transport exists. Nothing at complete
> roster; the protocol on the face.**

## §5 — THE RECORD

*`v0.12` (`§25`); rows 117–121; `F.2026-08-18v`; both legs' prints run; this report the bank.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. `(L-identity)` NOT CLAIMED; THE INEQUALITY NOT
CERTIFIED — `(INEQ-no)` AT ITS NAMED LINK, SAID SO. NOTHING PROMOTED. NOTHING DEPOSITS. NOTHING
CIRCULATES.**
