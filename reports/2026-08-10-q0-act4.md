# `W-ORD-Q0` ACT 4 — LOCATED, ACCESSIBLE, NOT MACHINE-READABLE — 2026-08-10

**Citation corrected and expectations registered at `bc22827` BEFORE the fetch.**
**Path discipline at head: `Q0` → `Q1` → `Q2` → `Q3`; no arrow supplies the sign.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — THE CITATION CORRECTED, BEFORE ANYTHING WAS READ

> ### **Deninger, *On the Γ-factors attached to motives*, Inventiones mathematicae **104** (1991) 245–262 — THE CONSTRUCTION.**
> **NOT** the *Motives* (Seattle) item act 3 named from Hesselholt's reference [15] — **that is the
> programmatic cousin, and it stays declared unread.** *Act 3 named the wrong paper for the role,
> from a downstream citation rather than from the construction itself.*

## §2 — THE ROUTE WORKS, AND IS RECORDED

`eudml.org/doc/143882` (200, title confirmed) → `GDZ resolveppn PPN=GDZPPN002108461` (200) →
**`PPN356556735_0104`** = Inventiones Mathematicae 1991, scan p. 251 → **full-volume PDF, 200,
`application/pdf`, 54,092,297 bytes, downloaded.**

## §3 — THE EXTRACTION'S HONEST COVERAGE: **NIL**

**`pdftotext` over the ENTIRE volume: 2,285 bytes — 1,405 non-whitespace characters — all of it GDZ
boilerplate.** Hits on `gamma`, `motive`, `Deninger`: **ZERO.**

> ### **GDZ serves image-only scans with no OCR layer. The coverage verdict is not "partial" — it is NIL for the article's mathematics.**

## §4 — E1 / E2 / E3: **NOT ANSWERED — unanswerable with this tooling, not refuted**

No statement quoted, no space named from source, no spectrum read. **The `Q0` delta's target
category REMAINS LORE, NOT QUOTED.**

> ### **THE REGISTER DOES NOT REACH "KNOWN ANSWER AT FULL CONTENT."** It reaches **`LOCATED ·
> ACCESSIBLE · NOT MACHINE-READABLE`** — better than act 3's state, **and not the state item 4
> anticipated.** *Said plainly rather than rounded up.*

## §5 — THE REGISTER AT ITS TRUE FORM

**Question · standard · typed delta with mechanism · the conversion's parts list · the chain
`Q0`→`Q3` with `Q3` = the clause — all held.** **Known answer: SHAPE + HYPOTHESES held; the
CONSTRUCTION located and unextractable.** **Declared-unread remainder: Inventiones 104 (image-only)
· the Seattle cousin · Bökstedt's numbers.**

> ### **READS: `EXTRACTION-COMPLETE AT THE MACHINE-READABLE SOURCES`.** Every source that could be
> read has been; **the one remaining is readable by a human and not by this tooling.** **Further
> motion is construction, a library visit, or rest — the author's ruling.**

## §6 — FOUND-BEYOND: A SYSTEMATIC INSTRUMENT ERROR OF MINE

> ### **EVERY FILE SIZE I REPORTED THIS SESSION FROM `ls -l … | awk '{print $5}'` WAS THE GROUP ID, NOT THE SIZE.**
>
> The owner name contains a space, so `ls -l` gains a column and **`$5` lands on the group — the
> constant `197121`** — printed as *"bytes"* for **Durov's thesis, the Borger PDF, the
> Clausen–Scholze notes, and this volume.** **True size here: `54,092,297` by `wc -c`.**
>
> **Why it survived: it always returned a plausible-looking number, and always the SAME one.**
> **A probe that fails loudly gets fixed; a probe returning a constant that looks like a size gets
> believed — and its constancy reads as stability rather than as the tell it is.**
> **Filed to plausible-neighbour drift AND the false-absence probe family.** *Correct idiom: `wc -c`.*
>
> **NOTHING SUBSTANTIVE DEPENDED ON IT** — no verdict in any act turned on a byte count — **but the
> figures are wrong wherever printed, and this is the correction of record.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `bc22827` (registration) → `71ee65f` |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE — untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing deposits. `h2` exactly where it was.**
