# THE SECOND OBJECT — b248, 2026-08-29

**Scope:** reads (quotation-only) plus one banked-data split. No face-off ran; no residual was
recomputed; no ruling executed. **PARALLEL-OK with b249** — b248 owns the ledger, PLACE-papers,
HANDOFF and mirror writes; b249's filings defer to this close. A finite-place-set object at a finite
cutoff decides NOTHING global (b14/b15). Nothing deposits.

---

## The arrangement: **(ADDITIVE-FORCED)**

Does *"ε-regularized archimedean E₁-trace"* mean the ε-corrections **subtract** from the raw trace,
or **add**?

### **Three arrangements by three owners, and all three put `E2` with a plus:**

- **§19's comparison:** `W_∞ − Σ_p W_p ≟ [Tr_∞(ϑ(g)S_Son) + ∫g ε] − Θ_quotient(g)`
- **`b36_act8.py:175`** — the only place in the corpus where the combination is executable code:
  `RIGHT = (Tr_full + E2 - Dneg) - Thq`, the parenthesis itself stating which terms belong to the
  trace side
- **File E's ruled binding (C2 as amended by b244's D1):** `value := Tr_full + E2 − Δ₋`

**`b38:182` does not dissent, because it is not an arrangement of `T`.** `A = Tr_full − E2N −
resid47` is `A`'s expression, and §20(b) reads that same line as *"the CC-4.7 reproduction error at
the bench: `Tr_full − [A + E2]`"* — in which `E2` is added to `A`, not subtracted from `Tr`.

### **And the word "regularized" names a different subtraction, which is unperformed.** §20(b): *"the
divergent-part subtraction is exactly the trace-class bookkeeping already owed to the ε-lemma"* — and
b241/b245/b247 located its bench size as `resid47`. ### **That is M-4, not `E2`.** `∫g ε` has its own
row in §19, separate from the trace's: **a named constituent, not the regularization.**

### The standing clause, obeyed at the hard end

The registration disclosed — **before any verdict was drafted** — that the subtractive reading would
have cut `L − R` by **2·E2 = 1.950128 to 3.358857**, i.e. **45%–50% of a shortfall running 4.07 to
6.66**, and it banked the executor's draft verdict in the same file. ### **The texts went the other
way.** An executor who reports a verdict *against* the direction of the named crime has done the easy
half; the hard half was banking the draft first, and it is banked.

### **No decision card was assembled** — it was conditional on (SUBTRACTIVE-FORCED). **An executor
does not manufacture a card for a ruling the texts did not ask for.**

---

## The split — the junction piece gets its own table for the first time

`−D_dict = (E2full + E2even) + (PR − Θ_q)`, from banked data only, with every bank's axes printed
before any number and **b38's row bank named and *not* used** (its `NMODE = 10` is the trap b245 fell
into). The mask certificate was **re-derived from the arrays, not quoted**.

| `a²` | terms | ARCHIMEDEAN | JUNCTION | junc % |
|--:|--:|--:|--:|--:|
| 2 | 1 | 2.681241965 | **0.000000000** | 0.00% |
| 3 | 1 | 2.427587794 | 0.106484000 | 4.20% |
| 4 | 3 | 2.208083644 | **0.087342000** | 3.81% |
| 8 | 5 | 1.781753737 | 0.244027000 | 12.05% |
| 9 | 6 | 1.723442056 | **0.135020000** | 7.27% |
| 12 | 6 | 1.595154295 | 0.195843000 | 10.93% |

### **The archimedean piece carries 88%–100%; the junction piece 0%–12%.**

**The registered prediction is half right, and is reported as half right.** **Limb 1** — vanishes at
`a² = 2` — **CONFIRMED**, and exactly: `PR` and `Θ_q` are identically zero there. **Limb 2** — grows
with the active primes — ### **REFUTED**, with two drops (at `a² = 4`, 0.106484 → 0.087342; at
`a² = 9`, 0.244027 → 0.135020). ### **The prediction treated a difference of two weighted sums as
though it were a count.** The second object's two natures do **not** separate as cleanly as hoped —
the archimedean piece is smooth and dominant, but the junction piece **mixes**, and the mixing is now
on the record with its own table.

---

## The consequence, in symbols only

Under **(ADDITIVE-FORCED)**: `L − R = resid47 + (E2full + E2even) + (PR − Θ_q)` — unchanged, and the
second object stands as **two named pieces with separate owners**. Under (SUBTRACTIVE-FORCED), which
the texts did not give, `L − R` would have lost `2·E2full`. ### **That line is written in symbols and
is not evaluated at any cell.**

**Filed:** the ledger's second-object row restated as two pieces with owners — **piece 1** (the
archimedean piece) sits on a **converged** series and is therefore not a tail and not M-4's;
**piece 2** (the junction piece) names **M-2 again**, since `Θ_q`'s aggregation into `Q.value`
remains unstated even under RULE Q. ### **Neither piece is M-4's — M-4 covers `resid47` and nothing
else** (b246, unrevised).

---

## Gates

**11 of 11 PASS, CLEAN** — on the second run. Run 1 failed the scope check because it matched
`left_side` **inside a comment**. ### **b242 was forced into that exact repair, and b243 and b246
carried it; this act wrote a fourth matcher without it.** *A repair that must be remembered at each
new matcher is a repair that will be forgotten at one of them, and it was.* Fixed by carrying the
`ast` comment-stripper forward, with the recurrence named in the helper's own docstring.

Gate 6 is a **positive control on an absence** — the card-assembly sentence is shown *findable* in
the registration, so its absence from the bank means something. Term scan **CLEAN**, 0 live over 707
lines.

**Shared artefacts (b248 owns them):** the PLACE-papers hook was **exercised and reported CLEAN, 0
foreign hits**; the loom append is **one hunk, 91 insertions, 0 deletions**, prefix verified
byte-for-byte; the mirror rebuilt and **verified CLEAN on all three clauses** (40 files, declared
HEAD `f5e878c` against `ls-remote`).

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b248
  run at    : 2026-08-29T12:27:52 (local)
  input     : 11 checks routed through the harness
  checks    : 11
  pass      : 11
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 50463240c5f43b448f71a2f7df00a998
=== END AUDIT SIDECAR ===
```

---

### **THE SECOND OBJECT IS TWO NAMED PIECES WITH SEPARATE OWNERS AND AN (ADDITIVE-FORCED)
ARRANGEMENT. NO RULING EXECUTED, NO CARD MANUFACTURED, NO RESIDUAL RECOMPUTED. M-2…M-5 OPEN.
`h2` STANDS EXACTLY WHERE THE DEPOSIT LEFT IT. NOTHING DEPOSITS. LOCKS LAST.**
