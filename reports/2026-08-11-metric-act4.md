# `W-ORD-METRIC` — ACT 4: THE THETA OF A NUMBER FIELD — 2026-08-11

**Registered at PLACE-papers `73311ff` BEFORE any fetch.**
**REVIEW GATE: `E3` was registered NO and the source confirms NO — nothing reached the gate.**
**Nothing deposits. Rail `de621b1` / `2147a03` unmoved.**

**van der Geer–Schoof, `arXiv:math/9802121`** — the ferry offered the ID tentatively; **the abs-page
verification was run, not skipped**, and it is correct.

---

## §1 — E1 CONFIRMED

> *"the number theoretical analogue of a divisor on a curve is an **Arakelov divisor**."*
> *"we attach to every Arakelov divisor `D` its **effectivity, a real number between 0 and 1** … a
> positive real number **`h⁰(D)`** … ### **It can be interpreted as the logarithm of a value of a
> theta function.**"*

## §2 — E2 CONFIRMED, AND THE MECHANISM IS THE FUNCTIONAL EQUATION

> ### *"**The Poisson summation formula implies a Riemann-Roch Theorem** involving the numbers
> `h⁰(D)` and `h⁰(κ−D)` … it is a special case of **Tate's Riemann-Roch formula**."*

**The registered expectation lands exactly: what plays Serre duality is Poisson summation — the
functional equation appearing AS duality. Filed at question grade against the FE-free-layer
holdings. No promotion.**

**And the authors' own confessed limit:**

> ### *"**Unfortunately, we do not have a definition of `h¹(D) := h⁰(κ−D)` for an Arakelov divisor
> without recourse to duality.**"*

## §3 — E3: NO POLARIZATION

*"polarization"* — ### **0 occurrences.** *"theta"* 13 · *"Riemann-Roch"* 21 · *"degree"* 28.

> **Registered NO; confirmed NO.** The theta is a **section-counting device** — an effectivity in
> `[0,1]` and an `h⁰` that is a theta value. **No ample class, no induced pairing, no Rosati-shaped
> structure.** *In their terms: they have `h⁰`, `h¹`, `deg` and a Riemann–Roch; they have no `End`,
> no involution, and nothing they call a polarization.*

## §4 — E4: HEIGHTS — PARTIAL

`deg(D)` is present and pervasive. **But it is LINEAR, and the well's height row wants what
Néron–Tate supplies: a quadratic form. PARTIAL.**

## §5 — THE WELL TABLE, RE-GRADED

| ingredient | idele class group | seat's tori | CC's monoid-Jacobian |
|:--|:--|:--|:--|
| group structure | **PASS** | ABSENT | PASS (finite) |
| **theta** | ### **LOCATED AT CITE** | not located | not located |
| endomorphisms | NOT LOCATED | NOT LOCATED | NOT LOCATED |
| **polarization** | ### **ABSENT — confirmed, 0 occurrences** | ABSENT | not stated |
| **heights** | ### **PARTIAL** — `deg` linear, not quadratic | ABSENT | VACUOUS |

**Compactness-as-product-formula appended and LABELLED OURS** — not stated by any source read.

> ### **The delta: the theta supplies an effectivity, an `h⁰`, an RR by Poisson, and a linear degree.
> The well requires a polarization inducing a pairing, and a quadratic height. NEITHER IS SUPPLIED.**
> *The theta is the closest object located, and it is a different kind of thing.*

## §6 — FOUND-BEYOND

1. > ### **The source confesses the same SHAPE of gap this corpus has.** *"…without recourse to
   > duality"* — **their dual object is defined by the duality rather than constructed.** Structurally
   > the corpus's own position: `C5`'s forbidden real structure, `Δ2`'s missing pairing, `h¹`'s
   > missing independent definition — **each a thing named by its relation rather than built.**
   > *Question grade. `KIN` by shape; no text connects them; no promotion.*
2. > **Act 3's `E1` shortfall is partly repaired, one level down.** The dictionary IS stated in the
   > literature — **for divisors**, not for Jacobians. *That locates exactly where the statement
   > exists and where it does not.*

## §7 — TWO LOOM LINES FILED

**A retrieval gloss is not the source** — the fetch appended a conclusion no author wrote; had it
been read as result, `E1` would have passed on a sentence nobody wrote. *Treat everything outside
quotation marks as the tool's opinion.*
**Used-not-stated** — a correspondence can be load-bearing and have no statement site. *Look one
level down, and record "used-not-stated" as its own grade — neither stated nor absent.*

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `73311ff` (registration) → this pass |
| relay | this report's commit |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**`Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow. Nothing reached the review gate.**
