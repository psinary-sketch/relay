# THE MODE SUM'S LIMIT — b252, 2026-08-29

**Scope:** a **bounded bench act** — the b249 instrument extended. **CONCURRENCY: SOLO.** No ruling
is made and no reading is chosen; this act produces the fact the M-2∞ card will cite. A finite cell
decides NOTHING global (b14/b15). Nothing deposits.

**Order of record:** meanings banked and hashed (`0c562286…77be`, 10898 bytes) → registration citing
that hash → the solve → the measurement → the verdict. **No computation had run when the branches,
the band and both seats' expectations were fixed.**

---

## The branch: **(DIVERGES/WANDERS)**, at every cell

`S_N` misses the registered 1%-of-`|S_N|` settling threshold by **7.5× to 11.3×** at all six cells.

### And the threshold-free evidence, which is the real one

| `a²` | `n·w(n)` @14 | @17 | @20 | `w(20)` | `S_20` | `A + E2` |
|--:|--:|--:|--:|--:|--:|--:|
| 2 | 1.8223 | 1.8549 | **1.8759** | 0.093795 | 5.6931 | −0.31110 |
| 3 | 1.1669 | 1.1833 | **1.1938** | 0.059692 | 4.2403 | −0.39226 |
| 4 | 0.9285 | 0.9407 | **0.9486** | 0.047432 | 3.6334 | −0.41245 |
| 8 | 0.6219 | 0.6298 | **0.6350** | 0.031749 | 2.7568 | −0.42923 |
| 9 | 0.5889 | 0.5963 | **0.6013** | 0.030066 | 2.6538 | −0.43003 |
| 12 | 0.5213 | 0.5279 | **0.5325** | 0.026624 | 2.4361 | −0.43033 |

### **`n·w(n)` rises and flattens toward a nonzero constant at every cell — that is `w(n) ~ C/n`,
whose sum diverges logarithmically.** The log form checks against `S_20 − S_10` to about 6%.

**I say threshold-free because my own gate 8 fixture made the alternative visible:** the last-third
change is 7.5%–11.3% of `|S_20|`, so **a 12% threshold would have called every cell settled.** The
threshold was fixed in the hashed meanings file before any number existed — **and it is still not
the act's principal evidence.** `n·w(n)` rising to a nonzero constant is incompatible with any
convergent sum at any tolerance.

### **And the limit in the same breath: a measured decay law over `n = 0…20` is not a theorem.**
b242's rule governs — **a measured rate is not a tail bound.** The divergence is the reading the
measured form implies; **it is not proved here and is not banked as proved.**

---

## The one exact fact, derived from source before the instrument was built

At `u = 0` the overlap is `½‖ξ_n‖²`, and pin P2's half-line norm makes that exactly 1:

> ### **`A_n(0) = 1`, exactly, for every `n`.**

**So the integrand of `tr[n]` does not decay in `n` at all at `u = 0`, and `tr[n]` is not governed by
`μ_n`, which does not appear in it.** Any expectation that `tr[n]` inherits the eigenvalue's
factorial decay (b250's S2) is a **double-name**, and it was refused in advance exactly as b251
refused the envelope. **The instrument reproduces the fact to 3.0e−13.**

---

## The finding registered in advance as an expected failure — and it happened

| `n` | b38 float64 | clean vectors | ratio |
|--:|--:|--:|--:|
| 5 | 0.288754049 | 0.288754050 | 1.0× |
| 6 | 0.256925975 | 0.256925192 | 1.0× |
| **7** | 0.051277815 | 0.230314245 | **4.5×** |
| **8** | 0.003358201 | 0.207943388 | **61.9×** |
| **9** | 0.013136604 | 0.189255165 | **14.4×** |
| **10** | 0.012775050 | 0.173614900 | **13.6×** |

### **b38's float64 eigenvectors for `n ≥ 7` are noise.** Its `tr[n]` collapse by up to 62× and
wander non-monotonically while the clean values decay smoothly — b242's `n_last = 6` seen from the
other side.

**G-REPRO was split in two so this could not be mistaken for an arithmetic error.** **G-REPRO-A** —
this act's instrument in *b38-emulation mode* against b38's float64, `n ≤ 6` — passes at
**3.076e−15, machine precision.** **G-REPRO-B** is the scheme difference (`4.3e−04`), and it too was
named from source before it was seen: `trace_modes` runs Gauss–Legendre over the full `[−1,1]` while
the integrand is zeroed outside `|x| ≤ 1/λ` — **a quadrature across a kink** — and interpolates
linearly. **A scheme difference, reported as one — not an error in either party, and not a
correction this act is entitled to impose on b38's record.**

### The consequence for b251 — filed as a fact, not as a re-verdict

b251's `TrTail(7)` of `0.0805` at `a² = 2` was built from noise. **The clean value over the same
modes is `0.801` — ten times larger — and over `n = 7…20` it is `2.024`.** ### **b251's branch is
NOT re-verdicted: a banked branch is not re-verdicted because a later act explains it (b246's rule).
The fact is filed; the branch stands as banked.**

### **And `Δ_2real := Tr_∞ − A − E2` has no limit to be.** b251 computed a **partial sum at `N = 10`**
and the name presumed a limit the measurement does not find. **Any future act quoting `Δ_2real` must
quote its `N` with it.**

---

## The instrument's gates

- **G-EXACT** — `A_n(0) = 1`: max deviation **3.015e−13** over `n = 0…20`.
- **G-EQ** — max eigenfunction-equation residual over all 21 modes: **4.405e−120.** **The vectors are
  genuine eigenvectors, which is the whole difference between this instrument and the float64 one.**
- **Reach** — `N = 20`, prolate index 40, `μ₂₀ = 7.162e−80`. **The registered target was met and the
  affordability clause did not need to be invoked.**
- **G-SELF** — dps 120/NQ 120 against dps 60/NQ 100: agree to **8.5e−16 up to `n = 15`**, parting at
  `n = 16`. **That is the same veil one level up** — at dps 60 the floor is ~1e−60 and `μ₁₆` is at it.
  ### **So G-SELF covers `n ≤ 15` and not the whole range, and the act says so rather than letting
  the gate's name imply more than it checked.**
- **G-INDEP** — the quadrature column from `left_side` and `e2_of_grid`, **their own owners, imported
  as a module and not re-implemented.**

**The two axes are not comparable, as a table:** the quadrature axis (`NU` 401→801) moves `S_20` by
`3.1e−4` to `1.1e−3`; **the mode axis moves it by about 2.0 between `N = 6` and `N = 20` and is still
moving — three orders larger.** b242's finding on a different object.

---

## Gates — 15 of 15 CLEAN on the first run

### **Every check is a pure conjunction.** b251's gate 4 read `(A and …) or E`, and `and` binds
tighter than `or`, so a true `E` carried the whole thing — the third appearance of the
decorative-gate species. **This file contains no `or` in any check.** **And every numpy-valued
predicate is `bool()`-wrapped**, b242's species having REFUSED three of b251's gates on its first run.

**The tautology control** runs the registered settling test on **arbitrary sequences** and requires it
to separate them — harmonic NOT settled, geometric settled, constant NOT settled. **A settling test
that passed every sequence would be measuring its own tolerance, and the branch would be an artefact
of the threshold rather than a fact about the object.** It is also re-implemented in the checks from
the meanings file's words rather than imported, **so that a gate and the thing it gates are not the
same code.**

Term scan **CLEAN, 0 live over 1625 lines.** **PLACE-papers was NOT touched, so the hook was not
exercised and the mirror was not rebuilt — reported either way.**

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : check_harness
  act       : b252
  run at    : 2026-08-29T13:59:38 (local)
  input     : 15 checks routed through the harness
  checks    : 15
  pass      : 15
  fail      : 0
  error     : 0
  refused   : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 dece10a0acadfbce474eee5e8aff9f80
=== END AUDIT SIDECAR ===
```

---

## Filings

- **The M-2∞ dossier is appended, prefix byte-for-byte intact, and still NOT decided (b237).** Its
  section (5) had asked, in b251's own words, *"If the mode sum converges to something, **WHAT?**"*
  — **b252 answers that item with a measurement, not a derivation, and the appendix says which.**
  The bearing on the three readings is stated **as bearing**: **(R-II) takes it hardest** — an object
  defined as the limit of a sum that does not settle is not defined by that limit — but that is
  bearing, not refutation, since a different summation could still denote and b252 did not test one.
  **(R-I) is consistent with it; (R-III) is untouched. b252 expresses no preference.**
- **The card is citation-shaped**, per the pre-banked MEANS. The ruling requested is unchanged from
  b251 — **what has changed is that it now cites a table.**
- **Index:** `mode-sum-limit` keyed on filing; queried **NO KEY** before any computation.
- **The ledger:** the shortfall's three named pieces stand as b251 banked them, with the first
  piece's name now qualified by its `N`.
- **The thirty-seventh seam's debt restated:** term 2's formalization stands, **unpaid and
  untouched.**
- **In flight:** M-2…M-5 open, **none closed**. `W-ORD-XI-PERMODE`, `W-ORD-ORDINATE-CACHE`,
  `W-ORD-STAGING-GUARD`, `W-ORD-FILE-E-WORKING-COPY-STALE`, `W-ORD-TE-SPEC` open.
- **HANDOFF** brought current by demotion, prior content kept, read-back identical.

### The fork at this stop

1. **The M-2∞ ruling**, which now cites a table rather than taste.
2. **M-2's finite-place address, M-3, M-5** — and the junction piece still waits naked in its own
   column.
3. **The patent session**, which slots here on your word and needs nothing from this act.

---

### **THE MODE SUM'S LIMIT IS A FACT WITH A TABLE: IT DOES NOT SETTLE, AND ITS TERMS GO LIKE `C/n`.
b38's MODES PAST SIX WERE NOISE AND THE RECORD NOW SAYS SO. NO RULING WAS MADE AND NO READING CHOSEN.
M-2…M-5 OPEN. `h2` STANDS EXACTLY WHERE THE DEPOSIT LEFT IT. NOTHING DEPOSITS. LOCKS LAST.**
