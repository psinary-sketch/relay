# `W-ATTEMPT-2` · SITTING 16 — THE BENCH LINEAGE INTERRELATED
## ### **ONE AXIS FOR THE PUNCTUATIONS AND THE SAWTOOTH — THE ATLAS'S ARCH-INCUMBENT SHAPE MATCHED AT SHAPE — THE `2T²` IDENTIFICATION REFUSED BY THE CORPUS'S OWN `T9` — AND THE ARRANGEMENT `Z = P + A − PR` FOUND ON THE OBJECT WITH `P` TURNED FROM TERM TO CONSTRAINT AND `Z` ABSENT: THE MISSING LEFT SIDE IS THE MISSING CODOMAIN IN LEDGER FORM**

**Relay report · 2026-08-18 (thirty-sixth sitting) · author-ruled · Fable throughout · every match at
grade, every non-match named · nothing deposits**

> ### **RULE-3 LOG:** *the atlas rows and the `−2.000127` mechanism at `SIGN_ARRANGEMENT_RECONCILIATION`
> ✓ · the two-branch (`δ/L`) law at `THE_ATTEMPT_RECORD`'s restated form ✓ · the finite-range Li row at
> `§12.1` (`n ≤ ⌊2T²⌋`, three named premises) ✓ · `T8`/`T9` at the sealed table ✓. The reach theorem
> rides every bench row below: nothing here is evidence about the Hypothesis. Terms coined: none.*

---

## §1 — ### **ONE AXIS: `a²`, THE ARRIVAL SCHEDULE `T1` DERIVED — EVERY PUNCTUATION EVENT AND EVERY SAWTOOTH TOOTH AT A PRIME-POWER ADDRESS**

| `a²` | arrival (`T1`: `p^k` enters at `a = √(p^k)`) | the object's measured events | the lag-form's (`δ/L`) events *(axis identified at content: the correlation reach `[1/a², a²]` ↔ the window length — a reading, not a merge)* |
|--:|:--|:--|:--|
| `< 2` | none — ### **the largest prime-power-free window = CC's `(1/2, 2)`, the `T1` row's own derivation** | the prime-free cells: class dim 2, positive (beside CC Thm 1) | — |
| 2 | `2¹` | ### **the constrained class DIES** (no fixed sector at `M = [i]`); weight still 0 | the first lag address enters |
| 3 | `3¹` | ### **the class REVIVES (conjugate sectors); the FIRST negative directions (flip channel, edge)** | second lag address |
| 4 | `2²` | ### **the first finite weight factor comes ALIVE (`Q(2,2) = √2`)**; product still 0 (`Q(3,1) = 0`) | ### **the sawtooth's FIRST APEX at `L = 4` (`r = 2`)** |
| 5 | `5¹` | the flat dead arrival (×16, imbalance-free) | — |
| 8 | `2³` | deepening; off-class counts step | trough at `L = 8` (`r = 3`) |
| 9 | `3²` | ### **the full weight comes alive: `6√6` exact (three-place)** | — |

> ### **THE VERDICT: MATCH-AT-ADDRESS, MECHANISMS DISTINCT, NO MERGE.** *Every event of the built
> object AND every tooth of the two-branch law sits at a prime-power arrival `a² = p^k` — the address
> arithmetic is ONE fact (`T1`'s derived boundary). The mechanisms are two: the lag-form's is a
> matching number; the object's is the dead-edge factor. The apex-at-`p²` and the first-live-weight
> coincide at `a² = 4` because both are "the second power's arrival" — same address, two laws.*

## §2 — THE ATLAS AND THE ARCH TURNING POINT

1. ### **MATCH-AT-SHAPE, GRADES APART: the atlas's arch share redistributing `1.0000 → 0.6581` as the
   prime share climbs `0 → 0.3419` (141 points, `corr = 0.8654`) is the SAME SHAPE as the object's
   ledger — the archimedean channel the incumbent, the primes buying in at their arrival addresses.**
   *One is BENCH on the classical functional; the other is MODEL on the built section; the reach
   theorem rides both; laid side by side, never fused.*
2. ### **THE `−2.000127` TURNING POINT: NON-MATCH, NAMED WITH ITS REASON.** *The atlas's arch mechanism
   is `A ≈ −P` (the pole count 2, the reconciliation's own resolution). The object HAS NO SUCH TERM
   BECAUSE IT ATE THE POLE: the vanishing conditions are vacuous-by-construction (sitting 7 — the
   Sonin condition IS orthogonality to the pole directions), so on the object `P` is a CONSTRAINT, not
   a term. The two live on opposite sides of the pole-handling choice; no analogue should exist, and
   none does.*

## §3 — THE FINITE-RANGE LI CERTIFICATE'S `2T²`

> ### **IS "CERTIFIED TO `n ≤ 2T²`" A DIAGONAL CELL? — NO, AND THE REFUSAL IS THE CORPUS'S OWN `T9`:**
> *Li-index truncation DOES NOT LOCALIZE SUPPORT (the 99%-mass width saturates near `15.71` and stops
> responding to `n`, while support truncation controls width exactly) — DISTINCT kinds of restriction,
> measured-at-bank. The diagonal is a SUPPORT object (`p^k ≤ a²`); the Li range is an INDEX range. The
> quadratic-reach resemblance (`2T²` vs `a²`) is recorded as shape-only and the identification is
> REFUSED by the corpus's own row.* **Three positivity-shaped facts at three grades (`§12.1`'s own
> line) stay three: finite-range Li (`INTERFACES`) · the model's constrained-class positivity (`MODEL`,
> now exact) · the atlas hover (`BENCH`). No merge.**

## §4 — ### **THE ARRANGEMENT `Z = P + A − PR` ON THE OBJECT: THE RHS REALIZED, `P` A CONSTRAINT, `Z` ABSENT**

> *The reconciliation's identity: `Σ_ρ h = W_pole + W_∞ − Σ_p (…)` — `Z = P + A − PR`. On the built
> object:* ### **`P` IS PRESENT AS A CONSTRAINT** *(the constrained class's own definition — the pole
> directions excluded by construction, the classical `ĝ(±i/2) = 0` move built into the space)* **·**
> ### **`PR` IS PRESENT AS THE PUNCTUATED PRODUCT — AND IT IS EXACTLY THE PRIME SUM'S DIAGONAL FORM:
> at bound `a` the live factors are precisely the `p^k ≤ a²` the correlation window reaches (`T1`)** **·**
> ### **`A` IS PRESENT AS THE MODEL FACTOR** *(structure landed; magnitude convention-pending)* **·**
> ### **AND `Z` IS ABSENT — THE OBJECT HAS NO LEFT-HAND SIDE. The missing `Z` channel IS the missing
> codomain (self-product / Frobenius lift / leaves) IN LEDGER FORM: what the explicit formula's
> crossing supplies (`S6′`'s information carried past the boundary) is exactly what the model does not
> reach. One absence, third vocabulary — the ledger's, after the roads' and the corpus's own.**
>
> *Verdict: MATCH-AT-STRUCTURE for the three right-hand channels (with the term-vs-constraint
> difference named); the left side's absence is the finding, not a failure of the match.*

## §5 — THE COMPILATION TARGETS *(item 4 — named, sized, NONE started)*

| target | content | home | expected profile | size |
|:--|:--|:--|:--|:--|
| `constrained_class_positive` | the sector-forcing (`λμν = 1 ∧ ν = 1 ⟹ λμ = 1`) + `C\|fix ≻ 0` from `(4,1,1) > 0`, at the `a = √3` cell | `DiagonalSection.lean` | no axioms (decide) | ~½ sitting |
| `off_class_negative_antiinvariant` | T-fixed ∧ antipode-anti ⟹ `B = −‖v‖²` (the flip-channel theorem, integer form) | `DiagonalSection.lean` | no axioms | ~½ sitting |
| `arrival_multiplicative` | flat factor (`d,d,d,d`, `tr M = 0`) ⟹ uniform sector multiplication (integer combinatorics; the `(5,1)` lemma) | `DiagonalSection.lean` or `SIDE-window` | no axioms | ~1 sitting |
| `staircase_arrival_schedule` | `n_p(a)` + the `T1` boundary (`p^k` enters at `√(p^k)`) as integer arithmetic; the one-axis event table's address law | `SIDE-window` companion | no axioms | ~½ sitting |
| `tower_level_stability` (smallest instance) | `ι(Son(2,1)) ⊂ Son(2,2)` + Gram stability over `ℚ(ζ₁₆)`-as-pairs | new module | no axioms if the pair-tower suffices; else the Mathlib companion | ~2 sittings |
| the cyclotomic Mathlib companion | standing (named since sitting 5) | Mathlib-dependent | std3 | open |

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. EVERY MATCH AT GRADE, EVERY NON-MATCH NAMED, NO
MERGE ANYWHERE. NOTHING DEPOSITS. NOTHING CIRCULATES.**
