# `W-ORD-H1` — CONSTRUCTION ACT 5 — 2026-08-10

**Registered at PLACE-papers `5eee2d5` BEFORE any fetch (E1–E3, glue-seat, table re-grade).**
**The definitional-pass loom line was RATIFIED by the author and filed to `VERIFICATION_LOOM.md`
in the same commit as the registration.**
**Disclaimed register governs. NO ordinate comparison in any branch. No positivity, no `C₂`, no sign.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — THE SOURCE

**Antieau–Mathew–Morrow–Nikolaus, *On the Beilinson fiber square*, `arXiv:2003.12541`.**
Located via the arXiv API on an author query; **the ID was taken from the API's own listing**, not
guessed. **The abstract was checked first and found insufficient — it names the BMS filtration on
`TC` but states none of the groups.** The full text was then fetched and read **at the statements**.

> **The PDF's text layer is letter-spaced** (`T h e o r e m G`). **It mangled; the decode is stated,
> not hidden.**

## §2 — WHAT THE SOURCE SAYS

> **Theorem G.** *"If `R` is a **quasisyntomic ring**, then `ℤ_p(i)(R) ∈ D^{≤i+1}(ℤ_p)`. If `R` is
> w-strictly local (e.g., strictly henselian local), then `ℤ_p(i)(R) ∈ D^{≤i}(ℤ_p)`. **Consequently,
> `Fil^{≥i} TC(R;ℤ_p)` is concentrated in homological degrees `≤ i − 1`** (and pro-étale locally
> `≤ i`)."*

> *"For `R ∈ QRSPerfd`, **`THH(R;ℤ_p)` is concentrated in even degrees, so the homotopy fixed point
> and Tate spectral sequences for `TC⁻(R;ℤ_p)` and `TP(R;ℤ_p)` degenerate** and `TP(R;ℤ_p)` is
> 2-periodic."*

> **Proposition 5.12.** *"`TP(R;ℤ_p)` … **is concentrated in even degrees and `p`-torsion free**."*

**`ℤ_p` IS quasisyntomic, so Theorem G applies to it directly** — `STATED-BY-A-SOURCE`.
**The move from the `TC` statement to `TC⁻`/`TP` is `DERIVED-AT-CONTENT`** (same graded pieces
`ℤ_p(i)`, different indexing) — **marked as derived, not sourced.**

## §3 — E1 IS **REFUTED**. CASE A HOLDS.

Act 4 reduced the whole nominee to one question: in a fixed total degree, do the torsion
contributions assemble to an **infinite product** (case B) or a **finite group** (case A)?

> The bound `ℤ_p(i) ∈ D^{≤i+1}` together with `ℤ_p(i) ∈ D^{≥0}` forces `0 ≤ 2i − n ≤ i+1` for a
> nonzero contribution in total degree `n` — **so `i` ranges over a FINITE set. Finitely many graded
> pieces per degree, each a finitely generated `ℤ_p`-module.**

> ### **`TC⁻_n(ℤ;ℤ_p)` and `TP_n(ℤ;ℤ_p)` are FINITELY GENERATED `ℤ_p`-modules in each degree. CASE A.**

> ### **E1 REGISTERED PROFINITE-TYPE, CASE B. THE SOURCE SAYS OTHERWISE. E1 IS REFUTED, AND THE REFUTATION IS THE ACT'S RESULT.**

**The mechanism was mis-registered too.** E1 named **`lim¹ ℤ/k = Ẑ/ℤ`** as the classical candidate.
**The source's mechanism is not a `lim¹` at all — it is evenness forcing degeneration.**
*The register predicted the wrong answer by the wrong route, and both halves are recorded.*

## §4 — E3 FIRES: THE NOMINEE GRADES `FAIL-WITH-SHAPE`

Case A ⇒ the torsion dies under the final `⊗ℚ`:

> ### **`H¹-cand₂ = (𝔸_f/ℚ)[u]`** (resp. `[u^{±1}]` for `TP`) — **act 3's answer tensored with a ladder. No new direction.**
>
> **`FAIL-WITH-SHAPE`, and the shape inherits: going equivariant does not by itself produce `H¹`.
> The equivariant refinement MULTIPLIES the plain answer by the ladder; it does not ADD one.**

*Registered in advance (E3) precisely so this branch would cost nothing to report. It cost nothing.*

## §5 — ITEM 3: THE GLUE-SEAT QUESTION'S **PREMISE FAILS**

The question was conditional: *if* finite-place content arises as a **limit phenomenon** of the
gluing, does the archimedean leg admit a limit-phenomenon seat of its own?

> ### **Case A means the antecedent is FALSE.** The finite-place content is not a limit phenomenon —
> it is `∏_p ℤ_p ⊗ ℚ = 𝔸_f`, an ordinary product-then-rationalize, **with no `lim¹` anywhere in it.**
>
> **The hoped-for symmetry never gets off the ground. ADJUNCTION REMAINS THE ARCHIMEDEAN LEG'S
> PERMANENT, DECLARED STATUS — unless some other mechanism seats it.**

**Routes still named, none claimed:** the `T`-Tate construction's own completion behaviour · CC's
`𝔸_∞` · Deninger's boundary. **Adjudication future.**

## §6 — ITEM 4: THE TABLE RE-GRADED

| item | act 4 | **act 5** |
|:--|:--|:--|
| **screen column 2** | overturned / undetermined | **RESOLVED: computed `FAIL` at BOTH levels.** The construction does not retain the torsion `ℚ` forgets, plain or equivariant. **The definitional pass is fully overturned by computation.** |
| **tower reading, middle level** | UNDETERMINED | **`FAIL`.** The middle carries `𝔸_f/ℚ` in every even weight, not the zeros. **The rhyme breaks exactly at the level that mattered.** |
| **dossier row 1** | MATCH / UNDET. / VOID | **`H⁰` MATCH · `H¹` FAIL · `H²` VOID-IF-ASSERTED** |
| screen column 4 | `FAIL-WITH-SHAPE` | **UNCHANGED — §5 hardens it** |
| screen columns 1, 3 | PASS / UNDETERMINED | **UNCHANGED** |
| dossier rows 2–7 | mixed | **UNCHANGED — all inherited at earned size** |

## §7 — FOUND-BEYOND-REGISTRATION *(two items)*

1. > ### **AN AUTHOR QUERY RETURNED A ROBOTICS PAPER, AND THE NAME-IDENTITY DISCIPLINE FIRED ON A SEARCH LISTING.**
   > `au:Antieau AND au:Nikolaus` returned *"Robotic Agentic Platform for Intelligent Electric
   > Vehicle Disassembly"* — **Lyle Antieau and Nikolaus Correll: two different people, one matched
   > on a FIRST name.** *Caught at the listing, not carried.* **Recorded because the discipline was
   > written for mathematical objects and it just caught a false positive in an author search — a
   > wider scope than it was built for.**

2. > **THE ACT'S RESULT IS THAT THE REGISTER WAS WRONG, AND THE REGISTRATION IS WHAT MADE THAT
   > LEGIBLE.** E1 predicted case B by a `lim¹` mechanism; the source gives case A by degeneration.
   > **Had the expectation not been written verbatim before the fetch, this would have been reported
   > as "the source says case A" rather than as "the register predicted the opposite and was
   > refuted."** *Recorded as the registration discipline paying out — which is the only way it ever
   > pays out.*

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `5eee2d5` (registration + ratified loom line) → this pass |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE, untouched |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing claimed beyond grade. `Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow.**

> ### **Realization is not placement.**
