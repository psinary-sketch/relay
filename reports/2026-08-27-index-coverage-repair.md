# b216 — THE INDEX COVERAGE REPAIR

**2026-08-27 · relay `reports/2026-08-27-index-coverage-repair.md`**
**Ferry part 1 of 1, receipt confirmed IN FULL. Bank: `data/b216_index_coverage_repair.txt`. Fixtures: `data/b216_fixtures.txt`.**

> *** ### **REPAIRED. 15 keys, 15 rows, three lanes — and the fixtures PASS in both polarities:
> 27 of 27 must-hit, 10 of 10 must-not-hit.** ### **The five misses the act was sent to close are
> hits, and each returns its row with its owning act's grade intact.** ***
>
> ### **An instrument repair. No grade is conferred by any key. Nothing deposits.**

## ONE CORRECTION, MADE BEFORE ANY KEY WAS WRITTEN

The ferry's Rule-3 line says *"the b208 query list read at content with its five NO KEY results."*
### **b208 has no index-query audit.** They run `b180`, `b184–b207`, then `b210–b215` — **b208 and
b209 have none.**

### **The act with exactly five misses is b211**, and its list is `alpha, beta, psi, wronskian,
alternation` — matching the ferry on both count and content (b210 has six, b212 seven, b207 none of
this kind). ### **I proceeded on b211's list and named the substitution rather than making it
silently.** *Recorded as the act's one divergence.*

## (P1) THE KEYS — THREE LANES, 15 KEYS, 15 ROWS

**The prolate-arc lane (9):** `alpha` · `beta` · `psi` · `wronskian-identity` · `alternation` ·
`the-ladder` · `odd-family` · `transform-convention` · `eigenfunction-scale`

**The term-2 lane (4):** `quotient-trace` · `file-d` · `class-richness` · `weil-ledger`

**The filing lane (2):** `parked-note` · `naming-ruling` — ### **neither is a result, and both rows
say so on their face.** The naming ruling's row carries *"PROVENANCE: THE CONVERSATION LAYER. NOT A
DERIVATION. NOT CITABLE AS EVIDENCE ABOUT THE SECTOR. STRIKEABLE."*

### **Every grade is the one its own act recorded**, and several rows carry the awkward parts rather
than the tidy ones: `alpha`'s row records that **my registered dissent on `|α_odd|` was wrong**;
`psi`'s records that the odd `D_τ` form is an **executor inference from a delegated variant, not a
quotation**; `class-richness`'s records that **its citation is UNREAD**; `file-d`'s records **both**
blockers, the aggregation and the toolchain.

**Key/row consistency checked mechanically: 41 keys, 41 rows, no key without a row and none without
a key** (26 pre-existing + 15).

## (P1) THE ALIASES DELIBERATELY *NOT* ADDED

### **The bare `prolate` is still not an alias**, as the ferry requires and b181 ruled — ambiguous
across three keys. **`positivity` likewise, unchanged.** And this act refuses four more:

| refused | because it is ambiguous across |
|:--|:--|
| `sign` / `the sign` | `alpha`, `alternation`, `the-ladder`, and b205's discrepancy |
| `convention` | `transform-convention`, `eigenfunction-scale` |
| `the trace` | `quotient-trace`, `weil-ledger`, the archimedean trace |

### **And `the identity` is NOT reused for the Wronskian identity** — it is already row 24's key, and
reusing it would make one query return two unrelated objects. ***A nearest string is how a miss
becomes a false hit, and that sentence is older than this act.***

## (P1) THE FIXTURES — BOTH POLARITIES, BEFORE SERVICE

| set | result |
|:--|:--|
| **must-hit** | ### **27 of 27** — the five b211 misses, the arc's twelve further objects, **and the eight pre-existing keys re-run** |
| **must-not-hit** | ### **10 of 10** — the two bare words b181 protected, the four this act adds, **and four genuinely-absent objects** |

### **The pre-existing keys were re-run because a repair that breaks what already worked is not a
repair, and the only way to know is to re-run them.** The known-absent fixtures (`term 4`,
`the fifth sector`, `riemann hypothesis proof`, `nonexistent object xyzzy`) test that the index
**still says NO KEY when it should**.

> ### **A repair tested only on what it adds is untested.** An index that hits everything is exactly
> as useless as one that hits nothing — b181 minted that rule when it added the first prolate lane.

## (P2) THE FIVE MISSES, RE-RUN

`alpha` · `beta` · `psi` · `wronskian` · `alternation` — ### **five of five, NO KEY → HIT.**

Spot-checked at content: `wronskian` returns `wronskian-identity` with grade *"DERIVED AT CONTENT in
seven steps, ON NAMED IMPORTS … **NOT A PROOF FROM NOTHING**"*; `class richness` returns its row with
*"**ITS CITATION EXACT STATEMENT IS UNREAD**"*. ### **The grades survived the move into the index,
which is the only thing that makes a pointer safe.**

## WHAT THIS DID NOT DO

No grade conferred, raised or lowered. Nothing promoted. No bank edited, no claim restated —
**the corpus is unchanged except for one tool file.**

*** ### **The reach line still governs and is unchanged:** *"ABSENCE FROM THE INDEX IS NOT ABSENCE
FROM THE RECORD … An entry is a POINTER, never a promotion; the grade is its own act's, and where
the index and the act disagree, THE ACT WINS."*
### **This repair makes the fold's objects RETRIEVABLE. It does not make them truer — and six acts
of misses did not make them falser.** b164's limit, quoted through b202, states exactly what was and
was not repaired: ### **keys close false hits; they do not close false misses.** ***

## THE AUDIT SIDECAR (emitted; copied from the sidecar file)

```
=== AUDIT SIDECAR (emitted; do not retype) ===
  tool      : banned_terms
  act       : b216
  run at    : 2026-08-27T15:05:46 (local)
  input     : whole file b216_index_coverage_repair.txt (created this act)
  input     : whole file b216_fixtures.txt (created this act)
  input     : added lines in D:/relay vs HEAD
  stems     : gap, blind
  files     : 5
  lines     : 555
  hits      : 0
  live uses : 0
  VERDICT   : CLEAN
  self-hash : sha256/32 74f20b6dfb6c784317beb893825574b0
=== END AUDIT SIDECAR ===
```

## PINS

| repo | pin | note |
|:--|:--|:--|
| relay | `ddb83d8` → the b216 pin-line commit | one tool file, two data files |
| PLACE-papers | `aee890a` — ### **UNMOVED** | *no filing; the mirror is not rebuilt* |
| SIDE-global-section | `356010f` — **UNMOVED** | no build |
| SIDE-kernel | `0256e9e` — **UNMOVED** | — |
| HELD | `6eada6a` — LOCAL-ONLY, untouched | — |

**DEVIATIONS:** none.
**DIVERGENCES:** one — the ferry names *"the b208 query list"* and **b208 has no index-query audit**;
the act proceeded on b211's list and named the substitution. ### **The six acts that named this
queue were b210–b215, and none of them was b208** — consistent with the ferry having meant the run
of misses rather than that particular act.
