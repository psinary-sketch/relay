# `W-ATTEMPT-2` · SITTINGS 23–24 — WHAT REACHES `E₁`, AND THE MATHLIB LINEAGE
## ### **THE FERRIED ONE-PROJECTION RECONCILIATION FAILS AND THE REGISTERED SUB-BRANCH LANDS: THE PROJECTED WEIL FAMILY SPLITS ITS MASS EVENLY BETWEEN THE SECTORS (≈½–½, N-STABLE), AND `E₁` IS REACHED BY THE TWICE-PROJECTED FAMILY — WHILE THE PROLATE LADDER NAMES `E₁` BY CC'S OWN BASIS (+ − + − EXACTLY) — AND THE LINEAGE ROWS LAND WITH THE READ-AT-CONTENT FACTS, TWO MERGED AND TWO CLOSED-UNMERGED**

**Relay report · 2026-08-18 (forty-third/fourth sittings) · author-called · Fable/registration and (by
the routing note below) Fable/bench · nothing deposits · nothing circulates**

> ### **RULE-3 LOG:** *sitting 22 at `708b280` ✓ · the PR cites were TITLE-VERIFIED only on the ferry's
> own line, and the bodies were READ AT CONTENT before any row landed — the read CHANGED the record:
> the two April lemmas are CLOSED WITHOUT MERGING (details in the lineage sheet). ROUTING NOTE,
> recorded not smoothed: the ferry routed the bench to Opus; the Opus lane failed twice on server
> overload (API 529) and the bench executed on Fable with the registration unchanged (the note is in
> the banked registration itself). Terms coined: none.*

```
instruments   tools/e16/b33_e1_reach.py (registration banked BEFORE the run) ·
              data/b33_registration_2026-08-18.txt · data/b33_2026-08-18.txt · 82/82 checks
lineage       PLACE-papers kernel-docs/MATHLIB_LINEAGE.md (every PR body read at content)
amendments    FACES_OF_H2_AT_FINITE_INSTANCE.md (the softening, at the author's correction) ·
              THE_H2_PROGRAMME_CHARTER.md (the dated register-vs-clause STANDING NOTE)
```

---

## §1 — SITTING 23: WHAT REACHES `E₁` *(N ∈ {511, 1023, 2047}; a ∈ {√2, 2, 3}; float declared)*

| branch | verdict |
|:--|:--|
| `(R-proj)` — one projection suffices (the ferried reading) | ### **FAILS: the Sonin-projected Weil family's `E₋₁` fraction does NOT die — it stabilizes at `≈ 0.48–0.50`, N-stable to `~0.01`, at every `a` and every degree. The family splits its mass EVENLY between the sectors.** |
| `(R-proj′)` — the registered sub-branch: two projections | ### **LANDS: the twice-projected family `Π₊SΨ_d` reaches into `E₁` with GROWING rank (9 → 21 at `a = 3`; no collapse) and the count of exact `E₁`-directions within 0.1 rad rises with degree (2–4 → 10–11). The reconciliation of `W-exact` with the sector structure costs exactly TWO projections: Sonin, then the `+1` sector.** |
| `(R-prolate)` — `E₁` named by CC's basis | ### **LANDS: the even soft-compression ladder alternates `+ − + − …` EXACTLY (top 12, every `(N, a)`), each mode concentrated `~0.95–0.97` in its matching sector — `E₁` is the EVERY-OTHER-EVEN prolate list (the Hermite shadow `(−i)ⁿ`: `n ≡ 0 (mod 4)`), CC's own basis naming the constrained sector.** *Odd control: `⟨v, Fv⟩` purely `±i` ✓ — the parity structure rides the whole prolate ladder.* |

> ### **THE ARCHIMEDEAN CONSTRAINED SECTOR NOW HAS BOTH A NAME AND A REACHING FAMILY:** *named — the
> every-other-even prolate modes; reached — by `Π₊SΨ_d` with growing dimension. And the even ½–½
> split of the projected Weil family is itself a clean structural fact, recorded plainly.*

## §2 — SITTING 24: THE LINEAGE, FILED WITH THE READ-AT-CONTENT FACTS

*`kernel-docs/MATHLIB_LINEAGE.md` — five rows, "kin never identical", the Schwarz/Schwartz distinction
on the row's face:* ### **#36881 (log-derivative residue at simple zeros) and #36865 (analytic order
one at a simple zero) — MERGED; the compiled kin of the simplicity arc and of the constrained sector's
NONDEGENERACY (radical zero as the pairing's simple-zero-shaped fact). #36324 (Schwarz reflection for
Mellin, real kernels — `Λ₀_conj` et al., the compiled kin of the involution's Hermitian half, the
object's `G† = G·Π`) and #36871 (punctured neighborhood) — CLOSED WITHOUT MERGING (April 10/11), the
reviewer citing AI-generated review responses and reviewer capacity.** *What the merged pair did NOT
anticipate is stated (no class resolution, no Sonin/tower route, no `E₁`); and the closures' stated
reason is filed as PART OF THE PRECEDENT for the A–C companion campaign: one small statement per PR,
the author's own hand in review responses.*

## §3 — THE AMENDMENTS AND THE RECORD

1. ### **THE KEYSTONE SOFTENED TO ITS TRUE FORM** *(at the author's correction): "a simplification of
   the map, not progress on the clause" → "progress on the clause's UNDERSTANDING, not on the
   register" — and* ### **THE CHARTER GAINS THE DATED STANDING NOTE: the register-vs-clause
   distinction** *(the closure protocol gates the register and public claims, not investigation; the
   register moves only by `§3`'s four steps, in order, if ever).*
2. **The mirror is rebuilt at PLACE-papers HEAD after this batch's commits (clearing the FINDINGS
   lag), with probes — the pins in the closing message.**
3. *Correspondence rows 62–64; the packet current.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING AT COMPLETE ROSTER. NOTHING DEPOSITS.
NOTHING CIRCULATES.**
