# THE RIGIDITY ACT — CLOSURE-SEQUENCE STEP FOUR: THE INDEX FACE RUN UNDER ITS ARMED CIRCULARITY GATE
## ### **ALL THREE PREDICTIONS LAND ON THEIR REGISTERED BRANCHES. THE ABSTRACT RIGIDITY LEMMA COMPILES AT THE ZERO-AXIOM BAR — THE DISCRETE-INVARIANT MECHANISM AT KERNEL GRADE. THE (I) DERIVATION UNDER THE GATE: DISCRETENESS DERIVED; WITHIN-SCHEME CONTINUITY AT CITE GRADE; AND THE CONNECTEDNESS OF THE ADMISSIBLE SCHEME SPACE IS THE NAMED RESIDUE — S1'S INVARIANCE CLAUSE REDUCES TO ONE TOPOLOGICAL STATEMENT ABOUT ONE SPACE. THE (I) VERDICT IS THREE-GRADED; (V) IS UNTOUCHED AND SAID SO; THE GATE'S RE-CHECK PASSES — THE WINDOW LINE'S VALUE APPEARS NOWHERE. ONE TAINT CAUGHT BY PRINT AND ROUTED. CORE 237/237.**

**Relay report · 2026-08-21 · ferry-executed (part 1 of 1, receipt confirmed, Rule 1) ·
Rules 3/5 and Rule 4 (the run-registration with the circularity gate's input
enumeration written BEFORE any work) banked first · every sentence behind the
firewall carries its grade · the tag and grade-label audits as named sections
below · nothing about `h2` claimed · nothing deposits · nothing circulates
beyond this report.**

> ### **RULE-3 LOG:** *(a) THE CIRCULARITY GATE satisfied by enumeration,
> quoted verbatim in §1, written in the registration before the act; the
> re-check at this report's close is §5 — PASS. The ferry's "gate-four line"
> read as the ledger's gate FOR step four (Gate 3 as filed at b79) — the
> divergence resolved in the ledger's favor and disclosed. (b) row 55's
> existence verdict read with its exact conditionality — the LIMIT-SENSE
> clause and the [UNDER H-COH-∞] tag travel wherever they apply: the window
> line's rigidity is filed as CONDITIONAL RIGIDITY, said so. (c) "admissible"
> fixed from the record as far as it goes (the declared-pin structure; the
> ε-machinery's closed-form dependencies, b61 at its lines) — the
> within-scheme fibers record-fixed; the cross-scheme topology NAVIGATOR-OPEN,
> never invented. (d) the three components ordered (2–3 consume 1) and say
> so. ONE TAINT CAUGHT BY PRINT AND ROUTED, disclosed: the draft class table
> used List.getD, whose instance carries propext at the pin; the print
> flagged the terminal at [propext]; rerouted to a plain match; the print now
> clean — the verification law (axiom prints, not grep) working as designed.
> Banned-term grep on this act's writing: clean. Terms coined: THE
> CONNECTEDNESS RESIDUE names the (i3) open (its statement in §3); nothing
> else.*

---

## §1 — THE GATE'S ENUMERATION AND THE PREDICTIONS, VERBATIM, WITH VERDICTS

**The input enumeration (from the registration, before any work):** *(i1) the
discreteness of the class set — F⁴ = 1, derived, row-anchored; (i2) continuity
of the class along admissible deformations — to be READ from the recorded
sources; (i3) connectedness (or its failure) of the admissible scheme space —
read or named open; the window line's VALUE is not an input anywhere.*

> **(P1)** *"The abstract rigidity lemma compiles at the zero-axiom bar in its
> combinatorial form: a function from a connected step-graph to a discrete
> value set that is constant on edges is constant on the graph — the
> discrete-invariant mechanism at kernel grade, with the fourth-root value set
> instantiated."* ### **LANDS.** `walk_constant` compiles with no axioms: an
> edge-constant function is constant along every walk (head-threaded
> `isWalk`/`lastOf` recursions; induction; no algebraic hypotheses on the
> value type — connectedness IS the hypothesis that a walk exists);
> `fourth_root_instantiation` specializes to the ℤ[i]-pair carrier;
> `fourth_root_discreteness` decides the four roots pairwise distinct.
> Nothing resisted at the lemma's level.

> **(P2)** *"(i2) derives at least partially from the recorded ground ...
> while (i3) — the connectedness of the admissible scheme space — becomes THE
> NAMED RESIDUE: S1's invariance clause reducing to one topological statement
> about one space, filed explicit."* ### **LANDS ON THE REGISTERED PATTERN.**
> The derivation and the residue are §3.

> **(P3)** *"The finite mirror's rigidity restates as the same mechanism's
> exact case ... so both sides of the mirror carry ONE rigidity mechanism at
> their own grades."* ### **LANDS.** The finite tower is the lemma's exact
> case — vertices the levels, edges the ι steps, values the fourth roots, f
> the banked per-level class [BANKED DATA, declared — the unit trace i at
> every banked place-2 level, rows 35–36]; the walk 0-1-2-3 and the bounded
> edge-constancy DECIDED (`tower_walk_instances`). One mechanism, two grades.

## §2 — THE ADMISSIBILITY READ (component 2), EVERY STEP GRADED

**Within-scheme deformations [derived-from-record at cite grade]:** the
sources present the recorded masses by a closed-form pattern in their declared
parameters (b61, at its lines — "the sources' own closed-form pattern, in the
functional's own units"); a closed-form family is continuous in its parameters
wherever defined, so the class map is continuous along the ε-direction WITHIN
a scheme — (i2)'s partial derivation, exactly the registered pattern.
**Cross-scheme deformations [NAVIGATOR-OPEN]:** no recorded structure connects
distinct regularization schemes; "admissible" is record-fixed at the
within-scheme fibers only.

## §3 — THE (I) VERDICT (component 2), THREE-GRADED, WITH THE RESIDUE EXPLICIT

**THE CONNECTEDNESS RESIDUE (W-TOP-ADMISSIBLE-CONNECTED), filed explicit:**
*the admissible scheme space — the space of source-licensed regularization
schemes, its within-scheme fibers as the record fixes them, its cross-scheme
structure pending the sources — is connected by class-continuous
deformations.* **S1's invariance clause = the lemma + (i1) + (i2) + this one
statement.**

**DERIVED:** the mechanism (kernel grade); (i1) — F⁴ = 1 from the recorded
F² = parity with the class set the four discrete roots (decided); (i2)'s
within-scheme half (cite grade); the finite mirror's rigidity (decided).
**CONDITIONAL:** the window line's rigidity is rigidity OF row 55's
conditional object — conditional rigidity, inheriting LIMIT-SENSE (the
analytic object) and [UNDER H-COH-∞] (the class reading), the tags traveling.
**OPEN (one sentence):** the connectedness residue above — S1's invariance
clause's entire remaining content.

**(V) is UNTOUCHED** — no sentence of this act selects, constrains, or
consumes the line's value; step five owns it.

## §4 — THE TAG AUDIT (named section)

Every coherent-class or analytic-limit sentence in this act's writing (the
registration's C2 frame, the bank's (I) verdict, row 56's CONDITIONAL cell,
the OPEN_TRAILS bullet, §3 above) carries its clause inline — LIMIT-SENSE for
the analytic object, [UNDER H-COH-∞] for the class reading; the kernel module
carries neither (arithmetic and the abstract lemma only — correct, stated in
its docstring). **No untagged descendant found. PASS.**

## §5 — THE GRADE-LABEL AUDIT AND THE GATE RE-CHECK (named sections)

**Grade labels at first appearance:** the lemma and instances — [DERIVED,
kernel grade]; F⁴ = 1 — [DERIVED from recorded F² = parity]; the four roots'
distinctness — [DERIVED, decided]; the closed-form continuity — 
[derived-from-record at CITE grade]; the banked class table — [BANKED DATA,
declared]; the cross-scheme structure — [NAVIGATOR-OPEN]; the residue — 
[OPEN, named]. **No unlabeled number or claim found. PASS.**

**The gate re-check (the enumeration against the act as run):** the act used
exactly (i1), (i2), (i3) — nothing else entered the derivation; **the window
line's VALUE appears nowhere** (the only class value anywhere in the act is
the finite mirror's banked table (0,1), which is the FINITE tower's data, not
the window line's; the (V) clause's content is absent from every input and
every conclusion). **RE-CHECK: PASS.** The checkpoint ledger's step-four gate
line is marked exercised.

## §6 — THE KERNEL, THE RANGE LAW, THE RECORD

`RigidityShadow` — 4 terminals (`walk_constant`, `fourth_root_instantiation`,
`fourth_root_discreteness`, `tower_walk_instances`), each *"does not depend on
any axioms"* — **Core 237/237**; row 56 filed, no blank cells.

**The range law, on the face:** the mechanism is general and compiled; every
archimedean-facing clause carries its grade or its tag; the residue is one
named open, not smoothed; (V) untouched; the junction stays a fence; nothing
about `h2` follows.

*Relay: the b80 run-registration + bank + the working-layer copy
(`tools/lean/RigidityShadow.lean`) + this report. SIDE-global-section:
`RigidityShadow` + row 56 + regenerated prints (237/237) + README.
PLACE-papers: the act record + the (I) verdict in the closure sequence + the
gate line marked exercised. Mirror: refreshed —
**`mirror-refresh-2026-08-21-ag.zip` is the one to load.***

> **PIN LINE (post-push read-back, ls-remote):**
> `SIDE-global-section origin/main = 6018f5784b058f642da8ab7b5c28c0aade476e91`
> (the act commit; tag `v0.1.0` = peeled `706a81b` unmoved) · `PLACE-papers
> origin/main = 9139e5f45ccedf530587814b5346ecb7bdbee9c8` (parent `48642ba`) ·
> `relay origin/main = ec7cf7e8db04247408cff2b37ccfe65e196b4ed1` (this act,
> parent `622c593`; pushed from `push-rigidity` per Rule 4.10, hook standing;
> HELD ancestry clean; the HELD commit's two carrier-build act files verified
> ABSENT from the public tree by name, count 0). **MIRROR:**
> `mirror-refresh-2026-08-21-ag.zip` built at PLACE-papers `9139e5f`. *This
> pin-line commit's own SHA is stated in the closing message, per the regress
> rule.*

## FOOT

**After this act the closure sequence reads its own state: (E) at row 55's
grades; (I) at this act's verdict — the mechanism derived, the conditional
rigidity tagged, the connectedness residue the one open; (V) next as step five
(the sign mechanism) with its own circularity gate to be written before its
act; then the mandatory I-14 pass before step six per the checkpoint ledger.
The TECHNE lane unchanged at the author's schedule; the desk holds its
one-line items; the restart kit stands complete; the author's mirror should be
at this act's export (`-ag`).**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED. NOTHING
DEPOSITS. NOTHING CIRCULATES.**
