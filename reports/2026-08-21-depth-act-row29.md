# THE DEPTH ACT, ROW 29 — THE ONLY-IF OF THE SILENCE THEOREM, WITH TWO DESK CLOSURES RIDING
## ### **ROW 29 CLOSES: THE ORBIT-CHARACTER WITNESS SCHEMA PROVES THE GENERAL-q ONLY-IF — EVERY OFF-BALL WEIGHT-ONE OPERATOR IS NON-SILENT ON E₁, WITH UNIT-VALUED DISCREPANCIES, THE WITNESS FAMILY SPANNING E₁ AT EVERY CELL (A FINDING BEYOND THE REGISTRATION, BANKED). WITH THE PROVED DIRECTION AND THE COUNT LAW, THE SILENCE THEOREM IS WHOLE AT GENERAL q: SILENT EXACTLY ON THE BALL. BOTH STRIKEABLES EXECUTED: THE P2-10 CITATION FLAG CLOSED BY THE AUTHOR'S RULING; THE FORMATION-CARD CONVERSION REPLICATED IN-KERNEL. CORE 169/169.**

**Relay report · 2026-08-21 · ferry-executed (part 1 of 1, receipt confirmed, Rule 1) ·
Rules 3/4/5 · the registration (containing the full longhand derivation) banked BEFORE
the instrument ran and before any Lean construction · nothing about `h2` claimed; the
range law governs · nothing deposits · nothing circulates beyond this report.**

> ### **RULE-3 LOG:** *(a) KLSilence read at its statements before extending (the proved
> direction, the count law, and the only-if's named-open header verbatim). (b) the b57
> chart machinery is the construction's ground (the single-row characters live on the
> same chart; the S-action collapse is the same geometric-sum species). (c) the banked
> b45/b48 certificates entered as instance evidence only; the schema was checked against
> (2,2) exactly before anything filed (P1). (d) the three components touched disjoint
> repositories (relay+SIDE-global-section / PLACE-papers / SIDE-kernel), pairwise
> independent. REGISTERED INTERPRETATION RECORDED IN THE REGISTRATION: P1's "matching
> the banked certificate exactly" operationalized as verdict-SET identity plus the
> first-witness value in the same ring, since the b45 certificate is basis-dependent and
> the schema uses a different exact basis. OPERATIONAL DISCLOSURES: one instrument-crash
> from a dead first-draft code block (removed before any measurement); one compile
> attempt from a wrong working directory missed the toolchain pin and elan fetched a
> non-pinned toolchain — the compile FAILED before producing anything, and the re-run at
> the pinned v4.29.1 (version printed in-run) produced every artifact. Banned-term grep
> on registration, bank, instrument, and this report: clean. Terms coined: none.*

---

## §1 — THE REGISTERED PREDICTIONS, VERBATIM, WITH VERDICTS BESIDE THEM

> **(P1)** *"the witness construction specializes at (2,2) to a discrepancy matching the
> banked b45 certificate exactly."* ### **LANDS AS OPERATIONALIZED** — the (2,2) verdict
> set matches the banked set exactly: the same 25 off-ball operators certified
> non-silent, the same 7 ball-touching operators verified silent (c = 0) on the family
> (the b48 law's pass set); the first-witness operator E₁₍₁,₁₎'s schema certificate is
> the ratio pair with exact values 8 + 2ζ + 2ζ⁷ + 2ζ⁹ + 2ζ¹⁵ (reducing to 8) against 4
> at equal norms 4q = 16, banked and kernel-decided.

> **(P2)** *"the general witness's discrepancy formula is nonzero … by an explicit
> nonvanishing argument (a unit or a stated congruence), not by inspection."* ###
> **LANDS** — every schema discrepancy is q·(root of unity), a root of unity, or a
> two-term sum whose ratio is kept off −1 (at odd q, −1 is not in the order-q character
> group; at even q the free parameter steps the exponent by an odd increment, so q/2 is
> dodged by explicit choice) — the unit-criterion lemma, exercised at both cells with
> ZERO uncertified operators.

## §2 — COMPONENT 1: ROW 29 CLOSES — THE SILENCE THEOREM IS WHOLE

**The construction** (the registration's longhand body, three lines of it): the
single-row character vectors g_{a,t} lie in the Sonin space; the transform acts
monomially on them (M g_{a,t} = ζ^{a(q−t)} g_{q−t,a}); the orbit map (a,t) ↦ (q−t, a)
has order four, and each free orbit carries exactly one E₁ line
u = (1 + M + M² + M³)g — pairwise orthogonal, norm² = 4q.

**The theorem:** for every q with d₁ ≥ 2, every off-ball weight-one operator is
NON-SILENT on E₁ — the registered witness schema (orthogonal pairs for off-diagonal
and frequency operators; equal-norm ratio mismatches for diagonals), discrepancies
unit-valued. q ≥ 5 by the general schema; q = 4 exhaustively this act (all 32
operators, exact in ℤ[ζ₁₆], the verdict set matching b45 operator-for-operator);
q ≤ 3 vacuous (d₁ ≤ 1, stated plainly). **With the proved silence direction and the
count law: a weight-one operator is Knill–Laflamme-silent on E₁ IFF its support
touches the ball — silent exactly on the ball, non-silent count (q−1)² + q², at
general q.**

**The finding beyond the registration, banked openly:** the witness family SPANS E₁ —
the free-orbit count equals d₁ at both parities (verified 2 = d₁ at (2,2), 4 = d₁ at
(5,1), exact E₁ membership for every member), the even-q center degenerating by
(1 + i + i² + i³)g = 0 — the b55 screen-artifact mechanism now DERIVED. The family is
an explicit orbit-character E₁ basis at general q, which makes the certificates
complete: a non-silent operator always certifies on a spanning orthogonal family of
equal norms.

**The kernel:** `SilenceOnlyIfShadow` — the abstract orbit-four-periodicity lemma at
named hypotheses; the degenerate-center vanishing decided; the (2,2) certificate
values decided nonzero in ℤ[ζ₁₆] 8-coordinates — 3 terminals, each *"does not depend
on any axioms"*; **Core 169/169**; row 37 filed, row 29's cell updated additively.
The general schema stays longhand (registration- and bank-resident) — the cyclotomic
ring at general q is beyond decide, stated plainly.

**The range law, on the face:** the theorem is a statement about the finite places'
E₁ sectors at exact levels; nothing about the level limit, the archimedean junction,
or `h2` follows.

## §3 — STRIKEABLE 2: THE P2-10 SUPERSESSION CLOSE — **EXECUTED**

The author's ruling (by this ferry's paste, the strikeable standing unstruck) is
filed: one dated additive line beside the counter-note in `STORMER.md` ruling the
2026-07-28 corrective note **SUPERSEDED** — the live-repository verification governs
(the terminal `SIDEOmegaB.omega_b_equals_4_over_81` at `Theorem.lean:130`, present
since the repository's only commit `9c80279`); the original citation was correct;
both earlier notes stand verbatim. REGISTRY's p2-10 row carries the closure note.
The desk item closes.

## §4 — STRIKEABLE 3: THE FORMATION-CARD CONVERSION, IN-KERNEL — **EXECUTED**

Per the author's route choice (by paste): `SIDE-kernel/Bridge/CardDerived.lean` —
the substrate assignment replicated in-kernel (C1 ↦ −1, C2 ↦ 2, C3 ↦ −2, C4 ↦ 3,
C5 ↦ −3, C6 ↦ 6, C7 ↦ −6; a provenance comment cites SIDE-trivium's
`Trivium/CardDerived.lean` @ `1aac3a9` as the pattern's origin; NO cross-repository
dependency), the value map proved injective as a decided computation on ℤ, and the
mechanism-class count DERIVED through the integer image {−1, 2, −2, 3, −3, 6, −6}
rather than read off the declaration; the original `seven_classes` stands (additive).
**Build green (2949 jobs); prints verbatim at the repository's stated bar:**
`substrate_value_injective` and `mechanism_class_card_derived` at {propext,
Classical.choice, Quot.sound}; `mechanism_class_card_value` at {propext, Quot.sound};
`AxiomCheck_CardDerived.lean` added per the repository's convention. The loom's
census series gains its third completed
conversion; **W-ORD-FORMATION-CARD-CONVERSION closes**; main moves by commit, tags
unmoved, the deposit line untouched.

## §5 — THE RECORD AND PINS

*Relay: the b58 registration + raw run + assembled bank (`data/b58_row29_onlyif.txt`)
+ instrument (`tools/e16/b58_row29_onlyif.py`) + working-layer copy
(`tools/lean/SilenceOnlyIfShadow.lean`) + this report. SIDE-global-section: the module
+ rows 37/29 + regenerated prints (169/169) + README. PLACE-papers: the p2-10 ruling
line + REGISTRY row + the loom's third-conversion entry + the OPEN_TRAILS work-order
close. SIDE-kernel: the CardDerived module. Mirror: refreshed at the new PLACE-papers
HEAD (PLACE-papers moves this act).*

> **PIN LINE (post-push read-back, ls-remote):**
> `SIDE-global-section origin/main = e0f0124eaab283f286cbb34d8e145fee61b28590` (tag
> `v0.1.0` = peeled `706a81b` unmoved) · `SIDE-kernel origin/main =
> 0256e9e1297fcf3bc6bd2a6b15d7c3d986a67164` (parent `5e668b4`; tag `v1.5` peeled =
> `0e5233f` unmoved by ls-remote; the deposit line untouched) · `PLACE-papers
> origin/main = 454197caad647eef17b42079f126a45a63ba5122` (parent `a565095`) ·
> `relay origin/main = [READ BACK AT PUSH]` (pushed
> from `push-depth-row29` per Rule 4.10, hook standing; HELD ancestry clean; the HELD
> commit's two carrier-build act files verified ABSENT from the public tree by name).
> **MIRROR:** `mirror-refresh-2026-08-21-g.zip` at the new PLACE-papers HEAD. *This
> pin-line commit's own SHA is stated in the closing message, per the regress rule.*

## FOOT

**The author's desk after this act holds the two voice items only: the keystone §1/§4
voice sentences (the consolidation report's §3) and the staged flatness sentence (the
row-31 report's §4), each awaiting his word. The depth line's continuation queues: the
remaining local-formalization units, the Θ_T identification lemma among them. The
navigator presents the state at the next seam; the updated handoff addendum is ready
on the author's word — pins have moved since the drafted handoff.**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NOTHING PROMOTED. NOTHING DEPOSITS.
NOTHING CIRCULATES.**
