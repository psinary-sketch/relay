# `W-ATTEMPT-2` · SITTING 7 — THE CONSTRAINED CLASS AND ITS INERTIA
## ### **`P+` AS REGISTERED, WITH THE MECHANISM EXACT: THE PAIRING IS POSITIVE-DEFINITE ON THE CONSTRAINED CLASS AT EVERY CELL WHERE THE CLASS LIVES — THE POSITIVITY RIDES THE EULER COEFFICIENTS `(4,1,1)` — AND THE CLASS IS ITSELF PUNCTUATED BY PLACE-ARRIVALS, WITH THE FIRST OFF-CLASS NEGATIVE DIRECTION SITTING EXACTLY AT A PLACE-ARRIVAL EDGE**

**Relay report · 2026-08-18 (twenty-seventh sitting) · author-ruled ("test our own answer") · attempt
work RELAY-ONLY · nothing deposits**

> ### **THE GUARD, CARRIED FROM SPEC `§6(c)` VERBATIM IN CONTENT: positivity of the LEDGER's
> `ψ`-component form is GRH for that `L(s,ψ)`. This sitting measured a FINITE CONSTRUCTED MODEL
> SECTION, never the ledger. Nothing below is evidence about any hypothesis, and nothing bears on the
> sign of `W_∞ − ΣW_𝔭`. THE PROTOCOL CORRECTION ON THE FERRY'S FACE, carried: the closure protocol
> gates the register and public claims, not investigation — the programme tests its own object.**

```
PLACE-papers  origin/main : f247d85 (UNTOUCHED)
relay         origin/main : (this report, in the sittings-7/8 batch)     local +1 HELD
instruments   tools/e16/b20_attempt2_s7.py (registration banked BEFORE the run:
              data/b20_registration_2026-08-18.txt) · data/b20_2026-08-18.txt · 52/52 EXACT
```

> ### **RULE-3 LOG (executor side), TWO FLAGS:** *(1) the ferry's head wrote the vanishing pair
> `ĝ(±i/2)`; its item 1 wrote `ĝ(i/2) = ĝ(0) = 0`. The BANKED pair (SIGN_ARRANGEMENT_RECONCILIATION:
> `P = ĥ(i/2) + ĥ(−i/2)`, the two poles `s = 0, 1`) is `±i/2`; `ĝ(0)` is the parity-ledger quantity —
> a different object. Executed with the banked pair, discrepancy reported. (2) "REVIEW-STAGED" appears
> NOWHERE in either repository — navigator-named vocabulary; the arc line is created in the relay
> record (the correspondence table's face) and the method register is untouched. Other cites: the
> exact certificate ✓ · the twist's signature ✓ · the pentagon's law ✓ · CC's Theorem 1 at cite.
> Terms coined: none; banned check clean.*

---

## §1 — THE CLASS, DEFINED AT CITE BEFORE ANY INERTIA WAS READ

*The involution as banked (spec `§6(b)`): the antipode-twisted reflection with the unitary root-number
element; on the model section the reflection is the CERTIFIED `M` (`M² = Π`, sitting 5) tensored with
the antipode, the unitary twist computed per `Π`-sector from the certified structure, both sign
choices run.* ### **AND THE TWO VANISHING CONDITIONS ARE VACUOUS BY CONSTRUCTION — the registered
reason, stated in advance: by Tate the pole directions are the ball indicator and its Fourier dual,
and the Sonin condition `f|_B = 0 ∧ f̂|_B = 0` IS orthogonality to both. Class-resolved refinement at
cite: the poles live only in the `ψ₀`-channel (`ζ_K`; the `L(s,ψ)` are entire).**

## §2 — THE MEASURED TABLES *(exact at all five finite factors — `(2,1), (3,1), (2,2), (2,3), (3,2)` — eigen-dims by the trace formulas, verified by direct nullity at the small factors)*

**Eigen-structure:** `(2,1)`: `(d₁,d₋₁,dᵢ,d₋ᵢ) = (0,0,1,0)` · `(3,1)`: `(1,1,1,1)` · `(2,2)`:
`(2,2,3,2)` · `(2,3)`: `(12,12,13,12)` · `(3,2)`: `(16,16,16,16)`. *A structural note observed: `p = 2`
factors carry `tr M = i`; `p = 3` factors carry `tr M = 0` (forced — `ℚ(ζ₈₁)` contains no `i`).*

| `a²` | cell | plain class dim | off-class negative dim | twisted (`ε = +1,+i`) |
|--:|:--|:--|:--|:--|
| 1.69 | (empty) | **2** — positive-definite, exact pivots `{2, 4}`; `C|fix` spectrum `{4, 1}` exact | 0 | 2 |
| 2, 2.25 | `{2:1}` | ### **0 — THE CLASS DIES at the arrival of 2** *(M = [i]: no fixed sector exists — registered sub-prediction, LANDED)* | 0 | 2 *(the twisted reading fills the dead cell, as registered)* |
| 3 | `{2:1,3:1}` | ### **2 — THE CLASS REVIVES through exactly the registered conjugate sector `(i, −i, +1)`** | ### **1 — the FIRST off-class negative direction, place-support `(2,1):i × (3,1):i`** | 4 |
| 4 | `{2:2,3:1}` | 18 | 9 | 36 |
| 9 | `{2:3,3:2}` | 1568 | 784 | 3136 |

## §3 — THE VERDICT: ### **`(P+)` — WITH THE MECHANISM VERIFIED ENTRY-EXACT, AND THE CERTIFICATE HONEST ABOUT ITS OWN ROUTE**

1. ### **NO NEGATIVE DIRECTION APPEARS ON THE CONSTRAINED CLASS AT ANY CELL.** *The mechanism, verified
   entry-exact at `{2:1,3:1}`: `B|_class = (the eigenbasis L²-Gram) ⊗ C|_fix` — the fixing forces the
   eigenvalue product to `+1`, and `C|_fix` has spectrum `{4, 1}`:* ### **the positivity ON THE BUILT
   OBJECT rides exactly the positivity of the Euler coefficients `(4,1,1)`.** *At the prime-free cells
   this sits BESIDE CC's Theorem 1 (single-place archimedean positivity on the constrained class) — at
   cite, never merged; the `∞`-factor's compressed transform shows the same `(n, n, n+1, n)` eigen-dim
   shape as the `p = 2` factors and min Rayleigh quotient `1.000000000000` on its fixed part (float,
   declared).*
2. ### **THE CLASS IS ITSELF PUNCTUATED BY PLACE-ARRIVALS** *— dead through `2 ≤ a² < 3` (the arrival
   of 2 admits no fixed sector), alive again at `a² = 3` through the conjugate pair — the constrained
   class's own sawtooth, the third appearance of the punctuation shape (weight, coupling, now the
   class).*
3. ### **THE FERRY'S QUESTION ANSWERED WITH AN ADDRESS: the first negative direction (off-class,
   antipode-anti-invariant channel, `B = −‖v‖²` verified entry-exact) appears at `a² = 3 — A
   PLACE-ARRIVAL EDGE, not a cell interior — with place-support `(2,1):i ⊗ (3,1):i`.**
4. **Certificate honesty, disclosed not smoothed:** *the registered congruence route wanted rational
   pivots at `{2:1,3:1}` and the pivots are real-cyclotomic irrationals; the "sign off the rational
   part" fallback was run and RECORDED AS WEAK EVIDENCE ONLY; positivity is instead certified exactly
   and embedding-independently by the factorization `B|_class = E† S E ⊗ C|_fix` with `S` and `C|_fix`
   rational SPD and `E` of full column rank. Both routes are printed in the run file.*

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO SIGN AT COMPLETE ROSTER. NOTHING DEPOSITS.**
