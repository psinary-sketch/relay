# `W-ATTEMPT-2` · SITTING 8 — THE LOCAL MODEL LIFTED ONE RUNG
## ### **THE SQUARE ON `ℚ_p` ITSELF: THE GENUINE TRANSFORM IS THE MODEL DFT ENTRY-EXACT, EVERY LAW LIFTS `VERBATIM`, BOTH TRANSPOSITION CAVEATS ARE MET — AND THE MODEL'S ONE ARTIFACT IS LOCATED EXACTLY: PUSHFORWARD WHERE THE GENUINE SCALING IS PULLBACK, ADJOINT NOT EQUAL**

**Relay report · 2026-08-18 (twenty-eighth sitting) · author-ruled ("test our own answer") · attempt
work RELAY-ONLY · nothing deposits**

> ### **THE CORRECTED STOP IN FORCE; the protocol correction carried (the closure protocol gates the
> register, not investigation). Everything below is a property of finite-level objects on `ℚ_p`; no
> sign; no complete roster; the register untouched.**

```
PLACE-papers  origin/main : f247d85 (UNTOUCHED)
relay         origin/main : (this report, in the sittings-7/8 batch)     local +1 HELD
instruments   tools/e16/b21_attempt2_s8.py (registration banked BEFORE the run:
              data/b21_registration_2026-08-18.txt) · data/b21_2026-08-18.txt · 215/215 EXACT
              (17 float lines, every one declared; exact cells (2,1),(2,2),(2,3),(3,1),(3,2))
```

> ### **RULE-3 LOG:** *the transposition table's two caveats read verbatim from the spec (`§8`-region,
> banked) ✓ · the six model laws ✓ · b8's declared edge artifact ✓. Terms coined: none; banned check
> clean.*

---

## §1 — THE LIFT: ### **LEVEL-`n` SCHWARTZ–BRUHAT `V_n ⊂ L²(ℚ_p)` IS CANONICALLY THE MODEL SPACE — AND THE GENUINE TRANSFORM DESCENDS TO EXACTLY THE MODEL DFT**

*The coset integral `∫_{x₀+pⁿℤ_p} ψ(xy)dx = ψ(x₀y)·p^{−n}·[|y| ≤ pⁿ]` computed refinement-stably and
checked on ALL `N²` chart pairs at every exact cell:* ### **`F_genuine = ζ_N^{mm′}/pⁿ` ENTRY-FOR-ENTRY
— THE MODEL TRANSFORM IS THE GENUINE LOCAL TRANSFORM AT LEVEL `n`, WITH NO ARTIFACT AT ALL.** *(Float
cell `(3,3)`: max deviation `7.8×10⁻¹⁸` over 531441 pairs.) And the ball-row factorization
`F[qγ, α+qβ] = ζ_q^{αγ}/pⁿ` — independent of `β` — is verified exact: the tensor square is a
DERIVATION on the genuine field, not an assumption.*

### **THE FIRST CAVEAT ("the `O`-invariant functions are not `L²`") DOES NOT ARISE AT FINITE LEVEL** *—
`V_n` is an honest finite-dimensional subspace of `L²(ℚ_p)`; what remains of the caveat is exactly the
`n → ∞` limit, stated plainly: `dim Son = (pⁿ−1)²` grows, no limit object is constructed and none is
claimed.*

## §2 — THE GENUINE SCALING: ### **THE ESCAPE IS WITNESSED, AND THE MODEL'S ARTIFACT IS LOCATED TO THE EXACT OPERATOR**

*`(Uf)(x) = p^{−1/2}f(px)` maps `V_n` into `V_{n+1}` and ESCAPES `V_n` — the escaping mass witnessed
exactly on host indices with `p ∤ m″`. The embedding `ι: V_n ↪ V_{n+1}` verified isometric, and* ###
**`F_host ∘ ι = ι ∘ F_n` EXACTLY on every Sonin column — the genuine transform commutes with
level-inclusion (the registered question's honest answer: `D = 0`, not a basis coincidence: `ι` is
function-inclusion and `F` preserves both levels).**

> ### **THE ONE STRUCTURAL FINDING: compressed to the level, the genuine `U` is the PULLBACK
> `Pᵀ/√p`, while b8's model `U` is the truncated PUSHFORWARD `P/√p` — ADJOINT, NOT EQUAL. That
> adjointness IS the wraparound artifact, now located precisely. Every transpose-symmetric quantity
> (Frobenius norm, trace) therefore lifts VERBATIM — which is exactly why `Q` and `t_k` survive — and
> a direction-sensitive law would not.** *b8's "edge effects declared at the outer shell" is upgraded
> from a declaration to a derivation.*

## §3 — THE LAW-BY-LAW VERDICT TABLE *(the registered map: `VERBATIM` / `CORRECTED` / `FAILS`)*

| law | lift verdict | the exact number |
|:--|:--|:--|
| dim `(pⁿ−1)²` | ### **VERBATIM** | canonical isomorphism, checked |
| the tensor square `Son = V₁⊗W₁` | ### **VERBATIM** | the ball-row factorization, exact on `ℚ_p` |
| `M² = Π` | ### **VERBATIM** | brute-forced ≤ dim 9; DERIVED exact at 49/64 (route disclosed in-run) |
| radical zero | ### **VERBATIM** | `det KᵀK = 2, 64, 2097152, 9, 43046721` — all nonzero |
| the twist `G† = G·Π` | ### **VERBATIM** | exact |
| the fifth law `Q² = p(p^{n−1}−1)²` | ### **VERBATIM — `Q_gen² = Q_model²` EXACTLY: `0, 2, 18, 0, 12` (and `192` float at `(3,3)`)** | *including the never-before-run `n = 3` cells* |
| fixed-point localization `t_k = 0` | ### **VERBATIM AND LEVEL-STABLE: `Tr(U^k S) = 0` EXACTLY, `k = 1, 2`, at hosts `n+k` and `n+k+1`** | *and re-derived on `ℚ_p`: a diagonal term needs `α(p^k−1) ≡ 0 mod pⁿ` with `1 ≤ α < pⁿ` — impossible, `gcd(p^k−1, p) = 1`* |

### **THE SECOND CAVEAT ("(iv) transposes as an orbital integral requiring regularization") IS MET:
the finite-level trace is LEVEL-STABLE, so the stable value `0` IS the regularized orbital integral at
bench grade — no residual to name.**

## CLOSING — RELAY-ONLY, FOR THE AUTHOR'S WORD

1. ### **EVERY ONE OF THE SIX LAWS SURVIVES THE LIFT `VERBATIM` — none corrected, none failed — and the
   verdict is exact, not float, at five of six cells.**
2. ### **THE MODEL'S ONLY ARTIFACT IS NOW A THEOREM-SHAPE, NOT A DECLARATION: pushforward vs pullback,
   adjoint on the level, invisible to every transpose-symmetric law.**
3. ### **BOTH CAVEATS OF THE TRANSPOSITION TABLE ARE RESOLVED BY COMPUTATION** *(the `L²` issue does not
   arise at finite level; the orbital integral regularizes by level-stability)* **with the `n → ∞`
   limit named as what remains.**

**`h2` UNCHANGED. THE REGISTER SENTENCE UNTOUCHED. NO SIGN AT COMPLETE ROSTER. NOTHING DEPOSITS.**
