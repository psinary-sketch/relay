# W-CONTROL-AUDIT — the era's relative verdicts re-sorted — 2026-08-05

Triggered by E-25's artifact finding. Keystone touch HELD throughout (stated in the ledger
before the work). Pins at open: PLACE-papers = `4909895`; relay = `130ebdf`; lv `14720d9`,
kernel `44895f9` — unmoved; rail at the post-rename baseline. Nothing deposits.

## §1 — THE HOLD

Filed in OPEN_TRAILS before any computation: the eight-deep keystone cargo does not land
until the census and the re-derivation return. It returned; see §6.

## §2 — THE CENSUS

| era verdict | class | exposure reason |
|:--|:--|:--|
| the identity balance (coeff = spec, ≤9×10⁻³⁹⁸) | **INTRINSIC** | per object against itself; the control is only a second instance |
| the K200 shape verdict (D = 0.6913 · small-gap 0.0513 · KS 0.0777) | **INTRINSIC** | ζ's own unfolded resolved gaps; the control only exhibits the rigid null |
| the K64 shape read (140× the density null) | **INTRINSIC statistic; the ratio compares two intrinsics** | the offset enters neither variance |
| the tracking constant 1.1157 | **INTRINSIC per object** | each object's diagonal against its OWN atoms |
| E-23's oscillation (56× above null) | **RELATIVE, low exposure** | built from α_k(ζ)/α_k(ctrl), but the statistic is the residual ABOUT THE MEAN — a smooth offset moves the mean, not the point-to-point oscillation |
| **the two-sides attribution (97% pair-energy)** | **RELATIVE, EXPOSED** | a difference against the control; entropy and repulsion absorb the offset differently |
| **the sum-rule separation (Z₉, Q₉)** | **RELATIVE, EXPOSED** | Σ log(a_k^ζ/a_k^ctrl) over control coefficients the offset shifts |
| **the whole c-sequence** (W-CONVLAW · the sixth point · the re-price) | **RELATIVE, EXPOSED** | E-25: ~80% of it is the offset |

## §3 — THE RE-DERIVATION AGAINST A POSITION-MATCHED CONTROL

The corrected control solves N(γ) = j + a with a = mean(N(γ_j) − j) = **−0.50009606** —
its counting function agrees with ζ's pointwise on average, not merely asymptotically.
(The measured a ≈ −½ is itself the diagnosis: the old control was off by half a zero — the
counting convention, exactly.) Built at J = 1,200, dps 2,700, factorized to K = 256, every
row re-derived through the identical pipeline.

**(a) The two-sides attribution — the registration's BOTH clauses fire.**

| K | control | Δcoeff | Δentropy | Δrepulsion | repulsion share |
|--:|:--|:--|:--|:--|:--|
| 12 | old | 29.94311 | 1.29824 | 28.64486 | 95.66% |
| 12 | **corrected** | **0.693452** | 0.062036 | 0.631416 | **91.05%** |
| 16 | old | 44.88019 | 1.46078 | 43.41941 | 96.75% |
| 16 | **corrected** | **0.661080** | 0.046758 | 0.614322 | **92.93%** |

**The magnitudes collapse by 45–68×** — the difference between ζ and the old control was
almost entirely the offset. **The share shifts materially, 97% → 92%** (the reportable
number), **and the qualitative claim SURVIVES: the arithmetic is pair-energy-dominated.**
Registered as filed on both halves.

**(b) The sum-rule separation — survives, at a thirtieth of its stated magnitude.**
Z₉: 1.6172 → **0.0507141**. Q₉: 0.770833 → **0.0951036**. Against the ~10⁻³/term control
noise floor the separation is still ~50× — **the arithmetic remains sum-rule-visible, but
the "10²–10³× above noise" figure was offset-inflated by 8–32× and is withdrawn in favour
of these numbers.**

**(c) The c-sequence — the rising sequence was the offset, entire.**

| K | R | c (old control) | c (corrected) | shift |
|--:|--:|:--|:--|:--|
| 16 | 8 | 0.578066 | −0.0768281 | −0.6549 |
| 32 | 17 | 0.729836 | −0.0377845 | −0.7676 |
| 64 | 36 | 0.825790 | −0.0276045 | −0.8534 |
| 128 | 74 | 0.882448 | −0.0380008 | −0.9204 |
| 200 | 118 | 0.930494 | −0.0347073 | −0.9652 |
| 256 | 153 | 0.949762 | −0.0369443 | −0.9867 |

**Corrected, the sequence is flat and slightly negative (≈ −0.035), not rising toward
unity** — and its sign and order match E-25's derived c_ideal(L) = (γ−1)/(log L + γ−1)
(−0.25 → −0.09 across this range), at ~35–40% of the derived magnitude, the same shortfall
ratio the unfolded test shows. **Everything the c-sequence was asked — the unit constant,
the convergence law, the sixth point, the seventh point's price — was a contest over the
extrapolation of an artifact.**

## §4 — THE THREE TESTS ON THE REPAIR

**(2) The residual test — registered CONFIRMED.** The coefficient closing the L = 1200
shortfall (3.833) predicts 1.173 at L = 50 against 0.223 measured — mispredicting by more
than the whole statistic (0.4228). The derivation's own (log L)²/L term does NOT account
for the shortfall; shortfall × log L is near-constant (0.87 · 1.07 · 1.08 · 1.05 · 1.14 ·
1.14) — **the decay is 1/log L**, pointing at item 3.

**(3) The height-vs-L test — registered CONFIRMED; the discriminator.** At matched L = 300:
low window (zeros 1–300, γ ≤ 542) = −0.245342; **high window (zeros 901–1200, γ ≈ 1303–1648)
= −0.282305** — closer to γ−1 (0.1405 vs 0.1774). **Same L, different height, different
answer: the shortfall is the low-height systematic, not an L-effect** — the same clause the
shape verdict carries, and consistent with 1/log γ decay.

**(4) The reference constants — the statistic becomes a one-number shape discriminant.**

| process | per-point unfolded pair energy |
|:--|:--|
| rigid integer lattice | **0** (exact) |
| GUE / sine kernel | **γ − 1 = −0.4227843** (derived) |
| Poisson (R₂ = 1) | **−log 2π = −1.837877** (derived the same way) |
| **ζ measured (L = 1200)** | **−0.2622117** — 62% of the way from lattice to GUE; 14% toward Poisson |

**HONESTY CLAUSE, ON THIS NOTE'S FACE:** the GUE value is derived FROM pair correlation, so
agreement is CONSISTENCY, never evidence — **this channel can falsify (a reading near −1.84
would refute the correlation picture) and cannot confirm.** The independent test remains the
K200 shape verdict (intrinsic to ζ's own gaps). Note the recognition: all three anchors are
pre-stated basis elements — 0, γ−1, −log 2π — and the log 2π that cancels in the GUE
derivation is exactly what survives in the Poisson one.

## §5 — π₀, RE-POSED

**Old definition: ILL-POSED.** The harmonic weight makes the ideal limit vanish like
(γ−1)/(log L + γ−1) — E-25's closed form — so the quantity had no limit to measure, and
what was measured was the control's construction offset.

**New definition (offset-invariant by construction):** in unfolded coordinates x_j = N(γ_j)
the position-matched control IS the integer lattice, so no control need be built and any
constant offset cancels:
  **Π(L) = (1/L) Σ_{i<j≤L} 2 log( |x_j − x_i| / (j−i) )**, **derived ideal γ − 1**,
grade **CONDITIONAL-ON-PAIR-CORRELATION**. Measured: −0.200 · −0.190 · −0.220 · −0.248 ·
−0.252 · −0.262 at L = 50…1200 — non-vanishing, right sign, drifting toward the derived
value, at 62% of it with the shortfall diagnosed as the height systematic (§4).

## §6 — FILINGS

1. **THE CONTROL-MATCHING LAW** (this era's earned clause): *density-matching is not
   position-matching. Any difference statistic taken through a nonlinear coordinate map must
   state which equivalence class its control matches — and if the map is nonlinear, a
   density-matched control leaks its counting-convention offset into every relative verdict.*
   The measured leak here: a = −½ zero, producing 45–68× inflation in the two-sides
   magnitudes, 8–32× in the sum rule, and a wholly spurious rising c-sequence.
2. **π₀'s ROW RE-POSED:** old definition **ILL-POSED** (E-25's closed form as the reason);
   new definition **= the unfolded per-point pair energy with derived ideal γ−1**, at
   CONDITIONAL-ON-PAIR-CORRELATION grade, with three reference constants and the
   falsify-not-confirm clause on its face.
3. **THE CIRCULAR-DECOMPOSITION SELF-CATCH credited in the loom:** the first decomposition
   defined its scale factor from the sum it then removed, yielding a zero residual by
   construction — *a void test that resembles a triumphant null is exactly what the
   verification law exists to catch.* Caught in-pass, discarded, replaced by the
   non-circular surrogate, and recorded.
4. **c_ideal(L) = (γ−1)/(log L + γ−1) FILED AS A REAL RESULT** at
   CONDITIONAL-ON-PAIR-CORRELATION grade — the era's one derived formula, and the instrument
   that exposed the artifact.

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `4909895` at open → this pass's commit |
| SIDE-lv-conservation | main = `14720d9` — unmoved |
| SIDE-kernel | `44895f9`; v1.7 — unmoved |
| relay | this report's commit; instruments `audit_*.py` |
| rail | `de621b1` / `2147a03` — at the post-rename baseline |

**The keystone cargo, corrected by this audit** (the two-sides share 97% → 92%; the sum-rule
figures withdrawn and replaced; the c-sequence rows void; π₀ re-posed; the shape verdict and
the identity balance UNCHANGED as intrinsic). The touch remains HELD for the author's call —
the audit's own condition is discharged. Mirror rebuilt with the standing check. Nothing
deposits.
