# The Corner Map — the (non-archimedean, unbounded-height) quadrant occupied

*W-QUADRANT, 2026-07-27. The honest banner: this corner IS RH by the compiled `union_leaves_nonarch_unbounded` — this is OCCUPATION (mapping partial coverage, grading routes in), NOT conquest. Classical results verified at source. Findings-before-interpretation. Nothing deposits.*

## Corner coordinates
The Weil-functional quadrant (non-archimedean place, unbounded height) is, in (σ, t) terms, the off-line strip **½ < σ < 1 at t → ∞** — whether any zeros sit off the critical line at arbitrarily large height.

## The map — held / mostly-held / genuinely-open

| Subregion | status | holder (verified at source) | grade |
|:--|:--|:--|:--|
| **The σ=1 EDGE STRIP** (σ ≥ 1 − C(log t)^{−2/3}(loglog t)^{−1/3}) | **HELD** (zero-free) at all heights — but the strip **width shrinks to 0** as t→∞ | Vinogradov–Korobov (Ford, arXiv 1910.08205) | classical theorem |
| **The mid-strip, count** (½ < σ < 1) | **MOSTLY-HELD** — off-line zeros are density-thin: N(σ,T) ≤ T^{30(1−σ)/13+o(1)} (first improvement near σ=¾ in 85 yrs; sup shifted ¾→7/10) | Guth–Maynard 2024 (arXiv 2405.20552) | bounds the COUNT, not absence |
| **The height-axis, on-line proportion** (σ=½) | **≥ 41%** on-line (Bui–Conrey–Young); recent work to ~47% | Levinson → Conrey → BCY (arXiv 1002.4127) | a positive FRACTION, not all |
| **The bounded-height FLOOR** | **HELD** — positivity certified to N₀(T)≈2T² | our `partialPositivity_finiteRange` | the standard three axioms {propext, Classical.choice, Quot.sound} |
| **Why the floor doesn't rise** | the detection threshold is rigid (2T², FE-forced; no mollifier shifts it) | our `detection_threshold_mono` (the barrier) | the standard three axioms {propext, Classical.choice, Quot.sound} |

## The genuinely-open core (the residue's sharpest geographic form yet)
**The mid-strip at unbounded height**: `½ < σ < 1`, **bounded away from both the critical line and the shrinking σ=1 zero-free edge**, at `t → ∞` — where zero-density proves the off-line count is *thin* but **not zero**, and neither the edge strip (Vinogradov–Korobov) nor the bounded-height floor (our certificate) reaches. This is the residue, geographically: *not "the global positivity," not even "the corner," but the density-thin mid-strip at unbounded height where an off-line zero is not excluded.*

## The resolve routes graded (CRITICAL_RESOLVE, run through current instruments)
- **Route A — the amplifier (positivity-shaped).** Amplifiers convert average→individual (the mollifier lineage that gives the ≥41% proportion). **Grade: coverage-improvement (raises the on-line proportion), does NOT cross the open core** — it meets the **average-vs-uniform wall** the family results hit (W-FAMILY: generic/average ≠ universal; the measure-zero escape is the ruler). A proportion, never the uniform.
- **Route B — density.** **Grade: coverage-map improvement, NOT corner-crossing.** Guth–Maynard is exactly this — it shrinks the mostly-held region's off-line count but **bounds the count, never proves absence**. Says so exactly.
- **Route C — trace formula on Γ₀(4) (Level-2).** Graded against the Selberg near-miss (W-SIGN-7: 3 of 4 clauses, `zeroActing` the stall). **Does Γ₀(4)/weight-½ change any clause's grade vs the compact-quotient?** In the **non-compact** Γ₀(4) setting the ζ-zeros DO appear — as **scattering resonances** (poles of the Eisenstein series / zeros of the scattering determinant, which is built from ζ) — unlike the compact case where the Laplacian spectrum is simply the wrong spectrum. **But the scattering determinant is DEFINED using ζ**, so the resonances = ζ-zeros *by construction* (input-side, the same "ζ put in" as W-PRIME-PHYSICAL's permitted-not-forced). **Verdict: NO clause grade changes** — `zeroActing` still fails (fitted, not forced) — **BUT the Γ₀(4) setting is confirmed as the right ALTITUDE**: the ζ-zeros are *present in the Level-2 picture* (as resonances), a "warmer" stall than the compact case. This is the one place the old route adds something — not a regrade, an altitude confirmation.

**Coverage-ledger correction (filed honestly):** CRITICAL_RESOLVE's Routes A/B/C were an ungraded proposal set that *should* have been in the original B1 census. Now graded (A/B coverage-improvement; C altitude-confirmation; none crosses the open core) — the coverage ledger tags CRITICAL_RESOLVE as now-graded. Consistent with CRITICAL_RESOLVE's own banner: "every approach encounters the SAME barrier … transfer to ζ unproven."

## The Γ₀(4) convergence (census-grade, held)
Three **independent strata**, without mutual citation, converge on the **Γ₀(4) / weight-½ / Level-2** setting:
1. **Resolve's Route C** (2025-era): the trace formula on Γ₀(4) (CRITICAL_RESOLVE §8.3; Γ₀-scattering).
2. **The compiled spinor calibration** (𝖶⁴=−Id, the level-4 congruence subgroup; SPINOR_CALIBRATION §V).
3. **CCM's prolate / metaplectic framework** (the scaling Hamiltonian / Weil representation; W-SONIN).
The multi-instrument standard applies: **three independent instruments land on the same altitude.** **Operationally: any future attempt-spec starts at Γ₀(4).**

*Warrant: Route C does not regrade any clause — no warrant fires. The corner is occupied, its open core named, the altitude confirmed; the wall is unmoved but its geography is now exact.*
