# `W-ORD-ARITH` — ACT 6: THE ARCHIMEDEAN POSITIVITY AT CONTENT — 2026-08-11

**Prior-register check run FIRST, as ordered. Navigator-supplied URL opened the thrice-blocked road.**
**No keystone lands — THE RAIL DID NOT MOVE. Nothing deposits. `h2` unchanged.**

---

## §1 — THE PRIOR-REGISTER CHECK — **AND IT INDICTS ACTS 3–5**

The corpus has held this result, **graded, since 2026-08-02**:

> *"Sonin/CCM (the archimedean-place Weil positivity via Sonin spaces — **the proved truncation**) …
> **PROVED at the archimedean truncation; OPEN beyond it** (the W-UNION quadrant: **(nonArch,
> unbounded) uncovered**)"* · *"**archimedean-place positivity (proven)** | the non-archimedean/
> unbounded quadrant"* · *"the (nonArchimedean, unbounded) quadrant — **the W-UNION residue exactly**"*

> ### **Acts 3, 4 and 5 spent three retrieval attempts (`429`, `429`, `403`) hunting a result the
> corpus had already sourced, graded and filed** — with its scope, its obstruction, and its screen
> verdict. **Act 4 called it "the single most load-bearing unread item on this face." It was not
> unread. It was unlooked-for.**
> ### **The ferry's "PRIOR-REGISTER CHECK FIRST" is vindicated completely — and was needed because
> the executor did not do it unprompted.**

## §2 — E1: THE THEOREM AT CONTENT

> ### **"Theorem 1. Let `g ∈ C_c^∞(R*₊)` have support in the interval `[2^{−1/2}, 2^{1/2}]` and
> Fourier transform vanishing at `i/2` and `0`. Then one has `W_∞(g* ⋆ g) ≥ Tr(ϑ(g) S ϑ(g)*)`."**

Their framing, **hedge carried verbatim and never upgraded**: *"provides an **operator theoretic
CONCEPTUAL REASON** for Weil's positivity, rooted in the compression of the scaling action `ϑ` of
`R*₊` in `L²(R)_ev` … on the **Sonin's space**."*

**Consequence they draw:** *"`W_∞(f) ≥ 0` for any smooth positive-definite `f` **with support in
`(1/2,2)`**, whose Fourier transform vanishes at `±i/2`."*

> ### `PROVEN-AT-CITE`. **Scope: the archimedean place; support in `(1/2,2)`; Fourier vanishing at
> `±i/2` and `0`.**

## §3 — E2: **`SAME-TERRITORY`** — the cross-validation files

> ### The field proves at the **archimedean** place. The corpus's `λ_A` **is** the archimedean,
> unconditional channel. **The field proves exactly where the ledger says the free layer is, and stops
> exactly where the ledger says the frontier begins.**
> ### **Two constructions of the same boundary from unrelated starting points.** `SAME-TERRITORY`,
> **not `SAME-OBJECT`** — no text identifies `λ_A` with `W_∞`.

## §4–5 — E3 AND E4

**Obstruction:** the `(nonArchimedean, unbounded)` quadrant — the W-UNION residue. Beside `N5`
(difficulty localized at the principal character): **KIN, different coordinates, no promotion.**

**Mechanism parts list:** Sonin's space · compression of the scaling action `ϑ` · `L²(R)_ev` (the
FE-even class) · the compression operator `S` · **the support cutoff `(1/2,2)`** · Fourier vanishing
at `±i/2`, `0` · **Toeplitz-type norm control** (explicit `J(N)`, `A(N)` eigenvalue bounds).

## §6 — THE THREE WINDOWS: **UNREACHED**

CC's is a **multiplicative support** window `(1/2,2)`; the corpus's is an **index** range; **Burnol's
exact form is unread.** ### **Not adjudicated — without Burnol the comparison would be two-thirds
guessed.** Declared.

## §7 — THE CHARTER QUESTION, RE-RUN

> ### The map now has a genuine proven positivity, its scope, mechanism and obstruction — **and the
> assessment does not improve, it SHARPENS.** What a direct register must add is the passage from
> bounded support to unbounded and from one place to all: **the `(nonArch, unbounded)` quadrant the
> corpus named nine days ago and has not moved since.**
> ### **STILL NOT READY. The corpus's contribution at the field's stopping point is a NAME for the
> residue, not a technique against it.**

## §8 — FOUND-BEYOND

> ### **The register spent three acts re-discovering its own holdings, and the citation discipline is
> what hid them.** Every act correctly demanded a source at cite; **none asked whether the corpus
> already had one.** A rule saying *verify from content* pointed the register **outward** at the
> literature and never **inward** at `relay/reports/`, `GAMMA04_ATTEMPT_SPEC`, or the E-1 screen's own
> table. ### **The corpus's memory is not searched by the same reflex that searches arXiv, and nothing
> in the standing rules made it one.** *Candidate standing rule: **before any external retrieval, grep
> the corpus for the object's name.***

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | **`e6fc3e7`** |
| relay | this report's commit |
| **rails** | **`a23ba23` / `de621b1` — both UNMOVED** |

**Standing: visibility `HOLD` · Weil 1952 second-hand · walk-sheet open. Nothing deposits.**
