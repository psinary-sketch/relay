# `W-ORD-H1` — CONSTRUCTION ACT 2 — 2026-08-10

**Registered at PLACE-papers `4c32e1d` BEFORE any fetch, with the three outcome classes pre-committed.**
**Spectrum computation was AUTHORIZED under the disclaimed register. It was NOT performed — the
inputs do not support it. The authorization is unused, not spent.**
**Rail `de621b1` / `2147a03` unmoved. Nothing deposits.**

---

## §1 — THE TWO DEBTS FROM ACT 1

### (a) THE FRACTURE SQUARE — **DISCHARGED AT SOURCE**

*A symmetric monoidal fracture square*, **`arXiv:2411.05467`** (located via the arXiv API, not a
guessed ID; 1,935 lines fetched and read):

> *"we refer to `U` as the **open part** of `X`, `Z` as the **closed part**, and to `Φ := i*j*` as
> the **gluing functor**. One important feature of recollements is that they allow a description of
> the larger category `X` in terms of smaller, simpler categories `Z` and `U` **with possibly
> complicated glue**."*

> ### **THE GLUING MAP NOW HAS ITS EXACT FORM AT CITE: `Φ := i*j*`, the gluing functor of a recollement.**
> *A debt carried since act 1's close, closed. The source's own phrase — **"with possibly
> complicated glue"** — is this register's thesis in the literature's words.*

### (b) `THH(ℤ; ℤ_p)` IN LOW DEGREES — **NOT DISCHARGED**

*On higher topological Hochschild homology of rings of integers*, **`arXiv:1502.02504`** (929 lines,
fetched). What the probe returned, line 246:

> *"**Bökstedt [Bö] calculated `THH(Z)`** and his result gives the `n = 1` case of the theorem"*

**The source CITES the calculation. It does not restate the numbers in extractable form.**
Adjacent material located and read — line 245 (`THH(ℤ, 𝔽_p) = THH(ℤ) ∧_{HZ} H𝔽_p`, a commutative
`H𝔽_p`-algebra with canonical augmentation) and line 50 (`THH[2](ℤ_(p))`) — **none of it supplies the
low-degree homotopy groups.**

> ### **BÖKSTEDT'S NUMBERS DID NOT RE-ENTER AT CITE. The per-degree dependency stands DECLARED — exactly as act 1 declared it, one source later.**

## §2 — THE COMPLEXIFICATION RULING, APPLIED AS DESIGN *(question grade)*

**The nominee is re-stated with coefficients along the archimedean place** — `⊗ℝ at v = ∞`, per CC's
`𝔸_∞` pattern — **canonical, NOT `ι`-chosen.**

*That is the intended repair of row 3's fail: the complexification stops being an arbitrary embedding
of a `p`-adic ring and becomes the completion at a place the object already carries.*

**THE CIRCLE-SUPPLIES-`ℂ` READING, FILED AT QUESTION GRADE:**

| step | content |
|:--|:--|
| **(i)** | weight spaces of a circle action are **canonically complex** |
| **(ii)** | a **real structure** on them is precisely an **`F_∞`-involution** |
| **(iii)** | **Deninger's no-go read as the justification**: `H¹` must be `ℂ`-linear **because it is un-conjugatable** |

> **ADJUDICATION ROUTE, NAMED AND NOT RUN: CC §5's real-place mechanism against the no-go's §2 —
> `SAME` / `KIN` / `DISTINCT`, by text.** *Coherent is not adjudicated. It stays at question grade,
> and the `ℤ/2`-at-`∞` merge discipline still forbids promotion.*

## §3 — THE SPECTRUM: **OUTCOME CLASS (iii) — INPUTS INSUFFICIENT**

Three classes were pre-committed at registration. **The act lands in the third.**

> ### **THE EXACT MISSING COMPUTATION, NAMED: `π_*(THH(ℤ; ℤ_p))` in low degrees, in a form quotable at cite.**
> Without it the `p`-complete corner's homotopy is unavailable; therefore **the cofiber's homotopy is
> unavailable; therefore there is no weight table and no module list to report.**

**NO spectrum was computed, attempted, or estimated. NO numerical comparison against zero ordinates
was made, in this or any branch** — the registration forbade it absolutely and the question never
arose, because the prior step never produced a list.

## §4 — THE GRADED TABLE, UPDATED

| item | act 1 | **act 2** |
|:--|:--|:--|
| **row 3 — forced shape (`ℂ`, infinite dim)** | **FAIL** (`ℚ`-linear) | **REPAIRED IN DESIGN, question-graded** — canonical archimedean coefficients, not an `ι`-choice |
| rows 1, 2, 4, 7 | UNDETERMINED-AT-INPUTS | **UNCHANGED** — debt (b) was the input they waited on |
| screen col 1, 2 | PASS definitionally | **UNCHANGED** |
| screen col 3 (`s`: scaling vs compact) | UNDETERMINED | **UNCHANGED** |
| **screen col 4 (re-description)** | AT RISK | **AT RISK, ARGUED** — below |

### COLUMN 4, RE-EXAMINED — one paragraph, honest either way

*Does reading the datum through the place's own completion answer the seal, or restate it?*
**It is better than a restatement, and it is not yet an answer.** Completing at a place is not a
re-description of the decomposition: it **adds** the place's own analytic structure, which the bare
fracture square does not carry, and that is a real difference from act 1's position. **But the
seal's demand is that something be read from the glue which the legs do not already contain, and
completing a corner does not by itself do that** — the completion enriches the corner, and the seal
asks about the glue. ***Column 4 therefore moves from AT RISK to AT RISK, ARGUED: the risk is
unchanged, the argument for it is now specific.***

## §5 — FOUND-BEYOND-REGISTRATION *(one item)*

> ### **THE AUTHORIZATION ARRIVED BEFORE ITS PREREQUISITES.**
> The spectrum computation was ruled permissible, under a carefully constructed disclaimed register
> — **and the act could not use the permission, because a source debt outstanding since act 1 was
> never closed.** *Not registered. It is a fact about sequencing rather than about mathematics: **a
> ruling that unblocks a step cannot supply that step's inputs.** This register now holds a standing
> permission it has no way to exercise.* **Recorded so the permission is not mistaken for progress.**

## PINS

| repo | pin |
|:--|:--|
| PLACE-papers | `4c32e1d` (registration) → this pass |
| relay | this report's commit |
| `SIDE-carrier-spec` | tag `v0.4.1` = `33fb981`, PRIVATE |
| **rail `de621b1` / `2147a03`** | **UNMOVED** |

**Nothing claimed. No spectrum computed. `Q2`–`Q3` untouched. Nothing deposits. `h2` outside every arrow.**

> ### **Realization is not placement.**
