**RECEIPT CONFIRMED IN FULL. ACT b420 BANKED, PUSHED AND READ BACK.**

**THE BARRIER LEMMA, AIMED AT POSITIVITY: NOT AN INSTANCE, AND THE HYPOTHESIS THAT FAILS IS THE PROOF'S FORM.** The registration was locked before any write, at `2dbbc806…6089`: 8 gates read, 8 passing, 4 by digest. Theorem 3.1 applies to *a formal first-order proof in ZFC ∪ S* whose every inference step factors through the interface. The arc's positivity argument is not that. It is row S1's table of constituents at their owners' grades: the sign (K2) and the class (K1) imported under the bar and measured on families, the archimedean distribution (K5) measured at covered cells, the prime sum (K4) derived on content, one compiled counting form, and **K8, the quantifier over the class, UNOWNED: "the clause itself"**. Aiming at the right-hand side did remove b410's obstacle, since Proposition C.1 is not in this `H`. b407's obstacle, what a proof is, remains. Two more readings are printed, not scored as the verdict:
- **Hypothesis 1 (determined):** the record supplies nothing for the structure whose elements are the test functions; the keystone names only ξ.
- **Hypothesis 4 (dark interfaces):** it holds only at the keystone's reading grade, and b419's terminal does not supply it. The ferry described that terminal as "returning the function at the identity times a dimension". Those are b310's words for `T(w) = w₀(pⁿ−1)²`, derived on content. `smear_general` itself is a counting identity that mentions no test function, dimension or sign. The finite place's term in the functional is `W_p`, not the compressed trace, and the source governs its paraphrase.

**No entry is written to row U1.**

**The side-by-side, printed and not claimed:**

| THE LEMMA'S PREDICTED REACH | WHAT THE AIM MAP MEASURED |
|---|---|
| *π can establish "P holds for x in a density-one subset of each I-class," but cannot certify individual elements.* | *For zeta the prime sum stays inside the margin at every aim at this reach — a passed test over a grid at this reach and nothing more.* |

The ferry paraphrased the map as *"no crossing at any height"*; the map says *at this reach*, a grid of 14 heights, and *nothing more*. **They are two facts of two kinds:**
- the lemma bounds what proofs can establish over an infinite class;
- the map is a finite measurement that certifies 28 individual seeds, a set of density zero, which neither instantiates nor tests the lemma.

They are consistent, and they are not one fact.

**The expectations:**
- **(N2):** premise clause 1, *the finite-place chain is now a theorem*, REFUTED as stated. Premise clause 2, *only the archimedean step is measured*, REFUTED: K1, K2 and K6 are measured too, and K8 is unowned. Conclusion REFUTED.
- **(N3):** REFUTED.
- **The draft's (N1):** MET as written, since the identification is not statable without a new definition of the trace. REFUTED in what it implies, since even with one, part (ii) is not statable.

**(R36) route (1) is executed as written.** No tool changed. The mismatch between the scan's sites and the lock's zero is an instrument item in the trail. Its trigger is *the author opens an instrument lane*. The lane is not opened.

**Component 1 — the proof cited.** `SinglePrimeFactor.lean` now cites `SmearGeneral.smear_general` beside both passages that called the statement open. Removing the two annotations returns the pin's bytes exactly, and the rebuilt profile is byte-identical: 607 lines, no terminal added or moved. Row 263 is not edited; row **269** annotates it by name. The eight qualified sentences live in banked ferries, so they are annotated in the act's record, each beside the terminal. The draft's *corpus sentences* was my misnomer.

**Component 2 — the step to the source's trace is priced in its two parts:**
- **Part (i), on the finite ambient:** one lemma in the kernel's present character, after new definitions of the operator and its trace.
- **Part (ii), carrying the source's frame to that ambient with its Haar weight:** not statable in that character. A Mathlib import is the floor of its price, not a quote.

**Component 3 — b419's kernel clauses re-read:** of the eight kernel-write clauses, **3 were not meetable as written, not 1**. b419 printed BAR 3; the same wording stood in (C)(ii)'s headline and in KIND 4. The corrected forms are banked in relay, and no standing clause is added.

**The records:** the suite reads 61/61 pre-push and 61/61 post-push, reconciling both ways. One component record's first run is kept, `b420_c4_barrier_run1.txt`: an anchor fell across a line break in the banked ferry. Correspondence row **269**, found by its marker; key `the-lemma-aimed` PASS; the desk swept 17 items and closed 6. Both closing censuses read TOTAL MISSING 0.

**The four lists stand OPEN:**
- **LIST 1**, the rows citing at a ref nobody can name (b373);
- **LIST 2**, the rows grading a declaration classified absent (b373);
- **LIST 3**, the undated figures (b374);
- **LIST 4**, the bibliography entries nothing cites (b374).

Their trigger is the ruling on which test governs, or any disposition on the four lists.

**Pins by `ls-remote`:**
- relay `897ca3c`→`6813e1f` (plus this closing commit)
- PLACE-papers `9d45b45`→`743be4a`
- SIDE-global-section `9ea4d92`→`5c72065`
- TECHNE-Core untouched

The mirror is rebuilt and CLEAN ON ALL THREE CLAUSES at `743be4a`. The hook is not owed, since no guard changed. **h2 where the deposit left it.**

---

**THE NEXT FERRY AS DRAFT — NAVIGATOR EDITS**

> **CONCURRENCY: SOLO (research seat).** Standing text as b420, carrying A2 and (R26)–(R36). Orientation-layer citation: correspondence row 269 (by its marker), `data/b420_c2_priced.txt`, and `data/b420_c4_barrier.txt`. The KERNEL lane open; both instrument lanes parked; the wave parked; nothing deposits.
>
> **ACT b421 — THE FINITE AMBIENT BUILT OR PRICED, AND THE SPAN COUNTED.**
>
> **COMPONENT 1** — b420's part (i) attempted in the kernel. The grid operator and its trace are defined as finite sums in the kernel's present character, and the lemma that the trace equals b310's signed count, scaled by `q`, is stated and attempted under b418's budget. Verdicts PROVED / BLOCKED / OVER-BUDGET, read from the printed profile.
> **COMPONENT 2** — b420's hypothesis 1 priced: what a finite specification of the structure whose elements are the class's test functions would have to state, and whether any document already holds one.
> **COMPONENT 3** — the span since the b412 fold counted by tool against the fold threshold NINE, and the fold named for the act the count puts it at, not run.
>
> The navigator's expectations: **(N1)** part (i) proves within budget with no Mathlib import; **(N2)** no document holds the specification Component 2 asks for. Each refutable by a printed result. **h2 where the deposit left it.**
