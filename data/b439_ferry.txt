CONCURRENCY: SOLO (research seat). Standing text as b438,
carrying A2 and (R26)-(R50); the addendum slot begins at this
act's face per (R50). Orientation-layer citation: row 287,
b438's crossing radii and s values, SIDE-window's non-claims.
Both instrument lanes parked -- the archimedean route may be
evaluated as b438 evaluated it, no new family and no new
instrument; the KERNEL lane open to read; the wave parked;
nothing deposits.

FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b439 -- THE FIXED PROFILE, THE FIXED POINT, AND ONE
ARITHMETIC CLAIM CHECKED.

COMPONENT 1 -- THE PROFILE, STATED FROM THE CONSTRUCTION. The
seed is three bumps at widths a, a^1/2, a^1/4. In s = log x / log a
those widths are 1, 1/2, 1/4 for every radius. State whether the
seed's autocorrelation is therefore a FIXED profile in s,
independent of a -- derived from the instrument's own source,
not asserted -- and if so, print the profile's zeros in s and
check b438's two banked crossings against them. Verdicts: FIXED
(the two crossings are one zero, and their 2.27e-04 spread is
the grid's and not the object's) / NOT FIXED (what depends on
a, quoted from the source) / PARTLY (which part).

COMPONENT 2 -- THE ARITHMETIC CLAIM, CHECKED NOT ASSERTED.
b438's closing explains the alternation by the prime two's
weight being the largest of any prime power. The navigator
computes that weight over prime powers below sixteen and gets
two at 0.980 against seven at 1.471, sixth of ten -- his
arithmetic, to be checked and not adopted. Print the weight at
every prime power the instrument admits, name the maximizer,
and state whether b438's explanation stands, needs replacing,
or was never load-bearing. If it needs replacing, the
navigator's candidate replacement, his and to be scored: the
two-term dominates by POSITION, not weight -- its log is the
smallest, so its s is the smallest, so it sits deepest in the
profile where the amplitude is largest. Decide from the printed
profile values, not from either of us. b438's bank is not
edited; the correction rides in this act's record.

COMPONENT 3 -- THE FIXED POINT, LOCATED AND CHARACTERIZED. h+'s
sign change at u0 ~ 6.2898 is fixed and family-free. Read what
the record holds about it: has any act named it; is it where
h+'s two constituents balance; does the record give it a closed
form or only a value. Then locate it by both archimedean
routes, which share no code, to a tolerance stated before the
comparison. State whether the corpus can say WHAT it is or only
WHERE it is.

COMPONENT 4 -- THE LARGE-RADIUS QUESTION, PRICED AND NOT
OPENED. If Component 1 returns FIXED, then the prime sum is one
fixed profile sampled at the points log n / log a, and the
radius enters only through the sampling density. Name what a
statement about its large-radius behaviour would require -- the
classical density result it would rest on, how the seed's two
vanishing moments bear on it, and whether the square-root decay
or the sampling density governs. Report READ / MEASUREMENT /
BUILD, and whether either parked lane blocks it. PRICED; NOT
OPENED. And name, without building, the three finite facts from
b438 that the axiom-free window kernel could carry: the
entering term is exactly zero at the support edge; the shells
sit at fixed normalized positions; the weight table's
maximizer.

CLOSING as the standing pattern, with Components 1 and 2's
verdicts and Component 4's price printed in the closing message
itself; the disproof lane's fired trigger restated as awaiting
disposition; the arc checkpointed after (iv); the four lists
OPEN. The navigator's expectations: (N1) FIXED, and the spread
is the grid's; (N2) the weight explanation needs replacing and
position is the reason; (N3) the record names u0 nowhere and
can say only where it is; (N4) the large-radius question prices
as a READ, blocked by neither lane. Each refutable by a printed
result. h2 where the deposit left it.

paste ends (part 1 of 1)
