CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step; canonical drive only).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE NEGATIVE CONTROL — the confinement keystone's Epstein
case, read at content and priced, and the archimedean
instrument aimed at it if the price fits one act: does the
instrument see a hypothesis that fails, one act. Scope: a READ
and a PRICING mandatory; the RUN only if priced inside the act,
else split and the run is the next act; NO claim about ζ, h2,
or the roster from anything here — the negative control
decides what the ζ window WAS, and that decision is stated at
exactly its scope; NO aggregation stated, M-2 owed under its
cap; normalizations before counts; every quantity by two
routes sharing no code where two exist; the noise-floor gate
in the path; every reported number from a committed tool with
fixtures; needles from the extract file; stem sweep at
extended scope; hedge audit; registration term-scanned,
satisfiability-checked, sealed, no counts predicted; the suite
re-run after the push; tools that write run files write once
per path (the fold's recurring defect, now structural);
nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
push anything ahead of origin, saying so if nothing is.

COMPONENT 1 — THE READ: the confinement keystone's Epstein
test at content — which quadratic form, which ledger it calls
positive, which register that ledger lives in, where its
zeros come from and which lie off the line, and what the
keystone concludes from it — every claim quoted with its
location. Then what the corpus already holds toward running
it on the arc's instrument: the archimedean term (the same
distribution, or not — decided by the Epstein explicit
formula's own archimedean factor, quoted from a real source
under the import bar), the finite side (representation
numbers where ζ had prime powers — the explicit formula for
the Epstein function quoted), and a zero library including
off-line zeros (owned, importable, or to be computed, with
the method named).

COMPONENT 2 — THE PRICING, typed: each missing constituent as
read / import / build, with an estimate in acts; the run's
shape written down — the places side of the Epstein balance
on lawful test functions at the arc's cells, both routes, and
the sign of the places sum read WITHOUT any zero — and the
falsifier fixed: the instrument sees the failure if a lawful
test function at some cell gives the places sum the sign the
criterion forbids, with the zero side then computed as
corroboration only.

COMPONENT 3 — THE RUN, only if Component 2 prices it inside
this act: as written down, with every control the ζ window
carried (the explicit formula closing at every cell with the
Epstein zero library; the archimedean control where the
source's theorem covers it). Verdicts: (SEES IT) — a lawful f
and a cell where the places side alone takes the forbidden
sign, corroborated by the zero side, with the entailment for
the ζ window stated at its scope: that window was a passed
test, not a tautology, and finite instances carry the
information the criterion says they carry; (DOES NOT SEE IT)
— the places side keeps the permitted sign at every cell
tried, with the entailment stated: the instrument's
discriminating power is bounded by the cells reached, and the
ζ emptiness stands as intrinsic at this reach; (UNTESTABLE
HERE) — the missing constituent named.

CLOSING: the correspondence rows by the idempotent tool; every
result keyed; the confinement keystone gains an append-only
cross-reference line; M-2's row unchanged under its cap; the
seam's debt item 1 restated; the patent clock restated; the
in-flight register; the ledger census; the bridge read named
as next; the wave's candidate list updated by what this act
decides. The navigator's expectation registered here, at
pattern grade: the instrument SEES IT at some cell within the
current reach — refutable by a permitted sign everywhere.
Registration gate; index queried; negative-control, epstein
keyed. Components ordered. THE SHADOW: expected nothing; say
so.

Bank: data/b325_the_negative_control.txt. Deviation rule
standing.

EXECUTION: ferry scan first; the extract step for every read;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite, re-run after the
push; pins by ls-remote across all three repos; the hook if
PLACE-papers is touched; mirror if it moves. STOP.

FOOT: after this act the corpus knows whether its instrument
can say no — and therefore what its ζ window's yes was worth;
the bridge read follows; h2 stands exactly where the deposit
left it; locks last.

paste ends (part 1 of 1)
