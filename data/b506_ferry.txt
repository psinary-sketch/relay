RULING (R116) BEGIN
RULING (R116), the author's, ratified by this paste and strikeable.

(1) b505 CLOSES AS REGISTERED: Component 1 banked and (N1) scored
HELD — r_Q0(n) = (2/3) A(n) + (4/3) a(n) at all 10,000 n, and the
places-side coefficients of -Z0'/Z0 agreeing with b325's LAMQ at
4095 of 4095 to 2.3e-13; Component 2 REFUSED AT ITS FIXTURE, the
count unread; Component 3 not run; (N2) and (N3) NOT SCORABLE. Its
files are committed as they stand. The (R115)(1)/(2) annotations
are NOT executed at b505; they wait for b506's measured count.
(2) A FIXTURE BAR IS STATED IN THE OBJECT'S OWN UNITS AND AT THE
PRECISION THE BANK CARRIES: b505's "|Z| < 1e-10 at every banked
zero" was finer than |Z'| times the bank's ordinate error, and the
refusal was correct as registered. b506's bar reads: every banked
zero lies within 1e-9 of a zero located by Newton from it, and
route A agrees with the Chowla-Selberg evaluator to 1e-8 at every
banked ordinate. A bar that a correct bank cannot pass measures the
bar.
(3) The numerical lane reopens for b506 and shuts at its close; the
kernel lane reopens for the act after, (R104)'s act 3.

RULING (R116) END

FERRY BEGIN — paste for Claude Code (part 1 of 1; confirm receipt-in-full)

ACT b506 — b505 RE-ISSUED FROM ITS COMPONENT 2, THE BAR IN THE
OBJECT'S UNITS.

Component 1 — carried from b505's bank, not re-run: the
decomposition, the constants, and the LAMQ agreement cited at their
addresses in data/b505_*.

Component 2 — the bank's completeness below 150, under (R116)(2)'s
fixture: every banked zero within 1e-9 of its Newton-located zero,
route A against the evaluator to 1e-8, both printed as counts of
passing zeros out of 148. Then the argument principle on the
rectangle sigma in [1/2, sigma_max], t in [0, 150], sigma_max from a
zero-free bound the seat prints; the count against 178.6 and
against |S(T)|; every missing zero located, on and off the line,
sigma > 1 included, each with its |Z| after Newton; the known pair
at 16.290216 and 29.551761 recovered as the positive control;
registered_gate_passed re-examined and its verdict printed.

Component 3 — on the completed bank alone: b326's derived kernel at
the wide cells (a >= 5, truncation inside bound at T = 150) with the
exact transform and (R114)'s grid rule; the consistent residual
against the chain's bound; the count verified. If it fails there,
STOP and print where.

Component 4 — the (R115)(1)/(2) annotations executed, citing the
measured count of missing zeros; the extension toward T near 1,500
and the margin deferred to the acts after.

The navigator's expectations: (N1) all 148 banked zeros pass the
restated fixture; (N2) the complete bank below 150 holds between 25
and 33 zeros the present one lacks, at least one at sigma > 1; (N3)
on the completed bank the derived kernel verifies at 40 or more
wide cells. Each refutable by a printed cell. The lane shuts at
this act's close; nothing compiled; nothing deposits; h2 where the
deposit left it, and no cell of this act is a statement about RH.

FERRY END (part 1 of 1)
