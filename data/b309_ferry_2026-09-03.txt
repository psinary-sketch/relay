CONCURRENCY: SOLO (research seat).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE SCALING TRACE — compute, on the local-field instrument,
the quantity the model could not reach: the compression of the
scaling action against the object's projection, one act.
Scope: a COMPUTATION on a reproduced instrument and a
DERIVATION of what it means; NO aggregation stated, M-2 owed;
NO claim that a nonzero value is a route — a value is a value,
and whether anything reads it is a separate question this act
does not open; the barrier and the compression are statements
about the pairing of the object's space against the top-level
power and are neither extended nor weakened here; exact
arithmetic in every verdict, zero float literals in the
deciding runner; every reported number from a committed tool
with fixtures; needles pulled from emitting files; stem sweep
at extended scope; hedge audit; registration term-scanned,
satisfiability-checked, sealed, no counts predicted; nothing
about the h2 identity beyond the register sentence exact;
nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope
stated; push anything ahead of origin, saying so if nothing
is.

COMPONENT 1 — THE OBJECT COMPUTED: the trace of the scaling
action composed with the object's projection, at every
nonzero power the instrument reaches, exact, at the banked
cells. Positive controls in both polarities first — a case
known zero and a case known nonzero — and a not-dead witness
with an UNAVAILABLE arm where no control can exist. Report
per cell and per power: exactly zero, or the exact value.
Before any value is read, state from the frame algebra which
frames the composed map connects and whether the object's
projection can meet its image at all — so a zero, if it
comes, is understood rather than merely observed.

COMPONENT 2 — THE MECHANISM: whatever Component 1 returns,
derive why, from the frame algebra and the object's two
conditions — if zero, the derivation is a new barrier
statement and its scope is printed (which map, which frames,
what it does NOT say: nothing about the archimedean place,
the identity, h2, or the roster); if nonzero, derive what
carries it and whether the value depends on the power in a
stated way. In either branch state whether the result is a
property of the object's conditions or of the particular
cells computed, and say which by derivation, not by sweep.

COMPONENT 3 — THE SMEARED FORM, only if Component 1 is
nonzero somewhere: assemble the source's own move on the
instrument — integrate the scaling action against a test
function over the scaling part, then compress — with the test
function taken from the corpus's own instrument and its class
stated (the corpus's bump is outside the source's class and
the source prices that exit; quote the price, do not pay it
here). Report the compressed value exactly. NO comparison to
any target, no fit, and b262's mass is named as context only.
If Component 1 is zero everywhere, say plainly that there is
nothing to smear and stop.

COMPONENT 4 — WHAT IT DOES AND DOES NOT DO: in the act's own
words, at exactly its scope; the exposure of this act's own
numbers stated by call path; and the standing sentence that
the instrument reproduces the model where the model was right
and sees one direction further, not that it decides anything
the model left open.

CLOSING: the correspondence rows by the idempotent tool, read
back; every result keyed; the object's three conditions
restated; M-2's row unchanged; the seam's debt item 1
restated; the patent clock restated as unconfirmed; the
window question named as the author's and not opened; the
in-flight register; the ledger census with its scope. Both
seats' expectations registered in advance — the navigator's:
the composed map carries the object's frame off the diagonal
so the projection may not meet its image, and the honest
prediction is zero with a derivable mechanism rather than a
route; refutable by a printed nonzero. No counts predicted.
Registration gate; index queried; scaling-trace keyed.
Components ordered. THE SHADOW: only if finite-decidable AND
able to carry its own scope in its own statement; the frame
arithmetic may be; say which, checked not assumed.

Bank: data/b309_the_scaling_trace.txt. Deviation rule
standing.

EXECUTION: ferry scan first; owners read at content first;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite; pins by ls-remote
across all three repos; the hook if PLACE-papers is touched;
mirror if it moves. STOP.

FOOT: after this act the scaling part has been computed
against the object for the first time, with its mechanism
derived either way; no route is claimed; M-2 is owed; h2
stands exactly where the deposit left it; locks last.

paste ends (part 1 of 1)
