CONCURRENCY: SOLO (research seat; fresh session after a
machine restart — orient from HANDOFF and the b327 bank at
relay HEAD; every read through the extract-to-disk step).

FERRY → CLAUDE CODE — RESUME b328 FROM DISK — paste begins
(part 1 of 1; confirm receipt-in-full)

The machine restarted during b328's derivation phase. The
remote holds b327 as the last complete act; b328's
registration and tool exist on disk only. Resume from disk,
not memory.

(0) git status all three repos; list every untracked and
modified path; report any file with a byte-order mark or
line-ending change against its blob; confirm nothing is
staged.

(1) THE SEAL: verify the b328 registration's hash on raw
bytes against the banked seal line (ba4b7b96…, 23096 bytes).
If intact, proceed under it and do not re-seal. If broken,
STOP and report the byte-level difference before anything
else — a seal broken by a restart is a finding about the
file, and the act does not continue on a registration it
cannot trust.

(2) THE TOOL: confirm tools/b328_family.py is complete
(its final lines present, its own fixtures runnable); run
its fixture suite and report.

(3) THE DERIVATION PHASE: if a derive-phase output file
exists and is complete, read it; otherwise run the derive
phase again — it is exact arithmetic on banked inputs under a
sealed registration and a re-run is the same computation.
Report (B1) through (B4) as the tool prints them: the
quadruple sum against b326's banked four terms at the
thirteen cells; the even reduction; b326's seeds' phases at
the first off-line zero shown below the threshold; the two
transform routes agreeing.

(4) CONTINUE the act as its ferry wrote it: the two
constructions with their phases measured against the
threshold; the control run on both functions with both
libraries; the verdict at scope; the closing register.
Every writer to its own path; the suite re-run after the
push; pins by ls-remote. STOP.

paste ends (part 1 of 1)
