RULING (R104), the author's, ratified by this paste and
strikeable: THE FORWARD ORDER OF ACTS IS FIXED AS: b493 ->
the SIDE-explicit-formula creation under (R103) -> b321's
identity derived from the vendored explicit formula rather
than interfaced -> h2 stated as one Prop, the sign of A - PR
with K8 as binder, on the object the kernel constructs ->
the W-ORD-QUADRATURE-BOUND repair -> the ladder extended
past sqrt(32) with per-n terms banked and the asymptote of
m(a) read -> the register-equivalence read at the compiled
Prop. Each act is ordered by its own ferry; this ruling
fixes the sequence and opens nothing by itself. When b493
closes, the act after it opens with one table read against
this sequence: the 2026-07-26 pending-work census in
VERIFICATION_LOOM and the current OPEN_TRAILS desk, one row
per item, TAKEN UP BY (R104) / TRIPPED BY (R104) / UNTOUCHED,
so the sequence is reconciled with the living record and
not laid beside it.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

b493 RUNS NOW as banked at data/b493_ferry.txt, with one
clause on the source. The eight-record listing the order
names is the author's Zenodo screen capture of 2026-09-14,
and it follows this paste verbatim between the markers
LISTING BEGIN and LISTING END. Write it unchanged to
data/zenodo_listing_2026-09-14_author_screen.txt, print its
line count and sha256, and read the eight records from it.
Where a record's description is truncated on the screen,
say so in the record's row and read the remainder from
b359's fetch JSON or the record's .zenodo.json, naming
which. Everything else in the banked order stands. Nothing
at Zenodo written; nothing deposits; h2 where the deposit
left it.

paste ends (part 1 of 1)

LISTING BEGIN ... LISTING END -- the listing itself is banked verbatim at
data/zenodo_listing_2026-09-14_author_screen.txt and is NOT duplicated here.
