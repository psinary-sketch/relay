CONCURRENCY: SOLO (research seat; navigator Fable 5.1 and
auto-mode approval declared on the face). Standing text as
b470, carrying A2 and (R26)-(R79); the addendum slot per
(R50); scanned before sending, zero banned hits. Both
instrument lanes parked; the kernel lane OPENS for Component
1 and closes when the detached run reports; the wave parked
under (R66); nothing deposits.

RULING (R80), the author's, ratified by this paste and
strikeable: A DETACHED BUILD IS AUTHORIZED for the zeta23
axiom run, on D:, named on the face, logging to a file the
face names, with no foreground limit; the harness's halt is
superseded for this run only. The run's output enters the
record only when the seat reads the log at its end.

RULING (R81), the author's, ratified by this paste and
strikeable: THE FERRY SCAN FLAGS first, never, only and ever
beside the two banned stems. A flagged word goes out only
with its record check printed beside it in the navigator's
message; the seat refuses a ferry whose flagged word carries
none. The navigator's two overclaims of 2026-09-22 are the
incident, entered on the navigator's ledger at the next fold.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b471 - THE AXIOM RUN, DETACHED, AND THE PRICE OF THE
IMPORT READ FROM THE RULES.

COMPONENT 1 - THE DETACHED RUN UNDER (R80). In the clone at
fbdc36b: start lake build for the Solution library as a
detached process, LEAN_NUM_THREADS unset, its stdout and
stderr to relay data/b471_zeta23_build.log, its pid printed
on the face. Then scripts/PrintAxioms.lean as shipped, plus
EF_lit_zetaZeroConfig, EF_lit and EF_lit_zeta, in the same
log. Do not poll it inside the act; the act closes with the
run RUNNING and the log path named, and the next act reads
the log. Print elapsed time at the close.

COMPONENT 2 - THE PRICE OF VENDORING, READ AND NOT PAID.
Quote SPIRAL_MAP section 7 rules 2 and 7 at their lines.
Then, in the clone, compute the import closure of
EF_lit_zetaZeroConfig within Zeta23/ - the modules a
vendored copy would have to carry - and print its module
count, line count and the Mathlib modules it imports
directly. Quote the Apache-2.0 and NOTICE terms that govern
vendoring. State the three things the ruling on
W-ORD-GW-IMPORT would fix: the pin (fbdc36b or a tag), the
attribution header rule 7 requires, and the toolchain the
vendored copy would build under - v4.33.0-rc2 against the
federation's v4.29 pins, a divergence rule 4 already
permits. No file is copied; nothing imported.

CLOSING as the standing pattern, with the pid, the log path
and Component 2's closure counts printed in the closing
message itself; the span counted by tool; the four lists
OPEN. The navigator's expectations: (N1) the detached run is
still RUNNING at the close; (N2) the import closure of the
explicit formula is more than half of Zeta23/ by module
count - the analytic inputs are not separable from the
headline; (N3) the direct Mathlib imports number more than
twenty. Each refutable by a printed result. No grade of the
corpus moves; h2 where the deposit left it.

paste ends (part 1 of 1)
