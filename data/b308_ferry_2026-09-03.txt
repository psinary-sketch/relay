CONCURRENCY: SOLO (research seat; fresh session — orient from
HANDOFF and the b307 bank at relay HEAD; read outputs through
a summarizing step).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE LOCAL-FIELD INSTRUMENT, ACT ONE — build the instrument
that carries the scaling part of the multiplicative group:
locally constant compactly supported functions on the local
field, with the corpus's own chart and Haar normalization,
two radii untied, one act. Scope: an INSTRUMENT BUILD and its
reproduction controls; NO new mathematics and NO first-level
value computed on the new instrument — that is a later act
under its own registration; the instrument must REPRODUCE the
banked results it can reach before any new computation is
trusted, and it retires the escaped-mass artifact only if it
demonstrably has nothing to fold; exact arithmetic in every
verdict; every reported number from a committed tool with
fixtures; needles pulled from emitting files; the stem sweep
at its extended scope; hedge audit; registration
term-scanned, satisfiability-checked, sealed, no counts
predicted; nothing about the h2 identity beyond the register
sentence exact; nothing deposits.

STEP ZERO: the ferry scan; the HANDOFF census; push anything
ahead of origin, saying so if nothing is.

COMPONENT 1 — THE OBJECTS, from owners: the chart and the Haar
normalization as b21 and b280 state them; the level space as
locally constant compactly supported functions; the two radii
— support radius and constancy radius — named separately and
the model's tie between them quoted from the file that ties
them; the scaling action of the full multiplicative group
written on these functions from the chart, with the compact
part and the scaling part identified as such.

COMPONENT 2 — THE BUILD, exact: functions represented on the
untied two-radius grid; the transform on them from the
corpus's own definition; the two Sonin conditions as tests;
dilation by the prime in both directions; the compression by
the constrained space's projection. Every operation with a
positive control in both polarities before it is used.

COMPONENT 3 — THE REPRODUCTION, mandatory before anything
else is trusted: the two conditions at the banked cells; the
constrained space's dimension against the keystone's formula;
the compact-part smear's zero with its mechanism; the
annihilation criterion at every cell the instrument reaches;
the not-dead witness that the projection's trace equals the
constrained dimension. Each reproduced exactly or the
disagreement reported at full prominence with no repair to
the instrument to make it agree.

COMPONENT 4 — THE ARTIFACT: show, by computing the same
quantity the model folds, that on the local field there is
nothing to fold — the scaling part acts without wraparound —
and state exactly which banked model results were exposed to
the artifact and which were not, by call path. Retire the
artifact for this instrument only if the demonstration is
exact; otherwise state what remains.

CLOSING: the instrument's capabilities and limits stated as
a list; what the next act may compute on it named and not
computed; the correspondence rows by the idempotent tool;
every result keyed; the object's three conditions restated;
M-2's row unchanged; the seam's debt item 1 restated; the
patent clock restated; the in-flight register; the HANDOFF
census run and its count reported. Registered in advance:
(F1) the reproduction matches at every reachable cell; (F2)
the scaling part acts without wraparound on the local field;
(F3) the untied radii recover the two-radius family exactly
— each expected true, each refutable by a printed
disagreement. Registration gate; index queried;
local-field-instrument keyed. Components ordered. THE
SHADOW: expected nothing — an instrument build; say so.

Bank: data/b308_the_local_field_instrument.txt. Deviation
rule standing.

EXECUTION: ferry scan first; owners read at content first;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite; pins by ls-remote
across all three repos; the hook if PLACE-papers is touched;
mirror if it moves. STOP.

FOOT: after this act the corpus has an instrument that sees
the direction the model could not, proven against everything
the model got right; what it can compute for the first time
is named and waits; h2 stands exactly where the deposit left
it; locks last.

paste ends (part 1 of 1)
