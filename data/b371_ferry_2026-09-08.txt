CONCURRENCY: SOLO (research seat). FERRY_STANDING v2 by
reference. Anchors by the tool, never typed; extract-to-disk
for every read; the registration LOCKED before any write; gate
arms by content, not by address; no claim about h2, totality or
the roster; the instrument lane stays parked; the wave stays
parked; nothing deposits.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

RULINGS, the author's, ratified by this paste and strikeable:

(R6) THE AUDIT IS RE-SCOPED TO WHAT IS CITED. Its first pass is
the correspondence rows - every row across the papers repo that
names a kernel, a terminal and a pin - checked at the kernel
and pin the row itself names. The federation-wide surface sweep
b369 priced becomes a SECOND pass, opened only if the first
finds a pattern that reaches beyond cited claims. Reason,
recorded: the rows are the surface a reader traverses and a
wave exposes; the rest is protected by the standing rule that a
kernel's own front matter governs before anything is cited from
it.

(R7) THE DESK CLOSES ITEMS. An item whose occasion is gone
closes, with the file and date that killed it named. Sweep the
current desk once under this rule and close what is dead - the
scaffold repair among them, retired at its kernel and confirmed
so - reporting what closed, what stands, and what is
UNCONFIRMED. A desk that only accumulates is a list.

ACT b371 - THE AUDIT'S FIRST TARGET, THE DESK CLOSED, THE HOOK
MADE DURABLE. Number not claimed by any unclosed ferry.

COMPONENT 1 - THE ONE CONFIRMED LIVE CLAIM: the construction
kernel's public description names a Core terminal count. Read
what that count counts and what the kernel's certified profile
now carries, and settle it: SCOPE-DEPENDENT (the two words
count different things - say which, and whether the description
says so plainly enough that a reader would not be misled) or
STALE (the number no longer holds). If stale, repair the
description; if scope-dependent but unclear, say so and route
the wording to the author. One read, one settling, no sweep.

COMPONENT 2 - THE ROW INVENTORY, LISTED AND NOT CHECKED: under
(R6), enumerate the correspondence rows across the papers repo
that name a kernel, a terminal and a pin. For each, record the
kernel, the terminal, the pin as the row states it, and the
grade - from the row, without opening any kernel. Then price
the check: what verifying one row costs, what the whole set
costs, and which rows are highest risk by a stated criterion
(pin age, whether the row names a count rather than a terminal,
whether the kernel has moved since). Audit nothing.

COMPONENT 3 - THE HOOK MADE DURABLE OR STRUCK: the pre-push
hook lives where a clone does not carry it. Either move it to a
tracked directory with the repository configured to read hooks
from there, so a clone plus one config command restores every
guard - exercised in both polarities in each repository, with
the residual manual step named - or strike the item with the
reason. Not both, and the choice is made by what the tracked
path can actually carry.

CLOSING: (R7)'s desk sweep with its closures; the trails ledger
updated through the writer for each item that closed, naming
the file and date that killed it; correspondence rows; keys;
both censuses; pins by ls-remote through the roster; hook and
mirror where owed; the next ferry as DRAFT - NAVIGATOR EDITS,
and it should propose the first batch of row checks under (R6),
ordered by the risk criterion Component 2 states. The
navigator's expectation: the count claim is SCOPE-DEPENDENT and
the description does not say so plainly - refutable by the
description's own words. h2 where the deposit left it.

paste ends (part 1 of 1)
