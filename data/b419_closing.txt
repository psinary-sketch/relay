**RECEIPT CONFIRMED IN FULL. ACT b419 BANKED, PUSHED AND READ BACK.**

**THE GENERAL CLAUSE IS PROVED, WITH AN EMPTY AXIOM PROFILE, ON THE FIRST PROBE.** The registration was locked before any write, at `11b3857e…8ea8`: 8 gates read, 8 passing, 4 checked by digest. `pow_pred` was repaired exactly as b418 named it, `.trans (Nat.pow_succ r (j - 1))`. The first probe was b418's probe 16 with that one line changed. Its profile, not its exit code, read "does not depend on any axioms" on all 16 printed lines. It carries 0 `sorry` and 0 `axiom` in code. The kernel now carries the proof as `Core/SmearGeneral.lean`, and the regenerated `AXIOM_PRINTS.txt` prints: `'SmearGeneral.smear_general' does not depend on any axioms` / `'SmearGeneral.cells_are_instances' does not depend on any axioms`. The prior profile is a true byte prefix of the new one, which grows from 605 to 607 lines. The seven decided cells (2,1), (2,2), (3,1), (3,2), (5,1), (7,1), (2,3) are instances. The seal's (T1.4) carries b416's drafted annotation word for word, and every existing word survives in order. b414's twenty-eight are re-classified over its governing hand-read: 8 QUALIFIED BY THE PROOF and 20 UNREACHED BY THE PROOF, with 0 sentences edited. Classes.lean and SinglePrimeFactor.lean are unmoved; SinglePrimeFactor's docstring still calls the statement open, and it is dated by the new module and routed, not edited. **(N1)** is scored in three clauses, each MET: the verdict is PROVED, the profile is empty, and it closed on the first attempt. **This act's own face carries one clause that cannot be met as written.** BAR 3 asks for AllPrints' prior bytes to be a prefix of its new ones, but Lean takes an import only at a file's head. It is printed FALSE beside the measure the face's (C)(ii) states, which is met: one import inserted, two prints appended. The locked face is not edited. **The lock and the scan are PRICED, NOT RESOLVED, and routed to you.** The lock gate admits only a scan of zero, while the scan's own doctrine says a hit is a string, not a fault, for the reader to rule on. It has refused one ferry in 43 gated scans. Re-pasting and a one-act ruling each cost you one action per hit and change no instrument. Only permitting a face-named hit inside the paste needs the lock gate changed, which is the parked instrument lane's work. **Of 24 walker calls in every transcript that reached the tool's limit, 14 ran over relay or a directory holding it.** 9 said they timed out. 5 were silent, and every silent one was issued beside siblings, so the record does not allow the duration guard to decide it, and the act says so. Two banked verdicts rest on a timed-out call, and **both are CONFIRMED** by bounded re-runs over the relay trees on either side of the call. The first is b231's (ABSENT): 0 files on the tree before its call. The second is b300's DIFFERENT: every line from the adoption on names the same object, with two pre-adoption usages printed and not ruled on. The positive control passed first, and neither act's bank was edited. Four first runs are kept as `_run1`, each with its defect named: the survey mapped acts by paste text; the lock record compared a CRLF working copy by raw bytes instead of git's view; and two suite arms, one of which read a docstring and one of which read a raw text. The suite reads 69/69 pre-push and 69/69 post-push, reconciling both ways. The four lists stand OPEN: **LIST 1**, the rows citing at a ref nobody can name (b373); **LIST 2**, the rows grading a declaration classified absent (b373); **LIST 3**, the undated figures across the roster (b374); **LIST 4**, the bibliography entries nothing cites (b374). Their trigger is the ruling on which test governs, or any disposition on the four lists. Correspondence row **268**, found by its marker, and key `the-clause-printed` PASS; the desk swept 16 items and closed 4. Both closing censuses read TOTAL MISSING 0. Pins by `ls-remote`: relay `a8de7d9`→`e0b8347` (plus this closing commit), PLACE-papers `bd8b1ba`→`9d45b45`, SIDE-global-section `61a8abd`→`9ea4d92`; TECHNE-Core untouched. The mirror is rebuilt and CLEAN ON ALL THREE CLAUSES at `9d45b45`. The hook is not owed, since no guard changed. **h2 where the deposit left it.**

---

**THE NEXT FERRY AS DRAFT — NAVIGATOR EDITS**

> **CONCURRENCY: SOLO (research seat).** Standing text as b419, carrying A2 and (R26)–(R35). Orientation-layer citation: correspondence row 268 (by its marker), `Core/SmearGeneral.lean`, and `data/b419_lock_priced.txt`. The KERNEL lane open; the instrument lanes as the ruling below leaves them; the wave parked; nothing deposits.
>
> **ACT b420 — THE PROOF CITED, THE LOCK RULED.**
>
> **RULING (R36)** — on the lock and the scan: route (1), (2) or (3) as priced at b419, and if (2), the instrument lane opened for that one gate with fixtures in both polarities.
> **COMPONENT 1** — the records that the proof dates: SinglePrimeFactor's docstring, b414's correspondence row naming the open statement, and every corpus sentence b419 re-classified QUALIFIED BY THE PROOF. Each is listed with what it now says against what is compiled, and each is annotated by append only, the original preserved, under (R4)'s precedent.
> **COMPONENT 2** — the proof's reach priced: what separates `smear_general`'s model arithmetic from the source's trace (b310's uncompiled identification), and whether that step is one lemma, a Mathlib import, or a question the kernel cannot state.
> **COMPONENT 3** — the face's write-list clauses re-read against the kernel build procedure, so that no future face asks AllPrints for a byte prefix.
>
> The navigator's expectation: **(N1)** the identification in Component 2 prices as NOT STATABLE in the axiom-free kernel without a new definition of the trace, refutable by a printed statement. **h2 where the deposit left it.**
