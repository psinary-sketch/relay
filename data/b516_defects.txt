    (a) ### **READING (5)`S JSON RULE WAS TOO WIDE AS REGISTERED** -- "any string in them in the captured-stdout dialect" --
        and the first two runs promoted three SIDE-kernel rows to PROFILED from b456`s bank, a search whose verdict is ABSENT,
        quoting deposit notes and reports. ### Repaired twice inside the fold (a quotation marked not printed against its ref;
        a record carrying a file and a line), each with a fixture both ways; the runs banked as `b516_table_first.json`,
        `b516_table_first_run.txt`, `b516_table_first_diff.json`, `b516_table_second_run.txt`; the table restored to b515`s
        close before each re-run so the diff reads against it. The final count: 5 rows profiled from banked JSON, not 8.
    (b) ### **A QUOTED HEREDOC COLLAPSED A BACKSLASH AGAIN**, in a one-line tidy of the patch script; the assertion refused
        it and nothing was written -- the patch ran from its file as written.
    (c) ### **`terminal_table.py` CARRIES THREE PRE-EXISTING `invalid escape sequence` WARNINGS** (a backslash-space in three
        strings, present before this act`s edit); observed, not repaired -- outside the two work-orders -- and routed.
