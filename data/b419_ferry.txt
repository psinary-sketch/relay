CONCURRENCY: SOLO (research seat). Standing text as b418,
carrying A2 and (R26)–(R35). Orientation-layer citation: b418's
attempt record at data/b418_n_attempt.txt and correspondence
row 267. The KERNEL lane open; both instrument lanes parked; the
wave parked; nothing deposits.

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b419 — THE HELPER FIXED, THE CLAUSE PRINTED. Three
components, small on purpose.

COMPONENT 1 — pow_pred repaired exactly as b418 named it, the
module recompiled, and the general clause's axiom profile
printed and read from the profile, never from the exit code.
Verdicts: PROVED — the profile is empty, the seven decided
cells shown to be instances, the terminal added to the printed
profile with the prior profile a true byte prefix, and the
twenty-eight sentences b414 counted re-classified as qualified
by the proof; the seal's own caveat (T1.4) annotated per b416's
drafted line, now that the condition it names is a theorem /
STILL FAILING — the next failing declaration quoted with its
error, and the act stops at the budget b418 set, not a larger
one / OVER-BUDGET — as b418 reported it, with the partial state
banked. A sorry is never a terminal; a silent sorry axiom is
caught by profile.

COMPONENT 2 — the lock-versus-scan conflict PRICED, not
resolved: the two rules quoted, the three resolutions b418
named, what each costs, and which needs the instrument lane.
Routed to the author.

COMPONENT 3 — silent timeouts outside this arc: every recorded
search call in the relay whose duration reaches the tool's
limit, listed with its act and whether any banked verdict rests
on it. The walker guard applied retroactively where the records
allow; where they do not, the act says so. CONFIRMED or MOVED
per verdict; none re-verdicted by the seat.

CLOSING: one paragraph; Component 1's verdict and profile
printed in the closing message itself; the four lists restated
OPEN with triggers; correspondence row; key; both censuses;
pins by ls-remote; hook and mirror where owed. The next ferry
as DRAFT — NAVIGATOR EDITS. The navigator's expectation: (N1)
PROVED, empty profile, first attempt. h2 where the deposit left
it.

paste ends (part 1 of 1)
