CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY → CLAUDE CODE — RESUME b327 FROM DISK — paste begins
(part 1 of 1; confirm receipt-in-full)

The prior session was cut by a dropped connection after the
extract landed and before the gate suite ran. Resume from
disk, not memory.

(0) git status all three repos; report anything written,
staged, or half-written; confirm whether a b327 registration
exists and, if so, whether it is sealed; confirm the extract
file's quotation count and misses from the file itself, not
from the prior session's message.

(1) Do NOT re-run the extract if the file is complete; a
re-run is allowed only if the count or the misses differ
from what the file reports about itself.

(2) If no registration is sealed: write and seal it now,
before any tool runs — the ledger's columns and seed rows,
the bridge read's verdict set, the two notes' text, the
cascades' pairing rule, and expectations with no counts
predicted. If one is sealed, verify it intact and proceed.

(3) COMPLETE b327 as written in its ferry: the ledger built
with its row-writer, anchor, and loom entry; the bridge read
as the first live row; the two notes filed in the emerging-
programmes ledger and nowhere research-facing; the cascades
as ledger content with owed bridges cross-filed to the
trails — including the discriminating family for the
negative control as an owed construction, and the third-
identity-element keystone flagged as a candidate row for the
author's word, not added.

(4) CLOSE: correspondence rows, keys, M-2 under its cap, the
seam's debt, the patent clock, the census extended to the
new ledger; full gate suite re-run after the push; the hook
exercised with its result reported; the mirror rebuilt after
the commit and verified on all three clauses; pins by
ls-remote. STOP.

paste ends (part 1 of 1)
