CONCURRENCY: SOLO (research seat). Standing text as b417,
carrying A2 and (R26)–(R34). Orientation-layer citation: b417's
defect report and its two-readings table; correspondence row
266; the kernel's classA_distinct_from_classD. Both instrument
lanes parked; the KERNEL lane open; the wave parked; nothing
deposits.

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

RULING (R35), the author's, ratified by this paste and
strikeable: READING (b). The tuples are right and the sentence
over-claims. The universality sentence is narrowed in place to
its theorem's own hypothesis — connected reductive symmetry
groups — with the original preserved, and one line added beside
it stating that B's and D's second component remain
STIPULATED, since no document establishes that their substrates
carry such a group. Reason recorded: the kernel compiled that A
and D are distinct, reading (a) would falsify it, and the
corpus's precedence is kernel over prose. No tuple moves; no
kernel figure moves; class A is unmoved.

ACT b418 — THE RULING CARRIED, THE WALKER MEASURED, THE
REPAIRS MADE, AND (N) OPENED. The executor's draft is ADOPTED
in its items, reordered, with (N) added as the act's research
component.

COMPONENT 1 — THE WALKER, FIRST. b417 found ripgrep missing
files in relay's data records. Measure it: which files, why —
binary detection, size, encoding, a glob — and how many prior
ABSENT verdicts this arc banked used that walker over that
directory. Print the list. For each such verdict, re-run its
search with a walker shown to reach the missed files, and
report CONFIRMED or MOVED. An absence banked by a walker with a
blind region is a claim about the walker, and the act says so.

COMPONENT 2 — (R35) EXECUTED in the owner document, and the two
citers of the sentence updated by append, not edit; the kernel
not touched; the narrowing measured against the pre-act blob.

COMPONENT 3 — THREE REPAIRS: the keystone's false sentence about
forty-three, one line, original preserved, routed if the
document's class forbids the edit; registry row p2-35's two
stale figures; and the hygiene tool RETIRED — its last verdict
kept, its writes documented, its file moved to a retired
directory with the reason, and the repair-snapshot guard
recorded as what would have caught it.

COMPONENT 4 — (N), ATTEMPTED. The general clause stands in the
kernel with the single-prime-factor hypothesis. Attempt its
proof: the smear over the unit group at a prime power
collapsing to the weight at the identity times the count.
State the proof strategy before writing a line — which
induction, over which variable, what the kernel already
supplies — and build. Verdicts: PROVED (the terminal named, its
profile printed, the seven decided cells shown to be instances)
/ BLOCKED (the first step the kernel cannot supply, quoted, and
what would supply it — a lemma the axiom-free module lacks, or
a Mathlib import that changes the module's character) /
OVER-BUDGET (the act's cap reached, the partial state banked,
and the next step named). A sorry is never a terminal; an
over-budget decide is caught by profile, per b414.

COMPONENT 5 — the draft's remaining items — the three misplaced
example systems parsed — as drafted.

CLOSING as the draft states it, with Component 1's list and
Component 4's verdict printed in the closing message itself;
the slots and the fifteen in separate paragraphs. The
navigator's expectations: (N1) the walker misses files by a
default binary heuristic on one file kind, and at least one
prior ABSENT verdict MOVES; (N2) (N) reaches BLOCKED at a
finite-sum lemma the axiom-free module lacks, and the block is
priced as one lemma, not a Mathlib import. Each refutable by a
printed result. h2 where the deposit left it.

paste ends (part 1 of 1)

----------------------------------------------------------------------
THE RESUME ORDER, PASTED AFTER THE CONNECTION DROPPED (2026-09-11),
BEFORE ANY REGISTRATION EXISTED. QUOTED IN FULL.
----------------------------------------------------------------------

RESUME b418. Connection dropped mid-Component-1.

STEP ZERO ON RESUME: report the act's state before any action —
registration LOCKED or ABSENT; writes staged or committed, by
repository; working tree status in all four. If ABSENT: start
clean, bank the pre-drop reads as reads, declare the drop on
the face. If LOCKED: resume at Component 1, declare the drop on
the face, and do not re-run any component the bank shows
complete.

COMPONENT 1, ADDITION: test first the hypothesis that the
missed files match an ignore pattern the walker honours by
default — the walker with ignore rules disabled against each
missed file, and the repository's own ignore check on each
missed path, both printed. If confirmed: state which pattern,
when it entered the record, and that every absence banked by
the walker over that directory was an absence over the TRACKED
population — then re-run those absences with ignore disabled as
the ferry orders, reporting CONFIRMED or MOVED per verdict. If
refuted: continue the investigation as the seat had planned —
tool metadata, bundled binary, controlled probes.

All other components and rulings stand as sent.

----------------------------------------------------------------------
STEP ZERO ON RESUME, AS REPORTED BEFORE ANY ACTION.
----------------------------------------------------------------------

Registration ABSENT: no b418 file of any kind in relay. Writes staged:
0 in each of relay, PLACE-papers, SIDE-global-section, SIDE-effects.
Writes committed: none. Each repository at its public tip (relay
1dc9f72, PLACE-papers 09a1e80, SIDE-global-section ad7dba6,
SIDE-effects ef4cff7). Working trees: PLACE-papers, SIDE-global-section
and SIDE-effects clean; relay shows only data/b369_hooks.txt and
data/b369_hygiene.json as modified -- b417's stat-cache artifact,
their content shown equal to the blob by hash-object at b417.
The drop fell during pre-registration reads; no face was typed.
