====================================================================================================
b41 REGISTRATION - THE SCALE-COVARIANT ARCHIMEDEAN FAMILY. NO MEASURED NUMBER IN THIS
BLOCK.
====================================================================================================
W-CONSTRUCTION-1 act 13, items 1-2. Relay-only. ON THE FACE: the archimedean factor is
rebuilt as a family; no identity decided; the register moves only via the protocol. NO
SIGN SENTENCE AT COMPLETE ROSTER. h2 UNCHANGED. NOTHING DEPOSITS. NOTHING CIRCULATES.

ITEM 1 - THE DICTIONARY, DERIVED WITH EACH STEP'S GRADE:
 D1 (cite): tr Q_c = 2c/pi (the Shannon/Landau trace fact; the sitting-5 artifact's
     N = floor(W/pi) beside it). Per-c GATE in this instrument: |tr Q_c - 2c/pi| small.
 D2 (content): CC's machine at c = 2pi IS their Theorem-1/epsilon analysis at the
     sqrt2 window - the corpus's 15/15 battery pinned their Lemma-5.4 numbers at
     c = 2pi. THE ANCHOR: (a = sqrt2) <-> (c = 2pi).
 D3 (OURS, question grade): the interpolation c(a) = pi a^2 - the self-dual time-band
     product through the anchor; where CC's text is silent this is the corpus's own
     reading, labeled so.
 D4 (the b19 clarification, filed from the bank): the corpus holds TWO archimedean
     "Sonin" objects - the b19 grid model (ball at the INNER radius 1/a or 1/a^2,
     dim N-dominated: 237/255 at N=255, growing with N) and CC's S_{inf,lambda} (the
     lambda-SCALE ball, trace-finite only through the resonance geometry - the
     1/(1-lambda(n)^2) structure the eps-series encodes). THE ACT-12 TRANSPORT GAP
     SITS BETWEEN THEM. This registration follows the ferry's operationalization: the
     family is the c-PROLATE layer (CC's own section-4 handle), not the grid model.

ITEM 2 - THE FAMILY, THE INSTRUMENT:
  At each cell a^2 in {2, 3, 4, 8, 9, 12}: c(a) = pi a^2; the prolate layer REBUILT at
  that c (the certified machinery with c a parameter): lambda(n)^{(c)}, xi_n^{(c)};
  gates per c: the Shannon trace (D1) to 1% (the GL truncation's envelope) and the
  layer's symmetry checks; THE E_1 RULE RE-VERIFIED PER c, NOT ASSUMED: the finite
  Fourier transform F_c (kernel e^{i c x y} on the GL grid) applied to each even-ladder
  mode; the phase of <psi_k, F_c psi_k> recorded; the every-other-even alternation
  measured per c (the b33 rule's per-c test). The E_1-trace on the constrained family
  at each cell: the raw band terms tr_n^{(c)} (the b36 machinery on the c-layer's
  grid), the E_1 share per the MEASURED rule; N-stability: NQ in {500, 700, 900}.
  RECORDED beside: the bound W_inf^CC = -A per cell; sum t(n)^{(c)} as data.

REGISTERED LONGHAND, with the structural facts said BEFORE any number:
  THE ANCHOR CELL IS INVARIANT: c(sqrt2^2) = 2pi reproduces the act-12 layer exactly,
  where the E_1-restricted band form ALREADY exceeds the bound (2.12 > 1.99). So
  (COV-repairs), which requires EVERY cell, is EXCLUDED BY THE ANCHOR unless the
  per-c rule re-verification changes the E_1 share at 2pi (it uses the same layer; not
  expected). THE LIVE BRANCHES:
  (COV-partial): the scaled cells repair (E_1 band form <= bound) while the anchor
     cell fails - the failing address then isolated AT the anchor.
  (COV-none): the family changes nothing or worsens - PREDICTED LONGHAND: c grows
     with a^2 (more modes, larger raw trace) while the bound -A SHRINKS (1.99 ->
     1.41): the raw band form should move AWAY from the bound with covariance. If so,
     the obstruction is NOT covariance, and THE NEXT NAME is registered now as a
     candidate, not a conclusion: THE SONIN SUBTRACTION - the band-to-Sonin correction
     (the double-vanishing geometry the eps-machinery itself encodes; the competing
     reading of act 12's ~ -0.5 plateau: not mere truncation but the band-vs-Sonin
     gap). The run's numbers decide between the candidates only as data.
  (COV-third) filed.

ITEM 3 (licensed only by COV-repairs or COV-partial): the covariant equation
  D_cov = [W_inf^CC - Sum_p W_p] - [Tr_E1^{(c)} + Theta_q] per cell; branches
  (R-dies)/(R-stable)/(R-third) as the ferry orders. If item 2 lands (COV-none),
  item 3 is NOT licensed and the report says so.

GATES: C0 as before (carto far-ends; the c = 2pi pins - the anchor layer must
reproduce the battery); per-c Shannon gates; VOID on C0, FLAG on per-c gate misses
with the miss printed.

ROUTING (Rule 4): registration + instrument by Fable; the banked run to the OPUS lane
if available. Outcome: THE OPUS LANE WAS AVAILABLE - the banked run executed on Opus, first attempt
(4 gate PASSes; 2 REPAIRED rows); no fixes to any registered quantity during debug.
STABILITY NOTE filed at the run: the spreads at a^2 = 8, 9, 12 (0.076, 0.353, 0.766)
ENGULF those cells' margins - the three large cells are NUMERICALLY INDETERMINATE (the
instrument's reach, not the mathematics); the verdict rests on the stable subset
a^2 in {2, 3, 4}, said so in the report.
====================================================================================================
