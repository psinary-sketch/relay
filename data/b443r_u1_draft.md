# DRAFT — ROW U1, RESTATED IN THE CONSPIRACY KEYSTONE’S RESIDUE FORM

*Drafted at b443 on the author’s order. **NOT APPLIED.** Row U1 in `PLACE-papers/FACES_LEDGER.md` is unedited; this text becomes anything only on the author’s word. Every count below is read from banked JSON (`b436_prior_sites.json`, `b436_candidates.json`, `b442_site_v.json`, `b443r_site_vi.json`).*

## The row, per site: what the record holds, the missing statement, and the residue

| site | what the record holds | the missing statement, named exactly | the residue |
|:--|:--|:--|:--|
| **(i)** the clause`s quantifier | the quantifiers are *"UNOWNED, and they are the clause"* (b332) | one owner for the quantifiers over the class and, through the explicit formula, over the zeros | **one owner for the quantifiers over the class and, through the explicit formula, over the zeros is the whole of the remaining weight** |
| **(ii)** the height coordinate`s enumeration | a census that produces zeros one at a time; the coordinate *"BOUNDED BY A MEASUREMENT and not by an argument"* (b351) | one argument serving every height | **one argument serving every height is the whole of the remaining weight** |
| **(iii)** the width coordinate`s union | Boas–Kac at each support; *"AN EXHAUSTION AT EVERY WIDTH IS NOT AN EXHAUSTION ACROSS WIDTHS"* (b353) | one `g` serving every width | **one `g` serving every width is the whole of the remaining weight** |
| **(iv)** the prime constituent at a widened support | ten values indexed by `a` (b321) | one statement uniform in the width `a` bounding `Σ_p W_p(f)` against an archimedean quantity | **one statement uniform in the width `a` bounding `Σ_p W_p(f)` against an archimedean quantity is the whole of the remaining weight** |
| **(v)** the representation-dependent constant | Theorem 6.1`s constant *"depends on π"*, over a cuspidal class the second object is not established to belong to | one constant serving every representation the record holds | **one constant serving every representation the record holds is the whole of the remaining weight** |
| **(vi)** the Type-D residue at every finite modulus | a compiled exclusion at every finite modulus; the keystone files the density bound as *the whole of the remaining weight* | a positive global density lower bound across moduli | **a positive global density lower bound across moduli is the whole of the remaining weight** |

## The witness arc’s taxonomy of failure, six sites

| kind | (i) | (ii) | (iii) | (iv) | (v) | (vi) | total |
|:--|--:|--:|--:|--:|--:|--:|--:|
| CLASS BOUNDARY | 13 | 10 | 12 | 1 | 7 | 4 | 47 |
| ABSENT | 0 | 0 | 3 | 1 | 0 | 4 | 8 |
| IMPORT UNDER THE BAR | 2 | 2 | 1 | 1 | 0 | 1 | 7 |
| BOUNDED BY A MEASUREMENT | 0 | 2 | 2 | 0 | 1 | 0 | 5 |
| COORDINATE BOUNDARY | 0 | 2 | 0 | 1 | 1 | 0 | 4 |
| FORM | 0 | 1 | 0 | 1 | 0 | 1 | 3 |
| INSTANCES NOT A CLASS | 0 | 1 | 0 | 1 | 0 | 0 | 2 |
| INSTRUMENT DOES NOT REACH | 0 | 0 | 2 | 0 | 0 | 0 | 2 |
| REFUTED | 1 | 0 | 0 | 0 | 1 | 0 | 2 |
| DENSITY-PLACEMENT SPLIT | 0 | 1 | 0 | 0 | 0 | 0 | 1 |
| WRONG DIRECTION | 0 | 0 | 0 | 1 | 0 | 0 | 1 |
| **candidates** | 16 | 19 | 20 | 7 | 10 | 10 | **82** |

**82 candidates across six sites; no witness held; 11 kinds of failure.** The class boundary is the largest kind overall and the majority at four sites of six — not at site (iv) (`1` of `7`) and not at site (vi). The premise that it is the majority at every site was the navigator’s, and is withdrawn as false.

## The row’s refusal, verbatim

> THE DEPOSIT’S REFUSAL GOVERNS THIS ENTRY AS IT GOVERNS THE LEDGER: the deposit, section 27.3: *"compiling the *structure* of the one-premise-in-five-registers claim while deliberately **not** compiling the cross-register equivalences, since to compile 'discharge one and you discharge all five' would be to compile RH-equivalence itself."* NOTHING IS PAID HERE, NO GRADE IS CONFERRED, AND NO EQUIVALENCE IS COMPILED.

---

# DRAFT UPDATE BLOCK — ROW U1, ENTRY (v): A TRANSCRIPTION DEFECT OF THE b341 SPECIES

*Drafted at b443 and routed for the author’s word. **NOT APPENDED.** The species is b341’s: a transcription defect in a quantity carried from the literature.*

| row / entry | as the row writes it | as the source reads | the defect |
|:--|:--|:--|:--|
| **U1**, entry **(v)** | `S_f(n,π) = λ_n(n,π) + O(n log n)` | `S_f(n,π) = λ_n(√n,π∨) + O(√n log n)` (Lagarias math/0404394v4 Theorem 6.1, relay `data/b358_source_lagarias0404394.txt:1994`) | the Li index is `n` where the source has `√n`, the dual representation is dropped, and the error term`s argument follows; the dependence of the implied constant on `π` — the site’s content — is unaffected |

