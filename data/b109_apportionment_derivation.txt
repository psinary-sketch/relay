====================================================================================================
b109 -- THE APPORTIONMENT DERIVATION ACT -- THE BANK. Registration:
data/b109_registration_2026-08-22.txt (the target fixed; the (I-differ)
fence quoted; TWO DISCLOSURES made in advance). Instrument:
tools/e16/b109_share_check.py. NO KERNEL CHANGES -- b38's ITEM 4 NOTE
governs here as it did at b107 ("if no new apportionment arithmetic is
decide-reachable (the t(n) are not rational), the vanilla leg does NOT grow
this act and the report SAYS SO rather than forcing a module"); honored, not
worked around. Core stands 268/268. Components ordered: 2 consumes 1.
THE (I-differ) VERDICT IS FENCED THROUGHOUT: cited at its grade, never
re-interpreted, never extended, and never read as bearing on anything at
complete roster.
====================================================================================================

COMPONENT 1 -- THE SHARE STATEMENT (P1).
THE PREDICTION, VERBATIM: "(P1) THE SHARE STATEMENT FORMS from recorded
ground alone -- the target equation stated with every constituent's owner and
grade listed, the (I-differ) fence quoted beside it (what the statement is
NOT); branch (b): the non-formable constituent named, first-class."

### P1 LANDS -- THE STATEMENT FORMS. As registered:

  THE SHARE STATEMENT:   f_cell  -->  sigma_even

  CONSTITUENT      OWNER                                    GRADE
  f_cell = W+/A    b38's registered apportionment applied    BENCH (values at
                   to the certified arch column              the declared pins,
                                                             (NQ,NMODE) triple)
  W+, W-           b38's definition, w_n summed by sector    BENCH; the void-gate
                                                             W+ + W- = A verified
                                                             at content (b107)
  A                the atlas's certified arch column         BENCH at its pins;
                                                             pole-free by the
                                                             arrangement's own
                                                             bookkeeping (b38 ITEM 2)
  sigma_even       sum_{n even} t(n) / sum_n t(n), the       BENCH (recorded as
                   t-mass even share                         the b35 even share,
                                                             0.6165)
  t(n)             the closed form lambda(n)^2 xi_n(1)^2 /   THE SOURCES' OWN
                   (1 - lambda(n)^2), both parities          GRADE, in the
                                                             functional's own
                                                             units; EVEN PINNED,
                                                             ODD UNPINNED
  the E1 sector    = the even sector, per b38's own reading  the reading is b38's,
                   (its verdict names "the b35 EVEN share"   cited not invented
                   and contrasts the odd sector's mass)

### THE (I-differ) FENCE, QUOTED BESIDE THE STATEMENT -- WHAT THE STATEMENT
IS NOT: b38's closed-equation verdict is "(I-differ) at every cell, per the
registered map and no wider - with the obstruction FULLY DECOMPOSED into
three computed pieces and nothing open ... (I-match) did NOT land; the
closure protocol's step one is NOT triggered." THE SHARE STATEMENT SAYS
NOTHING ABOUT THAT DIFFERENCE. It is a statement about the APPORTIONMENT'S
INTERNAL STRUCTURE -- whether the sector split follows the mass split -- and
not about whether the two sides of the closed equation agree. A reader who
takes the share statement as bearing on (I-differ) has crossed the fence;
the two live at different objects.

COMPONENT 2 -- THE DERIVATION (P2).
THE PREDICTION, VERBATIM: "(P2) THE DERIVATION: the share structure derived
at the closed-form layer -- the honest prior PARTIAL-OR-BETTER with the
pre-fixed candidates (the mass-ratio limit from the closed forms; the
monotone decline from the recorded lambda behavior) ...; branch (a) whole:
the apportionment's status upgrading DERIVED-AT-CLOSED-FORM-LAYER ...;
branch (b): the withholding step named exactly, the partial banked at its
grade, the map standing."

### THE VERDICT: BRANCH (b) -- PARTIAL, WITH THE WITHHOLDING STEP NAMED
EXACTLY. And the partial carries a DERIVED REDUCTION that is the act's real
content, so it is not a bare stall.

(2a) ### THE EXACT REDUCTION, DERIVED LONGHAND FROM b38's DEFINITION ALONE
  (no new hypothesis; the definition is linear in its three inputs):
    W+  =  sum_{even} tr_n_raw  -  sum_{even} E2_n  -  sigma_even * resid_N
       [by definition, since sum_{even} s_n = sigma_even]
    resid_N = Tr_raw_N - A - E2_N              [b38's own definition]
  Substituting the second into the first and collecting:
    W+  =  sum_{even} tr_n_raw - sum_{even} E2_n
           - sigma_even * Tr_raw_N + sigma_even * A + sigma_even * E2_N
  Hence, moving sigma_even * A across:
  ### W+ - sigma_even * A
      =  [ sum_{even} tr_n_raw  -  sigma_even * Tr_raw_N ]
       -  [ sum_{even} E2_n     -  sigma_even * E2_N     ]
  IN WORDS: THE DEVIATION OF THE APPORTIONED SHARE FROM THE MASS SHARE IS
  EXACTLY THE RAW TRACE'S EVEN-SHARE EXCESS MINUS THE EPSILON INTEGRAL'S
  EVEN-SHARE EXCESS, where "excess" means (the even-sector sum) minus
  (sigma_even times the total). [DERIVED, exact algebra; each step from
  b38's registered definition and nothing else.]
  A CONSISTENCY NOTE, checked: summing the same identity over BOTH sectors
  gives 0 = 0 (both excesses vanish identically when the sector is
  everything), which is the void-gate again from the other side -- the
  reduction reproduces the gate rather than contradicting it.

(2b) ### WHAT THE REDUCTION SHOWS, AND WHY IT IS THE WITHHOLDING STEP: the
  share statement f -> sigma_even holds EXACTLY WHEN THE TWO EXCESSES AGREE.
  And the closed forms govern t(n) -- they do NOT govern tr_n_raw (the b36
  raw dilation traces) and they do NOT govern E2_n (the mode-parts of the
  epsilon integral, act 9). SO THE CLOSED-FORM LAYER CANNOT DECIDE THE SHARE
  STATEMENT: the question it reduces to is an analytic question about two
  OTHER recorded objects' even-shares. ### THE WITHHOLDING STEP, NAMED: THE
  AGREEMENT OF THE RAW-TRACE AND EPSILON-INTEGRAL EVEN-SHARE EXCESSES -- two
  named quantities with recorded owners (b36's machinery; act 9's
  mode-diagonality), neither of them t(n), and neither reachable from the
  lambda-scale closed forms. The executor's registered expectation landed on
  both clauses.
  WHAT THE WARRANT DOES AND DOES NOT SUPPLY, said precisely because it is
  the tempting place to over-read: b38's warrant records that the DIVERGENT
  subtraction is t(n)-weighted (eps'(1+) = Sigma t(n), at cite). That fixes
  the t-weighting of the DIVERGENT part -- which is why s_n was chosen as it
  was -- but E2_n is the full mode-n epsilon part, not only its divergent
  piece, and tr_n_raw is not addressed by the warrant at all. The warrant
  justifies the definition's weighting; it does not evaluate either excess.

(2c) ### THE RECORDED PATTERN, EXAMINED AT THIS ACT'S OWN TARGET -- AND IT
  IS NOT WHAT "DECLINING TOWARD" WOULD SUGGEST (disclosed in advance at the
  registration; confirmed at the instrument). The six recorded f values
  against sigma_even = 0.6165 give deviations
     +0.0758, +0.0473, +0.0368, +0.0095, +0.0039, -0.0073
  -- STRICTLY DECREASING, AND THE LAST ONE IS NEGATIVE. So within the
  recorded range the sequence does not approach the t-share from above and
  settle: IT CROSSES IT, bracketed between a^2 = 8 and a^2 = 12 (a linear
  interpolation puts the crossing near a^2 ~ 10, INSTRUMENT GRADE -- an
  interpolation, not a recorded value). CONSEQUENCE FOR ANY FUTURE
  DERIVATION, stated as the act's own finding: a derivation of
  f -> sigma_even must ACCOUNT FOR THE CROSSING (as a finite-size effect, or
  as evidence against the limit); citing the decline as though it were
  convergence would be fitting, and this act declines to do it.
  *** THIS IS AN OBSERVATION ABOUT THIS ACT'S TARGET QUANTITY AND IS IN
  SCOPE. IT IS NOT A RE-INTERPRETATION OF b38's (W-split) VERDICT, WHICH
  STANDS EXACTLY AS RECORDED -- "a stable fraction, NAMED AS DATA, declining
  toward the t-share 0.6165" is a true description of the trend across the
  range, and b38 flagged its own marginal cell rather than smoothing it. The
  present act adds only that the trend's endpoint lies below the target, a
  fact visible in b38's own numbers and relevant BECAUSE THE SHARE STATEMENT
  IS THIS ACT'S TARGET. ***

(2d) THE PRE-FIXED CANDIDATES, each reported rather than quietly dropped:
  THE MASS-RATIO LIMIT FROM THE CLOSED FORMS -- NOT REACHED, and (2b) says
  why: the closed forms fix sigma_even but say nothing about the two
  excesses that stand between it and f. THE MONOTONE DECLINE FROM THE
  RECORDED LAMBDA BEHAVIOR -- NOT REACHED as a derivation: the decline is
  present in the data and is monotone (checked), but deriving it would again
  require the excesses' behavior, not lambda's alone. Neither candidate
  failed for want of trying; both are downstream of the same named step.

THE STOP-LAW: armed at every matching step; NO TRIGGER. Nothing was matched
by likeness; the reduction is exact algebra on a registered definition.

THE GRADE, STATED UNDER THE b108 CONVENTION: the apportionment's status is
UNCHANGED by this act -- CLOSED-AT-BENCH, OPEN-AT-DERIVATION. What this act
adds is at DERIVATION grade but is a REDUCTION, not a discharge: the
derivation-grade content is the exact identity of (2a), which converts the
open question into a sharper open question with two named owners. No status
word in this bank appears without its axis.

THE FILINGS:
 THE IDENTITY LANE, RESTATED: (1) THE APPORTIONMENT AT DERIVATION GRADE --
 still the near item, now with its interior mapped: what must be shown is
 THE AGREEMENT OF THE TWO EVEN-SHARE EXCESSES (raw trace; epsilon integral),
 and the crossing at a^2 ~ 10 must be accounted for by whatever shows it;
 (2) the T-reading's completion, behind it (form fixed, grade wanting);
 (3) the translation re-rerun, HELD -- D2 did not upgrade, because the cell's
 condition was the reading's completion and the reading did not complete;
 (4) the boundary question, guarded. LANE TWO unchanged: the orientation
 fact; the dictionary question. The D3 re-grade stays HELD.
 THE ELEVENTH SEAM CLOSE'S DEBT: b108's close covered through b107; this act
 stands as the eleventh seam's first item, owed at the cadence.
 NO KERNEL GROWTH, per b38's ITEM 4 NOTE, honored and cited.

HIT RECORD: P1 landed as registered (the statement forms; constituents,
owners and grades listed; the fence quoted). P2 branch (b) with the
withholding step named exactly -- and the partial carries a derived exact
reduction, which is more than a bare stall and is graded as such. BOTH
ADVANCE DISCLOSURES LANDED: the crossing was confirmed at the instrument,
and the reduction fell out exactly as the executor's expectation predicted
(a difference of two excesses over objects the closed forms do not govern).
The two-seat discipline's FIFTH exercise. Neither pre-fixed candidate was
quietly dropped. Deviations: the two disclosures. Divergences: none beyond
them. The surprise protocol: NOT FIRED.
====================================================================================================
