CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE FOLD — b314 through b322 into the findings document under
its own rules, nine acts, one act. Scope: FILINGS only; NO
grade moves, NO act re-verdicted, no new mathematics;
obstacles QUOTED and emitted by the constructive gate against
originating acts; every reported number from a committed tool
with fixtures; needles from the extract file; stem sweep at
extended scope; hedge audit over every file written;
registration term-scanned, satisfiability-checked, sealed, no
counts predicted; the suite re-run after the push;
PLACE-papers is touched, so the hook fires and the mirror is
rebuilt after the commit and verified on all three clauses;
nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
push anything ahead of origin, saying so if nothing is.

COMPONENT 1 — THE FOLD, nine acts, each with its grade, owning
act and scope sentence, every obstacle a verified quotation:
the cold clone (byte equality from a clean toolchain; the
coverage shortfall found); the calibration and the rate (a
sign only; the multiplicative measure absorbing the exponent
along the cutoff); the instrument built from the definition
(the unit not landing; the old vectors fully outside); the
trace on the object (the prediction landing on a broken link;
the compression removing the bump); the forced sign (the
corpus's window a seed, not a product; the square nonnegative
as arithmetic); the stable rank and the coverage repair
(spectrum in its interval; 475 to 566, all axiom-free); the
lawful function and the control (the inequality holding at
twenty-seven of twenty-seven after FAILS and a named defect;
the digamma witness); the window (three theorems; the
identity unable to resolve the exponent by a stated ratio;
the headline empty by structure; the prime sum inside the
margin); the membership (the residual as truncation, minus
one half predicted and measured; the leg under-resolved with
its price; the weaker branch taken). Also folded: the arc's
corrections to its own readings, and every sealed bar found
defective, as their own table.

COMPONENT 2 — THE ARC AS ONE STATEMENT, at the grade the acts
support, scope beside it: the finite-instance identity is
realized on lawful objects with the instrument certified by
three theorems; its archimedean side splits as the object's
square plus a remainder that is the margin; the prime sum sits
inside the margin at every cell; the zero side's sign is
inherited from a library of zeros on the line, so the window
decides nothing and the act said why before counting; the
object's archimedean unit is in its space by derivation and
under-resolved at bench by a stated factor; the clause lives
at totality and the arc names the mechanism by which no finite
instrument reaches it. Nothing about the register sentence
moves.

COMPONENT 3 — THE LORE, consolidated with incidents,
mechanized and judgement lists apart: check normalizations
before counts; a check at a zero cannot see a factor; one
identity, two conventions, one file; a computation that
matters gets a second route sharing no code before its first
number is banked; verify against the stored blob, not the
working copy; a question is under-resolved, not open, when
the candidates sit closer than the instrument's distance from
the answer, and the price is the ratio; when a defective rule
licenses two branches equally, take the weaker; a dichotomy
that is not a partition cannot be read either way; a ranker
does not know what causes what; a run file may not carry a
claim the act declined; the ceiling is where the evaluator
fails, not the last place it works.

COMPONENT 4 — THE INSTRUMENT SUITE inventoried with what each
catches and its incident, including the archimedean
instrument's certifications (three theorems, each with its
cells and margins) and its limits (the domain axis's rate, the
resolving power priced twice).

COMPONENT 5 — THE DESK, one list: M-2 under its cap with its
narrowed space; the object's conditions with the archimedean
leg's status and price; the exponent under-resolved with its
ratio; the window ruling applied at the instrument; the
absolute-path order; the artifact-count counter's repair or
retirement as the author's; the keystone re-read named as next;
the reconciliation wave named as the author's, with the note
that the arc's results are of the kind a wave would carry;
the patent clock, restated as unconfirmed on the record.

CLOSING: the correspondence rows by the idempotent tool; every
result keyed; the fold purely additive with the no-grade-moved
check mechanical; the seam's debt item 1 restated; the
in-flight register; the ledger census with its scope; the
hook exercised with its result reported either way; the mirror
rebuilt AFTER the commit and verified on all three clauses.
Registration gate; index queried; fold rows keyed. Components
ordered.

Bank: data/b323_the_fold.txt. Deviation rule standing.

EXECUTION: ferry scan first; the extract step for every read;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite, re-run after the
push; pins by ls-remote across all three repos. STOP.

FOOT: after this act the nine acts are one section, the arc is
one sentence with its scope, the lore and the suite are
current, and the desk carries the keystone re-read and the
wave as the two things that follow; h2 stands exactly where
the deposit left it; locks last.

paste ends (part 1 of 1)
