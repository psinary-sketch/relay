    (a) ### **ATTEMPT 2 RAN ON THE UNREPAIRED SOURCE.** The seat`s repair script printed a `ℂ` to the Windows console
        (cp1252), raised before it wrote the file, and the compile was not chained on the repair`s success -- so attempt
        2 is attempt 1`s source again (identical sha256), the same three errors in 48 s. ### It is a run of the module and
        is counted as an attempt under READING (4); the repair was re-applied with UTF-8 output, diffed, and attempt 3 ran.
    (b) ### **THE PROFILE`S #check CAPTURE RAN ON INTO THE #print LINES** (no blank line between them in Lean`s output),
        so (N4)`s "ends in Prop" test would have misread it; the scorer reads the `#check` line alone. ### Repaired
        before the desk ran.
    (c) ### **A HEREDOC COLLAPSED BACKSLASHES AGAIN** in an edit script for the record tool (the recorded trap); its
        assertion failed and nothing was written; the edits were made with the editor instead.
    ### Attempt 1`s two elaboration faults -- `ContDiffBump.contDiff` is stated for `n : ℕ∞`, and `(deriv F t : ℂ)`
    ### elaborates `deriv` into `ℂ` -- are the attempt`s errors, repaired for attempt 3, and are in the components.
    (d) ### **TWO ARMS FAILED LIVE AT THE FIRST SUITE RUN.** `G-PROP-NOT-PROVED`'s pattern `[^:]*` could not cross the
        colons inside the def's binders, so it could not find the `: Prop :=` it tests for -- an arm defect, the
        pattern repaired to run to the line's end. `G-TABLE-ROW` read a table in which the three theorems resolved in
        NO repository: the table resolves names through git refs and tracked profile banks, so it cannot see an
        uncommitted module -- not a defect of the module, an ordering fact; the suite was re-run after the local
        commits and before the push. Also: the suite generator's attempt-source glob caught
        `b524_attempt0_stopped.lean`, repaired to the numbered names.
