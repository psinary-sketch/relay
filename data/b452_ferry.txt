CONCURRENCY: SOLO (research seat). Standing text as b451,
carrying A2 and (R26)-(R63); the addendum slot per (R50);
scanned before sending, zero hits. Both instrument lanes
parked; the wave parked; nothing deposits.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b452 - THE CLASS BOUNDARY READ BY ITS SIDE, AND THE SIX
SITES READ BY THEIR GENERATOR.

COMPONENT 1 - WHICH SIDE THE BOUNDARY IS ON, PER CANDIDATE.
For each of the forty-seven K1 candidates in the six banks:
print the class the source's statement ranges over, as the
bank quotes it; print the corpus object the site put against
it; and decide, from the corpus's own reparameterization
results (the two-kinds windows verdict and whatever the
REPARAMETERIZATION keystone holds, read at their addresses),
whether that object is expressible inside the source's class
by a re-expression the corpus already holds. Three verdicts
per candidate: OBJECT-SIDE (a held re-expression places it
inside; the re-expression named), SOURCE-SIDE (no held re-
expression does; the deciding clause quoted), UNDECIDED (the
bank does not carry enough to decide; what is missing named).
Positive control: any candidate among the thirty-five non-K1
failures whose class the corpus's object is already inside,
found by the same test - if none exists, say so and the
control is ABSENT, not passed. No re-expression is performed;
this act reads whether one exists. Print the partition's
counts and the per-site breakdown.

COMPONENT 2 - THE SIX SITES, BY WHAT NAMED THEM. Read at its
own address the act that named each site and print the rule
or list it came from. State whether the six are a CLOSED
ENUMERATION - generated by a rule whose range is finite and
printed, such as the identity's channels or the pentagon's
registers - or a LIST assembled site by site. If closed, print
the generator and its range and state whether the range has
members the arc never visited; if a list, say so and print
what the record would need to close it. No seventh site is
proposed.

CLOSING as the standing pattern, with Component 1's partition
and Component 2's verdict printed in the closing message
itself; the span counted by tool; the four lists OPEN. The
navigator's expectations: (N1) OBJECT-SIDE is not empty -
at least one of the forty-seven has its boundary on the
corpus's side; (N2) the six sites are a LIST and not a closed
enumeration. Each refutable by a printed result. h2 where the
deposit left it.

paste ends (part 1 of 1)
