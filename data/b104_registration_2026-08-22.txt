====================================================================================================
b104 -- THE ORIENTATION-DEPENDENCE READ -- THE RUN-REGISTRATION. BANKED
BEFORE THE GRADING. A grading read at statement level plus decidable checks;
NOTHING IS BUILT. The canon-XIX bar rides. THE DISSOLUTION VERDICT, IF IT
LANDS, APPLIES TO THE IDENTITY'S PATH ONLY AND SAYS SO -- any claim needing
an absolute wing keeps its named missing fact. Nothing about h2 beyond the
read's own graded rows; nothing deposits.
====================================================================================================
FERRY 2026-08-22 (part 1 of 1, receipt confirmed in full). Relay-only.

SOURCES (rule a, at content, QUOTED BEFORE GRADING):
(S1) FILE E'S STATEMENT AT ITS OWNER, read whole
  (Interfaces/FiniteInstanceIdentity.lean). THE TERMS, WITH THEIR DECLARED
  TYPES -- this is the census's decisive material and it is visible in the
  file's own signatures:
    structure DiagonalCell where a_sq : Q ; one_lt : 1 < a_sq
    structure ArchimedeanE1Trace (cell) where value : R
    structure QuotientTrace (cell) where value : R
    structure WeilLedger (cell) where wInf : R ; wPrimes : R
    def finiteInstanceIdentity (T Q W) : Prop :=
      T.value + Q.value = W.wInf - W.wPrimes
  The docstrings: T is "the epsilon-regularized archimedean E1-trace on the
  constrained class at a cell"; W is "Weil's ledger at the cell: W_inf (CC
  convention, the act-12 dictionary) and the prime sum"; the cell is "the
  window a^2 > 1"; and the file states "THIS FILE STATES; IT DOES NOT PROVE".
(S2) THE b103 EXCHANGE THEOREM (row 66): conjugation acts on the extension's
  wings and on the frame's twisted witnesses by a FIXED-POINT-FREE INVOLUTIVE
  EXCHANGE; the absence of a fixed point IS the absence of a selection.
(S3) D3'S RELATION (row 63): the conjugate correlation, derived at the built
  pair, gauge-invariant AS A RELATION.
(S4) THE JUNCTION LEDGER'S ARITHMETIC (rows 57, 61), verbatim at row 57:
  "THE SUSPECT'S VALUE sqrt(2) e^{-i pi/4} = 1 - i IS THE LEDGER'S SUM, exact
  in Z[i]: (1,0) + (0,-1) = (1,-1), norm^2 = 2 (decided)"; and "the ledger
  (q,0) + (0,q) = (q,q) = the banked trace q(1+i) ... ONE LEDGER LAW AT TWO
  ORIENTATIONS (finite +i; archimedean conjugate, tagged)".
(S5) THE ROW-52 PRECEDENT, quoted as THE SHAPE BEING TESTED and never as a
  conclusion to import: the class-bit dissolution -- the absolute class
  reading is a pseudo-question by construction (the conjugation re-gauge
  composed with the relabeling is a symmetry of the entire finite record),
  while THE BREAKING and THE RELATIVE CONJUGACY survive as content.

(rule c) The read grades under the three-grade rubric; A DISSOLUTION IS
PRONOUNCED ONLY IF EVERY TERM'S INVARIANCE DECIDES AT STATEMENT LEVEL, else
the dependent term is named and the dissolution FAILS HONESTLY.
(rule d) The three components are ORDERED (3 consumes 1-2).

REGISTERED PREDICTIONS, VERBATIM FROM THE FERRY:
(P1) THE INVARIANCE CENSUS: every term of file E's identity is
  conjugation-invariant at statement level -- real-valued traces, weights,
  and counts on a conjugation-stable class -- decided term by term with the
  statement's words quoted; branch (b): the orientation-dependent term named
  exactly, first-class, the dissolution refused.
(P2) THE LEDGER CHECK, decidable: the junction ledger under F -> F^3 -- the
  sum (1,0) + (0,-1) maps to (1,0) + (0,+1), the real part invariant at 1,
  the imaginary parts exchanging -- decided in kernel; the
  conjugate-pair-real-sum mechanism exhibited at the banked cells (the finite
  twin: q(1+i) + conjugate = 2q, real).
(P3) THE VERDICT, on P1(a) + P2: THE ABSOLUTE ORIENTATION DISSOLVES ON THE
  IDENTITY'S PATH -- the identity consumes the relative correlation (derived,
  D3) and the real sum (invariant, P2), and no absolute wing selection --
  filed with the row-52 precedent cited as the second instance of the
  dissolution shape; the campaign map re-ordered accordingly: the identity's
  blocker list = G-EXT-3 + the re-rerun + the guarded boundary, the missing
  fact retiring to the absolute-claims lane only; branch (b): the dependence
  named, the map standing as is.

THE ARGUMENT THIS ACT WILL TEST, WRITTEN OUT BEFORE THE GRADING so the read
is a CHECK of a stated chain and not a search for a congenial one:
  (a) Every term of the identity is declared REAL (T.value, Q.value, W.wInf,
      W.wPrimes : R) over a cell whose datum is RATIONAL (a_sq : Q). Real and
      rational data are pointwise fixed by conjugation. [Statement level;
      visible in the file's signatures.]
  (b) Conjugation EXCHANGES the two wings and fixes neither [S2, decided/
      derived at row 66].
  (c) THE ORBIT ARGUMENT, the act's load-bearing step: if a real-valued
      quantity is produced from the recorded data by a conjugation-
      EQUIVARIANT construction, then its value on one wing equals its value
      on the other -- because equivariance sends the value to its own
      conjugate, and a real number is its own conjugate. So the two wings
      yield THE SAME REAL TERM, and an identity written in such terms cannot
      distinguish them: there is no absolute selection for it to consume.
  (d) THE EQUIVARIANCE PREMISE OF (c), and where it is supported: row 50's
      audit (the recorded standard's weight-bearing facts are F -> F^3-
      symmetric) and row 52's one-dictionary audit. IF that support is found
      wanting at the read, (c) fails and the dissolution is REFUSED -- this
      is the pre-named failure mode, and it is the one to watch.
  (e) WHAT THE ARGUMENT DOES NOT REACH, fixed now so the verdict cannot
      swell: it says nothing about claims ABOUT the wing or the class
      themselves (those keep the named missing fact); it does not re-grade
      row 57's three-tag sentence (a tag re-grade needs its own act and its
      own gate, and is NOT performed here); and it does not touch the
      canon-XIX bar's own text.
  (f) ONE ABSENCE ALREADY VISIBLE AND NAMED IN ADVANCE: the record states the
      ledger's sum as the COMPLEX value 1 - i and file E's terms as REAL, but
      it does not anywhere state WHICH real functional of the ledger data the
      identity's archimedean term is. The orbit argument (c) is designed not
      to need that -- it holds for ANY real-valued equivariant functional --
      and the act must say so plainly rather than quietly assuming a
      particular one.

THE PLAN:
(C1) THE CENSUS: term by term, the file's words and declared types quoted,
  each graded invariant or dependent.
(C2) THE LEDGER CHECK: kernel where decidable (planned OrientationShadow,
  row 67, zero-axiom): the component map under conjugation; the
  conjugate-pair real sum at the banked q; the equality of real part and
  norm-square across the two wings (the decidable core of (c)).
(C3) THE VERDICT AND THE FILINGS per its branch.

Bank: data/b104_orientation_dependence.txt (per the ferry). Report:
reports/2026-08-22-orientation-dependence.md. Deviation rule standing.
Divergence note (rule a): none; the argument and its pre-named failure mode
are registered above.
====================================================================================================
