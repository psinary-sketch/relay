====================================================================================================
b65 -- THE TOWER-LIMIT CONSTRUCTION ACT -- BANK. The staged registration RUN;
run-registration: data/b65_registration_2026-08-21.txt (the construction's load-bearing
laws (L1)-(L4) derived longhand there BEFORE the run; the gates ran first). Raw run:
data/b65_run_raw.txt (all gates PASS).
====================================================================================================

THE CONSTRUCTION (component 1) -- LANDS, explicit and finite:
(L1) DERIVED + instance-decided: the embedding is MONOMIAL on the g-basis,
  iota(g_{a,t}) = g+_{pa,pt} (the instrument PASS at (2,1)->(2,2) in exact Z[zeta_16]
  dicts; the kernel decide embedding_instance -- the fiber-constancy of the embedded
  coefficient the content).
(L2) DERIVED + instance-decided: THE EXACT INTERTWINING M+ o iota = iota o M on ALL
  of Son (S+ iota = p * iota S; instrument PASS; kernel decide
  intertwining_instance). CONSEQUENCE: the four sectors are iota-stable on the nose.
(L3) THE COLIMIT, EXPLICIT: the g-span over the limit grid -- classes (a,t) ~
  (pa,pt), i.e. pairs of p-power-denominator fractions (alpha, tau) in (0,1)^2 (the
  fraction constancy decided); M acts by (alpha,tau) -> (1-tau, alpha) with the
  constant phase fraction alpha(1-tau). CARRIER: the limit g-span. SECTORS: the four
  eigenclasses of the order-four action, iota-stable by (L2). TRANSFORM: M on
  classes, exact. GRADE PER RULE (c): everything above is Core-grade finite algebra;
  THE COMPLETION WAS NEVER LEANED ON -- no step needed it, so NOTHING ENTERED
  INTERFACES and the row-21 declaration stands untouched (T2 RESOLVED).
(L4) DERIVED + instance-decided: the norm transport ||iota g||^2 = p ||g||^2 on the
  whole family (the transport-by-p register now derived on ALL of Son, not only the
  E_1 tables).

P1 -- THE INVARIANTS PASS AS CONSTANTS: LANDS.
  The unit trace i at the place 2: row 36's constant sequence + (L2)'s label
  stability -- the limit sectors well-defined, the trace datum the constant i per
  level. The boundary index (1,1): row 41's constant at every banked size. Odd
  flatness: row 36's constant. THE N5 OPPORTUNITY, DECIDED NOT PRESUMED: the seed
  chain is an iota-compatible family BY CONSTRUCTION -- one class of the colimit --
  and with row 41's address coincidence at every level plus the purity-locus
  uniqueness: ### THE BOUNDARY LINE OF THE LIMIT OBJECT IS THE SEED CHAIN'S CLASS,
  at the banked normalization (the shell dictionary). The per-level operator-vector
  half of N5 (the corpus's compressed scaling's own defect vector at each finite
  level) REMAINS OPEN, said so -- the residue at its address.

P2 -- THE BOUNDARY PHASE: LANDS IN THE ZETA_8 FAMILY WITH THE PLUS SIGN, as
  registered. The boundary line's M-eigenvalue is the constant i (banked every
  level); its C_8 root pair is +-zeta_8 and the PLUS-SIGN root zeta_8 = zeta_16^2
  (the b59 scalar, kernel grade) is LEVEL-INDEPENDENT in exact exponent arithmetic:
  the eighth-root exponent N/8 embeds to p^2 * N/8 = N+/8 exactly (decided,
  constancy_instances). THE BOUNDARY PHASE OF THE CONSTRUCTED LIMIT = zeta_8, the
  plus sign, computed exactly. [Every number here finite-side; grade: decided/
  derived; no archimedean comparison of any kind -- the guard's record clean.]

P3 -- THE REGISTER TEST: BRANCH (INVARIANT), with the T1 resolution banked.
  Both registers run in full: (i) every P1/P2 invariant is SCALE-FREE (eigenvalue
  equations, ranks, sector labels, phases -- no metric rescale touches them):
  INVARIANT under both registers. (ii) the limit METRIC exists in the p-register
  (class norms constant, q/p^n = 1 at the place 2 -- decided) and the p^2-register
  applied to the SAME pairing collapses (2^n < 4^n -- decided). (iii) T1 RESOLVED AT
  CONTENT: the two recorded registers are registers OF DIFFERENT OBJECTS -- the
  p-register is the space-pairing transport (now derived on all of Son, (L4)); the
  p^2-register is the volume convention at the quotient object (the act-7 record's
  own register) -- there is no conflict to unify; the space limit uses the
  p-register and the volume convention keeps its own object. THE DICTIONARY GAINS
  THE SCHEME <-> REGISTER ENTRY AT PREDICTION GRADE, AND THE S1 PRESSURE
  STRENGTHENS. THE S1-FACING LINE, VERBATIM: "A PREDICTION, NEVER A DISCHARGE: the
  scheme-invariance clause of S1 has NO finite analogue -- the recorded reason: the
  no-compact-open theorem (the archimedean place has no finite exact levels; the
  finite tower's limit and the eps-scheme's identity limit are different objects
  until a derivation says otherwise)."

THE MARKS: T1 RESOLVED (different objects; banked above). T2 RESOLVED (the algebraic
colimit carries every deliverable; the completion untouched, nothing entered
Interfaces). T3 STAYS OPEN (the SOT operator-limit relation -- the translation
hypothesis, untouched). T4/N5: the LIMIT half CLOSED (P1); the per-level operator
half the named residue. T5: the meeting point now EXISTS finite-side -- the
constructed limit's twisted half carries the C_8 action (the exponent constancy);
the frame-boundary extensions stay staged, now with a built object to meet.

Kernel: TowerLimitShadow (4 terminals, each "does not depend on any axioms"; Core
194/194); correspondence row 45. Nothing entered Interfaces, and the report says
why.

RANGE LAW: the constructed limit is a finite-side algebraic object with exact
invariants; nothing archimedean was computed; the SOT operator limit and S1 stay at
their own rows; the junction stays a fence; nothing about h2. Compatibility, not
factorization.
====================================================================================================
