RULING (R140) BEGIN
RULING (R140), the author's, ratified by this paste and strikeable.

(1) b529 is entered: (f)(ii) compiled as far as it derives — the
pair's algebra and the near factor DERIVES, the far factor an
INTERFACE on the named hypothesis farSmall with its number at the
witness (epsilon_delta = 3.9e-5, bound -201.81 against -201.85);
the seat's three declared departures (evenness to name the factors,
farSmall at delta = 0, c = 2 for the pair) stand as correct readings
of the kernel against a looser ferry.
(2) f3 IS STATED WITH ITS TWO INGREDIENTS AS HYPOTHESES AND ITS
LIMIT PRINTED: the rest of the zero sum is bounded above under
H-STRIP (every zero has real part in (0, 1)) and H-COUNT (the
kernel's local count), by a decay lemma (d') of the order of phi's
smoothness; and the bound's growth in the width is compared with
the pair's on the face — the rest is allowed e^{L} by H-STRIP alone
while the pair grows e^{2 delta L}, so for delta below 1/2 the
assembly f4 does not close from f3 with H-STRIP alone. That
comparison is a printed inequality, not a sentence, and f4's
remaining need is named from it: either a zero of maximal real
part (H-MAX) or an argument that avoids it, priced and not
attempted.
(3) The kernel lane opens for b530 and shuts at its close.

RULING (R140) END

FERRY BEGIN — paste for Claude Code (part 1 of 1; confirm receipt-in-full)

ACT b530 — f3, THE OTHER ZEROS BOUNDED ABOVE, WITH ITS LIMIT PRINTED.

Component 1 — the decay lemma (d'), compiled: for phi real, C^p,
supported in [-L, L], and any complex z with Re z ≠ 0,
‖paperFT phi z‖ ≤ (∫ ‖phi^(p)‖) · exp(L |Im z|) / |Re z|^p, by
integration by parts p times on the compact support, with (d) as
the case p = 0; then the window's transform through b524's
factorisation, ‖paperFT h z‖ ≤ |gamma_0^2 - z^2| · (∫ ‖(cos(gamma_0
·) phi)^(p)‖) · exp(L |Im z|) / |Re z|^p; both printed from the
declaration line to the :=, profiled.

Component 2 — the rest, bounded, compiled under hypotheses: with
H-STRIP and H-COUNT as named hypotheses of the statement, for the
window at gamma_0 and k = h ⋆ h~, the sum of ‖k̂(rho)‖ over all
zeros other than rho_0 and 1 - conj rho_0 is at most an explicit
R(L, p, gamma_0) built from (d'), e^{L}, and the local count summed
over unit intervals; the statement printed; the grade by
statement-read (DERIVES of its model statement under the two named
hypotheses).

Component 3 — the exponent comparison, printed: the pair's lower
bound from f2 under farSmall, of order delta^2 slope^2 G(L)^2 with
G at least e^{delta L} times the plateau's edge fraction, against
R(L, p, gamma_0)'s e^{L} times a polynomial in L; the inequality
2 delta < 1 for delta < 1/2 stated as a theorem of real numbers,
and the consequence written as a Prop f4_needs: that f4 closes
from f2 and f3 alone iff the rest's allowed growth is at most the
pair's, which H-STRIP does not give. Beside it, at Q0's B-spline
instance at a = 34, R evaluated numerically against the measured
rest and against the pair's term, so the crudeness of R is a
printed ratio.

Component 4 — f4's remaining need priced in five lines each: (A)
H-MAX, a zero of maximal real part, and whether the supremum of
real parts of zeros is attained — stated as a Prop, its status
UNKNOWN in the kernel and in the record; (B) a route without H-MAX,
by a sequence of zeros approaching the supremum with widths chosen
against the count at their heights, its lemmas named or marked
ABSENT. Neither attempted.

Component 5 — the terminal table through an AxiomCheck file; f3
marked DERIVES-under-H-STRIP-and-H-COUNT in W-ORD-WEIL-CONVERSE's
table; f4 OPEN with (A) and (B) beside it.

The navigator's expectations: (N1) (d') compiles standard three
within three attempts; (N2) the rest's bound compiles with the two
hypotheses named and no others; (N3) the exponent comparison
prints 2 delta < 1 and f4_needs is not closable from f2 and f3 with
H-STRIP; (N4) at a = 34, R exceeds the measured rest by more than a
factor of 100. Each refutable by a printed cell. Compiles in the
kernel alone; nothing at Zenodo written; nothing deposits; h2 where
the deposit left it.

FERRY END (part 1 of 1)
