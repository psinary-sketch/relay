====================================================================================================
b63 -- THE EPSILON-MINUS ACT -- BANK. The staged epsilon-minus registration RUN;
run-registration: data/b63_registration_2026-08-21.txt (the candidate construction
registered before anything ran; the guard held; divergence: none). File name per the
ferry, verbatim: data/b61_epsilon_minus.txt.
====================================================================================================

P1 -- PART A AT THE EVEN STANDARD: LANDS, precisely. The odd-index series' exact
machinery is the ALREADY-BANKED machinery (the closed form t(n) =
lambda^2 xi(1)^2/(1-lambda^2); per-term positivity structural; the split; the
eps'(1+) pin 8.8191383 of 22.9964757; the tail 3.19e-05 bounded) -- the same standard
the even part's discharge met, WITH THE SAME TWO NAMED REMAINDERS AND NO OTHERS: the
HS stub (Mathlib holds no Hilbert-Schmidt class; named stub, never a sorry) and the
W_inf apportionment joint (recorded open; assumed nowhere in this act).

P2 -- THE +-i WEIGHTS BY DERIVATION: LANDS; GAMMA-2 DISCHARGED AS LOCATION. The
purely-imaginary pairing lemma (B2, compiled abstract: odd_pairing_imaginary): with
S* = Pi compose S (the kernel ground of the recorded F^2 = parity) and Pi-S
commutation, a real parity-odd vector's transform pairing is conj-antisymmetric --
PURELY IMAGINARY; the even twin (even_pairing_real) is conj-symmetric -- REAL. The
junction act's gamma-2 (the real-valuedness that blocked the eighth root) is thereby
LOCATED: a property of the even class, not of the functional. The mod-four structure
explicit (B4): F^2 = -1 on the odd part forces the +-i classes (the class equation
(+-i)^2 = -1 decided, odd_weights_table); the four classes = the recorded even pair
(+1, -1) joined with the derived odd pair (+i, -i); the finite mod-four classes ARE
the banked twisted sectors. Decided finite instances: the (2,1) seed's pairing 4i,
conj-antisymmetric; the even contrast delta_0's pairing 1, conj-symmetric
(seed_pairing_instances).

P3 -- THE ODD COLLAPSE: LANDS. The transform-weighted odd sum is
i*m_i + (-i)*m_{-i} = i*(m_i - m_{-i}) -- the odd twin of the derived even collapse
m_1 - m_{-1} (the b62 chain; the algebraic core row 35); the shape compiled at named
hypotheses (odd_collapse).

P4 -- THE INVARIANCE CLAUSE, RESOLVED PER ITS OWN TERMS, NOT SMOOTHED: the
regularization-invariance of the odd masses does NOT derive by the sources' own
scheme -- the scheme's recorded pins are declared evaluations (the same recorded
ground as gamma-1) -- and therefore LANDS AS THE EXACT NAMED RESIDUE OF S1: the
identity-limit invariance, at its own row, cross-referenced; not this act's.

THE CONSTRUCTION (B1-B5): the odd home recorded (b32); the pairing lemma derived
(B2); the odd ladder at the recorded standard (the concentration kernel
parity-symmetric by the same substitution; the spectral step AT CITE -- the SAME
grade the even ladder carries); the odd masses t^-(n) EXIST by the sources' own
closed-form pattern, in the functional's own units; NO ODD PINS EXIST and none were
invented -- no numerical odd value appears in this act. The classical prolate
identifications were NAVIGATOR-ASSERTED throughout and USED NOWHERE.

THE KINSHIP GATES (shape agreements, NOT proofs -- said so, gate by gate):
(G-a) PASS (kinship): the odd pair's two-class structure matches the finite twisted
  pair (d_i, d_{-i}) with the boundary excess (rows 35-36).
(G-b) PASS (kinship): the odd collapse i*(m_i - m_{-i}) is the parity-split twin of
  the even m_1 - m_{-1}.
(G-c) PASS (kinship): the odd weights admit square roots with fourth power -1 by the
  row-40 eighth-root pairing arithmetic -- the C_8 action's odd counterpart.

### VERDICT: BRANCH (a). The construction lands and every gate passes at its stated
species. ### S2 IS DISCHARGED AT THE RECORDED STANDARD: the +-i-separated odd masses
EXIST, DERIVED, IN THE FUNCTIONAL'S OWN UNITS (the closed-form pattern at the odd
ladder; the spectral step at the same cite-grade the even side carries; the
invariance clause the named residue of S1, per P4). THE JUNCTION'S THIRD VISIT (the
product-law comparison re-run in the ODD CLASS, where the registered suspect
e^{-i pi/4} can live -- the class obstruction gamma-2 now located, not standing) is
REGISTERED as the next act, NOT run.

THE MARKS: N1 RESOLVED by construction (the odd-class extension exists at the
recorded standard); N2 RESOLVED (the method is the sources' pattern at odd index);
N3 STAYS OPEN, assumed nowhere; N4 -> P4 (the S1 residue); N5 already filed.

Kernel: OddPairingShadow (5 terminals, each "does not depend on any axioms"; Core
188/188); correspondence row 43. The zero-weight observation, banked
OBSERVATION-ONLY per the guard: (1+i) * e^{-i pi/4} = sqrt(2), real -- observed,
weighted zero, used nowhere.

RANGE LAW: the derivations are statements about the recorded machinery and its
parity structure; the odd masses exist at the recorded standard with NO numerical
pins; the identity limit (S1) and the archimedean junction stay undecided; nothing
about h2. Compatibility, not factorization.
====================================================================================================
