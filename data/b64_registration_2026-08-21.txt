====================================================================================================
b64 -- THE JUNCTION'S THIRD VISIT -- THE RUN-REGISTRATION. BANKED WHOLE BEFORE ANY
COMPUTATION. The registered third-visit frame (the epsilon-minus act's dated line in
the junction registration) governs; divergences resolve in its favor; the guard (b56,
verbatim: one candidate, C1 = e^{-i pi/4}; anything else OBSERVATION-ONLY at zero
weight) and the surprise protocol carry over. S1 stays at its own row. THE
BENCH/DERIVED PRE-COMMITMENT (the ferry's (d)): pin-evaluation quantities are BENCH
GRADE -- they may evidence, never discharge; every number's grade is labeled at first
appearance here and in the report.
====================================================================================================
FERRY 2026-08-21 (part 1 of 1, receipt confirmed). Relay-only. NO SIGN SENTENCE.
h2 UNCHANGED BY CONSTRUCTION; the range law governs every sentence. NOTHING DEPOSITS.

SOURCES (rule b/c, at content): the odd-class machinery at its filed standard (row
43, the b61 bank: the odd collapse i(m_i - m_{-i}); the masses' per-term positivity
by the sources' closed-form pattern -- each t(n) a square over (1 - lambda^2) > 0,
REAL AND POSITIVE, the banked structural sentence); the even-visit chain (b60: the
even collapse m_1 - m_{-1}; gamma-1 the identity limit); the finite side VERBATIM
(rows 35-36): tr(S) = q(1 + i) at the place 2 [DERIVED] -- the whole-chart pair
(1, 1): real part = the whole-space even imbalance, imaginary part = the whole-space
odd imbalance, EQUAL at the place 2 by the chart Gauss computation; the Sonin unit i;
odd places flat. The registered suspect C1 = e^{-i pi/4} [REGISTERED-CANDIDATE
GRADE].

----------------------------------------------------------------------------------------------------
COMPONENT 1 -- THE ODD-CLASS COMPARISON. THE PARTNER OBJECT, DERIVED LONGHAND HERE BY
THE SOURCES' OWN PATTERN, BEFORE ANY VALUE:

(1) THE ODD PARTNER ALONE: the odd-class transform-weighted trace is
  i*(m_i - m_{-i}) [DERIVED, row 43]. Its masses are REAL AND POSITIVE per mode (the
  closed-form pattern), so the odd partner is PURELY IMAGINARY: its extractable unit
  is +-i and NOTHING ELSE. DERIVED CONSEQUENCE, stated before any comparison: NO
  SINGLE CLASS CAN CARRY THE SUSPECT -- the even class is real-valued (gamma-2, now
  located), the odd class imaginary-valued (this line); an eighth-root phase
  requires BOTH.
(2) THE WHOLE-TRACE OBJECT AND THE UNIT EXTRACTION: by the same pattern the full
  mu_4-weighted trace is (m_1 - m_{-1}) + i*(m_i - m_{-i}) [DERIVED at form level;
  the algebraic core row 35]. THE UNIT is its phase; the normalization (the positive
  real scale) is EXTRANEOUS to the law form L (which asks only that
  (1+i) * u_inf be positive real).
(3) THE CONVERSION, derived here: C1 = e^{-i pi/4} holds IFF the CROSS-PARITY
  BALANCE holds in the identity limit:
      m_1 - m_{-1} = -(m_i - m_{-i}) = m_{-i} - m_i > 0,
  i.e. the archimedean whole-trace pair is (a, -a) with a > 0 -- THE CONJUGATE TWIN
  of the derived finite whole-chart balance (1, 1) at the place 2 (equality with the
  PLUS sign, banked: tr S = q(1+i)). The law's algebraic core: (1 + i)(a - ai) = 2a,
  REAL -- the finite unit times any conjugate-balanced pair is real [DERIVED; to be
  compiled abstract at named hypotheses].
(4) WHICH STEPS REACH WHICH GRADE (pre-committed per (d), BEFORE computing):
  DERIVATION GRADE REACHABLE: steps (1)-(3) -- the partner's form, the unit species
  per class, the conversion, and its finite twin. BENCH GRADE ONLY: any VALUE of the
  masses -- the recorded pins are declared evaluations (gamma-1's ground); the even
  imbalance at the eps'(1+) pin (5.358167159 [BENCH], the b62 datum) and the absence
  of any odd pin (NO odd evaluation data exist in the banks -- stated, not padded).
  UNREACHABLE WITHOUT S1: the identity-limit values of the four masses -- the
  comparison's final step.

REGISTERED EXPECTATION: BRANCH (gamma) -- the derivation reduces EXACTLY to S1 at
the named step (the identity-limit values of the four masses), and the third visit
closes by CONVERTING the product law into the precise S1-conditional statement:
  IF the identity limit exists and is scheme-invariant (S1), THEN the partner is
  u_inf = phase[(m_1 - m_{-1}) + i(m_i - m_{-i})]|_limit, and the suspect C1 holds
  IFF the cross-parity balance m_1 - m_{-1} = m_{-i} - m_i > 0 holds in the limit
  -- the conjugate twin of the finite place-2 balance.
The branches (alpha) and (beta) stand as the ferry states them, armed: an exact
derived supply of the limit would fire the surprise protocol in full.

BENCH EVIDENCE TO BE RECORDED BESIDE THE CONDITIONAL (per (d), weighted as such and
no more): the even imbalance at the pin is POSITIVE (consistent with the balance's
sign requirement m_1 - m_{-1} > 0); the odd side has NO pins -- no bench evidence
exists there, said plainly.

LEAN PLAN: the conversion's algebraic core -- the conjugate-balance lemma
((1+i) * (a, -a) = (2a, 0), abstract at named hypotheses) and the finite place-2
balance instance ((1,1) with its conjugate (1,-1); pmul (1,1) (1,-1) = (2,0),
decided). Correspondence row 44.

COMPONENT 2 -- THE TOWER-LIMIT OPENING, STAGED (one dated strategy line + one
staging note; the opening act itself is the NEXT act, not this one): the strategy --
the tower limit attacked FIRST ON THE FINITE SIDE AT EXACT GRADE; the translation's
prerequisites named: the Theta_T limit clause (the SOT limit of the level
compressions, row 41's named open) and the N5 open (the operator-level
defect-vector identity); the opening act (registration produced corpus-first, the
Theta_T-opening pattern) staged as the depth line's next act, trigger: the author
schedules.

Bank: data/b62_third_visit.txt (the ferry's name, verbatim). Deviation rule
standing. Divergence note (rule a): none.
====================================================================================================
