CONCURRENCY: SOLO (research seat).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE IDENTITY'S NEIGHBOURHOOD — read the source's proof of its
positivity theorem for where the trace side's content comes
from, and decide whether the finite side's fixed-point
mechanism types at the archimedean place, one act. Scope:
READS under the import bar and a DERIVATION at definitions;
NO archimedean number computed; NO aggregation stated, M-2
owed and the cap on M-2 verdicts carried; the finite-side
results are quarantined at their scope and NOT transported —
the hazard register governs, and the resemblance between a
discrete fixed-point count and a continuous one is named and
refused as evidence until a definition bridges it;
normalizations checked before counts wherever a count
appears; every reported number from a committed tool with
fixtures; needles pulled from emitting files; stem sweep at
extended scope; hedge audit; registration term-scanned,
satisfiability-checked, sealed, no counts predicted; the
suite re-run after the push; nothing about the h2 identity
beyond the register sentence exact; nothing deposits.

THE RULING (WINDOW), the author's, ratified by this paste and
strikeable: "W2 — a mean-zero variant of the corpus's bump is
added beside the existing one, replacing nothing; the
variant's construction is a later act and this act only
records the ruling and what it obligates."

STEP ZERO: the ferry scan; the ledger census with its scope
stated; push anything ahead of origin, saying so if nothing
is.

COMPONENT 1 — THE PROOF READ, quoted at content: the source's
argument for its inequality — how the compressed trace is
evaluated or bounded; whether its content arrives as a term
concentrated at the identity of the scaling group, as a
spread over scalings, or otherwise; what role the test
function's support plays in the trace side as distinct from
the distribution side; and the exact objects (operator,
projection, trace) with their definitions unfolded. Each
claim a quotation with its location.

COMPONENT 2 — THE MECHANISM AT INFINITY, decided by
definitions: at a scaling other than the identity, the map
fixes only zero, and the object's first condition is
vanishing on an interval containing zero. Derive, from the
source's definitions and the object's, whether the
compression of a single non-identity scaling has vanishing
trace — or state the exact step at which the continuous case
differs from the discrete one (trace-class status, the
measure on the group, the singularity at the identity). If
it vanishes, state what that entails: the source's positive
quantity is a statement about the identity's neighbourhood
alone, and the corpus's open-window question is a question
about that neighbourhood with primes admitted. If it does
not, the differing step is the finding.

COMPONENT 3 — THE OBLIGATIONS: what an archimedean
instrument computing on the object's own space would need —
a truncation of the object's space stated from the source's
own characterization (the eigenvalue-one eigenspace of the
projection sandwich), the unit already derived to be in it,
the scaling action, the compression — each typed as read /
result / ruling / construction, with the W2 variant's
obligations listed beside it. Priced in acts, not run.

CLOSING: the import ledger's rows; the correspondence rows by
the idempotent tool, read back; every result keyed; the
object's three conditions restated; M-2's row unchanged under
its cap; the seam's debt item 1 restated; the patent clock
restated; the in-flight register; the ledger census with its
scope; the fold (b307–b311) named as due after this act. The
navigator's expectations registered in the ferry: (F1) the
source's trace-side content is concentrated at the identity
of the scaling group; (F2) the single non-identity
compression has vanishing trace at infinity by the same
fixed-point reasoning, with the difference from the discrete
case living in the measure at the identity; each refutable
by a quotation or a derived step. Registration gate; index
queried; identity-neighbourhood keyed. Components ordered.
THE SHADOW: expected nothing — reads and a definitional
decision; say so.

Bank: data/b311_the_identitys_neighbourhood.txt. Deviation
rule standing.

EXECUTION: ferry scan first; owners and the source read at
content first, page image where the text layer fails;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite, re-run after the
push; pins by ls-remote across all three repos; the hook if
PLACE-papers is touched; mirror if it moves. STOP.

FOOT: after this act the corpus knows whether the source's
positivity is a statement about the identity's neighbourhood
in the scaling group, whether the finite side's mechanism
reaches infinity, and what the archimedean instrument on the
object's own space would cost; the window ruling is on the
record; h2 stands exactly where the deposit left it; locks
last.

paste ends (part 1 of 1)
