RULING (R122) BEGIN
RULING (R122), the author's, ratified by this paste and strikeable.

(1) RH → h2_sign IS COMPILED BEFORE THE MATCHED WINDOW IS MEASURED:
b512 found every lemma present, and a Prop with one proved
implication is a different object from a Prop with none. The kernel
lane opens for b513 and shuts at its close; the numerical lane
opens for b514, the matched window, after.
(2) THE MATCHED WINDOW AND THE CONVERSE ARE ONE OBJECT IN TWO
REGISTERS, and the record says so before either is built: the
absent construction b512 names for h2_sign → RH is a test function
that makes zeroSide negative at an off-line zero, and the matched
window of (R121)(4) is that construction attempted numerically at
Q0's pair. If b514 exhibits the violation, its window is the
witness the kernel's converse would need, stated for an arbitrary
off-line rho; if b514 does not, the converse's construction is
harder than a modulated bump and the record knows it. Neither act
claims the other's result.
(3) The register-equivalence finding is entered at its grade: four
of the five deposited registers are NOT STATABLE in the kernel's
objects, the two-channel Li form is NOT STATABLE for want of
lambda_A, and the cell form is derivable from h2_sign by a compiled
step with no route back in this kernel — a fact about derivability
here, not a theorem that the forms differ in truth.

RULING (R122) END

FERRY BEGIN — paste for Claude Code (part 1 of 1; confirm receipt-in-full)

ACT b513 — RH → h2_sign COMPILED.

Component 1 — the theorem. In SIDEExplicitFormula, rh_imp_h2_sign :
RiemannHypothesis → h2_sign, proved along the route b512 read: for
k = weilTest h h in classK, each zero term of zeroSide k under all
zeros on the line is m_rho times |ĥ(gamma)|^2 by paperFT_weilTest and
Complex.mul_conj, nonnegative by Complex.normSq_nonneg, the sum
nonnegative by tsum_nonneg, and b321_identity carries the sign to
poleTerm k - primeSum k + archTerm k. The statement printed from
its declaration line to its :=; #print axioms matched on the whole
string; no sorry; the proof's length in lines and the compile time
printed.

Component 2 — the chain. rh_imp_cell : RiemannHypothesis → the cell
form, as rh_imp_h2_sign composed with h2_sign_imp_cell, one line,
profiled. The three-link chain RH → h2_sign → cell form named in
the terminal table with its grades, each DERIVES of its model
statement, the statement-read of each printed.

Component 3 — the converse, priced and not attempted. The
statement h2_sign → RiemannHypothesis written as a Prop beside the
theorem, with a docstring naming the construction it needs in
(R122)(2)'s words; the Mathlib objects a witness would need (a test
function in classK concentrated at an arbitrary off-line rho, its
zeroSide's sign) named by their real names or ABSENT.

The navigator's expectations: (N1) rh_imp_h2_sign compiles with
the standard-three profile and no sorry at the seat's second
attempt at most; (N2) the proof is under 80 lines; (N3) Component 3
finds at least one ABSENT object. Each refutable by a printed cell.
Compiles in the kernel alone; nothing at Zenodo written; nothing
deposits; h2 where the deposit left it — and the implication from
RH to it now compiled, which moves h2 not at all.

FERRY END (part 1 of 1)
