CONCURRENCY: SOLO (research seat).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE SMEAR COLLAPSES — assemble the source's construction on
the local-field instrument and derive what survives, one act.
Scope: a COMPUTATION on the reproduced instrument and a
DERIVATION; NO aggregation stated, M-2 owed; b309's zero is
carried, not re-derived, and its scope travels (one trace of
one map against one projection); NO comparison to any target
and no fit — the corpus's prime sum and b262's mass are named
as context only; the archimedean contrast is NAMED and NOT
derived (this act is about finite places and says so); exact
arithmetic in every verdict, zero float literals in the
deciding runner; every reported number from a committed tool
with fixtures; needles pulled from emitting files; stem sweep
at extended scope; hedge audit; registration term-scanned,
satisfiability-checked, sealed, no counts predicted; the
suite re-run AFTER the push and its result reported; nothing
about the h2 identity beyond the register sentence exact;
nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope
stated; push anything ahead of origin, saying so if nothing
is.

COMPONENT 1 — THE ASSEMBLY, from the source's own description
quoted: the scaling action integrated against a test function
over the scaling part of the multiplicative group — which at a
finite place is a SUM over powers of the prime with the test
function evaluated at those powers — then compressed by the
object's projection. Write it on the instrument exactly, with
the test function taken as a symbolic weight per power (no
particular bump chosen, so no class question arises and no
price is paid). Then evaluate: with b309's zeros at every
nonzero power, state exactly which terms survive.

COMPONENT 2 — THE DERIVATION: derive, from the frame algebra
and b309's mechanism, that the compressed smear equals the
test function at the identity times the trace of the
projection, which b308 verified equals the constrained
dimension — general in p, n, and the weight, with the
seven-cell computation as the check and not the proof. State
plainly what the surviving term contains and what it does
not: no logarithm of the prime, no sampling at its powers, no
dependence on the weight away from the identity.

COMPONENT 3 — THE READING, at exactly its scope: at a finite
place the source's construction carries no arithmetic; the
prime's contribution lives in the local distribution the
source integrates against (eq. 149, quoted from b305) and
not in any compressed trace on the object's space. State the
fixed-point sentence as the mechanism common to b304's
compact part and b309's scaling part, so that the finite
side's closure reads as ONE statement about the full
multiplicative group — and print its scope: finite places
only; the object as defined; nothing about the archimedean
place, where the group is continuous and this act derives
nothing; nothing about the identity, h2, or the roster.

COMPONENT 4 — THE BEARING on b263's branch, carried as
bearing and never as decision: the finite side cannot supply
the first-level mass through the object; any finite-place
contribution is the arithmetic distribution itself, which
the corpus holds as its prime sum on no space. Say what this
does to M-2's specification — which of its three properties
can still be met by an object on the finite places, and
which cannot — as a restatement of the specification's
scope, not a verdict on M-2.

CLOSING: the correspondence rows by the idempotent tool, read
back; every result keyed; the object's three conditions
restated; M-2's row restated with its scope narrowed if
Component 4 narrows it, and unchanged otherwise; the seam's
debt item 1 restated; the patent clock restated; the window
question named as the author's and not opened; the in-flight
register; the ledger census with its scope. The navigator's
expectation registered in the ferry: the smear collapses to
the identity term at every cell, and the collapse derives
generally — refutable by a printed surviving term at any
nonzero power. Registration gate; index queried;
smear-collapse, fixed-point-silence keyed. Components
ordered. THE SHADOW: only if finite-decidable AND able to
carry its own scope in its statement — the survival of the
identity term alone over an explicit list of powers may be;
say which, checked not assumed.

Bank: data/b310_the_smear_collapses.txt. Deviation rule
standing.

EXECUTION: ferry scan first; owners read at content first;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite, re-run after the
push; pins by ls-remote across all three repos; the hook if
PLACE-papers is touched; mirror if it moves. STOP.

FOOT: after this act the source's construction at a finite
place is one surviving term with no arithmetic in it, the
finite side's closure is one statement with one mechanism,
and the branch has its first structural bearing from the
finite side; M-2 is owed; h2 stands exactly where the deposit
left it; locks last.

paste ends (part 1 of 1)
