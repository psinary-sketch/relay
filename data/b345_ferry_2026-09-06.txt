CONCURRENCY: SOLO (research seat). SORTIE: two legs in order,
each sealed and closed as its own act. FERRY_STANDING v1 by
reference; extract-to-disk for every read; registration sealed
before any instrument runs; no claim about h2, totality, or the
roster; no grade conferred by a seat; the wave stays parked.

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

LEG 1 (b344) — THE LI CONTROL, RE-RUN: the prior leg's bar is
sealed and unmet; this act registers its own, with the tail
panel's quadrature rule fixed BEFORE any value (the rule its
diagnosis named, quoted from that bank) and the two routes
required to share no code. Then: the identity at the tabulated
indices with the pole constant as its own column; the drift
between routes against the new bar; the deposit's proven
finite-range positivity restated at its scope beside the
instrument's values. Verdicts: (FOURTH CONTROL HOLDS — the
instrument agrees with a margin the deposit proved, on a family
outside the lawful class, with the certifications that apply
and those that do not listed as the prior leg listed them) /
(DIFFERING CONSTITUENT, quoted). The faces ledger's bridge row
updated through the writer to one distribution on two families,
with what is measured and what is not.

LEG 2 (b345) — THE EXPONENT BY RATE: the value-based split is
banked UNAFFORDABLE and its side reading says the residual
descends to a floor; register that reading as this act's
premise, tested not assumed — first, measure the residual's
limit against the candidates' separation on the frames already
in the record and state whether a floor is present, since if it
is, no domain resolves the exponent by value and the act says
so at full prominence. Then the alternative discriminator: the
even sector's decay rate along the argument, which the record
measured moving a full power under the flip. Compute the rate
under both conventions on the copies, by two routes, with the
rate's own convergence and the noise-floor gate, and state
whether the separation exceeds the instrument's own uncertainty
in the rate. Verdicts: (RESOLVED, the convention named, the
erratum's consequence clause fired and appended) / (STILL
UNDER-RESOLVED, the new figure in the rate's units) /
(UNAFFORDABLE, priced). No bar moved; nothing re-verdicted.

CLOSING for the sortie: one summary paragraph per leg; the
faces ledger and the clause's grade table updated through the
writer where a leg moves a row; correspondence rows; keys; both
censuses; pins by ls-remote; hook and mirror where the papers
repo moves; the next ferry appended as DRAFT — NAVIGATOR EDITS.
The navigator's expectations: (L1) the control HOLDS under the
fixed rule; (L2) a floor is present and the rate separates the
conventions. h2 where the deposit left it; nothing deposits.

paste ends (part 1 of 1)
