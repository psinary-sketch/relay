====================================================================================================
b97 -- THE COMPLEX-EXTENSION BUILD ACT -- THE BANK. Registration:
data/b97_registration_2026-08-22.txt (the probe point, the chains, the LIFT
criteria L1-L3, and the I-15 gauge precision ALL FIXED BEFORE ANY
COMPUTATION; the staged registration at its filed path GOVERNED; divergence:
none). Raw instrument run: tools/e16/b97_complex_extension_build.py (ALL
CHECKS PASS; the probe inside the derived interval at 1e-12). Kernel:
ExtensionShadow (4 terminals, zero-axiom; Core 258/258; row 63). N-EXT-2
HELD ABSOLUTE: no boundary value computed, approached, or cited anywhere in
this act (Re z = 1 throughout the instrument; the carrier open).
====================================================================================================

COMPONENT 1 -- THE BUILD AND THE GATE.
THE OBJECT, BUILT (every label on its sentence): Theta_ext = the recorded
mass series at complex argument, termwise on the licensed carrier -- the
open right half-plane Re(z) > 0 in the record's own CC-scale variable
[the staged registration's carrier verdict, quoted not paraphrased] -- at
ABSOLUTE-CONVERGENCE grade [derived: the modulus line + termwise domination
against the recorded real-ray ground (b90 G1 as corrected; b87-P2's mass
pattern at its grades)]. The masses are argument-independent recorded
constants [recorded]; the argument dependence is the kernel-form reading at
the S0 attachment caveat [the caveat rides]. N-EXT-1 CARRIED AS THE NAMED
OPEN IT IS: no operator-level sense off the ray exists in the record; every
sentence about Theta_ext below is a sentence about the absolutely
convergent series, nothing more.
P0 -- THE RESTRICTION GATE, SAT FIRST: ### PASS.
  RG-1: at z = x real > 0 every term IS the recorded real-ray term; the
  restriction is the built object's profile BY CONSTRUCTION (structural,
  definitional -- said so, not overdressed).
  RG-2: the one law derived for Theta_ext this act is Schwarz covariance
  (P1(i)); restricted to the ray it says the profile is real, which is the
  recorded ground (real masses, real weights). The b93 transformation law
  was NOT re-derived off-ray and NO off-ray transformation law is claimed
  this act.
  No wrong-object trigger; the act proceeds.

COMPONENT 2 -- THE READINGS, IN ORDER.

(P1 = D1) THE ORIENTATION READING: ### BRANCH (a) -- THE RECORD'S FIRST
ORIENTATION-BEARING ARCHIMEDEAN FACT, at its gauge-honest form.
  (i) SCHWARZ COVARIANCE [derived]: conj(e^{-pi n^2 z}) = e^{-pi n^2
  conj(z)} termwise (the exponent's coefficient -pi n^2 is real); the
  masses are real recorded constants and pass through conjugation; the
  series converges absolutely on the carrier so the termwise identity sums:
  Theta_ext(conj z) = conj(Theta_ext(z)). Derived, each step one line.
  Corollary [derived]: conjugation exchanges the two wings and inverts the
  phase sense; THE RAY IS THE FIXED LOCUS OF THE ACTION ON THE CARRIER, and
  off the ray the action is free.
  (ii) THE PHASE PATTERN at the registered probe z_0 = 1 + i/2 [derived +
  DECIDED at the shadow]: n odd => n^2 == 1 (mod 8) => e^{-i pi n^2/2} =
  e^{-i pi/2} = -i exactly; n even => n^2 == 0 (mod 4) => the phase real.
  (probe_phase_instances, decided for all n < 256; the general pattern the
  same arithmetic at every n, longhand: odd n => n^2 - 1 = (n-1)(n+1)
  divisible by 8.) Hence Im Theta_ext(z_0) = -(Sigma_{n odd} e^{-pi n^2}):
  one constant phase, no cancellation possible.
  (iii) THE NONZERO CERTIFICATE [derived]: -Im Theta_ext(z_0) in
  [e^{-pi}, e^{-pi} + 2 e^{-9 pi}]: the leading term exactly e^{-pi} > 0;
  the tail Sigma_{n >= 3 odd} e^{-pi n^2} <= e^{-9 pi} (1 + e^{-16 pi} +
  ...) < 2 e^{-9 pi} [geometric majorant, longhand]. NONZERO WITH SIGN, at
  DERIVED grade -- the bound is the content; the instrument's decimal
  (-0.043213918264, inside the interval at 1e-12) is illustration
  [bench-instrumental].
  (iv) THE ROW-50 AUDIT ON THE FACT: the signed fact "Im Theta_ext(1 + i/2)
  < 0" maps under F -> F^3 to the conjugate fact at 1 - i/2 -- NOT
  invariant: the fact FAILS F -> F^3. THE I-15 GAUGE PRECISION (registered
  in advance, applied as registered): THE BREAKING -- Im Theta_ext not
  identically zero off the ray; equivalently Theta_ext(z) != Theta_ext(conj
  z) off the fixed locus; conjugation acts FREELY on the object's values
  there -- is GAUGE-INVARIANT and is THE NEW RECORDED FACT; THE DIRECTION
  (which sign at which wing) is embedding-gauge exactly per the
  dissolution's ledger (rows 51-52). The new content enters as breaking
  (absolute) + direction (relative to the one dictionary).
  THE FREE-LOCUS LAW'S ARCHIMEDEAN PREDICTION: CONFIRMED (the first
  archimedean carrier where conjugation acts freely carries the record's
  first archimedean breaking). The b93 miss's lesson held: the address was
  consulted first this time, and the expectation landed.
  FINITE SHADOW DECIDED BESIDE IT: finite_breaking_16 -- the level-16
  gaussian fold 4e_0 + 4e_4 (= 4(1+i), the banked place-2 trace shape) vs
  its conjugate 4e_0 - 4e_4: the finite breaking at the even cell.

(P2 = D2) THE T-IDENTIFICATION: ### PARTIAL -- THE SHORT STEP NAMED
EXACTLY: G-EXT-3. The corridor read: the extension side now carries the
Mellin corridor (attached per the recorded line, THE b93 BENCH FLAG RIDING
on the vertical-decay bound) and the built object's trace-side data at
their recorded grades; file E's T at its owner is the epsilon-regularized
archimedean E1-trace whose machinery (b87-P2) lives on the REAL geometry
(the lambda-scale Sonin pair structure). THE BINDING ABSENCE: no recorded
statement reads the BUILT object's trace through the epsilon-machinery --
G-EXT-3, the first of the three candidates fixed in advance; N-EXT-1 (no
operator sense off the ray) stands behind it. WHAT ADVANCED: both corridor
endpoints are now constructed/located (at b83 neither was; at b94 one was);
the residual coin's address sharpens from "the analytic identification" to
ONE NAMED STEP: the epsilon-read of the built trace. GRADED UNDER THE
RUBRIC: OPEN-AT-G-EXT-3 (no row reached RESEMBLANCE; the stop-law had no
trigger; nothing carried across the absence). The S0 attachment caveat and
the bench flag traveled with every corridor sentence [checked at the tag
audit].

(P3 = D3) THE COHERENCE DECISION: ### THE CONJUGATE CORRELATION DERIVED AS
THE RELATION; THE SELF-GRADING LANDS STAND-WITH-RELATION. THE CHAIN AS RUN
(identical to the registered chain; divergence: none):
  (i) [P1's derived law] the wings are conjugate-exchanged; the upper
  wing's sense at the registered probe is the MINUS sense (the leading
  odd-n phase exactly -i; decided pattern).
  (ii) [row 51, derived; the plus dictionary the recorded convention]: the
  coherent family's real component is FORCED: chi_inf = e^{-2 pi i x} --
  the MINUS sense relative to the plus finite dictionary (the rational
  reciprocity theorem unconditional; H-COH-fin the decided property, b75).
  (iii) [the joint reading -- the new content]: in the joint object (the
  built extension + the plane's coherent family, ONE recorded dictionary,
  both sides internal -- no embedding gauge to quotient), THE CORRELATION
  IS DERIVED: the wing whose phase sense agrees with the forced chi_inf is
  the minus-sense wing, and that sense is the CONJUGATE of the shared
  finite orientation -- THE CONJUGATE CORRELATION AS THE RELATION,
  gauge-invariant as a relation (I-15), its finite mirror DECIDED
  (schwarz_witness_pairing -- conjugation swaps the twisted witnesses
  coherently; mirror_product_one -- the witness times its conjugate is 1;
  finite_breaking_16 beside them). The b71 forced conjugacy is hereby
  EXERCISED AT A BUILT ARCHIMEDEAN PAIR for the first time -- a property of
  a constructed pair, no longer a conditional about a hypothetical limit
  object.
  (iv) THE SELF-GRADING against the pre-fixed criteria: L1 -- LANDS (the
  relation derived at the built pair, the chain above). L2 -- DOES NOT
  LAND: the relation's transport to the record's consuming sentences (the
  three-tag value derivation at the boundary line) requires the
  extension-datum <-> boundary-phase identification (T5's archimedean
  half), NOT derived this act and not attempted. L3 -- FAILS TODAY: the S0
  attachment caveat and the SOT stated-not-proved sense ride the
  extension's chain, and a tag may not retire onto ground carrying an
  undischarged stated-not-proved sense. ### VERDICT: STAND-WITH-RELATION
  -- NO LIFT PRONOUNCED; the canon-XIX bar RIDES; the tagged expectation
  stays a tag; the mandatory I-14 is NOT staged (its trigger is a LIFT);
  THE HONEST ALTERNATIVE'S LANDING IS THIS ONE and it is first-class per
  the registration's face: the relation constructed where it lives, the
  absolute left as the gauge story says it must be until L2's link and
  L3's senses are discharged. WHAT WOULD DISCHARGE THEM, named: L2 -- the
  extension-datum <-> boundary-phase identification (T5's archimedean
  half; adjacent to G-EXT-3's territory); L3 -- the SOT sense proved at
  its owner (the standing open, not new).

COMPONENT 3 -- THE FILINGS.
THE AG-5 REVISIT (triggered: D1 landed branch (a), the fact exists): under
b90's own fixed ground the stand's reason was ABSENCE ("the recorded
standard's weight-bearing facts are orientation-symmetric ... the build's
only orientation-bearing datum is that convention"). THE GROUND CHANGES:
the record now holds an orientation-bearing archimedean fact (the breaking,
gauge-invariant; the direction relative) AND its derived correlation with
the forced chi_inf. THE DECISION RE-RUN LANDS WHERE P3's CRITERIA PUT IT:
STAND-WITH-RELATION -- one decision, said once, cited at D3 and AG-5 both;
the pronouncement is blocked no longer by absence but by THE NAMED LINK
(L2) and THE RIDING SENSES (L3). THE RESIDUE SHARPENS: from "no fact
exists" (b90) through "the fact's address is off the ray" (b93) to "the
fact EXISTS and CORRELATES; the remaining content is the LINK to the
value-bearing boundary line + the senses" (this act).
THE CAMPAIGN STATE RESTATED: the one-construction campaign ADVANCES INSIDE
ITS ONE CONSTRUCTION -- the extension is BUILT and two of its three
readings landed their registered branches (D1 the fact; D3 the relation);
the third (D2) named its step exactly. The remaining research content:
G-EXT-3 (the epsilon-read of the built trace), L2 (the boundary-line link
-- T5's archimedean half), L3 (the SOT sense at its owner), N-EXT-2 (the
boundary license -- THE VALUE CLAUSE'S NAMED FRONTIER, filed at the
sequence per the FOOT), and the standing tags. THE SEVENTH SEAM CLOSE'S
DEBT: row 63 + this act's arc, owed at the cadence.

HIT RECORD: P0 PASS as registered (structural, said so); P1 branch (a) as
registered WITH the pre-registered I-15 precision (nothing smoothed -- the
gauge reading was fixed before the computation); P2 PARTIAL at the FIRST
pre-fixed candidate (G-EXT-3) exactly as the honest expectation stated; P3
the registered honest expectation landed exactly (L1 yes, L2 no, L3 no,
STAND-WITH-RELATION). The registered probe computed inside its derived
interval. Deviations: none. Divergences from the staged registration:
none. The surprise protocol: NOT FIRED (no beyond-registration event).
====================================================================================================
