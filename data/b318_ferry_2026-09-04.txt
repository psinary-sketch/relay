CONCURRENCY: SOLO (research seat; read every file through the
extract-to-disk step, never by paging raw text).

FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

THE FORCED SIGN -- the source's trace side is a square; test
the instrument against that, and decide what the mean-zero
column's sign change is, one act. Scope: a COMPUTATION on the
instrument as certified (the space, not the unit; no unit
used) and a DEFINITIONAL decision; NO aggregation stated, M-2
owed under its cap; the inequality's other side is NOT
evaluated; normalizations before counts, and the square form
written in the source's own operators with each convention
quoted once; every number with two-axis convergence and its
rank at each frame printed beside it; the noise-floor gate in
the path; every reported number from a committed tool with
fixtures; needles from the extract file; stem sweep at
extended scope; hedge audit; registration term-scanned,
satisfiability-checked, sealed, no counts predicted; the suite
re-run after the push; nothing about the h2 identity beyond
the register sentence exact; nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
the kernel-coverage gate restated as failing; push anything
ahead of origin, saying so if nothing is.

COMPONENT 1 -- THE CLASS OF THE VARIANT, read off directly: its
moment in the source's sense (eq. 54) at every cell; whether
its support lies inside the source's window (eq. 53) at every
cell; whether it is of the autocorrelation form in the
source's sense -- decided by unfolding the corpus's construction
of the variant against the source's definition of the square
form, not by the shared word. Verdicts per condition.

COMPONENT 2 -- THE SQUARE, computed: the source's trace side in
its square form -- the smeared scaling operator times its
adjoint, compressed, traced -- for the variant at the same
cells, same frames, same quadrature as b317; and beside it,
b317's smeared-trace column recomputed by the same code path.
A positive control: at a frame small enough to decide exactly,
the square form's nonnegativity shown as arithmetic. Report
per cell: the square, the smear, their difference, the rank.

COMPONENT 3 -- THE DECISION: if the square is nonnegative
everywhere and the smear is not, the two are different
objects and the first differing constituent between the
corpus's smear and the source's square is named -- the
instrument's smear is then not the source's trace side and
b317's numbers are re-labelled as what they are, WITHOUT
b317 re-verdicted; if the square itself goes negative, the
truncation has lost positivity and the rank instability is
the named cause, with a rank-stable refinement scheme
specified (fix the rank, refine the grid; or the source's
eigenvalue-one characterization used to select a stable
subspace) and NOT built; if the variant fails Component 1's
form condition, the sign change is not a violation of anything
and the act says so.

COMPONENT 4 -- THE REACH, honestly: which cells, if any, both
axes now agree on inside the sealed bar with the rank stable
across the refinement; if none, the band statement stands and
the rank-stable scheme is filed as the instrument's next
build.

CLOSING: the correspondence rows by the idempotent tool; every
result keyed; the object's conditions restated with the
archimedean leg derived-not-confirmed; M-2's row unchanged
under its cap; the seam's debt item 1 restated; the patent
clock restated; the window-class order updated with Component
1's findings; the in-flight register; the ledger census; the
membership act named as next. The navigator's expectation
registered here, normalizations first: the square is
nonnegative and the smear is not the square -- the instrument's
smearing differs from the source's trace side by a constituent
this act will name; refutable by a square that goes negative
or a smear that matches it. Registration gate; index queried;
forced-sign keyed. Components ordered. THE SHADOW: only if the
small-frame nonnegativity is finite-decidable and carries its
scope; say which.

Bank: data/b318_the_forced_sign.txt. Deviation rule standing.

EXECUTION: ferry scan first; the extract step for every read;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite, re-run after the
push; pins by ls-remote across all three repos; the hook if
PLACE-papers is touched; mirror if it moves. STOP.

FOOT: after this act the instrument has been tested against
the one thing the source guarantees for free, the sign change
has a named cause, and the corpus knows whether its smear is
the source's trace side or a cousin of it; the membership
decision follows; h2 stands exactly where the deposit left it;
locks last.

paste ends (part 1 of 1)
