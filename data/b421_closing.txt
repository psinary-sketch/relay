**RECEIPT CONFIRMED IN FULL. ACT b421 BANKED, PUSHED AND READ BACK.**

**THE FINITE AMBIENT IS BUILT: PROVED, WITH AN EMPTY AXIOM PROFILE, ON THE THIRD OF THE SIXTEEN PROBES b418'S BUDGET ALLOWS.** The registration was locked before any write, at `a245bee5…3b42`: 8 gates read, 8 passing, 4 by digest. `GridTrace.grid_trace_is_signed_count` (`Core/GridTrace.lean`) names both of its objects in its statement, `gridTrace p n t = B329.signedTrace p n t`, for every `p ≥ 1`, every `n` and every `t ≥ 1`.
- The first object is the model's counting form.
- The second is the trace of a grid operator the module defines: `T_t = q·A_t − B_t`, where `A_t` is the scaling map read between off-ball points and `B_t` is the same map followed by the fibre-sum modulo `q`. Each trace is the sum of its diagonal.

**Its docstring says the source's `Tr(θ(t)Π)` is not this object,** that b310's reading stays uncompiled, and that the source governs where the ferry differs. The kernel prints `'GridTrace.grid_trace_is_signed_count' does not depend on any axioms`, alongside six clean helpers. The regenerated profile grows from 607 to 614 lines, and the prior profile is a true byte prefix. AllPrints changed by one import inserted after the last import and seven prints appended, nothing else, which is b420's corrected form. No existing kernel file moved. The run took 8.2 of 150 minutes, and no Mathlib was imported:
- probe 1 mapped which library lemmas carry `propext`;
- probe 2 failed only at `beq_self'`, because Nat's `==` is the decide-based `BEq`;
- probe 3 closed it.

**(N1): both clauses MET** — proved within the budget, and no Mathlib import. The step from the source's space down to the grid, b420's part (ii), is unchanged: still not statable in the kernel's present character.

**Component 2, hypothesis 1's specification, priced under both definitions the record holds.** The keystone's §2.1 fixes classical first-order logic, and Definition 2.1 asks for a finite specification with M as its *unique model up to isomorphism*. **By the upward Löwenheim–Skolem theorem, which is the seat's citation and which the keystone names nowhere, no infinite structure has that property.** So under Definition 2.1 as written, the specification is **NOT SUPPLIABLE**, for the test functions and for ξ alike. Which reading of *unique model* the keystone intends is routed to you, and b407 is not re-verdicted. Under the monograph's own definition, which carries no uniqueness clause, the specification is writable in its clauses (i) and (ii), unverified in (iii), and not written. The survey's single hit is b407's trail, not a specification. **(N2): MET.**

**Component 3, the span.** The tool counts **9 acts with this act and 8 without**. Against (R1)'s threshold of nine, **the fold of b413–b421 is named for b422, and not run.** **(N3):** its first clause, *counts eight*, is MET only without this act and REFUTED at the act's own count, as the face fixed in advance. Its second clause, the fold named for b422 and not run, is MET.

**The closing's added line: the navigator's three conflations at b420, entered as the navigator's with the seat's corrections** (`data/b421_conflations.txt`, and the trail):
1. b419's theorem was described in the words of a derived result. The theorem is a counting identity with no test function, dimension or sign.
2. The compressed trace was treated as the finite-place term. That term is `W_p`, and *"the arithmetic is in the distribution, not in the compressed trace."*
3. The map was paraphrased as *"no crossing at any height"*. The map says *"a passed test over a grid at this reach and nothing more."*

In all three the source governs.

**The records:** the suite reads 61/61 pre-push and 61/61 post-push. Its first run is kept: one arm looked for the tool's file name where the tool prints its own header. Correspondence row **270**, found by its marker; key `the-finite-ambient` PASS; the desk swept 15 items and closed 3. Both closing censuses read TOTAL MISSING 0.

**The four lists stand OPEN:**
- **LIST 1**, the rows citing at a ref nobody can name (b373);
- **LIST 2**, the rows grading a declaration classified absent (b373);
- **LIST 3**, the undated figures (b374);
- **LIST 4**, the bibliography entries nothing cites (b374).

Their trigger is the ruling on which test governs, or any disposition on the four lists.

**Pins by `ls-remote`:**
- relay `df1cb0d`→`f690c00` (plus this closing commit)
- PLACE-papers `743be4a`→`2249436`
- SIDE-global-section `5c72065`→`d8acb1d`
- TECHNE-Core untouched

The mirror is rebuilt and CLEAN ON ALL THREE CLAUSES at `2249436`. The hook is not owed, since no guard changed. **h2 where the deposit left it.**

---

**THE NEXT FERRY AS DRAFT — NAVIGATOR EDITS**

> **CONCURRENCY: SOLO (research seat).** Standing text as b421, carrying A2 and (R26)–(R36). Orientation-layer citation: correspondence row 270 (by its marker), `data/b421_c3_span.txt`, and `data/b421_c2_specification.txt`. Both instrument lanes parked; the KERNEL lane open to read; the wave parked; nothing deposits.
>
> **ACT b422 — THE FOLD, b413 TO b421.**
>
> **RULING SLOT (R37), THE AUTHOR'S TO FILL OR LEAVE EMPTY** — which reading of Definition 2.1's *unique model up to isomorphism* the keystone intends: as written, internal to ZFC, or second-order. If filled, the keystone is annotated by append only, its original sentence kept.
> **COMPONENT 1** — the nine acts folded into `FINDINGS.md` by the fold generator's pattern, the span re-counted by tool at the act's own run. Every statement about the object the span added is counted and graded at its owner's grade: b419's `smear_general` and b421's `grid_trace_is_signed_count` among them, each with the scope its docstring gives it.
> **COMPONENT 2** — the three conflations b421 entered, carried into the fold's own section as the navigator's, with their corrections.
>
> The navigator's expectations: **(N1)** the fold carries at least two compiled statements about the object, the first fold since b370 not at zero; **(N2)** no statement in the span moves a grade. Each refutable by a printed result. **h2 where the deposit left it.**
