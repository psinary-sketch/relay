====================================================================================================
b421 COMPONENT 2 -- WHAT A SPECIFICATION OF THE TEST-FUNCTION STRUCTURE WOULD HAVE TO STATE.
====================================================================================================

### WHAT IT WOULD HAVE TO DESCRIBE -- THE SAME UNDER BOTH DEFINITIONS.
  the carrier, as the source states the class (b400, verified at digest) :
      | ### **PROPOSITION C.1 CARRIES NONE** (page index `51`): *"...a finite set disjoint from Z and
      | containing {0,1}, then RH <=> SUM_v W_v(g*g^#) <= 0 for all g in C_c^inf(R*_+) with g-tilde(z) = 0
      | for all z in F."*
  So the specification must name: the carrier -- smooth, compactly supported functions on the positive
  reals, with the vanishing set `F` fixed (the arc fixes it by its own vanishing conditions, `i/2` and
  `0`); the operations the target reads -- the convolution `g conv g-bar^#` and the involution; and the
  functional`s terms place by place, `W_p` by the source`s eq. (149) and `W_inf`, with the sum over
  places, which is the interface. ### Nothing here is new: it is a list of what row S1 already names.

### UNDER THE KEYSTONE`S DEFINITION 2.1, IN THE SETTING ITS OWN §2.1 FIXES.
  the setting :
      | Throughout, we work in classical first-order logic. Let 𝓛 be a first-order language. A *structure* M
      | over 𝓛 interprets 𝓛's symbols in a domain |M|.
  the definition :
      | **Definition 2.1 (Determined system).** A structure M is *determined* by specification S if S is a
      | finite set of 𝓛-sentences that has M as its unique model up to isomorphism.
  the keystone names Lowenheim-Skolem, categoricity or second-order semantics : False
  ### ### **THE SEAT`S CITATION, NOT THE RECORD`S -- A CLASSICAL THEOREM OF FIRST-ORDER MODEL THEORY:** by
  the upward Löwenheim-Skolem theorem, a set of first-order sentences with an infinite model has models of
  every larger cardinality, and models of different cardinalities are not isomorphic. ### So no set of
  first-order sentences, finite or not, has an infinite structure as its UNIQUE model up to isomorphism.
  ### The test-function structure is infinite -- the class contains infinitely many functions.
  ### ### **PRICE UNDER DEFINITION 2.1, READ AS WRITTEN: NOT SUPPLIABLE.** No specification of any length
  can have the property the definition demands, so the missing specification is not a piece of writing
  nobody has done; it is one no writing can do. ### Readings under which it could be supplied --
  uniqueness proved INSIDE ZFC (as ZFC proves any two complete ordered fields isomorphic), or second-order
  semantics -- ### **ARE STATED IN NEITHER DOCUMENT**, and choosing one is the author`s.
  ### ### **AND THE BEARING, ROUTED AND NOT RULED:** the same citation reaches every infinite structure,
  `xi``s among them. b407 read hypothesis 1 as met for `xi` on the keystone`s own assertion; ### **THAT
  ACT IS NOT RE-VERDICTED**, and the question -- which reading of *unique model* the keystone intends --
  is routed to the author.
    b407`s reading, quoted :
      | ### Four of Theorem 3.1’s five hypotheses are ### **MET AT THE CORPUS’S OWN ### ### OBJECT, IN THE
      | DOCUMENT’S OWN WORDS**: ### `xi` is determined; the interface is ### the product formula; the target
      | parameter is the clause’s own; and ### **`kappa = 0` ### ### IS ASSERTED BY THE RECORD ITSELF** --
      | *"For ξ, I is essential and ### κ(σ, I) = 0."*

### UNDER THE MONOGRAPH`S FORMAL DEFINITION.
  the definition :
      | **Formal:** System X is determined by specification S if (i) S is finite and explicit, (ii) S
      | contains no free parameters, (iii) all properties of X are logical consequences of S.
  and the one it writes, for xi :
      | For ξ(s): the specification is θ(τ) = Σ exp(πin²τ), followed by ξ(s) = Mellin transform of (θ − 1)/2
      | with explicit gamma factors. The formula contains no adjustable parameters. No human choices enter.
  ### This definition carries ### **NO UNIQUENESS-UP-TO-ISOMORPHISM CLAUSE**, so the citation above does
  not bear on it. ### Clauses (i) and (ii) -- finite and explicit, no free parameters -- ### **ARE
  WRITABLE**: the list above, with `F` fixed by the arc`s own vanishing conditions rather than left free.
  ### Clause (iii) -- *all properties of X are logical consequences of S* -- is a demand on what follows
  from the list, and the record establishes it for no structure; it is priced as ### **UNVERIFIED, NOT AS
  MET.**
  ### ### **PRICE UNDER THE MONOGRAPH`S DEFINITION: WRITABLE IN (i) AND (ii), UNVERIFIED IN (iii) -- AND
  NOT WRITTEN.**

### WHETHER ANY DOCUMENT ALREADY HOLDS ONE -- EVERY HIT OF THE SURVEY`S SEARCH, HAND-READ.
  the one hit, OPEN_TRAILS.md :
      | **And the resemblance is exact, which is precisely why it must not be filed as an instance.**
      | Definition 2.5 says a step factors through `I` when it references only *"cumulative invariants of I
      | (integrals, densities, or measure-theoretic quantities averaging over I), but not individual-element
      | specifications requiring P-information to cross I"*. The corpus’s criterion is a **sum over the
      | places** — a cumulative invar
  ### it is b407`s trail on the resemblance between Definition 2.5 and the E0 gate; it names no carrier,
  no operation and no place term. ### **NOT A SPECIFICATION.**
  documents holding the specification : 0

### ### **`(N2)` -- *no document holds the specification Component 2 asks for* -- ### **MET**: the one hit is not one;
### and under Definition 2.1 read as written, none could be.
====================================================================================================
