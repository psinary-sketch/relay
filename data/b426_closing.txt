**RECEIPT CONFIRMED IN FULL. SORTIE LEG 1, ACT b426, BANKED, PUSHED AND READ BACK.**

**THE FALSIFIER RE-READ BY ADDRESS: THE COLLABORATION'S OWN SECOND-RELEASE PAPER STATES 2.8–4.2σ, SAYS *CHALLENGED* AND NEVER *EXCLUDED*, AND UNDER `(R38)` IT SPANS THE THREE-SIGMA LINE WITHOUT REACHING FIVE.** The registration was locked before any write, search or fetch, at `35c132d4…6c92`: 8 gates read, 8 passing, 4 by digest.

**The two rulings, carried as quoted and neither extended.** `(R38)` keys the condition on a threshold, not a word — UNDER PRESSURE at three sigma or more from any source read at address; FIRED at five sigma from the collaboration's own paper or two independent analyses each at five; UNDECIDED below three; a source's own word quoted and never governing; disagreeing sources combined by the **weakest** reading. `(R39)` holds `p2-d6` in place and gives its status a note.

**THE ADDRESS, LOCATED BY THE RULE THE FACE FIXED BEFORE ANY SEARCH.** `arXiv:2503.14738` — *DESI DR2 Results II: Measurements of Baryon Acoustic Oscillations and Cosmological Constraints*, **DESI Collaboration** first in the author field. Three queries were declared; **two ran and the third refused after eight attempts** (`A3`, HTTP 429), and 93 unique results were printed, 3 qualifying on all three tests, 1 naming cosmological constraints. Abstract page sha256 `46a596a0…c1ca`; PDF sha256 `1e82f26e…7841`, 41 pages, its text on disk.

**THE THREE PARTS, RESTATED UNDER `(R38)`:**

- **(a) IS THE MEASURED QUANTITY THE ONE THE LANE FIXED? — SAME QUANTITY.** The paper *"will primarily use the so-called Chevallier-Polarski-Linder (CPL) parametrization"*, and *"the ΛCDM limit ( w0 = −1, wa = 0) lies at their intersection"* — which is the lane's `w = −1`. The lane's own reach stands beside it: FANO line 246 leaves the residual's `w` unspecified.
- **(b) WHAT THE CONDITION NOW SAYS.** The lane's sentence is unchanged — *"Evolving dark energy refutes the decomposition"* (`FORMATION_DISTANCE.md` line 160) — and `(R38)` supplies the thresholds it never stated.
- **(c) AT THE PAPER'S OWN FIGURE — SPANNING UNDECIDED / UNDER PRESSURE.** *"the preference for a dynamical dark energy model over ΛCDM ranges from 2.8−4.2σ depending on which SNe sample is used"*, and in the body *"The significance of rejection of ΛCDM is 2.8σ, 3.8σ and 4.2σ … and 3.1σ for DESI+CMB without any SNe"*. At the low end the condition reads **UNDECIDED**; at the high end **UNDER PRESSURE**. **Five sigma is not reached at either end, so `(R38)`'s FIRED clause is not reached by the one source that could reach it alone.**

**ON EXCLUSION, THE PAPER'S OWN WORD IS `CHALLENGED`.** *"Unless there is an unknown systematic error associated with one or more datasets, it is clear that ΛCDM is being challenged by the combination of DESI BAO with other measurements and that dynamical dark energy offers a possible solution."* The stem *exclud* occurs 9 times in the PDF and none of them says the cosmological constant is excluded; the nearest is a figure-caption fragment whose subject the text layer drops, printed rather than paraphrased.

**b425's THREE, RE-SCORED UNDER `(R38)` FROM b425's OWN BANKED RECORD:**

| source | figures b425 banked | b425's verdict | under `(R38)`, low → high |
|:--|:--|:--|:--|
| arXiv:2609.10567 | 2.1–3.7σ | UNDECIDED | UNDECIDED → UNDER PRESSURE (**spanning**) |
| arXiv:2609.10133 | 2.50σ | **FIRED** | **UNDECIDED → UNDECIDED** |
| arXiv:2609.05321 | 3.4–4.0σ | UNDECIDED | UNDER PRESSURE → UNDER PRESSURE |

**THE ONE b425 READ `FIRED` ON ITS OWN WORD READS `UNDECIDED` UNDER `(R38)`**, because 2.50σ is below three. That is the ruling doing exactly what it was minted to do.

**THE COMBINATION, AND THE RULE'S OWN DIVERGENCE, PRINTED RATHER THAN REPAIRED.** Over the four sources read at address: `(R38)`'s *"from any source read at address"* clause reads **UNDER PRESSURE**; its *"combined by the WEAKEST reading, not the strongest"* clause reads **UNDECIDED**. **The two clauses of `(R38)` do not agree on a mixed set. The divergence is the rule's and not the set's.** The act carries the **weakest** as the combined reading, because it is the ruling's own last sentence on combination, prints the other answer beside it, and **routes the divergence to the author unruled**.

**WHAT WAS WRITTEN INTO THE LANE, AND NOTHING ELSE.** `FORMATION_DISTANCE.md` gained **one inserted line** after line 160 carrying `(R38)`'s clause, with line 160 **byte-identical** and the pin's text equal to the new text less that one line. `REGISTRY.md`'s `p2-d6` row gained a note **inside its status cell**, all seven other cells byte-identical, the status cell still beginning `READY`, the register sentence unchanged, and the file's line count unchanged. Each was preserved verbatim with its sha256 in a record written **before** the edit. **No lane verdict is changed by the seat, and `p2-d6` does not move.**

**(L1), its two clauses apart — and each clause's own halves apart, because they do not agree:**
- *between three and four sigma* — **REFUTED** (the paper states 2.8–4.2σ, straddling both ends of that band).
- *excludes nothing* — **MET**.
- *the lane reads UNDER PRESSURE* — **REFUTED** under the clause the act carries (weakest: UNDECIDED); **MET** under the ruling's other clause (any source: UNDER PRESSURE).
- *and not FIRED* — **MET**.

**THE SEARCH'S OWN GAP, SAID RATHER THAN HIDDEN.** Query `A3` never ran: the listing refused it eight times (429/503). Run 1 of the locate refused on **all three** queries, sixteen refusals in total, and both runs are on disk. The address was found by `A1` and `A2`. **A third query that never ran is a gap in the search, and it is printed as one.**

**Suite 60/60 pre-push and 60/60 post-push.** Both censuses **TOTAL MISSING : 0** at step zero and at close. Pins by `ls-remote` through the roster: **REPOS HARD-FAILING : 0**, all four equal, 0/0. The HELD commit's files are absent from the pushed tree (`0` of them in `origin/main`). The hook was exercised in **3 of 4** repositories and **SKIPPED in relay**, whose tree carries two uncommitted paths — `data/b369_hooks.txt` and `data/b369_hygiene.json`, the hook tool's own outputs, modified before this act began and named in no write list of this act. The mirror was rebuilt AFTER the commit and verified **CLEAN ON ALL THREE CLAUSES** at source HEAD `f1a5d22`.

**The four lists:** **LIST 1**, **LIST 2**, **LIST 3**, **LIST 4** — all **OPEN**. Trigger: the ruling on which test governs, or any disposition on the four.

**Desk: 15 swept, 2 closed, 13 standing.** Closed: the falsifier re-read by address; and b425's three limits (a word, a window, no combining rule) — `(R38)` answers all three, and opens a fourth in their place: **the combining rule's own two clauses diverge.**

**CORRESPONDENCE ROW : 275. KEY : PASS.** Pins: relay `48a3bb0`, PLACE-papers `f1a5d22`, SIDE-global-section `f14f0ac`.

**0 grades moved. 0 premises discharged. 0 doors restated. 0 routes proposed. 0 kappa measured. 0 lane verdicts changed. 0 rows of `FACES_LEDGER.md`. 0 fits, samples or likelihoods; 0 significances converted. 0 rules struck or amended by the seat. 0 kernel files touched. 0 deposit actions; the only hosts reached were the arXiv listing's. And h2 where the deposit left it.**
