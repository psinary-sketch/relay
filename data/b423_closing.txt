**RECEIPT CONFIRMED IN FULL. SORTIE LEG 1, ACT b423, BANKED, PUSHED AND READ BACK.**

**THE (R37) QUESTION READ: BOTH READINGS CONFIRMED, NEITHER VERDICT MOVED, NOBODY RE-VERDICTED.** The registration was locked before any write, at `c8bd37fc…f4f5`: 8 gates read, 8 passing, 4 by digest.

**b407 — hypothesis 1 for ξ, *"`xi` is determined"* — CONFIRMED.** Deciding sentence, §10.2: *"the specification is a set-theoretic definition and uniqueness is a statement inside the ambient theory, which is how the natural numbers, the reals and ξ are characterized in practice."* The monograph writes that definition (θ, then the Mellin transform). Under the first-order reading b421 found Definition 2.1 met by no infinite structure; under the internal reading the ruling names ξ itself.

**b420 — hypothesis 1 for the structure whose elements are the class's test functions, *NOT SUPPLIED BY THE RECORD* — CONFIRMED.** Deciding sentences: §10.2's *"the specification is a set-theoretic definition and uniqueness is a statement inside the ambient theory"*, b421's *"documents holding the specification : 0"*, and the source's own *"a finite set disjoint from Z and containing {0,1}"*. The source (CC Prop. C.1, eq. 149, W_inf) defines the pieces with `F` left free, and no document writes the specification. A search of 350 tracked PLACE-papers files found 8 hits, all hand-read, none a specification. **What lapses is b421's extra ground, not b420's reading:** under the first-order reading the specification was NOT SUPPLIABLE; under the internal reading it is suppliable and not written.

**Both verdicts rest on hypothesis 5, the proof's form**, so no reading of hypothesis 1 could move either; 0 re-verdicts. **Routed to the author, not ruled:** read internally, hypothesis 1 is met by any structure once its definition is written, so it no longer separates one structure from another. Whether the lemma applies to anything the witness arc meets turns on the fifth hypothesis, which failed at b407 and at b420 on form. The keystone is not edited.

**(L1):** clause *b407's reading CONFIRMED* — MET; clause *b420's reading CONFIRMED* — MET, with b421's lapsed ground named apart.

Runs kept: the reading record's run 1 and the report's runs 1 and 2 (a wrapped anchor, then an end anchor equal to its start); the suite's run 1 (one arm read the wrapped record raw). The suite reads 46/46 pre-push and 46/46 post-push. The first push attempt was refused by the pre-push hook in all three repositories: the push branch was created but not checked out, so nothing left the machine and every read-back still equalled its pin. The push then went out from the checked-out push-b423. Correspondence row **272**, found by its marker; key `the-r37-question-read` PASS; the desk swept 14 items and closed 1. Both closing censuses read TOTAL MISSING 0.

**Pins by `ls-remote`:** relay `de33805`→`15915dd` (plus this closing commit); PLACE-papers `53e9cd3`→`a00c72c`; SIDE-global-section `c803412`→`527c861`; TECHNE-Core untouched. The mirror is rebuilt and CLEAN ON ALL THREE CLAUSES at `a00c72c`. The hook is not owed, since no guard changed. **h2 where the deposit left it.**

**CHECKPOINT: LEG 2 (b424), THE WITNESS ARC AT SITE (i), IS NEXT.**
