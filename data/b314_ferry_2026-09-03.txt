CONCURRENCY: SOLO (research seat).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE FOLD AND THE COLD CLONE — b307 through b313 into the
findings document under its own rules, and the kernel
certified from a clean clone by a build that inherits nothing
from the machine that made it, one act. Scope: FILINGS and a
CERTIFICATION TEST; NO grade moves, NO act re-verdicted, no
new mathematics; obstacles QUOTED and emitted by the
constructive gate against originating acts; the ERRATA entry
written per the author's ruling on the E1 precedent, owner
files byte-identical; every reported number from a committed
tool with fixtures; needles pulled from emitting files; stem
sweep at extended scope; hedge audit; registration
term-scanned, satisfiability-checked, sealed, no counts
predicted; the suite re-run after the push; PLACE-papers is
touched, so the hook fires and the mirror is rebuilt after the
commit and verified on all three clauses; nothing deposits.

THE RULING (CONVENTION ERRATUM), the author's, ratified by
this paste and strikeable: "E1 pattern — an internal-record
ERRATA entry for the remainder's normalization convention:
the owner instrument untouched, b313's bank the correction of
record, and banked remainder values quotable only with their
convention named."

STEP ZERO: the ferry scan; the ledger census with its scope;
push anything ahead of origin, saying so if nothing is.

COMPONENT 1 — THE FOLD, seven acts, each entry with its grade,
owning act and scope sentence, every obstacle a verified
quotation: the prior fold's filing and the census that
licensed a struck phrase; the local-field instrument (the
model a point, the instrument the plane; the artifact retired
for it alone); the scaling trace (zero, alive operator, the
fixed-point sentence); the collapse (one surviving term, no
arithmetic in it, the finite side's closure as one mechanism);
the identity's neighbourhood (the mechanism does not type at
infinity — dimension is the step; the source's trace side as
distribution plus remainder); the remainder (DIFFERENT by one
constituent, the exponent, hidden by a check at a zero; one
identity, two conventions, one file); the exponent (the
residue shrinks by a sixth and stays; the rate moves a full
power; the unasked column and its two cautions). Also folded:
the arc's corrections to its own readings, and the three
sealed predictions that failed at a normalization, as one
row.

COMPONENT 2 — THE COLD CLONE: on a path the corpus does not
own, clone the kernel repository fresh at its current pin;
build with the pinned toolchain from a clean cache; regenerate
the certification profile from source; compare byte-for-byte
against the banked profile at HEAD, line-ending and byte-order
hazards checked on raw bytes; report the count and every
difference if any. Then the coverage sweep the record has
carried: enumerate the modules outside the certification file,
build each, print its terminals' axiom profiles, and report
any terminal that has been sitting compiled and uncertified —
found or not found, with the reason. Then every instrument's
reproduction controls run cold in the fresh clone of the
relay: pass or the disagreement at full prominence. Nothing
is repaired in this act; findings are filed.

COMPONENT 3 — THE ARC AS ONE STATEMENT, at the grade the acts
support, scope beside it: the finite side is arithmetically
silent on the object's side by one mechanism; the primes
enter only through the distribution; the archimedean trace
side is distribution plus remainder in the source's own
theorem; the corpus's remainder differs from the source's by
an exponent that accounts for a sixth of the residue; the
rest of the residue is owed, with the vectors-outside-the-
object hypothesis named as a hypothesis; nothing about the
identity, h2, or the roster follows.

COMPONENT 4 — THE LORE AND THE SUITE, consolidated with
incidents: check normalizations before counts (three sealed
predictions); a check taken at a zero cannot see a
multiplicative factor; one identity, two conventions, one
file; asserting a negative from a search still running; a
green gate with no content is reported as such; re-run the
suite after the push; an exception list is a list of places
a check has agreed not to look. Mechanized and judgement
lists kept apart.

COMPONENT 5 — THE DESK, one list: M-2 under its cap with its
narrowed space; the object's three conditions; the truncation
nobody owns as the archimedean instrument's central cost; the
calibration read and the rate re-derivation as the next two
reads; the unasked column's work-order; the window ruling
recorded and unapplied; the keystone re-read; hook
persistence; the patent clock, restated as unconfirmed on the
record — the one item with a date on it.

CLOSING: the ERRATA entry written, read back, its ID recorded;
the correspondence rows by the idempotent tool; every result
keyed; the fold purely additive with the no-grade-moved check
mechanical; the seam's debt item 1 restated; the in-flight
register; the ledger census with its scope; HANDOFF's license
earned by the census or the phrase not used. Registration
gate; index queried; fold rows and cold-clone keyed.
Components ordered.

Bank: data/b314_the_fold_and_the_cold_clone.txt. Deviation
rule standing.

EXECUTION: ferry scan first; owners read at content first;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite, re-run after the
push; the hook exercised with its result reported either way;
the mirror rebuilt AFTER the commit and verified on all three
clauses; pins by ls-remote across all three repos. STOP.

FOOT: after this act the seven acts are one section, the
kernel is certified from a clone that inherited nothing, the
coverage question has an answer, and the desk carries the two
reads that the exponent act left owed; h2 stands exactly
where the deposit left it; locks last.

paste ends (part 1 of 1)
