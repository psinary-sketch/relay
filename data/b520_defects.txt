    (a) ### **READING (3) WAS NOT PRICED AGAINST THE BANKS THAT COULD PRICE IT.** ### The face read "the tail against the cell`s
        bound" as `E_tail` against `B' = E_u + E_k + E_round`, because `B` contains the tail; b519`s own banked variant-(B)
        cells -- readable before the seal -- have Q0`s `E_tail` above `B'` at EVERY ladder width, by 1.0e3 at the widest to
        1.2e5 at the narrowest. ### So Q0`s reach ended at the first width of this grid (a = 15, `E_tail / B'` = 789) and
        NO Q0 CELL OF THIS ACT IS READ. ### The rule is the registered one and is not changed after the run; halting on it is
        the act`s result. ### The same read of b519`s bank is printed in the components as a declared post-run read of a
        prior bank.
    (b) ### **b518`S AND b519`S Q0 VERIFICATIONS WERE BOUNDS DOMINATED BY THE TAIL MAJORANT** -- valid upper bounds, the sign
        decided beyond them by up to 3e6, but the verification is the tail`s, not the quadrature`s. ### Found here, not
        edited there; entered in this act`s trail.
    (c) ### **THE REPORT ASSUMED A WIDEST Q0 CELL** and crashed when the registered reach left Q0 none; repaired to say, in
        (R127)(2)`s words, that no Q0 cell was read and why, before the desk or the suite ran on it.
    (d) ### **(S2)`S SCORER FIRST READ Q0`S ROWS ONLY** -- an empty set here; the realized growth is the window`s at the pair`s
        delta and every width read carries it (xi`s 46), so (S2) is scored over every width read, the desk saying so.
