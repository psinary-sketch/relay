FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

THE DIFFERENCE -- is the corpus's cell-level imbalance the
same object as the source's arithmetic-minus-trace difference,
decided by definitions, one act. Scope: READS and a
DERIVATION; the resemblance is NAMED AND REFUSED AS EVIDENCE
at every use -- a one-signed difference at cells and a
positive inequality at infinity share a shape and nothing
else until a definition bridges them; NO aggregation stated,
M-2 owed; nothing about the h2 identity beyond the register
sentence exact; b305's identification (the corpus's prime
summand IS the source's local term, factor for factor) is
carried, not re-derived; the demand is per-index at one-level
primes by b304 and no smearing over the index is used;
nothing deposits.

STEP ZERO: the ferry scan; push anything ahead of origin,
saying so if nothing is; extend the stem sweep's scope to the
correspondence table (the hole b305 named) and run it,
reporting hits.

COMPONENT 1 -- THE SOURCE'S DIFFERENCE, quoted whole: the
statement of its Theorem 1 as an inequality between the
arithmetic distribution and the forced-positive trace, with
each side's definition unfolded -- the test-function class,
the smeared operator, the compression, the trace, and the
places summed over. State exactly which places enter and
whether the finite places enter through eq. (149) or are
excluded by the support condition, quoting the source's own
sentence about primes not being involved.

COMPONENT 2 -- THE CORPUS'S DIFFERENCE, pulled from emitting
files: the fourth face-off's L - R = -(E2even + junction)
with every constituent unfolded to its owner (the archimedean
terms, the junction PR - Theta_q, the test function and its
cutoff, the cells), and the sign result carried at its grade
(junction signed by theorem; E2even at bench).

COMPONENT 3 -- THE DECISION, by definitions: for each
constituent of the source's difference, name the corpus
object that would correspond and decide SAME / DIFFERENT /
NO COUNTERPART -- in particular: (a) is the corpus's
archimedean trace the source's compressed smeared trace, or
a different functional (b291's finding that the corpus's
instrument vectors lie outside the object's space bears here
and is quoted); (b) is the corpus's prime side the source's
places-sum at the finite places (b305 says yes for the
summand; the CUTOFF and the test-function class are the open
halves -- decide them); (c) does the source's difference have
a finite-window version at all, and if so is it the corpus's
cell. Verdicts: (SAME OBJECT AT A WINDOW, derived, every
correspondence shown) / (DIFFERENT, the first differing
constituent quoted, at full prominence) / (UNDECIDED, the
resisting step named).

COMPONENT 4 -- WHAT FOLLOWS, at exactly its scope: if SAME,
the corpus's one-signed imbalance is a finite-window
instance of the source's inequality and the junction's
derived sign is a derived version of what the source gets by
construction -- stated as a correspondence, NOT as evidence
for or against anything about the identity, h2, or the
roster; if DIFFERENT, the corpus's difference is its own
object and the source's method's applicability to it is an
open question, named. In every branch: no measurement is
disturbed and no grade moves.

CLOSING: the fold (b297-b306) named as next; the
correspondence rows by the idempotent tool with the notation
guard, read back; every result keyed; the object's three
conditions restated from the list; M-2's row unchanged; the
seam's debt item 1 restated; the in-flight register; the
patent receipts restated as unconfirmed on the record.
Registered in advance, written here as the seat's to use:
(F1) the corpus's archimedean trace is NOT the source's
compressed trace -- different functional; (F2) the corpus's
prime side IS the source's finite-places sum at the window,
cutoff included; (F3) the source's difference has no
finite-window version stated by the source. Each expected
true, each refutable by a quotation. No counts predicted.
Registration gate; index queried; the-difference keyed.
Components ordered. THE SHADOW: expected nothing -- reads and
a definitional decision; say so, checked not assumed.

Bank: data/b306_the_difference.txt. Deviation rule standing.

EXECUTION: ferry scan first; owners and the source read at
content first, page image where the text layer fails;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite; every reported
number from a committed tool with fixtures. STOP.

FOOT: after this act the corpus knows whether its month of
cell-level measurements has been computing the source's
difference or its own; the fold follows; h2 stands exactly
where the deposit left it; locks last.

paste ends (part 1 of 1)
