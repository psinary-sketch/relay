CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE MEMBERSHIP — which realization of the object's
archimedean unit the derivation was about, decided by
unfolding both, with the domain ladder run on its residual
first, one act. Scope: READS at owners and a DEFINITIONAL
decision, plus one measurement the ladder makes askable; NO
unit adopted or replaced — the derivation and the measurement
each stand at their grade until one constituent gives way,
and if none does the act says so; NO aggregation stated, M-2
owed under its cap; normalizations before counts; every
number with two-axis convergence and rank beside it; the
noise-floor gate in the path; every reported number from a
committed tool with fixtures; needles from the extract file;
stem sweep at extended scope; hedge audit; registration
term-scanned, satisfiability-checked, sealed, no counts
predicted; the suite re-run after the push; nothing about the
h2 identity beyond the register sentence exact; nothing
deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
push anything ahead of origin, saying so if nothing is.

COMPONENT 1 — THE LADDER ON THE RESIDUAL: b319 measured the
unit's membership residual constant at 0.4395 across four
GRID refinements; b321 measured the instrument's distance
from the object falling geometrically along the DOMAIN ladder.
Run the unit's residual along the same domain ladder, same
frames, stable rank. Report its course. If it falls at the
instrument's own rate, the residual is the truncation's and
the derivation is not contradicted by it; if it holds, the
residual is the realization's and Component 2 must explain a
fixed fraction. State which, from the numbers, before any
definition is unfolded.

COMPONENT 2 — THE TWO REALIZATIONS, side by side, each
unfolded to base objects: the derived unit as the deriving
act built it — the lemma placing it on the half-line, the
eigenvalue relation under the corpus's transform, the picture
it lives in, its normalization; and the instrument's
realization — the bench eigenfunction as the corpus holds it,
its even extension, the source's transform, the source's
inner product, the space as the stable-rank scheme selects
it. Neither described in the other's language. Then the first
differing constituent named, or (SAME) with the residual's
cause assigned to Component 1's finding, or (UNDECIDED) with
the missing statement typed.

COMPONENT 3 — THE VERDICT ON THE LEG, at exactly its scope:
the object's archimedean unit is in its space (derived and
now measured to converge), or the derivation and the
instrument are about different vectors (the constituent
quoted, and which vector the object's product leg actually
uses stated as a ruling item for the author), or neither is
yet decidable and the resolving power required is priced as
b321 priced the exponent. In every branch the object's
conditions are restated from the list.

CLOSING: the correspondence rows by the idempotent tool; every
result keyed; the lore gains the resolving-power rule with its
incident (a question is under-resolved, not open, when the
candidates sit closer than the instrument's distance from the
answer — and the price is the ratio); M-2's row unchanged
under its cap; the seam's debt item 1 restated; the patent
clock restated; the in-flight register; the ledger census;
the fold (b314 onward, eight acts) named as next and due, with
the keystone re-read after it. The navigator's expectation
registered here: the residual FALLS along the domain ladder
at a rate comparable to the instrument's own — refutable by a
flat course. Registration gate; index queried; membership
keyed. Components ordered. THE SHADOW: expected nothing; say
so.

Bank: data/b322_the_membership.txt. Deviation rule standing.

EXECUTION: ferry scan first; the extract step for every read;
registration satisfiability-checked, term-scanned, sealed;
components in order; full control suite, re-run after the
push; pins by ls-remote across all three repos; the hook if
PLACE-papers is touched; mirror if it moves. STOP.

FOOT: after this act the object's archimedean unit is inside
its space by derivation and measurement together, or the two
are shown to be about different vectors with the difference
named, or the question carries its price; the fold follows;
h2 stands exactly where the deposit left it; locks last.

paste ends (part 1 of 1)
