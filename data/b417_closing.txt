**RECEIPT CONFIRMED — PART 1 OF 1, IN FULL. ACT b417 BANKED, PUSHED AND READ BACK.**

The amendment arrived before any registration existed, so it sits on the face as arriving before the lock, and it governs this message: the slots and the fifteen are reported in separate paragraphs and never in one sentence.

Registration **LOCKED BEFORE ANY WRITE** at `53f6c571…5aea` — 8 gates read, 8 passing, 4 by digest. The kernel lane was used to read: 0 `.lean` files touched, 0 builds, and `FiniteSideSeal.lean` and `Classes.lean` carry the same sha256 before and after.

---

## THE SLOTS

### Component 1 — the defect report, for the author to rule on

| half | what it says, in its own words | where |
|---|---|---|
| **the sentence** | *"Three of four formation components are universal across IDS-amenable systems: n₂ = 3 by Chevalley–Steinberg, n₃ = 2 by bipartite complex analysis, n₄ = 0 by Schur's lemma."* | `MATTER_AS_ARITHMETIC.md`, lines 14 and 27 |
| **the tuples** | `classB := (3, 2, 2, 0)` and `classD := (2, 2, 2, 0)` — n₂ = 2 in both | `Classes.lean`, and the owner's own table at lines 40 and 42 |

**Both halves are in the owner's own document**, eleven lines apart. Under (R34) this seat does not say which half is wrong.

**Who cites what.** **Two** live documents cite the sentence: the owner, and `phase1.5/structural/AT_REST.md`. **Five** cite the disagreeing tuples without it. The first matcher found one citer; a second shape found AT_REST, and its residue was hand-read. Five more documents use the word *universal* beside the second component and assert something else, a claim across Dedekind zeta functions.

### Component 2 — the ratio at the four tuples

It matches all four figures the kernel's header prints. **n₂ = 2 is load-bearing for both B and D**: with 3 in its place, B moves from 9/32 to 1/27 and D moves from 1/4 to 4/81.

### Addition Two and (R34) — the two readings priced

| | **reading (a)**: the sentence is right | **reading (b)**: the tuples are right |
|---|---|---|
| the tuples | B → (3, 3, 2, 0), which is C's; D → (2, 3, 2, 0), which is A's | unchanged |
| distinct classes | **four named classes become two distinct tuples** | four |
| kernel figures | five move; `classA_distinct_from_classD` becomes false and could not compile | none move |
| the owner's figures | 9/32 and 1/4 move; D lands on A at 0.13σ, so *selecting the Arithmetic class* no longer selects one class | none move |
| what is edited | two declarations, and every figure above | the sentence, narrowed to its own theorem's hypothesis, *connected reductive symmetry groups*, which AT_REST already writes inline |
| **class A** | **4/81, 0.13σ — unmoved** | **4/81, 0.13σ — unmoved** |

The ferry expected two classes to become one. **The arithmetic finds two collisions.** Under reading (b), no live document says whether the substrates of B and D carry a connected reductive group, so their n₂ = 2 stays stipulated. **Neither reading is adopted. The author rules.**

A second disagreement, of another kind, is routed and not folded in. COMPLEX_ANALYSIS and MATTER place the genetic code, Shannon's system and Navier–Stokes at different tuples.

### Component 4 — the Core caveats

**One of the thirteen has been tested outside its own evidence**: (T1.4), by b414, at 4, 8 and 9. **Twelve are untested.** Six of the thirteen state a bounded condition; the other seven are scope disclaimers or library forms with nothing to leave.

### The expectations about the slots

- **(N1)**: its first clause is the author's and is not scored. *At least four cite it* is **REFUTED: two**.
- **(N2)**: **MET**.
- **(N3)**: **MET**, at one, and nearly vacuously.
- **(N4)**: **MET IN PREMISE, NOT IN CONCLUSION.** Four distinct pairs do need the second component to vary. But the header is computed from the same declarations, so it is the same half stated twice and cannot decide between them.
- **(N5)**: **REFUTED.** The only document arguing n₄ that asserts the sentence is MATTER, and it cites all four tuples. COMPLEX_ANALYSIS asserts a different universality, and BSD asserts none.

---

## THE TOOLS

**Component 3.** `b369_hygiene.py`, run broken, **wrote 0 files** — measured, not assumed. Repaired, it ran first against a captured snapshot. Its 3 contained writes were its own run records. **3 writes escaped: one object each into the stores of SIDE-global-section, PLACE-papers and SIDE-effects**, from the guard exercise it runs through `b304_hooks.py`, two calls deep. Its own source names none of those repositories. Live, it rewrote 2 prior-act records and created 1, and all three were restored. Git's own clean conversion returns the committed blob id for both records. `git status` still marks them modified, which is a stat-cache artifact with no content change. Its verdict, **FAILED**, is reported and not edited, because its success conditions were dated by its own earlier success. The 2 SIDE-effects backups were swept under the stated rule, **content lost 0**.

**Addition One.** *A broken tool is safe by being broken.* `SAFE_BY_BEING_BROKEN.md` is filed in TECHNE-Core beside DATED_ARM and GUARD_WITH_NOTHING_LISTENING, as local commit `4aaf600`, **not pushed** (0 origin-only, 17 local-only). It is mechanized as relay `tools/repair_snapshot.py`, whose fixtures pass in both polarities. **The harness found a defect in itself on its first live use**: it reported 2 files differing whose bytes equalled their blob, because it compared mtime after a restore. The harness was repaired, and both figures are kept.

The suite's first run failed three arms, **all three defects of the suite**: a backtick the fold strips, unescaped regex parentheses, and `G-NOBORROWEDBAR` catching `G-NO-SEAT-RULING` reading raw text. All three were repaired, and run 1 is kept.

---

## THE FIFTEEN

**Component 5.** All fifteen members of the placed Reader's set are still cited in a Prime-Core paragraph of a live document, and **none is dropped**. **One live document asserts the Reader's desert: CONSTANCE, in its own words.** Its own sentence says no combination of powers yields 43, which is 2⁴ + 3³. It is routed, not repaired.

**(R33), executed.** The Reader's head note grew **one** clause. It says the desert sentence is false, names the seven (43, 59, 67, 73, 83, 89, 97), and cites the Reader's own predictions section as naming 43 and 67. b416's three clauses survive byte-for-byte, and **the body is byte-identical** (sha256 `a090ea63…`). The stretch between 41 and 137 also carries 113 and 131. They are in this act's record, not the clause, because the clause names what the ruling names.

---

## ROUTED FROM PRIOR RECORDS

- b416's registry row and correspondence row say the stretch between 41 and 137 holds six sums of powers that are prime. **b416's own extract computed nine.**
- b416's closing printed correspondence row 266, but **its row is 265**. The ferry carried 266. This act's row takes 266 and was found by its marker.
- Registry row p2-35 still says *three-clause currency note*.

Suite **93/93** pre-push, reconciling both ways. Mirror **CLEAN ON ALL THREE CLAUSES** at `09a1e80`. Censuses `0`; pins `0` hard-failing. Pins: relay `20ce1c7`→`cf09c22`, PLACE-papers `85b8daa`→`09a1e80`, SIDE-global-section `e9effce`→`ad7dba6`, TECHNE-Core `4aaf600` local. Correspondence row **266** by its marker, key `the-contradiction-put` PASS, desk **26 swept / 6 closed**. **0 content lost. h2 where the deposit left it.**

---

## THE NEXT FERRY AS DRAFT — NAVIGATOR EDITS

> **CONCURRENCY: SOLO (research seat).** Standing text as b417, carrying A2 and (R26)–(R34). Orientation-layer citation: correspondence row 266 (by its marker), b417's two-readings table, and registry row p2-35. Both instrument lanes parked; the KERNEL lane open; the wave parked; nothing deposits.
>
> **ACT b418 — THE ROUTED FIGURES SETTLED, AND THE CONTRADICTION HELD FOR ITS RULING.**
>
> **COMPONENT 1** — the author's ruling on the contradiction between the universality sentence and the tuples, carried into exactly the halves it names. If no ruling has been given, this component is PARKED and says so.
> **COMPONENT 2** — registry row p2-35's two stale figures, its *three-clause currency note* and its count of six, settled on b371's precedent: preserve by quotation first, remove rather than restate.
> **COMPONENT 3** — the example systems COMPLEX_ANALYSIS and MATTER place at different tuples: for each system, which decomposition each document parses, read from its own definitions and Gate 1b's note. Counted, not repaired.
> **COMPONENT 4** — CONSTANCE's sentence about 43: an annotation drafted, its exact bytes shown, not applied.
> **COMPONENT 5** — why `rg` returned no line from relay records that carry one, diagnosed, and whether any banked ABSENT was taken by it.
>
> The navigator's expectations: **(N1)** at least one banked ABSENT in the arc was taken by `rg` over relay `data/`; **(N2)** the three example systems are parsed by different decompositions, not misplaced. Each is refutable by a printed result. **h2 where the deposit left it.**
