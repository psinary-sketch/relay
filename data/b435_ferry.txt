CONCURRENCY: SOLO (research seat). Standing text as b434,
carrying A2 and (R26)-(R45); the write-list form standing per
(R44). Both instrument lanes parked -- and this act is named an
exception to that parking, for the guard layer only, on the
ruling below; the KERNEL lane open to read; the wave parked;
nothing deposits.

FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

RULING (R46), the author's, ratified by this paste and
strikeable: A CURE THAT LIVES IN ONE TOOL IS NOT A GUARD. When
an act writes a handler for an environmental failure -- a
read-only tree, a rate limit, an encoding, a lock -- the handler
is extracted to a shared tool in the same act, or the act
states why it cannot and files the extraction with a trigger.
The instrument lane is opened FOR THIS ACT AND FOR THE GUARD
LAYER ONLY, to carry the extraction b433 needed and b314
already had. It closes at this act's end.

ACT b435 -- THE CURE SHARED, AND THE SKIPS THAT PRINT LIKE
PASSES.

COMPONENT 1 -- THE EXTRACTION. b314's read-only handler,
located at its own file and quoted, extracted to a shared tool
with fixtures in both polarities -- a read-only tree removed, a
genuine failure still failing loudly. Then the ten call sites
in seven tools repointed, each named, with the lock gate and
the hash tool reported separately since they are guards
themselves. Any site where the pattern is correct as written is
named and left, with its reason. No tool's behaviour changed
except the silent-skip.

COMPONENT 2 -- THE SKIP THAT PRINTS LIKE A PASS. The pre-push
guard cannot be exercised at step zero in the working
repository, and reports a one that reads as a failure or a pass
depending on the table. Make the report say SKIPPED with its
reason wherever that state occurs, so a reader of the closing
tables can tell a skip from a pass from a failure. Fixtures for
all three states. Then sweep, by description, for any other
arm or report whose skip is indistinguishable from its pass --
counted, and repaired only where the repair is the same one
line.

COMPONENT 3 -- THE SHARED-CURE CENSUS, ONCE. Search the tools
for handlers of environmental failures that live in one tool
and nowhere else -- by description, with a positive control that
finds b314's -- and report how many, with what each handles.
Repair nothing beyond Component 1; the census is the product,
and (R46) governs what happens to the rest going forward.

CLOSING as the standing pattern, with Component 1's call-site
table and Component 3's census printed in the closing message
itself; the instrument lane's closure at the act's end stated
explicitly; the witness arc restated checkpointed after site
(iii); the four lists OPEN. The navigator's expectations: (N1)
at least two of the ten sites are correct as written and are
left; (N2) the census finds more than three single-tool cures,
and at least one handles a failure another act has already met
independently. Each refutable by a printed result. h2 where the
deposit left it.

paste ends (part 1 of 1)
