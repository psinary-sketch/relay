====================================================================================================
b65 -- THE TOWER-LIMIT CONSTRUCTION ACT -- THE RUN-REGISTRATION ON THE STAGED TOWER
REGISTRATION. BANKED BEFORE THE INSTRUMENT RUNS AND BEFORE ANY LEAN CONSTRUCTION. The
staged registration (data/tower_limit_construction_registration_STAGED_2026-08-21.txt,
with its dated register-invariance addition) is the frame; divergences resolve in its
favor and are recorded; the T1-T5 marks resolve from sources at content or stay open.
====================================================================================================
FERRY 2026-08-21 (part 1 of 1, receipt confirmed). Relay-only. NO SIGN SENTENCE.
h2 UNCHANGED BY CONSTRUCTION; everything below is finite-side and exact; nothing
archimedean is computed; every S1-facing sentence carries the prediction-never-
discharge clause verbatim. The range law governs. NOTHING DEPOSITS.

SOURCES (rule b, at their pins): the transport laws (b49/b50/b55; the exponent
theorems row 30; the norm's exact failure); the g-family (b58/row 37: g_{a,t} =
sum_b omega^{tb} delta_{a+qb}, a basis of Son -- (q-1)^2 orthogonal vectors, norms
q; the monomial S-action S g_{a,t} = q zeta^{a(q-t)} g_{q-t,a}); the embedding iota
(b55: coefficients zeta_N -> zeta_{N+}^{p^2}; chart delta_{a+qb} -> sum_j
delta_{pa + q+(b+qj)}); the purity-locus and flatness laws (rows 33/36); the
boundary rows (row 41); the row-21 interface-declared colimit/completion path
(never load-bearing, rule c).

----------------------------------------------------------------------------------------------------
THE CONSTRUCTION'S LOAD-BEARING LAW, DERIVED LONGHAND HERE (before any run):

(L1) THE EMBEDDING IS MONOMIAL ON THE g-BASIS: iota(g^(n)_{a,t}) = g^(n+1)_{pa, pt}.
  Derivation: iota carries delta_{a+qb} to sum_j delta_{pa + q+(b+qj)} with the
  coefficient exponent map e -> p^2 e. The g-coefficient omega_n^{tb} embeds to
  zeta_{N+}^{q p^2 t b} = omega_{n+1}^{pt b}; and for b'' = b + qj one has
  omega_{n+1}^{pt b''} = omega_{n+1}^{pt b} * zeta_{N+}^{q+ pt q j} with the second
  factor's exponent p q * p t q j = p^2 q^2 t j == 0 mod N+ -- so the coefficient is
  CONSTANT on each j-fiber and the image is exactly sum_{b'' mod q+}
  omega_{n+1}^{pt b''} delta_{pa + q+ b''} = g^(n+1)_{pa, pt}. QED (longhand; the
  smallest instance decided in the kernel this act).
(L2) THE EXACT INTERTWINING OF THE NORMALIZED TRANSFORM: M^(n+1) o iota = iota o M^(n)
  ON ALL OF Son. Derivation: apply (L1) to the monomial S-action: iota(S g_{a,t}) =
  q zeta^{a(q-t)} g+_{p(q-t), pa} and S+(iota g) = S+ g+_{pa,pt} = q+ zeta+^{pa(q+-pt)}
  g+_{q+-pt, pa}; the grid points agree (p(q-t) = q+ - pt) and the phases agree
  (zeta^{a(q-t)} embeds to zeta+^{p^2 a(q-t)} = zeta+^{pa(q+-pt)}); the scales give
  S+ iota = p * iota S, hence M+ iota = iota M exactly (M = S/q). QED (longhand;
  instance decided). CONSEQUENCE: the sector labels and the whole sector structure
  are iota-stable on the nose -- the colimit's four sectors are well-defined.
(L3) THE COLIMIT, EXPLICIT: with (L1) the direct limit is the g-span over the limit
  grid -- classes of (a,t) under (a,t) ~ (pa,pt), i.e. pairs of p-power-denominator
  fractions (alpha, tau) = (a/q, t/q) in (0,1)^2 (the fraction is constant along the
  system: a/q = pa/q+ exactly). M acts on limit basis classes by (alpha,tau) ->
  (1-tau, alpha) with phase the constant fraction a(q-t)/N = alpha(1-tau) of the
  circle (well-defined: the exponent embeds by p^2 as the level squares). The
  ALGEBRAIC colimit carries every finite-side deliverable below; the COMPLETION is
  never leaned on (rule c) -- it stays at the row-21 interface declaration, untouched.
(L4) THE NORM TRANSPORT ON THE WHOLE FAMILY: ||g^(n)_{a,t}||^2 = q, so
  ||iota g||^2 = q+ = p * q -- the transport-by-p register is now DERIVED on all of
  Son (previously the E_1-table law); under the p-register the class norms are
  constant (q/p^n = 1 at place 2) and the limit pairing is well-defined on classes;
  under the p^2-register applied to the same pairing the class norms are q/p^{2n}
  -> collapse (2^n < 4^n).

REGISTERED PREDICTIONS (the ferry's, verbatim), OPERATIONALIZED BEFORE THE RUN:
(P1) the level-constant invariants pass as constants: the unit trace i at the place
  2 (row 36's constant sequence + (L2)'s label stability); the boundary index (1,1)
  (row 41's constant + the shell embedding); odd flatness (row 36); and THE N5
  OPPORTUNITY, DECIDED NOT PRESUMED: the seed chain is an iota-compatible family BY
  CONSTRUCTION (each ι^k(u) is the iota-image of the previous), hence ONE class of
  the colimit; with row 41's address coincidence at every level and the purity-locus
  uniqueness, THE BOUNDARY LINE OF THE LIMIT OBJECT IS THE SEED CHAIN'S CLASS at the
  banked normalization (the shell dictionary). The per-level operator-vector half of
  N5 (the corpus's compressed scaling's own defect vector at each finite level)
  remains open and is said so.
(P2) THE BOUNDARY PHASE: on the constructed limit, the boundary line's M-eigenvalue
  is the constant i (banked at every level); its C_8 root pair is +-zeta_8, and the
  PLUS-SIGN root zeta_8 = zeta_16^2 (the b59 scalar at kernel grade -- the stated
  ground) is CONSTANT along the system in exact exponent arithmetic: the eighth-root
  exponent N/8 embeds to p^2 * N/8 = N+/8 exactly. REGISTERED TARGET: the boundary
  phase lands in the zeta_8 family with the plus sign; any other landing is a banked
  finding locating where constancy breaks.
(P3) THE REGISTER TEST, both registers run in full: the p-register (the space-pairing
  transport, (L4), derived) and the p^2-register (the volume-convention Gram factor,
  the act-7 record, at its own object). THE OPERATIONALIZED DECISION: (i) every P1/P2
  invariant is SCALE-FREE (eigenvalue equations, ranks, labels, phases -- untouched
  by any metric rescale): INVARIANT under both registers; (ii) the limit METRIC
  exists in the p-register (constant class norms) and does not exist for the space
  pairing under the p^2-register (the collapse arithmetic); (iii) the T1 mark's
  resolution to check at content: the two registers are registers OF DIFFERENT
  OBJECTS (the space pairing vs the quotient-volume convention) -- if that reading
  holds at the act-7 record, branch (INVARIANT) files with the register-relation
  banked and the dictionary gains the scheme <-> register entry AT PREDICTION GRADE;
  the S1-facing line carries VERBATIM: "A PREDICTION, NEVER A DISCHARGE: the
  scheme-invariance clause of S1 has NO finite analogue -- the recorded reason: the
  no-compact-open theorem (the archimedean place has no finite exact levels; the
  finite tower's limit and the eps-scheme's identity limit are different objects
  until a derivation says otherwise)."

THE MARKS, RESOLUTION PLAN: T1 -> per (P3)(iii); T2 -> RESOLVED (the algebraic
colimit suffices; the completion never leaned on); T3 -> STAYS OPEN (the SOT
operator-limit relation untouched); T4/N5 -> the LIMIT half closes per (P1), the
per-level operator half the residue; T5 -> the constructed limit's twisted half
carries the C_8 action (the exponent constancy) -- the meeting point now EXISTS
finite-side; the extensions stay staged.

THE RUN PLAN: (i) the instrument verifies (L1)/(L2) exactly at the smallest instance
((2,1) -> (2,2), Z[zeta_16] dicts) and the fraction/exponent constancies; (ii) the
kernel decides the instance endpoints (Core, zero-axiom): the monomial embedding
instance, the intertwining instance, the eighth-root exponent constancy, the
fraction constancy, the register arithmetic; (iii) nothing enters Interfaces -- the
report states why (no step leaned on the completion). Gates first: the transport
laws re-verified at the consumed instance before dependence (the instrument's gate).

Bank: data/b65_tower_limit.txt (the ferry's name). Deviation rule standing.
Divergence note (rule a): none.
====================================================================================================
