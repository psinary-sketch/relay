RULING (R139) BEGIN
RULING (R139), the author's, ratified by this paste and strikeable.

(1) b528 is entered: on the kernel's plateau, xi is positive beyond
its estimated bound at 46 of 46 widths to 60; Q0 is within reach at
none, the tail ESTIMATE running 15 to 2000 times B'; (N2) and (N3)
REFUTED on the kernel's plateau as ordered; the four defects
entered as declared, D3's roundoff floor added to W-ORD-SMOOTH-TAIL.
(2) THE REASON IS ENTERED AS A FINDING ABOUT WINDOWS: the exp(-1/x)
transition, though C-infinity, has a transform near t = 150 about
five orders above the order-7 B-spline's. Smoother is not better at
finite height; a finite-order ramp with fast decay at the bank's
height beats an infinitely smooth one whose decay sets in later.
The record says so, and any future instance is chosen by its
transform at the bank's height, not by its smoothness class.
(3) CHOICE (c): (f)(ii) PROCEEDS AS RULED, for every C^4 phi >= 0
with the realized G as hypothesis, instance-free. Q0's witness
stays on the order-7 B-spline, and the corpus cites it so: the
kernel's theorems cover the B-spline; the kernel's named instance
is a function on which Q0 could not be read. W-ORD-BSPLINE-INSTANCE
is filed — a B-spline ramp of order p constructed in the kernel as
a C^(p-2) phi, so that a compiled witness at a specific rho can one
day cite numbers on its own function; trigger: the act that wants
a compiled witness at Q0, or the author's word. Route (a), widening
past 60, is noted as capped by the prime table at a = 64 and not
taken.
(4) The kernel lane opens for b529 and shuts at its close.

RULING (R139) END

FERRY BEGIN — paste for Claude Code (part 1 of 1; confirm receipt-in-full)

ACT b529 — (f)(ii), THE PAIR'S TERM, AS FAR AS IT DERIVES.

Component 1 — the exact algebra, compiled. For the window of b524
at gamma_0 with phi >= 0 and ∫ phi > 0, and rho_0 = 1/2 + delta +
i gamma_0 with delta > 0: the pair's contribution to zeroSide,
term(rho_0) + term(1 - conj rho_0), written through the
factorisation of b524 as an explicit expression in delta, gamma_0,
and the four values of the cosine window's transform at gamma_0 -
i delta and at -gamma_0 - i delta and their mirrors; the near
factor identified as ∫ phi cosh(delta u) du times a constant, the
far factor as the transform at 2 gamma_0 - i delta; the statement
printed from its declaration line to its :=, profiled.

Component 2 — the near factor bounded below, compiled: from
phi >= 0, ∫ phi cosh(delta u) du >= ∫ phi > 0, and the realized G
of b524 is that ratio; the sign of the near-factor product at the
pair fixed and printed as a theorem.

Component 3 — the far factor as a named hypothesis, not proved: the
Prop that the transform at 2 gamma_0 - i delta is smaller in
modulus than epsilon times the near factor, for an epsilon the
statement carries; under it, the theorem that the pair's term is
at most -(c - epsilon') delta^2 slope^2 G^2 with c and epsilon'
printed from their definitions. Graded INTERFACES on that named
hypothesis by statement-read; the hypothesis's numerical value at
Q0's B-spline instance at a = 34 read from b522's bank and printed
beside it, so the interface has a number.

Component 4 — the terminal table through an AxiomCheck file; f2
marked DERIVES for Components 1 and 2 and INTERFACES for Component
3 in W-ORD-WEIL-CONVERSE's table; f3 and f4 unchanged.

The navigator's expectations: (N1) Components 1 and 2 compile
standard three within three attempts; (N2) Component 3's
hypothesis, evaluated at Q0's B-spline instance at a = 34, holds
with epsilon below 0.1; (N3) no vendored lemma beyond b524's is
needed. Each refutable by a printed cell. Compiles in the kernel
alone; nothing at Zenodo written; nothing deposits; h2 where the
deposit left it.

FERRY END (part 1 of 1)
