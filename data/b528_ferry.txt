RULING (R138) BEGIN
RULING (R138), the author's, ratified by this paste and strikeable,
the seat's 04:17 draft taken whole.

(1) b527 is entered: the kernel's plateau is concrete — the product
of two Real.smoothTransition factors, no chosen base, #print
showing so — with plateau_apply_abs proving it equal to the
absolute-value form; b524's 17 statements byte-identical; 11
profiled theorems standard three; the kernel's values and the
chain's phi_chain agreeing to 4.4e-16 at forty points. The seat's
counting of its own timeout as an attempt stands: a stop the seat
caused is an attempt, a stop the host caused is not.
(2) b528 measures on the kernel's function: xi at 14.1347 and Q0 at
16.290216 on phi_chain, widths 15..60, per-width reach, b522's
columns with G per cell. The smooth bump's transform has no closed
form and is computed numerically, fixtured under (R125)(2) by two
independent quadratures; the tail above each bank is priced as an
ESTIMATE — an envelope of the computed transform times the count —
and every cell says ESTIMATE where b523's said majorant. A
rigorous tail on this phi needs high-order derivative norms of
smoothTransition and is filed as W-ORD-SMOOTH-TAIL, trigger: the
author's word or the act that cites a b528 cell in a keystone.
(3) The form of words is re-derived from b528 alone; the instance
sentence names the window each number was taken on.
(4) The numerical lane opens for b528 and shuts at its close; the
kernel lane takes (f)(ii) after.

RULING (R138) END

FERRY BEGIN — paste for Claude Code (part 1 of 1; confirm receipt-in-full)

ACT b528 — BOTH OBJECTS ON THE KERNEL'S FUNCTION.

Component 0 — phi_chain imported from b527's bank, its agreement
with the kernel's definition re-checked at the forty points and
printed; the transform by two independent quadratures at three
widths, agreeing under (R125)(2)'s floor, printed.

Component 1 — Q0 at 16.290216 on phi_chain, widths 15..60,
per-width reach with the tail as an ESTIMATE labelled so: P - PR + A
and its sign against the estimated bound; the pair's term, the
other sixteen pairs, the on-line part; the pair's ratio to the
rest; G per cell from the integral the kernel defines; the
narrowest negative width or NONE; G there beside the B-spline's
1.361.

Component 2 — xi at 14.1347 on phi_chain, the same widths and
columns.

Component 3 — the form of words re-derived from Components 1 and 2
alone and printed, each number tagged with its window and its
tail status (majorant or ESTIMATE).

The navigator's expectations: (N1) xi is positive at every
verified width to 60 on the kernel's plateau; (N2) Q0 is negative
beyond its estimated bound at some width at or below 60; (N3) G at
Q0's crossing is within 20 percent of 1.361. Each refutable by a
printed cell. The lane shuts at this act's close; nothing
compiled; nothing deposits; h2 where the deposit left it, and no
cell of this act is a statement about RH.

FERRY END (part 1 of 1)
