CONCURRENCY: SOLO (research seat).

FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

THE TRACE ON THE OBJECT -- the source's compressed smeared
trace computed on the truncated archimedean space, window
open, both test functions, one act. Scope: a COMPUTATION on
the instrument AS CERTIFIED -- for the space, not for the unit;
no unit is used anywhere in this act and the act says so; the
registered prediction is tested and scored, its sign chain
checked link by link before any number is read;
normalizations before counts; NO aggregation stated, M-2 owed
under its cap; NO comparison of the trace to any target other
than the corpus's own banked trace values, and that only as
the prediction's stated referent; every number carries its
convergence under grid AND domain refinement, two axes
independently (replicates sharing an error source certify
nothing), with the noise-floor gate in the path; exact where
the object is exact, floating point declared where not, zero
float literals in any deciding runner; every reported number
from a committed tool with fixtures; needles pulled from
emitting files; stem sweep at extended scope; hedge audit;
registration term-scanned, satisfiability-checked, sealed, no
counts predicted; the suite re-run after the push; nothing
about the h2 identity beyond the register sentence exact;
nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
the kernel-coverage gate restated as failing, nothing
repaired; push anything ahead of origin, saying so if nothing
is.

COMPONENT 1 -- THE ASSEMBLY, from the source's definitions
quoted: the scaling action integrated against the test
function over the multiplicative group in the source's
measure, then compressed by the projection onto the truncated
space, then traced. The corpus's integral-one bump and a
mean-zero variant both written in the source's variable with
their transforms at zero stated (nonzero for the first, zero
for the second -- the source's class boundary, quoted). The
smearing quadrature's own positive control: a scaling by the
identity alone recovers the projection's trace, the
truncation's dimension.

COMPONENT 2 -- THE NUMBER, at the banked cells and along the
cutoff at the instrument's reach: the compressed smeared trace
for each test function, with two-axis convergence tables and
the reach stated as the last cell where both axes agree
inside a bar fixed before the run. Beside it, for the
integral-one bump only and as the prediction's referent: the
corpus's banked trace values at the same cells, quoted from
their emitting file with their convention named.

COMPONENT 3 -- THE PREDICTION SCORED: small relative to the
corpus's banked trace, or not, with "small" the ratio fixed in
the registration before any value; and if small, the
entailment at exactly its scope -- the source's Theorem 4.7 is
the definition the cancellation lacked, stated as a
correspondence and not as evidence about the identity, h2, or
the roster; the corpus's old trace values named as evaluations
of a different quantity WITHOUT any act re-verdicted. If not
small, the cancellation stays a fact without a home and the
act says so; the sign chain's links re-examined for the
trivial failure before any other reading.

COMPONENT 4 -- THE MEAN-ZERO COLUMN, reported as its own
measurement with no prediction attached: the trace for the
variant inside the source's class, its convergence, and the
sentence that this is the first archimedean number in the
programme computed on the object's space for a test function
the source's inequality covers -- with the inequality itself
NOT evaluated here, because its other side (the archimedean
Weil distribution for this variant) is a separate computation
under its own registration.

CLOSING: the correspondence rows by the idempotent tool; every
result keyed; the object's conditions restated with the
archimedean leg marked as derived-not-confirmed pending the
membership act; M-2's row unchanged under its cap; the seam's
debt item 1 restated; the patent clock restated; the window
ruling recorded as applied at the instrument and nowhere else;
the in-flight register; the ledger census; the membership act
named as next, as a definitional decision between the derived
unit and the instrument's realization of it. Registration
gate; index queried; trace-on-the-object keyed. Components
ordered. THE SHADOW: expected nothing -- quadrature on a
truncated space; say so.

Bank: data/b317_the_trace_on_the_object.txt. Deviation rule
standing.

EXECUTION: ferry scan first; owners and the source read at
content first; registration satisfiability-checked,
term-scanned, sealed; components in order; full control suite,
re-run after the push; pins by ls-remote across all three
repos; the hook if PLACE-papers is touched; mirror if it
moves. STOP.

FOOT: after this act the programme has the source's compressed
trace evaluated on the object's own space for the first time,
scored against a prediction registered before the instrument
existed, with the mean-zero column beside it as the first
number the source's inequality can lawfully see; the unit's
membership is the act after; h2 stands exactly where the
deposit left it; locks last.

paste ends (part 1 of 1)
