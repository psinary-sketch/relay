========================================================================================================
b538 -- THE DESK. ### **THE EXPECTATIONS, SCORED ON THE BANKS.**
========================================================================================================

### THE NAVIGATOR`S SIX.
--------------------------------------------------------------------------------------------------------
  **(N1)** ### **HELD.** -- 'SIDEExplicitFormula.RegisterDepth.not_register1' does not depend on any axioms ; failing at NONE.
  **(N2)** ### **HELD.** -- 'SIDEExplicitFormula.RegisterDepth.mellin_Phi_eq_zero_of_re_le_one' depends on axioms: [propext, Classical.choice, Quot.sound] ; failing at NONE.
  **(N3)** ### **HELD.** -- 'SIDEExplicitFormula.RegisterDepth.register5_output_holds' depends on axioms: [propext, Classical.choice, Quot.sound] ; no countability premise.
  **(N4)** ### **REFUTED.** -- R3 graded UNDECIDED: no compiled theorem decides it at every s or refutes it at one; `register3_of_one_lt_re` decides 1 < re s only.
  **(N5)** ### **HELD.** -- grade cells filled : 6 of 6.
  **(N6)** ### **HELD.** -- ERRATA, README, REGISTRY byte-identical to b7e0c52 : {'ERRATA.md': True, 'README.md': True, 'REGISTRY.md': True} ; SIDE-kernel and lv at their HEADs, clean : {'sk': True, 'lv': True} ; b538 tools naming the platform`s address NONE ; token hits 0 ; deposit tree clean True.

### THE SEAT`S THREE.
--------------------------------------------------------------------------------------------------------
  **(S1)** ### **HELD.** -- mellin_Phi_eq_zero_of_re_le_one failing at : NONE.
  **(S2)** ### **HELD.** -- R3 UNDECIDED in the census.
  **(S3)** ### **HELD.** -- 'SIDEExplicitFormula.RegisterDepth.zeta_zeros_countable' depends on axioms: [propext, Classical.choice, Quot.sound].

### ### **THE NAVIGATOR`S : HELD 5 ; REFUTED 1.** ### ### **THE SEAT`S : REGISTERED 3 ; HELD 3 ; REFUTED 0.**

### THIS ACT`S OWN DEFECTS.
    (a) ### **THE PROFILE READ A WRAPPED AXIOM LINE AS ITS FIRST PHYSICAL LINE**: Lean's pretty-printer wrapped
        `mellin_Phi_eq_zero_of_re_le_one`'s `depends on axioms: [propext, Classical.choice, Quot.sound]` over three lines,
        and `b538_kernel.py profile`, carried from b536, kept one physical line per theorem -- so it read `[propext,` and
        printed 10 of 11 within the standard three. The raw log was read by hand, the parser now joins an opened list to its
        `]`, and the re-run reads 11 of 11 (the first profile kept as `b538_profile_first.json`). No axiom changed.
    (b) ### **TWO ARMS DEFECTIVE ON THE SUITE`S FIRST TRIAL, REPAIRED BEFORE ANY COMMIT**: `G-MELLIN-PRINTED` looked for the
        probe`s `in Ioi 0`, where the profile run (no `open Set`) prints `in Set.Ioi 0`, and its positive control renamed
        `def mellin` to `def mellinX`, which still contains the needle; `G-CENSUS-ENTERED` accepted a grade cell of `** **`.
        The needle now matches the profile`s own print, the control breaks the integrand, and a grade cell must open with a
        non-space; the re-run reads every control as it should.
========================================================================================================
