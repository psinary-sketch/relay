====================================================================================================
b119 -- THE DOMINANCE ACT -- THE RUN-REGISTRATION. BANKED BEFORE ANY
COMPUTATION, WITH THE DECOMPOSITION FIXED AND BINDING AND BOTH SEATS'
EXPECTATIONS AND TRANSFERS DISCLOSED. Enclosure-grade work CONDITIONAL ON THE
SAMPLES (the b117 conditionality inherited and to be stated in every verdict;
the prolate error bound remains the named blocker to any higher grade). THE
HAZARD FENCE GOVERNS: BOUNDS DERIVE, COHERENCE NARRATES NOTHING. The guard
rides every location and coincidence; the scale discipline governs every Psi
claim; nothing promoted; nothing about h2; nothing deposits.
====================================================================================================
FERRY 2026-08-23 (part 1 of 1, receipt confirmed in full, Rule 1). Relay-only.

(a) LOAD-BEARING, READ AT CONTENT THIS ACT:
 - b115's collapse and scale-average forms: N(a) = 2*int_0^{2L} C_a(u) Psi(u) du;
   N(L) = 2*int_0^2 K(s) Psi(Ls) ds.
 - b117's enclosures: the gross rise +1.338017313; the sign margin (strictly
   negative I(L) at nine cells and sixty sweep points, least negative upper
   bound -0.059482665, margin ~4,280x); THE FLOOR 2.774e-05.
 - PhiK's exact structure: MEAN-ZERO (int_0^2 PhiK = 0), ONE SIGN CHANGE at
   sigma = 0.6215, POSITIVE THEN NEGATIVE; int_0^2 K = 0.500000000 exact (K
   even, total mass 1 over [-2,2]).
 - The licensed L-range: a^2 = 2 to 48.001, i.e. u in [0, 3.871222].
 - The channel kernel's own scale as the record fixes it: K is supported on
   |sigma| <= 2, K(2) = 0 at the support edge, K >= 0, K(0) = 0.675116813.

*** (b) THE DECOMPOSITION, PRE-REGISTERED HERE, BINDING, AND NEVER REVISITED
IN-ACT. A FAILED DECOMPOSITION FAILS HONESTLY.
  psi_coarse(u) := int_{-2}^{+2} K(s) * Psi(u + s) ds
  psi_fine(u)   := Psi(u) - psi_coarse(u)
 with K normalized so that int_{-2}^{+2} K = 1 (its recorded total mass), and
 Psi extended OUTSIDE the licensed range by its endpoint values (a stated
 convention, fixed here, not chosen later).
 *** THE READING BEING FIXED, AND THE ONE BEING DECLINED, both written down so
 the choice cannot look tuned after the fact. THE FERRY SAYS: "the convolution
 of Psi with the channel's own kernel K at its recorded scale (the channel's
 view of its own density -- no free parameter, no tuning)". TWO READINGS EXIST:
  (R1, TAKEN) K applied in the u variable at ITS OWN RECORDED WIDTH (support
    |s| <= 2). This is L-INDEPENDENT and has NO free parameter -- one fixed
    smoother, one fixed width, the same at every cell.
  (R2, DECLINED) K applied in the sigma variable, i.e. smoothing Psi(L.) at
    width 2 in sigma = width 2L in u. This is L-DEPENDENT: the decomposition
    would change with the window, which is a different object at every cell and
    is not a single density's coarse/fine split.
 R1 IS TAKEN BECAUSE THE FERRY ASKS FOR A SPLIT OF Psi -- one function, split
 once -- AND R2 DOES NOT PROVIDE ONE. R2 is recorded so that if R1 fails, no
 later act can claim the "obvious" alternative was quietly avoided. ***
 *** AND THE HAZARD I NAME NOW, BEFORE SEEING ANY NUMBER: K's support is 2 wide
 while the licensed u-range is 3.871 wide. A SMOOTHER OF NEARLY HALF THE RANGE'S
 WIDTH IS VERY WIDE. I expect psi_coarse to be close to flat and psi_fine to
 carry most of Psi's structure, which would make the trend's pairing SMALL and
 the remainder's LARGE -- i.e. it points toward branch (b). I AM REGISTERING
 THAT EXPECTATION RATHER THAN QUIETLY NARROWING THE KERNEL, WHICH IS EXACTLY
 THE TUNING THE FERRY FORBIDS. ***
 Both computed at the refinement-stable scale, with the interpolation floor's
 contribution separated.

(c) THE MEANING TABLE CARRIES THE MINTED THIRD-OUTCOME CELL, quoted from the
ferry: "if the dominance bound fails while the sign still holds (already known
from b117), the pre-committed reading is -- the candidate mechanism is refuted,
the sign banks unchanged at its evidence grade, and the mechanism question
re-opens with the failed estimate as its new coordinate."

*** (d) THE TWO SEATS, BEFORE ANY COMPUTATION.
THE NAVIGATOR'S TRANSFER, named by the ferry itself: the "channel suppresses
the fine" sentence transfers b116's coarse/fine finding at MODERATE confidence
and nothing else. Recorded as given.
THE EXECUTOR'S:
 ON P1 -- I expect the split to compute cleanly and psi_coarse to carry a rise,
   but I expect that rise to be a FRACTION of Psi's +1.338 rather than close to
   it, because a width-2 smoother over a 3.871-wide range flattens hard.
 ON P2 -- ### I EXPECT THE COARSE PAIRING TO BE NEGATIVE (the b116 orientation:
   a rising function against a mean-zero positive-then-negative kernel pairs
   negative) BUT SMALL IN MAGNITUDE.
 ON P3 -- ### I EXPECT BRANCH (b): THE REMAINDER NOT DOMINATED. GROUND, stated
   before computing: the two pairings must sum exactly to b117's I(L), which is
   about -0.06 to -0.083. If psi_coarse is flattened by the wide kernel, its
   pairing carries only part of that and psi_fine's pairing carries the rest --
   and "the rest" being comparable to or larger than the coarse part is
   precisely dominance FAILING. MY TRANSFER, named: I transfer b117's measured
   I(L) values as the SUM the two pairings must reproduce (a consistency check,
   not an input to either bound), and NOTHING ELSE.
 THESE EARN NOTHING. Only the computed bounds decide.
 *** AND THE SEATS' DISAGREEMENT IS ON THE RECORD: the ferry's P3 states branch
 (a) as its expectation; the executor registers branch (b). ***

(e) INDEPENDENCE CHECK, RUN: the components are ordered, 2-3 consume 1.

*** (f) A REFERENT I CANNOT LOCATE, NAMED NOW RATHER THAN IMPROVISED LATER.
Component 3 asks for "the superlative ledger's dated entries (the author-caught
phrase from the sixty-seventh close, verbatim, attributed)". ### THERE IS NO
SIXTY-SEVENTH CLOSE IN THIS RECORD: the campaign has FOURTEEN seam closes, and
the fourteenth is the most recent. A content grep for "sixty-seventh" and "67th
close" returns nothing. THE NEAREST KIN THE GREP DOES RETURN are author-caught
corrections of other kinds ("correction twenty-seven, author-caught", the
augmentation-silent repair, the repair whose retired term was "norm-blind"
[quoted as a correction record's name per EXECUTOR_RULES section 5]). I WILL
SEARCH AT CONTENT IN
COMPONENT 3 AND, IF THE REFERENT REMAINS UNLOCATED, FILE THE ABSENCE WITH THE
NEAREST KIN NAMED AND QUOTE NOTHING. An "verbatim, attributed" quotation whose
source I cannot find is exactly the thing Rule 3 forbids me to supply. The
other half of the component -- new superlatives in THIS act's shipped prose,
entering at shipping -- is well defined and will be run. ***

REGISTERED PREDICTIONS (P1)-(P3), VERBATIM FROM THE FERRY, banked as given:
 (P1) "THE SPLIT COMPUTES: psi_coarse and psi_fine at the stable scale, their
   profiles banked with the floor separated; psi_coarse carries the gross rise
   (its endpoint difference within the b117 enclosure's neighborhood, stated
   not fitted)."
 (P2) "THE TREND BOUND: the pairing int_0^2 PhiK(sigma) psi_coarse(L sigma)
   d sigma bounded away from zero with one sign across the licensed range at
   enclosure grade -- the coarse channel-pairing strictly negative (the b116
   orientation: rising coarse part against positive-then-negative mean-zero
   kernel)."
 (P3) "THE REMAINDER BOUND: |int_0^2 PhiK(sigma) psi_fine(L sigma) d sigma|
   bounded above across the range, and THE VERDICT: branch (a) -- the
   remainder's bound is strictly inside the trend's margin at every L:
   DOMINANCE DERIVED at enclosure grade conditional on the samples, the sign's
   reason NAMED (coarse-trend dominance through a fine-suppressing channel),
   the b117 mystery closing at that grade with the conditionality and continuum
   limits restated; branch (b) -- the remainder's pairing is NOT dominated at
   some L: the candidate mechanism REFUTED per the third-outcome cell's
   pre-committed reading, the failing window named, the sign banking unchanged,
   the mechanism question re-coordinated on the failed estimate."

THE PLAN: (C1) the split, profiles and floors verbatim; (C2) the two bounds
with EVERY ROUNDING OUTWARD, margins tabulated across the range, the verdict
QUOTED FROM ITS REGISTERED BRANCH; (C3) the filings and the three named audits.
Kernel: none anticipated; b38's ITEM 4 NOTE governs and the report says so.

Bank: data/b119_dominance.txt. Report: reports/2026-08-23-dominance.md.
Deviation rule standing. Divergence note (rule a): none at registration.
====================================================================================================
