========================================================================================================
b533 -- THE CLOSING RECORD. ### **f4, THE POWER-WINDOW ROUTE, ACT ONE OF TWO: THE WINDOW AND THE DOMINANT ZERO, UNDER (R143).**
========================================================================================================
    the module : attempt 1 exit 1, 5 errors, 31.1 s, failing ['neg_one_pow_mul_iz_pow', 'real_even_interpolant'] ; attempt 2 exit 0, 0 errors, 61.2 s, failing NONE ; std3 34 of 34 ; halted NONE
    row 382 : `paperFT_conj_of_real_even` DERIVES ; `zero_term_sq` DERIVES ; `weilTest_even_of_even` DERIVES ; `classK_of_real_even` DERIVES ; `selfConv_im_zero` DERIVES ; `paperFT_power` DERIVES ; `power_contDiff` DERIVES ; `power_support` DERIVES ; `paperFT_polyOp` DERIVES ; `base_nonzero_at` DERIVES ; `off_finite_above` DERIVES ; `dominant_exists` DERIVES ; `real_even_interpolant` DERIVES ; `rh_imp_rh_strip` DERIVES -- each by statement-read against the ferry`s model statement; nodes_distinct_nonreal and rh_strip_imp_rh STATED, no grade
    stated, not proved : nodes_distinct_nonreal (SIDEExplicitFormula.B321.nodes_distinct_nonreal : Zeta23.ZeroConfig → (ℝ → ℝ) → ℝ → Prop) ; rh_strip_imp_rh (SIDEExplicitFormula.B321.rh_strip_imp_rh : Prop)
    (N1) HELD (N2) HELD (N3) HELD (N4) HELD (N5) HELD (N6) HELD ; (S1) HELD (S2) HELD (S3) HELD

### THIS ACT`S OWN DEFECTS (the desk).
    (a) ### **THE FACE'S FIRST DRAFT CARRIED A NUMBER BESIDE "module"** ("L1-L6 and the seam in one module"), which the
        prediction counter read as an artifact count; `reg_satisfiable.py` refused (NOT SATISFIABLE, 1 contradictory clause)
        and the words were changed to "in the PowerWindow file" before the seal. The same trap as b532's (c).
    (b) ### **THE PROFILE WAS RUN TWICE**: the seat re-ran `b533_kernel.py profile` to read the head of its own output, so
        a second run file stands as `b533_profile_log2.txt`. Its `depends on axioms` lines are identical to the first's
        (diff empty, stamp and timing lines aside); `b533_profile.json` is the second run's.
    (c) ### **THE STATEMENT BANK OVERRUNS ON EQUATION-COMPILER DECLARATIONS**: `b533_kernel.py statement` reads each
        head to its first `:=`, and `def power`, `theorem power_even`, `theorem power_support` and `theorem power_contDiff`
        (pattern-matched, `| 0 => ...`) carry no `:=` before their bodies -- so the heads of `power` and `power_even` run on
        into later declarations, and `power_support`'s text appears in the bank three times. Carried from b532's tool, which
        had no such declaration. The grades were read from the module's source, not from those heads.
    (d) ### **TWO STATEMENTS CARRY A HYPOTHESIS THE FACE DID NOT DECLARE**: `power_contDiff` and `paperFT_power` take
        `Function.support g ⊆ Set.Icc (-L) L` (the convolution's smoothness and the zero-term square need compact support
        at every power); the ferry's L3 states power_contDiff as "ContDiff R n whenever g is", with the support in [-L, L]
        stated in the same paragraph for power_support. Read as L3's standing window context (L1's zero_term_sq, which
        paperFT_power's induction uses, carries compact support in the ferry's own words) and graded DERIVES on that
        reading, which is printed here and in the trail for the author.
    (e) ### **ATTEMPT 1 FAILED IN TWO LEMMAS, EACH ONCE**: `neg_one_pow_mul_iz_pow` (`neg_pow` rewrote the `(-1)^j` factor
        first) and `real_even_interpolant` (`lifts_iff_coeff_lifts` returns its witness through `mapRingHom`, so `rw [hQ]`
        missed `Polynomial.map`; and one `rfl` after a closing `rw`). Repaired; attempt 2 clean. No lemma reached its second
        failure; the halt rule did not fire.

### THE COMMITS, THE MIRROR, THE CENSUSES.
    pre-push : ### ### **ARMS RUN : 51. ### LIVE PASSING : 51. ### LIVE FAILING : 0 .**
    post-push : ### ### **ARMS RUN : 52. ### LIVE PASSING : 52. ### LIVE FAILING : 0 .**
      relay                  local 760aa9039598 ; remote 760aa9039598 ; AGREE
      PLACE-papers           local b8cce570b265 ; remote b8cce570b265 ; AGREE
      SIDE-global-section    local d52cf6180d33 ; remote d52cf6180d33 ; AGREE
      SIDE-explicit-formula  local f42102fb3b9e ; remote f42102fb3b9e ; AGREE
      SIDE-kernel            local 0256e9e1297f ; remote 0256e9e1297f ; AGREE
    mirror : ### VERDICT: CLEAN ON ALL THREE CLAUSES
    censuses : ### ### **TOTAL MISSING : 0** / ### ### **TOTAL MISSING : 0**
    pins : ### REPOS HARD-FAILING : 0

### CARRIED FORWARD.
    (a) ### **THE KERNEL LANE IS SHUT.** L7 (the limit) and L8 (the assembly) are b534`s, under (R143)`s halt rule.
    (b) ### **nodes_distinct_nonreal CARRIES A CAVEAT FOR b534**: the square of gammaOf rho is real when Im rho = 0, which the
        kernel`s configuration does not exclude; b534`s nodes need Im rho /= 0 from somewhere, or a statement that avoids it.
    (c) ### **THE SEAM`S LEFT HALF-PLANE IS ABSENT FROM MATHLIB BY NAME**; priced as derivable from the trivial zeros and the
        functional equation, not attempted (R143)(4).
    (d) ### **E-2026-09-25-1 STAYS DRAFTED; THE THREE (R110) EDITS UNAPPLIED; THE CEILING`S NEXT WORDING UNRULED** until the
        f4 attempt closes (R143)(1).
    (e) ### **defect (d)`s reading waits on the author**: power_contDiff and paperFT_power carry the window`s support in
        [-L, L], undeclared on the face, graded DERIVES as L3`s standing context.
========================================================================================================
