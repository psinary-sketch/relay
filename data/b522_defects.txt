    (a) ### **THE SUITE GENERATOR MISSED ONE ACT NUMBER**: `G-RECEIPT-IN-FULL` still asked for `ACT b521` in this act`s
        ferry after generation (the arm`s text was carried from b521`s suite and no replacement named it). ### Found by
        reading the generated suite for stray act numbers BEFORE its first run; repaired in place (line 421). b521`s
        defect (e)`s species: a generator`s rename list is a claim about every string it must touch.
    (b) ### **THE BASH HEREDOC COLLAPSED BACKSLASHES** in a scratch rename script (the recorded trap); the script failed
        to parse and wrote nothing; it was moved into a file. No bank was touched.
