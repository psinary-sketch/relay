CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE FINITE-SIDE SEAL — a kernel module in the places-catalogue
pattern: the multiplicative structure at a finite place
decomposed, the object's silence proved per part, and
exhaustiveness over the decomposition, with the general and
the per-cell kept apart in the module's own header, one act.
Scope: a KERNEL BUILD inside the existing kernel's core,
imported by the certification file in the same commit; vanilla
Lean, `theorem` never `lemma`, `by omega` preferred for finite
arithmetic, zero-axiom target at the audit bar, no
native_decide, no sorry; NO new mathematics — every statement
compiled is a banked derivation, cited by act in the
docstring; NO claim beyond the finite places, the object as
defined, and the pairing of the shape the acts pairs; the
profile regenerated from source and read, every pre-existing
print byte-identical against the blob at HEAD, counts before
and after; registration sealed before any build; the suite
re-run after the push; nothing deposits.

STEP ZERO: the ferry scan; both censuses; push anything ahead
of origin, saying so if nothing is.

COMPONENT 1 — THE DECOMPOSITION, general: every nonzero index
modulo a prime power factors uniquely as a unit times a power
of the prime with exponent below the level — existence and
uniqueness, over all primes and levels, by induction in
vanilla Lean. This is the exhaustiveness statement's
foundation and the module says so.

COMPONENT 2 — THE SCALING PART'S SILENCE, general: for any
positive power of the prime, the index map "multiply by that
power" fixes no index off the ball — because a power of the
prime minus one is coprime to every power of the prime, so the
fixed-point congruence forces the index into the ball. Over
all primes, levels, and powers. Cited to the acts that derived
it; polarity controls (a map that does fix off-ball indices
shown to exist) beside it.

COMPONENT 3 — THE COMPACT PART'S SILENCE, per cell: the
compressed smear over units vanishes because the object's two
conditions make it orthogonal to the unit-invariants — decided
at the banked cells as exact arithmetic over the explicit
list, with a not-dead witness (the trace at the identity
returning the constrained dimension) and an UNAVAILABLE arm
where no control can exist. The header states that this is
per-cell and why (linear algebra over a cyclotomic field is
not vanilla-decidable in general).

COMPONENT 4 — EXHAUSTIVENESS: from Component 1, every
multiplicative action on the model's indices factors through
the two parts; with Components 2 and 3, the object is silent
under every such action at the cells where 3 is decided and
under the scaling part everywhere — stated as one theorem whose
hypotheses name exactly which cells and which generality, and
whose docstring quotes the fixed-point sentence from its
emitting act.

COMPONENT 5 — THE PROFILE AND THE ROWS: the module imported
by the certification file in the same commit; the profile
regenerated, read, compared on raw bytes; the coverage gate
still passing; the correspondence rows written for each
theorem with its generality stated in the row; the faces
ledger's fixed-point-silence row updated through the writer to
PROVED-GENERAL for the scaling part and PROVED-AT-CELLS for the
compact part.

CLOSING: every result keyed; the lore gains "general and
per-cell are stated in the module header, never averaged";
M-2's row unchanged under its cap; the seam's debt item 1
restated; the patent clock restated; both censuses; the
TECHNE extraction and the fold named as next. Registration
gate; index queried; finite-side-seal keyed. Components
ordered. THE SHADOW: this act IS the shadow; the module's own
header carries what it certifies and what it does not.

Bank: data/b329_the_finite_side_seal.txt. Deviation rule
standing.

EXECUTION: ferry scan first; registration sealed before any
build; owners read at content; components in order; a claimed
compile reported only from its printed profile; full control
suite, re-run after the push; pins by ls-remote across all
three repos; the hook if PLACE-papers is touched; mirror if
it moves. STOP.

FOOT: after this act the finite side's silence is one
compiled statement with its generality honest in its own
header, sitting in the certification file beside the places
catalogue's seal; h2 stands exactly where the deposit left
it; locks last.

paste ends (part 1 of 1)
