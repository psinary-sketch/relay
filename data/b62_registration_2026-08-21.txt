====================================================================================================
b62 -- THE JUNCTION REVISIT -- THE RUN-REGISTRATION ON THE STAGED RECIPROCITY
REGISTRATION. BANKED BEFORE ANY DERIVATION STEP IS TRUSTED AND BEFORE ANY VALUE IS
STATED. The staged registration (data/junction_reciprocity_registration_STAGED_
2026-08-21.txt, with its two dated additions) is the frame; its branches and guard are
carried verbatim; divergences resolve in its favor and are recorded.
====================================================================================================
FERRY 2026-08-21 (part 1 of 1, receipt confirmed). Relay-only. NO SIGN SENTENCE.
h2 UNCHANGED BY CONSTRUCTION; the junction remains a fence and this act DERIVES at it.
The range law governs every sentence. NOTHING DEPOSITS.

SOURCES READ AT CONTENT BEFORE ANY STEP (rule b; the recorded addresses): the keystone
v2.0 section 1's carried archimedean derivation (the trace at infinity is the
epsilon-regularized one, W_inf^CC + int f eps; the sector split EXACT BY
MODE-DIAGONALITY; the epsilon-denominators the lambda-scale Sonin geometry; the sign
dictionary W_inf^CC = -A; the scale dictionary c(a) = pi a^2); the b35 bank AT ITS
LINES (the CC Lemma 5.4 pins t(0..4), assembly 22.9964757; THE SPLIT BY XI-INDEX
PARITY: E_1 share 14.177305459 = n even, (-1) share 8.8191383 = n odd; the tail's
parity UNDECLARED; "the shares are DATA at this pin"); the b32 bank (the eps'(1+)
evaluation pin's standing); the proved obstruction (R has no compact open subgroup --
no finite exact level at infinity); the inherited row 41 (the boundary line's address;
the mechanism line's two-readings wonder, marked).

THE GUARD (rule d, verbatim from b56): "no candidate admitted after this registration
-- the anti-numerology guard (c) governs; any match observed outside these is banked
OBSERVATION-ONLY at zero weight."

THE REGISTERED PRODUCT-LAW FORM AND THE ONE CANDIDATE (fixed HERE, before any
archimedean quantity is stated; the guard governs):
  THE LAW FORM L: over the set {2, infinity} adjoined to the flat odd places, the
  NORMALIZED chart traces multiply to a roster-independent positive-real constant:
  u_2 * u_inf in R_{>0}, where u_2 = tr(S)/q = 1 + i at the place 2 (rows 35-36,
  proved; odd places contribute 1) and u_inf is the unit part of the archimedean
  chart trace IF it derives.
  THE ONE CANDIDATE C1: u_inf has unit part zeta_8^{-1} = e^{-i pi/4} (equivalently
  (1-i)/sqrt(2)), determined up to an underived positive-real normalization by L and
  u_2 = sqrt(2) * zeta_8. No other candidate; the classical-reciprocity motivation
  REMAINS the navigator's assertion unless the derivation reaches it.

THE DERIVATION CHAIN (written BEFORE any value; each step to be graded derived /
underivable-at-source exactly):
  STEP 1 (banked): the finite chart trace's anatomy (b57): tr(S) = the diagonal sum
    of the transform kernel over the chart; tr(S|Son) = tr(S) - q by the ball-pair
    inclusion-exclusion. The archimedean analogue must be the regularized diagonal
    mass of the archimedean transform.
  STEP 2 (to derive from the recorded mode-diagonality): for a test object phi(g) in
    the sources' regularized class, the transform-weighted trace splits over the
    mu_4-graded modes: tr_reg(F phi(g)) = sum_lambda lambda * m_lambda(g) -- the
    mu_4-character-weighted mass sum, whose algebraic core is row 35's compiled
    character-sum shape. EXPECTED: DERIVES as structure from the recorded "sector
    split exact by mode-diagonality".
  STEP 3 (the identity limit): the archimedean CHART TRACE is the g -> identity
    limit of step 2 inside the epsilon-scheme. TO CHECK AGAINST THE SOURCES: whether
    any recorded object fixes a scheme-invariant value for that limit. THE RECORDED
    STATE: the pins are at the eps'(1+) evaluation of ONE assembly (CC Lemma 5.4's
    t(n) series) -- a declared test-object pin; the no-compact-open theorem is the
    recorded structural reason no finite-level shortcut exists.
  STEP 4 (the mu_4 resolution): the weighted sum of step 2 needs the FOUR sector
    masses separately -- in particular m_i - m_{-i} for the imaginary part. THE
    RECORDED STATE: the b35 split is by XI-INDEX PARITY (E_1 vs (-1) inside the even
    assembly; the odd-sector defect Delta_- a COMBINED object; the tail's parity
    undeclared); a mod-4, +-i-separated resolution is not among the recorded
    derivations.
  STEP 5 (the comparison): only if steps 3-4 derive does any value meet the law form
    L; the commensurability is declared at step 2 already: the object is a COMPLEX
    MASS IN THE FUNCTIONAL'S OWN UNITS at a declared (g, eps) -- comparable to the
    integer-unit finite side only through the underived limit and resolution.

REGISTERED EXPECTATION (stated before the run, from the recorded state read above):
BRANCH (gamma) -- underivable at the sources, at steps 3 and 4 exactly; the surprise
protocol (rule e) stands armed should the derivation instead reach a value (branch
(alpha) would then double-check per the protocol; branch (beta) if a value derives
and mismatches). THE BRANCHES THEMSELVES: verbatim from the staged registration
((alpha) the derived partner; (beta) the exact mismatch; (gamma) the named
underivability closing as the poster's SECOND SPECIFICATION -- what the sources would
need to fix -- first-class).

THE BOUNDARY-PHASE QUESTION (component 3's second clause): whether the derived
quantity reads as the boundary phase of the inherited object (row 41; the mechanism
line's two-readings wonder) -- under (gamma) this inherits the underivability and the
wonder KEEPS its grade, said plainly.

STRIKEABLE COMPONENT 1 (disjoint files; the author's ruling by paste): the boundary
vocabulary for row 41's objects -- the boundary-weight shift (the operator); the
boundary line (the classical defect direction, at the ball-boundary shell); the
boundary index (the (1,1) count); the boundary phase (the extension parameter) --
classical terms preserved at cite as dated aliases in the rows; the loom notes the
ruling; banks and pins untouched.

Bank: data/b60_junction_revisit.txt (the ferry's name, verbatim). Deviation rule
standing. Divergence note (rule a): none -- the ferry's components map onto the
staged frame without conflict.
====================================================================================================
