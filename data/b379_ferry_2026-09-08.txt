CONCURRENCY: SOLO (research seat). FERRY_STANDING v2 by
reference; anchors by the tool; extract-to-disk for every read;
registration LOCKED before any write, chained on every pre-lock
gate and re-checked if the registration is rewritten; every
absence claim carries a positive control that finds a known
presence first; an error exit is not an answer. The instrument
lane and the wave stay parked; nothing deposits. No class
ruled, no declaration moved, no list closed.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b379 - THE APPARATUS AXIS RE-SCORED, AND TWO FILINGS. The
executor's draft is ADOPTED, with three additions.

ADDITION ONE - THE DIRECTION IS REGISTERED BEFORE THE RUN: a
widened matcher finds more, never fewer, so the re-score can
only move a document toward certifying. Register that the
corrected population is bounded below by the prior figure and
that the both-axes quadrant can only grow, and report the
corrected quadrant table with both the prior and corrected
columns side by side. State which of the ruling's options the
correction weakens, without recommending any.

ADDITION TWO - THE ROW CATEGORIES, from what b378 found: a
cited name may belong to a library rather than a corpus kernel;
a row may name a corpus document rather than a terminal, which
the front door already calls manuscript-resident; a name may
live on a tag and no branch. Add these as categories to the row
taxonomy, with the front door's own words quoted where it
already has one, so a checker stops reporting a category as an
absence. Fixtures both polarities.

ADDITION THREE - TWO FILINGS ABOUT A DOCUMENT OUTSIDE THE TREE,
filed and NOT repaired: (i) the download-layer narrative book's
registry row carries it at an earlier version as non-keystone
and not for publication, while the loom carries it at a later
version as a Tier C narrative keystone gated on the patent
gate. Quote both, name the precedence rule the corpus states,
and file the drift for the author - the registry is not edited
by this act. (ii) That document is outside the repo tree by
ruling and so was outside the census's population on both axes.
File that fact where the ruling's evidence sits, so the ruling
can say whether it governs documents outside the tree. Neither
filing opens work on the document itself.

CLOSING: the trails ledger through its writer; the four lists
restated OPEN; correspondence rows; keys; both censuses; pins
by ls-remote through the roster; hook and mirror where owed;
the next ferry as DRAFT - NAVIGATOR EDITS, proposing only what
this act found. The navigator's expectations: (F1) the
corrected apparatus-axis population is materially larger than
the prior figure; (F2) at least one document moves into the
both-axes quadrant that the prior predicate placed outside it.
Each refutable by a printed classification. h2 where the
deposit left it, and this act makes no claim about it in either
direction.

paste ends (part 1 of 1)
