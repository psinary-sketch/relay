====================================================================================================
b431_components.py -- THE LongGapsBetweenPrimes GRADING, AND THE TYPE-D QUESTION.
====================================================================================================
  ### THE REGISTRATION WAS LOCKED BEFORE ANY CLONE, FETCH OR BUILD.
  ### **NO CORPUS FILE IS WRITTEN BY THIS TOOL. ### NO KEYSTONE IS EDITED.**

====================================================================================================
  COMPONENT 1 -- THE ADDRESSES, PINNED; AND BOTH PINS OF THE PRE-LOCK READ, SIDE BY SIDE.
====================================================================================================
  ### THE SEAL THIS RUN READ OFF THE LOCKED FACE : fce23cad96671d86c4978fb3c393847db7c570a63ae244d708f9776b6d7f2cc0

    the repository, github.com/openai/LongGapsBetweenPrimes
      pin read at STEP ZERO, BEFORE the lock : 03a1190d0bc5502d9f54eeb60ad3e45e22b0df0b
      pin read AFTER the lock, from the clone: 03a1190d0bc5502d9f54eeb60ad3e45e22b0df0b
      ### **THE TWO AGREE : True**
      ### The pre-lock read was a fault of sequencing and the face declares it. ### It is
      ### printed here rather than absorbed, and had the pins differed this line would say
      ### so. ### **A DECLARED BREACH THAT CHANGED NOTHING IS STILL A DECLARED BREACH.**

    paper url      : https://cdn.openai.com/pdf/51126fac-1b68-4128-9666-c908bcc16033/long_gaps.pdf
    paper status   : 200
    paper bytes    : 166213
    paper sha256   : e7ede84ba0b24b9d14c58d8a846876fd9f4d021b2101d0c43926ddb7f12f104b
    ### **TWO ADDRESSES SUPPLIED, TWO RESOLVED, `0` GUESSED.**

====================================================================================================
  COMPONENT 2 -- THE PAPER'S THEOREM 1.1, AND ITS COVERING CONSTRUCTION, VERBATIM.
====================================================================================================
    ### THEOREM 1.1, AS THE PAGE STATES IT:
        Theorem 1.1. There is an absolute constant c> 0 such that, for all suﬃciently large X,
        G(X) ≥c logX(log2X)2 log4X (log3X)2 . This improves (1.1) by a factor log2X. The main
        input is the following statement about translates of a set of integ

    ### AND PROPOSITION 1.2, WHICH THE PAPER CALLS ITS MAIN INPUT:
        Proposition 1.2 (Short translates) . There is an absolute constant 0 < δ < 1/2 such that
        the following holds for all suﬃciently large x. Let x < H≤ x(logx)2, let Q = Q(x), and let
        S ⊆ [1,H ] ∩ Z have cardinality k ≤δx. For every integer 0 ≤b<Q , there is an integer 1 ≤t
        ≤ex such that b +Qt +s is composite for every s ∈S. 1 The estimates are uniform in H, b,
        and S. Section 2 outlines the choice of weight and its truncation. Sec

    ### THE COVERING CONSTRUCTION, AT THE PASSAGE THAT STATES IT:
        maining positions. Let S be the set left after these choices. Mertens' product estimate
        gives |S| ≤ |S0| ∏ w<p≤z (1 − 1/p) ≪ H logx logw logz ≪ H(log2x)2 (logx)2 log3x ≪ηx. (3.1)
        Choose arbitrary classes for x/2 < p≤ x and discard any further positions covered from S.
        For suﬃciently small η, the remaining set satisﬁes |S| ≤ δx. By the Chinese remainder
        theorem, choose 0 ≤ b < Qwith b ≡ −ap (mod p) for every prime p ≤ x. For a covered
        position n, the integer b +Qt +n is divisible by its covering prime for every t ≥ 1, and
        is greater than that prime. Proposition 1.2 handles the remaining positions S. It supplies
        1 ≤ t ≤ ex such that N + 1,...,N +H are co
    ### AND WHAT `Q` IS, from the paper's own line: Q(x) = product of p <= x

====================================================================================================
  COMPONENT 3 -- THE REPOSITORY, THE TERMINAL, AND THE TOOLCHAIN.
====================================================================================================
    ### **THE REPOSITORY NAMES ITS OWN MAIN RESULTS, IN `formalization.yaml`** -- which is
    ### where b429 learned to look when a README named no theorem.
        LongGapsBetweenPrimes.long_prime_gaps
        LongGapsBetweenPrimes.long_gap_theorem
        LongGapsBetweenPrimes.short_translates
    ### the order names "the theorem named as Theorem 1.1 in LongGapsBetweenPrimes.lean";
    ### the manifest`s `sources` entry says it formalizes ### **"Theorem 1.1 and
    ### Proposition 1.2"**, and of the three declarations the one whose STATEMENT is
    ### Theorem 1.1 is `LongGapsBetweenPrimes.long_prime_gaps` -- chosen by reading the statements, not the names.
    LongGapsBetweenPrimes.lean:4502
        theorem long_prime_gaps :
            ∃ c X₀ : ℝ, 0 < c ∧ ∀ X : ℝ, X₀ ≤ X → ∃ n : ℕ,
              (Nat.nth Nat.Prime (n + 1) : ℝ) < X ∧
                c * (Real.log X * Real.log (Real.log X) ^ 2 *
                  Real.log (Real.log (Real.log (Real.log X))) /
                    Real.log (Real.log (Real.log X)) ^ 2) <
                  (Nat.nth Nat.Prime (n + 1) : ℝ) - Nat.nth Nat.Prime n := by
          obtain ⟨c, hc, hgap⟩ := long_gap_theorem

    foreign toolchain            : leanprover/lean4:v4.33.0
    the corpus`s own             : leanprover/lean4:v4.29.1 (SIDE-global-section)
    ### **THE FOREIGN PIN IS NEWER THAN THE CORPUS`S**, as b429 also found.

====================================================================================================
  COMPONENT 4 -- THE BUILD, AND THE PROFILE FROM THE PRINTER'S OWN OUTPUT.
====================================================================================================
    jobs, one at a time : 1
        LongGapsBetweenPrimes (retry 3, foregrou rc=0  OK
    ### **BUILT : True**
    ### THE HISTORY, NOT ONLY THE OUTCOME:
      attempts before this one that did not finish : 3
      ### **THREE ATTEMPTS WERE KILLED FOR LOW MEMORY**, the last of them with a SINGLE
      ### worker at ~1.5 GB and climbing -- so the pressure was never parallelism.
      ### b429 recorded that `lake` has no `-j`; ### **THAT IS STILL TRUE AT LAKE 5.0.0 /
      ### LEAN 4.33.0**, checked in `lake --help` and `lake build --help` in full.
      ### **WHAT ACTUALLY UNBLOCKED IT WAS RUNNING IN THE FOREGROUND**: the kills came
      ### from this harness protecting the system against BACKGROUND tasks, not from
      ### Lean`s own allocator. ### 8707 jobs, rc=0, on the foreground attempt.
      ### **AND THE MACHINE WAS NEVER THE OBSTACLE IT LOOKED LIKE** -- which is worth
      ### recording, because b429 banked the OOM as a property of the machine.

    ### THE PRINTER'S OUTPUT, THE TERMINAL'S OWN LINE (A2):
        'LongGapsBetweenPrimes.long_prime_gaps' depends on axioms: [propext, Classical.choice, Quot.sound]
    axioms printed                        : ['propext', 'Classical.choice', 'Quot.sound']
    beyond the standard three             : 0 
    sorryAx anywhere in the printer output: 0

    ### THE SOURCE SWEEP, COMMENTS AND STRING LITERALS STRIPPED FIRST (BAR 5):
        Challenge.lean                     raw 1   AS A TERM 1
        LongGapsBetweenPrimes.lean         raw 0   AS A TERM 0
    ### **RAW 1, AS TERMS 1** -- and the manifest declares `sorry_count: 0`.

====================================================================================================
  COMPONENT 5 -- THE GRADE, UNDER (R40), AGAINST THE PAPER'S THEOREM 1.1.
====================================================================================================
    ### THE CLAIM NAMED : the paper's Theorem 1.1, quoted in component 2.
    ### THE TERMINAL    : LongGapsBetweenPrimes.long_prime_gaps

    ### THE READING, CLAUSE BY CLAUSE:
      an absolute constant c > 0                           : True
      for all sufficiently large X                         : True
      G(X) is a gap between CONSECUTIVE primes             : True
      the gap is below X                                   : True
      the bound is log X (log2 X)^2 log4 X / (log3 X)^2    : True
    ### **5 OF 5 CLAUSES OF THEOREM 1.1 MAP TO THE TERMINAL'S OWN TEXT.**

    ### THE FOUR CLAUSES, APPLIED -- b430 SETTLED THAT THEY ARE THE DISCIPLINE'S:
      (1) the profile from the printer, sorryAx sought by name : True
      (2) the statement unfolded to base objects               : 5 of 5 clauses mapped
      (3) the fourth grade available                           : True (R40)
      (4) definitions checked by rowgen`s defenc, not read     : defenc False ; control fires True
          control: b431_control_stub := 0

    ### THE RESIDUE b429 NAMED, CARRIED FORWARD AND MEASURED RATHER THAN INHERITED:
      the repository ships `comparator.json`                   : True
      and `Challenge.lean`, the reference statement            : True
      ### **THE PROVED STATEMENT AND THE REFERENCE STATEMENT, COMPARED CHARACTER FOR
      ### CHARACTER : True**
      ### That is a stronger agreement than b429 could establish, where the definitions
      ### were agreed on a reading and no reference statement was shipped.
      Comparator RUN by this act                               : NO
      ### **AND THE REASON IS NAMED RATHER THAN LEFT AS A CHOICE**: Comparator requires
      ### `landrun`, a LINUX landlock sandbox, in PATH. ### This machine is Windows.
          landrun        on PATH : False
          lean4export    on PATH : False
          nanoda_bin     on PATH : False
      ### **SO THE RESIDUE IS NARROWED, NOT DISCHARGED**: the statement graded is proved
      ### to be the reference statement character for character, and the kernel-level
      ### judgement Comparator would add is UNAVAILABLE ON THIS PLATFORM, not declined.

    ### ### **GRADE, AGAINST THE PAPER'S THEOREM 1.1 : DERIVES**
    ### **THE GRADE NAMES ITS CLAIM IN THE SAME SENTENCE** (R40, BAR 7). ### Against any
    ### other claim it would have to be graded again, and this act grades it against one.

====================================================================================================
  COMPONENT 6 -- THE TYPE-D QUESTION, DECIDED BY UNFOLDING.
====================================================================================================
    ### **BAR 8: NEITHER SIDE IS READ BY ITS NAME.** ### One of these is CALLED
    ### `crt_exhaustiveness`; the other USES the Chinese remainder theorem in its proof.
    ### Neither fact is evidence about what either STATES.

    ### SIDE A -- THE PAPER'S COVERING CONSTRUCTION, quoted in component 2:
        maining positions. Let S be the set left after these choices. Mertens' product estimate
        gives |S| ≤ |S0| ∏ w<p≤z (1 − 1/p) ≪ H logx logw logz ≪ H(log2x)2 (logx)2 log3x ≪ηx. (3.1)
        Choose arbitrary classes for x/2 < p≤ x and discard any further positions covered from S.
        For suﬃciently small η, the remaining set satisﬁes |S| ≤ δx. By the Chinese remainder
        theorem, choose 0 ≤ b < Qwith b ≡ −ap (mod p) for every prime p ≤ x. For a covered
        position n, the integer b +Qt +n is divisible by its covering prime for every t ≥ 1, and
        is greater than that prime. Proposition 1.2 handles the remaining positions S. It supplies
        1 ≤ t ≤ ex such that N + 1,...,N +H are co

      ### WHAT IT QUANTIFIES OVER : one residue class `a_p` FOR EVERY PRIME p <= x, with
      ### `Q = Q(x) = prod_{p <= x} p`, assembled by CRT into one class `b` mod `Q`.
      ### **THE NUMBER OF MODULI IS pi(x) AND IT GROWS WITHOUT BOUND**, because the
      ### theorem is a statement about all sufficiently large X and x = (log X)/3.
      ### WHAT IT CONCLUDES : that every covered position is composite for every t >= 1.

    ### SIDE B -- THE CONSPIRACY KEYSTONE'S COMPILED LEMMA, quoted at its own file:
      SIDEEffects/Phase15/Module1.lean:146  crt_exhaustiveness
          theorem crt_exhaustiveness (sc : StructuralCoupling) :
              ∃ m : ModularCoupling, ∀ n : ℕ, sc.eval n ↔ m.eval n :=
            ⟨to_modular sc, fun n => to_modular_correct sc n⟩
          
          def TypeD : Type :=
            { sc : StructuralCoupling //
              ¬ ∃ m : ModularCoupling, ∀ n, sc.eval n ↔ m.eval n }
          
          theorem no_type_d_conspiracies : IsEmpty TypeD := by
      SIDEEffects/Phase15/Module1.lean:139  to_modular
          noncomputable def to_modular (sc : StructuralCoupling) : ModularCoupling :=
            ofPeriodic sc.period sc.period_pos sc.eval
          
          theorem to_modular_correct (sc : StructuralCoupling) (n : ℕ) :
              sc.eval n ↔ (to_modular sc).eval n :=
            (ofPeriodic_eval sc.period sc.period_pos sc.eval sc.eval_periodic n).symm
          
          theorem crt_exhaustiveness (sc : StructuralCoupling) :
              ∃ m : ModularCoupling, ∀ n : ℕ, sc.eval n ↔ m.eval n :=
      SIDEEffects/Phase15/Module1.lean:111  ofPeriodic
          noncomputable def ofPeriodic (L : ℕ) (hL : 0 < L) (P : ℕ → Prop) :
              ModularCoupling where
            moduli := {L}
            allowed_residues := fun q hq =>
              haveI : NeZero q := ⟨by rw [Finset.mem_singleton] at hq; subst hq; exact hL.ne'⟩
              haveI : DecidablePred (fun r : ZMod q => P r.val) := Classical.decPred _
              Finset.univ.filter (fun r : ZMod q => P r.val)
          
          theorem ofPeriodic_eval (L : ℕ) (hL : 0 < L) (P : ℕ → Prop)
      SIDEEffects/Phase15/Module1.lean:18  StructuralCoupling
          inductive StructuralCoupling : Type where
            | residue (q : ℕ) (hq : 0 < q) (a : ZMod q) : StructuralCoupling
            | divisible (q : ℕ) (hq : 0 < q) : StructuralCoupling
            | coprime (m : ℕ) (hm : 0 < m) : StructuralCoupling
            | conjunction (left right : StructuralCoupling) : StructuralCoupling
            | disjunction (left right : StructuralCoupling) : StructuralCoupling
            | shifted (k : ℕ) (inner : StructuralCoupling) : StructuralCoupling
          
          def StructuralCoupling.eval : StructuralCoupling → ℕ → Prop
      SIDEEffects/Phase15/Module1.lean:36  ModularCoupling
          structure ModularCoupling : Type where
            moduli : Finset ℕ
            allowed_residues : (q : ℕ) → q ∈ moduli → Finset (ZMod q)
          
          def ModularCoupling.eval (m : ModularCoupling) (n : ℕ) : Prop :=
            ∀ q, ∀ h : q ∈ m.moduli, (n : ZMod q) ∈ m.allowed_residues q h
          
          /-! ## Period -/

      ### WHAT IT QUANTIFIES OVER : one `StructuralCoupling` -- a SYNTACTIC term built
      ### from finitely many explicit congruence, divisibility and coprimality
      ### conditions, whose `period` is the lcm of its own moduli and is FIXED BY THE
      ### TERM. ### WHAT IT CONCLUDES : that term is extensionally a `ModularCoupling`.
      ### **AND THE WITNESS`S MODULUS SET IS `moduli := {L}` -- A SINGLETON.**
      ### So the witness uses ONE modulus, the term`s own period. ### **NO PRODUCT OVER
      ### PRIMES APPEARS IN THE STATEMENT OR IN THE WITNESS**, and no Chinese remainder
      ### theorem is invoked: the proof is the periodic lift, `ofPeriodic_eval`.

    ### THE DECIDING CLAUSES, SET AGAINST EACH OTHER:
                                   | THE PAPER                      | THE KEYSTONE`S LEMMA
      the moduli                   | every prime p <= x             | one: the term`s own period
      how many                     | pi(x), unbounded in x          | exactly one, fixed by the term
      what varies                  | x, and with it the modulus set | nothing; the term is given
      what CRT does                | assembles pi(x) classes into one | nothing; it is not used
      the conclusion               | covered positions are composite | extensional equality of two
                                   |                                | predicates

    ### ### **VERDICT : TWO THEOREMS SHARING A NAME**
    ### **AND THE PARTING IS AT WHAT IS QUANTIFIED**, which is where the navigator said
    ### it would be: ### **A FIXED PERIOD AGAINST ALL MODULI.** ### The measurement is
    ### sharper than the prediction in one respect and the act says so: the keystone`s
    ### lemma does not range over all moduli and then collapse them -- ### **ITS WITNESS
    ### NEVER LEAVES A SINGLE MODULUS AT ALL.**

    ### **WHAT THE SHARED NAME IS.** ### The paper invokes the Chinese remainder theorem
    ### as a TOOL, to build one class mod Q out of pi(x) chosen classes. ### The corpus`s
    ### lemma is NAMED after it -- `crt_exhaustiveness` -- and the keystone`s prose says
    ### the exclusion covers *"what the Chinese Remainder Theorem explains at every
    ### finite modulus"*. ### **THE NAME AND THE PROSE DESCRIBE THE INTUITION; THE
    ### STATEMENT IS THE PERIODIC LIFT.** ### That is a finding about the corpus`s own
    ### keystone and it is ROUTED, not acted on: ### **NO BRIDGE IS TYPED, THE KEYSTONE
    ### IS NOT EDITED, AND ITS GRADE IS NOT MOVED.**

    ### AND WHAT THE VERDICT DOES NOT SAY:
      ### **IT DOES NOT SAY THE KEYSTONE`S LEMMA IS FALSE.** ### It is true and cleanly
      ### proved; a predicate built from finitely many congruence conditions IS a
      ### congruence condition modulo the lcm of its moduli.
      ### **IT DOES NOT SAY THE PAPER`S CONSTRUCTION IS AN INSTANCE OF IT, OR THAT IT
      ### IS NOT.** ### The two statements simply do not quantify over the same thing,
      ### so neither contains the other, and no bridge is available to be typed.

====================================================================================================
  ### MISSES : 0
  ### **A MISS IS PRINTED, NEVER PATCHED.**
====================================================================================================
