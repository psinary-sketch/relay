**Synthesis — the standing next mathematical target.** The R4 tail (`TailBoundPremise` + `ExplicitFormulaDecomp` in Lean) is the **analyst-startable, community-scale** leftward move — the standing next mathematical target, beside h2's five doors. *(Calibrated 2026-07-22, K3 scoping: the target is analyst-startable and community-scale — NOT "startable this afternoon." Both premises are **Mathlib-absent-with-ingredient-named** at the lv pin; the honest work is building the Bombieri–Lagarias explicit formula and the Voros tail bound in Mathlib, a real analytic development, not a programme sitting. See "K3 scoping — the frozen R4-tail statements" below.)* It reinforces **VAJRA-PLINKO row 1** (§I-bis): the R4 positivity-ladder chip is the one about to drop. The only *unconditional* A's in the corpus are the **surround** (`h1_complete_at_Phi`) and **sub-propositions** (Route 2's on-line derivative geometry; the four σ=1/2 identifications); **RH proper** is reached only *conditionally on h2* (Route 3, T3 goal state) or as a *B-with-named-tail* (R4). Every conditional route turns on the one hinge, h2 — highest-leverage move; R4's tail is the most *concrete* move (a named analytic estimate, not a totality premise).
