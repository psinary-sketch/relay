1. **No *unconditional* positivity is proved, and the bench numbers remain measurements** — but the finite range is no longer only measured. λ_n > 0 for n ≤ 130 on this bench is a *measurement* (§V; Keiper 1992 measured it too). Separately, and since this bench was built, a finite range is now **derived, not measured**: `PartialPositivity.partialPositivity_finiteRange` (SIDE-lv-conservation `v0.8.0` = `6efa9e5`) *proves* λ_n ≥ 0 for 1 ≤ n ≤ N₀(T) ≈ 2T² from verified zeros to height T, the Bombieri–Lagarias explicit-formula decomposition, and the Voros tail bound — a proof, not a table. Two things keep it honest and short of the criterion. First, it is **finite-range**: the all-n tail is the open gap, so this is a certificate, **not RH** (the Li criterion is a statement about *all* n, and this touches n ≤ N₀(T)). Second — and this is the point §C.6 makes — its cutoff N₀(T) ≈ 2T² is *exactly* Voros's detection threshold: the certificate proves positivity up to precisely where an off-line zero could first register, and no further. So the derived range and the discriminating range meet at 2T² and do not overlap. The certificate sharpens the boundary between proved and open; it does not cross it. *(K3 scoping, 2026-07-23: the certificate's two named premises — the Bombieri–Lagarias `ExplicitFormulaDecomp` and the Voros `TailBoundPremise` — are now frozen verbatim and graded **Mathlib-absent-with-ingredient-named** at the lv pin, with the bounded increment **W-ORD-P1-FINSET** the one analyst-startable move; PATHS §ANNEX A / VAJRA-PLINKO row 1.)*
