====================================================================================================
b304_hooks.py -- THE PRE-PUSH HOOK, INSTALLED IN ALL 4 ROSTERED REPOS AND EXERCISED IN BOTH
                 POLARITIES. ### ONE TRACKED SOURCE, ONE INSTALL EACH.
====================================================================================================
  classifier fixture                                         got/expected                     agree
  the hook refusing, its own message                         REFUSED/REFUSED                  YES
  a clean dry-run through the hook                           ALLOWED/ALLOWED                  YES
  ### the empty-scope state, NOT a pass                      NOTHING TO PUSH/NOTHING TO PUSH  YES
  ### a refusal that is the REMOTE's, not the hook's         OTHER/OTHER                      YES
  ### a network failure is not a refusal                     OTHER/OTHER                      YES
  classifier self-test : PASS

  tracked source : tools/git-hooks/pre-push
  installed at   : .githooks/pre-push in each repo ### **TRACKED; A CLONE CARRIES IT**
    relay                  core.hooksPath = '.githooks'    ### READS THE TRACKED DIR
    SIDE-global-section    core.hooksPath = '.githooks'    ### READS THE TRACKED DIR
    PLACE-papers           core.hooksPath = '.githooks'    ### READS THE TRACKED DIR
    SIDE-effects           core.hooksPath = '.githooks'    ### READS THE TRACKED DIR
  bytes / sha256 : 3078 / 146330836e1ae8e945c4bd65a756dfec2f5b78f91f623272ae6545a31ff93abb

  repo                   install                                sha256 on disk (first 32)
  relay                  ALREADY IDENTICAL                      146330836e1ae8e945c4bd65a756dfec
  SIDE-global-section    REPLACED (previous kept as pre-push.b304-backup) 146330836e1ae8e945c4bd65a756dfec
  PLACE-papers           REPLACED (previous kept as pre-push.b304-backup) 146330836e1ae8e945c4bd65a756dfec
  SIDE-effects           REPLACED (previous kept as pre-push.b304-backup-1) 146330836e1ae8e945c4bd65a756dfec
  ### ALL 4 BYTE-IDENTICAL TO THE TRACKED SOURCE : True  PASS

  ### THE EXERCISE. ### **BOTH POLARITIES, AGAINST `main`, UNDER `--dry-run`.**
  ### A throwaway empty commit is made so the scope is NOT empty -- git skips the hook
  ### entirely when there is nothing to push, and a pass in that state is a pass over
  ### nothing. ### **THE COMMIT AND BOTH BRANCHES ARE DELETED AFTERWARDS.**

  repo                   NEGATIVE     POSITIVE     HEAD ok   remote ok branch restored
  relay                  ### **SKIPPED -- working tree carries 2 uncommitted path(s). ### THE GUARD IS NOT EXERCISED HERE.**
        ### **RUN THIS AFTER THE PUSH, WHEN THE TREE IS CLEAN** -- which is where
        ### the closing sequence has always run it.
  SIDE-global-section    ### **SKIPPED -- working tree carries 1 uncommitted path(s). ### THE GUARD IS NOT EXERCISED HERE.**
        ### **RUN THIS AFTER THE PUSH, WHEN THE TREE IS CLEAN** -- which is where
        ### the closing sequence has always run it.
  PLACE-papers           ### **SKIPPED -- working tree carries 1 uncommitted path(s). ### THE GUARD IS NOT EXERCISED HERE.**
        ### **RUN THIS AFTER THE PUSH, WHEN THE TREE IS CLEAN** -- which is where
        ### the closing sequence has always run it.
  SIDE-effects           ### **SKIPPED -- working tree carries 1 uncommitted path(s). ### THE GUARD IS NOT EXERCISED HERE.**
        ### **RUN THIS AFTER THE PUSH, WHEN THE TREE IS CLEAN** -- which is where
        ### the closing sequence has always run it.

  ### REPOS FAILING : 4
  ### **INSTALLED BUT NOT EXERCISED HERE, AND SAID RATHER THAN IMPLIED:** ### the
  ### `held/*` refusal and the `DO NOT PUSH` ancestry refusal are present in the copied
  ### text and were NOT run. ### They are installed, not demonstrated.
  ### **AND THE GUARD IS NOW TRACKED AT `.githooks/`, SO A CLONE CARRIES IT (b371)** -- but
  ### `core.hooksPath` is LOCAL CONFIG, so ### **A FRESH CLONE STILL RUNS NO GUARD UNTIL
  ### ### SOMEONE RUNS `git config core.hooksPath .githooks`.**
  ### ### **AND ONE HAZARD THIS TOOL CANNOT CATCH, NAMED RATHER THAN IMPLIED:** ### the
  ### guard is a TRACKED TEXT FILE, so git rewrites its line endings on checkout. ### On a
  ### platform that checks it out with CRLF, ### **`#!/bin/sh` CARRIES A CARRIAGE RETURN**
  ### and the guard may not run at all.
  ### ### **`.gitattributes` NOW PINS `eol=lf` IN EVERY ROSTERED REPOSITORY (b372)**, and a
  ### fresh checkout of this guard was shown byte-identical to its blob in all of them.
  ### ### **THAT FIXES WHAT THE NEXT CHECKOUT PRODUCES, NOT WHAT IS ON A DISK TODAY**, and
  ### the identity arm above is still EOL-normalised, so ### **THIS ARM STILL WOULD NOT
  ### ### TELL YOU** -- what changed is that the hazard is now pinned shut upstream of it.
====================================================================================================
