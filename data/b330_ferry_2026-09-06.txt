CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE TECHNE EXTRACTION — the arc's methods written as dated
module drafts into TECHNE's private core, each mapped to
TECHNE's existing objectives and module families, each with
provenance to the act and incident that bore it and the tool
that mechanizes it, one act. Scope: FILINGS into the local
TECHNE clone the August extraction used (the origin/main line;
the other clone untouched; the two-clone divergence neither
reconciled nor mentioned as resolved); NOTHING PUSHED — TECHNE
stays private until its four provisionals are filed, and the
bank says so; NO research content enters TECHNE — method only,
no mathematics, no zeta, no object names beyond what a method
statement needs; no grade moves anywhere; registration sealed
before any write; the suite re-run after the commit; nothing
deposits.

STEP ZERO: the ferry scan; both censuses; push anything ahead
of origin in the three research repos, saying so if nothing
is; and the hygiene fix — a .gitattributes in the kernel
repository forcing line feeds on disk, mirroring the papers
repository's, committed with its effect verified on the
profile file (working file equal to its blob on raw bytes
after a re-run).

COMPONENT 1 — THE MODULES, one file each under
modules/2026-09/, claim-shaped (a method stated as what it
does, when it applies, and what it refuses), with provenance
lines (act, incident, relay tool path, date): the ferry-intake
scan with the struck-clauses record (strikes as enforceable
artifacts); the hedge audit (grade tokens and hedge stems
cannot share a sentence); needle pulling from emitting files,
owner and self (quotations pulled, never typed, never taken
from a quoter); the constructive quotation gate (a generator
cannot emit a paraphrase); the idempotent row-writer with
notation guard and read-back; the noise-floor gate with its
drift arm (the naive magnitude cut preserved as a must-fail
fixture); registration seal and satisfiability check, with
order and post-hoc bar marking; the extract-to-disk reading
protocol; exposure graded by call path before measurement;
the vacuity taxonomy in full (dead, inverted and substring
fixtures; empty scope; the always-firing detector; the check
at a zero; the gate that can never pass; the plausible-but-
wrong gate as its own fixture); the sealed-bars-found-
defective table; the two-routes rule; the resolving-power rule
with its price; the weaker-branch rule; the scope-bound-
constant rule; the cold-clone certification with the
module-set-equals-import-set gate; the census that licenses a
phrase; general-and-per-cell stated in a module's header; the
negative-control protocol (an instrument that has never said
no has said nothing; the family, the reach, and the library as
the three places a null can hide); certification of an
instrument against theorems it did not prove.

COMPONENT 2 — THE INDEX: modules/INDEX.md extended, each new
module mapped to its family (HARNESS_LORE, DISCRIMINATOR_
PROTOCOL, IMPORT_LEDGER, or a new family named once — VACUITY,
REGISTRATION, READING, CERTIFICATION, NEGATIVE_CONTROL), with
the August modules cross-referenced where a September one
supersedes or extends them, originals untouched.

COMPONENT 3 — THE PATENT NOTE, in the bank and not in TECHNE:
which of the new modules are plausibly novel as method, listed
for the methodology-and-patent day with no legal claim made;
the standing rule restated that nothing from TECHNE's core is
public until the provisionals are filed.

CLOSING: the local TECHNE commit's hash recorded in the bank
with "NOT PUSHED" beside it; the correspondence row; every
result keyed; both censuses; M-2's row unchanged under its
cap; the seam's debt item 1 restated; the patent clock
restated; the fold (b323 onward, seven acts) named as next.
Registration gate; index queried; techne-extraction keyed.
Components ordered. THE SHADOW: expected nothing; say so.

Bank: data/b330_the_techne_extraction.txt. Deviation rule
standing.

EXECUTION: ferry scan first; registration sealed before any
write; components in order; full control suite, re-run after
the research repos' push; pins by ls-remote across the three
research repos; TECHNE's local hash read back and NOT pushed.
STOP.

FOOT: after this act the arc's methods are claim-shaped
modules in TECHNE's private core with their provenance, the
kernel no longer drifts on line endings, and the patent day
has material; h2 stands exactly where the deposit left it;
locks last.

paste ends (part 1 of 1)
