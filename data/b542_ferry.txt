RULING (R152) BEGIN

RULING (R152), the author's, ratified by this paste and
strikeable: E-2026-09-25-5 FILED; THE INHERITANCE CENSUS
ACROSS THE FEDERATION; THE MONOGRAPH READ'S SECOND ACT
DEFERRED ONE ACT.

(1) E-2026-09-25-5 IS FILED, in the DEPOSIT-FACING list, as
drafted at relay data/b541_erratum_draft.md (19 rows), with
one Status line naming this act. Its two new kinds are named
in the entry's own heading words: the deposit cites a file
(ProductFormula_Prime.lean) and four terminals no version of
SIDE-kernel holds; and four sentences call the derivative of
completedRiemannZeta₀ ξ′.

(2) THE QUESTION THE CASCADE NOW ASKS OF EVERY KERNEL is the
one the navigator put in words and the author quoted back:
for a mechanism class C, does the federation hold a compiled
theorem of the INHERITS kind — a zero of ξ inherits C's
constraint, `IsNontrivialZero ρ → constraintC ρ`, with
constraintC stated about ξ or ζ at ρ — and of the SECOND
kind, `constraintC ρ → ρ.re = 1/2`? SIDE-kernel v1.5 holds
the FORCES kind as real algebra about σ (TheBridgeComplete.
lean:157-165, PoissonExhaustion.lean:43-69, read at the pin
this evening) and no theorem of the INHERITS kind. The census
of (3) settles by statement whether any other kernel does.
The census reads and does not write: no kernel is edited and nothing is
compiled at this act except probes.

(3) THE INHERITANCE CENSUS runs over every Lean repository
under psinary-sketch that SPIRAL_MAP lists, at its cited pin
(DEPOSIT-PIN where one exists, else WORKING-HEAD), including
SIDE-interfaces, SIDE-effects, the six checkpoint kernels,
SIDE-global-section, SIDE-li-map, SIDE-trivium, SIDE-window
and SIDE-grh-transfer, and TECHNE-Core if the author's clone
is on D: (private; read locally, nothing of it copied into
any public bank — its rows carry the module name and the
statement's shape and nothing more). For every theorem or def whose
statement mentions a zero of zeta or xi by any of the
kernel's names (IsNontrivialZero, is_xi_zero, riemannZeta _
= 0, completedRiemannZeta _ = 0, completedRiemannZeta₀ _ =
0, zetaZeroConfig, ZeroLoc, mellin Phi, a `zero` structure
field, or a docstring saying "zero of"), one row: kernel,
pin, module:line, the statement verbatim, KIND (INHERITS /
FORCES / NEITHER / BOTH), and for a INHERITS-kind row the class
it inherits and its grade by statement. A theorem is BOTH
exactly when its hypothesis is a zero of ξ stated about ξ and its
conclusion is ρ.re = 1/2 with no other hypothesis; such a
row is RH proved and is not expected.

(4) The per-class dichotomy the navigator expects, refutable
row by row: for each of C1–C7, either the inherited
constraint is a true theorem that does not force ρ.re = 1/2
(C1: conj ρ is a zero; C3: 1 − conj ρ is a zero; C6: the
Cauchy–Riemann equations hold at ρ; C7: ρ appears in the
Hadamard product), or the constraint that forces ρ.re = 1/2
is itself equivalent to RH at ρ (C2: the balance at ρ.re;
C4, C5 as the kernel states them). No class has both columns
T0. If the census finds a class with both, that row is the
theorem and the act stops on it, printed verbatim, before
anything else is written.

(5) The monograph read's act two (Ch 26 to end, 110
sentences, §27.3 among them) is the act after this one, so
that §27.3's register-3 sentences are read with the census
in hand. BALANCE_AND_POSITIVITY follows.

(6) The local branches push-b541 and push-b541-closing are
deleted by `git branch -d <name>` with each name written in
full; the standing delete rule is satisfied by the explicit
name.

RULING (R152) END

FERRY BEGIN — paste for Claude Code (part 1 of 1; confirm receipt-in-full)

ACT b542 - E-2026-09-25-5 FILED; THE INHERITANCE CENSUS ACROSS THE FEDERATION, under (R152).

Read before anything, and cite by path and line: relay data/b541_erratum_draft.md whole; ERRATA.md's partition block; SPIRAL_MAP.md's kernel table (every repository, its DEPOSIT-PIN and WORKING-HEAD columns, read live per its own rule); SIDE-kernel v1.5 Bridge/TheBridgeComplete.lean:150-252 and Kernel/PoissonExhaustion.lean whole; Kernel/Integration.lean's StructuralExhaustiveness definition and rh_from_structural_exhaustiveness; Bridge/ConservationBridge.lean whole; SIDE-lv-conservation T3_StepNineBridge.lean:44-70; the monograph's §10.1 syllogism (A_Place_to_Stand.md, the five numbered steps) and §15.4, printed, because the census is the compiled form of steps 2 and 4.

COMPONENT 1 - THE ERRATUM. File E-2026-09-25-5 per (R152)(1); DEPOSIT-FACING bullet appended after E-4's; line range and backtick count printed.

COMPONENT 2 - THE CENSUS. (a) Enumerate the repositories from SPIRAL_MAP; print the list with the pin each is read at and whether a local clone exists on D: (clone, without writing back, into a scratch path made by mktemp for any that don't; nothing written back). (b) For each, grep every .lean file for the needles of (R152)(3) (print the needle list); for every hit inside a theorem, lemma, def or structure statement, extract the full statement to its `:=` or `where` (the b533 tool's overrun on equation-compiler declarations is known: read pattern-matched declarations up to their opening `|`). (c) One row per statement as (R152)(3) specifies. (d) Grade KIND by reading: INHERITS if the hypothesis is a zero of ξ/ζ stated about ξ/ζ and the conclusion is a constraint on ρ stated about ξ/ζ at ρ; FORCES if the hypothesis is a constraint about a real σ or about ρ and the conclusion is σ = 1/2 or ρ.re = 1/2; BOTH per (R152)(3); NEITHER otherwise. (e) For SIDE-kernel's seven classes, print the two-column dichotomy of (R152)(4) with the row (or ABSENT) that fills each cell. (f) For SIDE-interfaces' parametric mechanism theorem and SIDE-effects' exclusion engine, print their main terminals' statements verbatim and grade them the same way; say in one line whether either takes a zero of ξ as input at all. (g) TECHNE-Core rows, if read, carry module name and statement shape and nothing more, per (R152)(3).

COMPONENT 3 - THE FINDINGS ENTRY, titled by its objects: "The inheritance census: every compiled statement in the federation that mentions a zero of zeta or xi, by kind, with the per-class dichotomy for SIDE-kernel's seven classes". It carries the counts (rows, INHERITS, FORCES, BOTH, NEITHER; per repository), the seven-class table, and the sentence the census earns about the catalogue: which classes have a compiled inherited constraint, which have a compiled forcing constraint, and whether any class has both. A Correspondence row for each INHERITS-kind and BOTH-kind terminal found.

COMPONENT 4 - THE BRANCHES. Delete push-b541 and push-b541-closing per (R152)(6), after confirming by `git branch --merged` that both are merged; print the confirmation and the deletion lines.

THE NAVIGATOR'S EXPECTATIONS, each refutable by a printed line: (N1) the census finds no BOTH row in any repository. (N2) SIDE-kernel's seven classes have no class with a INHERITS-kind row stated about ξ (the kernel's `produces_offline` and `BalanceContradiction` are FORCES-kind algebra about σ). (N3) the INHERITS-kind rows the federation does hold are the reflection and conjugation facts (Zeta23's reflect_zero and its multiplicity twin, lv-conservation's reflection field, SIDE-explicit-formula's uses), and none of them forces ρ.re = 1/2. (N4) SIDE-interfaces' mechanism theorem and SIDE-effects' engine take no zero of ξ as input — they are stated over abstract systems. (N5) E-5 files with 0 backticks outside code spans; nothing at Zenodo; no kernel edited; nothing deposits.

The federation is read and not written [procedural]. Delete commands on verified absolute paths or explicit branch names in the same command (standing rule). Record, correspondence rows, trail record in the house form. Nothing deposits.

FERRY END (part 1 of 1)
