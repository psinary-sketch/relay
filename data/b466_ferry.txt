CONCURRENCY: SOLO (research seat; navigator Fable 5.1 and
auto-mode approval declared on the face). Standing text as
b465, carrying A2 and (R26)-(R74); the addendum slot per
(R50); scanned before sending, zero hits. Both instrument
lanes parked; the kernel lane OPENS for Component 2 only and
closes at the act's end; the wave parked under (R66); nothing
deposits.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b466 - THE ANTHROPIC PROPORTION RESULT READ AT ADDRESS
UNDER THE IMPORT BAR, AND ITS LEAN READ FOR THE EXPLICIT
FORMULA.

PRECONDITION: the paper, the informal note, the methodology
note, the transcripts and a clone of anthropics/zeta-23-lean
are in relay data with sha256 and, for the clone, the HEAD
SHA. If any is absent, print ABSENT and run only what the
present artefacts allow.

COMPONENT 1 - THE STATEMENT AT ADDRESS. Quote the main
theorem verbatim with every hypothesis; state whether it is
unconditional; state what it counts - zeros on the line,
simple zeros on the line, or both - in its own words; quote
the test-function class and its support condition; quote
the three cited ingredients as the paper cites them. Then
three reads under the record's own rules: (a) the import-
bar grade - the statement's class against the corpus's
objects, verdict K1 class boundary or not, with the support
sentence quoted; (b) row U1 - does the paper hold a
statement uniform in any of the six indices, and at which
site would it enter, or NONE; (c) the census - the row it
would add to PATHS's proportion table and section 24.4's
table, drafted as an era-annotation candidate and NOT
applied, authoring being routed. No bridge typed.

COMPONENT 2 - THE LEAN, READ NOT TRUSTED. In the clone at
its SHA: list every top-level theorem whose statement
mentions the explicit formula, Guinand-Weil, a sum over
zeros against a test function, or a prime sum; for each,
print the statement as declared and run #print axioms;
print the Mathlib pin from the lakefile. Then the one
question: does any declared statement match, or contain, the
Guinand-Weil decomposition the deposit names as pending
Mathlib formalization - the statement quoted from the
deposit's own line beside it? Verdicts: MATCHES / CONTAINS
WITH A NAMED SPECIALIZATION / DOES NOT. If MATCHES or
CONTAINS, file W-ORD-GW-IMPORT with its trigger: the author's
ruling on importing a third party's terminal under the bar.
Nothing is imported in this act.

CLOSING as the standing pattern, with Component 1's grade
table and Component 2's theorem list printed in the closing
message itself; the span counted by tool; the four lists
OPEN. The navigator's expectations: (N1) the theorem is
unconditional and counts zeros on the line, with simplicity
not asserted; (N2) the test class is restricted-support and
grades K1 at every site - class boundary, again; (N3) the
Lean declares an explicit-formula statement that CONTAINS
the deposit's pending input with a named specialization.
Each refutable by a printed result. h2 where the deposit
left it.

paste ends (part 1 of 1)
