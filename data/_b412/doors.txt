| Door | Register | Depth NOW | Compiled instrument (pin) |
| **centre** | goal state | `goal ⇐ h1 ∧ h2`; **h1 complete at Φ** (only h2 open) | `h1_complete_at_Phi`, `RegisterPentagon.goalState_sevenClasses_of_h2` (lv v0.6.0/v0.7.0) |
| **R4 — nearest** | balance → positivity | finite-range certificate λ_n ≥ 0 for n ≤ N₀(T)≈2T² (Voros threshold), on-line term proved; **finite-set conjunct now DERIVES** (`lowFinset_mem_iff`, v0.10.0); all-n tail open | `PartialPositivity.partialPositivity_finiteRange`, `blTerm_nonneg_of_onLine`, `lowFinset_mem_iff` (landed lv v0.8.0 / v0.10.0; deposited SIDE-lv-conservation v0.10.0 = `93c27ec`) |
| **R1 / R2** | universality / conservation | named-premise bridges (INTERFACES, ξ-pattern) | `RegisterPentagon.R1_universality_to_goal`, `R2_conservationHypothesis_to_RH` (lv v0.7.0; R2 restated from kernel v1.3) |
| **R3** | totality-through-places | h2 raw (`∀∃⟹∃∀`); NOT-COMPILED (T3 pinned); naive bridge disproved | countermodel `T3.T3doubleprime_general_commutation_fails` |
| **R5 — farthest** | spectral realization (input/output) | input certified {n²}; output disclaimed (HP); **the distance now carries a compiled boundary marker** | input `C5_input_at_Phi`; marker `certifiedInput_not_zeroRealizing` (lv v0.9.0); residue **C5-DIST-A** · **inherited spec (W-MINE-QUADRATIC, 2026-07-26): any R5 pairing inherits aperture 1/7, [[7,1,3]] distance-3, and FE-even = twist-even; the refined re-attempt question (sharpened by W-SIGN-7, 2026-07-27) is not "find Level-2 positivity" — had three ways (theta / Selberg / Waldspurger) — but "find Level-2-native **zeroActing**: the operator realizing one ζ's zeros without a family"** · **W-FAMILY (2026-07-27) confirms the door's object is SINGLE-OBJECT in kind (the realization X = `zeroActing`; X×X → universal positivity), not family-shaped — the family-workaround was a ℚ-side artifact** · **WARRANT RE-SPEC (post-completer, 2026-07-27): W-SIGN-5 warrant (a)'s trigger is now *positive-space evidence* — any structure bearing on `B(E)`-existence (Hermite–Biehler membership at the centre) or FE-even positivity on the zeros (the same target, two names, per the chiasmus); the old quadratic-form trigger is SUBSUMED (substrate-side, W-MINE-QUADRATIC). The de Branges deformation family (Lagarias 2006: `A_h = ½(ξ(s+h)+ξ(s−h))`, `B_h = (1/2i)(ξ(s+h)−ξ(s−h))`, `E_h = A_h − iB_h`) has all zeros **on-line and simple for |h| ≥ ½ unconditionally**; |h| < ½ is the open regime; the wall is the **h = ½ threshold** (= strip half-width = edge distance). **Warrant assessment (W3-1-CHILD): does NOT fire on evidence** — Conrey–Li 2000 (via Sarnak, `F(W)`, `W=1/ξ(1−iz)`) refuted de Branges' centre positivity for ζ; the family is approach-geometry, not a construction. The deformation files as the wall's **fourth face** (census-grade), not a re-attempt trigger.** |
