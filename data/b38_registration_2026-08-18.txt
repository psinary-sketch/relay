====================================================================================================
b38 REGISTRATION - THE APPORTIONMENT JOINT. NO MEASURED NUMBER IN THIS BLOCK.
====================================================================================================
W-CONSTRUCTION-1 act 10, items 1-3. Relay-only, bench-grade. ON THE FACE: the act
computes the joint; a match at the model would be FINITE-INSTANCE and goes only through
the closure protocol. NO SIGN SENTENCE AT COMPLETE ROSTER. h2 UNCHANGED. NOTHING
DEPOSITS. NOTHING CIRCULATES.

ITEM 1 - THE APPORTIONMENT, ITS REGISTERED DEFINITION AND ITS WARRANT (said before any
number, so nothing is fitted):
  W_inf is a distribution; its sector apportionment needs a DEFINITION. The registered
  one, with the warrant named:
    per mode n < N:  w_n(f) := tr_n_raw(f) - E2_n(f) - s_n * resid_N(f)
  where tr_n_raw = the raw dilation trace of mode n (the b36 machinery, unchanged);
  E2_n = the mode-n part of the eps integral (the single-mode mask of the qeps series -
  the mode-diagonality is EXACT, act 9); resid_N = Tr_raw_N - A - E2_N (the divergent +
  truncation residual, the act-8 resid47 at the same N); s_n = t(n)/sum_{m<N} t(m).
  THE WARRANT for the s_n-weighting (the one non-trivial choice): CC's divergent
  subtraction is t(n)-WEIGHTED - Lemma 5.4's eps'(1+) = Sigma t(n) IS the N_I scalar's
  own assembly (Prop 5.5, at content): the delta_1-type divergent mass carries per-mode
  coefficient t(n). AT CITE, not fitted. CONSEQUENCE (exact algebra, a VOID-GATE):
    W_plus + W_minus = A identically (the apportionment sums to the atlas's certified
    arch column by construction; checked to 1e-10 per cell, else VOID).
  RECORDED per cell: W_plus, W_minus, f_cell = W_plus / A, at the b36 stability triple
  (NQ, NMODE) in {(500,8), (700,10), (900,11)}, headline middle, spread reported.
  REGISTERED LONGHAND: (W-even) E1-dominant (f_cell -> 1 or stable > 0.9); (W-split) a
  stable fraction f_inf named as DATA (the joint closed as a number per cell); (W-third)
  mixed/unstable, named.

ITEM 2 - THE POLE-RESIDUE INTUITION, REGISTERED FIRST:
  The corpus already holds, at proof grade or banked: the pole functional acts only
  through the ball directions (Tate at cite; act 4's (beta) PROVED); ball is orthogonal
  to Son at the banked finite cells (vanilla, no axioms) and the ball indicator has NO
  E1-representative (banked correction); the atlas's ARRANGEMENT SEPARATES the pole
  column P from the arch column A (Z = P + A - PR, residual 1e-13 certified) - so the
  atlas's W_inf ( = A) is ALREADY pole-free by the arrangement's own bookkeeping.
  THE BENCH TEST registered: record P per cell beside the banked W_pole = -2.000127
  (the sign-arrangement reconciliation's number, ITS family) - consistency of sign and
  magnitude expected, exact equality NOT expected (different families; said now).
  (P-ball): the -2 lives off-object (the ball directions the object excludes); W_inf's
  apportionment ON the object is its Sonin-projected remainder's - the joint closes
  STRUCTURALLY, cross-linked to SIGN_ARRANGEMENT_RECONCILIATION and the atlas columns.
  (P-mixed): P shows sector content at the bench - named as data. (P-third) filed.

ITEM 3 - THE CLOSED-EQUATION TABLE (licensed if items 1-2 close the joint):
  Same cells, same family, same C0 gates as b36/b37 (carto far-ends; the three prolate
  pins; the b35 parity pins; the mask algebra - now also the per-mode mask:
  sum_n E2_n = E2 to 1e-10). resid47's role is ABSORBED into the apportionment (the
  registered definition allocates it); recorded per cell for the read-back.
  BOTH BRACKET READINGS re-recorded:
   (B-dictated) unchanged from b37 (uses the full trace; the apportionment does not
      enter): D_dict = (Thq - PR) + (E2odd - 2 E2). Re-recorded for the table's
      completeness.
   (B-sector), NOW CLOSED - no open term: Tr_reg_plus := W_plus + E2even; then
      D_closed = (A - PR) - (Tr_reg_plus - Thq) = W_minus - E2even + (Thq - PR)
      (the second equality is exact algebra via the sum gate; both forms printed).
  VERDICT MAP (carried, applied per reading and no wider): (I-match) |D| < 0.01 at
  every cell and stable -> finite-instance identity, range law on the face, step one
  triggered by (I-match) ALONE; (I-differ) stable nonzero -> located by the component
  table; (I-third) otherwise.

ITEM 4 NOTE: if no new apportionment arithmetic is decide-reachable (the t(n) are not
rational), the vanilla leg does NOT grow this act and the report SAYS SO rather than
forcing a module.

ROUTING (Rule 4): registration and instrument by Fable; the banked run to the OPUS lane
if available. Outcome: THE OPUS LANE WAS AVAILABLE - the banked run executed on Opus, first attempt;
all gates PASS (the one grep hit on "VOID" is the section header "C0: VOID GATES",
checked and said so); deterministic re-run of the debugged instrument (no fixes to any
registered quantity during debug).
====================================================================================================
