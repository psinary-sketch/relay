RULING (R107), the author's, ratified by this paste and strikeable

THE KEYSTONE REFRESH PROGRAMME IS OPENED AS ACTS, NOT DOCUMENTS, in four checkpoints: K0 the
terminal table generated; K1 claim-to-row per keystone; K2 refresh per keystone; K3 the
monograph's v6 on the object the (R104) sequence leaves. K0 runs now, while the b495 build
runs, because it reads and builds nothing. K1-K3 wait until the (R104) acts through h2-as-one-
Prop have closed; each is ordered by its own ferry. The table K0 emits is an INSTRUMENT OUTPUT
regenerated at every close by the suite, filed under relay data and cited by keystones as a
bank; it is not a tracking document and no prose lives in it.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm receipt-in-full)

ACT b496 - K0, THE TERMINAL TABLE GENERATED. The b495 build at pid 7860 is not polled, its log
not read; b497 owns it. No lake command runs in this act.

Component 1 - the population. For every kernel repository on disk in the federation (the roster
SPIRAL_MAP carries), at the pin REGISTRY's kernel table cites AND at HEAD: every theorem or
lemma name that appears in a #print axioms line of the repository's own AxiomCheck, PrintAxioms
or AXIOM_PRINTS output as committed, and every name a ledger grade cell names (b378_terminals.py's
matcher, both dialects). Union, deduplicated by (repo, name).

Component 2 - the columns, one row per (repo, name): repo; name; pin cited and HEAD; STATEMENT
as the source file prints it, from the declaration line up to its :=, taken from the file at the
pin, not from a README; PROFILE as the committed print carries it, or NOT PROFILED; GRADE as the
ledgers' cell reads it with the act and line, or UNGRADED; and CONFLICT where two ledger cells
grade one name differently, both cells quoted. Emitted to data/terminal_table.json and
data/terminal_table.md, and the generator added to the closing suite so every later close
regenerates it and diffs against the prior run.

Component 3 - the counts printed: rows; NOT PROFILED; UNGRADED; CONFLICT; ENCODES-graded; names
a ledger cites that resolve in no repository at any ref.

The navigator's expectations: (N1) more than 150 rows; (N2) at least five NOT PROFILED,
C7_finite_type_false among them; (N3) at least one CONFLICT. Each refutable by a printed cell.
No grade is conferred or moved by this act; nothing compiled; nothing at Zenodo written; nothing
deposits; h2 where the deposit left it.

paste ends (part 1 of 1)
