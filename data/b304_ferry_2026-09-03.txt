FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

THE DEMAND'S SHAPE -- must the first-level requirement be a
per-index value, and what does the source's own move look
like at the finite places: reads, derivations, and one
decision card, one act. Scope: nothing about the h2 identity
beyond the register sentence exact; NO aggregation stated,
M-2 owed; the barrier and the compression are carried, not
re-derived, and their scope -- one operator, the top-level
power with the quotient projection -- is quoted wherever their
reach is asked about; the model's escaped-mass artifact is a
standing hazard with two sightings and any result near
scaling states whether it is exposed; nothing deposits.

STEP ZERO: the ferry scan; push everything ahead of origin in
every repo, from push branches, read back by ls-remote;
install the pre-push hook in the two repos that lack it,
mirroring relay's, and exercise it in both polarities.

COMPONENT 1 -- THE FOURTH CONDITION. The archimedean unit's
membership in the ambient space -- square-integrability -- is
the definition's first conjunct and is undischarged at one
place. Pull the unit's definition from its owner and derive
membership from it (an eigenvector of an operator on that
space is in that space by definition, IF that is how the
owner defines it -- quote the definition), or name the
resisting step. Discharge the work-order or restate it
precisely.

COMPONENT 2 -- THE DEMAND'S PROVENANCE. Pull the three
specification properties and the derivation that produced
them. State from that text whether the per-index first-level
demand is downstream of requiring TERMWISE agreement with the
quotient channel's terms below the top -- i.e. whether
loosening that agreement to AGGREGATE would dissolve the
per-index demand. Derived from the text, not argued.

COMPONENT 3 -- SMEARING OVER THE INDEX. At a place whose tower
has one level, the index set is a single point coinciding
with the top -- read the index set's definition and confirm or
refute. If confirmed, state that smearing over the level
index cannot supply the first-level mass at exactly the
places that carry it, since the barrier annihilates a
one-term sum as readily as a value. Falsifier: an owner
statement giving such a place more indices.

COMPONENT 4 -- SMEARING OVER THE GROUP. Define the finite
analogue of the source's move from the source's own
description: integrate the scaling action over the place's
multiplicative group against a test function, then compress
by the object's projection. Decide BY DEFINITIONS, unfolding
both, whether that object is a functional of the barrier's
operator -- if it is, the barrier reaches it and the route
closes in one line; if it is not, say exactly why. If it is
not reached: compute its compression at the banked cells,
exact rational arithmetic, positive controls both polarities,
with the escaped-mass hazard stated per cell; report zero or
the exact value, and whether any value appears at a
one-level place. NO claim that a nonzero value is a route.

COMPONENT 5 -- THE DECISION CARD, assembled and routed with NO
recommendation: termwise agreement below (the per-index
demand follows and is annihilated by the barrier) versus
aggregate agreement below (the per-index demand dissolves,
and so does most of what identifies a candidate as THE
aggregation) -- each with what it entails for the
specification, for the barrier's reach, and for the source's
positivity-by-construction, which is named as forced rather
than earned and whether a forced positivity can carry
arithmetic content is stated as the question behind the
question, not answered.

CLOSING: the falsifiers registered in advance are the five
drafted in the navigator layer 2026-09-02, pulled into the
registration verbatim with provenance; no counts predicted;
the correspondence rows by the idempotent tool with the
notation guard, read back; every result keyed; the object's
conditions restated after Component 1, counted from the list
and not from a headline; M-2's row unchanged; the seam's
debt item 1 restated; the in-flight register; the mirror
rebuilt and verified if the papers repo moves. Registration
gate; index queried; demands-shape keyed. Components ordered.
THE SHADOW: only if finite-decidable and able to carry its
own scope -- the one-level index-set fact may be; say which,
checked not assumed, and the seal's caps written so they do
not forbid an ordered build.

Bank: data/b304_the_demands_shape.txt. Deviation rule
standing.

EXECUTION: ferry scan first; owners read at content first --
and where a text layer is truncated, the page image is the
content; registration satisfiability-checked, term-scanned,
sealed; components in order; full control suite; every
reported number from a committed tool with fixtures. STOP.

FOOT: after this act the object has three conditions or a
named fourth, the per-index demand is shown downstream of a
choice or shown necessary, index-smearing is dead or alive by
derivation, and the source's move has a finite analogue with
its compression computed; the card is on the author's desk;
h2 stands exactly where the deposit left it; locks last.

paste ends (part 1 of 1)
