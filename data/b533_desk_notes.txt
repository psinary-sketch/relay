========================================================================================================
b533 -- THE DESK. ### **THE EXPECTATIONS, SCORED ON THE BANKS.**
========================================================================================================

### THE NAVIGATOR`S SIX.
--------------------------------------------------------------------------------------------------------
  **(N1)** ### **HELD.** -- first clean attempt 2 (at most 2) ; L1-L2 std3 : [True, True, True, True].
  **(N2)** ### **HELD.** -- base_nonzero_at std3 True ; `paperFT_decay` in its text : False ; a zero in its statement : False.
  **(N3)** ### **HELD.** -- dominant_exists std3 True ; its statement: `theorem dominant_exists (Z : Zeta23.ZeroConfig) {g : ℝ → ℝ} {L : ℝ} (hg : ContDiff ℝ 4 g) (hs : Function.support g ⊆ Set.Icc (-L) L) (hL : 0 ≤ L) (ρ₁ : ℂ) (h₁ : ρ₁ ∈ Z.carrier) (hoff : ρ₁.re ≠ 1 / 2) (hpos : 0 < offScore g ρ₁) : ∃ ρs ∈ Z.carrier, ρs.re ≠ 1 / 2 ∧ 0 < offScore g ρs ∧ ∀ ρ ∈ Z.carrier, ρ.re ≠ 1 / 2 → offScore g ρ ≤ offScore g ρs`.
  **(N4)** ### **HELD.** -- right half-plane `riemannZeta_ne_zero_of_one_le_re` on the face and beside the Prop ; left ABSENT on both ; `rh_strip_imp_rh` a def.
  **(N5)** ### **HELD.** -- RestBound.lean blob : working fe1dcd47c3be, at 7224b87 fe1dcd47c3be, at HEAD fe1dcd47c3be.
  **(N6)** ### **HELD.** -- ERRATA.md blob : working 7f0c17ef2e51, at 810f944 7f0c17ef2e51, at HEAD 7f0c17ef2e51 ; b533 Zenodo files : NONE.

### THE SEAT`S THREE.
--------------------------------------------------------------------------------------------------------
  **(S1)** ### **HELD.** -- halted NONE ; first clean attempt 2 (at most 3) ; failures per declaration {'neg_one_pow_mul_iz_pow': 1, 'real_even_interpolant': 1}.
  **(S2)** ### **HELD.** -- real_even_interpolant std3 : True.
  **(S3)** ### **HELD.** -- off_finite_above std3 : True.

### ### **THE NAVIGATOR`S : HELD 6 ; REFUTED 0.** ### ### **THE SEAT`S : REGISTERED 3 ; HELD 3 ; REFUTED 0.**

### THE GRADES (READING (10)).
  `paperFT_conj_of_real_even` DERIVES -- the model`s identity for every z, under evenness alone (continuity and compact support dropped)
  `zero_term_sq` DERIVES -- the model`s square, under the model`s three hypotheses
  `weilTest_even_of_even` DERIVES -- the model`s evenness, for every real g (evenness of g dropped)
  `classK_of_real_even` DERIVES -- the model`s membership, under C^2 and compact support (evenness of g dropped)
  `selfConv_im_zero` DERIVES -- the model`s vanishing imaginary part, for every real g
  `paperFT_power` DERIVES -- the model`s transform (paperFT g)^(2^j), for g even, continuous (C^n, any n) and supported in [-L, L] -- L1`s hypotheses; the support form is defect (d)`s reading
  `power_contDiff` DERIVES -- C^n for every n : ℕ∞ whenever g is, under L3`s window context (support in [-L, L]) -- defect (d)`s reading
  `power_support` DERIVES -- the model`s support [-2^j L, 2^j L] from [-L, L]
  `paperFT_polyOp` DERIVES -- the model`s factorisation, for g C^(2 length a) supported in [-L, L] (READING (5))
  `base_nonzero_at` DERIVES -- the model`s statement for every z and every plateau parameter F in (0, 1); no hypothesis on any zero
  `off_finite_above` DERIVES -- the model`s finiteness for every c > 0, for any C^4 window supported in [-L, L] and any configuration (READING (6))
  `dominant_exists` DERIVES -- the model`s dominant off-line zero, from the off-line rho_1 with offScore > 0 its route starts from; any C^4 window, any configuration
  `real_even_interpolant` DERIVES -- the model`s real interpolant with coefficients bounded by the nodes alone, over any conjugation-closed node set (the vanishing points are nodes with target 0), as a Polynomial ℝ (READING (7))
  `rh_imp_rh_strip` DERIVES -- the model`s statement exactly

### THIS ACT`S OWN DEFECTS.
    (a) ### **THE FACE'S FIRST DRAFT CARRIED A NUMBER BESIDE "module"** ("L1-L6 and the seam in one module"), which the
        prediction counter read as an artifact count; `reg_satisfiable.py` refused (NOT SATISFIABLE, 1 contradictory clause)
        and the words were changed to "in the PowerWindow file" before the seal. The same trap as b532's (c).
    (b) ### **THE PROFILE WAS RUN TWICE**: the seat re-ran `b533_kernel.py profile` to read the head of its own output, so
        a second run file stands as `b533_profile_log2.txt`. Its `depends on axioms` lines are identical to the first's
        (diff empty, stamp and timing lines aside); `b533_profile.json` is the second run's.
    (c) ### **THE STATEMENT BANK OVERRUNS ON EQUATION-COMPILER DECLARATIONS**: `b533_kernel.py statement` reads each
        head to its first `:=`, and `def power`, `theorem power_even`, `theorem power_support` and `theorem power_contDiff`
        (pattern-matched, `| 0 => ...`) carry no `:=` before their bodies -- so the heads of `power` and `power_even` run on
        into later declarations, and `power_support`'s text appears in the bank three times. Carried from b532's tool, which
        had no such declaration. The grades were read from the module's source, not from those heads.
    (d) ### **TWO STATEMENTS CARRY A HYPOTHESIS THE FACE DID NOT DECLARE**: `power_contDiff` and `paperFT_power` take
        `Function.support g ⊆ Set.Icc (-L) L` (the convolution's smoothness and the zero-term square need compact support
        at every power); the ferry's L3 states power_contDiff as "ContDiff R n whenever g is", with the support in [-L, L]
        stated in the same paragraph for power_support. Read as L3's standing window context (L1's zero_term_sq, which
        paperFT_power's induction uses, carries compact support in the ferry's own words) and graded DERIVES on that
        reading, which is printed here and in the trail for the author.
    (e) ### **ATTEMPT 1 FAILED IN TWO LEMMAS, EACH ONCE**: `neg_one_pow_mul_iz_pow` (`neg_pow` rewrote the `(-1)^j` factor
        first) and `real_even_interpolant` (`lifts_iff_coeff_lifts` returns its witness through `mapRingHom`, so `rw [hQ]`
        missed `Polynomial.map`; and one `rfl` after a closing `rw`). Repaired; attempt 2 clean. No lemma reached its second
        failure; the halt rule did not fire.
========================================================================================================
