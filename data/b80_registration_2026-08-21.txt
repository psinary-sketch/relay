====================================================================================================
b80 -- THE RIGIDITY ACT -- THE RUN-REGISTRATION. BANKED BEFORE ANY WORK.
Closure-sequence STEP FOUR: the index face -- the (I) clause by the
discrete-invariant route -- run under the ARMED CIRCULARITY GATE (the
checkpoint ledger's gate for step four, filed at b79 as Gate 3; the ferry's
"gate-four line" read as the gate FOR step four -- the divergence resolved in
the ledger's favor and said so). Every sentence behind the firewall carries
its grade; the tag and grade-label audits run as named report sections.
====================================================================================================
FERRY 2026-08-21 (part 1 of 1, receipt confirmed). Relay-only. NO SIGN SENTENCE.
h2 UNCHANGED BY CONSTRUCTION. NOTHING DEPOSITS.

THE CIRCULARITY GATE -- THE INPUT ENUMERATION, WRITTEN BEFORE THE ACT (rule a;
re-checked at the report's close against the act as run):
  (i1) THE DISCRETENESS OF THE CLASS SET: F^4 = 1 -- derived, row-anchored
       (b32's recorded F^2 = parity and parity^2 = 1; the class set is the
       four fourth roots, a discrete set).
  (i2) CONTINUITY OF THE CLASS ALONG ADMISSIBLE DEFORMATIONS: to be READ from
       the recorded sources (the epsilon-machinery's declared dependencies;
       the closed-form pattern of the recorded masses).
  (i3) CONNECTEDNESS (or its failure) OF THE ADMISSIBLE SCHEME SPACE: read,
       derived, or NAMED OPEN.
  EXCLUDED FROM THE INPUTS: THE WINDOW LINE'S VALUE -- it appears nowhere in
  the derivation; the (V) clause is untouched. (The FINITE mirror's banked
  class value appears in the P3 instance as banked data about the FINITE
  tower -- that is not the window line's value and the re-check will say so.)

SOURCES (rule b/c, read at content): row 55's existence verdict WITH ITS EXACT
CONDITIONALITY (LIMIT-SENSE; [UNDER H-COH-inf] -- the tags travel: rigidity of
a conditional object is filed as CONDITIONAL RIGIDITY, said so); the recorded
scheme/evaluation structure (the declared-pin structure -- "the pins are
declared evaluations"; the epsilon-machinery's stated dependencies; b61's
closed-form pattern for the recorded masses in the functional's own units);
rows 35-36 (the banked level-constant unit trace i -- the finite mirror's
data); row 45 (the tower edges: iota); b32 (F^2 = parity). "ADMISSIBLE" IS
FIXED FROM THE RECORD OR LANDS NAVIGATOR-OPEN, NEVER INVENTED.

REGISTERED PREDICTIONS (the ferry's, verbatim):
  (P1) The abstract rigidity lemma compiles at the zero-axiom bar in its
  combinatorial form: a function from a connected step-graph to a discrete
  value set that is constant on edges is constant on the graph -- the
  discrete-invariant mechanism at kernel grade, with the fourth-root value
  set instantiated.
  (P2) The pattern prior: (i2) derives at least partially from the recorded
  ground (the sources' evaluations vary continuously in their declared
  parameters), while (i3) -- the connectedness of the admissible scheme space
  -- becomes THE NAMED RESIDUE: S1's invariance clause reducing to one
  topological statement about one space, filed explicit.
  (P3) The finite mirror's rigidity restates as the same mechanism's exact
  case (the class constant along the tower because the level maps are the
  edges and the value set is discrete -- row-anchored), so both sides of the
  mirror carry ONE rigidity mechanism at their own grades.

----------------------------------------------------------------------------------------------------
THE CHAINS, LONGHAND BEFORE ANY WORK (every claim pre-labeled):

(C1 = P1) THE ABSTRACT LEMMA [DERIVED at kernel grade if it compiles]: model a
  step-graph by an edge predicate adj : Nat -> Nat -> Bool and connectedness by
  WALKS (lists with consecutive vertices adjacent, own recursion isWalk); the
  lemma: if f is constant on edges (adj i j = true -> f i = f j) then f is
  constant along every walk (f (l.getLastD a) = f a) -- induction on the walk,
  no algebraic hypotheses on the value type; discreteness enters as
  DecidableEq at the instantiation (the fourth-root carrier Int x Int; the
  four roots pairwise distinct, decided). Whatever resists is named exactly.
(C2 = P2) THE ADMISSIBILITY READ [each step graded at first appearance]:
  WITHIN-SCHEME deformations: the record's own machinery presents the masses
  by a CLOSED-FORM pattern in the declared parameters (b61, at its grade) --
  a closed-form family is continuous in its parameters wherever defined, so
  (i2) DERIVES PARTIALLY: the class map is continuous along the
  epsilon-parameter direction within a scheme [derived-from-record at cite
  grade]. CROSS-SCHEME deformations: NO recorded structure connects distinct
  regularization schemes -- the record fixes the space's within-scheme fibers
  only; the cross-scheme topology is NAVIGATOR-OPEN. (i3) THEREFORE lands as
  THE NAMED RESIDUE (the registered expectation), stated exactly:
    THE CONNECTEDNESS RESIDUE (W-TOP-ADMISSIBLE-CONNECTED): the admissible
    scheme space -- the space of source-licensed regularization schemes,
    within-scheme fibers as the record fixes them, cross-scheme structure
    pending the sources -- IS CONNECTED BY CLASS-CONTINUOUS DEFORMATIONS.
  S1's invariance clause = the lemma + (i1) + (i2) + THIS ONE STATEMENT.
  THE (I) VERDICT, three-graded frame fixed in advance: DERIVED (the
  mechanism; (i1); (i2)'s within-scheme half; the finite mirror); CONDITIONAL
  (the window line's rigidity inherits row 55's clauses -- conditional
  rigidity of a conditional object, tags traveling); OPEN (the connectedness
  residue -- one sentence).
(C3 = P3) THE MIRROR RESTATEMENT [DERIVED]: the finite tower is the lemma's
  exact case -- vertices the levels, edges the iota steps, value set the
  fourth roots, f the banked per-level class (the unit trace i at every
  banked place-2 level, rows 35-36); the walk [0,1,2,3] decided; the
  edge-constancy decided on the bounded range from the banked table; the
  constancy re-anchored THROUGH the lemma. Both sides of the mirror carry ONE
  mechanism: the finite side at decided grade, the window side at the
  three-graded (I) verdict.

LEAN PLAN: RigidityShadow.lean -- isWalk (own recursion); walk_constant (the
general lemma, induction, no axioms expected); fourth_root_instantiation (the
specialization to the Int x Int carrier); fourth_root_discreteness (the four
roots pairwise distinct, decided); tower_walk_instances (the [0,1,2,3] walk
and the banked table's bounded edge-constancy, decided). Row 56, no blank
cells. THE GATE RE-CHECK at the report's close: the enumeration above against
the act as run; the window line's value verified absent from every input.

Bank: data/b80_rigidity.txt. Deviation rule standing. Divergence note (rule
a): the gate-numbering reading disclosed at the head.
====================================================================================================
