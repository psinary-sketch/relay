========================================================================================================
b516 -- THE DESK. ### **THE EXPECTATIONS, SCORED ON THE BANKS.**
========================================================================================================

### THE NAVIGATOR`S FOUR.
--------------------------------------------------------------------------------------------------------
  **(N1)** ### **HELD.** -- 12 of 12 verdict strings matched in their own closing banks.
  **(N2)** ### **HELD.** -- the columns: {'OBJECT': 0, 'MODEL': 8, 'RECORD': 0}.
  **(N3)** ### **HELD.** -- the fold section`s work-order table names W-ORD-TABLE-PROFILE-JSON, W-ORD-TABLE-SHORTNAME-DEDUP, W-ORD-WEIL-CONVERSE, each with where filed, its trigger and its status.
  **(N4)** ### **REFUTED.** -- classK`s generating h is typed `ℝ → ℂ` (True) and `h = i phi`, not real, is ADMITTED (k even-defect 0.0e+00).
    ### ITS "SO" CLAUSE, PRINTED SEPARATELY: the classical `e^{i gamma u} phi` is NOT admitted (k even-defect 1.96) -- by
    evenness, not by type -- and its real part `cos(gamma u) phi` IS; the construction needs a real-valued variant or an even
    combination, which is the clause`s second disjunct, and no widened class.

### THE SEAT`S THREE.
--------------------------------------------------------------------------------------------------------
  **(S1)** ### **HELD.** -- 16 short rows dropped.
  **(S2)** ### **HELD.** -- rows profiled from banked JSON: ['SIDEExplicitFormula.B321.b321_identity', 'SIDEExplicitFormula.B321.h2_sign_imp_cell', 'SIDEExplicitFormula.B321.rh_imp_cell_form', 'SIDEExplicitFormula.B321.rh_imp_h2_sign', 'Zeta23.WeilEF.EF_lit_zetaZeroConfig'].
  **(S3)** ### **HELD.** -- `h = i phi` admitted.

### ### **THE NAVIGATOR`S : HELD 3 ; REFUTED 1.** ### ### **THE SEAT`S : REGISTERED 3 ; HELD 3 ; REFUTED 0.**

### THIS ACT`S OWN DEFECTS.
    (a) ### **READING (5)`S JSON RULE WAS TOO WIDE AS REGISTERED** -- "any string in them in the captured-stdout dialect" --
        and the first two runs promoted three SIDE-kernel rows to PROFILED from b456`s bank, a search whose verdict is ABSENT,
        quoting deposit notes and reports. ### Repaired twice inside the fold (a quotation marked not printed against its ref;
        a record carrying a file and a line), each with a fixture both ways; the runs banked as `b516_table_first.json`,
        `b516_table_first_run.txt`, `b516_table_first_diff.json`, `b516_table_second_run.txt`; the table restored to b515`s
        close before each re-run so the diff reads against it. The final count: 5 rows profiled from banked JSON, not 8.
    (b) ### **A QUOTED HEREDOC COLLAPSED A BACKSLASH AGAIN**, in a one-line tidy of the patch script; the assertion refused
        it and nothing was written -- the patch ran from its file as written.
    (c) ### **`terminal_table.py` CARRIES THREE PRE-EXISTING `invalid escape sequence` WARNINGS** (a backslash-space in three
        strings, present before this act`s edit); observed, not repaired -- outside the two work-orders -- and routed.
========================================================================================================
