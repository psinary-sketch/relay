====================================================================================================
b79 -- THE WINDOW EXISTENCE ACT -- THE BANK. Registration:
data/b79_registration_2026-08-21.txt (chains longhand, every claim pre-labeled).
Raw run: data/b79_run_raw.txt. Instrument: tools/e16/b79_window_existence.py.
Kernel: WindowShadow (3 terminals, zero-axiom; Core 233/233; row 55). The
staged registration run EXACTLY AS FILED; divergence: none. THE DECLARED
EVALUATION PIN for every BENCH value below: this act's instrument run -- the
plus-kernel DFT at odd centered sizes N = 33, 65, double precision, self-dual
scaling x = m/sqrt(N).
====================================================================================================

(P-A) VERDICT: LANDS AT EVERY BANKED TRUNCATION; NO REFUTATION. [DERIVED] The
  excess/seed row's shell address is v = n-1 at n = 1..4 (the gcd law); the
  model's deficient weight sits at the same address (b61, decided there); the
  ball-boundary transition is the same shell (b76, decided). The seed-defect
  correspondence SURVIVES its refutable test at all four anchors.
(P-B) VERDICT: LANDS AS THE CONDITIONAL IT IS. [DERIVED arithmetic + the tag]
  The plus-gauge reading of the line's class is +i (BENCH at the pin: the
  concentrated odd vector's eigenvalue residual 1.35e-11 at N = 33, 5.02e-16
  at N = 65; the continuum first-odd-Hermite statement STANDARD-AT-TEXT); the
  conjugate-kernel reading of the SAME line is -i (pconj arithmetic, decided);
  [UNDER H-COH-inf] the coherent family's real transform is the
  conjugate-kernel one (rows 51-53), so THE LINE'S COHERENT READING IS THE
  CONJUGATE FAITHFUL VALUE -- the relative conjugacy, exactly the staged
  prediction, gauge-honest. Every sentence using the coherent reading carries
  the tag; no unconditional class sentence exists in this act.
(P-C) VERDICT: LANDS. [DERIVED] The line is odd-class with the excess on the
  +i side at the plus gauge: 4*d_i = (q-1)^2 + 3, d_i - d_{-i} = 1, the even
  sides balanced (d_1 = d_{-1}, 4*d_1 = q(q-2)) at all four banked place-2
  cells -- the collapse form's requirement (b61 G-b: i*(m_i - m_{-i}) the
  parity-split twin) carried by the finite anchors exactly.

(C1) THE TRUNCATED LINE FAMILY [DERIVED]: built at every banked truncation as
  the i-sector excess; level-coherent (iota fixes the seed chain, row 45); the
  ALGEBRAIC colimit class exists (row 45's constructed boundary line) even
  though {V_n} does not stabilize (row 50).
(C2) THE RECORDED-STANDARD READINGS [BENCH at the pin above]: eigenvalue +i at
  the plus gauge (residuals above); radial concentration at the center:
  mass(|x| <= 1) = 0.9920 (N = 33), 0.9973 (N = 65); mass(|x| <= 2) = 1.000000
  (both). The line concentrates at the center as the object clause requires.
(C4) LIMIT-SENSE: RESOLVED AS FAR AS THE RECORD FIXES IT. [DERIVED] the
  algebraic limit line (the seed chain's colimit class, row 45). [NAMED, open]
  the analytic limit: the {V_n} bearing stated exactly -- the non-stabilization
  means the analytic limit cannot be inherited along the raw scalings; any
  analytic sense is the SOT limit of the level compressions (the owner file's
  stated-not-proved sense, b61 N2). NO THIRD STATE INVENTED. G1 stands
  PARTIALLY: the analytic half of LIMIT-SENSE resisted exactly as the staged
  branch (b) anticipated for that half; the algebraic half landed branch (a).
  G2: no concentration failure at the standard (the bench readings clean).
  G3: no address refutation (P-A).

THE EXISTENCE VERDICT (component 2), three-graded per the hard-clause
inventory, filed to the closure sequence:
  DERIVED: the truncated line family at every banked truncation; its addresses
    (= the deficient/seed address); its level-coherence; the ALGEBRAIC colimit
    class; the parity split with the excess on the +i side.
  CONDITIONAL: the ANALYTIC limit object -- under the named clause LIMIT-SENSE
    (the SOT sense, its identification open at its owner); the COHERENT class
    reading -- the conjugate faithful value [UNDER H-COH-inf].
  OPEN (one sentence each): LIMIT-SENSE's analytic resolution is the window's
    remaining existence content. (I) rigidity is UNTOUCHED -- step four's
    clause, not consumed here. (V) value is UNTOUCHED -- step five's clause,
    not consumed here (the gauge pair is arithmetic about labels, not a value
    selection).

THE CHECKPOINT LEDGER (component 3): filed in OPEN_TRAILS' closure-sequence
entry -- six gates, each trigger/test/failure action (the firewall labels; the
tag audit; the circularity gate for steps four/five; the mandatory I-14 pass
between five and six with the surprise protocol on any (alpha) landing; the
translation gate's stop-on-resemblance; the protocol gate -- the sequence
arrives at the door, only the author's ordered process opens it).

HIT RECORD: P-A, P-B, P-C all on their registered branches; LIMIT-SENSE split
exactly along the record's own line (algebraic derived / analytic named); no
G-step produced an unnamed resistance.
====================================================================================================
