====================================================================================================
b431 -- THE LongGapsBetweenPrimes GRADING, AND THE TYPE-D QUESTION. ### THE CLOSING RECORD.
====================================================================================================

**THE TERMINAL GRADES `DERIVES` AGAINST THE PAPER`S OWN THEOREM 1.1, AND THE TYPE-D QUESTION
READS `TWO THEOREMS SHARING A NAME` -- PARTING AT WHAT IS QUANTIFIED.** The registration was
locked before any clone, fetch or build, at `fce23cad...2cc0`: 8 gates read, 8 passing, 4 by
digest.

----------------------------------------------------------------------------------------------------
### THE EXPECTATION (L2), EACH CLAUSE SCORED APART.
----------------------------------------------------------------------------------------------------
  (L2)(a) the terminal grades DERIVES               : True    HELD
  (L2)(b) its profile is clean                      : True    HELD
  (L2)(c) the Type-D verdict reads TWO THEOREMS     : True    HELD
  (L2)(c) ITS REASON, scored apart -- a fixed period against all moduli : True    HELD
  ### **ALL THREE CLAUSES HELD, AND SO DID THE REASON THE NAVIGATOR GAVE FOR (c).**
  ### And the measurement is SHARPER than the prediction: the keystone`s lemma does not range
  ### over all moduli and then collapse them -- **ITS WITNESS NEVER LEAVES A SINGLE MODULUS**.

----------------------------------------------------------------------------------------------------
### WHAT THE ACT FOUND THAT NO ONE ASKED FOR.
----------------------------------------------------------------------------------------------------
  ### **b429`S RESIDUE IS NARROWED, NOT INHERITED.** b429 graded DERIVES with the repository`s
  own checker unrun and its definitions agreed ON A READING. This repository ships
  `Challenge.lean` -- an independently written reference statement with an intentional `sorry`
  placeholder -- and **THE PROVED STATEMENT IS IDENTICAL TO IT CHARACTER FOR CHARACTER.**
  Comparator itself still did not run, and **THE REASON IS A FACT ABOUT THIS MACHINE, NAMED**:
  it requires `landrun`, a Linux landlock sandbox. This machine is Windows. Unavailable on this
  platform, not declined.

  ### **THE MACHINE WAS NOT THE OBSTACLE IT LOOKED LIKE.** Three build attempts were killed for
  low memory, the last with a SINGLE worker at ~1.5 GB -- so the pressure was never parallelism,
  and `lake` has no `-j` at 5.0.0 either (checked in full, as b429 checked v4.29). **WHAT
  UNBLOCKED IT WAS THE FOREGROUND**: the kills came from the harness protecting the system
  against BACKGROUND tasks, not from Lean`s allocator. 8707 jobs, rc=0. **b429 BANKED THE OOM
  AS A PROPERTY OF THE MACHINE; ON THIS EVIDENCE IT IS AT LEAST PARTLY A PROPERTY OF HOW THE
  BUILD WAS LAUNCHED.**

  ### **THE WRITE-LIST ARMS WERE BLIND TO NEW FILES.** `git status` marks a new file `??`, and
  both b430`s arms and this act`s first writing skipped those lines -- so the arms judged only
  MODIFIED TRACKED files, while every act tool and every bank file is NEW. **b430`S "2 UNLISTED
  WRITES" WAS MEASURED BY A BLIND INSTRUMENT AND IS A LOWER BOUND, NOT A COUNT.**

  ### **THE INHERITED DIRT WAS NEVER DRIFT.** Two files trailed through b430 and b431 as
  uncommitted. `git status` called them modified; `git diff` called them identical to HEAD. It
  was line-ending noise, and **IT HAD A CONSEQUENCE**: the pre-push guard could not be exercised
  in `relay` at all while they sat there -- REPOS FAILING read 1, and the one was a SKIP, not a
  failure. Restoring them discarded nothing and the guard now runs in all four.

----------------------------------------------------------------------------------------------------
### THE TWO ARMS THAT FAILED BEFORE THE PUSH, AND WHY THEY STAND.
----------------------------------------------------------------------------------------------------
  `G-WRITELIST-KINDS` and `G-NOEXTRAKIND` FAILED. **THE ACT WROTE 7 ACT TOOLS AGAINST A FACE
  DECLARING 6**, and `tools/b431_fetch.py` is named nowhere on it. It exists because the
  paper`s digest was first produced by an ad-hoc shell script with **NO COMMITTED TOOL BEHIND
  IT** -- a figure nobody could reproduce or fail to reproduce. Fixing that cost a tool.
  **THE FACE IS LOCKED AND IS NOT EDITED.** ROUTED, and now in both directions: a write list
  omits what the ritual writes, AND caps what the act will need before the act has discovered
  what it needs. An act finds its defects while running.

----------------------------------------------------------------------------------------------------
### THE CLOSE.
----------------------------------------------------------------------------------------------------
  suite, pre-push  : 79 arms, 77 passing, 2 failing (the write-list breach, above)
  suite, post-push : 79 arms, 79 passing, 2 of them vacuous and declared so
  ### **AND THE TWO READINGS LANDED IN TWO FILES** -- BAR 11, minted because b430`s post-push
  ### run overwrote its own pre-push record and only the commit saved it.
  censuses         : TOTAL MISSING 0, both
  pins             : 0 hard-failing; all four 0 behind, 0 ahead
  hook             : REPOS FAILING 0, and relay EXERCISED rather than skipped
  mirror           : rebuilt after the commit; CLEAN ON ALL THREE CLAUSES at e5ad674
  correspondence   : row 280; row 279 still reads at its own marker
  desk             : 22 items swept, 2 closed, 20 standing
  the four lists   : ALL FOUR OPEN
  the witness arc  : CHECKPOINTED after site (iii); no site attempted
  h2               : WHERE THE DEPOSIT LEFT IT

  ### **NO BRIDGE TYPED. ### NO KEYSTONE EDITED. ### NO GRADE MOVED.** The finding about the
  ### conspiracy keystone`s lemma NAME and its prose is ROUTED to the author, not acted on.
====================================================================================================
