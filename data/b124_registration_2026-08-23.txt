====================================================================================================
b124 -- THE PROLATE REPAIR ACT -- THE RUN-REGISTRATION. BANKED BEFORE ANY
COMPUTATION, with ROUTES, GATES AND BOTH SEATS' EXPECTATIONS. Instrument repair
plus re-verification; THE REPRODUCE-BEFORE-EXTEND GATE governs the repaired tool
absolutely; THE FLOOR-AXIS LAW governs every floor quote; THE GRADE-INDEX
CONVENTION governs every re-grade; the guard rides every location; b38's
verdicts FENCED; nothing promoted past its grade; nothing about h2; nothing
deposits.
====================================================================================================
FERRY 2026-08-23 (part 1 of 1, receipt confirmed in full, Rule 1). Relay-only.

(a) LOAD-BEARING, READ AT CONTENT THIS ACT:
 - THE PROLATE LAYER AT ITS OWNER: prolate_layer.prolate(NQ) forms the sinc
   kernel K(x,y) = sin(C(x-y))/(pi(x-y)) on NQ Gauss-Legendre nodes, symmetrizes
   A = sqrt(w) K sqrt(w), calls numpy.linalg.eigh, and sets xi = V/sqrt(w).
 - b121's finding verbatim: the dilated-evaluation INTERPOLANT is the cause,
   established by the truncation test (the excursion persists at six
   well-separated modes, so not the degenerate cluster); the NQ excursion is
   4.361e-01; the perturbation route is vacuous at separations of 4.271e-17.
 - b119's split machinery and ratio table AT ITS WITHDRAWN-TO-BASIS STATUS.
 - b117's enclosures; the licensed range.

*** (a-bis) A PROVENANCE FACT THAT DECIDES THE PRIMARY ROUTE, FOUND AT THE OWNER
READ AND RECORDED BEFORE ANY BUILD: THE LAYER ALREADY USES THE NYSTROM EXTENSION
AT ONE POINT, AND SAYS SO IN ITS OWN COMMENT. prolate_layer computes xi_n(1) as
  k1 = kernel(ones, x) ; xi1 = (k1 * w) @ xi / lam
under the comment "xi_n(1) from the eigenfunction equation, not from the grid".
### SO THE PRIMARY ROUTE IS NOT A NEW DEVICE: IT GENERALIZES A STEP THE LAYER
ALREADY TAKES AND ALREADY TRUSTS, from the single abscissa x = 1 to every
dilated abscissa. The repair's warrant is the layer's own idiom. ***

(b) THE REPAIR ROUTES, FIXED IN ADVANCE.
 ### PRIMARY -- THE NYSTROM EXTENSION. For any abscissa z, inside [-1,1] or
 outside it, the eigenfunction is evaluated through the integral equation it
 satisfies:
   xi_n(z) = (1/lam_n) * SUM_j w_j K(z, x_j) xi_n(x_j)
 the quadrature-weighted kernel row applied to the node values, divided by the
 eigenvalue. ### NO INTERPOLANT ANYWHERE.
 *** ITS EXACTNESS PROPERTY, stated before it is tested because it is the reason
 to prefer it: at a node z = x_i the formula returns (1/lam) * lam * xi_n(x_i) =
 xi_n(x_i) EXACTLY, by the eigen-equation. So the extension is CONSISTENT WITH
 THE NODE VALUES BY CONSTRUCTION -- which the analytic continuation an() was not
 (b121 measured its node discrepancy at 3.626e+02). THAT IS THE STRUCTURAL
 DIFFERENCE BETWEEN THE TWO, and it is why the route b121 closed is not this
 route. ***
 FALLBACK -- spectral reconstruction from Legendre coefficients, as named at
 b121. THE ROUTE TAKEN AND THE ROUTE NOT TAKEN ARE BOTH RECORDED IN THE REPORT.
 *** AND THE HAZARD I NAME NOW, BEFORE BUILDING: the extension divides by lam_n,
 and the modes in use include four with lam ~ 1e-16. The DIVISION IS THE SAME
 ONE THE LAYER ALREADY PERFORMS for xi_n(1), so it is not new -- but it is the
 place the repair can fail, and if it does the residual will show at the small
 modes and the report will name them. ***

(c) THE GATES, IN ORDER AND BINDING.
 G1 -- the banked cells reproduced at recorded tolerance by the repaired tool.
 G2 -- ### THE b121 NQ-VARIATION TEST RE-RUN ON REPAIRED SAMPLES, with the
 excursion's collapse quantified against the old 4.361e-01, AND THE NEW FLOOR
 MEASURED ACROSS ALL FOUR INPUT AXES -- NQ, the epsilon grid, the u grid, and
 the truncation NMODE -- each quoted with its axis named, per the floor-axis
 law. ### A G2 FAILURE HALTS THE ACT with the residual excursion named.

*** (d) THE TWO SEATS, BEFORE ANY COMPUTATION.
THE NAVIGATOR'S, quoted from the paste: one-sign survival at HIGH confidence
(transfer: the b121 basis-robust margin); coarse-rise survival at HIGH
confidence (transfer: Psi(0)'s NQ-invariance and the enclosure's five-order
clearance); DOMINANCE AT LOW CONFIDENCE EITHER WAY, with the four misses priced.
THE EXECUTOR'S:
 ON P1 -- ### I EXPECT THE REPAIR TO LAND AND G2 TO SHOW A LARGE COLLAPSE.
   GROUND: the extension is exact at nodes by construction, and the interpolant
   it replaces was linear on a grid that is sparse exactly where the dilated
   points fall. I DO NOT PREDICT A SPECIFIC ORDER, because the paste asks for
   "at least two orders" and I have no basis for a number -- I predict
   qualitative collapse and will report whatever it is.
 ON P2 -- I expect the gross rise to persist and the FINE STRUCTURE TO CHANGE
   MATERIALLY, since the fine structure is precisely what the bad interpolant
   was corrupting.
 ON P3 -- I expect one-sign survival, agreeing with the navigator.
 ON P4 -- ### I DECLINE TO PREDICT, AND I SAY WHY RATHER THAN HEDGING. My
   dominance predictions have now missed twice (b119 branch (b), wrong; b121's
   Psi-stability, wrong). A third guess would be noise dressed as a seat. THE
   NAVIGATOR'S LOW-CONFIDENCE-EITHER-WAY IS THE HONEST POSTURE AND I ADOPT IT
   RATHER THAN INVENT A DISSENT. *** RECORDING A DECLINE IS ITSELF A REGISTERED
   POSITION and is scoreable: if dominance resolves cleanly either way, the
   record shows the executor had no view when a view was cheap to state. ***
 MY TRANSFERS, named: I transfer b121's measured basis-robustness of the SIGN
   and nothing about dominance; I transfer the interpolant diagnosis as the
   repair's target; I transfer NOTHING from b119's ratio table, which is
   withdrawn to its basis.
 THESE EARN NOTHING. ***

(e) INDEPENDENCE CHECK, RUN: components ordered, 2-4 consume 1.

REGISTERED PREDICTIONS (P1)-(P4), VERBATIM FROM THE FERRY, banked as given.
 (P1) "THE REPAIR LANDS: the extension built; G1 passes; G2 shows the NQ
   excursion collapsing by at least two orders toward the multi-axis floor, the
   new floor quoted with axes; branch (b): the residual excursion named with its
   tested cause."
 (P2) "PSI RE-PROFILED on repaired samples: the gross rise persists within its
   enclosure's neighborhood; the fine structure's change quantified; the
   refinement-stable scale re-measured and restated."
 (P3) "THE ONE-SIGN RE-RUN: I(L) strictly negative across cells, sweep, and
   bases on repaired samples -- the verdict restated at its grade with the new
   floor; branch (b) first-class."
 (P4) "THE DOMINANCE QUESTION, RE-ASKED ONCE: the b119 split re-run on repaired
   samples with the basis sweep built in (the recorded basis plus the b121
   alternates plus a registered fresh draw) -- branch (a): dominance holds
   basis-independently at every cell: the verdict RESTORED at enclosure grade on
   repaired samples, the mechanism's status line updated, the withdrawal fence
   replaced by the restoration record; branch (b): dominance fails somewhere
   basis-independent or remains basis-dependent: the candidate mechanism's
   status resolved accordingly per the third-outcome cell (the sign banking
   unchanged either way), the failing structure named, the mechanism question
   re-coordinated; NO third asking -- whichever branch fires is the record's
   answer."
 ### THE FRESH DRAW, REGISTERED HERE SO IT CANNOT BE CHOSEN AFTER SEEING A
 RESULT: NQ = 1100. Fixed now, before any computation.

Bank: data/b124_prolate_repair.txt. Report:
reports/2026-08-23-prolate-repair.md. Deviation rule standing.
Divergence note (rule a): none at registration.
====================================================================================================
