CONCURRENCY: SOLO (research seat; navigator Fable 5.1 declared
on the face). Standing text as b460, carrying A2 and
(R26)-(R71); the addendum slot per (R50); scanned before
sending, zero hits. Both instrument lanes parked; the kernel
lane closed; the wave parked under (R66); nothing deposits.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b461 - THE SUITE GIVEN CONTROLS, AND THE ARMS THAT CANNOT
HAVE THEM RETIRED.

COMPONENT 1 - THE INVENTORY. For every arm the suite declares
at b460 - sixty-four - print in one table: its name; what it
reads; its positive control (an input the arm must FAIL on,
named concretely, drawn where possible from the record's own
incidents - b458's write-list turning PASS after the commit,
b459's cut swallowing a second enumeration, b455's docstring
read for a definition); its negative control (an input it
must PASS on); and whether each control exists today, is a
one-line addition, or cannot be stated. Under (R70): rehearse
the inventory on the suite file before the seal and print the
count of arms found against the sixty-four declared; a
difference is printed, not reconciled.

COMPONENT 2 - THE RULE APPLIED. b396's rule, quoted at its
address: a filter that keeps nine tenths of what it is given
is not a filter. Applied to arms: an arm that has never
failed on any input in the record and cannot be given a
positive control is RETIRED, its name and reason kept in the
suite as a comment; an arm whose controls are one-line
additions receives them; an arm with both already is kept as
is. Run the suite once with every arm's controls exercised
and print, per arm, PASS on its negative and FAIL on its
positive; any arm that passes its positive control is
defective and is repaired or retired in the act, both yields
printed. Print the before and after counts: arms declared,
retired, given controls, kept.

FILING, NOT A COMPONENT. The two observations from b460,
entered in the outlier's trail entry as observations and not
as candidates: the class boundary coincides with the line
where the prime channel switches on, a = sqrt 2, by two
routes; and the outlier aim is sqrt 17 to seven digits with
the support edge on the prime 17. A measurement is priced,
not run, no lane opened: the chain at a = sqrt p for the
first several primes against cells between them, asking
whether the b447 growth recurs at crossings.

CLOSING as the standing pattern, with Component 2's before
and after counts and the retired arms' names printed in the
closing message itself; the span counted by tool; the four
lists OPEN. The navigator's expectations: (N1) fewer than
half of the sixty-four carry both controls today; (N2) at
least ten arms are retired; (N3) the suite after the act
declares fewer than forty arms. Each refutable by a printed
result. No grade moves; nothing about the object; h2 where
the deposit left it.

paste ends (part 1 of 1)
