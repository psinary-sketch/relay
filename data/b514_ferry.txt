RULING (R123) BEGIN
RULING (R123), the author's, ratified by this paste and strikeable.

(1) THE CHAIN RH → h2_sign → cell form IS ENTERED AT ITS MEANING:
h2_sign is a consequence of RH, compiled, so it is at most as strong
as RH; whether it is as strong — the converse — is the wall, and the
two objects the converse needs are named at b513: a Paley-Wiener-
type growth bound (d) and the dominance lemma (f), the off-line
term negative and larger than the rest for a window matched to the
zero. Neither is in the kernel or in Mathlib by name.
(2) The terminal table's two quirks are work-orders: profiles taken
from banked JSON as well as captured output; short-name rows
deduplicated against qualified ones. Trigger: the next fold.
(3) The numerical lane opens for b514 and shuts at its close; the
kernel lane stays shut.

RULING (R123) END

FERRY BEGIN — paste for Claude Code (part 1 of 1; confirm receipt-in-full)

ACT b514 — THE MATCHED WINDOW, the seat's 19:48 draft with
expectations attached.

Component 0 — the window. g(u) = cos(gamma_0 u) times a cubic
B-spline bump, gamma_0 = 16.290216 for Q0 and 14.1347 for xi, put in
classK form as g ⋆ g~ with the transform in closed form; the
fixture: closed form against a numerical integral to 1e-10, and
membership in classK stated from H2Sign.lean's definition with the
generating h named. The transform's value at gamma_0 and at Q0's two
nearest on-line zeros printed at three widths, so the "matched"
claim is a printed ratio and not a name.

Component 1 — Q0 across the ladder's widths, on the completed bank
with the exact transform and (R114)'s grid rule: the margin, its
sign, its consistent residual and bound, VERIFIED-EST with the
u > 1200 shortfall printed; the zero sum split into the pair's
term, the other off-line pairs, and the on-line part; the pair's
share of the on-line part per cell; the cells where the margin is
negative, or NONE.

Component 2 — xi with its own matched window at the same widths,
the same columns.

Component 3 — the reading in (R122)(2)'s words and no others: if a
Q0 cell is negative on a closing bank, the window is the witness
construction h2_sign_imp_rh names, exhibited at one rho; if none
is, the converse's construction is harder than a modulated bump.
Nothing claimed of the kernel either way.

The navigator's expectations: (N1) Q0's margin is negative at one
width at least; (N2) xi's margin is positive at every verified cell;
(N3) the pair's share of the on-line part exceeds b511's maximum of
0.50 at some cell. Each refutable by a printed cell. The lane shuts
at this act's close; nothing compiled; nothing deposits; h2 where
the deposit left it, and no cell of this act is a statement about
RH.

FERRY END (part 1 of 1)
