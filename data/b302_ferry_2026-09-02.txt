FERRY -> CLAUDE CODE -- b302, 2026-09-02. Received in full, part 1 of 1.
CONCURRENCY: SOLO (research seat).

THE UNIT REQUIREMENT, A STALE OPEN, AND ONE TERMINAL -- one act. Scope: a RULING executed against
quoted text, one READ, and one kernel build; NO aggregation stated, M-2 owed; nothing about the h2
identity claimed beyond the register sentence exact; nothing deposits.

STEP ZERO: the ferry scan; then push this act's pending commits and the prior act's, from push
branches per the standing rule, read back by ls-remote, before anything new is written.

THE RULING, the author's, ratified by this paste and strikeable: "RULE ARCH-UNIT: A -- space
membership suffices. The product construction requires a unit vector in the local Hilbert space;
the sector wording in the original choice is descriptive and is retained as description. Execute
only if the construction's own text, quoted, supports it; if the text requires more, HALT and
report, since a ruling against a quoted requirement is not a ruling this author makes."

COMPONENT 1 -- THE REQUIREMENT: quote the product construction's own requirement on the chosen
vectors at each place. Then execute the ruling or halt per its condition. If executed: the open
item's row updated with what the ruling narrowed said plainly -- A narrows the original wording
rather than fulfilling it -- and the sector description retained with a note that its
establishment is not required by the construction and is not claimed.

COMPONENT 2 -- THE POSSIBLY-STALE OPEN: the finite unit's nonvanishing at the generic odd place is
listed open, and an earlier act derived the canonical generator nonzero at every odd prime at
level one with its support formula derived generically. Decide by definitions whether these are
the same statement: (SAME, so the open is stale and closes, with the earlier act cited and not
re-derived) / (DIFFERENT, the difference quoted and the open restated precisely) / (UNDECIDED, the
missing statement named). Do not close it by resemblance of wording.

COMPONENT 3 -- THE TERMINAL: build the shadow candidate the prior act identified and could not
build under its own seal -- the rational enclosure, which is finite-decidable and carries its own
scope in its own statement. Vanilla discipline, zero axioms at the audit bar, polarity controls
first, a not-dead witness with an UNAVAILABLE arm where no control can exist; imported by the
certification file in the same commit that creates it; the whole profile regenerated and read,
every pre-existing print compared byte-identical against the banked file at HEAD, counts before
and after stated, and any byte-order-mark hazard checked rather than assumed. If on inspection the
statement cannot carry its own scope, do not build it and say why.

CLOSING: the object's construction status restated with its remaining conditions after this act,
each typed; the correspondence row by the idempotent tool, read back, no blank cells; the import
ledger's rows; M-2's row unchanged; the seam's debt item 1 restated; the in-flight register; the
ledgers reported as what was written and checked; the mirror rebuilt and verified if the papers
repo moves; pins by ls-remote across all three repos. Registered in advance: both seats'
expectations, no counts predicted. Registration gate; index queried; unit-requirement keyed.
Components ordered.

Bank: data/b302_the_unit_requirement.txt. Deviation rule standing.

EXECUTION: ferry scan first; owners read at content first; registration satisfiability-checked,
term-scanned, sealed -- and its caps written so they do not forbid this act's own ordered build;
components in order; full control suite; every reported number from a committed tool with
fixtures; a claimed compile reported only from its printed profile. STOP.

FOOT: after this act the archimedean unit's requirement is settled against quoted text or the halt
says why, one open is closed or restated precisely, and the kernel carries the enclosure or the
reason it cannot; h2 stands exactly where the deposit left it; locks last.

paste ends (part 1 of 1)
