====================================================================================================
b80 -- THE RIGIDITY ACT -- THE BANK. Registration: data/b80_registration_2026-08-21.txt
(the circularity gate's input enumeration written there BEFORE the act; the
gate-numbering reading disclosed at its head). Kernel: RigidityShadow (4
terminals, zero-axiom; Core 237/237; row 56).
====================================================================================================

(P1) VERDICT: LANDS -- THE ABSTRACT RIGIDITY LEMMA COMPILES at the zero-axiom
  bar. walk_constant: an edge-constant function on a step-graph is constant
  along every walk (head-threaded isWalk/lastOf recursions; induction on the
  walk; no algebraic hypotheses on the value type; connectedness IS the
  hypothesis that a walk exists). Instantiated at the fourth-root carrier
  (fourth_root_instantiation); the four roots pairwise distinct
  (fourth_root_discreteness, decided). NOTHING RESISTED at the lemma's level.
  ONE TAINT CAUGHT BY PRINT AND ROUTED, disclosed: the first draft's class
  table used List.getD, whose instance carries propext at the pin (the known
  tainted list); the print flagged tower_walk_instances at [propext]; the
  table was rerouted to a plain match and the print is clean -- the
  verification law working as designed (axiom prints, not grep).

(P2) VERDICT: LANDS ON THE REGISTERED PATTERN.
  (i2) DERIVES PARTIALLY [derived-from-record at cite grade]: within a scheme,
  the recorded masses follow the sources' closed-form pattern in their
  declared parameters (b61: "the odd masses t^-(n) EXIST by the sources' own
  closed-form pattern, in the functional's own units"); a closed-form family
  is continuous in its parameters wherever defined, so the class map is
  continuous along the epsilon-direction WITHIN a scheme. CROSS-SCHEME: no
  recorded structure connects distinct regularization schemes -- "admissible"
  is record-fixed at the within-scheme fibers only; the cross-scheme topology
  is NAVIGATOR-OPEN (not invented).
  (i3) THE NAMED RESIDUE, filed explicit:
    THE CONNECTEDNESS RESIDUE (W-TOP-ADMISSIBLE-CONNECTED): the admissible
    scheme space -- the space of source-licensed regularization schemes, its
    within-scheme fibers as the record fixes them, its cross-scheme structure
    pending the sources -- IS CONNECTED BY CLASS-CONTINUOUS DEFORMATIONS.
  S1'S INVARIANCE CLAUSE = the lemma + (i1) + (i2) + THIS ONE STATEMENT --
  the reduction to one topological statement about one space, exactly the
  registered expectation.
  THE (I) VERDICT, three-graded per the hard-clause inventory:
    DERIVED: the mechanism (kernel grade); (i1) -- F^4 = 1 from the recorded
      F^2 = parity and parity^2 = 1, the class set the four roots, discrete
      (decided); (i2)'s within-scheme half (cite grade); the finite mirror's
      rigidity (decided instance).
    CONDITIONAL: the window line's rigidity is rigidity OF row 55's
      conditional object -- CONDITIONAL RIGIDITY, inheriting LIMIT-SENSE (the
      analytic object) and [UNDER H-COH-inf] (the class reading), the tags
      traveling; said so on every applicable sentence.
    OPEN (one sentence): the connectedness residue above -- S1's invariance
      clause's entire remaining content.

(P3) VERDICT: LANDS -- THE MIRROR RESTATEMENT. The finite tower is the
  lemma's exact case: vertices the levels, edges the iota steps (towerAdj),
  values the fourth roots, f the banked per-level class (the unit trace i at
  every banked place-2 level, rows 35-36 -- BANKED DATA, declared); the walk
  0-1-2-3 and the bounded edge-constancy DECIDED (tower_walk_instances). ONE
  RIGIDITY MECHANISM on both sides of the mirror: the finite side at decided
  grade; the window side at the three-graded (I) verdict above.

THE CIRCULARITY GATE'S RE-CHECK (at the report's close; the result here and
there): the enumeration (i1)/(i2)/(i3) against the act as run -- the act used
exactly those three inputs; THE WINDOW LINE'S VALUE APPEARS NOWHERE (the only
class value in the act is the FINITE mirror's banked table (0,1) -- the
finite tower's data, not the window line's; the (V) clause untouched, its
sentence absent from every derivation). RE-CHECK: PASS.

THE CLOSURE SEQUENCE'S STATE: (E) at row 55's grades; (I) at this act's
verdict (derived mechanism + conditional rigidity + the connectedness
residue); (V) NEXT as step five (the sign mechanism) with its own circularity
gate to be written before its act; then the mandatory I-14 pass before step
six per the checkpoint ledger.

HIT RECORD: P1, P2, P3 all on their registered branches; the one taint caught
by print and routed, disclosed above.
====================================================================================================
