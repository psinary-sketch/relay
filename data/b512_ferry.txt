RULING (R121) BEGIN — THE RECTIFICATION
RULING (R121), the author's, ratified by this paste and strikeable.

(1) THE LADDER'S SHAPE FINDINGS ARE WITHDRAWN AS WINDOW ARTEFACTS.
b511 measured xi's margin monotone at all 118 steps in a family
inside the compiled class; the three extrema of the piecewise-
linear family (4.062, 5.196, 13.153), their attribution to zeros 1
and 4 (b503, b504), the "rise after the minimum", and the c/a^2
reading are readings of the old windows' slope jumps and not of the
object. Each is annotated by appended note; b507's fold digest is
annotated where it carries them. The identity m = Z - P stands: it
is b321_identity, not a reading.
(2) THE LADDER'S ROLE IS RESTATED. On every family tried — piecewise-
linear, B-spline, notch at k = 1, 3, 5 — the control Q0, with
seventeen off-line pairs below 150, has a positive margin at every
verified cell, and its off-line part stays below its on-line part
(ratio 0.03 to 0.50). The ladder is therefore an instrument for
EXHIBITING a violation of the sign clause in an object known to
have one, and it has not yet exhibited it. It is not evidence for
h2 at xi, and no sentence of the corpus cites it as such; the
(R118)(1) bar stands. Its family samples a slice of the class
h2_sign quantifies over, and the register "balance-to-positivity at
the multiplicative place, at a cell" is recorded as the LEAST
discriminating of h2's forms measured to date.
(3) VERIFICATION HAS TWO STATUSES FROM THIS ACT ON: VERIFIED-STRICT,
where the bound is a proved bound; VERIFIED-EST, where the bound is
b501's estimate or its descendants. Every cell of the record to
date is VERIFIED-EST, and the archimedean tail beyond u = 1200,
priced nowhere, is a named shortfall of the estimate. A strict
bound is a work-order, W-ORD-STRICT-BOUND, trigger: the author's
word or the next act that cites a cell count in a keystone.
(4) W-ORD-FAMILY-SENSITIVITY IS AMENDED TO A THIRD FAMILY designed
against the pair itself: the MATCHED window g(u) = cos(gamma_0 u)
times a B-spline bump, gamma_0 = 16.290216, inside classK, whose
transform concentrates at the pair's height so the pair's cosh
growth in the width competes with its on-line neighbours alone;
for xi the same window at 14.1347 as its own control. Trigger: the
act after b512.
(5) The kernel lane opens for b512 and shuts at its close.

RULING (R121) END

FERRY BEGIN — paste for Claude Code (part 1 of 1; confirm receipt-in-full)

ACT b512 — THE REGISTER-EQUIVALENCE READ AT h2_sign.

Component 1 — the forms, stated or not. For each of: the five
deposited registers of §27.3 (universality of silence_universal;
the ConservationHypothesis of Route 3; totality of realization
through places; the balance-to-positivity distance at the
multiplicative place; the spectral-realization distance); the
two-channel Li form lambda_Z(n) >= -lambda_A(n); the cell form
m(a) >= 0 on the ladder's family; and RH as Mathlib states it —
one row: STATABLE in the kernel's own objects with the statement
printed, or NOT STATABLE with the object that is missing named.

Component 2 — the implications. For each ordered pair among
h2_sign, RH, and every STATABLE form: COMPILED (the theorem exists
at a pin, named), STATABLE-NOT-COMPILED (the implication can be
written and is not proved, with the lemma it would need named or
marked ABSENT), or NOT STATABLE. In particular RH → h2_sign: whether
zeroSide k >= 0 for k = weilTest h h follows in the kernel from all
zeros on the line, by the transform of a convolution being a
product; the Mathlib lemmas needed named by their real names, or
ABSENT.

Component 3 — the weakest form. From Component 2's table, which
STATABLE form is implied by h2_sign and does not imply it on
classK, if any — the strictly weaker register, named; or NONE.

Component 4 — b511's measurements placed as measurements, not
terminals, beside the cell form's row, with (R121)(1) and (2) cited.

The navigator's expectations: (N1) at least two of the five
deposited registers read NOT STATABLE in the kernel's objects;
(N2) RH → h2_sign reads STATABLE-NOT-COMPILED with every needed
lemma present in Mathlib by name; (N3) the cell form reads
STATABLE, implied by h2_sign, and NOT implying it. Each refutable
by a printed cell. Compiles in the kernel alone where anything
compiles; nothing at Zenodo written; nothing deposits; h2 where the
deposit left it.

FERRY END (part 1 of 1)
