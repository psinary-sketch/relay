====================================================================================================
b37 REGISTRATION - THE ACT-8 RE-RUN UNDER THE REGULARIZED ARCHIMEDEAN TRACE. NO MEASURED
NUMBER IN THIS BLOCK.
====================================================================================================
W-CONSTRUCTION-1 act 9, item 2 (licensed by item 1's verdict, recorded in the act report:
(REG-lands) at content MODULO ONE NAMED JOINT - the geometric term's sector apportionment;
the epsilon-part's sector split is exact by mode-diagonality, Lemma 5.4's mode-attached
t(n)). Relay-only, bench-grade. ON THE FACE: the act computes and records; it decides
nothing; a match would be a FINITE-INSTANCE statement, the double limit untouched; the
register moves only via the closure protocol. NO SIGN SENTENCE AT COMPLETE ROSTER.
h2 UNCHANGED. NOTHING DEPOSITS. NOTHING CIRCULATES.

WHAT CHANGES FROM b36 (same cells, same family, same staircase, same place sets S3/S4,
same C0 pins): the archimedean trace is NOT the 11-mode model sum; it is SUBSTITUTED at
content by CC Thm 4.7 (the banked statement: Tr_reg(full Sonin) = W_inf + int f eps
d*rho), so the archimedean side is exact-at-content and resid47 is removed from the
anatomy BY CONSTRUCTION (the regularization's own residual under substitution is zero
identically; said so, as the ferry orders). Delta_- is now the SERIES object (act 7's
closed form: the odd-index part of the eps machinery), not the truncated mode trace.

TWO BRACKET READINGS, BOTH REGISTERED LONGHAND BEFORE ANY NUMBER (no fitting; the two
readings differ by pure algebra and both are computed):
 (B-dictated) the act-8 ferry's bracket: RIGHT = [Tr_reg(full) + E2 - Delta_-] - Thq
     = A + 2 E2 - E2odd - Thq. Then D = LEFT - RIGHT = (Thq - PR) + (E2odd - 2 E2).
     ALGEBRAIC EXPECTATION, registered: E2odd - 2 E2 = -(2 E2even + E2odd) < 0, so a
     stable negative D of order E2 is EXPECTED under this bracket - the epsilon-channel
     double-count made manifest as exact algebra.
 (B-sector) the sector reading: RIGHT' = Tr_reg,+ - Thq with Tr_reg,+ = A + E2 - E2odd
     (the E1-sector regularized trace; NOTE the flagged joint: this assigns ALL of
     W_inf to the even sector - the apportionment choice on the face). Then
     D' = (Thq - PR) - E2even. ALGEBRAIC EXPECTATION: the finite-level deficit
     (Thq - PR) is CLOSED-FORM (act 8/9's lemma: Thq/PR coefficient ratio =
     (p^n - p^k)/(p^n - 1) < 1, deficit -> 0 at the level limit), and E2even is the
     even-sector epsilon mass - the density question's content: D' localizes the
     equation's open content onto [the level limit] + [the even-sector epsilon term].

TERMS: A, PR from the atlas's certified channels (unchanged); Thq from the b36 quotient
machinery (unchanged); E2 = 2 int_1^{a^2} f_a(rho) eps(rho) drho/rho (the x2 symmetric-
extension REGISTERED CHOICE carried from b36, unchanged); E2even/E2odd = the same
integral with the eps series' index mask (CC index n even / odd - the act-7 sector
naming, verified sitting 23 x P2). eps by the qeps layer, NTERM = 11, NQ = 700, NG = 400,
the fixed 240-point rho grid carried from b36.

CHECK CLAUSES:
 C0 (VOID gates): the b36 pins unchanged (carto far-ends; sum lam2; sum lam2 xi1^2;
    epsprime1). PLUS the parity-split gates: |epsprime1_even - 14.177305| <= 1e-3 and
    |epsprime1_odd - 8.819138| <= 1e-3 (the b35 banked split reproduced by the mask
    BEFORE use) and the mask algebra E2even + E2odd = E2 exact to 1e-12 per cell.
 C1 (removed): resid47 does not exist under substitution; recorded as "0 by
    construction (substitution at content)" per cell, as the ferry orders.
 VERDICT MAP (carried from b36, applied PER READING and no wider): (I-match) |D| < 0.01
    at every cell and stable -> finite-instance identity, range law on the face, the
    closure protocol's step one triggered (by (I-match) ALONE); (I-differ) stable
    nonzero -> the obstruction located by the component table ((Thq - PR) vs the
    epsilon components - each closed-form or series-computable, nothing fitted);
    (I-third) otherwise.
 STABILITY: the archimedean side is series-exact (no NQ/NMODE triple needed); the
    stability column reports the eps-grid interpolation error bound (the two NG-doubled
    probes) propagated per cell; STABLE if < max(5% |D|, 0.005).

FLAGGED NOTE, filed here so it is not silent (a b36 registration gap, found act 9): the
atlas family f_a is Weil-form-compatible but NOT pole-constrained (the banked pole
column is nonzero: 2.0095 at a = sqrt2) - the acts-8/9 benches run the equation's
anatomy on an unconstrained family. The component conclusions (algebraic anatomy,
coefficient ratios, epsilon shares) are family-independent; a CONSTRAINED-family bench
(ghat(+-i/2) = 0 imposed on the bump) is PRICED as a future instrument, not run here.

ROUTING (Rule 4): registration and instrument by Fable; the banked run to the OPUS lane
if available. Outcome: THE OPUS LANE WAS AVAILABLE - the banked run executed on Opus, first attempt,
all C0 gates + the b35 parity pins + the mask algebra PASS (deterministic re-run of the
debugged instrument; the Fable debug pass had no fixes to the registered quantities).
====================================================================================================
