CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

THE STABLE RANK -- the archimedean instrument's subspace
selected by the source's eigenvalue-one characterization so
its dimension stops stepping under refinement, with the prior
act's square reproduced at stable rank and a nonempty reach,
one act. Scope: an INSTRUMENT BUILD (act two of the
archimedean instrument) and its reproduction; NO test function
constructed, NO archimedean Weil distribution computed, NO
unit used; NO aggregation stated, M-2 owed under its cap;
normalizations before counts; every number with two-axis
convergence and its rank printed beside it; the noise-floor
gate in the path; every reported number from a committed tool
with fixtures; needles from the extract file; stem sweep at
extended scope; hedge audit; registration term-scanned,
satisfiability-checked, sealed, no counts predicted; the suite
re-run after the push; nothing about the h2 identity beyond
the register sentence exact; nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
push anything ahead of origin, saying so if nothing is; and
the KERNEL-COVERAGE REPAIR, sixteen acts overdue: add every
Core module carrying a print target to the certification
file's import list, rebuild, regenerate the profile from
source, compare -- every pre-existing print byte-identical, the
count before and after stated, every newly-certified terminal
listed with its axiom profile read from the printed file; the
coverage gate then run and shown to pass, with its fixtures
showing it can still fail. If any newly-imported module
carries an axiom-bearing terminal, that is reported at full
prominence and the profile is banked as it prints, not as it
was hoped.

COMPONENT 1 -- THE CHARACTERIZATION, quoted from the extract:
the object's space as the eigenvalue-one eigenspace of the
projection sandwich, with the sandwich's constituents in the
source's normalization written once. Then the scheme: build
the sandwich on the grid, take its spectrum, and select the
eigenvectors within a threshold of one -- the threshold fixed
in the registration before any spectrum is seen, with the
argument for it stated (the source's prolate eigenvalues fall
strictly below one and the corpus knows their values). Report
the rank so selected at each grid and domain, and show it
stable across the refinement ladder where the grid scheme's
rank stepped.

COMPONENT 2 -- THE REPRODUCTION at stable rank: the prior act's
square at the same cells; the identity between the square and
the smear of the autocorrelation re-proved on the stable
subspace; the corpus's old expansion vectors still measured
outside; the source's worked inner product still recovered.
Then the reach: cells at which both axes now agree inside the
sealed bar with the rank constant -- reported as the
instrument's reach, and if still empty, the reason named and
the second scheme (pin the rank) tried before the act closes.

COMPONENT 3 -- WHAT THE INSTRUMENT NOW IS: capabilities and
limits as a list; the subspace's relation to the grid scheme's
stated (which vectors the threshold admits that the grid
constraints did not, or the reverse, measured); the unit's
membership residual recomputed on the stable subspace and
reported as a measurement beside b316's, with no verdict --
that decision stays the membership act's.

CLOSING: the correspondence rows by the idempotent tool; every
result keyed; the certification profile's new count and the
coverage gate's pass recorded; the object's conditions
restated; M-2's row unchanged under its cap; the seam's debt
item 1 restated; the patent clock restated; the in-flight
register; the ledger census; the lawful-function-and-control
act named as next, with the three covered cells named as
where the source's theorem will serve as the control.
Registration gate; index queried; stable-rank keyed.
Components ordered. THE SHADOW: expected nothing beyond any
finite-decidable rank arithmetic that carries its scope; say
which.

Bank: data/b319_the_stable_rank.txt. Deviation rule standing.

EXECUTION: ferry scan first; the coverage repair; the extract
step for every read; registration satisfiability-checked,
term-scanned, sealed; components in order; full control suite,
re-run after the push; pins by ls-remote across all three
repos; the hook if PLACE-papers is touched; mirror if it
moves. STOP.

FOOT: after this act the archimedean instrument has a
dimension that holds still, a reach that is not empty, and a
certification file that carries every terminal the kernel
actually compiles; the lawful function and the source's
theorem as control are next; h2 stands exactly where the
deposit left it; locks last.

paste ends (part 1 of 1)
