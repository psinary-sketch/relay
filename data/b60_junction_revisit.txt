====================================================================================================
b62 -- THE JUNCTION REVISIT -- BANK. The staged reciprocity registration RUN;
run-registration: data/b62_registration_2026-08-21.txt (banked before any step was
trusted; the guard and the branches verbatim; the one candidate C1 and the law form L
fixed there before any value). File name per the ferry, verbatim:
data/b60_junction_revisit.txt.
====================================================================================================

THE DERIVATION CHAIN, EXECUTED, EACH STEP GRADED:

STEP 1 -- DERIVED (banked, b57): the finite chart trace is the diagonal sum of the
transform kernel; the archimedean analogue must be the regularized diagonal mass of
the archimedean transform.

STEP 2 -- DERIVES from the recorded sources: by the recorded mode-diagonality (the
sector split exact by mode-diagonality; the keystone's carried line), the
transform-weighted trace of a test object in the sources' class splits as the
mu_4-character-weighted mass sum (the algebraic core = row 35's compiled shape). ON
THE RECORDED CLASS this collapses further: the class is EVEN (the b32 footnote-10 pin,
verbatim: CC's class is "only the even prolate functions"; F^2 = 1 on evens, spectrum
{+1, -1}, "the +/-i sectors live entirely in the ODD part" -- b32's longhand,
registered and banked), so the transform-weighted mass is m_1 - m_{-1}: EXACTLY the
shape of the b35-banked parity split. THE DERIVED DATUM at the recorded pin
(arithmetic on banked pins, no comparison licensed, commensurability declared: a
REAL mass in the functional's own units at the declared eps'(1+) evaluation of the
CC Lemma 5.4 assembly):
    m_1 - m_{-1} = 14.177305459 - 8.8191383 = 5.358167159    (real; bench-grade DATA).

STEP 3 -- UNDERIVABLE AT THE SOURCES (gamma-1): the archimedean CHART TRACE is the
identity limit of step 2 inside the eps-scheme, and NO recorded object fixes a
scheme-invariant value for that limit: the pins are declared-evaluation DATA at bench
grade (the corrected stop's own language, b32: "measured properties of constructed
objects are DATA at bench grade"); the no-compact-open theorem is the recorded
structural reason no finite-exact-level shortcut exists. Which object: the identity-
limit of the regularized transform-weighted mass. Which step: the g -> identity limit
inside the eps-scheme.

STEP 4 -- UNDERIVABLE AT THE SOURCES, TWO-FOLD:
  (gamma-2) THE REAL-CLASS OBSTRUCTION: the recorded functional's class is EVEN; on
  evens the transform-weighted mass is m_1 - m_{-1}, REAL-VALUED. No non-real unit --
  in particular NO EIGHTH ROOT -- can arise within the recorded class, regardless of
  the limit. The registered candidate C1 (unit e^{-i pi/4}) is off the real line:
  the partner cannot be reached from the recorded class at all.
  (gamma-3) THE ODD-PART RESOLUTION: the imaginary content i(m_i - m_{-i}) lives in
  the odd part, and the sources record the odd sector only as the COMBINED defect
  Delta_- (the odd-index part of CC's series; the b35 tail's parity undeclared); the
  full eps_-(rho) function and its trace-class bookkeeping are ALREADY the
  epsilon-lemma's NAMED OWNER in the corpus -- the +-i-separated masses are not
  derived anywhere.

STEP 5 -- MOOT: no derived chart trace exists to meet the law form L. The surprise
protocol never armed (branch (alpha) never threatened); no mismatch was measured
(branch (beta) requires a derived partner; none derives).

### VERDICT: BRANCH (gamma) -- THE REGISTERED EXPECTATION. Underivable at the
sources, at three named points (gamma-1, gamma-2, gamma-3 above, each source-checked
verbatim). THE ACT CLOSES AS THE POSTER'S SECOND SPECIFICATION, first-class:

THE SECOND SPECIFICATION (filed to the wanted-poster row): for the {2, infinity}
product-law derivation to EXIST, the sources would need to fix exactly two things:
(S1) a DERIVED, REGULARIZATION-INVARIANT IDENTITY-LIMIT for the transform-weighted
  mass in the eps-scheme (the pins are evaluations; the limit is the missing object
  -- the same species of invariant the first specification names);
(S2) THE ODD-PART SECTOR THEORY RESOLVED TO +-i: the eps_-(rho) function's
  trace-class bookkeeping (the epsilon-lemma's named owner) carried to the point
  where m_i and m_{-i} exist separately -- without it the recorded class is even and
  real-valued, and no eighth root can appear.
THE REGISTERED SUSPECT, at its registered-candidate grade WITH its chain: if S1 and
S2 both exist, the wanted unit is C1 = e^{-i pi/4} (fixed by the law form L and the
proved finite side u_2 = 1 + i, up to the underived positive-real normalization) --
the chain stops exactly at S1/S2. The classical-reciprocity motivation REMAINS the
navigator's assertion (the derivation did not reach it; no classical fact entered).

THE BOUNDARY-PHASE READING: inherits the underivability; the mechanism line's
two-readings wonder KEEPS its wonder grade, said plainly.

THE GUARD'S RECORD: no candidate beyond C1 was evaluated; no numerical match of any
kind was observed or claimed; the one derived datum (5.358167159) was compared to
NOTHING.

RANGE LAW: the derived half is structure plus one bench-grade datum at a declared
pin; the underivability verdicts are statements about the sources AS RECORDED;
nothing about the level limit, the junction's eventual content, or h2. The junction
remains a fence; this act derived AT it and named what is missing.
====================================================================================================
