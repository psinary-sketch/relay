CONCURRENCY: SOLO (research seat). FERRY_STANDING v2 by
reference; anchors by the tool; extract-to-disk for every read;
registration LOCKED before any write, chained on every pre-lock
gate; the face is written from a survey, never from a belief
about the terrain; the face is not widened mid-act. The
instrument lane and the wave stay parked; nothing deposits —
and nothing is written at Zenodo in any branch.

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

RULING (R18), the author's, ratified by this paste and
strikeable: TWO MAPS, TWO KEYS. A CLUSTER map keys on subjects
and domains. A CONSTELLATION map keys on interrelated verified
statements — terminals, premises, correspondence rows and the
kernels that carry them. The federation map is the cluster map;
the keystone correspondence union is the constellation map.
Each states in its own head which it is and what the other
holds. Recorded with the reason: subjects emerge from research
and are RECORDED, so a cluster map drifts when nobody records;
verifications are dug for and are BUILT, so a constellation map
drifts when nobody builds. Neither map is merged into the
other, and this ruling adds a head note to each and nothing
else.

ACT b389 — THE LOOK-SEE, THE DEPOSITED LAYER, AND THE UNREACHED
REPOSITORY. Number not claimed by any unclosed ferry.

COMPONENT 1 — THE CLUSTER SYNTHESES ENUMERATED: every document
in the papers repo's clusters directory, and every row in the
registry's support tier that names a cluster synthesis or a
cluster. For each: its date, the cluster it synthesizes in its
own words, and whether that cluster appears in the federation
map's table as now refreshed. Report whether more than six
cluster syntheses exist, and whether any names a subject the
map does not carry. Nothing added to the map by this act;
findings routed.

COMPONENT 2 — THE DEPOSITED LAYER, READ LIVE AND READ-ONLY:
every Zenodo record under the programme, enumerated from the
platform, with its version, date and concept identifier. For
each software record, the repository it names, read live, and
what the repository's current state is against the deposited
version — a version distance, not a judgement. Then: which
records the corpus's own deposit note lists as citable, and
which exist but are unlisted. Then, from the record and not
from inference: is there a WRITTEN rule for what deposits?
Search for it in the author's words; if none is located, say so
and route a request for one, with the practice this act
observed stated as observation rather than as rule.

COMPONENT 3 — THE UNREACHED REPOSITORY: the citation naming a
repository that returns not-found, with its document and line.
Attempt a read by every route available that does not require
credentials — the account listing, any archived copy, any other
corpus citation of the same name. Report ABSENT, PRIVATE, or
UNDECIDABLE-WITHOUT-CREDENTIALS with what each would mean for
the citing document's Proposition. No document edited; routed
with the three readings b388 already drafted, corrected by
whatever this component learns.

COMPONENT 4 — (R18) APPLIED, head notes only: one head note to
each map naming which key it uses, what the other map holds,
and the drift each is prone to. Nothing else in either
document is edited, and the prior heads are preserved as the
map's own precedent requires.

CLOSING: the trails ledger through its writer, with the
constellation map's own currency entered as an item (five named
keystones carry no correspondence table, so the graph it
assembles is missing nodes) — entered, not opened; the four
lists restated OPEN; the citation question restated as awaiting
the author, unmoved; the five clusters that changed shape
restated as awaiting the author's reshaping; correspondence
rows; keys; both censuses; pins by ls-remote through the
roster; hook and mirror where owed. The next ferry as DRAFT —
NAVIGATOR EDITS. The navigator's expectations: (F1) more than
six cluster syntheses exist; (F2) at least three deposited
software records sit at versions their repositories have passed;
(F3) no written deposit rule is located. Each refutable by a
printed result. h2 where the deposit left it, and this act
makes no claim about it in either direction.

paste ends (part 1 of 1)
