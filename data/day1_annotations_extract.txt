#################### day1/A_Place_to_Stand.md
### ERA ANNOTATION (2026-08-12) — bearing on the sign-face alignment table

> **`W_∞` is NOT sign-definite.** The archimedean kernel `Re ψ(1/4 + iu/2) − log π` is negative at low frequency and positive at high, crossing at `u = 2π`; measured `W_∞ < 0` across the certified band on test functions of the admissible form `f ⋆ f̃`, with the explicit-formula identity closing to `10⁻¹⁰`. The positive quantity facing the prime sum is **`W_pole + W_∞`**, not `W_∞` alone.

*Provenance: `phase2/method/SIGN_ARRANGEMENT_RECONCILIATION.md` (at filing `THE_SIGN_RECONCILIATION.md`; filename aligned to its H1 2026-08-17), 2026-08-12. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
### ERA ANNOTATION (2026-08-14) — bearing on the sign-face registers

> **The two arrangements are IDENTICAL — there is no sign-convention difference.** Both write the pole and the archimedean terms with the same signs, so the gap this era went looking for does not exist. Three consequences follow and none of them is a claim about ζ. **(1)** `W_∞` is **not sign-definite**: it crosses at `u = 2π`, so no argument may assume a fixed sign for it. **(2)** The `−2` orphan is **solved as `W_pole`** — the prior verdict that it was a determined orphan with no mechanism is **withdrawn**. **(3)** The Day-1 row is repaired to *pole + arch*. ### **`h2` — the sign of `W_∞ − W_2` — IS UNCHANGED BY ALL OF THIS.** *The reconciliation removed a suspected convention gap; it did not move the premise.*

*Provenance: `phase2/method/SIGN_ARRANGEMENT_RECONCILIATION.md` (at filing `THE_SIGN_RECONCILIATION.md`; filename aligned to its H1 2026-08-17), 2026-08-12/13. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
#################### day1/ONE_PAGE_PROOF.md
### ERA ANNOTATION (2026-08-14) — bearing on the S-table's vocabulary, which this page's premises use

> **The specification table moved `v0.1 → v0.3`, four rows split or re-worded, every correction struck-visible with its date and its forcing witness.** `S4` totally-multiplicative → **multiplicative** (Ramanujan `Δ` has an Euler product and is not totally multiplicative — measured from the corpus's own banked `τ` stream: `τ(2)² = 576 ≠ −1472 = τ(4)`). `S1` → **`S1a` ambient / `S1b` monoid / `S1c` element**. `S3` → **`S3a` continuation / `S3b` functional equation**. `§10` ratifies **`VACUOUS`** as a fourth emptiness kind. ### **THE PAGE'S ARGUMENT IS UNTOUCHED — this is the vocabulary its premises are stated in, made finer.** *And the page's own last row still holds: the step from the reduction to RH itself has* ### **NO terminal — `h2`, undischarged, manuscript-resident.**

*Provenance: `phase2/method/THE_EULER_SPECIFICATION.md` `v0.3`, 2026-08-13; `FINDINGS` `F.2026-08-13`. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
#################### day1/Which_Structure_Confines.md
### ERA ANNOTATION (2026-08-12) — bearing on the Epstein comparison

> **This paper's own witness localizes the placement-carrier to the coefficient law.** In the sealed Euler inventory the witness has the place roster (`S2`), the analytic shape (`S3`) and explicit addresses (`S5`) — the corpus has even computed them (`r_Q = [2,0,0,2,0,4,…]`, disc `−23`, `h = 3`) — and lacks only the multiplicativity of the coefficients (`S4`). Placement fails. **The removal is clean because the witness keeps the addresses.**

*Provenance: `phase2/method/THE_EULER_SPECIFICATION.md`, 2026-08-12. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
### ERA ANNOTATION (2026-08-14) — bearing on the Epstein comparison, sharpened at the monoid

> ### **`Z_Q` AT `h > 1` HAS NO GENERATING MONOID AT ALL — a single ideal class is not multiplicatively closed, so `S1b` is `VACUOUS` for it, not false.** *The Epstein witness this paper builds its comparison on is therefore* ### **a shard of an object that has a monoid, and `ζ_K` is that object.** **This sharpens the paper's own finding rather than qualifying it.** *The comparison is usually read as "Epstein lacks the Euler product" — an absence. The finer reading is that the shard has no monoid to carry a product in the first place, which is why the absence is structural and not an accident of presentation.* **`§10` ratifies `VACUOUS` as a fourth emptiness kind precisely to carry this distinction**, and the 2026-08-12 annotation above — localizing the placement-carrier to the coefficient law — is the same finding read one stage earlier.

*Provenance: `phase2/method/THE_EULER_SPECIFICATION.md` `v0.3` §§10–11, 2026-08-13; `FINDINGS` `F.2026-08-13` claim 7. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
#################### day1/Exhaustive_Enumeration.md
### ERA ANNOTATION (2026-08-14) — bearing on the two-kinds windows, now compiled

> **This paper's method now has a machine-checked instance in the window arithmetic.** The two windows are of different kinds and the difference is the point: the largest **prime-free** window is `(1/2, 2)`, and the largest **one-prime** window is `(1/3, 3)` — one prime power, namely `2`. Both, with Lemma A (*exactly one prime power is `≤ 2`*), are compiled in a vanilla Lean 4 kernel of eleven terminals, every one closed by `decide` and every one reporting ### **no axioms at all** — not the standard three. *That is this paper's template exactly: enumerate a finite specification, verify the enumeration is complete, check each case, conclude by exclusion — with the completeness check discharged by the kernel rather than by the reader.* ### **WHAT THE KERNEL DOES NOT BUY, recorded here because an enumeration paper is where the temptation to over-read is largest:** it proves the **arithmetic residue** — a statement about prime powers below a bound — **not** the corpus's statement over the real interval `λ² ∈ (2,3)`; the windows are **half-vacuous**, since every prime power is `≥ 2 > 1 ≥ 1/L` and the lower endpoint never binds; and maximality is proved **at integer bounds only**.

*Provenance: `SIDE-window` `v0.1.0` = `ecd5cf7` (PUBLIC, 2026-08-14); `FINDINGS` `F.2026-08-14`. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
#################### day1/HELD_F7_two_clause_reframe.md
### ERA ANNOTATION (2026-08-14) — bearing on this set's held status, which the era did not change

> ### **THIS SET REMAINS `HELD` AND `NOT APPLIED`.** *No act of this era landed any site in it, and this annotation lands none either — it is filed so a later reader does not mistake the era's activity elsewhere for movement here.* **Where it would be re-checked if the author ever lands it:** the S-specification, which moved `v0.1 → v0.3` this era and now carries a dependency structure the sealed table did not have. *The two-clause accounting is a claim about which premises carry which load, and the S-table is the corpus's inventory of exactly that.* ### **Part III's single-premise claim is UNCHANGED, as this document already states. Nothing deposits; checksums untouched.**

*Provenance: `phase2/method/THE_EULER_SPECIFICATION.md` `v0.3`, 2026-08-13. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
#################### day1/Seven_Mechanism_Classes.md
### ERA ANNOTATION (2026-08-12) — bearing on the `n₄ = 0` result

> **The additive–multiplicative conspiracy has no seat in the Fano incidence, and this paper's own theorem is why.** Its site is the internal (distributive) interface, which `n₄ = 0` assigns zero mechanism classes. It is not a point, so it lies on no line, and the incidence structure constrains nothing about its exclusion.

*Provenance: `phase2/method/THE_DEEP_STRUCTURE_CROSSREFERENCE.md` (Q2) and `THE_DAY1_IMPLICATIONS.md` (H1), 2026-08-12. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
### ERA ANNOTATION (2026-08-14) — bearing on placement, and what it tracks

> ### **PLACEMENT TRACKS THE OBJECT'S OWN MONOID, NOT THE AMBIENT RING.** *`𝒪_K` has `h > 1` in both rows of the comparison, yet the element-level failure is common to the whole and to the shard and does not track placement — so the ambient ring cannot be what places the zeros.* **Grade: `OURS`, question-grade** — recorded as a reading this corpus owns, not as a theorem. ### **BEARING ON THIS PAPER: the seven classes are derived for `ξ`, and this finding is about what would have to be re-derived for anything else.** *A class enumeration transported to another object inherits the ambient ring for free and the object's own monoid not at all — which is the step to check first, and the one this paper's framing does not by itself supply.*

*Provenance: `phase2/method/THE_EULER_SPECIFICATION.md` `v0.3` §11, 2026-08-13; `FINDINGS` `F.2026-08-13` claim 8. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
#################### day1/Silence_of_Foundations.md
### ERA ANNOTATION (2026-08-14) — bearing on the naming of silence

> **The corpus repaired its own term for this paper's subject: `S5`-blindness → ### `S5`-SILENCE** (and *the `S5`-silence theorem*; *the instrument is silent across `S5`*). ### **THE THEOREM'S CONTENT IS UNCHANGED. The reading is constant — non-separating — over `S5`'s addresses.** *The repair is a register repair, and it is this paper's register: an interface that says nothing about a parameter is not failing to see it. Blindness names a defect of the instrument; silence names a property of the reading, which is what `κ = 0` actually asserts.* **Counted rather than narrated:** 7 scoped hits across 4 files at `9641627` → **1**, that one residual being the correction entry's own quotation of the old term, classed `PROVENANCE-QUOTATION` and kept struck-visible by design.

*Provenance: `phase2/method/THE_ATTEMPT_RECORD.md` corrections 12 and 13, 2026-08-13/14. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
#################### day1/Spectral_Inertness.md
### ERA ANNOTATION (2026-08-12) — bearing on the interface result

> **The placement-mute reading, stated at its earned grade.** This paper's inertness result and the `n₄ = 0` theorem together say the additive–multiplicative interface transmits density information and not placement information. A derivation joining them concludes that any decider must supply placement at the multiplicative place from a source that is *not* that interface — **a conclusion that re-derives §27.3's fourth register and is therefore a consistency check, not a new result.**

*Provenance: `phase2/method/THE_DAY1_IMPLICATIONS.md` (H3), 2026-08-12. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
### ERA ANNOTATION (2026-08-14) — bearing on the instrument register, and why it moved to match this paper's

> **The corpus's instrument register now says *silent* where it said *blind*, for the same reason this paper says *inert* rather than *absent*.** ### **A component that contributes no `s`-dependence is not failing to act — it is acting without separating.** *The product formula builds the entire integration domain `C¹_ℚ` and contributes nothing to zero location; the instrument reads constantly across `S5`'s addresses and separates none of them. The two statements have the same shape, and* ### **non-separating-over-`S5` is the exact analogue of this paper's `κ = 0`.** **The theorem's content is unchanged on both sides** — only the word that had smuggled in a defect where a property was meant.

*Provenance: `phase2/method/THE_ATTEMPT_RECORD.md` corrections 12 and 13, 2026-08-13/14. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
#################### day1/Third_Identity_Element.md
### ERA ANNOTATION (2026-08-12) — bearing on the substrate and its axes

> **The substrate's `(ℤ/2)³` is kin to the `ℤ/2`-at-`∞` family at exactly one axis and distinct from it as a family.** `Gal(ℂ/ℝ)`, `Br(ℝ)` and `ℝ*/ℝ*²` are one `ℤ/2` by local class field theory; `⟨−1, 2, 3⟩` is that `ℤ/2` times a `ℤ/2`-at-`2` times a `ℤ/2`-at-`3`. Separately: the kernel's `Substrate` type carries no arithmetic at all — no `ℚ`, no places, no localization — so no question about the archimedean axis can be settled there in either direction.

*Provenance: `phase2/method/THE_DEEP_STRUCTURE_CROSSREFERENCE.md` (Q1), 2026-08-12. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
### ERA ANNOTATION (2026-08-14) — bearing on element-level versus monoid-level, and the non-canonical pairing

> **`ζ_K` at `h > 1` separates ELEMENT-level from MONOID-level factorization**: `S1c` ✗, `S1b` ✓, no known off-line zeros, GRH open. **And the sharper half — `Z_Q` at `h > 1` has ### NO generating monoid at all**: a single ideal class is not multiplicatively closed, so `S1b` is ### **`VACUOUS`** for it rather than merely false. ### **BEARING ON THIS PAPER, AND IT IS DIRECT: the class ↔ discriminant pairing above is declared non-canonical — basis-relative under `GL(3,𝔽₂)`, forbidden-as-canonical by the functorial-limit negative theorem. The era now supplies a SECOND and independent reason to keep element-level assignments off the canonical shelf** — *arithmetic itself distinguishes what holds at the element from what holds at the monoid, and it is the monoid level that survives the passage to `h > 1`.* **The count-level bijection — identity count equals formation count — is untouched and remains the theorem.**

*Provenance: `phase2/method/THE_EULER_SPECIFICATION.md` `v0.3`, 2026-08-13; `FINDINGS` `F.2026-08-13` claims 6–7. **This note states a later finding and edits no argument above it.** No claim in this document moves.*

----------
