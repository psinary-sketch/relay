FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

WHERE THE ARITHMETIC ENTERS, AND WHAT AN INSTRUMENT THAT SEES
THE SCALING WOULD COST -- one read and one pricing, one act.
Scope: nothing about the h2 identity beyond the register
sentence exact; NO aggregation stated, M-2 owed; nothing
built; the source is read at content and where a text layer
is truncated the page image is the content; the barrier, the
compression and the escaped-part finding are carried, not
re-derived; nothing deposits.

STEP ZERO: the ferry scan; push anything ahead of origin,
saying so if nothing is.

COMPONENT 1 -- THE ENTRY. The source's positivity is of the
shape A*A and carries no arithmetic content by itself. Read
the source for where the arithmetic enters its construction:
the test function's role, the trace formula the smeared
operator is paired against, and the specific operator whose
square gives the positivity. Quote each with its location.
Then state, in the source's own terms, what quantity carries
the primes -- and whether it is a property of the operator, of
the test function, or of the pairing between them. Decided by
the source's text; no analogy to the corpus's objects is drawn
in this component.

COMPONENT 2 -- THE COMPARISON, only after Component 1 is fixed:
the corpus's prime side is the adopted prime sum; its
quotient channel is an orbit count silent at the top level.
State, by definitions, whether the source's prime-carrying
quantity is the same species as either -- and if it is the
same species as neither, say so at full prominence, since that
would mean the corpus's left side has never had an object of
the kind the source uses to carry the primes.

COMPONENT 3 -- THE INSTRUMENT PRICED, not built. The model
carries the compact part of the multiplicative group and
drops the scaling part. Price an instrument that carries the
scaling part: what objects it would compute on (locally
constant functions of compact support on the local field are
the natural candidate -- state whether the corpus's own chart
and Haar normalization already define them), what exact
arithmetic it would need, what the banked results it must
reproduce are (the constrained space's two conditions; the
compact-part zero; the annihilation criterion at the cells
it can reach), and what it would let the campaign compute for
the first time. A cost in acts, a list of what it must match,
and the artifact it retires -- no code.

CLOSING: the forced-positivity work-order discharged or
restated with what Component 1 found; the correspondence rows
by the idempotent tool with the notation guard, read back;
every result keyed; the object's three conditions restated
from the list; M-2's row unchanged; the seam's debt item 1
restated; the in-flight register; the fold (acts since the
last one) named as due after this act; the mirror rebuilt and
verified if the papers repo moves. Registered in advance --
written here, in this ferry, as the seat's to use: (F1) the
arithmetic enters through the test function and not the
operator; (F2) the source's prime-carrying quantity is the
same species as the corpus's prime sum; (F3) the corpus's
chart already defines the objects the instrument needs; each
expected true and each refutable by a quotation. No counts
predicted. Registration gate; index queried;
arithmetics-entry keyed. Components ordered. THE SHADOW:
expected nothing -- reads and a pricing; say so, checked not
assumed.

Bank: data/b305_the_arithmetics_entry.txt. Deviation rule
standing.

EXECUTION: ferry scan first; owners and the source read at
content first; registration satisfiability-checked,
term-scanned, sealed; components in order; full control
suite; every reported number from a committed tool with
fixtures. STOP.

FOOT: after this act the corpus knows where the source's
arithmetic enters and whether it has an object of that species
at all, and it holds a price for the instrument that could see
the direction the model cannot; h2 stands exactly where the
deposit left it; locks last.

paste ends (part 1 of 1)
