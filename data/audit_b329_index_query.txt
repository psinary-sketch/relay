############ QUERY: finite-side-seal
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'finite-side-seal'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: the finite-side seal
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'the finite-side seal'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: the fixed-point silence
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'the fixed-point silence'
==============================================================================
  ### THE RECORD HOLDS 2 INDEXED RESULT(S) ABOUT THIS OBJECT:

    key      : faces-ledger
    act      : b327 (a ledger build, author-authorized 2026-09-04)
    result   : PLACE-papers/FACES_LEDGER.md: thirteen rows -- the pentagon's five faces as the deposit states them, the finite-instance identity, the Sonin margin, the Li margin, the spectral-realization wall, the fixed-point silence, the two-radius family, the Epstein negative control at b326's result, and the live row (the Li-to-Weil bridge) -- each with its claim quoted from its emitting file, graded PROVED / MEASURED / IMPORTED / NAMED-ONLY, its correspondence rows, and its owed bridges; a cascade section with one of STATED / OWED / NONE for all 78 pairs
    grade    : ### A MAP OF THE PREMISE, NOT A CARRIER OF IT. ### THE LEDGER CERTIFIES NOTHING AND COMPILES NO EQUIVALENCE -- the deposit's refusal is quoted in its head and governs it; every row's grade is its owning act's and no row is promoted by its neighbours. ### Rows enter only through tools/b327_faces_row.py (duplicates refused, notation guarded, every quotation verified against its emitter before writing, read back after every write). ### The owed bridges by ID: W-ORD-LI-WEIL-BRIDGE, W-ORD-DISCRIMINATING-FAMILY, W-ORD-LI-FAMILY-CONTROL. ### NO GRADE MOVED   ### as its own act recorded it
    location : PLACE-papers/FACES_LEDGER.md; FINDINGS.md anchor faces-ledger; data/b327_the_faces_ledger.txt; data/b327_registration_2026-09-05.txt (sealed before any instrument ran); CORRESPONDENCE.md row 166

    key      : faces-ledger
    act      : b327 (two contacts in the emerging-programmes ledger; not seeds)
    result   : EMERGING_RESEARCH_PROGRAMMES.md, Contacts filed 2026-09-05: the Curie reading of the eigenvalue-one boundary (the remainder's weight diverging at the boundary as a susceptibility; one consequence: sensitivity of the remainder to test-function perturbation near the boundary, checkable on the instrument, not checked); the cubit reading of the 256 rules (each a function on (Z/2)^3; rule 110's one-set a five-point Fano subset containing two lines, counted by tools/b327_notes.py; the question which Fano subsets define universal rules, not answered)
    grade    : ### CONTACTS, NOT SEEDS: no promotion criterion, no claim, no grade. ### Provenance: the navigator's conversation layer, 2026-09-04, ratified by the b327 ferry. ### Filed nowhere research-facing   ### as its own act recorded it
    location : PLACE-papers/EMERGING_RESEARCH_PROGRAMMES.md (the b327 contacts block); data/b327_notes_run.txt

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: the fixed-point sentence
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'the fixed-point sentence'
==============================================================================
  ### THE RECORD HOLDS 1 INDEXED RESULT(S) ABOUT THIS OBJECT:

    key      : fixed-point-silence
    act      : b310 (a derivation, and a bearing that is never a decision)
    result   : b304 computed the COMPACT part of the local multiplicative group and found its smear over the units zero; b309 computed the SCALING part and found it zero at every nonzero power. ### **THOSE ARE ONE STATEMENT: Tr(theta(t) Pi) IS A SIGNED COUNT OF THE OFF-BALL POINTS t FIXES, IN THE TWO CONGRUENCES THE OBJECT'S TWO CONDITIONS IMPOSE, WEIGHTED BY THE EMBEDDING'S HAAR FACTOR.** ### At t = 1 every off-ball point is fixed and the count is (p^n - 1)^2; at a nonzero power NOTHING off the ball is fixed, because p^k - 1 is a unit, and the only point fixed is the one place the object must vanish. ### **AT A UNIT OTHER THAN 1 THE COUNT IS GENERALLY NONZERO -- b304's zero is the SUM over the units, not a per-unit vanishing, and the two halves are NOT the same kind of zero.** ### Checked against b304's own trace_scaled at every unit and b309's reduced sum at every carried power, 0 disagreeing
    grade    : ### A DERIVATION, AND A BEARING THAT IS NEVER A DECISION. ### At a finite place the source's construction carries NO ARITHMETIC; the prime's contribution lives in the local distribution the source integrates AGAINST -- eq. (149), read at content by b305 -- which carries the log p and samples at exactly the powers this trace does not read. ### **THE BEARING: THE FINITE SIDE CANNOT SUPPLY THE FIRST-LEVEL MASS THROUGH THE OBJECT**, the coefficient at p^1 being exactly zero. ### On b263's three properties, for candidates of THIS CLASS: **(SPEC-1) CANNOT be met** -- the one place it demands weight is exactly where the zero sits; **(SPEC-3) CAN be met**; **(SPEC-2) IS NOT DECIDED BY THIS ACT.** ### **SCOPE: NOT A DECISION ON b262's BRANCH** (b262's own sentence is a REQUIREMENT on the archimedean side and expressly not a claim that it fails; the disjunction is b263's FORMULATION, not b262's wording); **NOT A VERDICT ON M-2**, which stays (SPECIFIED-NOT-STATED) under b263's own 'these exclude; they do not determine'; **NOT A CLAIM THAT THE FINITE SIDE CONTRIBUTES NOTHING** -- a distribution is not a trace on a space; **AND NOT AN ARGUMENT FOR THE ARCHIMEDEAN BRANCH**, where this act derives nothing. ### M-2 UNCHANGED   ### as its own act recorded it
    location : data/b310_the_smear_collapses.txt; data/b263_top_level_silence.txt (SPEC-1..3); data/b262_junction_limit.txt (its own sentence); CORRESPONDENCE.md row 133

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: the scaling trace
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'the scaling trace'
==============================================================================
  ### THE RECORD HOLDS 1 INDEXED RESULT(S) ABOUT THIS OBJECT:

    key      : scaling-trace
    act      : b309 (a computation on the b308 instrument)
    result   : Tr(theta(p^k) Pi) for k != 0 -- the compression of the SCALING part of Q_p^x against the projection onto the object's own space. ### b304 computed the compact part and REFUSED this one because the model folds it; b308 built the frame where it does not fold and NAMED this computation without performing it. ### **THE FIRST THING ESTABLISHED IS THAT THE TRACE IS NOT DEFINED UNTIL AN AMBIENT IS NAMED**: theta(p^k) carries V(n,n) to V(n-k, n+k), so the composed map is no frame's endomorphism, and the smallest frame containing both is V(max(n,n-k), max(n,n+k)). ### **THE VALUE: EXACTLY ZERO AT EVERY NONZERO POWER IN [-2n, 2n] AT ALL SEVEN BANKED CELLS**, by two independent routes -- 34 cell/power pairs by both, 10 by the reduced route only where the ambient exceeds 1024 chart points and the bound is PRINTED. ### 0 disagreeing
    grade    : ### A COMPUTATION, AND ITS VALUE IS ZERO. ### **NOT AN OBSTRUCTION THEOREM, AND NEITHER A ROUTE NOR AN ANTI-ROUTE** -- the order forbids reading a nonzero as a route and this act adds that the converse reading is forbidden too. ### **SCOPE: one trace of one map against one projection, at the cells and powers listed, in the smallest ambient containing source and target -- a different ambient is a different number.** ### It says nothing about any other functional on the instrument and nothing about the source's own functional, which smears against a test function over the whole group: a vanishing of every individual term is a statement about terms. ### **b273's A at k = n IS A DIFFERENT OPERATOR; the barrier and the compression are neither extended nor weakened.** ### NOTHING ABOUT THE ARCHIMEDEAN PLACE (b285's boundary stands). ### NO AGGREGATION IS STATED. ### M-2 UNCHANGED   ### as its own act recorded it
    location : data/b309_the_scaling_trace.txt; data/b309_components_run.txt; tools/b309_scaling_trace.py; tools/b309_components.py; CORRESPONDENCE.md row 130

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: no off-ball fixed point
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'no off-ball fixed point'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: the smear collapses
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'the smear collapses'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: the compact part
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'the compact part'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: exhaustiveness
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'exhaustiveness'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: the places catalogue
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'the places catalogue'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
############ QUERY: the unit decomposition
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'the unit decomposition'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.
