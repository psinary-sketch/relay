====================================================================================================
b72 -- THE CRITICAL PASS ACT -- THE RUN-REGISTRATION. BANKED BEFORE ANY RUN.
Six components; order of execution: 1 (gauge audit) -> 3 (general lemma) -> 2
(inventory) -> 4 (requirement) -> 5 (red team; after 1 per the ferry) -> 6
(staging). The red-team charter binds component 5: attacks written at FULL
STRENGTH first, grades decided FROM THE RECORD ONLY afterward; NO GRADE IS
REGISTERED IN ADVANCE -- pre-registering expected grades would smooth the
adversary, so the registration deliberately abstains; what IS registered is the
rubric (refuted-by-record / stands-as-risk / upheld-finding) and the law that an
attack the record cannot refute FILES AS STANDS-AS-RISK, never smoothed, and an
upheld finding enters the ledgers first-class.
====================================================================================================
FERRY 2026-08-21 (part 1 of 1, receipt confirmed). Relay-only. NO SIGN SENTENCE.
h2 UNCHANGED BY CONSTRUCTION; no archimedean number computed; the range law
governs every sentence. NOTHING DEPOSITS.

SOURCES (rule a/b, at pins): the banks' embedding usage read at content (the
instruments' root representations; the reading layer's i-identifications); rows
47, 50, 51 at their pins; the staged registrations where S1 is quoted (grep-
enumerated at the run, listed in the bank); the b71 registration's H-COH
paragraph (the premise's home).

----------------------------------------------------------------------------------------------------
THE DERIVATIONS AND PLANS, LONGHAND BEFORE ANY RUN:

(D1) COMPONENT 1 -- THE GAUGE AUDIT. The audit question: does every bank share ONE
  embedding dictionary zeta_N |-> e^{+2 pi i/N}? The layers, named in advance:
  (L-sym) the SYMBOLIC layer -- the instruments and kernel modules compute in
  Z[x]/(Phi) coordinate vectors (mono/vadd tables; the zero tests); a coordinate
  vector carries NO embedding -- the audit checks that no instrument smuggles a
  numeric embedding (grep for exp/cmath/2j/complex-literal usage).
  (L-read) the READING layer -- wherever a bank READS a symbolic value as a
  complex number ("tr S = q(1+i)"; "the center phase i"; "zeta_8 plus sign") it
  uses the dictionary zeta_4 = +i, i.e. zeta_N |-> e^{+2 pi i/N}; the audit greps
  the banks and the kernel's zpow4-style tables (| 1 => (0, 1) -- the +i cell) for
  uniformity. ANY mixed dictionary is the act's finding.
  THE GAUGE DERIVATION, longhand: let c be the coefficient-fixing ring
  automorphism zeta_N |-> zeta_N^{-1} (equivalently: reading the SAME symbolic
  record through the conjugate embedding zeta_N |-> e^{-2 pi i/N}). c fixes every
  delta_m and every rational phase (Pi's phases are +-1 -- c-invariant); c sends
  the S-kernel zeta^{+mm'} to zeta^{-mm'}, i.e. conjugates S to S*; c permutes
  the g-basis (g_{a,t} |-> g_{a,(q-t)%q ... the -t index}). Under c EVERY banked
  equation maps to its conjugate equation: the record is c-COVARIANT. THEREFORE:
  -- the BREAKING (tr S /= tr S*) is c-INVARIANT (c injective);
  -- the DIRECTION (which member of {S, S*} is "the transform"; which class is
     "plus") is GAUGE -- the composite of c with the relabeling S <-> S* is a
     symmetry of the entire finite record;
  -- b71's forced conjugacy is GAUGE-INVARIANT AS A RELATION: applying c to every
     place's character at once swaps both orientations simultaneously, so "the
     real component is CONJUGATE TO the shared finite orientation" survives in
     every gauge [the H-COH-dependent step keeps its tag];
  -- the SIGN BIT is gauge-invariant: c(-x) = -c(x), so c maps each class to the
     conjugate class PRESERVING the sign within it;
  -- THE PSEUDO-QUESTION DISSOLVES: "which ABSOLUTE eighth-root class does the
     archimedean scalar carry" has no gauge-invariant answer BY CONSTRUCTION; the
     well-posed residue is (the relative conjugacy [UNDER H-COH]) + (the sign
     bit). Filed dated and additive at rows 49/51 and the poster.
  Registered prediction P1: the audit finds ONE dictionary -- symbolic layer
  embedding-free, reading layer uniformly +; any mixed finding is the act's
  finding and the restatement then reruns against it.
  DECIDABLE CORE: the gauge covariance at (2,1) -- conjugation carries the plus
  reading (2,2) to the minus reading (2,-2) and back, and the breaking holds in
  BOTH gauges (all four conjuncts decided in Z[i] pairs).

(D3) COMPONENT 3 -- THE GENERAL LEMMA, stated in advance at full generality:
  THE MONOMIAL-TRACE FIXED-PHASE-SUM IDENTITY. For ANY size n, ANY index map
  f : Nat -> Nat (bijectivity NOT required), ANY phase assignment into ANY type R
  with ANY binary operation add and ANY zero: the diagonal sum of the monomial
  operator (column j carries phase(j) at row f(j); diagonal entry at j is
  phase(j) exactly when j = f(j)) equals the fixed-locus phase sum:
    sumR add zero (fun j => if j = f j then phase j else zero) n
      = fixSum add zero f phase n.
  PROOF PLAN: induction on n; the step is the if-condition symmetry (j = f j vs
  f j = j), by cases on Nat.decEq with if_pos/if_neg -- NO algebraic hypotheses
  on add/zero are needed (both sides assemble the same sum in the same order);
  that the identity is STRUCTURAL, not algebraic, is itself the finding to state.
  COROLLARIES THROUGH THE LEMMA (rw the lemma, then decide), one per parity at
  the smallest cells: (2,1) -- sigma, phases in Z[i] pairs, trace value (2,2)
  re-derived through the mechanism (= the banked discriminator value); (3,1) --
  sigma, phases in Z[zeta_9] six-coordinates (x^6 = -1 - x^3), trace value
  (3,0,0,0,0,0) (odd flatness: origin only). The remaining banked cells' row-47
  decided instances become READABLE AS INSTANCES of the compiled lemma -- cited
  additively at row 47's pin, not recomputed. Registered prediction P3: the lemma
  lands whole at the zero-axiom bar; whatever resists is named exactly.

(D2) COMPONENT 2 -- THE HONEST INVENTORY. S1's THREE CLAUSES, fixed here once and
  copied verbatim to every quote site (grep-enumerated at the run):
    (E) EXISTENCE -- the identity-limit/boundary-line object exists in the
        epsilon-scheme; its FINITE SHADOW is the {V_n} convergence question on
        the completed g-span (row 50).
    (I) INVARIANCE -- the scheme/regularization-invariance of that limit; its
        ASSERTED owner is the index face (NA-3) -- asserted, not derived.
    (V) VALUE -- the limit's value: THE SIGN BIT (absolute, gauge-invariant) +
        THE CLASS BIT (relative-under-H-COH per the gauge audit; H-COH itself
        under the component-4 requirement).
  No compression of one clause may speak for the others; each site gets the
  three-clause block dated and additive.

(D4) COMPONENT 4 -- THE H-COH OVERDETERMINATION REQUIREMENT, the rule verbatim
  (dated 2026-08-21, filed additively at the premise's sites):
    H-COH MAY BEAR NO FURTHER WEIGHT -- no new conclusion may be filed
    [UNDER H-COH] -- until either (i) its OWNER CONSTRUCTION is built (THE
    ADELIC PHASE PLANE, staged at component 6; the author's word), or (ii) a
    SECOND INDEPENDENT TESTABLE CONSEQUENCE of H-COH is filed and decided,
    independent meaning not derivable from the b71 forced-conjugacy chain
    already banked. The b71 class-bit conclusion STANDS at its filed
    conditional grade; it does not extend.

(D5) COMPONENT 5 -- THE RED-TEAM READ. Targets, per the ferry: (A1) H-COH
  circularity; (A2) the gauge issue as filed BEFORE component 1; (A3) the fence
  language's cross-row consistency, rows 35-51; (A4) the bench/derived
  boundary's enforcement; (A5) the dictionary's asserted faces. Attacks at full
  strength in the bank and report VERBATIM, then graded from the record; grades
  not registered (see head). Component 1 executes before this component.

(D6) COMPONENT 6 -- THE ADELIC STAGING, the dated line verbatim (nothing built):
    STAGED 2026-08-21 (b72): THE ADELIC PHASE PLANE as H-COH's OWNER
    CONSTRUCTION -- the quarter-turn on the full adele-class plane, triviality
    on Q built in, the global center its fixed point, the product law its
    stationary-phase computation; the registration to be produced CORPUS-FIRST
    at the author's word. The radialization's relation to it is NOTED AS A
    QUESTION, not a claim.

LEAN PLAN: CriticalPassShadow.lean -- monomial_trace_fixed_phase (the general
lemma); row47_corollary_2_1; row47_corollary_3_1; gauge_covariance_2_1 (4
terminals; Core 215 -> 219). Row 52; rows 47/49/51 and the poster carry dated
additive lines; the S1 quote sites carry the three-clause block; FINDINGS carries
any upheld finding first-class.

Bank: data/b72_critical_pass.txt (the ferry's name). Deviation rule standing.
Divergence note (rule a): none.
====================================================================================================

----------------------------------------------------------------------------------------------------
DEVIATION RECORD (Rule 4, appended at the run, 2026-08-21): P1's L-sym clause
registered "the symbolic layer is embedding-free" -- WRONG AS STATED. The audit
found 35 instrument files using numeric complex exponentials: the ARCHIMEDEAN and
calibration instruments (b25/b27/b32 and kin) compute numerically; only the
finite cyclotomic instruments are symbolic. THE AUDIT QUESTION SURVIVES INTACT:
every numeric kernel is F[m,m'] = e^{+2 pi i m m'/N} -- the three minus-sign
occurrences (b25_attempt2_s11.py, b27_attempt2_s12.py, b32_arch_e1.py, line
p = exp(-2 pi i c k/N)) are the CENTERED-INDEX FACTORIZATION of that same plus
kernel, derived line-by-line in the instruments' own docstrings
(e^{2 pi i (k-c)(k'-c)/N} expanded; numpy ifft carries the +2 pi i kernel), and
the conj() usages are adjoint/pairing operations, not dictionary choices. The
corrected L-sym statement: the numeric layer exists and uses the SAME plus
dictionary; the verdict of one dictionary stands on the corrected statement, and
the correction is this record.
----------------------------------------------------------------------------------------------------
