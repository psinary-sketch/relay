b210 INDEX QUERIES -- one query per key, per clause (e). Queried BEFORE the registration
was banked and BEFORE any computation.

  sonin eigenfunctions             HIT
  sonin space                      HIT
  the sonin sector                 HIT
  prolate operator                 HIT
  phi_mu                           HIT
  archimedean sector               HIT
  prolate continuum positivity     HIT
  e1-even-bridge                   HIT     (hyphenated form; "e1 even bridge" returns NO KEY)
  ------------------------------------------------------------------------------
  alpha                            NO KEY
  beta                             NO KEY
  psi                              NO KEY
  wronskian                        NO KEY

### 8 OF 12 HIT.
### AND THE FOUR MISSES ARE THE FOUR OBJECTS THIS ACT IS ABOUT. alpha, beta, psi and the
### Wronskian are the entire subject of b205, b206, b207 and this act, and NONE of them is
### reachable in the index by its own name. The record holds them -- b205's bank defines all
### four and the instrument implements them -- so this is the index's reach, exactly as the
### tool prints it: ### ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
### RECORDED AS A PLAIN STATEMENT OF FACT AND NOT REPAIRED HERE: minting keys is a curation
### act with its own bar, and this act is a computation. ### FILED FOR THE INDEX-COVERAGE REPAIR
### QUEUE, WHICH b209's FOOT ALREADY NAMED AS NEXT.

### ONE FURTHER NOTE ON THE HYPHEN: "e1 even bridge" returns NO KEY and "e1-even-bridge" HITS.
### The alias table is punctuation-sensitive in at least this one place. Reported, not fixed.
