RULING and ORDER, the author's, ratified by this paste and
strikeable:

(1) b346, THE EXPONENT BY RATE, runs next exactly as banked at
data/b346_ferry_2026-09-06.txt, unedited. Its registration
should record, where it quotes the order, that b344's floor
reading makes the rate the principled discriminator rather than
a second attempt at the value — and that this framing confers
nothing and is tested, not assumed.

(2) The executor's draft is ADOPTED as b347 with its three
items (the run file's own clock; the satisfiability audit's
numerical limit named and priced, not closed; FERRY_STANDING
v2 carrying the act-number clause), plus two additions:

ADDITION ONE — THE BAR-FLOOR RULE. b345's fixture demanded a
precision finer than the floor of the routine it tested, so it
could not separate a correct implementation from a broken one.
Mint the rule — a numerical bar is stated with the floor of the
object it tests, and a bar below that floor is uninformative
rather than strict — mechanize it in the registration gate (any
sealed numerical threshold carries either the floor it is
checked against or the word UNPRICED), fixtures both
polarities, and file it as a TECHNE method module under
modules/2026-09, committed locally, not pushed. It is the
resolving-power rule at the fixture layer and the module says
so, quoting both incidents.

ADDITION TWO — THE TWO-ROUTES QUALIFICATION. b345's (E3): two
routes sharing no code shared a quadrature engine. Carry the
qualification into the two-routes module by an appended block,
nothing edited: routes are stated as sharing no code AND no
engine, or the shared engine is named; a shared engine is a
shared error source and the act says what it therefore does not
certify.

(3) Standing: no grade is conferred by a seat; the ledger's
writer moves rows. h2 where the deposit left it; the wave stays
parked; nothing deposits.

STOP unchanged.
