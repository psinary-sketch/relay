CONCURRENCY: SOLO (research seat). FERRY_STANDING v2 by
reference. Anchors by the tool, never typed; extract-to-disk
for every read; the registration LOCKED before any write; gate
arms by content, not by address; no claim about h2, totality or
the roster; the instrument lane stays parked; the wave stays
parked; nothing deposits.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b368 - THE FRONT DOCUMENT RECONCILED, OR PRICED. The
executor's draft is ADOPTED, with four additions. Number not
claimed by any unclosed ferry.

ADDITION ONE - THE READ IS LIVE AND ITS REF IS NAMED: the
exclusion kernel is read at the ref its head actually sits on,
with the ref printed and pinned by ls-remote before any
classification; the eighteen absent names are re-derived from
the kernel's own files at that ref and NOT carried from b157 or
b367 - a figure quoted forward is a figure nobody re-measured,
and this act measures it.

ADDITION TWO - EACH NAME CLASSIFIED, one row each: PRESENT
(with its statement and its axiom profile read from a printed
profile); RETIRED (with the retirement ledger's own line
quoted); RENAMED (with the successor named and located); or
NEVER EXISTED (with the search recorded). No name is
classified from its own sound.

ADDITION THREE - THE REPAIR OR THE PRICE. If the front document
can be reconciled by APPEND-ONLY correction - a currency block
naming what is present, what is retired and where the
retirements are recorded, with no existing sentence edited -
write it, on the precedent the papers repo uses, and verify the
result names no absent terminal. If reconciliation would
require editing existing claims, do NOT edit: price it, route
it to the author, and say plainly that a public front document
currently exports names its kernel does not carry. In either
branch the kernel's .lean files are not touched.

ADDITION FOUR - THE DESK'S OWN FRESHNESS. This act's own
occasion was a stale desk item: the scaffold repair was carried
from the navigator's recall while the kernel had retired the
terminals and an act had banked it. File the rule - every desk
item names the file and date at which it was last confirmed,
and an item without one is re-verified before it is ordered -
as a TECHNE module beside the two arm species, committed
locally, not pushed, with b367 and b157 as its incidents. And
sweep the current desk once against it: for each item, the file
that confirms it is still owed, or the mark UNCONFIRMED. No
item is closed by this act; the sweep produces marks, not
verdicts.

CLOSING: the trails ledger updated through the writer with the
scaffold item's true state (retired at the kernel, the
restriction never a special rule, the front document the live
defect); the faces ledger only if a row moves; correspondence
rows; keys; both censuses; pins by ls-remote across every repo
read; hook and mirror where owed; the next ferry as DRAFT -
NAVIGATOR EDITS. The navigator's expectations: (F1) most of the
eighteen are RETIRED or RENAMED rather than never existing;
(F2) the reconciliation is append-only and does not need the
author. Each refutable by a printed classification. h2 where
the deposit left it.

paste ends (part 1 of 1)
