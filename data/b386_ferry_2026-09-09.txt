CONCURRENCY: SOLO (research seat). FERRY_STANDING v2 by
reference; anchors by the tool; extract-to-disk for every read;
registration LOCKED before any write, chained on every pre-lock
gate; the face is not widened mid-act for any reason — b385's
own rule, now standing. The instrument lane and the wave stay
parked; nothing deposits.

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

RULING (R15), the author's, ratified by this paste and
strikeable: ONE GUARD, ONE SOURCE. A guard has exactly one
tracked source of truth in each repository, and whatever
installs it reads from that source. A second copy is deleted,
or made a pointer to the source, or the repository is
reconfigured so the source is what runs — whichever the
repository's own mechanics allow. Divergence between two copies
of one guard is not a repair job; it is a topology defect, and
the topology is what gets fixed.

ACT b386 — THE GUARD MADE SINGLE-SOURCED. Number not claimed by
any unclosed ferry.

COMPONENT 1 — THE THREE OPTIONS, QUOTED FIRST: reproduce b385's
three routed options verbatim from its bank with their
locations, then state which of them implements (R15). Execute
that one. If none implements it, HALT and report — the author
does not rule on options a seat has not shown him.

COMPONENT 2 — THE REPAIR ITSELF, with the face written wide
enough to finish it so no mid-act widening is needed: the
single tracked source carrying the repaired line; the installer
reading from it, shown by running the installer and comparing
the installed bytes to the source's blob; the second copy
disposed of per (R15) with which disposal was chosen and why;
the guard exercised in both polarities in every rostered
repository afterward, with the failing gate cleared by the
repair and not by the arm.

COMPONENT 3 — THE INSTALLER'S SECOND DEFECT: the same tool
destroyed a prior backup's content and the backup was then
removed. Report what was lost, whether the lost content is
recoverable from any tracked blob, and repair the tool so it
does not overwrite a backup it did not create — or, if that is
outside this act's face, route it with the price. Say which.

COMPONENT 4 — TWO FILINGS: (i) the search lesson, minted — a
search for a rule uses the rule's own words, not the name a
reader gave it, with b383's clean absence and b385's location
as its two incidents, and the terms of both searches printed;
mechanized if a query-construction check is mechanizable, else
filed as judgement and not listed beside the mechanized ones;
a TECHNE module beside the arm species, local, not pushed.
(ii) the navigator's paraphrase of the reviewer rule corrected
on the record: the authority is the manifest's digest and
last-commit columns, not the manifest wholesale, and the
navigator's broader statement is marked as over-stated with
the rule's own words quoted beside it.

CLOSING: the trails ledger through its writer, with the guard
item closed only if Component 2's exercise passes and left open
with its reason otherwise; the four lists restated OPEN; the
citation question from b385's Component 4 restated as awaiting
the author, unmoved; correspondence rows; keys; both censuses;
pins by ls-remote through the roster; hook and mirror where
owed; the next ferry as DRAFT — NAVIGATOR EDITS. The
navigator's expectations: (F1) the disposal that implements
(R15) is deletion of the untracked-path copy with the installer
repointed; (F2) the destroyed backup's content is recoverable
from a tracked blob. Each refutable by a printed result. h2
where the deposit left it.

paste ends (part 1 of 1)
