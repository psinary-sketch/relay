**RECEIPT CONFIRMED — THE RE-PASTE, IN FULL. ACT b418 BANKED, PUSHED AND READ BACK.**

The act dropped once, halted once at the lock, and resumed on the author's re-paste. The registration was **LOCKED BEFORE ANY WRITE** at `52d873b0…ae31`: 8 gates read, 8 passing, 4 by digest, on the lock gate's second run. The refused first run and the first ferry file are kept as they ran.

---

## THE WALKER — Component 1

**The cause is a silent timeout at the tool's limit.** It is not a file kind, not a binary heuristic, and not an ignore pattern. The ignore hypothesis was tested first, as the resume order asked, and refuted: no ignore file names `data/`, and every missed file is NOT IGNORED by `git check-ignore --no-index`.

**The list the ferry orders.** These are the walker calls in the arc (b413–b417) that ran over relay's data records:

| act | call at | duration | files returned | hit the limit | ABSENT verdicts resting on it |
|---|---|---|---|---|---|
| b417 | 13:28:55Z | **20.04 s** | **13** | **yes** | **0** |

b413–b416 made no walker call over relay at all. The arc's six other relay-path calls read single files under `tools/` or one record, and none walked the directory.

**0 ABSENT verdicts rest on the truncated call.** b417's records name the walker only to say *it* missed something, which is a claim about the walker. So the order's CONFIRMED/MOVED verdicts are over an empty set, and I say so rather than report a pass. The call itself, re-run bounded in six glob parts that each finished under 0.6 s, reaches all **40** files that existed then, file for file with a Python walker. Its own result moved: 13 reported, 40 there.

**The guard:** relay `tools/walker_guard.py`. A call that reached its limit reads **INCOMPLETE and never as a count**, and its fixtures pass in both polarities. No search wrapper existed to hold that one line, so this is the smallest one that can, and the face says so. Its reach is limited: it guards what a record takes from a result, and it errs toward INCOMPLETE on batched calls.

**(N1):** its first half is withdrawn by the author. Its second half is **REFUTED, vacuously: no absent verdict existed that could move.**

---

## THE SLOTS — Components 2 and 5

**(R35) executed.** The universality sentence is narrowed in place at both of its occurrences to its n₂ theorem's own hypothesis, *connected reductive symmetry groups*. The original is quoted beside each, and one line says classes B and D keep their second component STIPULATED. Measured against the pin blob: 2 lines removed and 3 added. No tuple and no kernel figure moved, and class A is unmoved. AT_REST is updated by append only.

**The three example systems.** MATTER's table keys each system to its substrate class. COMPLEX_ANALYSIS's table keys each to its formation total and toolkit. The two Shannon rows name different objects: *Shannon entropy* in one and *Shannon channel* in the other. Gate 1b already says a tuple is decomposition-dependent. Counted, not repaired.

---

## THE KERNEL — Component 4, (N)

**VERDICT: OVER-BUDGET.** The cap of sixteen probes was reached, at about 31 of the 150 minutes. Every probe's source and printed profile is banked in `data/b418_n_attempt.txt`, and there are 0 `sorry` tokens across all sixteen.

**What was built axiom-free, each printed "does not depend on any axioms":**
- **S0:** a single-prime-factor base is a power of a prime.
- **Euclid's lemma**, gcd-free.
- **The remainder**, rebuilt from its own clean equation lemmas.
- **S1–S2:** the kernel's own sums rewritten as sums over the off-ball index, with the order of summation exchanged.
- **S3:** the progression count.
- **The S4 and S5 building blocks.**

**The one step not supplied:** `pow_pred`, the identity `r^j = r^(j−1)·r`. It failed in the last probe because this toolchain's `Nat.pow_succ` takes its base and exponent explicitly, and the term passed it bare. Every declaration that failed did so by depending on it, the general clause included.

**Next step:** `… .trans (Nat.pow_succ r (j - 1))`, then compile and read `smear_general`'s profile. **The theorem is not claimed, and 0 kernel files are touched.**

**(N2):** its premise, *BLOCKED at a finite-sum lemma*, is **REFUTED**: the finite-sum lemmas were supplied, and the verdict is not BLOCKED. Its conclusion, *priced as one lemma, not a Mathlib import*, is **MET in substance at a different step**: what remains is one lemma, and no import was needed.

---

## THE FIFTEEN — Component 3

**The keystone's sentence about forty-three** is kept byte-for-byte. One line below it says 43 = 2⁴ + 3³, and its class forbids no edit. **Registry row p2-35** was preserved verbatim in the act's record, then its two stale figures were removed rather than restated.

---

## THE TOOLS

**`b369_hygiene.py` is RETIRED** to `tools/retired/`, byte-for-byte; git recorded it as a pure rename. RETIRED.md carries its last verdict (FAILED), its writes, the repair-snapshot guard as what caught it, and the sixteen tools its move dates. The suite's first run failed two arms, both defects of the suite, and both were repaired with run 1 kept. The first searched for `sorry` in prose saying there was none. The second read the raw face.

Suite **81/81** pre-push, reconciling both ways. Mirror **CLEAN ON ALL THREE CLAUSES** at `bd8b1ba`. Censuses `0`; pins `0` hard-failing. Pins: relay `1dc9f72`→`f141d21`, PLACE-papers `09a1e80`→`bd8b1ba`, SIDE-global-section `ad7dba6`→`61a8abd`. Correspondence row **267** by its marker, key `the-ruling-carried` PASS, desk **19 swept / 8 closed**. **0 content lost. h2 where the deposit left it.**

---

## THE NEXT FERRY AS DRAFT — NAVIGATOR EDITS

> **CONCURRENCY: SOLO (research seat).** Standing text as b418, carrying A2 and (R26)–(R35). Orientation-layer citation: correspondence row 267 (by its marker), `data/b418_n_attempt.txt`, and `tools/walker_guard.py`. Both instrument lanes parked; the KERNEL lane open; the wave parked; nothing deposits.
>
> **ACT b419 — (N) FINISHED OR PRICED, AND THE LOCK'S MISMATCH NAMED.**
>
> **COMPONENT 1** — `pow_pred` repaired as b418 named it, the banked library recompiled, and `smear_general` and `cells_are_instances` read by their printed profiles. Verdicts PROVED / BLOCKED / OVER-BUDGET as b418's; on PROVED, one new `Core/` module, its lines appended to `AllPrints.lean`, and `AXIOM_PRINTS.txt` regenerated with the prior profile a true byte prefix.
> **COMPONENT 2** — the lock gate's ferry-scan gate against the scan's own *the reader rules*: the mismatch stated, and what changing either would cost, priced and not built.
> **COMPONENT 3** — the census of silent limits beyond the arc, re-read with batched calls told apart from single ones, and each remaining silent limit checked for a banked absence.
>
> The navigator's expectations: **(N1)** `smear_general` prints clean once `pow_pred` is repaired, with no further repair; **(N2)** at least one silent limit beyond the arc carries a banked absence. Each refutable by a printed result. **h2 where the deposit left it.**
