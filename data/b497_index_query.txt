### QUERY: EF_lit
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'ef_lit'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.

### QUERY: explicit formula
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'explicit formula'
==============================================================================
  ### THE RECORD HOLDS 1 INDEXED RESULT(S) ABOUT THIS OBJECT:

    key      : weil-criterion
    act      : b179
    result   : RH <==> sum over ALL PLACES of W_v(g*g-bar-sharp) <= 0 -- eq. (2) of 2006.13771v1
    grade    : EXTERNAL, quoted at content; credited by that work to A. Weil [33] following H. Yoshida [34]. ### NOT the corpus's result and NOT about one place   ### as its own act recorded it
    location : data/b179_enforcement_and_equivalence.txt

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.

### QUERY: axiom profile
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'axiom profile'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.

### QUERY: standard three
==============================================================================
BANKED-RESULT INDEX -- QUERY: 'standard three'
==============================================================================
  ### NO KEY.
  ### The query matched no DECLARED key or alias. No nearest string
  ### is offered: a nearest string is how a miss becomes a false hit.
  ### THIS IS NOT A FINDING THAT THE RECORD HOLDS NOTHING.

  ### REACH: ABSENCE FROM THE INDEX IS NOT ABSENCE FROM THE RECORD.
  ### The index holds what the scan surfaced and a human read at content.
  ### Querying the index is NOT reading the corpus.
  ### An entry is a POINTER, never a promotion; the grade is its own act's,
  ### and where the index and the act disagree, THE ACT WINS.

