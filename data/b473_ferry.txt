CONCURRENCY: SOLO (research seat; navigator Fable 5.1 and
auto-mode approval declared on the face). Standing text as
b472, carrying A2 and (R26)-(R82); the addendum slot per
(R50); scanned before sending, zero banned hits. Both
instrument lanes parked; the kernel lane OPENS to read the
log and closes at the act's end; the wave parked under
(R66); nothing deposits.

RULING (R83), the author's, ratified by this paste and
strikeable: IF (R82)'s condition holds, the vendored kernel
is named SIDE-explicit-formula, and its creation is its own
act after the fold, not this one; this act decides the
condition and nothing else about the import.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b473 - THE LOG READ TO ITS END, AND (R82) DECIDED.

COMPONENT 1 - THE BUILD. Read relay data/b471_zeta23_build.log
whole; commit it to relay as this act's evidence. Print: the
lake build exit status and the last forty lines; every
module that failed with its error, or NONE; the wall time
from the log's own timestamps. If the build failed, the
profiles were not produced; print that, and (R82) is VOID
for want of a run, not for a bad profile - say which.

COMPONENT 2 - THE PROFILES. For each of the seventeen
Challenge theorems and for EF_lit_zetaZeroConfig, EF_lit and
EF_lit_zeta: the #print axioms line verbatim from the log,
and beside it STANDARD THREE or the profile as printed. A
theorem absent from the log prints ABSENT, not assumed.
Then the verdict on (R82)'s condition: HOLDS if the three
explicit-formula declarations each read exactly [propext,
Classical.choice, Quot.sound]; VOID otherwise, with the
differing profile printed. The grade of zeta23's
formalization in the corpus's vocabulary follows: DERIVES
unconditional, or re-read.

COMPONENT 3 - THE COMPARATOR, IF CHEAP. If the repository's
scripts/verify-comparator.sh can run on this machine without
a network fetch it has not already made, run it on
comparator.json and print its last line; if it needs a
fetch, print NOT RUN and why. This is the source's own
check, not the corpus's, and it changes no grade.

CLOSING as the standing pattern, with Component 2's profile
table and the (R82) verdict printed in the closing message
itself; the span counted by tool; the four lists OPEN. The
navigator's expectations: (N1) the build exited 0; (N2) all
twenty profiles read the standard three and (R82) HOLDS;
(N3) the comparator is NOT RUN for want of a fetch. Each
refutable by a printed result. Nothing imported; no corpus
grade moves; h2 where the deposit left it.

paste ends (part 1 of 1)
