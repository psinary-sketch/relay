CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE REACH — the Epstein zeros on the line computed, both
windows extended with every prime and every representation
number to the crossing region, and the negative control
completed with its corroboration, one act. Scope: a
COMPUTATION on the explicit-formula instrument (which
transfers) and NOT on the source's inequality or the object's
decomposition (which do not, and the act says so with the
gamma factors quoted); NO claim about h2 or the roster; the
ζ window's extension is a CONTROL and its result at the new
width is reported as such; the eleven-prime constant replaced
by the full prime set with the scope written into the tool's
header; normalizations before counts; every quantity by two
routes sharing no code where two exist; the noise-floor gate
in the path; every reported number from a committed tool with
fixtures; needles from the extract file; stem sweep at
extended scope; hedge audit; registration sealed BEFORE any
new value (the prior act's ordering defect named at
registration as the thing this act does not repeat); no
counts predicted; the suite re-run after the push; run files
written once per path; nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
push anything ahead of origin, saying so if nothing is.

COMPONENT 1 — THE ZEROS: the Epstein function's zeros on the
line to the height the extended window's test functions
reach, by the corpus's own argument-principle census run at
the real part one half (the census's own caveat about
imposing the real part quoted, and answered: the off-line
zeros are already banked, so a line scan here counts what
lies ON it without pretending nothing lies off it), with an
independent second route (a library root-finder, or the
functional equation's symmetry) agreeing on every zero found;
the count and the height reported with the truncation
registered.

COMPONENT 2 — THE TWO WINDOWS, side by side, at cells from
the arc's thirteen out through the crossing region and past
it: ζ's places sum with every prime; the Epstein places sum
with every representation number through the finite side
built last act; each with its convergence and its sign
certified; the crossing cell located from the numbers.

COMPONENT 3 — THE CORROBORATION: the explicit formula closed
at every cell for BOTH functions with their own zero
libraries — ζ's on-line library, and the Epstein library
holding its on-line zeros AND its two banked off-line ones,
the off-line contributions computed as the complex terms they
are and reported separately. The formula must close for both;
a closure failure is reported at full prominence with the
link walked.

COMPONENT 4 — THE VERDICT, at exactly its scope: (SEES IT) —
a cell and a lawful f at which the Epstein places side takes
the forbidden sign, corroborated by the zero side with the
off-line terms accounting for it, while ζ keeps the permitted
sign at the same cell; then the entailment stated: on this
instrument, at this reach, the finite-instance places sum
discriminates a function whose hypothesis holds from one
whose hypothesis fails — the ζ window at this reach is a
passed test, and the arc's "could not have come out
otherwise" is restated as true of the library at the arc's
cells and false of the method at this reach; (DOES NOT SEE
IT) — the reason named; (PARTIAL) — sign crossed, corroboration
incomplete, what is missing typed. In every branch: nothing
about h2, the identity's truth, or the complete roster; the
ceiling printed.

CLOSING: the correspondence rows by the idempotent tool; every
result keyed; the lore gains "a constant is scope-bound and
its scope is written down" with its incident; the confinement
keystone's cross-reference line appended; M-2's row unchanged
under its cap; the seam's debt item 1 restated; the patent
clock restated; the in-flight register; the ledger census;
the bridge read named as next, and the finite-side exhaustion
module named as the kernel act after it; the wave's candidate
list updated. The navigator's expectation registered here:
SEES IT near the priced crossing, with ζ negative throughout
under the full prime set — refutable by a closure failure or
a permitted sign at every cell. Registration gate; index
queried; the-reach, epstein-zeros keyed. Components ordered.
THE SHADOW: expected nothing; say so.

Bank: data/b326_the_reach.txt. Deviation rule standing.

EXECUTION: ferry scan first; registration
satisfiability-checked, term-scanned, sealed before any
instrument runs; the extract step for every read; components
in order; full control suite, re-run after the push; pins by
ls-remote across all three repos; the hook if PLACE-papers is
touched; mirror if it moves. STOP.

FOOT: after this act the corpus's instrument has either said
no to a known counterexample while saying yes to ζ on the
same cells, or it has said why it cannot yet; the bridge read
follows, then the sealing module for the finite side; h2
stands exactly where the deposit left it; locks last.

paste ends (part 1 of 1)
