====================================================================================================
b109 -- THE APPORTIONMENT DERIVATION ACT -- THE RUN-REGISTRATION. BANKED
BEFORE ANY DERIVATION. Derivation-first at the recorded standard; every
sentence carries its grade; THE (I-differ) VERDICT IS FENCED (cited at its
grade, never re-interpreted); the grade-indexed convention (b108) governs
every status sentence; the boundary stays guarded; nothing about h2;
nothing deposits.
====================================================================================================
FERRY 2026-08-22 (part 1 of 1, receipt confirmed in full). Relay-only.

SOURCES (rule a, at content, QUOTED WHOLE AT THEIR GRADES BEFORE ANY WORK):
(S1) b38's APPORTIONMENT DEFINITION: "per mode n < N: w_n(f) := tr_n_raw(f)
  - E2_n(f) - s_n * resid_N(f)", with tr_n_raw the raw dilation trace of mode
  n (the b36 machinery), E2_n the mode-n part of the eps integral (the
  mode-diagonality EXACT, act 9), resid_N = Tr_raw_N - A - E2_N, and
  s_n = t(n)/sum_{m<N} t(m).
(S2) THE WARRANT: "CC's divergent subtraction is t(n)-WEIGHTED - Lemma 5.4's
  eps'(1+) = Sigma t(n) IS the N_I scalar's own assembly (Prop 5.5, at
  content): the delta_1-type divergent mass carries per-mode coefficient
  t(n). AT CITE, not fitted."
(S3) THE VOID-GATE: "W_plus + W_minus = A identically ... checked to 1e-10
  per cell, else VOID" -- verified at content at b107.
(S4) THE (W-split) VERDICT, verbatim: "f_inf = W+/A per cell: 0.692, 0.664,
  0.653, 0.626, 0.620, 0.609 -- a stable fraction, NAMED AS DATA, declining
  toward the t-share 0.6165 (the b35 even share). (W-even) did NOT land: the
  odd sector carries a genuine W_inf mass (W- = -0.55...-0.64) -- the
  E1-sector's positivity is NOT W_inf's own with eps a small correction.
  5 of 6 cells STABLE per the registered criterion; the a^2 = 2 cell marginal
  at 6.0%, flagged, not smoothed."
(S5) THE (I-differ) VERDICT, verbatim AND FENCED: "(I-differ) at every cell,
  per the registered map and no wider - with the obstruction FULLY DECOMPOSED
  into three computed pieces and nothing open: (i) the odd-sector W_inf mass
  (this act's number); (ii) the even-sector eps mass (the density question's
  content); (iii) the finite-level deficit (theorem-controlled, dies at the
  level limit). (I-match) did NOT land; the closure protocol's step one is
  NOT triggered." THE FENCE: this act cites that verdict and does NOT
  re-interpret it, does not extend it, and does not read it as bearing on
  anything at complete roster. The closed-equation reading is fenced; THE
  SPLIT STRUCTURE IS THE TARGET.
(S6) THE LAMBDA-SCALE CLOSED FORMS at their owners: t(n) =
  lambda(n)^2 xi_n(1)^2 / (1 - lambda(n)^2), both parities (b63: the odd
  series' machinery IS the even part's already-banked machinery), the even
  pins carrying their grades and THE ODD NO-PINS ASYMMETRY RIDING.
(S7) THE CERTIFIED ARCH COLUMN A at its grades; c1's E1 idempotent
  (real-explicit, b100).

(rule b) THE DERIVATION'S TARGET, FIXED HERE BEFORE ANY ATTEMPT --
THE SHARE STATEMENT: the E1-apportioned share of the arch column equals the
t-mass share determined by the pair structure. Written out:
    f_cell := W_plus / A   [the apportioned even share; b38's definition;
                            BENCH values at the declared pins]
    sigma_even := sum_{n even} t(n) / sum_n t(n)
                           [the t-mass even share; the closed forms' own
                            quantity; recorded as the b35 even share 0.6165,
                            BENCH]
  THE SHARE STATEMENT: f_cell -> sigma_even, with the recorded decline
  (0.692 -> 0.609 against 0.6165) AS THE PATTERN TO BE DERIVED, NOT ASSUMED.
  THE E1 SECTOR = THE EVEN SECTOR, per b38's own reading (its verdict names
  the t-share as "the b35 EVEN share" and contrasts the odd sector's mass).

(rule c) The three-grade rubric; the stop-law armed at any matching step; A
PARTIAL WITH A NAMED STEP IS THE SPECIES' HONEST SHAPE and is graded as such.
(rule d) The two components ordered (2 consumes 1).

REGISTERED PREDICTIONS, VERBATIM FROM THE FERRY:
(P1) THE SHARE STATEMENT FORMS from recorded ground alone -- the target
  equation stated with every constituent's owner and grade listed, the
  (I-differ) fence quoted beside it (what the statement is NOT); branch (b):
  the non-formable constituent named, first-class.
(P2) THE DERIVATION: the share structure derived at the closed-form layer --
  the honest prior PARTIAL-OR-BETTER with the pre-fixed candidates (the
  mass-ratio limit from the closed forms; the monotone decline from the
  recorded lambda behavior); values inherit their recorded grades (bench
  where pinned, the odd asymmetry said plainly); whatever lands at finite
  decidable core enters Core at the zero-axiom bar with correspondence rows,
  no blank cells; branch (a) whole: the apportionment's status upgrading
  DERIVED-AT-CLOSED-FORM-LAYER (the grade axis stated; bench values
  inherited, not promoted), the T-reading completing at its conditional
  grade, D2 upgrading per its cell, THE RE-RERUN STAGED; branch (b): the
  withholding step named exactly, the partial banked at its grade, the map
  standing.

*** TWO DISCLOSURES MADE IN ADVANCE (the two-seat discipline, fifth
exercise; both from the source read, neither to be produced later as though
discovered mid-derivation):
(D-i) THE RECORDED PATTERN IS NOT WHAT "DECLINING TOWARD" SUGGESTS, AND THE
  ACT MUST SAY SO PRECISELY BECAUSE THE PATTERN IS THIS ACT'S OWN TARGET.
  The six recorded f values against the t-share 0.6165 give deviations
  +0.0758, +0.0473, +0.0368, +0.0095, +0.0039, -0.0073: the sequence
  DECLINES MONOTONICALLY THROUGH ZERO AND ENDS BELOW THE t-SHARE. So within
  the recorded range f does not converge to sigma_even from above -- IT
  CROSSES IT, somewhere around a^2 ~ 10. This is an observation about THIS
  ACT'S TARGET QUANTITY and is therefore in scope; it is NOT a
  re-interpretation of b38's (W-split) or (I-differ) verdicts, which stand
  exactly as recorded and are fenced. A derivation of "f -> sigma_even"
  would have to account for the crossing (finite-size effect, or evidence
  against the limit); a derivation that ignored it would be fitting.
(D-ii) THE EXECUTOR'S EXPECTATION FOR P2: BRANCH (b), PARTIAL, with the
  withholding step expected to be AN ANALYTIC QUESTION ABOUT TWO NAMED
  QUANTITIES THAT ARE NOT t(n). Ground: b38's definition is linear in its
  three inputs, so the deviation of W_plus from sigma_even * A should reduce
  by exact algebra to a difference of two "even-share excesses" -- one for
  the raw dilation trace, one for the eps integral -- and the closed forms
  govern t(n), NOT tr_n_raw and NOT E2_n. If that reduction lands, the
  share statement's truth becomes a question about those two other objects'
  even-shares, and the closed-form layer alone cannot decide it. ***

THE PLAN: (C1) the statement formed, constituents and owners listed, the
fence quoted; (C2) the derivation attempted at the closed-form layer, the
exact algebra carried longhand, the verdict at its branch, the filings.
Kernel only if a finite decidable core appears -- and b38's own ITEM 4 NOTE
governs here too ("if no new apportionment arithmetic is decide-reachable
(the t(n) are not rational), the vanilla leg does NOT grow this act and the
report SAYS SO rather than forcing a module").

Bank: data/b109_apportionment_derivation.txt. Report:
reports/2026-08-22-apportionment-derivation.md. Deviation rule standing.
Divergence note (rule a): the two disclosures above, filed in advance.
====================================================================================================
