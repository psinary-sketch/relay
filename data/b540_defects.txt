    (a) ### **A DEPOSITED LINE CITED ONE OFF**: the face (READING (8)) and PATHS`s appended block (its (f) paragraph) cite the
        deposited monograph at `A_Place_to_Stand.md:1788`; the sentence "The formalization then did what formalization is for:
        it pinned the remaining content to a single goal state." stands at **:1787** (:1788 is blank; the quoted commutation
        begins at :1789). The E-2026-09-25-4 draft computed :1787 from the text and carries it correctly. The PATHS block took its
        one declared append, so it is not written again; the correction stands in this bank, the desk and the closing record.
    (b) ### **THE SENTENCE CENSUS REFUSED TWICE BEFORE IT RAN CLEAN**: a heading needle ("The corpus`s self-state (the h2 frame,
        aggregated)") also matched a DELTA sentence at `:267`, so that sentence met two typed rows; and a typed row for the R1/R2
        door row (`:47`) met no selected sentence, because its terminal is spelled `R2_conservationHypothesis_to_RH` and the
        Route-3 matcher reads `ConservationHypothesis`. The heading now matches by exact equality and the unselected row was
        dropped; nothing was written before the census ran clean.
    (c) ### **THE DESK`S (N5) READ A BOM AS A CHANGED LINE**: the scorer decoded each file`s prior blob with `utf-8`, keeping
        FINDINGS` and OPEN_TRAILS` byte-order mark, while it read the working file with `utf-8-sig`; the first line never matched
        and (N5) printed REFUTED for two files (OPEN_TRAILS not yet written). b536`s suite defect in a new place. Both sides now
        strip the mark; the desk was re-run.
    (d) ### **THE DELETE ARM MATCHED "form --"**: `G-DELETE-FREE`'s needle `rm -` found the letters "rm -" inside the string
        "the Weil form -- `h2_sign_iff_rh`" in this act's record tool (string literals are not stripped as prose). The needles are
        now regexes built from parts, and `rm` must not follow a letter; no tool of this act deletes anything.
    (e) ### **A BACKTICK POSSESSIVE HID A DECLARED PATH FROM THE WRITE-LIST ARM**: the face's (W) line for the terminal table reads
        "(CARRIED, SHARED, UNEDITED; the suite`s regeneration) | `relay/data/terminal_table.json`", and the possessive desynced
        `globs_of`'s backtick pairing, so `terminal_table.json` -- declared by name -- was read as uncovered. b537's face introduced
        the possessive and b539's carried it; neither met the miss, because the JSON did not change at those acts (the arm could not
        fire on that file there). The suite's parser now turns a possessive backtick into an apostrophe before pairing; it reads
        what the face declares and widens nothing.
