## THE ARCHIMEDEAN INSTRUMENT ARC, b314–b322 — THE FOLD

**Nine acts, 2026-09-03 to 2026-09-04.** A filings section: **no grade moves here, no act is re-verdicted, and nothing below is new mathematics.** Each entry carries its grade as *its own act* left it and its own scope sentence, and every quotation was checked verbatim against the act that **originated** it before this section was emitted.

### The nine

| act | what it is | grade, as its own act left it |
|---|---|---|
| **b314** | the prior fold filed, and the kernel rebuilt by something that inherited nothing | FILED — a filings act and a cold-clone certification; no grade moved |
| **b315** | the calibration read, and the rate re-derived under the source’s exponent | CORRECTED AT THE READING — a reading corrected, no banked number called wrong |
| **b316** | the archimedean instrument, built from the source’s Definition 4.4 and nothing else | BUILT — an instrument and its controls; one negative result, no first-level value |
| **b317** | the first archimedean trace computed on the object’s own space | MEASURED — the number exists; the registered prediction NOT confirmed |
| **b318** | the sign of the trace side forced, and the corpus’s window typed | MEASURED, AND ONE DEFINITIONAL READING — no unit adopted, no act re-verdicted |
| **b319** | the subspace by the source’s eigenvalue-one characterization, and the kernel-coverage repair | BUILT, AND ONE DEFECT DISCHARGED — a rank that holds still; no grade moved |
| **b320** | both sides of the source’s Theorem 1, computed and checked where it covers | HOLDS — the instrument certified against one theorem, at exactly that scope |
| **b321** | two further theorems as controls, and the window opened on lawful objects | HOLDS ON BOTH — three theorems now; the window reported and interpreted by nobody |
| **b322** | the unit’s membership residual given a rate, and the leg priced | UNDER-RESOLVED, WITH ITS PRICE — no unit adopted or replaced |

### Each with its own sentence, its scope, and its obstacle

- **b314 — the prior fold filed, and the kernel rebuilt by something that inherited nothing.** *Grade:* FILED — a filings act and a cold-clone certification; no grade moved.
  - Its own words: “…BYTE-FOR-BYTE EQUAL TO THE BANKED BLOB…”
  - **Scope, as its own act set it:** The clone certifies that the profile regenerates from source under a pinned toolchain. It certifies nothing about whether the profile covers the corpus, and the same act found that it does not.
  - **Obstacle, quoted:** “…MODULES SIT OUTSIDE THE CERTIFICATION FILE, ALL 25 ELABORATE, AND 91…”
- **b315 — the calibration read, and the rate re-derived under the source’s exponent.** *Grade:* CORRECTED AT THE READING — a reading corrected, no banked number called wrong.
  - Its own words: “…THE CALIBRATION FIXES A SIGN ONLY, AND THE ARCHIMEDEAN TERM IS DEFINED…”
  - **Scope, as its own act set it:** One name had been carrying two objects. The correction is to the reading and not to any value; every banked number survived and the wording did not.
  - **Obstacle, quoted:** “…UNDER THE SOURCE'S EXPONENT THE ENVELOPE BECOMES A CONSTANT…”
- **b316 — the archimedean instrument, built from the source’s Definition 4.4 and nothing else.** *Grade:* BUILT — an instrument and its controls; one negative result, no first-level value.
  - Its own words: “…THE INSTRUMENT EXISTS…”
  - **Scope, as its own act set it:** A computable truncation of `S(1,1)` at the source’s own parameters. It is a finite section and the act does not claim the source’s eigenspace has been computed.
  - **Obstacle, quoted:** “…b300's MEMBERSHIP IS NOT CONFIRMED…”
- **b317 — the first archimedean trace computed on the object’s own space.** *Grade:* MEASURED — the number exists; the registered prediction NOT confirmed.
  - Its own words: “…THE NUMBER EXISTS…”
  - **Scope, as its own act set it:** The number is a trace on a truncated space at thirteen banked cells. The prediction it was measured against rests on a five-link chain and one link is measured false.
  - **Obstacle, quoted:** “…A NUMBER THAT LANDS WHERE A BROKEN CHAIN SAID…”
- **b318 — the sign of the trace side forced, and the corpus’s window typed.** *Grade:* MEASURED, AND ONE DEFINITIONAL READING — no unit adopted, no act re-verdicted.
  - Its own words: “…THE SQUARE IS NONNEGATIVE AT EVERY CELL AND EVERY FRAME, AND THE SMEAR IS NOT…”
  - **Scope, as its own act set it:** The nonnegativity is arithmetic — a sum of squares of floats with no subtraction — and is kept apart from any claim about the positivity of the operator.
  - **Obstacle, quoted:** “…THE CORPUS'S WINDOW IS A CANDIDATE…”
- **b319 — the subspace by the source’s eigenvalue-one characterization, and the kernel-coverage repair.** *Grade:* BUILT, AND ONE DEFECT DISCHARGED — a rank that holds still; no grade moved.
  - Its own words: “…THE KERNEL-COVERAGE DEFECT IS DISCHARGED…”
  - **Scope, as its own act set it:** A rank that holds still is not convergence. The grid half of the reach is attained and the domain half is not, and the coverage repair proves nothing mathematical.
  - **Obstacle, quoted:** “…THE BAR THIS ACT SEALED IS DEFECTIVE…”
- **b320 — both sides of the source’s Theorem 1, computed and checked where it covers.** *Grade:* HOLDS — the instrument certified against one theorem, at exactly that scope.
  - Its own words: “…27 OF 27 FRAMES…”
  - **Scope, as its own act set it:** A control that holds certifies the INSTRUMENT and not the object. The sign of every margin is certified at every frame; the SIZE of none is.
  - **Obstacle, quoted:** “…FIRST REPORTED VERDICT WAS `FAILS`…”
- **b321 — two further theorems as controls, and the window opened on lawful objects.** *Grade:* HOLDS ON BOTH — three theorems now; the window reported and interpreted by nobody.
  - Its own words: “…THE PRIME SUM EXCEEDS THE MARGIN AT NO CELL OF THIS LADDER…”
  - **Scope, as its own act set it:** A finite window at a finite cutoff decides nothing global: 10000 ordinates, eleven primes, thirteen cells of one family, against a criterion that quantifies over every lawful `g`.
  - **Obstacle, quoted:** “…THAT COUNT IS FORCED BY THE SHAPE OF THE COMPUTATION AND IS NOT EVIDENCE…”
- **b322 — the unit’s membership residual given a rate, and the leg priced.** *Grade:* UNDER-RESOLVED, WITH ITS PRICE — no unit adopted or replaced.
  - Its own words: “…THE RESIDUAL FALLS, AT EVERY STEP OF THE DOMAIN LADDER…”
  - **Scope, as its own act set it:** A falling course at five frames is a falling course at five frames. The noise-floor gate refuses every step, so the DIRECTION is reported and the SIZE of no value on the ladder is.
  - **Obstacle, quoted:** “…SO THE VERDICT IS `UNDER-RESOLVED`, AND IT CARRIES ITS PRICE…”

### The arc’s corrections to its own readings

| act | what was corrected | **what did not move** |
|---|---|---|
| **b315** | one name had been carrying two objects — the `E2` of the calibration bracket is not the archimedean remainder | no banked value; the correction is to a reading |
| **b317** | the cancellation was read as the compression’s and is the test function’s — the same compression removes 98.6% of one bump and 55% of another | the trace values, which stand at their own frames |
| **b318** | the corpus’s window was being offered as the source’s test function; it is a candidate seed, not a candidate product | no act re-verdicted; b317’s numbers stand on b317’s cut |
| **b320** | b319’s prose named `69` as a dimension where its own table calls it a rank — filed against that bank, not edited into it | b319’s table, its numbers, and its verdict |
| **b321** | the window’s non-positive count was on its way to being reported as arithmetic evidence; the act established it was forced by construction before printing it | the numbers, which are exactly minus the zero side |
| **b322** | b316’s taper dichotomy was imported into a sealed bar as though it exhausted the possibilities | b316’s diagnostic, which measures what it measures |

### Sealed bars found defective, by the acts that sealed them

**This table is new to this fold and it is not a comfortable one.** Three times in nine acts, a bar was sealed before any value and the bar itself turned out to be wrong. **In no case was the sealed file edited.** A record whose registrations are only ever reported as having worked is a record that has stopped reading them.

| act | the bar | what was wrong with it | what the act did |
|---|---|---|---|
| **b319** | `(B3)`, the reach bar | it required the rank CONSTANT on BOTH axes, which on the domain axis is unsatisfiable by the nature of the object — a longer domain is a bigger space | reported the reach EMPTY under the bar as written, named the defect, and proposed the fix as a PROPOSAL for the next act rather than editing a sealed file |
| **b322** | `(B2)`, the edge diagnostic | the two labels it imported do not partition the possibilities — the taper smooths the discontinuity at the domain’s end and does not restore the mass beyond it | reported the verdict as the bar computes it and named the defect beside it |
| **b322** | `(B5)`, the branch rule | its branches are not mutually exclusive — two fired at once and the seal did not order them, so the runner’s `if/elif` chain imposed an ordering the registration never stated | took the WEAKER of the two branches, on the ground that between two branches a defective rule licenses equally an act may not help itself to the stronger one |

**And the thing worth more than the table:** in all three the defect was found *by running the sealed bar and reading what came back*, not by revising it. b319 reported an empty reach under a bar it knew to be unsatisfiable; b322 reported a verdict its own broken rule computed and then took the weaker of the two branches that rule licensed.

### The arc as one statement

At the grade these nine acts support, and no higher: **the finite-instance explicit formula is realized on lawful objects, on an instrument now certified against three of the source’s own theorems.** Its archimedean side splits, by the source’s Theorem 4.7, as the object’s compressed square plus a remainder — and **that remainder *is* the margin b320 measured**, confirmed along the domain ladder to within a residual falling by a factor of two to three at every step. **The prime sum sits inside the margin at every one of the thirteen cells.** The zero side’s sign is inherited from a library of zeros *on the critical line*, so **the window’s balance cannot come out positive**, and the act that opened it established that before it counted. The object’s archimedean unit is **in its space by derivation** and **under-resolved at bench by a stated factor**: its membership residual falls as `X^-0.5199`, which a second route sharing no code predicts from the vector’s own `1/x` decay, and reaching `0.01` would cost a factor of `3.104e+02` in domain. **The clause lives at totality, and this arc names the mechanism by which no finite instrument reaches it:** every quantity here is a truncation whose error the arc can now measure, and measuring it is what shows the distance.

**Scope, printed beside it.** This is a summary of nine acts at their own grades. **No theorem is proved here and none was proved by any act in the arc** — the source proved all three, and what the controls certify is the *instrument*, at exactly the scope of the control. **The window decides nothing:** 10000 ordinates, eleven primes, thirteen cells of one family, against a criterion that quantifies over every lawful `g`. **The SIZE of no margin on the domain axis is certified anywhere in this arc** — the noise-floor gate refused it in every act that measured it, and every one of those acts reported a direction instead. **Nothing about the identity, `h2`, or the complete roster follows from any of it.** **`M-2` is owed and no aggregation is stated.** **Nothing about the register sentence moves.**

### The lore this arc leaves, with the incident that bought each rule

**Mechanized** — a gate, a fixture or a tool enforces it:

- **Check normalizations before counts.** *Incident:* three sealed predictions in the prior arc each got the object right and the normalizing factor wrong; b313’s order put the check first and it found something.
- **A check evaluated at a zero cannot see a factor.** *Incident:* the archimedean control was passing at a point where both sides vanish.
- **One identity, two conventions, one file.** *Incident:* b313 built flipped COPIES of three instruments rather than editing the owners, and b322 could therefore still read both.
- **A computation that matters gets a second route sharing no code before its first number is banked.** *Incident:* b320 built the archimedean distribution by one route with good fixtures; TWO defects passed TWO rounds of those fixtures, the first producing a FAILS verdict and the second `1.9e9` in a data table.
- **Verify against the stored blob, not the working copy.** *Incident:* `core.autocrlf` makes a working file CRLF after any checkout while the blob stays LF; b309 lost a comparison to it and b319’s repair compared to the blob and passed.
- **A re-implementation that cannot reproduce its original is an overwrite.** *Incident:* b321’s channels are a re-implementation of the atlas’s; fixture (i) feeds them the atlas’s OWN bump and requires every channel back to `1.006e-16`.
- **A run file may not carry a claim the act declined.** *Incident:* b322’s gate caught that the run alone printed `DIFFERENT VECTORS` with no sign the act had taken the weaker reading; the run was rewritten, not the gate.
- **A gate arm that greps raw source fires on the act’s own sentence saying the thing was not done.** *Incident:* b317’s `G-NOUNIT` fired on its own docstring; every gate since strips comments and strings first.

**Judgement** — no mechanism enforces it and a seat must apply it:

- **Two quadratures over one integrand are a limit, not a corroboration — and must be called one before the value exists.** *Incident:* b322 declared it in `(B1d)`: there is exactly one implementation of the source’s (84) in this corpus, so `9.1e-08` confirms the quadrature and not the remainder.
- **A question is under-resolved, not open, when the candidates sit closer together than the instrument’s distance from the answer — and the price is the ratio.** *Incident:* b321’s identity control HELD and still could not tell two exponent copies apart; b322 applied the rule deliberately and priced its own question.
- **When a defective rule licenses two branches equally, take the weaker.** *Incident:* b322’s `(B5)` fired twice at once; the stronger branch was a positive claim about the object and the weaker was a claim about the resolution.
- **A dichotomy that is not a partition cannot be read either way.** *Incident:* b322’s `(B2)` imported two labels that leave the truncation unexpressible.
- **A ranker does not know what causes what.** *Incident:* b322’s `(B6)` named `CONDITION TWO` as the first differing constituent; the ladder had already shown it is what `THE DOMAIN` produces.
- **The ceiling is where the evaluator fails, not the last place it works.** *Incident:* carried from the prior arc and unchanged by this one.

### The instrument suite, and what each piece catches

| tool | what it catches | the incident that put it there |
|---|---|---|
| `ferry_scan.py` | struck clauses and banned stems in an order or a registration, before the act runs | b319 sealed a registration with a banned stem in it and had to re-seal; b321 caught two stems in its own prose before the push. |
| `reg_seal.py` + `reg_satisfiable.py` | a registration edited after a value was seen, and a set of caps whose demands exceed them | every act since b320 carries its seal hash as a literal in its own gate suite, so a re-seal cannot be hidden. |
| `needle_pull.py` | a claim in a bank with no anchor in an emitting file | six acts lost runs to anchors that wrapped mid-phrase; the practice is now to verify every anchor against its file BEFORE the gate is written. |
| `noise_floor.py` | a point verdict taken from an axis that has not resolved | it has REFUSED on the domain axis in every act from b317 to b322, and every one of those acts reported a direction and not a size. |
| `hedge_audit.py` | a graded hedge — a claim softened without its grade moving | run over every file each act writes. |
| `b306_stem_scope.py` | a stem in a shared target outside the act’s own files | two pre-existing hits in `CORRESPONDENCE.md` rows 2 and 101 are whitelisted and everything else is a failure. |
| `b307_handoff_census.py` | an act or a work-order missing from the ledger | it counts NAMES in ONE ledger and says so in its own output. |
| `b315_coverage_gate.py` | a `#print axioms` target compiled but never certified | it fired for sixteen acts and b319 discharged it: 475 → 566 prints, the old profile a literal byte prefix of the new one, 0 axiom-bearing terminals among the 91 newly certified. |
| `mirror_verify.py` | a mirror archive that is internally consistent and stale | at b182 a build verified CLEAN on both then-existing clauses at 33 files WITHOUT THE FILE IN IT; clause 3 was added because it is the only one that can see that. |
| the pre-push hook | a push to `main` from a branch that is not `push-*` or `repair-*`, a HELD ancestry, or a HELD carrier artifact by name | installed in all three repositories at b304; `.git/hooks/` is untracked, so it must be reinstalled after any clone. |

**The archimedean instrument’s certifications, and its limits.** It is certified against three of the source’s theorems:

| theorem | where checked | result |
|---|---|---|
| **Theorem 1** (b320) | the three cells its support condition covers, `a = 1.3, 1.35, 1.41` | holds, margins `+0.271444634`, `+0.285510313`, `+0.309777648`, and at **27 of 27** instrument frames |
| **Theorem 4.7** (b321) | the same three cells | holds as an **equality**; the residual along the domain ladder falls `0.896557, 0.306328, 0.112555, 0.047182, 0.023224` |
| **The explicit formula (148)** (b321) | **all thirteen cells** | holds, residuals `2.2e-09` to `3.6e-05` against the atlas’s own sealed `TOL = 1e-03` |

**And its limits, stated as measurements.** The domain axis has never converged: its rate is now known — the instrument’s own residual falls as `X^-1.324` while the unit’s falls as `X^-0.520`, a ratio of `0.393`. **The resolving power has been priced twice**, once for the exponent question and once for the membership question, and both prices are beyond what the instrument reaches.

### The desk

| item | state | what it needs |
|---|---|---|
| **`M-2`** | `(SPECIFIED-NOT-STATED)`, unchanged across all nine acts, under b310’s cap | the space it may be stated in has narrowed but no act in this arc states it. |
| **The object’s three conditions** | each typed — a premise, a ruling, a construction | **a condition discharged is not the object constructed.** The archimedean leg is derived and under-resolved at bench. |
| **The archimedean leg** | IN by b300’s derivation at `DERIVES-on-IMPORTS`; UNDER-RESOLVED at bench | the residual falls as `X^-0.5199` and would need `X = 3.973e+04` to reach `0.01` — a factor of `3.104e+02` beyond the domain reached. |
| **The exponent** | settled by b313’s READING of three source sites; UNDER-RESOLVED at bench | b321’s identity control held and the two copies were `0.000981` to `0.003994` apart where the instrument sat `0.018808` to `0.023224` from the answer. |
| **`W-ORD-PHI-MU-L2`** | OPEN | no owner states `phi_mu` in `L^2(R)`, and the instrument cannot see the question because a finite array is square-summable whatever lies behind it. |
| **`W-ORD-ARCH-MEMBERSHIP`** | OPEN | priced by b322 and not closed. |
| **`W-ORD-WINDOW-CLASS`** | OPEN | b321 opened the window and decided no class. |
| **The window ruling, applied at the instrument** | the balance is computed and interpreted by nobody | its sign is inherited from a library of zeros on the line, so it cannot come out positive and the act said so before counting. |
| **The absolute-path order** | standing | carried unchanged. |
| **The artifact-count counter** | lexical, and it produced a FALSE POSITIVE on a statement of measured fact in three consecutive acts | **its repair or its retirement is the author’s**, not a seat’s; acts rephrase around it. |
| **The keystone re-read** | NAMED AS NEXT | the order sequences it after this fold. |
| **The reconciliation wave** | the author’s | the arc’s results are of the kind a wave would carry; no seat starts one. |
| **The seam’s debt, item 1** | STILL UNPAID | restated, not discharged. |
| **The patent lane** | carried on the patent seat’s report | **unconfirmed on this seat’s record.** |

### **h2 UNCHANGED. NOTHING PROMOTED. NOTHING DEPOSITS.**

