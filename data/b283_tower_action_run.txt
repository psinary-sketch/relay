====================================================================================================
b283 -- THE TOWER'S ACTION. ### IS `iota` MULTIPLICATION BY `p`?
====================================================================================================
### DECIDED BY DEFINITIONS. ### **THAT BOTH INDEX FORMULAS CARRY A FACTOR `p` IS A
### RESEMBLANCE AND IS TREATED AS NO EVIDENCE (falsifier I1).**

  ---- CELL (p,n) = (2,1):  N = 4 ----
    (1) chart shift x'' - x lies in p^n Z, exactly       : YES, all 8 (m,j)
    (2) iota f = f on every refined cell                  : YES  (0 mismatches)
    (3) I2 SUPPORT TEST -- does iota move support?        : NO -- host points all refine a level-n point
        model U^n drives EVERY index into ball_n          : True
        iota carries an off-ball index into ball_{n+1}    : 0  NEVER
    (4) V_inv classes rebuilt from b10's x ~ px match the
        corpus builder                                    : YES
    noise-floor gate                                     : EXACT

  ---- CELL (p,n) = (2,2):  N = 16 ----
    (1) chart shift x'' - x lies in p^n Z, exactly       : YES, all 32 (m,j)
    (2) iota f = f on every refined cell                  : YES  (0 mismatches)
    (3) I2 SUPPORT TEST -- does iota move support?        : NO -- host points all refine a level-n point
        model U^n drives EVERY index into ball_n          : True
        iota carries an off-ball index into ball_{n+1}    : 0  NEVER
    (4) V_inv classes rebuilt from b10's x ~ px match the
        corpus builder                                    : YES
    noise-floor gate                                     : EXACT

  ---- CELL (p,n) = (3,1):  N = 9 ----
    (1) chart shift x'' - x lies in p^n Z, exactly       : YES, all 27 (m,j)
    (2) iota f = f on every refined cell                  : YES  (0 mismatches)
    (3) I2 SUPPORT TEST -- does iota move support?        : NO -- host points all refine a level-n point
        model U^n drives EVERY index into ball_n          : True
        iota carries an off-ball index into ball_{n+1}    : 0  NEVER
    (4) V_inv classes rebuilt from b10's x ~ px match the
        corpus builder                                    : YES
    noise-floor gate                                     : EXACT

  ---- CELL (p,n) = (3,2):  N = 81 ----
    (1) chart shift x'' - x lies in p^n Z, exactly       : YES, all 243 (m,j)
    (2) iota f = f on every refined cell                  : YES  (0 mismatches)
    (3) I2 SUPPORT TEST -- does iota move support?        : NO -- host points all refine a level-n point
        model U^n drives EVERY index into ball_n          : True
        iota carries an off-ball index into ball_{n+1}    : 0  NEVER
    (4) V_inv classes rebuilt from b10's x ~ px match the
        corpus builder                                    : YES
    noise-floor gate                                     : EXACT

  ---- CELL (p,n) = (5,1):  N = 25 ----
    (1) chart shift x'' - x lies in p^n Z, exactly       : YES, all 125 (m,j)
    (2) iota f = f on every refined cell                  : YES  (0 mismatches)
    (3) I2 SUPPORT TEST -- does iota move support?        : NO -- host points all refine a level-n point
        model U^n drives EVERY index into ball_n          : True
        iota carries an off-ball index into ball_{n+1}    : 0  NEVER
    (4) V_inv classes rebuilt from b10's x ~ px match the
        corpus builder                                    : YES
    noise-floor gate                                     : EXACT

  ---- CELL (p,n) = (7,1):  N = 49 ----
    (1) chart shift x'' - x lies in p^n Z, exactly       : YES, all 343 (m,j)
    (2) iota f = f on every refined cell                  : YES  (0 mismatches)
    (3) I2 SUPPORT TEST -- does iota move support?        : NO -- host points all refine a level-n point
        model U^n drives EVERY index into ball_n          : True
        iota carries an off-ball index into ball_{n+1}    : 0  NEVER
    (4) V_inv classes rebuilt from b10's x ~ px match the
        corpus builder                                    : YES
    noise-floor gate                                     : EXACT

====================================================================================================
### ALL CELLS: PASS
### ### **THE READING: ### `iota` REFINES THE CHART AND CHANGES NO FUNCTION AND MOVES
### ### NO SUPPORT. ### MULTIPLICATION BY `p` ESCAPES ITS LEVEL AND, ITERATED `n` TIMES,
### ### DRIVES EVERY INDEX INTO THE BALL. ### THEY ARE NOT THE SAME MAP.**
====================================================================================================
