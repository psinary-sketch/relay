CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE DISCRIMINATING FAMILY — derive the condition under which a
lawful test function's four-term sum at an off-line zero
quadruple is negative, construct a seed meeting it aimed at the
Epstein function's first off-line zero, and run the negative
control with it, one act. Scope: a DERIVATION, a CONSTRUCTION,
and a COMPUTATION on the explicit-formula instrument; the
criterion evaluated as the source states it, with pole terms
handled as the source handles them and quoted; NO claim about
ζ, h2, or the roster beyond the control's own scope; the ζ
control under the same seed must keep the permitted sign, and
a ζ flip is reported as the act's most prominent finding and
walked link by link before anything else; normalizations
before counts; every quantity by two routes sharing no code;
the Epstein zero library (146 on, 17 off) and ζ's used for
corroboration only — the places side is computed with no
zero; the noise-floor gate in the path; every reported number
from a committed tool with fixtures; needles from the extract
file; stem sweep at extended scope; hedge audit; registration
sealed before any instrument runs, no counts predicted; the
suite re-run after the push; run files written once per path;
nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope
(now including the faces ledger); push anything ahead of
origin, saying so if nothing is.

COMPONENT 1 — THE CONDITION, derived: for f = g ⋆ g^♯ in the
source's convention and a zero ρ = β + iγ off the line with its
three partners, write the four-term sum in terms of the seed's
transform at ρ and at 1 − ρ̄; then specialize to a real even
seed and show what the sum reduces to (the navigator's
assertion, to be derived or refuted: the squared modulus times
the cosine of twice the phase, negative exactly when the phase
exceeds forty-five degrees); then state what a seed needs —
phase, or an odd component, or both — and derive where an even
seed's phase comes from. Verify the derivation against b326's
own seeds: their phases at the first off-line zero must come
out below the threshold, exactly as their positive sums
require.

COMPONENT 2 — THE CONSTRUCTION: two seeds, one per route —
an even seed with width chosen from Component 1's derivation
to exceed the phase threshold at the first off-line zero
(β = 0.953260, γ = 16.290216, quoted from the library), and a
seed with an odd component whose phase is set by the
derivation. For each: lawfulness checked (the source's
Definition 3.1 on f, and the pole conditions as the criterion
requires — stated, not assumed), the transform computed by two
routes, the phase at the off-line zero measured and shown to
exceed the threshold. A seed that does not reach the
threshold is reported as such, not tuned after the fact.

COMPONENT 3 — THE CONTROL RUN: the Epstein places side under
each seed with every representation number, at cells to the
reach the seed's support requires; the ζ places side under the
same seed with every prime; the explicit formula closed for
both with their own libraries, the off-line terms reported
separately. Verdicts: (SEES IT) — a cell where the Epstein
places side alone takes the forbidden sign, corroborated by
the zero side with the off-line quadruple accounting for it,
while ζ keeps the permitted sign at the same cell under the
same seed; (DOES NOT SEE IT) — the reason named from the
numbers; (ζ FLIPS) — at full prominence, walked link by link,
since the theorem forbids it and a flip is a defect in the
chain until proven otherwise.

COMPONENT 4 — THE ENTAILMENT, at exactly its scope: on
(SEES IT), the finite-instance places sum computed without any
zero distinguishes a function whose hypothesis holds from one
whose hypothesis fails, on this instrument, at this reach, for
this family — the ζ window becomes a passed test for this
family; and the family's construction is filed as the first
enumerated way the clause could fail, on the faces ledger as
an owed-bridge row now partly paid. Nothing about totality.

CLOSING: the correspondence rows by the idempotent tool; every
result keyed; the faces ledger's Epstein row and discriminating-
family trail updated by the writer; the lore gains the phase
condition if derived; M-2's row unchanged under its cap; the
seam's debt item 1 restated; the patent clock restated; the
in-flight register; the census; the sealing module and the
TECHNE extraction named as next; the fold noted as
accumulating. The navigator's expectations registered here:
(F1) the even-seed reduction derives as asserted; (F2) an even
seed of sufficient width reaches the threshold without an odd
component; (F3) SEES IT with ζ holding — each refutable by the
derivation or a printed sign. Registration gate; index queried;
discriminating-family keyed. Components ordered. THE SHADOW:
expected nothing; say so.

Bank: data/b328_the_discriminating_family.txt. Deviation rule
standing.

EXECUTION: ferry scan first; registration sealed before any
instrument runs; the extract step for every read; components
in order; full control suite, re-run after the push; pins by
ls-remote across all three repos; the hook if PLACE-papers is
touched; mirror if it moves. STOP.

FOOT: after this act the corpus's instrument has said no to a
known counterexample while saying yes to ζ under one seed, or
the derivation says exactly why it cannot with this family
either; the first spade is in the well either way; h2 stands
exactly where the deposit left it; locks last.

paste ends (part 1 of 1)
