CONCURRENCY: SOLO (research seat). FERRY_STANDING v2 by
reference, carrying A2; anchors by the tool, tables by heading;
extract-to-disk for every read; registration LOCKED before any
write; the face written from a survey; the write list built as
KINDS; every arm names its reference and is taken at the same
moment as what it measures; every prose-reading arm
assertion-level with a control and folded free of markup; every
inherited arm re-pointed at this act before it is trusted, and
no arm demands a number a previous act happened to produce;
every write encodes before it opens; no in-place repair
searches a region this act itself wrote; an act's own run
record is read from its run; every search for a class is by
DESCRIPTION and under BOTH numberings. Both lanes stay parked;
the wave stays parked; nothing deposits.

FERRY -> CLAUDE CODE -- paste begins (part 1 of 1; confirm
receipt-in-full)

RULING (R25), the author's, ratified by this paste and
strikeable: THE TWO CLASS NUMBERINGS ARE BOTH KEPT, AND EVERY
DOCUMENT DECLARES WHICH IT USES. Disposition (B): a head-note
line per document naming its scheme, added where absent,
original heads preserved; no symbol renumbered anywhere; the
class matcher reads both schemes and prints which it matched.
A document whose scheme cannot be read from its own text is
marked SCHEME UNDECLARED and routed, not guessed.

ACT b409 -- THE OBSTACLES DISSOLVED WHERE THE RECORD ALLOWS.
The executor's draft is ADOPTED in its subjects and CONVERTED
from prices to actions where an action needs no ruling beyond
(R25). Number not claimed by any unclosed ferry.

COMPONENT 1 -- THE NUMBERING, MEASURED AND DECLARED. The draft's
sweep as written -- every class-symbol use, its scheme decided
from its document's own text, UNDECIDABLE where the text does
not say -- and then (R25) executed on the result: head-note
declarations added, the both-scheme matcher built with fixtures
in both polarities, and the count of SCHEME UNDECLARED
documents routed. The draft's Component 2 prices are printed
beside the execution as what (A) would have cost, so the
choice is on the record.

COMPONENT 2 -- THE GLOBAL CLASS, EXAMINED BY DESCRIPTION. First,
the two hits b408 filed as artefacts -- the output-stage classes
assembling without crossing a dark interface -- read whole under
their own scheme: what pair of classes they actually name, and
whether either is the modular relation between the two sides.
Then the search re-run by DESCRIPTION under both numberings,
with the positive control that the archimedean class's one
known examination is found. Verdict on examination: EXAMINED
(quoted, with what it concluded) / ABSENT UNDER BOTH SCHEMES
(the search printed). Then, if absent, the examination itself,
as a READING: from the monograph's own definition of the global
class, does the modular relation carry information about the
location of an individual zero, or does it -- like the
functional equation -- preserve the critical line and so
distinguish nothing? Quote what decides it. Verdicts: BRIGHT /
DARK / UNDECIDABLE FROM THE RECORD, with the deciding sentence.
No claim that a bright verdict opens anything; a bright verdict
would be a channel the corollary's existential could range over
and nothing more.

COMPONENT 3 -- THE LEMMA RELATIVIZED, WRITTEN OR SHOWN NOT TO.
State the Sieve Ceiling Lemma for a proof of H => for all x P(x): the
hypotheses carried through, the conclusion H => (P on a
density-one subset of each class), the element still
unreached. Then examine the original's proof at content and
decide whether it relativizes -- whether each step survives the
added hypothesis unchanged. Verdicts: RELATIVIZES (the restated
lemma written in full beside the original, additively,
with the step-by-step check printed, and its grade the
original's) / DOES NOT (the first step that fails quoted, at
full prominence) / UNDECIDABLE (the step whose behaviour under
H the record cannot read). If it relativizes: state what it
says of the corpus's reduction as b408 classified it -- the four
imported premises as H -- and file that as the row's organizing
citation, the refusal beside it, no bridge typed.

COMPONENT 4 -- THE SPINE, PROPOSED AND NOT BUILT, as the draft's
Addition Two states it: three mathematics-facing arms, each
named with the incident it would have caught, each with the
decision the author would have to make for it to be writable.

COMPONENT 5 -- THE ROW, FROZEN. Row U1 receives no entry from
this act and is marked, through its writer, as a register
complete at six until an act changes what it can say about the
object. Reason: the seat's own count -- zero statements about
the object in six acts.

CLOSING as the draft states it, with Component 2's verdict and
Component 3's verdict printed in the closing message itself;
the four-dresses law restated as ROUTED for the author's naming.
The navigator's expectations: (N1) a majority of symbol uses
CAN be assigned a scheme from their own document, because most
documents cite the monograph's table; (N2) the two artefact hits
name the local and order classes and neither is the modular
relation; (N3) the global class is ABSENT under both schemes and
reads DARK on the monograph's own definition, for the same
reason the functional equation does; (N4) the lemma
RELATIVIZES. Each refutable by a printed result. h2 where the
deposit left it.

paste ends (part 1 of 1)
