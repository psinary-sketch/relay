====================================================================================================
b76 -- THE RADIALIZATION ACT -- THE RUN-REGISTRATION. BANKED BEFORE ANY
CONSTRUCTION. This act RUNS the staged radialization registration
(data/radialization_construction_STAGED_2026-08-21.txt) EXACTLY AS FILED --
divergences resolve in its favor, recorded. Everything finite-side and exact;
THE S1 FIREWALL ABSOLUTE (the odd ladder's archimedean use is step three's
territory; nothing archimedean computed here); THE CLASSICAL THETA UNCLAIMED --
the act-6 antipode refutation CITED AT THE THETA CLIENT'S HEAD: the literal
s -> 1-s adjointness was refuted and is never re-walked; the theta client's
content is the CHART-SIDE identity only.
====================================================================================================
FERRY 2026-08-21 (part 1 of 1, receipt confirmed). Relay-only. NO SIGN SENTENCE.
h2 UNCHANGED BY CONSTRUCTION; the range law governs every sentence. NOTHING
DEPOSITS.

SOURCES (rule a/b, at content, at pins): the staged registration (the merged
object -- step A + N5's dictionary -- with its two clients and the b73/b75 plane
question); the banked g-family and valuation structure (rows 29, 41, 45, 48, 50);
THE SHELL MODEL'S BANK READ AT CONTENT THIS ACT (b61_theta_unit.txt): the
truncated shift T with all weights 1 and ONE deficient weight at the ball
boundary; tr T^k = 0 for 1 <= k <= 2n at every banked shell size 3, 5, 7, 9
(model_trace_silence); T*T = Id - P_top, TT* = Id - P_bottom -- defect EXACTLY
(1,1) at every banked size (defect_gate_instances); the seed-defect address v =
n-1 (seed_boundary_address: the imported seed's support row 2^{n-1} has 2-adic
valuation exactly n-1); N5 the operator-level defect-vector identity, its
address decided, "the vector awaits the operator's banked finite form" (V's
finite form banked at b66); the plane's radial coordinate at index grade (row
53); the banked traces q / q(1+i) (rows 35-36).

REGISTERED PREDICTIONS (the ferry's, verbatim, before any construction):
  (P1) THE MAP EXISTS at exact grade on the banked cells: the shell-summing map
  R from the chart's g-family to the shell model, by valuation, reproducing the
  boundary line's shell address (the row 2^{n-1} seed to the shell v = n-1) and
  the banked boundary-index data at every banked shell size.
  (P2) THE DEFECT DICTIONARY (N5's client): R intertwines the chart's p-term
  transfer with the shell model's boundary-weight shift up to the banked
  normalization -- decided at the banked levels; the radial-compression residue
  of row 50 closes or sharpens to its exact remaining step.
  (P3) THE THETA CLIENT'S FINITE HALF (step A's finite content): the radialized
  g-family assembles the chart theta-object at finite level with its
  quarter-turn symmetry -- the chart-side identity only; no classical
  identification claimed; a landing stages the bridge's continuation, so marked.
  (P4) THE PLANE QUESTION: R factors through the plane's radial coordinate,
  decided at index grade -- the radialization and the plane meeting as the
  staged files asked.

----------------------------------------------------------------------------------------------------
THE CONSTRUCTION AND DERIVATIONS, LONGHAND BEFORE ANY RUN:

(D1 = P1) THE MAP: R(f)(k) = sum over the shell {m : v_p(m) = k} of f(m) -- the
  shell-summing map by valuation, from the chart Z/N (N = q^2) onto the shell
  model's 2n+1 cells (shells v = 0..2n-1 and the zero position as the top cell)
  -- sizes 3, 5, 7, 9 at n = 1..4, both parities (the shell COUNT depends on n
  alone; the shell sizes are N(1-1/p)/p^v... the counts per shell decided). On
  the g-family: g_{a,t} (a /= 0) is supported on one coset a + qZ, whose
  positions all carry v(a) -- R(g_{a,t}) = (sum_b omega^{tb}) e_{v(a)} = q
  e_{v(a)} if q | t, ELSE 0 (the root-of-unity column sum): R KILLS THE TWISTED
  ROWS and carries the t = 0 rows to shell lines. THE SEED: the row a = 2^{n-1}
  at place-2 level n has v = n-1 -- R(seed row) = q e_{n-1}, THE DEFICIENT
  WEIGHT'S OWN ADDRESS (b61's seed_boundary_address, now derived from the map's
  side). THE VALUATION AS A GCD LAW (the decidable spine): v_p(m) on Z/N is
  carried by Nat.gcd m N = p^{v(m)} -- every shell statement below is a gcd
  statement. THE BOUNDARY-INDEX DATA re-derived from R's side: the
  p-multiplication image misses the primitive shell entirely (no p*m is
  coprime; one deficiency) and fixes the zero cell (the terminal; one) -- the
  (1,1) of b61's decided gates, re-derived.
(D2 = P2) THE INTERTWINING, exact on positions: R(V delta_m) = R(delta_{pm}) =
  e_{v(pm)} = shift(e_{v(m)}) -- as the gcd law: gcd(p*m mod N, N) =
  min(p * gcd(m, N), N), decidable over the FULL ranges at N = 4, 16, 64, 256.
  THE BANKED NORMALIZATION (where "up to" lives, stated exactly): the RAW
  transfer intertwines with the ALL-WEIGHTS-1 shift exactly; the COMPRESSED
  transfer (the corpus's scaling, compressed to Son -- vectors vanishing on the
  ball v >= n) differs from the raw shift at EXACTLY ONE transition: the shell
  that doubles into the ball is v = n-1 ALONE ({v < n : v+1 >= n} = {n-1}) --
  THE DEFICIENT WEIGHT'S ADDRESS IS THE BALL-BOUNDARY TRANSITION, decided as a
  filter identity (positions outside the ball whose double is ball-or-zero =
  the shell v = n-1) at the banked levels. THE ROW-50 RESIDUE: with R built,
  the dictionary CLOSES AT THE OPERATOR/INDEX GRADE -- R is the dictionary; the
  exit-space's twisted members lie in ker R (D1), so the radial compression of
  the exit-space is EXACTLY the boundary line's shell image, q e_{n-1} |->
  q e_n; the deficiency address = the seed address = the ball boundary, one
  coincidence decided from both sides (b61 + this act). WHAT REMAINS of N5,
  the exact remaining step, named: the VECTOR-level identity -- the defect
  vector of the compressed transfer in the chart (V's banked finite form, b66)
  against the R-preimage of the model's boundary cell at the deficient shell;
  all its pieces are now banked; NOT run here (not registered).
(D3 = P3) THE CHART THETA-OBJECT, chart-side only (the act-6 caution at the
  head): Theta_chart = sum_m zeta_N^{m^2} delta_m; its trace pairing with the
  quarter-turn is DEFINITIONAL -- the diagonal of the S-kernel zeta^{mm'} at
  m' = m is zeta^{m^2}, so sum_m zeta^{m^2} = tr S, the banked values q(1+i)
  at place 2 and q at odd (rows 35-36, banked grade). THE RADIALIZED ASSEMBLY,
  computed longhand for the decides: the theta sum grouped by shells --
  (2,1) N=4, in Z[i] pairs: v0 (m = 1, 3): 2 zeta_4 = (0,2); v1 (m = 2):
    zeta^0 = (1,0); v2 (m = 0): (1,0); total (2,2) = q(1+i) at q = 2. THE
    PRIMITIVE SHELL CARRIES 2i HERE (q = 2 the boundary cell's exception).
  (3,1) N=9, in Z[zeta_9] six-coords (x^6 = -1-x^3): v0 (six units): squares
    1,4,7 twice each: 2(x + x^4 + x^7) = 0 (x^7 = -x-x^4) -- THE PRIMITIVE
    SHELL IS SILENT; v1 (m = 3, 6): 2; v2 (m = 0): 1; total 3 = q.
  (2,2) N=16, in Z[zeta_16] eight-coords (x^8 = -1): v0 (eight odds): squares
    {1, 9} four each: 4x + 4x^9 = 4x - 4x = 0 -- SILENT; v1 (m = 2, 6, 10,
    14): squares all 4: 4x^4 = 4i; v2 (m = 4, 12): squares 0: 2; v3 (m = 8):
    1; v4 (m = 0): 1; total 4 + 4i = q(1+i).
  The chart theta-object assembles BY SHELLS with the primitive shell silent
  beyond the boundary cell -- the radial anatomy of the banked traces, decided
  at the three cells above. NO CLASSICAL IDENTIFICATION; a landing STAGES the
  bridge continuation (steps B-C at the federation's proved half), so marked.
(D4 = P4) THE FACTORIZATION at index grade: the shell is a function of the
  torus point through the place split -- gcd(j, D) = gcd(j mod q1, q1) *
  gcd(j mod q2, q2) at the composite denominators (decidable at D = 36, 100),
  and per place the gcd IS the shell (D1's law) -- R factors through the
  plane's radial coordinate; the radialization and the plane meet as the two
  staged files asked (N-OPEN-5 and the plane question, one answer).

LEAN PLAN: RadializationShadow.lean -- shell_partition_instances (P1: counts +
the seed addresses as gcd values); transfer_shift_gcd_law (P2: full ranges N =
4, 16, 64, 256); ball_boundary_address (P2: the filter identity at the banked
levels); theta_by_shells (P3: the three cells in pairs / six-coords /
eight-coords); plane_factorization (P4: gcd multiplicativity at D = 36, 100).
Row 54, no blank cells. Bank: data/b76_radialization.txt. Deviation rule
standing. Divergence note (rule a): none.
====================================================================================================
