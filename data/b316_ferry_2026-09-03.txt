CONCURRENCY: SOLO (research seat).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE ARCHIMEDEAN INSTRUMENT, ACT ONE — a computable truncation
of the object's archimedean space, built from the source's
conditions and verified against what the record already
owns, one act. Scope: an INSTRUMENT BUILD and its reproduction
controls; NO trace computed and NO smear assembled — that is
act two under its own registration; the prediction below is
REGISTERED, not tested; NO aggregation stated, M-2 owed under
its cap; the window ruling W2 is applied at construction only
to this extent — the instrument accepts either test function,
the corpus's integral-one bump and a mean-zero variant, and
computes with neither in this act; normalizations checked
before counts, and the half-line/full-line picture settled
first from the source's own inner product (W-ORD-ARCH-NORM-
READING, discharged or the act halts); exact arithmetic where
the object is exact, floating point declared where it is not,
zero float literals in any deciding runner; every reported
number from a committed tool with fixtures; needles pulled
from emitting files; stem sweep at extended scope; hedge
audit; registration term-scanned, satisfiability-checked,
sealed, no counts predicted; the suite re-run after the push;
nothing about the h2 identity beyond the register sentence
exact; nothing deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
the kernel-coverage gate run and its failure restated, nothing
repaired; push anything ahead of origin, saying so if nothing
is.

COMPONENT 1 — THE NORMALIZATION, before any construction: the
source's inner product quoted; the corpus's half-line picture
quoted from the act that confirmed it against the source; the
scaling action's exponent in the source's form; the transform
in the source's normalization. Each written once, used
everywhere, and the sign chain from the corpus's archimedean
term to the source's Weil distribution quoted from the acts
that settled it — because the registered prediction depends
on it and it is the hazard.

COMPONENT 2 — THE BUILD: even functions on a stated domain and
grid, the transform in the source's normalization discretized
with its own positive control (an even function with known
transform recovered to a stated precision); the two vanishing
conditions as linear constraints; the constrained subspace
extracted, its dimension at each truncation reported and its
behaviour under grid refinement stated rather than assumed.
The scaling action and the compression written as operators
on this space and shown to act, with the source's own sentence
that the scaling does not restrict to the subspace reproduced
as a measurement — the image leaving the space by a stated
amount.

COMPONENT 3 — THE REPRODUCTION, mandatory before anything is
trusted: the derived archimedean unit lies in the truncated
space to a stated precision (b300's membership, now measured);
the prolate vectors are orthogonal to it (the source's
statement, b291's finding, now measured); the source's own
worked inner product recovered; the corpus's remainder
instrument NOT reproduced here, and the reason stated — it is
built on vectors outside this space, and this instrument is
the first that is not.

COMPONENT 4 — THE PREDICTION, registered for act two and NOT
tested: under the source's Theorem 4.7, with the corpus's
archimedean term identified with the source's Weil
distribution by the record's settled sign, the corrected
remainder's near-cancellation against it says the compressed
smeared trace on this space, for the corpus's integral-one
bump, should be small relative to the corpus's own trace
instrument's values — the navigator's, refutable by act two's
number; and the same quantity for the mean-zero variant is
unpredicted. The sign chain's every link named as what would
make this prediction wrong for a trivial reason.

CLOSING: the instrument's capabilities and limits as a list;
what act two may compute named and not computed; the
correspondence rows by the idempotent tool; every result
keyed; the object's three conditions restated; M-2's row
unchanged under its cap; the seam's debt item 1 restated; the
patent clock restated; the in-flight register; the ledger
census with its scope; the coverage and absolute-path orders
restated. Registration gate; index queried;
archimedean-instrument keyed. Components ordered. THE SHADOW:
expected nothing — an instrument build; say so.

Bank: data/b316_the_archimedean_instrument.txt. Deviation
rule standing.

EXECUTION: ferry scan first; owners and the source read at
content first; registration satisfiability-checked,
term-scanned, sealed; components in order; full control suite,
re-run after the push; pins by ls-remote across all three
repos; the hook if PLACE-papers is touched; mirror if it
moves. STOP.

FOOT: after this act the corpus has, for the first time, a
computable truncation of the object's archimedean space with
its own unit inside it and the corpus's old expansion vectors
outside it, verified against everything the record owns, and
one registered prediction waiting for act two; h2 stands
exactly where the deposit left it; locks last.

paste ends (part 1 of 1)
