========================================================================================================
b495 -- THE CLOSING RECORD. ### **SIDE-explicit-formula CREATED, PUSHED, AND LAUNCHED UNREAD.**
========================================================================================================

### (1) THE PIN, AND THE READING THAT NEARLY CHOSE THE OTHER ONE.
--------------------------------------------------------------------------------------------------------
    `EF_lit``s statement at tag `v1.0` and at `fbdc36b`:
      sha256 `v1.0`    : 0255fa699a72c941d7dcb70e0232a0184fb9e0c48c66659a6a237c4022000fe6
      sha256 `fbdc36b` : 0255fa699a72c941d7dcb70e0232a0184fb9e0c48c66659a6a237c4022000fe6
      ### ### **BYTE-IDENTICAL : True** -- and the WHOLE MODULE agrees too, not only the
      ### statement. ### **PIN CHOSEN : `v1.0` = `3635e74826a4c1fcece7d1cd2b6fa75e43a00510`**, by the order`s
      ### own rule and by nothing else. ### The clone resolves `v1.0` to the SHA the order
      ### names (`3635e748`), and that agreement is printed rather than assumed.
    ### ### **AND THE FIRST READING SAID `NOT FOUND AT v1.0`.** ### It would have pinned
    ### `fbdc36b` -- and scored the navigator`s (N1) REFUTED -- for a reason the order never
    ### gave. ### The cause is a ### **LAYOUT MOVE**: at `v1.0` the tree is rooted at
    ### `Zeta23/`; by `fbdc36b` the `zeta23-palomar-layout` merge has moved it under
    ### `zeta23/`. ### **A PATH MISS IS NOT A FINDING ABOUT THE OBJECT.** ### The module is
    ### now resolved in ### **EACH REVISION`S OWN TREE**, and the path used is printed
    ### beside each reading, so the next reader sees the move rather than a verdict.

### (2) THE KERNEL.
--------------------------------------------------------------------------------------------------------
    `D:/SIDE-explicit-formula` -- the local import closure of `Zeta23.WeilEF.Main`,
    ### **COMPUTED AT THE PIN, NOT RECALLED FROM THE FACE**: ### **57 modules**, 90 external
    import roots (not vendored; `lake` fetches those from their own pins).
    ### ### **BODY DIGESTS AGREEING : 57 OF 57.** ### Per module, two digests: the source
    ### blob at the pin, and the copy ### **READ BACK FROM DISK BEFORE ANY HEADER EXISTED** --
    ### hashing the in-memory bytes would prove only that a variable was not reassigned.
    ### And after the prepend every body was re-read from the file`s TAIL and compared:
    ### ### **INTACT AT 57 OF 57.** ### The claim "nothing but a header was added" is
    ### CHECKED, not intended.
    `LICENSE` and `NOTICE` carried whole, Apache 2.0 section 4 -- digests IDENTICAL on both.
    ### ### **10 MODULES CARRY A SECOND HEADER** naming `PrimeNumberTheoremAnd`, because
    ### zeta23`s own `NOTICE` says *"The derived files are the ones under
    ### Zeta23/FromPNTPlus/"*. ### **THE PREDICATE IS THE SOURCE`S, NOT THE SEAT`S.**
    ### And those files ALREADY carry their own upstream notice -- *"Ported from
    ### PrimeNumberTheoremAnd at commit 6a380f0c..."* -- so they now hold ### **THREE
    ### ATTRIBUTION BLOCKS OF DISTINCT PROVENANCE**, which is correct and not a defect:
    ### the chain is carried unaltered, neither restated nor summarised.
    toolchain `leanprover/lean4:v4.33.0-rc2`, mathlib `51e6992efd06` -- ### **AS zeta23 PINS THEM.**
    ### SPIRAL_MAP rule 4 expects per-kernel divergence and this kernel STATES it: a vendored
    ### body must compile against the library its author compiled it against.

### (3) THE WAIVER, THE ROW, AND WHAT NEITHER OF THEM SAYS.
--------------------------------------------------------------------------------------------------------
    SPIRAL_MAP section 7, beside rule 9 (at line 384): ### **RULE 9 WAIVED FOR VENDORED
    ### NAMESPACES**, act b495 named as the reason. ### prefix True, ### **0 LINES REMOVED**,
    1 heading. ### **THE WAIVER NAMES ITS OWN BOUNDARY**: it lapses the moment this
    ### programme edits a vendored body, and it is not retroactive to any existing kernel.
    REGISTRY, one kernel-table row: deposit-pin ### **NONE**, working-head the pushed SHA,
    source zeta23 at the pin, profile ### **PENDING**. ### prefix True, 0 lines removed.
    ### ### **THE NO-CITATION CLAIM IS MEASURED, NOT ASSERTED.** ### The appended block was
    ### scanned for terminal-shaped names: ### **0.** ### The one dotted name in it is the
    ### vendoring seed, a MODULE, named as the thing copied. ### (R106)(3) is satisfied by
    ### an absence, and ### **THE ABSENCE WAS COUNTED.**

### (4) THE PUSH AND THE LAUNCH.
--------------------------------------------------------------------------------------------------------
    ### **THE PRE-PUSH GUARD WAS INSTALLED BEFORE THE FIRST PUSH THE REPOSITORY EVER TOOK**
    -- into the ### **TRACKED** `.githooks/` with `core.hooksPath`, byte-identical to relay`s
    single source: True. ### (R15): one guard, one source.
    `SIDE-explicit-formula` at `52d8cf9`, 67 files tracked, 58 `.lean`.
    ### ### **THE BUILD IS LAUNCHED AND UNREAD** -- pid ### **7860**, log
    ### `data/b495_ef_build.log`. ### The launcher stamps ### **AN INSTANT TAKEN AT EACH LINE**; b480
    ### precomputed one instant and stamped every mark with it, so its log could not say how
    ### long anything took or in what order. ### It profiles the three names ### **WHETHER OR
    ### NOT THE BUILD EXITS 0**, because a launcher that skips the profile on a non-zero exit
    ### hands the next act nothing to read.
    ### ### **AND THE LOG IS NOT COMMITTED BY THIS ACT.** ### It is still being written. ###
    ### b481-b489 spent nine acts excusing a live log inside a write-list arm; the cleaner
    ### answer is not to commit a file a running process still owns. ### **b496 READS IT AND
    ### BANKS IT.**

### (5) THE EXPECTATIONS -- AND TWO THAT CANNOT BE SCORED HERE.
--------------------------------------------------------------------------------------------------------
    ### **(N1) HELD.** ### byte-identical, pin `v1.0`.
    ### **(N2) HELD.** ### 57 of 57 body digests agree.
    ### **(N3) NOT SCORABLE IN THIS ACT.** ### **(S2) NOT SCORABLE, FOR THE SAME REASON.**
      ### Both name ### **THE LOG`S FIRST TEN MODULE MARKS**, and the same order says
      ### *"The log is not read in this act."* ### **SCORING THEM WOULD BREAK THE CLAUSE;
      ### SCORING THEM FROM ANYTHING ELSE WOULD ANSWER A DIFFERENT QUESTION.**
      ### ### **AND THE FACE DID NOT SEE THE CONFLICT WHEN IT WAS SEALED** -- section (S2)
      ### confidently re-pointed (N3) at "the ten the launcher marks first", which is the
      ### same unreadable cell. ### That is this act`s finding about ITS OWN REGISTRATION.
      ### What could be shown without the log was shown: ### **10 of 10 consecutive calls to
      ### the launcher`s `stamp()` gave DISTINCT instants**, `say()` calls it per line, and
      ### no precomputed module-level stamp exists. ### **THE WORDING`S OWN POPULATION IS
      ### b496`S CELL TO PRINT.**
    ### **(S1) HELD.** ### pid 7860 ALIVE at the desk`s reading, checked by `tasklist` -- which
      ### asks whether a process exists, ### **NOT WHAT IT WROTE.**
    ### **(S3) HELD.** ### the ten already carry their own upstream notice.

### (6) THIS ACT`S OWN DEFECTS -- FIVE.
--------------------------------------------------------------------------------------------------------
    (a) ### **THE IMPORT PARSER RETURNED A CLOSURE OF ONE.** ### It stopped at the first
        non-`import` line, which in every zeta23 module is the LICENCE HEADER`s first line.
        ### A closure of 1 with 0 external roots is a number that cannot be right, and it
        ### was caught by being ### **ABSURD**, not by an arm. ### `import` is now recognised
        ### anywhere in the file.
    (b) ### **THE PIN COMPARATOR CALLED A LAYOUT MOVE A STATEMENT CHANGE.** ### See (1).
        ### **THE DEFECT WOULD HAVE SCORED THE NAVIGATOR`S EXPECTATION REFUTED**, with a
        ### printed digest beside it, and nothing in the bank would have looked wrong.
    (c) ### **A CURE THAT ALREADY HAD A TOOL, REINVENTED.** ### The push component wrote the
        ### guard into `.git/hooks/` from `tools/git-hooks/pre-push` -- ### **A PATH THAT DOES
        ### NOT EXIST.** ### b371 moved the guard to a TRACKED `.githooks/` with
        ### `core.hooksPath` and b386`s (R15) made that the single source. ### Caught by the
        ### crash, which is luck: a tool that wrote a guard nothing reads would have passed.
    (d) ### **THE REGISTRATION REFUSED TWICE BEFORE IT SEALED.** ### The gate refused for
        ### want of a banked-index query (b160`s convention, b182`s failure) -- four queries
        ### run and their results reported. ### Then `reg_satisfiable` answered ### **DO NOT
        ### SEAL** on SIX numerals the prediction counter read as artifact-count predictions,
        ### every one of them a measured or quoted figure -- and ### **ONE OF THEM WAS THE
        ### TAIL OF THE HYPHENATED `FIFTY-SEVEN`**, matched as `SEVEN modules` because a
        ### hyphen is a word boundary. ### Caught by ### **READING THE TOOL`S VERDICT LINE**,
        ### which is b491`s lesson, not by a grep for a word.
    (e) ### **AN ARM PASSED ITS OWN POSITIVE CONTROL.** ### `G-PUSHED-PREDICATE-THREE-CLAUSED`
        ### was a DISJUNCTION over two spellings -- b494`s `components.txt` and this act`s
        ### `create.txt` -- and the mutation falsified only the second limb. ### **A CONTROL
        ### THAT LEAVES A LIVE LIMB CANNOT BITE.** ### Rewritten as a conjunction over the
        ### predicate`s three actual clauses, which is what its name always claimed.

### (7) THE SUITE, THE COMMITS, THE MIRROR.
--------------------------------------------------------------------------------------------------------
    pre-push  : ### ### **ARMS RUN : 63. ### LIVE PASSING : 63. ### LIVE FAILING : 0 .**
                64 declared on the face; `G-MIRROR-TAGGED-BUILD` deferred to post-push.
                ### 2 arms RETIRED IN WORDS on the face: `G-NOTHING-COMPILED` (the kernel
                ### lane is OPEN by the order`s first line, and an arm forbidding what the
                ### order commands is a FALSE ARM) and `G-CORPUS-LEAN-UNTOUCHED` (this act
                ### creates a repository OF Lean files). ### Their replacements are
                ### `G-BUILD-DETACHED`, `G-LOG-NOT-READ`, `G-NOCITE-BEFORE-PROFILE` and
                ### `G-NO-BODY-EDITED`, which bound the opening to what (R106)(3) allows.
    post-push : ### ### **ARMS RUN : 64. ### LIVE PASSING : 64. ### LIVE FAILING : 0 .**
                ### ### **VERDICT : ALL ARMS PASS AND EVERY CONTROL BEHAVES**
    ### **0 FILES WRITTEN THAT NO (W) GLOB COVERS.**

    the commits, each read back by `ls-remote`:
      relay                  local 5da68ef84d68 ; remote 5da68ef84d68 ; ### **AGREE**
      PLACE-papers           local 1bbf6e59f8ab ; remote 1bbf6e59f8ab ; ### **AGREE**
      SIDE-global-section    local ea3076bd33d1 ; remote ea3076bd33d1 ; ### **AGREE**
      SIDE-explicit-formula  local 52d8cf939220 ; remote 52d8cf939220 ; ### **AGREE**
    ### ### **relay`s FIRST PUSH FAILED: `Could not resolve host: github.com`.** ### A direct
    ### `ls-remote` answered immediately; the push was retried and succeeded. ### **THE SAME
    ### TRANSIENT SPECIES b494`S CLOSING PINS HIT**, now met with the same answer: test the
    ### claim directly, retry, and say so.
    the mirror : `mirror-refresh-2026-09-23-b495.zip` at `1bbf6e5` --
      ### **CLEAN ON ALL THREE CLAUSES**

### (8) THE CENSUSES, THE PINS, THE LISTS.
--------------------------------------------------------------------------------------------------------
    handoff census : ### ### **TOTAL MISSING : 0**
    faces census   : ### ### **TOTAL MISSING : 0**
    pins           : ### REPOS HARD-FAILING : 0 ### (run ALONE, per b494`s lesson)
    ### **THE FOUR LISTS STAY OPEN.** ### Nothing fetched but `git`; ### **NOTHING CITED,
    ### NOTHING GRADED, NOTHING PROFILED.** ### No corpus grade moved; ### **ROW U1
    ### UNEDITED**; nothing deposits; ### **NOTHING AT ZENODO IS WRITTEN**; ### **h2 WHERE
    ### THE DEPOSIT LEFT IT.** ### The numerical lane stays shut. ### The zeta23 clone was
    ### READ and never written and ### **NEVER COMMITTED** -- (R58).

### (9) WHAT IS CARRIED FORWARD.
--------------------------------------------------------------------------------------------------------
    (a) ### **b496 READS THE LOG AND PROFILES**, under (R106)(3), and commits the log. ### It
        ### also owes (N3) and (S2) their cells -- the two this act could not score.
    (b) ### **NO ROW MAY CITE THIS KERNEL UNTIL THAT PROFILE IS BANKED.** ### The REGISTRY
        ### row says `PENDING` and names no terminal, and that is not a placeholder to be
        ### filled in by anyone but the act that runs the profile.
    (c) ### **(R106)(1): `W-ORD-EPSTEIN-ISOLATION` IS FILED** -- the four-channel ledger for
        ### an Epstein zeta with a known off-line zero, on the same ladder, so the sign of
        ### `A - PR` at a non-xi object is measured beside xi`s. ### Trigger: (R104)`s ladder
        ### act, or the author`s word. ### b494`s Table 2 row 5 is corrected: the sentence IS
        ### in the corpus as PROSE at `A_Place_to_Stand` step (9), with NO BANK and NO
        ### TERMINAL. ### **b494 SAID "NAMES NOTHING" AND THE AUTHOR FOUND IT.**
    (d) ### **(R106)(2): `C7_finite_type_false` IS PROFILED** in the next act that opens the
        ### `SIDE-lv-conservation` lane, and `OPEN_TRAILS` line 102 annotated by APPENDED
        ### NOTE, not rewritten.
    (e) ### **THE PLATFORM SESSION IS STILL THE AUTHOR`S AND STILL BLOCKED** -- eight rows
        ### exceeding the (R102) ceiling with no draft, three of them titles; and (R99)`s
        ### kernel text is held because its target sentence is not in the deposit.
    (f) ### **`corr_row.py` CHECKS THE NUMBER AND NOT THE CELLS**, routed at b490 and
        ### ### **OVERDUE**. ### Row 344 went in clean, which is not evidence the guard
        ### exists -- it is evidence the cells happened to contain no pipe.
========================================================================================================
