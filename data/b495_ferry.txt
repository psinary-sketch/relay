RULING (R106), the author's, ratified by this paste and strikeable

(1) b494's Table 2 row 5 is corrected on the record: the Epstein sentence IS stated in the
corpus, as prose at A_Place_to_Stand step (9) ("the ingredient the Epstein comparison
isolates"), and it has NO bank and NO terminal. Its address is therefore PROSE, NO BANK,
and a work-order W-ORD-EPSTEIN-ISOLATION is filed: the four-channel ledger computed for an
Epstein zeta function with a known off-line zero, on the same ladder, so the sign of A - PR
at a non-xi object is measured beside xi's. Trigger: the ladder act of (R104), or the
author's word.
(2) C7_finite_type_false is profiled in the next act that opens the SIDE-lv-conservation
lane, and OPEN_TRAILS line 102 is annotated by appended note, not rewritten.
(3) The creation of SIDE-explicit-formula is two acts: b495 creates, pins, copies, pushes
and launches the build detached; the act after reads the log and profiles. No row cites the
kernel before the profile is banked.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm receipt-in-full)

ACT b495 - SIDE-explicit-formula CREATED UNDER (R82)(a)-(e) AND (R83). The kernel lane
opens for this act.

Component 1 - the pin. Print EF_lit's statement at tag v1.0 (3635e748) and at fbdc36b from
the corpus's own clone of anthropics/formal-math, and their sha256. Byte-identical -> pin
v1.0; else pin fbdc36b. Print the pin chosen and why.

Component 2 - the repository. Create D:\SIDE-explicit-formula under the federation's
naming: lean-toolchain v4.33.0-rc2, Mathlib 51e6992e as zeta23 pins them. Copy the
fifty-seven modules of the b470 import closure. For each: bank the sha256 of the copied
body BEFORE the header is prepended, and the source file's sha256 at the pin; the two must
agree at all fifty-seven, printed as a count. Prepend rule 7's header naming zeta23 at the
pin on all fifty-seven, and the second header naming PrimeNumberTheoremAnd on the ten, as
zeta23's NOTICE names it. LICENSE and NOTICE carried whole, sha256 of each against the
source. No file edited beyond its headers.

Component 3 - the waiver. SPIRAL_MAP section 7, beside rule 9: rule 9 waived for vendored
namespaces, this act named as the reason, appended and nothing above it rewritten.

Component 4 - the push and the launch. Push to GitHub as a federation member, read the
remote SHA back by ls-remote. Launch lake build DETACHED with the launcher's timestamp
defect from b480 repaired (a distinct instant per module mark), then PrintAxioms on
EF_lit_zetaZeroConfig, EF_lit and EF_lit_zeta appended to the same log. Print the pid and
the log path. The log is not read in this act.

Component 5 - REGISTRY. One row in the kernel table: SIDE-explicit-formula, deposit-pin
NONE, working-head the pushed SHA, source zeta23 at the pin, profile PENDING.

The navigator's expectations: (N1) EF_lit is byte-identical at v1.0 and fbdc36b, so the pin
is v1.0; (N2) all fifty-seven body hashes agree with the source at the pin; (N3) the
launcher's marks carry more than six distinct instants within the ten modules it builds
earliest. Each refutable by a cell. Nothing cited; nothing at Zenodo written; nothing
deposits; h2 where the deposit left it.

paste ends (part 1 of 1)
