CONCURRENCY: SOLO (research seat). FERRY_STANDING v2 by
reference. Anchors by the tool, never typed; extract-to-disk
for every read; the registration LOCKED before any read; gate
arms written by content, not by address, per (R2); no claim
about h2, totality or the roster; the instrument lane stays
parked; the wave stays parked; nothing deposits.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm
receipt-in-full)

ACT b367 - THE SCAFFOLD REPAIR: READ AND PRICED, NEVER A BUILD.
The executor's draft is ADOPTED as written, with four
additions. Number not claimed by any unclosed ferry.

CAP, quoted in the registration: ONE act; a location, a read
and a pricing; NO Lean file written, NO terminal replaced, NO
statement proved; NOT LOCATED stops the act, as the draft's own
first duty says.

ADDITION ONE - THE HINT, AND ITS STATUS. The navigator asserts
from RECALL and NOT from any file: the kernel is SIDE-effects;
the two terminals sit in a structural module; they concern
excluding the generalized hypothesis and excluding an
exceptional real zero; and the kernel's working head may sit on
a feature branch rather than main. Treat every clause as a
search string, never as a source. Report each as CONFIRMED,
CORRECTED (with the correction quoted from the file), or NOT
LOCATED. Check the branch explicitly and state which ref the
act read, since a read of main would miss a head that is not
there.

ADDITION TWO - WHAT THEY ARE, quoted: each terminal's full
statement as the file has it, its docstring, its axiom profile
read from a printed profile and not from a compile, every site
in the corpus that cites or imports it (papers, ledgers,
correspondence rows, other kernels), and the standing
restriction quoted from wherever the record states it. If any
paper cites either one as evidence, that is reported at full
prominence.

ADDITION THREE - THE THREE ROUTES, priced separately and none
attempted: (a) CONTENTFUL REPLACEMENT - what each terminal
would have to state to derive rather than stipulate, what that
statement rests on, and whether that is a bounded formalization
or an open problem in its own right, decided by reading what
the statement says and not by its name; (b) INTERFACES -
restate each as a theorem taking the statement as a NAMED
premise, deriving whatever the kernel wanted from it, with the
premise named in the row, which the record calls a legitimate
architecture and uses at its own route terminal; what that
would cost, and what it would let a paper cite that it cannot
cite now; (c) DELETION - remove them and whatever depends on
them, with what the kernel loses. For each route: the acts it
would take, what it must not disturb, and what it does NOT
buy.

ADDITION FOUR - THE RECOMMENDATION IS THE AUTHOR'S: the act
presents the three routes and does NOT choose. It may state
which route the record's own architecture already sanctions,
quoting the passage that sanctions it, without recommending.

CLOSING: the trail for the scaffold updated through the writer
with what this act located; the faces ledger only if a row
moves; correspondence rows; keys; both censuses; pins by
ls-remote across all repos read, including the exclusion kernel
at whatever ref was read; hook and mirror where owed; the next
ferry as DRAFT - NAVIGATOR EDITS. The navigator's expectations,
registered here: (F1) the terminals are located, on a non-main
ref; (F2) route (a) is an open problem for at least one of
them and the act says so from the statement's content; (F3)
route (b) is cheap and is the one the record's architecture
already sanctions. Each refutable by a quotation. h2 where the
deposit left it.

paste ends (part 1 of 1)
