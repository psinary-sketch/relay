====================================================================================================
b431_extract.py -- THE SURVEY. ### THE GRADING'S SUBJECTS, AND THE TYPE-D QUESTION'S.
====================================================================================================
  ### NOTHING IS FETCHED BY THIS TOOL. ### NO FOREIGN REPOSITORY IS CLONED HERE.

----------------------------------------------------------------------------------------------------
  (1) THE ADDRESSES, AS THE AUTHOR SUPPLIED THEM -- QUOTED, NOT RESOLVED HERE.
----------------------------------------------------------------------------------------------------
    the Lean repository                : github.com/openai/LongGapsBetweenPrimes
    the file the theorem is named in   : LongGapsBetweenPrimes.lean
    the paper                          : cdn.openai.com/pdf/51126fac-1b68-4128-9666-c908bcc16033/long_gaps.pdf
    ### **AND ONE FACT ABOUT STEP ZERO, RECORDED RATHER THAN HIDDEN.** ### The repository
    ### address was resolved by `ls-remote` at step zero, BEFORE this act's face was
    ### locked, while the seat was establishing that leg 2 could run at all. ### **THAT
    ### IS A NETWORK READ BEFORE THE LOCK AND THE FACE DECLARES IT.** ### The pin it
    ### returned is recorded below; the components re-read it AFTER the lock and print
    ### both, so a difference would be visible rather than absorbed.
    step-zero ls-remote record : PRESENT
        b431 STEP ZERO -- THE AUTHOR'S ADDRESS RESOLVED BY ls-remote, BEFORE THE LOCK.
        resolved at (UTC) : 2026-09-12
        address           : github.com/openai/LongGapsBetweenPrimes

----------------------------------------------------------------------------------------------------
  (2) THE CORPUS SIDE OF THE TYPE-D QUESTION, UNFOLDED FROM SOURCE.
----------------------------------------------------------------------------------------------------
    SIDE-effects SIDEEffects/Phase15/Module1.lean : 158 lines
    SIDE-effects HEAD                            : ef4cff776f8918761bdb08d9e23f684b0abd5875

    no_type_d_conspiracies   line 154
        theorem no_type_d_conspiracies : IsEmpty TypeD := by
          refine ⟨fun ⟨sc, h_no_modular⟩ => ?_⟩
          exact h_no_modular (crt_exhaustiveness sc)
        
        end SIDEEffects.Phase15.Module1

    crt_exhaustiveness       line 146
        theorem crt_exhaustiveness (sc : StructuralCoupling) :
            ∃ m : ModularCoupling, ∀ n : ℕ, sc.eval n ↔ m.eval n :=
          ⟨to_modular sc, fun n => to_modular_correct sc n⟩
        
        def TypeD : Type :=
          { sc : StructuralCoupling //
            ¬ ∃ m : ModularCoupling, ∀ n, sc.eval n ↔ m.eval n }
        

    TypeD                    line 150
        def TypeD : Type :=
          { sc : StructuralCoupling //
            ¬ ∃ m : ModularCoupling, ∀ n, sc.eval n ↔ m.eval n }
        
        theorem no_type_d_conspiracies : IsEmpty TypeD := by
          refine ⟨fun ⟨sc, h_no_modular⟩ => ?_⟩
          exact h_no_modular (crt_exhaustiveness sc)
        

    to_modular               line 139
        noncomputable def to_modular (sc : StructuralCoupling) : ModularCoupling :=
          ofPeriodic sc.period sc.period_pos sc.eval
        
        theorem to_modular_correct (sc : StructuralCoupling) (n : ℕ) :
            sc.eval n ↔ (to_modular sc).eval n :=
          (ofPeriodic_eval sc.period sc.period_pos sc.eval sc.eval_periodic n).symm
        
        theorem crt_exhaustiveness (sc : StructuralCoupling) :

    ofPeriodic               line 111
        noncomputable def ofPeriodic (L : ℕ) (hL : 0 < L) (P : ℕ → Prop) :
            ModularCoupling where
          moduli := {L}
          allowed_residues := fun q hq =>
            haveI : NeZero q := ⟨by rw [Finset.mem_singleton] at hq; subst hq; exact hL.ne'⟩
            haveI : DecidablePred (fun r : ZMod q => P r.val) := Classical.decPred _
            Finset.univ.filter (fun r : ZMod q => P r.val)
        

    ofPeriodic_eval          line 119
        theorem ofPeriodic_eval (L : ℕ) (hL : 0 < L) (P : ℕ → Prop)
            (hper : ∀ n, P n ↔ P (n % L)) (n : ℕ) :
            (ofPeriodic L hL P).eval n ↔ P n := by
          haveI : NeZero L := ⟨hL.ne'⟩
          unfold ModularCoupling.eval
          constructor
          · intro h
            have hm := h L (Finset.mem_singleton_self L)

    structure StructuralCoupling ### NOT LOCATED ###

    structure ModularCoupling    line 36
        structure ModularCoupling : Type where
          moduli : Finset ℕ
          allowed_residues : (q : ℕ) → q ∈ moduli → Finset (ZMod q)
        
        def ModularCoupling.eval (m : ModularCoupling) (n : ℕ) : Prop :=
          ∀ q, ∀ h : q ∈ m.moduli, (n : ZMod q) ∈ m.allowed_residues q h
        
        /-! ## Period -/
        
        def StructuralCoupling.period : StructuralCoupling → ℕ

----------------------------------------------------------------------------------------------------
  (3) WHAT THE KEYSTONE'S PROSE SAYS THE LEMMA IS.
----------------------------------------------------------------------------------------------------
    phase2/method/ADDITIVE_MULTIPLICATIVE_CONSPIRACY.md : 468 lines
      line 25:
          Twin primes (infinitely many primes $p$ with $p + 2$ prime), the binary Goldbach
          conjecture (every even $n \geq 4$ is a sum of two primes), and Sophie Germain primes
          (infinitely many primes $p$ with $2p + 1$ prime) share one obstruction-shape: the
          negation of each would require an additive–multiplicative conspiracy (internally
          classified Type D) -- a coupling between additive and multiplicative structure beyond
          what the Chinese Remainder Theorem explains at every finite modulus. Their negations
          differ from one another *only in analytic content*. The finite-modulus no-conspiracy is
          compiled: Module1.no_type_d_conspiracies (CRT exhaustiveness via periodic lift) in the
          SIDE-effects repository -
      line 38:
          An additive–multiplicative conspiracy is, formally, a coupling that cannot be explained
          by the joint modular structure of integers under CRT. The Chinese Remainder Theorem
          provides the canonical decomposition $\mathbb{Z}/n\mathbb{Z} \cong \prod_p
          \mathbb{Z}/p^{v_p(n)}\mathbb{Z}$, which factors any joint additive-multiplicative
          constraint modulo $n$ into per-prime constraints. The structural theorem no_type_d:
      line 73:
          - The Chinese Remainder Theorem decomposition into prime power moduli.
      line 85:
          Kernel-citation pin (2026-07-13). The placeholder instantiations quoted in this paper --
          TwinFinite, GoldbachFails, SGFinite, and no_conspiracy_twins / _goldbach / _sg -- exist
          in SIDEEffects/Structural.lean at pin c66f3c5, and are cited at that pin here. They were
          retired from the SIDE-effects tree on 2026-06-16 (withdrawal of the uniformly-True
          shells, work-order W-2; landed on main 2026-07-13), which enacts in the kernel what this
          paper's v0.2 scope ruling already stated on paper: they are placeholder forms, not the
          CRT content, and they carry no claim. The load-bearing terminal,
          Module1.no_type_d_conspiracies, is unaffected and survives -- see the Correspondence.
      line 227:
          The genuine structural exclusion is Module1.no_type_d_conspiracies (CRT exhaustiveness
          via periodic lift), vanilla Lean 4, no sorry, no axioms. (The
          no_conspiracy_twins/goldbach/sg instantiations shown above are over uniformly-True
          predicates -- TypeD Nat (fun _ => True) (fun _ => True) -- i.e. placeholder form, not
          the CRT content.) The full conjecture statements are TYPE II results in the
          methodology's classification (A_METHODOLOGY v0.5.4 §V.10): structural exclusion correct,
          analytic content needed to complete the statement.
      line 231:
          - Structural argument (kernel-verified): the negation of any of the three conjectures
          requires a additive–multiplicative conspiracy; no additive–multiplicative conspiracy
          exists; therefore the negation fails. The genuine verification is
          Module1.no_type_d_conspiracies (CRT exhaustiveness); the no_conspiracy_twins/goldbach/sg
          instantiations are placeholder forms over uniformly-True predicates.
      line 269:
          (2) The structural exclusion is kernel-verified as Module1.no_type_d_conspiracies (CRT
          exhaustiveness via periodic lift; vanilla Lean 4, 0 sorry, 0 axioms). The per-conjecture
          no_conspiracy_twins/goldbach/sg instantiations are placeholder forms over uniformly-True
          predicates, not the CRT content.
      line 281:
          SIDE-effects (c66f3c5; vanilla Lean 4, no Mathlib). The additive-multiplicative
          structural component -- the 0 sorry, 0 axioms claim holds of the structural theorems
          (Module1.no_type_d_conspiracies, crt_exhaustiveness, no_type_d); the milestone wrappers
          each carry one sorry, disclosed in the same list:
      line 282:
          - Module1.no_type_d_conspiracies -- the genuine structural exclusion (crt_exhaustiveness
          ⇒ IsEmpty TypeD), CRT exhaustiveness via periodic lift; no_type_d -- the general
          type-level exclusion lemma
      line 308:
          SIDE-effects -- the additive-multiplicative no-conspiracy skeleton
          (Module1.no_type_d_conspiracies, crt_exhaustiveness, no_type_d).
      line 324:
          Three classical conjectures of analytic number theory -- twin primes infinitude, binary
          Goldbach, Sophie Germain primes infinitude -- share a single structural exclusion. The
          negation of any of the three would require a additive–multiplicative conspiracy: an
          additive-multiplicative coupling beyond what the Chinese Remainder Theorem explains at
          every finite modulus. The finite-modulus no-conspiracy is kernel-verified as
          Module1.no_type_d_conspiracies (CRT exhaustiveness) in SIDE-effects, vanilla Lean 4, 0
          sorry, 0 axioms.
      line 354:
          SIDE-effects (additive-multiplicative component): Module1.no_type_d_conspiracies
          (genuine CRT exhaustiveness) and the general no_type_d lemma compile at 0 sorry, 0
          axioms; the no_conspiracy_twins/goldbach/sg instantiations are placeholder forms; the
          twin_primes_infinite / goldbach / sophie_germain_infinite milestone wrappers each carry
          one sorry at the analytic boundary. Vanilla Lean 4.
      line 394:
          Claim–certificate calculus (EXCLUSION_ENGINE §VIII.0). The calculus certificate-types
          the grades below: no_type_d (the row "whose entire weight is its hypothesis") is a
          conditional / interface-certificate; no_type_d_conspiracies is a derivation-certificate
          (scoped positive-modulus).
      line 398:
          | Finite-modulus no-conspiracy (CRT exhaustiveness) | SIDE-effects (c66f3c5) |
          SIDEEffects.Phase15.Module1.no_type_d_conspiracies (via
          SIDEEffects.Phase15.Module1.crt_exhaustiveness) | {propext, Classical.choice,
          Quot.sound} -- no sorryAx. Run at pin a27415d (tree clean: git status --porcelain empty;
          HEAD recorded at run), 2026-07-13, lake env lean #print axioms. | DERIVES -- the
          modular-conversion discharge landed at a27415d (W-4). to_modular_correct's four cases
          are proved via the periodicity restructure (StructuralCoupling.period / period_pos /
          eval_periodic, with to_modular := ofPeriodic … correct by ofPeriodic_eval); zero sorry;
          full lake build 8318 jobs, exit 0. Scope: for positive-mod
      line 417:
          *v0.2.3, 2026-07-19 -- ARM-6 A6.2 ENABLE-annotation + DOI pass. No claim reframed
          (v0.2.2's scope ruling stands: the classification is compiled; the conjectures are open,
          their shared difficulty located). Added census/trail notes: the E-Difficulty skeleton
          classifies the trio as TYPE-D not TYPE-III -- a domain-Ostrowski exists
          (CRT-at-every-prime) -- with the W-6 gate stated in-file (kernel terminals are W-6
          shells, SieveCeiling.lean; classification manuscript-level, FOUNDATIONS §VI.1–VI.5,
          until W-6 discharges); the D↔E unification (FOUNDATIONS §VI.5) naming the §VI
          local-to-global interchange; the h1-complete / h2-open decomposition shared with
          SIDE-lv-conservation at Φ, with partialPositi
      line 423:
          *v0.2, 2026-07-12 -- scope-ruling pass. Retitled "The Additive–Multiplicative
          Conspiracy" (classification-code naturalization). The status is corrected from
          "structurally proved, analytically open" to the classification is compiled; the
          conjectures are open, their shared difficulty located: the finite-modulus no-conspiracy
          (Module1.no_type_d_conspiracies, SIDE-effects) is compiled and rules out
          finite-modulus-unexplained couplings only; the density lower bounds (M3/M4/M5) are the
          entire analytic weight and are where the theorems live. Adds the local-to-global
          boundary paragraph (§VI, citing the SIDE-lv-conservation bracket and *The Unconditional
          Surround of ξ*); retires the placeholder miles
      line 436:
          | the finite-modulus no-conspiracy (§I) | SIDE-effects, pin c66f3c5, branch
          w-ladder-skeleton *(recorded 2026-08-12; the corpus had never named the branch)* |
          Module1.no_type_d_conspiracies | 0 sorry, 0 axioms (vanilla Lean 4, no Mathlib) |
          COMPILED | git -C SIDE-effects checkout w-ladder-skeleton && lake build, then #print
          axioms Module1.no_type_d_conspiracies |

----------------------------------------------------------------------------------------------------
  (4) THE PROTOCOL THIS ACT IS HELD TO -- b429'S, AS b430 APPLIED IT.
----------------------------------------------------------------------------------------------------
    THE SEAL THIS RUN READ OFF THE LOCKED FACE   : ### THE SEAL THIS RUN READ OFF THE LOCKED FACE : 620e081e6e5
    the pin the build was made at                : the pin the build was made at (from its log) : 6e454b1b7ba3a
    DEFINITION-ENCODED                           : DEFINITION-ENCODED     : False
    VERDICT : SYMMETRIC                          : ### ### **VERDICT : SYMMETRIC**
    ### **b430 SETTLED THAT THE PROTOCOL IS SYMMETRIC ON ONE TRIAL.** ### This act runs
    ### it on a second stranger, so the same four clauses must hold here too.

    ### AND THE BUILD PRACTICALITIES b429 LEARNED ON THIS MACHINE, CARRIED:
      ### **`lake` HAS NO `-j` FLAG IN THIS VERSION.** ### Default parallelism OOMs at
      ### ~12 lean processes; the cure is to build failing modules ONE AT A TIME.
      ### A missing `*.olean.private` is fixed by re-running `lake exe cache get`.
      ### `ELAN_HOME` must point at `D:` -- `C:` has under 2 GiB free.
      ### **KILLING `lake` MID-CLONE CORRUPTS `.lake/packages/mathlib`**; the cure is
      ### `rm -rf` and a fresh clone, never a repair in place.

====================================================================================================
  ### READS ATTEMPTED : 5
  ### MISSES          : 0
  ### **A MISS IS PRINTED, NEVER PATCHED.**
====================================================================================================
