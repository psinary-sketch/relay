====================================================================================================
b322_extract.py -- THE EXTRACT STEP. ### **THE FINDER IS b317's, IMPORTED NOT COPIED.**
====================================================================================================
  finder source    : b317_extract.py
  ### ITS FIXTURES, RUN HERE BEFORE IT IS TRUSTED : [True, True, True, True, True, True, True, True]  PASS

----------------------------------------------------------------------------------------------------
### THE PIN. ### **BEFORE A WORD OF THE ARTEFACT IS READ.**
----------------------------------------------------------------------------------------------------
  artefact  : cc_2006.13771v1.pdf
  cite      : Connes & Consani, "Weil positivity and Trace formula the archimedean place", arXiv:2006.13771v1
  sha256    : b8e0b54ade8535cf3ca633d1ef325bfc5c793b407da577a83d111726935b58e0
  ### MATCHES THE PIN b304 SET AND EVERY ACT SINCE RE-CHECKED : True  YES
  pages     : 57

----------------------------------------------------------------------------------------------------
### (A) THE SOURCE FRAGMENTS. ### **THE SPACE, THE TRANSFORM, THE INNER PRODUCT.**
----------------------------------------------------------------------------------------------------

### ==== ### DEFINITION 4.4 / (72) -- THE SPACE S(a,b), THE TWO CONDITIONS
###      page index 24 (printed page 25) | anchor 'Definition4.4For' | char offset 306
    | P1, pP1q|Sp1,1q“ 0.
    | Deﬁnition 4.4 Forα,β ą 0, let Sonin’s space Spα,βqĂ L2pRqev be deﬁned by
    | Spα,βq :“tξPL2pRqev|ξpqq“ 0, @q,|q|ď α, pFeRξqppq“ 0, @p,|p|ď βu. (72)
    | Given two vectorsξ,η P H in a Hilbert space H we shall use the Dirac notation |ξyxη| for the
    | rank one projection
    | |ξyxη|pη1q :“ξxη|η1y, @η1P H, (73)
    | where by convention the inner product xη|η1y

### ==== ### (16) -- THE INNER PRODUCT AND ITS NORMALIZATION
###      page index 6 (printed page 7) | anchor 'inL2pRqevasfollows' | char offset 714
    | igure 1. We normalize
    | the inner product in L2pRqev as follows
    | xη|ξy :“ 1
    | 2
    | ż
    | R
    | ηpxqξpxqdx“
    | ż8
    | 0
    | ηpxqξpxqdx. (16)
    | We ﬁrst relate the trace computations performed in L2pRqev with those done in the isomorphic
    | Hilbert space L2pR˚
    | `,d˚λq using the unitary isomorphism
    | w :L2pRqevÑL2pR˚

### ==== ### (24) -- THE TRANSFORM ON EVEN FUNCTIONS
###      page index 7 (printed page 8) | anchor 'itdefinestheunitary' | char offset 947
    | ransform with respect
    | to the basic character eRpxq“ e´2πix: it deﬁnes the unitary in L2pRqev
    | FeRpξqpyq :“
    | ż8
    | ´8
    | ξpxqe´2πixydy. (24)
    | On implementing the isomorphism w we obtain
    | Lemma 1.3 One has: Fw
    | eR“I˝ug
    | 8. Equivalently, the following equality holds in L2pRqev
    | FeR“w´1˝I˝ F´1
    | µ ˝u8˝ Fµ˝w, (25)
    | wher

### ==== (61) -- THE SCALING ACTION
###      page index 22 (printed page 23) | anchor 'itsactionisgivenby' | char offset 1567
    | also [18] Chapter 2, §3.3), for Λ “ 1.
    | We switch to the Hilbert space L2pRqev and use the isomorphism w of (17) to rewrite (60)
    | inL2pRqev. Let ϑ :“w´1ϑmw be the unitary representation ϑm conjugated by the isomorphism
    | w, its action is given by
    | pϑpλqξqpvq :“ λ´1{2ξpλ´1vq, @ξPL2pRqev. (61)
    | The projection P (still denoted by the same letter), becomes the multiplication by the charac

  ### ### **SOURCE FRAGMENTS NOT FOUND : 0**

----------------------------------------------------------------------------------------------------
### (B) THE CORPUS READS, EACH AT THE FILE THAT EMITTED THE SENTENCE.
----------------------------------------------------------------------------------------------------

### ==== ### CM LEMMA 3.1 -- CONDITION ONE, AND THE EVENNESS. ### EMITTER: b202
###      data/b202_sum_test.txt | lines 13-34
    | ----------------------------------------------------------------------------------------------------
    | 
    | ### SOURCE: arXiv:2112.05500v1 [math.NT], 10 Dec 2021, Connes-Moscovici, "Prolate spheroidal
    | ### operator and Zeta". ### M12 GUARD RUN FIRST.
    | ### THE SYMBOL COUNTS, READ FROM THE DOCUMENT: ### "phi_mu" 5 OCCURRENCES, "xi_mu" 3, ### ALL IN
    | ### SECTION 3. ### THERE IS NOWHERE ELSE TO LOOK, AND THE COUNTS SAY SO RATHER THAN A CLAIM THAT
    | ### I LOOKED EVERYWHERE.
    | 
    | ### LEMMA 3.1, WHOLE: "For any mu in R the asymptotic expansion of the unique solution xi_mu which
    | ### at infinity is asymptotically ~ - sin(2 pi x)/(pi x) is Borel summable and is equal to the
    | ### Fourier transform of the unique even solution phi_mu which is zero on [-1,1] and agrees with
    | ### f_mu(x) for x > 1." ### PROOF'S CLOSING LINE: "Thus it follows that the Fourier transform
    | ### F_eR phi_mu = xi_mu."
    | ### COROLLARY 3.2, WHOLE: "With the above notation, assume mu is a negative eigenvalue. Then
    | ### phi_mu belongs to the Sonin space." ### PROOF: "Sonin's space is the orthogonal of the
    | ### eigenspaces of Wsa associated to the classical prolate functions and their Fourier transforms."
    | 
    | ### WHAT THE SOURCE SAYS ABOUT phi_mu, SEPARATELY:
    |  · SUPPORT: "even and VANISHES ON [-1,1]".
    |  · A VALUE AT A POINT, PINNED: "agrees with f_mu(x) for x > 1" with "f_mu(x) = SUM U_n(mu)(x-1)^n,
    |    ### U_0(mu) = 1". ### SO phi_mu != 0, FROM THE SOURCE'S OWN NORMALIZATION.
    |  · MEMBERSHIP: in the SONIN SPACE, for negative mu (Cor 3.2).

### ==== ### b211's (C3) -- THE EIGENRELATION CONDITION TWO RESTS ON. ### EMITTER: b211
###      data/b211_alternation_derived.txt | lines 188-200
    |  ### THEREFORE sign(alpha_k) ALTERNATES AT CONSECUTIVE EIGENVALUES OF THE EVEN FAMILY.
    |  ### THIS IS THE PROSE ASSERTION OF I5 ("+- pi sqrt 2 ... ALTERNATIVELY"), DERIVED.
    |  ### AND IT IS b207's MEASURED ALTERNATION AND b210's s = +1, EXPLAINED RATHER THAN OBSERVED.
    | 
    | (C3) ### BOTH VALUES OF c OCCUR -- **DERIVES**, on b203's chain: I8 + I6 + I10.
    |  I8 gives commutation; I6 gives a one-dimensional eigenspace; together F phi_mu = c phi_mu for
    |  a scalar c; I10 gives c^2 = 1, so c = +-1. ### THIS IS b203's CHAIN, UNCHANGED, AND ITS FENCE
    |  ### RIDES WITH IT: ### F phi = xi IS NOT F phi = phi; the relation is DERIVED from simplicity
    |  ### plus commutation, not substituted.
    |  The identification of c with sign(alpha): I4 gives psi(Lambda) = +-1 in the W_Lambda
    |  normalization, I5 gives alpha = psi(1) = +- pi Lambda in the D_tau normalization, and I11
    |  gives alpha^D = (pi Lambda) alpha^W with pi Lambda > 0, so ### sign(alpha^D) = alpha^W = c.
    |  By (C2) sign(alpha_k) alternates; an alternating sign sequence takes both values (Core row 83,

### ==== ### b203's FENCE -- `F phi = xi` IS NOT `F phi = phi`. ### EMITTER: b203
###      data/b203_transform_convention.txt | lines 140-152
    |                                                  ### model-exact check and b32's longhand.
    |  ### ### **CONCLUSION: phi_mu IS AN F-EIGENVECTOR, AND IT LIES IN E_1 OR E_-1 OF THE ARCHIMEDEAN
    |  ### ### SONIN SPACE.** ### b201's (M-a) IS DISCHARGED. ### (M-b), THE SIGN, IS NOT.
    | 
    | ### THE FENCE HELD, AND HERE IS EXACTLY HOW:
    | ### ### THIS IS **NOT** F phi = xi READ AS F phi = phi. ### It is F phi = c phi DERIVED from
    | ### simplicity plus commutation, ### AFTER WHICH xi_mu IS **IDENTIFIED** AS c * phi_mu RATHER THAN
    | ### SUBSTITUTED FOR IT. ### The two functions are still two functions; what is new is that a
    | ### scalar relates them, and the scalar's VALUE is what remains.
    | 
    | *** ### M22, RESTATED, AND IT IS NOW **A SIGN**:
    | ### ### **IS c = +1 OR c = -1?** ### Equivalently: is phi_mu in the +1 sector or the -1 sector of
    | ### the archimedean Sonin space?

### ==== ### b300's VERDICT (b) -- THE DERIVATION UNDER TEST. ### EMITTER: b300
###      data/b300_the_archimedean_leg.txt | lines 283-300
    | ###     (`2.0e-4`) is carried with it, which b214 records as ### **the executor's sampling error
    | ###     inside its own excluded neighbourhood and not the object's.**
    | ###     ### **THIS IS QUOTED AS AN OWNER'S BANKED NUMBER. ### NOTHING IS COMPUTED FROM IT AND THE
    | ###     ### DERIVATION ABOVE DOES NOT REST ON IT.**
    | 
    | ### ### ### **VERDICT (b): ### (IN, DERIVED).**
    | ### ### **GRADE: ### DERIVES-on-IMPORTS** ### -- and the grade names its inputs, as b291's does:
    | ### CM Lemma 3.1 for CONDITION ONE and for the evenness; b211's (C3) chain on I8 + I6 + I10 for
    | ### CONDITION TWO's eigenrelation, at b211's own banked grade. ### **NO HIGHER. ### AND THE
    | ### AMBIENT'S SQUARE-INTEGRABILITY TRAVELS WITH IT AS `W-ORD-PHI-MU-L2`.**
    | 
    | ### ### **AND THE FINDING INSIDE THE VERDICT, WHICH IS SHARPER THAN THE VERDICT:**
    | ### ### ### **THE DERIVATION LANDS IN CC's OWN DEFINITION 4.4 AND NEEDS NEITHER CM COROLLARY 3.2
    | ### ### ### NOR `W-ORD-WSA-PSI-LINK`.**
    | ### The obvious route was the import: ### **CM Cor 3.2 ASSERTS the membership** ### for negative
    | ### `mu`, with RRT Lemma 2(iii) independently. ### **THAT ROUTE ASSERTS; IT DOES NOT TEST THE
    | ### CONDITIONS.** ### And its "Sonin space" is CM's ### ORTHOGONAL-COMPLEMENT ### description,
    | ### which reaches CC's `S(1,1)` only through b287's `(SAME SPACE)` -- itself only ### **to within

### ==== ### b319's RESIDUAL TABLE -- BOTH CUTS, EIGHT FRAMES. ### EMITTER: b319
###      data/b319_the_stable_rank.txt | lines 295-313
    | ### ### **THE UNIT'S MEMBERSHIP RESIDUAL, ON BOTH CUTS, AT THE SAME FRAMES.**
    | ### ### ### **A MEASUREMENT AND NOT A VERDICT. ### THE DECISION STAYS THE MEMBERSHIP ACT'S.**
    | ###
    | ###   N       X      residual stable   residual grid
    | ###   1024    8      0.7973            0.9963
    | ###   2048    16     0.6450            0.9460
    | ###   2048    32     0.4395            0.8084
    | ###   4096    32     0.4395            0.8081
    | ###   8192    32     0.4395            0.7880
    | ###   8192    64     0.2861            0.6031
    | ###   16384   32     0.4395            0.7880
    | ###   16384   128    0.1975            0.3998
    | ###
    | ### ### **TWO THINGS ARE WORTH SAYING AND NEITHER IS A VERDICT.** ### The residual is markedly
    | ### LOWER on the stable cut at every frame -- which follows from the subspace being larger and is not
    | ### independent evidence of anything. ### **AND ON THE GRID AXIS IT IS CONSTANT AT `0.4395` ACROSS
    | ### ### ALL FOUR REFINEMENTS**, where the grid cut drifts `0.8084, 0.8081, 0.7880, 0.7880`. ### **THE
    | ### ### MEASUREMENT NOW HOLDS STILL. ### IT IS STILL NOWHERE NEAR ZERO.**
    | ### ### b316 reported `0.9455, 0.8023, 0.5527, 0.6033, 0.4902` across ITS OWN five truncations, which

### ==== ### b321's INSTRUMENT LADDER -- THE RATE THIS ACT COMPARES AGAINST. ### EMITTER: b321
###      data/b321_the_window_opened.txt | lines 12-24
    | ### ### ### **(1) THE IDENTITY CONTROL HOLDS.** ### Theorem 4.7 / (83) is an ### **EQUALITY**, not
    | ### an inequality: `Tr(theta(f) S) = W_infinity(f) + INT f(rho^-1) eps(rho) d*rho`. ### By cyclicity
    | ### that makes b320's margin exactly minus the remainder integral. ### Measured:
    | ###   ### `a = 1.30` : ### **`- INT f eps = 0.158889558`**
    | ###   ### `a = 1.35` : ### **`- INT f eps = 0.186481766`**
    | ###   ### `a = 1.41` : ### **`- INT f eps = 0.221284108`**
    | ### ### **AND THE INSTRUMENT WALKS TOWARD EACH OF THEM.** ### The residual between the measured
    | ### margin and the theorem's value, along the domain ladder at `a = 1.3`, is
    | ###   ### **`0.896557, 0.306328, 0.112555, 0.047182, 0.023224`** ###
    | ### -- falling by a factor of roughly two to three at every step, at all three cells. ### **THAT IS
    | ### ### THE FINITE INSTRUMENT CONVERGING TO AN EXACT STATEMENT OF THE SOURCE'S**, and it is the
    | ### strongest thing this arc has measured about the instrument.
    | ### ### ### **(2) BUT IT DOES NOT CLOSE THE EXPONENT QUESTION, AND THE ORDER'S CONDITIONAL IS

  ### ### **CORPUS FRAGMENTS NOT PRESENT : 0**

----------------------------------------------------------------------------------------------------
### (C) THE CODE FRAGMENTS. ### **THE INSTRUMENT'S REALIZATION, UNFOLDED.**
----------------------------------------------------------------------------------------------------

### ==== ### THE INSTRUMENT REALIZATION -- b316 `sonin_unit`, WHOLE
###      tools/b316_instrument.py | lines 149-181
    | def sonin_unit(fr, dps=25, ncoef=60, nsteps_per_unit=40, order=12, mu_str=None):
    |     """### **b300's `u_inf`, ON THIS GRID, FROM THE CORPUS'S OWN SOLVER.**
    | 
    |     ### `u_inf` is `phi_mu` at the first even NEGATIVE eigenvalue, normalized in `L^2`: the unique
    |     ### even solution that is ZERO on `[-1,1]` and agrees with the radial solution beyond it.
    |     ### **CONDITION ONE IS THEREFORE EXACT BY CONSTRUCTION AND CONDITION TWO IS THE ONE TO
    |     ### ### MEASURE.**
    |     ### The Frobenius series is valid to about `x = 2.5`; beyond that the corpus's own marcher
    |     ### carries it, and both come from `b205_prolate.py` rather than from anything written here.
    |     """
    |     from mpmath import mp, mpf
    |     import b205_prolate as P
    |     mp.dps = dps
    |     mu, tau = mpf(mu_str or MU_SONIN), 2 * mp.pi
    |     XS = mpf('2.5')
    |     out = np.zeros(fr.N)
    |     inner = [j for j in range(fr.N) if 1.0 < fr.x[j] <= float(XS)]
    |     for j in inner:
    |         y, _dy = P.yI_eval(mu, tau, mpf(float(fr.x[j])), ncoef)
    |         out[j] = float(y)
    |     y, dy = P.yI_eval(mu, tau, XS, ncoef)
    |     cur = XS
    |     for j in range(fr.N):
    |         if fr.x[j] <= float(XS):
    |             continue
    |         tgt = mpf(float(fr.x[j]))
    |         ns = max(2, int(nsteps_per_unit * float(tgt - cur)) + 2)
    |         y, dy = P.integrate_in(mu, tau, cur, tgt, y, dy, ns, order)
    |         cur = tgt
    |         out[j] = float(y)
    |     n = fr.norm(out)
    |     return (out / n) if n > 0 else out
    | 

### ==== ### THE SOURCE'S TRANSFORM AND INNER PRODUCT, AS THE FRAME HOLDS THEM
###      tools/b316_instrument.py | lines 70-92
    |         self.NY = int(NY) if NY else max(64, self.N // 8)
    |         self.hy = BETA / self.NY
    |         self.y = (np.arange(self.NY) + 0.5) * self.hy
    | 
    |     # ---------------------------------------------------------------- (N1)
    |     def inner(self, u, v):
    |         """### `<u|v>` OF eq. (16), ON THE HALF LINE."""
    |         return float(np.dot(self.w * u, v))
    | 
    |     def norm(self, u):
    |         return math.sqrt(max(self.inner(u, u), 0.0))
    | 
    |     # ---------------------------------------------------------------- (N2)
    |     def transform_matrix(self):
    |         """### eq. (24) FOR AN EVEN FUNCTION: ### `2 INT_0^inf xi(x) cos(2 pi x y) dx`."""
    |         return 2.0 * np.cos(2.0 * math.pi * np.outer(self.y, self.x)) * self.w[None, :]
    | 
    |     # ---------------------------------------------------------------- (N4)
    |     def masks(self):
    |         """### THE TWO CONDITIONS AS INDEX SETS. ### `lo_x` is forced to zero; EVERY `y` row
    |         ### constrains, because the `y` grid is built inside `[0, BETA]` and nowhere else."""
    |         return self.x <= ALPHA, self.y <= BETA
    | 

### ==== ### THE TWO CONDITIONS AS INDEX SETS, AND THE PROJECTOR
###      tools/b316_instrument.py | lines 93-124
    |     def subspace(self, T=None):
    |         """### **THE CONSTRAINED SUBSPACE, AS A PROJECTOR ON THE FREE COORDINATES.**
    | 
    |         ### Condition one kills the coordinates with `x <= 1` outright. ### Condition two is
    |         ### `C f = 0` with `C = T[lo_y][:, hi_x]`, and the space is that null space.
    |         ### **THE PROJECTOR IS BUILT FROM AN ORTHONORMAL BASIS OF THE ROW SPACE, NOT OF THE NULL
    |         ### SPACE**, because the row space has the small dimension and the arithmetic is cheaper
    |         ### and better conditioned that way.
    |         """
    |         if T is None:
    |             T = self.transform_matrix()
    |         lo_x, lo_y = self.masks()
    |         hi_x = ~lo_x
    |         C = T[lo_y][:, hi_x]
    |         # ### `Q` spans the ROW space of `C`; the space is what `Q` misses.
    |         U, s, Vt = np.linalg.svd(C, full_matrices=False)
    |         tol = max(C.shape) * (s[0] if s.size else 0.0) * np.finfo(float).eps
    |         rank = int((s > tol).sum())
    |         Q = Vt[:rank].T                       # ### (free coords) x rank, orthonormal columns
    |         return dict(hi=hi_x, lo_x=lo_x, lo_y=lo_y, C=C, Q=Q, rank=rank,
    |                     free=int(hi_x.sum()), dim=int(hi_x.sum()) - rank, sing=s)
    | 
    |     def project(self, sub, f):
    |         """### `S f` -- the component of `f` INSIDE the space (free coordinates only)."""
    |         g = f[sub['hi']].copy()
    |         return g - sub['Q'] @ (sub['Q'].T @ g)
    | 
    |     def outside(self, sub, f):
    |         """### `(1 - S) f` -- the component that is NOT in the space."""
    |         g = f[sub['hi']]
    |         return sub['Q'] @ (sub['Q'].T @ g)
    | 

### ==== ### THE EDGE DIAGNOSTIC -- b316 `taper`
###      tools/b316_instrument.py | lines 183-195
    | def taper(fr, u, frac=8.0):
    |     """### **THE EDGE DIAGNOSTIC.** ### Replace the hard cut at `X` with a raised-cosine over the
    |     ### last `1/frac` of the domain. ### **IF THE VIOLATION OF CONDITION TWO IS THE STEP AT THE END
    |     ### OF THE DOMAIN, THIS MOVES IT; IF IT IS THE VECTOR, THIS DOES NOT.** ### The tapered vector
    |     ### is NOT `u_inf` and is used for nothing but this comparison."""
    |     x0 = fr.X * (1.0 - 1.0 / float(frac))
    |     w = np.ones(fr.N)
    |     m = fr.x > x0
    |     w[m] = 0.5 * (1.0 + np.cos(math.pi * (fr.x[m] - x0) / (fr.X - x0)))
    |     v = u * w
    |     n = fr.norm(v)
    |     return (v / n) if n > 0 else v
    | 

### ==== ### AND THE DECAY DIAGNOSTIC -- b316 `asymptotics`
###      tools/b316_instrument.py | lines 197-220
    | def asymptotics(fr, u, x_lo=2.0):
    |     """### **WHAT `x u(x)` DOES FAR OUT.** ### b300's `u_inf` should go like `sin(2 pi x)/x`, so
    |     ### `x u` should be BOUNDED and should change sign twice per unit. ### Returns the bound, the
    |     ### sign-change count, and what `sin(2 pi x)` would give on the same interval.
    |     ### **THIS MEASURES THE DECAY AND THE FREQUENCY. ### IT DOES NOT MEASURE THE EIGENVALUE, AND
    |     ### THE CALLER IS OBLIGED TO SAY SO.**"""
    |     m = fr.x > float(x_lo)
    |     xu = fr.x[m] * u[m]
    |     sgn = np.sign(xu)
    |     sgn = sgn[sgn != 0]
    |     changes = int(np.sum(sgn[1:] != sgn[:-1]))
    |     # ### the zeros of `sin(2 pi x)` sit at the half-integers; count the ones the NODES
    |     # ### actually straddle, not the ones the nominal domain would contain.
    |     expect = int(math.floor(2.0 * float(fr.x[m][-1]))) - int(math.floor(2.0 * float(x_lo)))
    |     return (float(np.max(xu)), float(np.min(xu)), changes, expect, int(np.sum(m)))
    | 
    | 
    | def far_bound(fr, u, x_lo=8.0):
    |     """### **THE DISCRIMINATION ARM FOR `asymptotics`**: the same bound measured deeper out, so it
    |     ### can be compared across values of `mu`. ### **IF NON-EIGENVALUES GIVE THE SAME NUMBER, THE
    |     ### CONTROL CANNOT FIRE AND MUST BE REPORTED AS NOT-A-CHECK** (b308's law)."""
    |     m = fr.x > float(x_lo)
    |     return float(np.max(np.abs(fr.x[m] * u[m])))
    | 

### ==== ### THE SPACE AS THE STABLE-RANK SCHEME SELECTS IT -- b319
###      tools/b319_stable.py | lines 71-96
    | BLOCK = 512
    | 
    | 
    | def stable_subspace(fr, tau=TAU, T=None):
    |     """### ### **THE SUBSPACE BY THE EIGENVALUE-ONE CHARACTERIZATION.**
    | 
    |     ### Returns the same dict shape `b316_instrument.subspace` returns -- `hi`, `lo_x`, `lo_y`, `Q`,
    |     ### `rank`, `free`, `dim` -- so ### **b317's `compressed_trace` AND b318's `square_trace` RUN
    |     ### ### AGAINST IT UNCHANGED**, and the reproduction compares two subspaces rather than two
    |     ### programs.
    |     ### It adds `eig` (the sandwich's non-one eigenvalues, ascending) and `tau`.
    |     """
    |     if T is None:
    |         T = fr.transform_matrix()
    |     lo_x, lo_y = fr.masks()
    |     hi_x = ~lo_x
    |     C = T[lo_y][:, hi_x]
    |     scale = math.sqrt(fr.hy / fr.h)
    |     U, s, Vt = np.linalg.svd(C * scale, full_matrices=False)
    |     eig = s ** 2
    |     out = eig > tau                      # ### OUTSIDE the eigenvalue-one eigenspace
    |     Q = Vt[out].T
    |     return dict(hi=hi_x, lo_x=lo_x, lo_y=lo_y, C=C, Q=Q, rank=int(out.sum()),
    |                 free=int(hi_x.sum()), dim=int(hi_x.sum()) - int(out.sum()),
    |                 eig=eig, tau=tau, sing=s)
    | 

### ==== ### THE BASE OBJECT -- the corpus radial solver both realizations bottom out in
###      tools/e16/b205_prolate.py | lines 1-22
    | # -*- coding: utf-8 -*-
    | """b205 -- the beta-dichotomy of RRT 2025, implemented from the ODE.
    | 
    |   D_tau - mu :   (x^2 - 1) f'' + 2 x f' + (tau^2 x^2 - mu) f = 0
    | 
    |   psi   : the solution asymptotic to -sin(tau x)/x at +infinity
    |   y_I   : the analytic local solution at x = 1 with y_I(1) = 1
    |   beta  : (x^2 - 1) * W(psi, y_I) at x_0 = sqrt(2);  its zeros are the eigenvalues
    |   alpha : at an eigenvalue beta = 0, so psi = alpha * y_I and alpha = psi(x_0)/y_I(x_0)
    | 
    |   EVERY RESOLUTION AXIS IS AN ARGUMENT AND IS QUOTED WITH EVERY NUMBER.
    | """
    | import sys
    | from mpmath import mp, mpf, mpc, sin, cos, exp, sqrt, pi, matrix
    | 
    | # ----------------------------------------------------------------- y_I at x = 1
    | def yI_series(mu, tau, ncoef):
    |     """Frobenius series for the analytic solution at x = 1, y_I(1) = 1.
    |        t = x - 1;  (t^2+2t) f'' + (2t+2) f' + (tau^2 (t+1)^2 - mu) f = 0.
    |        Coefficients a_n of f = sum a_n t^n, a_0 = 1."""
    |     T2 = tau * tau
    |     a = [mp.one] + [mp.zero] * (ncoef + 4)

  ### ### **CODE FRAGMENTS NOT PRESENT : 0**

  ### **AND THE ONE THING THIS FILE EXISTS TO PUT SIDE BY SIDE:** ### CM Lemma 3.1 defines
  ### `phi_mu` on the WHOLE half-line -- *"zero on `[-1,1]` and agrees with `f_mu(x)` for
  ### `x > 1`"* -- and `b316.sonin_unit` builds it on `[0, X]` for a FINITE `X`.
  ### **WHETHER THAT IS THE DIFFERENCE IS COMPONENT 1'S TO DECIDE FROM NUMBERS**, and
  ### this file only makes the two statements readable in one place.
====================================================================================================
