CONCURRENCY: SOLO (research seat).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE REMAINDER — is the corpus's archimedean remainder function
the source's, decided by definitions, one act. Scope: READS at
owners and at the source, and a DERIVATION at definitions; the
identification is the whole content and a shared letter, a
shared shape, or a shared position in a formula is refused as
evidence by name — only unfolded definitions decide; NO
archimedean number computed; NO aggregation stated, M-2 owed
under its cap; NO claim about the imbalance's cause is made
unless the identification lands, and then only as an
entailment at its scope; normalizations checked before
anything else, since the last two sealed predictions failed
there; every reported number from a committed tool with
fixtures; needles pulled from emitting files; the source's
artefact checked against the corpus's pin before a word is
read; stem sweep at extended scope; hedge audit; registration
term-scanned, satisfiability-checked, sealed, no counts
predicted; the suite re-run after the push; nothing about the
h2 identity beyond the register sentence exact; nothing
deposits.

STEP ZERO: the ferry scan; the ledger census with its scope;
push anything ahead of origin, saying so if nothing is.

COMPONENT 1 — THE TWO DEFINITIONS, side by side, each unfolded
to its base objects: the corpus's remainder as its owner
builds it (the supplied equation the corpus imported, its
normalization, its domain, the mode expansion it is computed
through, and the identity the corpus's left side decomposes
under — trace equals archimedean term plus this remainder
plus a residue); the source's remainder as its Theorem 4.7
defines it (the trace of the smeared compression equals the
archimedean Weil distribution plus the integral of the test
function against this function in the multiplicative
measure), with its normalization, domain, and the jump at the
identity. Neither described in the other's language.

COMPONENT 2 — THE DECISION: (SAME FUNCTION, derived — the two
unfold to one object with normalizations reconciled
explicitly, or shown equal up to a stated scalar with that
scalar's irrelevance derived) / (DIFFERENT, the first
differing constituent quoted at full prominence) / (UNDECIDED,
the exact missing statement named and typed). The archimedean
term's sign convention is checked against the corpus's banked
atlas rather than assumed.

COMPONENT 3 — THE ENTAILMENT, only on SAME and at its exact
scope: the corpus's finite-instance identity restated as the
source's Theorem 4.7 with the finite places attached, using
b310's result that the finite places contribute only their
dimension through this class; what that makes the open
clause's finite-instance form, in the identity's own terms;
and the weight-and-eigenvalue juxtaposition re-examined as a
question about where the remainder's mass sits relative to the
object's space — as a question, promoted to nothing. On
DIFFERENT or UNDECIDED: the corpus's remainder is its own
object and the entailment does not run.

CLOSING: the import ledger's rows; the correspondence rows by
the idempotent tool, read back; every result keyed; the
object's three conditions restated; M-2's row unchanged under
its cap; the seam's debt item 1 restated; the patent clock
restated; the window ruling recorded and unapplied; the
in-flight register; the ledger census; the fold (b307–b312)
with a fresh-clone certification test named as the next act.
The navigator's expectation registered here: SAME FUNCTION
up to the normalization the corpus records by a factor;
refutable by a differing constituent. Registration gate;
index queried; the-remainder keyed. Components ordered. THE
SHADOW: expected nothing — reads and a definitional decision;
say so.

Bank: data/b312_the_remainder.txt. Deviation rule standing.

EXECUTION: ferry scan first; owners and the source read at
content first, page image where the text layer fails, the
artefact checked against its pin; registration
satisfiability-checked, term-scanned, sealed; components in
order; full control suite, re-run after the push; pins by
ls-remote across all three repos; the hook if PLACE-papers is
touched; mirror if it moves. STOP.

FOOT: after this act the corpus knows whether the remainder it
has computed for a month is the source's, and if so, what its
own identity says in the source's vocabulary; h2 stands
exactly where the deposit left it; locks last.

paste ends (part 1 of 1)
