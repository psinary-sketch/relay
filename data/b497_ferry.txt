RULING (R108), the author's, ratified by this paste and strikeable

(1) THE GRADE CELL MAY BE SPLIT. Where a terminal is a biconditional or a conjunction and its
parts grade differently, the cell reads two grades each with its part named, in the form
DERIVES(->) / INTERFACES(<-) or DERIVES(part) / ENCODES(part). The four grades are unchanged;
this is a cell form, not a fifth grade. The table's matcher reads the form; no ledger line is
rewritten, and improvised prose splits are re-celled as they are met, by appended note.
(2) e_difficulty is routed to a statement-read act of its own, later, its three cells quoted;
this act does not choose.
(3) The navigator's counts are read from files, not recalled: the federation is 44 SIDE-*
repositories with .git as K0 read them; the keystone class is 16 under the census's test, 87 with
Day-1's nine and the 62 candidates; the terminal population is 1,021 rows. Earlier figures in this
window ("ninth kernel", "sixteen keystones" without its class) are corrected here.

FERRY -> CLAUDE CODE - paste begins (part 1 of 1; confirm receipt-in-full)

ACT b497 - THE b495 BUILD LOG READ, AND THE THREE PROFILES. The face declares
terminal_table_run.txt and terminal_table_diff.json in its write list, so the post-push suite can
pass on its own terms.

Component 0 - the run's state. Print whether pid 7860 is alive and the log's byte count. If alive:
print ALIVE, the elapsed time, the module mark most recently written, and STOP - this act closes as
a state report, the log unread, and re-issues itself when the process has exited. Nothing below
runs while the process owns the log.

Component 1 - the log, whole. Commit it to relay unchanged. Print: modules built; === EXIT lines
and how many non-zero; modules that failed, by name, or NONE; wall time from the log's own outer
marks against lake's summed durations; and the count of distinct instants across all marks - the
cell (N3) and (S2) of b495 are owed.

Component 2 - the profiles. The three #print axioms lines for EF_lit_zetaZeroConfig, EF_lit and
EF_lit_zeta, matched on the whole axiom string. Each STANDARD THREE, OTHER with the string printed,
or ABSENT. (R82) holds if all three read the standard three; otherwise print which and STOP before
Component 3.

Component 3 - the row. REGISTRY's SIDE-explicit-formula row: profile PENDING -> the three names
with their profile; the terminal table regenerated and the diff printed, the three rows expected to
move from NOT PROFILED. No keystone cites the kernel in this act.

The navigator's expectations, scored when Component 1 runs: (N1) 0 non-zero exits; (N2) all three
profiles the standard three; (N3) more than fifty distinct instants across the marks. Each
refutable by a printed cell. Nothing at Zenodo written; nothing deposits; h2 where the deposit left
it.

paste ends (part 1 of 1)
