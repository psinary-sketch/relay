====================================================================================================
b43 REGISTRATION - THE BALL AT CC'S SCALE. NO MEASURED NUMBER IN THIS BLOCK.
====================================================================================================
W-CONSTRUCTION-1 act 15. Relay-only. ON THE FACE: one definitional candidate tested; no
identity decided at complete roster; the register moves only via the protocol. Both
branches carry: the real place is an approximation scheme BY THEOREM (act 14), so
CONVERGENCE, not restriction, is what is claimed under any outcome. NO SIGN SENTENCE AT
COMPLETE ROSTER. h2 UNCHANGED. NOTHING DEPOSITS. NOTHING CIRCULATES.

ITEM 1 - THE EXACT RELATION, DERIVED FIRST (the deliverable, written before any number):
 R1  CC's S(lambda,lambda) (Def 4.4 at content) = (E + B)^perp in L^2_even: E = the
     time-ball-supported subspace, B = the transform-ball-supported one.
 R2  THE PAIR STRUCTURE at bandwidth c: per even-prolate mode, chi_E = xi_n (the
     ball-supported prolate) and chi_B = eta_n = F xi_n; the Gram <xi_n, eta_n> is the
     SIGNED lambda(n) (P1's convention; derived via P_E xi^an = xi, the restriction of
     the entire extension); F SWAPS the pair (F xi = eta, F eta = xi) - so the
     +1-sector constraint vectors are v_n^+ = (xi_n + eta_n)/sqrt(2(1+lambda_n)) and
     the E+B block inverses carry 1/(1 - lambda_n^2):
     ### THE eps-SERIES' DENOMINATORS ARE THE lambda-SCALE SONIN GEOMETRY - derived,
     closing the loop the Sonin-subtraction candidate pointed at: the regularized
     lambda-scale Sonin trace IS the object Thm 4.7 computes (W_inf^CC + E2, the
     corpus's b37/b39 substitution); the acts-13/14 BAND ladder was the E-BASIS
     positive form - a different object - and the anchor floor measured the gap
     between THAT and the Theorem-1 bound.
 R3  THE BALL-CORRECTED E_1 FORM at the bench (the S(lambda,lambda)-conditioned
     positive form): subtract the +1-sector constraint span from the band form:
        corrected_n = tr_n - <v_n^+, theta(g) v_n^+>
                    = (lambda_n tr_n - x_n) / (1 + lambda_n),
     x_n = <xi_n, theta(g) eta_n> (the cross term; eta evaluated by the c-kernel
     transform on a fine grid). STRUCTURAL SANITY, derived: at g = delta the corrected
     integrand vanishes per mode ((lambda*1 - lambda)/(1+lambda) = 0) - the
     subtraction exactly annihilates the constraint directions.
 R4  THE FINITE SHADOW, already banked: dim Son_p = (p^n - 1)^2 = p^{2n} - 2 p^n + 1
     IS the inclusion-exclusion of the SAME pair structure (E, B of dim p^n each,
     rank-1 intersection - b10's L-A). The vanilla leg compiles the instances.

ITEM 2 - THE INSTRUMENT:
  Cells a^2 in {2, 3, 4} (stable) + {8, 9, 12} (attempted; the act-13/14 stability
  protocol governs and may rule them indeterminate). Per cell at c(a) = pi a^2 (D3):
  the c-layer; per E_1 mode (even CC index, converged truncation NMODE = 8; NMODE = 11
  reported for the tail caveat): tr_n (the b41 machinery), x_n (the cross machinery),
  gram_n = B_n(1) MEASURED.
  GATES: (G1) the gram gate - |gram_n - signed lambda_n| per mode (the DERIVATION'S OWN
  CHECK: R2's Gram claim tested numerically; fail -> the derivation or pipeline is
  wrong, the run says which if it can); (G2) the b42 gates (anchor battery, Shannon);
  (G3) the F-commutation check at one cell (tr_n^B vs tr_n, derived equal on Weil-even
  data). VOID on G2; G1/G3 failures FLAG the derivation and stop the verdict.
  RECORDED: margin' = (-A) - Sum corrected_n and D_cov' = (-A - PR) - (Sum corrected +
  Thq), NQ triple {500, 700, 900}.

REGISTERED BRANCHES, longhand:
  (BALL-closes): the anchor floor DIES - |margin'| < 0.01 at the anchor, stable, and
     no cell stably below -0.01: the last archimedean absence was one definitional
     term; the covariant object at the model is Weil's up to the dying finite term - a
     FINITE-INSTANCE statement with the range law; the closure protocol's STEP ONE is
     triggered FOR THAT STATEMENT ALONE (the formal Lean statement, the classical
     profile, its owners).
  (BALL-persists): a stable floor at a stated value - the next name required; the
     candidate retired, its two signatures (size ~0.13; N-independence) explained
     otherwise or left open, honestly.
  (BALL-third): mixed/unstable/gate-flagged - named per cell.
  NO PREDICTION REGISTERED THIS TIME: the derivation gives the sign of the correction
  (the subtraction reduces the trace) but not its size against the 0.1254 floor; the
  run decides.

ROUTING (Rule 4): registration + instrument by Fable; the banked run to the OPUS lane
if available. Outcome: THE OPUS LANE WAS AVAILABLE - the banked run executed on Opus, first attempt
(6 PASS: the carto far-ends, the anchor battery, and the three stable cells' gram
gates; 3 FLAGs: the high-c cells' gram gates, which with their instability rules them
indeterminate per the registration); no fixes to any registered quantity during debug.
The gram gate CONFIRMED the derived pair structure at 1e-5 on the stable cells; the
corrected trace collapsed to ~0; verdict (BALL-third) with the mechanism resolved -
written in the act report.
====================================================================================================
