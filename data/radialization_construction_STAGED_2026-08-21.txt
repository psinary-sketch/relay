====================================================================================================
THE RADIALIZATION -- THE MERGED CONSTRUCTION REQUEST. STAGED 2026-08-21 (the
coherence act, component 4). NOT SCHEDULED; the author schedules. NOTHING IS BUILT
BY THIS FILE.
====================================================================================================

THE MERGE VERDICT (b71, P4): step A of the theta bridge (b70's named missing step:
the corpus's own chart -> theta link) and N5's radial-compression dictionary (b70's
named residue: the dictionary between the chart transfer's exit-space and the shell
model's (1,1)) are THE SAME REQUESTED OBJECT at the requested-construction level.
Both request:

  THE SHELL-SUMMING MAP FROM THE CHART/g-FAMILY TO THE SHELL MODEL
  (the radialization): the map that carries a chart function to its shell sums --
  the g-family/chart data summed along the norm shells -- landing in the shell
  model's space.

THE TWO CLIENTS (what each open would DO with the map -- the difference that
survives the merge, stated so nothing is smoothed):
  (client A, the theta bridge): the radialization applied to the chart's parity
  data would produce the theta-like series whose functional-equation-shaped
  symmetry steps B-C then carry (the federation's proved half:
  completedRiemannZeta_0(1 - s) = completedRiemannZeta_0(s) at the classical
  profile) -- the bridge's step A discharged BY the construction if it lands.
  (client B, N5's dictionary): the radialization applied to the chart transfer's
  exit-space (the row-q/2 even-t family; the center's class at every level) would
  compress it onto the shell model's (1,1) -- the dictionary's two sides matched
  BY the construction if it lands.

THE MERGED OPEN REPLACES THE TWO: W-CONSTR-RADIALIZATION (this file its home).
The prior two names retire INTO it -- "step A" and "the radial-compression
dictionary" are its two clients, not separate requests. Success criterion for a
future run: the map defined on the banked cells at exact grade; the two clients'
readings decided or named-open per branch.

Prerequisite ground, at pins: rows 45-46 (the colimit; the scaling/transfer), row
41 (the shell model's (1,1); the boundary index), b70's exit-set data, the b58
g-family. Range law: the banked cells and the constructed limit only; the
archimedean side enters only through the bridge's OWN gates (S1; H-COH or its
replacement); nothing about h2.
====================================================================================================
