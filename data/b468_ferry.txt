FERRY -> CLAUDE CODE - paste begins (part 2 of 2; confirm
receipt-in-full)

ACT b468 - THE PROPORTION RESULT READ AT ADDRESS, WITH THE
SOURCES PRESENT. Runs only when the (R75) artefacts are on
disk; prints ABSENT per artefact otherwise and stops.

COMPONENT 1 - THE STATEMENT. Quote the main theorem of the
Claude paper and of arXiv 2608.13637 verbatim; print whether
they agree and where the 2/3 and 0.6725 constants each come
from, in the papers' words. Quote the support condition and
the window. Then the three reads under the record's rules:
(a) the import bar - class against the corpus's objects at
support, verdict K1 or not, the deciding sentence quoted;
(b) row U1 - at site (ii), is the theorem uniform in T, and
at what support; at sites (v) and (vi), does the Dirichlet
extension's class contain the corpus's second object, and
at what support; verdicts per site, NONE where none; (c)
the census - section 24.4's analytic row and the intake
item owed since 2026-08-20, each drafted as an era-
annotation candidate, NOT applied, authoring routed. Quote
the paper's own ceiling sentence beside the corpus's
residue sentence at its FINDINGS address, and type nothing
between them.

COMPONENT 2 - THE LEAN, READ NOT TRUSTED. In formal-math at
its SHA, project zeta23: print the Mathlib pin; list every
declaration in the trusted statement files (Challenge.lean
and Challenge/*.lean) that carries sorry or is an axiom or
hypothesis - these are the inputs the formalization takes
on trust - each with its statement verbatim; run #print
axioms on the main theorem in Solution.lean and print it.
Grade the formalization in the corpus's own vocabulary:
DERIVES if the trusted files carry no analytic input;
INTERFACES on named premises otherwise, the premises listed.
Then the one comparison b466 re-aimed: does any declared
statement match or contain the second conjunct of
ExplicitFormulaDecomp - lam n = sum over low of blTerm plus
tail - quoted from lv v0.10.0 at 93c27ec beside it?
MATCHES / CONTAINS WITH A NAMED SPECIALIZATION / DOES NOT.
File W-ORD-GW-IMPORT only on MATCHES or CONTAINS. Nothing is
imported; no Lean of the corpus is touched; the kernel lane
opens for this component and closes at the act's end.

CLOSING as the standing pattern, both tables in the closing
message itself; the span by tool; the four lists OPEN. The
navigator's expectations: (N1) the two statements agree and
the theorem counts simple zeros on the line, unconditionally;
(N2) K1 at every site - the support restriction excludes the
corpus's objects, and (R61)'s trigger does not fire; (N3) the
trusted statement files carry the pair-correlation input as
a hypothesis, so the formalization grades INTERFACES, and the
explicit-formula comparison is DOES NOT. Each refutable by a
printed result. h2 where the deposit left it.

paste ends (part 2 of 2)
