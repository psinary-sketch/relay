CONCURRENCY: SOLO (research seat; every read through the
extract-to-disk step).

FERRY → CLAUDE CODE — paste begins (part 1 of 1; confirm
receipt-in-full)

THE ARCHIMEDEAN TERM DERIVED — the softest constituent of the
stated clause lifted from definition-only to derivation-on-
import, and the constituents re-ranked, one act. Scope: a
DERIVATION under the import bar; NO grade conferred by this
act — the constituent's new grade is the derivation's own and
is stated as such; NO change to the stated clause's text (an
append-only grade update through the ledger's writer and a
findings addendum); NO claim about the quantifier, which stays
unowned; normalizations before counts; every number by two
routes sharing no code; registration sealed before any write,
no counts predicted; needles from the extract file; stem sweep
at extended scope; hedge audit; the suite re-run after the
push; the hook and mirror if the papers repo moves; nothing
deposits.

STEP ZERO: the ferry scan; both censuses; push anything ahead
of origin, saying so if nothing is.

COMPONENT 1 — THE CLASSICAL TERM, quoted: the archimedean side
of the explicit formula for ζ as a real source states it — the
Gamma factor with its power of π, its logarithmic derivative,
and the transform of that against the test function, with the
principal value at the identity as the source handles it —
located under the import bar with an identifier and pinned by
hash; hypotheses checked against the corpus's transform and
measure conventions, each quoted once.

COMPONENT 2 — THE DERIVATION: from Component 1 to the source's
archimedean formula (its equation quoted) and to the corpus's
digamma integral (the atlas's line quoted), every step a
quotation or an elementary identity, with the sign chain the
record settled quoted at each link and the factor-of-two
hazard of the prior negative-control act named and checked.
Verdicts: (DERIVES-ON-IMPORT, the imports named) / (RESISTS,
the step named) / (MISMATCH, at full prominence, the differing
constituent quoted — since a mismatch here would touch every
archimedean number in the record and the act must say so
before anything else).

COMPONENT 3 — THE CONTROL: the derived expression evaluated by
a third route sharing no code with the two the record has, at
the arc's cells, agreeing inside a bar sealed before any value;
a disagreement reported at full prominence.

COMPONENT 4 — THE RE-RANK: the stated clause's grade table
updated through the ledger's writer (the constituent's row
only), the ranking re-run under the sealed rule with nothing
adjusted, the new softest constituent named, and the aim-map
named as next with its target being that constituent —
whatever it is. Both seats' expectations for the new softest
registered before the re-rank runs.

CLOSING: the findings addendum appended to the clause anchor
(originals untouched); the correspondence row; every result
keyed; M-2's row unchanged under its cap; the seam's debt item
1 restated; the patent clock restated; both censuses.
Registration gate; index queried; archimedean-term-derived
keyed. Components ordered. THE SHADOW: expected nothing — a
derivation under the import bar with analysis in it; say so.

Bank: data/b333_the_archimedean_term_derived.txt. Deviation
rule standing.

EXECUTION: ferry scan first; registration sealed before any
write; the extract step for every read, the source pinned by
hash before a word is read; components in order; full control
suite, re-run after the push; pins by ls-remote across all
three repos. STOP.

FOOT: after this act the archimedean distribution is derived
from the classical term or the mismatch is on the record at
full prominence, the clause's softest constituent is named by
a re-run rule and not by a seat, and the aim-map has its true
target; h2 stands exactly where the deposit left it; locks
last.

paste ends (part 1 of 1)
